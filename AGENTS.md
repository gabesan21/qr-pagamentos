# QR Pagamentos - agent instructions

> Project managed by the **ProjectOfProjects (PoP)** workflow. `CLAUDE.md` is a symlink to this file; always edit this file.

- **Type:** included - see [[TYPES|TYPES]].
- **Project language:** English - project-specific specs, notes, research, code comments, and kanban artifacts use English.
- **Vendored PoP language:** copied core workflow files and skills remain in upstream pt-BR; do not translate the shared source during project work.
- **Supported application languages (i18n):** Brazilian Portuguese (`pt-BR`) and English (`en`).
- **Project brief:** [[PROJECT|PROJECT]] - read when the task needs product purpose or harness decisions.
- **Roadmap:** [[ROADMAP|ROADMAP]] - read when selecting or sequencing work.
- **Modifications:** [[MODIFICATIONS|MODIFICATIONS]] (created on demand) - read for hotfixes, adjustments and small emergent features outside the plan.

## Repository

| Repo | URL | Clone path | PR branch |
|------|-----|------------|-----------|
| qr-pagamentos | https://github.com/gabesan21/qr-pagamentos.git | repository root | main |

In yolo scopes, the orchestrator mechanically integrates task branches into `develop`; there is no PR per task. When the last task of the marked scope (single task, phase/epoch or modification) closes, it automatically opens the final `develop` -> `main` PR; the human tests and merges it.

## Workflow

Every change to the application passes through `kanban/001_initial_task` -> `kanban/006_done`, with tasks coming from the roadmap (`<n>.<m>.<t>-<slug>`) or from modifications (`M-<n>.<t>-<slug>` — work arriving outside the plan):

A change request without an active card triggers `new-task` -> `advance-task`; the absence of a card is workflow input, never permission to edit directly. “Start the flow in yolo” materializes and releases the task with `yolo: true`, records the conversational origin, and traverses the complete yolo route.

1. **001** - create the task with `new-task` and explicit `depends_on` prerequisites.
2. **002** - a planner separate from execution writes a concise brief with objective, strategy, fronts, dependencies, contracts, risks, and acceptance criteria.
3. **003** - obtain human approval. In yolo this gate **only exists for `critical: true`** (strong independent critic; two returns allowed before the third failure trips the circuit breaker); non-critical yolo tasks transit 002 -> 004 directly.
4. **004** - send a cohesive front directly to one executor; use an execution orchestrator only for a DAG, multiple skills, or multiple write sets.
5. **005** - one fresh independent critic reviews the delivery. In yolo this is the **single quality gate** (always strong): it first verifies that the original request (the card's objective) was met — the brief is strategy, not an approved contract — choosing differential or full verification; full is mandatory for critical tasks or after a return, and the third rejection trips the circuit breaker.
6. **006** - mechanically integrate yolo tasks into `develop`, write memory and telemetry summary, clean the task, and automatically open the final scope PR to `main`.

Up to three independent yolo tasks may advance as a wave. Parallel execution requires logical and write independence plus repository/worktree isolation; any collision serializes. A missing dependency is `BLOCKED` and is never implemented opportunistically.

One execution continues until the next real gate. The complete state machine is in [[WORKFLOW|WORKFLOW]].

A direct human command overrides only the rule or gate it explicitly names. “Apply”, “execute”, “urgent”, “until finished”, or “in yolo” do not waive the card, kanban, memory, specs, or DOX. Only an unequivocal instruction such as “without kanban” or “outside PoP” activates [[WORKFLOW|the explicit deviation protocol]], which still requires an identifiable memory and a recorded specs/DOX impact assessment; those contracts are edited only when affected.

## Context protocol

1. Start from the card and plan; read only what they link.
2. If context is missing, delegate a specific question instead of reading a directory broadly.
3. Stop searching when the affected behavior and paths are known.
4. Record unresolved uncertainty as `RECON NEEDED` or `blocked`; never guess.
5. Consult specs and memory before archaeology in Git history or code.

## Skills

- **PoP workflow:** `.agents/skills/` contains task/roadmap/spec skills, `weekly-review`, `optimize-memory`, and `yolo-critic` (the strong independent yolo critic).
- **Project operations:** `skills/` will contain reusable build, test, run, migration, and deployment procedures as they become real.

## DOX index

- [`src/app-shell/AGENTS.md`](src/app-shell/AGENTS.md) — follow when changing
  role-neutral shell composition, navigation state, or the mobile boundary.
- [`src/brand/AGENTS.md`](src/brand/AGENTS.md) — follow when changing canonical
  identity geometry, compositions, generated assets, manifest, or usage rules.
- [`src/components/ui/AGENTS.md`](src/components/ui/AGENTS.md) — follow when
  changing owned Radix/nova shadcn source, its inventory, or its state contract.
- [`src/integrations/nautt/AGENTS.md`](src/integrations/nautt/AGENTS.md) — follow when changing Nautt HTTP adapters or owner-bound provider orchestration.
- [`src/checkout/AGENTS.md`](src/checkout/AGENTS.md) — follow when changing sessionless public checkout reservation, replay, or capability issuance.
- [`src/media/AGENTS.md`](src/media/AGENTS.md) — follow when changing media validation, persistence, lifecycle, reconciliation, or serving.
- [`src/data-directory/AGENTS.md`](src/data-directory/AGENTS.md) — follow when changing bounded query/cursor contracts or reusable responsive directory states.
- [`container/AGENTS.md`](container/AGENTS.md) — follow when changing startup, media preflight/inventory, one-shot, or health wrappers.
- [`install/AGENTS.md`](install/AGENTS.md) — follow when changing install, update, retention, purge, backup, restore, or recovery operations.

### Clean code

- `clean-code-change` (`.agents/skills/`) — follow when **planning (002) and executing (004)** any task that creates or changes code.
- `clean-code-review` (`.agents/skills/`) — follow when **verifying (005)** a code task and as a reading criterion in plan or PR gates.
- **Mandatory:** in 002, every task that creates/changes code enters `clean-code-change` on the **004** row and `clean-code-review` on the **005** row of the card's **Skills por etapa** table.

### UI and frontend

- `ui-change` (`.agents/skills/`) — follow when **planning (002) and executing (004)** any task that designs or implements UI (screens, components, styles).
- `ui-review` (`.agents/skills/`) — follow when **verifying (005)** a UI task and as a reading criterion in frontend plan or PR gates.
- **Mandatory:** in 002, every UI task enters `ui-change` on the **004** row and `ui-review` on the **005** row of the card's **Skills por etapa** table.
- **Vendored support batch** (`.agents/skills/`, consulted by the two skills above per each one's trigger): `frontend-design`, `web-artifacts-builder`, `taste-skill`, `impeccable`, `react-best-practices`, `web-design-guidelines`, `skill-a11y-audit`, `color-expert`, `design-tokens`, `shadcn`, `ux-audit-rethink`, `nielsen-heuristics-audit`, `wcag-accessibility-audit`, `cognitive-walkthrough`, `ui-design-review`, `don-norman-principles-audit`.

#### Project verification

| Check | Command |
|-------|---------|
| Formatter | — (no `format`/`prettier` script in `package.json`) |
| Linter | `pnpm lint` (plus `pnpm typecheck`) |
| Tests | `pnpm test` |

Aggregate gate: `pnpm check` (lint + typecheck + test + build) — see Application contract below.

## Application contract

Username and password are the only login credentials; email is optional and never used for login.

- `src/app/` owns App Router pages and endpoints; localized UI routes are unprefixed and resolve only the persisted `pt-BR` or `en` preference, while `/api/health` and `/api/nautt/webhooks` stay unlocalized. The Nautt callback is sessionless and returns only empty, no-store protocol statuses; never reintroduce `/{locale}` UI or mutation routes. BETA(M-5.1): the callback currently accepts bodies WITHOUT HMAC signature verification (human beta decision 2026-07-25; `401` suspended, owner attributed via local provider-order UUID) — MUST be reversed before production; see `pop/open_questions/2026-07-25-pre-production-gate-restore-webhook-hmac.md`.
- `ADMIN` and merchant `USER` are mutually exclusive business personas: `/` must resolve role before owner work and dispatch `ADMIN` to `/admin`; merchant business routes stay unprefixed and `USER`-only, while panel routes under `/admin*` are `ADMIN`-only. Administrators never enter merchant routes or perform owner mutations, but approved administrator-only global order, payment-link, and analytics projections remain read-only, re-authorized, and redacted. `/admin-access` is the only retained unprefixed administrator probe (`204` active `ADMIN`, empty `401` when no active principal resolves—including missing, expired, revoked, or disabled sessions—and empty `403` only for an active `USER`); `/admin/users/[id]/nautt-credentials` is denial-only (empty `401` unauthenticated, empty `403` for either authenticated role) and must parse no input or perform service work. Shared authentication, locale, and visual sources grant no cross-role business capability; public/sessionless routes, exact V1 identifiers, projections, opaque errors, and `[lang]/*` `404` stubs remain outside both panels. See [`pop/specs/administrative-foundation.md`](pop/specs/administrative-foundation.md) before changing role routing or capability ownership.
- `src/orders/order-view.ts` owns the only server-only read projection through which order data (including the policy-exact `CustomerSnapshotV1`) leaves persistence; it exposes no verifiers, nonce, key material, provider data, or lifecycle fencing fields, and cross-owner, malformed, and missing order identities share one opaque "unavailable" outcome.
- `src/app/orders/` owns the read-only owner order list/detail views; every read re-resolves the cookie principal, scopes to the principal's own orders, and renders only opaque empty/unavailable states. `order-views.tsx` (V1) stays byte-frozen; `order-v2-views.tsx` owns the Commerce V2 list/detail facts, comment thread, and guarded local-outcome forms, and never renders currency-pair UUIDs, line product UUIDs, or internal lifecycle fields.
- `src/app/admin/orders/` owns the read-only administrator global order list/detail views over the same projection; they follow the `src/app/admin/` authorization contract and hold no mutation surface. It also owns the administrator-global Commerce V2 order directory composed above the byte-frozen V1 ledger: `src/orders/order-v2-admin-directory.ts` is the server-only `admin-order-v2` registration over the foundation's `queryAdministratorDirectory` with the `ADMIN_GLOBAL` scope purpose (page sizes 10/20/50/100, default 50, the owner directory's `(createdAt, id)` keyset and filter/search set with provider UUIDs match-only in `q`), rows reusing `toOrderV2Summary` plus only the additive owner `username`/`deletedAt` attribution — soft-deleted owners are never filtered and render a localized non-color deleted badge with the interim `/admin/accounts` owner navigation. `/admin/orders/v2/[id]` is the read-only V2 detail: delivered `getForAdmin` first, then one bounded owner-attribution read, one opaque unavailable outcome, and never the owner-only comment thread or local-outcome forms.
- `src/app/admin/orders/` owns the read-only administrator global order list/detail views over the same projection; they follow the `src/app/admin/` authorization contract and hold no mutation surface.
- `src/auth/payment-link-v2-admin-directory.ts` owns the administrator-global, V2-only, read-only Commerce V2 payment-link directory behind `/admin/payment-links` — the foundation's `queryAdministratorDirectory` with the `ADMIN_GLOBAL` scope purpose (page sizes 10/20/50/100, default 50, the `(createdAt, id)` keyset, and the owner projection's derived-lifecycle/filter/search contract reused unchanged) — plus the bounded global detail read behind `/admin/payment-links/v2/[id]`. Rows are the delivered owner directory projection plus exactly the owner `username`/`deletedAt` attribution: soft-deleted owners are never filtered and render a localized non-color deleted badge with the interim `/admin/accounts` owner navigation, malformed and missing identities share one opaque unavailable outcome, and the detail's only drill-down targets `/admin/orders?link=<identifier>`. Never add a mutation surface, a V1 link projection, or currency-pair UUIDs, owner email/id, provider data, verifiers, or lifecycle CAS fields. `pnpm admin-payment-links:evidence` / `admin-payment-links:evidence:verify` hold the run-bound production evidence contract.
- `src/app/admin/` owns the administrator shell plus account, global-payment-settings, and supported-exchange-currency mutations; every read and POST must re-authorize the cookie principal, return only the documented empty `401`/`403` protected outcomes, and never disclose a target account or settings value to unauthorized callers.
- `src/auth/administration.ts` owns the terminal administrator-driven user soft-delete lifecycle behind `POST /admin/users/[id]/delete` — origin-guard first, re-authorized cookie admin, empty `401`/`403`, opaque `/admin?success=deleted` / `/admin?error=change-failed` redirects (unknown, already-deleted, and final-active-admin targets share the one failure cause), and exactly one literal server-request-log entry. One advisory-locked transaction writes the additive `user.deleted_at` marker with `status='DISABLED'` (sign-in, principal resolution, and `countActiveAdmins` stay byte-identical), revokes every session, disables the storefront, deactivates every V1/V2 payment link by service writes (frozen V1 files and the INSERT-only trigger untouched), and appends one `user_deletion` audit row (runtime `SELECT`/`INSERT` only, FK-less actor). Deletion is irreversible — status/role/password mutations on a deleted target share the opaque not-found outcome — never frees username/email/storefront slug, and never rewrites order/link/product/provider attribution. Only the administrator DTO (`toAdminUserDto`) exposes `deletedAt`; principal, merchant, and public surfaces never observe deletion state. See [`pop/specs/administrative-foundation.md`](pop/specs/administrative-foundation.md).
- `src/auth/supported-exchange-currency.ts` owns the dynamic supported-exchange-currency registry: the uppercase `[A-Z]{3}` ISO 4217 code is the only currency identity (no closed code list), and the Nautt `currency_uuid`/`exchange_currency_uuid` pair is administrator-registered runtime data — never build-time, inferred, or sourced from environment, headers, or owner browser fields. Registration, replacement (insert-or-repoint), and deactivation (pointer-row removal only) serialize in one transaction over the `supported_exchange_currency` pointer table, the sole authority for code-based new-selection eligibility; re-registering a known pair yields one typed duplicate-pair conflict, never a second row or leaked constraint error. Owner reads return only active redacted `{ code, label }` choices; the default resolves the active `BRL` mapping or explicit unavailable, never another code or order; missing configuration surfaces only the typed `NoActiveExchangeCurrencyMapping` signal to dependent backends. `POST /admin/exchange-currencies` (register/replace/deactivate intents) is its only mutation route — never add a public/sessionless discovery surface, an administrator management UI before Epoch 10, or owner capability over mappings. See [`pop/specs/catalog-and-payment-links.md`](pop/specs/catalog-and-payment-links.md).
- `src/app/nautt-credentials/reset/` owns the owner-initiated local-only webhook-registration reset (`REGISTERING`/`INDETERMINATE` → `UNREGISTERED`, provider fields nulled, zero provider calls and zero decryption); it re-authorizes the cookie principal and returns only an empty `401` or opaque relative redirects (`/settings?nautt=reset|changed|unavailable`), never distinguishing "no credential" from "wrong state".
- `next.config.ts` `headers()` serves the static security-header set (`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Strict-Transport-Security: max-age=63072000; includeSubDomains`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`) on `/:path*`; CSP is deferred (App Router inline hydration scripts need per-request nonces, and nonces require the forbidden middleware) — never add `unsafe-inline` CSP or a middleware instead.
- `src/app/origin-guard.ts` owns the fail-closed same-origin check that every cookie-authenticated POST route runs first, before any auth or service work: `Origin` is required and its host (hostname+port) must match `Host`, falling back to the first `X-Forwarded-Host` value behind the TLS proxy; missing, malformed, or mismatched origins get the same empty `403`. Never wire it into `/api/nautt/webhooks`, the sessionless `/api/payment-links/*` routes, `/api/health`, or the `[lang]/*` stubs.
- `src/security/public-rate-limit.ts` owns the bounded single-process limiter for only `GET /api/payment-links/[identifier]`, `POST /api/payment-links/[identifier]/checkout`, `POST /api/payment-links/[identifier]/checkout/status`, `POST /api/store/[slug]/checkout`, `POST /api/store/[slug]/checkout/status`, and `POST /api/store/[slug]/cart/checkout`. Each handler must decide before parsing, locale negotiation, or service work; a denial is only empty `429` with `Cache-Control: no-store`. Its key may contain only its closed surface and a SHA-256 hash of one canonical `X-Forwarded-For` IP literal supplied by a TLS proxy that overwrites that header; chains, malformed values, or absent forwarding share one anonymous bucket. Never store/log the raw address, expose limiter metadata, add a page/middleware/proxy integration, or claim cross-process protection.
- `src/observability/server-request-log.ts` owns the server-only completion record for the closed API and supported POST-mutation inventory. It accepts only the whole header-safe `X-Request-Id` grammar and literal route templates; records never contain a request path/query/body/header/cookie, identity, credential, customer snapshot, provider data, retry key, capability, verifier, nonce, or error detail. It may add only the normalized `X-Request-Id` response header and must never replace a response or thrown handler failure; `/api/health` remains entirely unwrapped. 9.2.1 extended the inventory by exactly the two literal templates `/api/store/[slug]/checkout` and `/api/store/[slug]/checkout/status`; 9.1.3 extended it by exactly the one literal template `/api/store/[slug]/cart/checkout`.
- `src/app/storefront/` owns the owner storefront-settings save: it re-authorizes the cookie principal, validates through `src/auth/storefront-settings.ts` (unique nullable slug, nullable bilingual display names, `#RRGGBB` accent color, enable-requires-slug, closed six-theme id, `boxed`/`table` layout, 43-character logo media identifier, standalone-payments toggle, `[A-Z]{3}` default currency), and returns only empty `401`/`403` or opaque `?storefront=changed|failed|conflict` redirects. Extended fields merge absent=unchanged (explicit blank clears the nullable ones), new currency assignments gate on the registry's `requireActivePair` while a stored code survives later deactivation, and logo replacement activates the new `STOREFRONT_LOGO` object, saves, then orphans the old one with a compensating orphan on save failure. `POST /storefront/logo` is the only logo staging route: origin-guard first, re-authorized cookie merchant, one bounded `multipart/form-data` file to media `create(..., "STOREFRONT_LOGO", ...)` (never lifecycle work), and only empty `401`/`403` or opaque `/settings?storefront-logo=staged&logo=<identifier>|failed` redirects carrying the public-safe identifier with no failure cause; it holds exactly one literal entry in the server-request-log inventory. The settings workspace client may only observe native form events — it omits unchanged extended fields from the save payload (absent=unchanged) and never intercepts either POST. The separate sessionless `/store/[slug]` route may read only `src/storefront/public-storefront.ts`'s redacted projection: the localized display name, accent color, resolved theme/layout, logo media identifier, the V1 link-driven `products` list, the 9.1.1 redacted catalog (the owner's active non-archived products grouped by localized active-category name with one uncategorized group last and empty categories omitted; per-product opaque product-UUID `reference` — authority-free, revalidated server-side by later cart commands —, bilingual title/description, exact-decimal price, resolved display currency code as product `currency_code` ?? store default ?? null surviving later registry deactivation, image media identifier, and the `available` eligibility verdict) under bounded `take`-capped deterministic reads, and the derived `standalonePayments` capability boolean with the stored store-default `standalonePaymentCurrencyCode` display label (the 9.1.2 boundary amendment: display metadata only for the custom-amount item and cart totals, surviving later registry deactivation like every stored code, never a pair UUID or an inactive-code discovery surface) — never the owner identity, username/email, checkout policy, credential or provider data (including pair UUIDs), raw toggle values beyond that capability flag, category UUIDs/state/versions/timestamps, or other internal fields beyond the product UUID reference and media identifiers.
- `src/app/store/[slug]/` owns the sessionless public storefront page: it scopes the resolved theme with `data-theme-preview` on `<main>` plus the validated `--storefront-accent` custom property (the only inline style the token lint allows here), renders the logo through `/media/[identifier]` with a localized alt or the official merchant-fallback lockup, and composes the grouped redacted catalog in the owner's `boxed` or `table` layout with a standalone custom-amount first item when `standalonePayments` is on (that item alone makes a store non-empty). The V1 link-driven `products` list is no longer rendered on the page (the DTO member stays byte-frozen and `/pay/[identifier]` links stay direct-only). The single client boundary owns the slug-scoped versioned browser-local cart (`qr-pagamentos:storefront-cart:v1:<slug>`, envelope `{ version: 1, items }`) backed by the pure framework-free `src/storefront/cart.ts`: product items hold only the opaque `reference` and a 1–9,999 integer quantity, at most one custom-amount item holds a canonical positive-decimal amount (the product price grammar class), and unknown versions or unparseable payloads discard silently. Every hydration reconciles the stored cart against the server-rendered catalog snapshot — stale or unavailable references drop, quantities clamp, and exactly one localized "cart was updated" notice appears on change; the cart never trusts stored data beyond item identity and never persists prices or totals. Totals are exact BigInt arithmetic at the grammar's 6-place scale, grouped per resolved currency code and never summed across currencies; a null-code group renders without a code label. The cart's only checkout affordance is the 9.1.3 control: it renders only for a populated product-only cart (a custom-amount member keeps 9.2.1's separate command), posts only product identity and quantity to `POST /api/store/[slug]/cart/checkout`, disables with `aria-busy` while pending, clears only that store's cart key and redirects to `/pay/[identifier]` on `201`, and keeps the cart intact behind one opaque localized error on any failure; no server cart exists, and the unavailable/empty/error states stay opaque. `pnpm storefront:evidence` / `storefront:evidence:verify` hold the run-bound production evidence contract. The 9.2.2 `/store/[slug]/pay` sub-route owns the sessionless standalone-payment buyer experience: the same `data-theme-preview` + `--storefront-accent` scoping (the token lint whitelist extends by exactly this route), the logo/fallback rail, the policy-exact amount/customer form whose policy comes from the projection's additive `checkoutDataPolicy` member (the 9.2.2 boundary amendment, V1 `checkoutPolicy` precedent — authority-free, re-derived server-side by 9.2.1), a prefill-only `?amount=` revalidated against the canonical amount grammar, the submit body exactly `{ idempotencyKey, amount, customer }`, the pinned client state union (9.2.1 accept states plus the V1 status vocabulary, `RESERVED`/`CREATING` waiting and the unchanged terminal set), V1's polling controller imported unchanged (5s interval, visibility pause, `1s·2^n` backoff, three failures to the manual-retry status error), every unavailability collapsing to the one opaque view, and the return-to-store link on waiting/failure/terminal views; it adds no API route and touches neither the rate-limit nor the request-log inventory. `pnpm standalone-payment:evidence` / `standalone-payment:evidence:verify` hold its run-bound production evidence contract.
- `src/auth/payment-link-v2.ts` owns the owner-scoped Commerce V2 payment-link service behind `POST /payment-links-v2` (create) and `POST /payment-links-v2/[id]` (edit/activate/deactivate actions) — origin-guard first, re-authorized cookie merchant, empty `401`/`403`, opaque `/links?payment-links-v2=…` redirects, and two literal server-request-log entries. Composition kind (`PRODUCT_LINES`: 1–20 ordered lines, 1–9,999 quantity, per-link product uniqueness; `FIXED_AMOUNT`: bilingual 1–160 single-line descriptions plus a canonical product-grammar amount), link type, and currency pair are immutable after creation; the shared 24-character identifier namespace retries collisions against both link tables. Active-dependency enforcement is service-fenced in one transaction under shared row locks (never a trigger). Financial members (lines, description, amount) are expected-version CAS-editable only while the attempt-existence seam observes none, expiry and the active flag CAS-editable at any time; post-attempt financial edits yield the typed locked error. 8.1.3 wired the real `checkout_attempt_v2` existence read into that seam; V2 checkout itself remains undelivered (9.3.1 owns it). Administrators gain no V2 link capability; V1 `payment_link`, its baseline trigger, routes, and DTO stay byte-for-byte frozen. See [`pop/specs/catalog-and-payment-links.md`](pop/specs/catalog-and-payment-links.md).
- `src/checkout/standalone-checkout.ts` and `src/checkout/standalone-payment-status.ts` own the sessionless standalone-payment pair behind `POST /api/store/[slug]/checkout` and `POST /api/store/[slug]/checkout/status` (9.2.1) — unlocalized, dynamic, no-store, limiter-decides-first, origin-guard never applies, outcome matrix mirroring V1 (400/one opaque 404/503, 201/202, capability-only 200/404 status polling). The browser supplies only the slug, an opaque retry key, the canonical-grammar amount string, and the customer snapshot; owner, policy, registry pair, fixed bilingual description, and the 24-hour capability TTL derive from locked persisted state (owner `FOR UPDATE`, registry pair `FOR SHARE`) inside the reservation transaction; every slug/toggle/currency failure is the one opaque 404 with never a BRL fallback. Replays reissue the capability only while unexpired, unrevoked, and both storefront toggles enabled, with zero provider calls. See [`src/checkout/AGENTS.md`](src/checkout/AGENTS.md).
- `src/checkout/storefront-cart-checkout.ts` owns the sessionless storefront cart command behind `POST /api/store/[slug]/cart/checkout` (9.1.3) — unlocalized, dynamic, no-store, limiter-decides-first, origin-guard never applies, one literal server-request-log entry, and the closed opaque outcome set (empty 429/400/404, `201 { paymentLinkIdentifier }`). The browser contributes only product identity and quantity; the command revalidates the slug (only an enabled storefront of an `ACTIVE` merchant `USER` resolves, else the opaque 404), every `reference` (the owner's active non-archived products, else the opaque 400), and the single-currency rule (every line's product code ?? store default must be one identical code with an active registry mapping, else the opaque 400) against locked persisted rows under shared row locks. Success issues exactly one active `SINGLE_USE` `PRODUCT_LINES` V2 link with null expiry through `src/auth/payment-link-v2.ts` reused server-side with a principal synthesized from the locked owner row — never minting authority beyond the active merchant owner — with no cart table, order, attempt, or provider call at issuance (9.3.1 consumes the link); a `custom-amount` member or any foreign payload key is the opaque 400, and thrown issuance faults propagate to the request-log `failed` record, never a mapped body.- `src/checkout/public-checkout-v2.ts` and `src/checkout/payment-status-v2.ts` own the sessionless Commerce V2 public checkout (9.3.1) behind additive V1-first branches on the existing `POST /api/payment-links/[identifier]/checkout` and `/checkout/status` routes — no new route, zero rate-limit/request-log inventory edits, the V1 outcome matrix unchanged (V1 resolves first; checkout falls through only on the V1 unavailable outcome, status only on a V1 capability miss). The browser body is exactly `{ idempotencyKey, customer }`; the quote amount is the locked link's exact total returned by the `createFromLink` seam (order shaping never leaves `src/orders/order-v2.ts`, and the seam's internal transaction nests as a savepoint inside the one reservation transaction); policy and pair derive from the locked link/owner (`FOR UPDATE`); `SINGLE_USE` links with a settlement claim are rejected before any attempt work; replays reissue only an unexpired, unrevoked capability of an active, unexpired link; the 24-hour capability TTL is bounded by link expiry; the `checkout-v2-` verifier namespace is distinct from V1 and standalone. `src/checkout/public-checkout-v2-presentation.ts` owns the redacted read behind the additive `/pay/[identifier]` V2 branch (V1 render path byte-stable), returning a discriminated outcome: the checkout view, the 9.3.2 paid terminal view for a consumed `SINGLE_USE` link, or the one opaque unavailable view covering every other non-payable state (malformed, unknown, inactive, expired, publicly inactive line product, missing members). The checkout view carries the localized composition, exact total, registry display code (locked pair → active pointer code; unmapped → explicit unlabeled), owner policy, and persisted owner branding (display name, accent, theme with design-system default, logo media identifier) resolved independent of `storefront_enabled`; never owner identity, pair UUIDs, state/version/timestamps, or credential/provider data. The paid view is keyed only to the persisted `payment_link_v2_single_use_settlement` claim — never live order state, so a later `REFUNDED`, deactivation, expiry, or line-product deactivation never flips it — and carries only the branding, one localized already-paid statement, and the public composition summary with exact total and display code; never order state or any timestamp (including `claimedAt`/`orderV2Id`), refund state, payer data, attempt/capability material, provider data, pair UUIDs, the checkout policy, or link internals. The V2 client mirrors `public-checkout-form.tsx` with import-only reuse of `createPollingController`, the pinned state union (`RESERVED`/`CREATING` waiting treatments plus the V1 vocabulary), and V1-identical polling cadence; `pnpm checkout:evidence` / `checkout:evidence:verify` hold the run-bound production evidence contract. See [`src/checkout/AGENTS.md`](src/checkout/AGENTS.md) and [`pop/specs/checkout-and-order-lifecycle.md`](pop/specs/checkout-and-order-lifecycle.md).
- `src/orders/order-v2.ts` owns the Commerce V2 order service: owner-scoped `AD_HOC` creation (stateless under this contract — no `state`, link, attempt, capability, or provider dispatch) behind `POST /orders-v2`, the server-only `LINK` creation seam with immutable exact-decimal monetary snapshot lines (BigInt micro-units, never JS `Number`), the server-only `STANDALONE` creation seam (`createStandaloneOrderRow`: no link, no lines, fixed bilingual description constant pair, `CREATED` state — consumed only by the sessionless standalone checkout), and the settlement-map V1 reconciliation CAS plus the atomic `payment_link_v2_single_use_settlement` claim rebound to V2 identities (link-less `STANDALONE` orders settle through the same versioned CAS with no link join and no claim). The source vocabulary (`LINK`|`AD_HOC`|`STANDALONE`) with per-source link/state coherence, the canonical decimal grammar, and active-dependency enforcement are service-fenced, never database constructs. `POST /orders-v2` and `POST /orders-v2/[id]` are the only V2 order mutation routes — origin-guard first, re-authorized cookie merchant, empty `401`/`403`, opaque `/orders?orders-v2=…` redirects, two literal server-request-log entries; never add a public/sessionless V2 checkout here (delivered by 9.3.1 under `src/checkout/`).
- `src/orders/order-engagement-v2.ts` owns owner-only V2 comments (append; author-only expected-version CAS edit stamping `edited_at`; body 1–2,000 code points, NFC/trim, internal line breaks preserved) and the append-only `order_local_outcome_v2` history (`LOCAL_FINALIZED`/`LOCAL_CANCELLED`, latest-entry-is-current, lifecycle-version CAS). Local outcomes never write, mask, or shadow `state`, never consume a `SINGLE_USE` link, and never attach or modify a provider order; administrators and customers never comment.
- `src/orders/order-v2-view.ts` owns the only server-only read projection through which V2 order data leaves persistence: re-authorized owner and administrator list/detail reads exposing source, the immutable monetary snapshot, the policy-exact `CustomerSnapshotV1` under the fail-closed guard (8.3.3: as `payer` on the summary and `customer` on the detail), the detail `lifecycleVersion` for the set-outcome CAS (8.3.3), `state`, and the current local outcome as separate fields, with one opaque "unavailable" outcome for cross-owner, malformed, and missing identities. V1 `src/orders/order-view.ts` keeps reading only V1 data.
- `src/auth/payment-link-v2-view.ts` owns the read-time derived lifecycle projection for the merchant V2 payment-link directory: `inactive` > `expired` > `paid` > `active` precedence computed at read time (never a stored column; `paid` is the SINGLE_USE settlement claim or at least one CONFIRMED LINK order), the read-time `orderCount` (a filtered Prisma `_count` of every LINK-source order in any state, never stored and never the `paid` signal), the foundation `DirectoryAdapter` over the keyset `(created_at, id)` window with `pageSize + 1` bounded reads and owner scope re-checked per read, and the `/links/v2/[id]` owner detail read with one opaque "unavailable" outcome for cross-owner, malformed, and missing identities. The read-only 8.2.3 drilldown pages under `/links/v2/[id]/orders` (list and `[orderId]` detail) re-authorize the owner, resolve the parent link through this view, and reuse the `src/orders/order-v2-directory.ts` entry with the parent identifier force-bound server-side into the `link` filter (a client-supplied value is never honored) and the detail page rendering an order only when its `paymentLinkV2Identifier` matches the parent identifier — otherwise the one opaque unavailable state. Never expose V1 data, provider data, currency-pair UUIDs, verifiers, or a mutation through it, and never persist a derived state. See [`pop/specs/catalog-and-payment-links.md`](pop/specs/catalog-and-payment-links.md).
- `src/orders/merchant-analytics.ts` owns the server-only, owner-scoped merchant analytics projection over Commerce V2: one re-authorized active-`USER` read returning the period-parameterized (`today`/`7d`/`30d`, fixed `America/Sao_Paulo` calendar-day half-open UTC bounds) aggregate DTO — exact BigInt micro-unit sales per snapshotted `(currency_uuid, exchange_currency_uuid)` group (provider-confirmed (`LINK` ∪ `STANDALONE`) vs locally finalized (`AD_HOC`-only), never merged, never `Number`, never cross-currency), the attempt funnel over both attempt tables (converted/abandoned/in-progress from persisted `created_at`/`capability_expires_at`/order `state` only, never double-counted), bounded best-seller and per-link metrics, and the redacted recent-activity feed. Aggregates, rates, bilingual labels, redacted registry currency labels, and 24-char link identifiers only — never payer data, internal UUIDs, verifiers, capability material, or provider data; no route, no mutation, no administrator variant, and V1 tables are never read. Definitions are pinned in [`pop/specs/checkout-and-order-lifecycle.md`](pop/specs/checkout-and-order-lifecycle.md) (Merchant analytics definitions); the merchant dashboard at `src/app/(merchant)/page.tsx` (8.4.2) is its only consumer.
- `src/orders/admin-analytics.ts` owns the server-only, re-authorized administrator global analytics projection over Commerce V2: one entry receiving the already-resolved active `ADMIN` principal (exact role/status denial before any I/O, one shared forbidden outcome) returning the period-parameterized global DTO — separate registered/active/deleted user counts (registered includes soft-deleted, activeNow is `ACTIVE` plus no `deleted_at` marker), in-period order counts by source/state, the 8.4.1 two never-merged sales members and the funnel union over both attempt tables reused globally with per-pair BigInt micro-unit discipline and registry-redacted labels, V2 link total/active and product active/archived counts, and bounded top-5 leaderboards (owners ranked by in-period confirmed orders with identity exactly `{ username, deleted }`; products by confirmed quantity with bilingual titles from current rows). Aggregates, counts, rates, and bilingual/redacted labels only — never payer data, owner email, internal or currency-pair UUIDs, verifiers, capability material, or provider data; soft-deleted owners stay inside every aggregate, unresolved owner identities fail closed off the leaderboard, V1 tables are never read, and there is no route, mutation, or per-owner drilldown (10.1.2 owns the `/admin` dashboard rendering). `src/orders/merchant-analytics.ts` may gain only additive `export` keywords on existing pure helpers for this projection — its DTO and behavior stay byte-compatible. Definitions are pinned in [`pop/specs/administrative-foundation.md`](pop/specs/administrative-foundation.md) (Administrator analytics definitions).
- `src/app/(merchant)/page.tsx` owns the bilingual merchant dashboard: a server-rendered read-only composition over `getMerchantAnalyticsService().getForOwner` plus the owner storefront settings read in parallel — no new route, mutation, client data fetching, or projection change. `?period=` accepts only the closed `today`/`7d`/`30d` set; absent, unknown, or `invalid-period` outcomes render the pinned `7d` default, period switching uses plain GET links with a non-color `aria-current` marker, and the single primary View Store action appears only when `storefrontEnabled && storefrontSlug !== null` and links the sessionless `/store/[slug]`. Provider-confirmed and locally finalized sales render as separate ruled per-currency groups and are never summed; rates render as exact-decimal localized percents with an explicit n/a, unlabeled currency pairs get an explicit localized unlabeled treatment, every section owns an explicit empty state, and loading uses owned skeletons. `pnpm merchant-dashboard:evidence` / `:verify` hold the run-bound production evidence contract.
- `src/orders/order-v2-directory.ts` owns the read-only owner V2 order directory on the `src/data-directory/server` foundation: the cookie-principal entry canonicalizes `/orders`-bound URLs (noncanonical → same-path `307`, invalid → zero-I/O invalid) and reads only the merchant-own scope through the registered `(createdAt desc, id desc)` keyset order with page sizes 10/20/50/100 (default 20), the closed `source`/`money` enums, exact-calendar-day `from`/`to` bounds, exact `link` identifier, and `q` (UUID equality on local/provider order IDs, otherwise owner-scoped payer-text containment). The `money` classification maps only through the durable registry pair behind the active `USD` pointer (never URL UUIDs/codes); rows reuse the `order-v2-view.ts` summary shape with no mutation, rendering, or administrator variant — 8.3.2 owns mutations and 8.3.3 owns the UI.
- `src/auth/public-payment-link-v2.ts` owns the additive V2 branch of `GET /api/payment-links/[identifier]`: V1 identifiers resolve first through the frozen V1 read; V2 identifiers return only the `{ composition, currencyPair }` superset (redacted bilingual product title/description/price plus quantity, or bilingual description plus amount, and the two pair UUIDs) under the same limiter, closed `pt-BR`/`en` negotiation, `no-store` on every outcome, and the uniform empty `404` — a link with any publicly inactive line product, no lines, or missing fixed-amount members is unavailable. Never expose identifiers, owner, state, version, timestamps, or other provider data through it, and never let it alter the V1 DTO, the rate-limit surface, or the checkout/status routes.
- `src/auth/product-category.ts` owns owner-scoped bilingual category validation, exact-name uniqueness mapping, expected-version CAS, and transactional one-way deactivation/reassignment; `POST /product-categories` is its only mutation route. Products may reference only a same-owner active category through the nullable composite key, and category reassignment moves every referencing product regardless of archival state. Never expose category data through current product, payment-link, checkout, order, or storefront V1 projections, and never grant administrators category ownership capability.
- `src/auth/product.ts` owns owner-scoped product validation, expected-version CAS, and the catalog-media extensions behind `POST /products` (create/update/active/archive/delete actions only). Nullable `currency_code` gates on the registry's `requireActivePair` only at genuine new assignment (typed `NoActiveExchangeCurrencyMapping` maps to the one opaque failed outcome); clearing never gates and a stored code survives later mapping deactivation. Nullable `image_media_id` holds only the opaque 43-character `PRODUCT_IMAGE` identifier with no database foreign key; assignment activates STAGED→ACTIVE before the save, replacement/removal orphans the previous object after it, and a failed save compensates with a best-effort orphan. Archival is one-way terminal deactivation (`active=false` + `archived_at` in one irreversible CAS) that edit-locks every later mutation; new-link ineligibility rides the existing baseline `payment_link_require_active_dependencies` trigger and active-only picker — never edit `src/auth/payment-link.ts` or any trigger for archival, and never claim checkout keeps serving links to archived products (preservation is data-level only, exactly V1 deactivation semantics). Never leak currency, image, category, or archival state into V1 payment-link, checkout, order, or storefront projections.
- `src/media/` owns the server-only canonical image boundary and `GET /media/[identifier]` is its only read route: accepted JPEG/PNG/WebP input becomes bounded single-frame WebP outside `public/`; owner/purpose/state/revision metadata, physical count-until-durable-delete quotas, local-POSIX descriptor/digest checks, and fail-closed reconciliation govern every operation. Server-side attachment flows use only the identifier-based owner-fenced `activateOwned`/`orphanOwned` (activate new → save referencing row → orphan old, compensating orphan on save failure); revision fencing stays inside this subtree. Public reads require `ACTIVE`; only the active merchant owner may read `STAGED` or grace-period `ORPHANED`; every outcome is `no-store` and every unavailable case is the same empty `404`. Administrators gain no owner capability. See [`pop/specs/media-storage.md`](pop/specs/media-storage.md).
- `src/data-directory/` owns the role-neutral bounded query, canonical URL, authenticated keyset cursor, and accessible responsive directory composition. Its only server entry points require an already-resolved active `USER` own scope or active `ADMIN` global-read scope; cursors bind a non-identifying scope purpose with a purpose/principal-derived HMAC and never grant authority. Keep concrete Orders, Links, Users, Products, Categories, and analytics projections out of this foundation.
- `src/app/(merchant)/catalog/` owns the merchant catalog workspace: a single-page products directory (foundation query-contract decoding with canonical `307` resets and no cursor URLs — any cursor parameter is invalid; the selected page size caps rendered rows behind an explicit truncation note), `/catalog/products/new` and `/catalog/products/[id]` forms with dirty-field omission for category/currency/image (absent = unchanged), a disabled currency select with bilingual explanation when no active mapping exists, a terminal read-only archived view with no mutation control, and `/catalog/categories` with inline create/edit plus deactivation-with-reassignment. `POST /products` and `POST /product-categories` remain the only product/category mutation routes; only their redirect targets repoint to `/catalog…`. `POST /products/images` is the only image staging route: origin-guard first, owner-only re-authorized, bounded multipart rejected above 5 MiB before any decode, media create `STAGED` → `200 {identifier}` with `no-store`, one opaque empty `422`-class/`401` failure outcome, and no rate-limiter changes. Upload previews ride the owner-fenced `GET /media/[identifier]` read; placeholders use only official `src/brand/` identities, and catalog-media state never leaks into V1 payment-link, checkout, order, or storefront projections.
- `src/app/(merchant)/orders/` owns the merchant orders workspace: `/orders` composes the Commerce V2 directory over the delivered `order-v2-directory.ts` query (notice strip of the closed `orders-v2=commented|comment-edited|outcome-set|failed` set before strict canonicalization, canonical `307` resets, zero-I/O invalid-query, all six states) above the byte-frozen V1 ledger section, and `/orders/v2/[id]` owns the V2 detail with the comment thread (append + author edit under CAS) and the guarded local-outcome forms posting the projection-supplied `lifecycleVersion` to `POST /orders-v2/[id]`; `/orders/[id]` keeps serving V1 detail and the shell route map stays at five entries. The page-size preference is one observation-only client boundary: it stores the toolbar choice in localStorage and, only when the URL carries no explicit `pageSize`, navigates once to the canonical URL with the stored registered size — never intercepting the native GET or violating canonicalization.
- `src/i18n/` owns the closed locale set and server dictionary loader; never add a locale without matching dictionary keys and contract tests.
- `src/brand/` owns the original four-identity QR Pagamentos family and its deterministic manifest/static derivatives; UI consumers use the shared semantic-color composition, never page-local marks or per-theme assets.
- `src/app-shell/` owns inert role-neutral shell presentation; separate
  `src/app/admin/` and `src/app/(merchant)/` adapters re-authorize their exact
  role and provide fixed five-entry route maps. Dashboard roots match exactly,
  descendant active state is segment-safe, and the focused client boundary owns
  only pathname and mobile disclosure state. Page routes are `/admin`,
  `/admin/orders`, `/admin/payment-links`, `/admin/accounts`,
  `/admin/settings` and `/`, `/orders`, `/links`, `/catalog`, `/settings`;
  `/profile` is a secondary merchant account affordance, never a sixth primary
  entry.
- `src/auth/profile.ts` owns active-merchant profile reads, username/email CAS,
  and current-password-proven rotation. Profile POSTs are
  `/profile/identity` and `/profile/password`: identity success retains
  sessions; password success atomically replaces the exact observed credential,
  revokes every session, expires the cookie, and returns to username-only
  login using only the validated persisted locale carried by the non-auth
  `qr_locale` cookie. Never grant this capability to administrators, use email for login,
  expose identity through public/store DTOs, or execute zero/two verification
  scrypts on an authorized parsed rotation attempt.
- Use the exact Node and pnpm pins in `.node-version` and `package.json`; install with `pnpm install --frozen-lockfile`.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` independently, or `pnpm check` for the aggregate gate.
- Run `pnpm db:test` separately for the disposable PostgreSQL contract; it is never part of the database-free `pnpm check` gate.
- `Dockerfile` and `compose.yaml` own production image and startup ordering; release db-ops/app images must carry the exact target SHA in `org.opencontainers.image.revision`; the steady app alone mounts the local Compose-owned `media-data` volume RW at fixed `/app/media` while remaining UID/GID `1000:1000` with a read-only root. Never add a floating base, secret-bearing build input/environment field, public database port, or retrying one-shot job.
- `container/` owns redacted non-shell bootstrap, migration, identity seed/recovery, runtime preflight, and liveness wrappers; preserve direct child spawning, required file-backed username, nullable file-backed email, immutable UUID recovery targeting, and `SELECT 1` before application bind.
- Run `pnpm container:contract-check` for static/digest contracts and `pnpm container:test --clean-clone --scenario <name>` only with disposable secrets/resources.
- `install/` owns operator install/update/uninstall, initial-admin recovery, paired backup/restore, and automatic restore recovery for any Linux host with Docker Engine + Compose v2 already installed and the invoking user already in the `docker` group (documented prerequisites, not installed or granted by the script — never reintroduce `sudo`/root escalation or ownership rewrites here); passwords live only in ignored source/staged files and recovery uses reviewed helpers.
- Bare `install/update.sh` serializes each checkout, requires a clean attached branch, fast-forwards only to its protected tracked upstream, and rechecks its exact target SHA/clean tree around every deployment boundary; never add backup/release prerequisites, a moving-ref handoff, network/secret/database access to the policy verifier, or a second re-entry.
- Update must prove compatible Compose ownership, both exact data volumes, all PostgreSQL/Nautt credential continuity, a healthy old app, and the target-image media preflight without rewriting secrets; keep that app running through candidate build and additive migration proof. Target-health rollback is image-only against retained database/media state. Default uninstall preserves both volumes and protected credentials; purge and restore require exact paired targeting.
- Backup publishes one checksummed PostgreSQL/media set after quiescing only app. Restore must validate and rehearse the exact release in a disposable closed inventory before managed mutation, preserve an automatic recovery pair, and leave the app stopped with artifacts intact on double failure.
- The 19 migration directories through `20260721060000_storefront_settings` are an immutable hash-pinned baseline whose reviewed anchor is independent of its editable inventory; every later migration must be canonical byte-exact SQL generated from the closed non-destructive `migration.safe.json` language governed by the Prisma subtree contract. Never admit raw SQL, destructive DDL/DML, data rewrites, rename/type change, or edits to existing history.
- Run `install/test.sh` after changing installer commands, secret validation, Compose deployment, health waiting, or uninstall flags.
- `MIGRATION_DATABASE_URL` is migration-only and `DATABASE_URL` is runtime-only; never share credentials or commit usable URLs.
- `NAUTT_WEBHOOK_CALLBACK_URL` is required non-secret server configuration; accept only canonical absolute HTTPS and never derive it from browser fields or request headers.
- `NAUTT_API_BASE_URL` is optional non-secret server configuration; accept only canonical absolute HTTPS without credentials or a fragment and default to `https://api.nauttfinance.com/api/v2` when unset.
- Generated Prisma code lives in ignored `src/generated/prisma/`; never edit or commit it.
- Never edit generated `next-env.d.ts`, `.next/`, `node_modules/`, coverage output, or TypeScript build-info files by hand.
- [`prisma/AGENTS.md`](prisma/AGENTS.md) — follow before changing the schema, bootstrap SQL, or immutable migration history.
- Do not add another child `AGENTS.md` unless its subtree meets an objective DOX trigger below.

## Processo DOX — contexto hierárquico de agentes no código

### O que é

Uma árvore de arquivos `AGENTS.md` dentro do código: o da raiz do código é o **trilho DOX** — regras do projeto inteiro + índice de alto nível; cada diretório relevante tem o seu, com regras locais e índice do próprio subtree. Cada `AGENTS.md` é um **contrato de trabalho vinculante para o seu subtree**: nenhuma edição às cegas, nenhuma documentação defasada.

### Regras

1. **Antes de editar:** leia o AGENTS.md raiz do código, identifique **todos** os caminhos afetados e **caminhe a árvore** até cada local de edição, lendo todo AGENTS.md aplicável no caminho. A caminhada pode ser delegada a um subagente que devolve **só as regras aplicáveis** aos caminhos da task — o executor recebe o extrato, não a árvore.
2. **Entendimento local:** qualquer ponto do código deve ser compreensível lendo apenas o AGENTS.md mais próximo + todos os pais acima dele. Se não for, falta contrato — crie/complete o local antes de editar.
3. **Conflitos:** o documento mais próximo manda nos detalhes locais; um filho **nunca enfraquece** diretiva do pai.
4. **Concisão operacional:** regras amplas nos níveis altos, detalhe concreto nos filhos. Só o que muda decisões de edição — nada de prosa. **Polaridade:** prefira constraints negativas ("nunca X neste subtree") e condicionais ("se Y, então Z"); evite diretriz positiva genérica ("siga o estilo") — guardrails rendem +13,8pp de acerto, guidance genérica −6,4pp. Teto: **~60 linhas** por contrato de subtree; estourou, o detalhe desce para um filho. Exceção: diretório de árvore grande (muitas subpastas) pode exceder para comportar o índice do subtree — a exceção cobre o índice, não prosa.
5. **Revisão obrigatória:** toda mudança relevante exige revisar os AGENTS.md afetados — atualize quando mudarem propósito, escopo, responsabilidade, estrutura, fluxos, entradas, saídas ou padrões de qualidade.
6. **Fechamento (closeout):** ao concluir o trabalho, re-cheque os caminhos alterados, atualize o documento dono e os pais afetados, refresque os índices, remova conteúdo obsoleto e rode as verificações pertinentes.
7. **Contratos relacionados:** seção opcional em cada contrato com links relativos markdown (`../services/payments/AGENTS.md`) para contratos de outros subtrees dos quais decisões locais dependem — cada link com **gatilho** de 1 linha (*quando segui-lo*). Máx. **~3 laterais (ideal 0-2)** e **<7 referências totais** por contrato (laterais + skills + índice de filhos); só dependência que muda decisão de edição (não todo import); link sem gatilho não vale. Precisou de mais? Sinal de acoplamento ou de roteamento que pertence ao índice do pai. A caminhada vira: vertical até o local de edição + laterais cujo gatilho casa com a task. O closeout (regra 6) atualiza também os laterais dos contratos tocados. **Elo contrato→spec:** quando o harness do PoP mora no mesmo repositório (`included` ou repo de `full-multi-repo`), o contrato pode linkar a **spec do tema** por caminho relativo markdown (`specs/<spec>.md`), com gatilho e contando no teto de referências; no type `default` a ponte contrato↔spec é o card/plano da task — o vault não resolve de dentro do repo (a direção spec→contrato existe sempre, no template de spec).
8. **Skills do subtree:** o contrato pode linkar skills do projeto (`skills/`) **específicas daquela pasta** — procedimento que muda como se edita o subtree (ex.: `migrations/` linka a skill de migration com gatilho "siga antes de criar/alterar qualquer migration"). Sempre link com gatilho, nunca cópia do conteúdo (cópia = drift). Skill de **workflow** (advance-task etc.) nunca entra em contrato — dona dela é a tabela "Skills por etapa" do card: o card responde "como trabalho esta task"; o contrato responde "o que vale ao editar esta pasta, seja qual for a task". Os links de skill contam no teto de referências da regra 7.
9. **Citações verificáveis:** contrato que cita arquivo ou trecho concreto do código pode fixar a citação com a anotação `<!-- pop-hash: <caminho-relativo> sha256=<hash do arquivo citado> -->` (comentário HTML, invisível; caminho relativo à pasta do contrato; hash via `sha256sum <arquivo>`). O `pop_validate` recomputa **fail-closed** — arquivo citado sumiu ou mudou → violação — onde o vault alcança o arquivo (repos embutidos de `full-multi-repo` e clones presentes na raiz da pasta do projeto). Ao revisar a citação, atualize o hash: a mensagem de violação imprime o novo.

### Inicialização

Código sem árvore DOX → varredura recursiva e construção da árvore: AGENTS.md raiz com o índice geral e contratos-filhos **só onde há gatilho objetivo** — não crie AGENTS.md vazio "por via das dúvidas". Em projeto importado (`import-project`), a inicialização é task da Epoch 1 (Organização).

- **Gatilhos de contrato-filho:** ≥2 convenções não óbvias; erro prévio de edição às cegas; stack diferente do resto do repo; ownership diferente (outro time/dono); regras de segurança/permissão distintas; código legado.
- **Árvore nasce enxuta:** contratos iniciais de **20–30 linhas**, crescendo até o teto de ~60 conforme necessidade real; raiz passou de ~150 linhas → desça detalhe para um filho. Escala de referência: **5–15 contratos** bastam para a maioria dos repos.
- **Curadoria humana obrigatória:** a árvore inicial passa pelo gate 003 da task que a cria — contrato LLM-gerado sem curadoria **piora** o resultado (−3% de sucesso, +23% de custo).

### No fluxo do PoP

- **002 (brief):** o recon inclui os AGENTS.md aplicáveis aos caminhos que a task toca; o brief lista os contratos que precisarão de atualização, sem microedições ou trechos de implementação.
- **004:** edite só depois de caminhar a árvore; os AGENTS.md alterados entram na mesma worktree/PR da task.
- **005:** a verificação confere que os contratos afetados foram atualizados — critério de aceite implícito de toda task de aplicação.
- **Type `default` com repo externo que deve ficar limpo de arquivos de IA:** decida com o usuário na entrevista — commitar a árvore DOX no repo (padrão do PoP) ou manter apenas o contrato raiz no AGENTS.md do projeto, dentro do PoP.

## Essential rules

- Use English for project content and ISO dates (`YYYY-MM-DD`). Use wikilinks for internal references and keep notes near 150 lines.
- Never implement or modify application behavior outside a task in `004_processing` with an approved plan.
- Never use Nautt Finance hosted payment links; this application owns its products, links, and checkout pages.
- Never expose Nautt API keys, session secrets, or webhook secrets to clients or committed files.
- Never execute an item owned by `(user)`.
- Every completed task writes `memory/<id>.md` with dates and the final commit.
