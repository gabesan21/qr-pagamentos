---
author: user
created: 2026-07-25
---

# Beta unverified webhook intake - 2026-07-25

- **Verbatim decision (human, 2026-07-25, explicit beta decision for closed testing):** "Nautt webhook HMAC verification is ON HOLD — the application must ACCEPT webhook bodies without verifying the signature. This will be resolved before production, at the human's explicit command."
- **Reasoning:** closed beta testing cannot depend on the unproven production HMAC contract; the `nautt-production-webhook-hmac-contract` research stays on hold and the pre-production gate must be impossible to miss.
- **Seam shape (task [[M-5.1-beta-unverified-webhook-intake]]):** one loudly marked `BETA(M-5.1)` seam inside `createWebhookIntake` (`src/integrations/nautt/webhook-intake.ts`). The signature gate, `loadCandidates`, and `verifyOwner` are skipped entirely — missing, malformed, and present-but-wrong `X-Nautt-Signature` values are all accepted. Owner attribution uses a new `resolveOwner(providerOrderUuid)` dependency over the globally unique `provider_order.providerOrderUuid`, wired as a direct Prisma lookup in `webhook-runtime.ts`. `webhook-signature.ts`, `webhook-secret-candidates.ts`, and their tests stay intact as the reversal target; `webhook-runtime.ts` keeps wiring `loadActiveWebhookSecrets`; `webhook-intake-beta.test.ts` pins the marker and the absence of verification. No ENV/config toggle — the bypass is unconditional marked code so it cannot be forgotten in production configuration.
- **Security honesty:** beta webhook intake is UNAUTHENTICATED. Any internet caller who learns a provider order UUID can trigger one owner-bound provider GET; forged bodies cannot inject false order state because notification status never reaches the mutation boundary (reconciliation takes state only from the authoritative owner-bound GET). Closed-beta exposure is the accepted mitigation, together with UUID unguessability, the one-GET economic bound, and dedup/lease fencing. The pre-production gate ([[open_questions/2026-07-25-pre-production-gate-restore-webhook-hmac]]) is the real fix.
- **Accepted deviations (restored on reversal):** `401` is suspended — the protocol narrows to empty no-store `204`/`400`/`503`; an unknown provider order UUID is acknowledged `204` with no durable evidence (replay re-runs one local indexed lookup, zero provider GETs); malformed bodies are parsed before any trust decision (256 KiB bound caps cost; no state change without a resolvable owner); provider-side orphan webhooks left by reset retries ARE accepted during beta instead of failing HMAC inertly.
- **Reversal instructions (before production, at the human's explicit command):** restore the signature gate + `verifyOwner` block and the `401` status in `webhook-intake.ts`; remove `resolveOwner` from the intake dependencies and `webhook-runtime.ts`; delete `webhook-intake-beta.test.ts`; convert the beta-acceptance pins in `webhook-intake.test.ts` back to 401 rejections; clear the `BETA(M-5.1)` caveats in `pop/specs/nautt-finance-integration.md`, root `AGENTS.md`, and `src/integrations/nautt/AGENTS.md`; only then resume the HMAC research if the production contract is still unproven.
