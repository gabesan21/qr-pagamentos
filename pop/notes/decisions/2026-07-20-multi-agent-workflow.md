---
author: user
date: 2026-07-20
---

# Multi-agent workflow adoption — 2026-07-20

QR Pagamentos adopts the parent harness contract introduced in commit `1c9b1f3` by explicit human request, outside the project's normal task flow.

The planner remains separate from execution but now persists a concise brief instead of a prescriptive wargame. Stage 004 uses adaptive specialist orchestration with explicit ownership and dependency contracts. Stage 005 combines behavioral verification and code-quality critique in one independent fresh-context reviewer; `critical` changes depth, not reviewer count. Missing dependencies block consumers instead of authorizing opportunistic implementation.

This propagation changes only the standalone PoP harness, its copied workflow skills, and its validation tools. It does not change QR Pagamentos application behavior.

Contract: [[pop/specs/multi-agent-orchestration|multi-agent orchestration]] — *follow when changing the adopted roles, ownership rules, or review gates*.
