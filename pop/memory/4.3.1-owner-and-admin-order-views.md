---
task: 4.3.1-owner-and-admin-order-views
project: pop
started: 2026-07-21
finished: 2026-07-21
commit: 272bb11d9012c1ef5df9dc45b26aadd54c3026fc
pr:
---

# 4.3.1-owner-and-admin-order-views — Owner and admin order views

Delivered protected order visibility closing Phase 4.3 and Epoch 4 on `develop`. A single server-only closed projection in `src/orders/order-view.ts` is the one authorization seam: a 12-field DTO plus policy-exact `CustomerSnapshotV1` for all five checkout policies; cross-owner, malformed, and missing identifiers share one opaque "unavailable" outcome (malformed never reaches the DB); every read re-resolves the cookie principal; lists are bounded (50, createdAt/id desc) and never select customer columns. Bilingual owner list/detail views live under `src/app/orders/` reusing the owner guard and shell; administrator global read-only list/detail live under `src/app/admin/orders/` with empty `401`/`403` only, zero POSTs and zero mutations. No schema change, no new dependencies.

Verification: full review (critical) — 56/56 focused tests (`src/orders`, owner/admin pages, i18n parity), lint/typecheck/build green, static no-exposure grep + embedded test, DOX/specs (root AGENTS.md, DESIGN.md, checkout-and-order-lifecycle, product-scope) synced. Integrated to `develop` as `272bb11` without a task PR.

Two follow-ups were filed to Epoch 0: `src/app/pay/[identifier]/page.test.tsx` fails pre-existing on develop (unauthenticated default locale pt-BR vs asserted English copy) and `toPolicySnapshot`'s default branch is fail-open for unknown policies (sugestão from 005).

- **Affected spec:** [[pop/specs/checkout-and-order-lifecycle|Checkout and order lifecycle]] — *follow when changing order read projection, customer-data exposure, or order views.*
- **Affected spec:** [[pop/specs/product-scope|Product scope]] — *follow when changing per-user order visibility boundaries.*
