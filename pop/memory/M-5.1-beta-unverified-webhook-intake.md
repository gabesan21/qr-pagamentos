---
task: M-5.1-beta-unverified-webhook-intake
project: pop
started: 2026-07-25
finished: 2026-07-25
commit: 00b488cd18c76ce1532d8be957279eeec873c788
pr:
---

# M-5.1-beta-unverified-webhook-intake — Beta unverified webhook intake

- **Delivery:** Explicit human beta decision (2026-07-25, closed testing): Nautt webhook HMAC verification is ON HOLD — intake accepts bodies with missing, malformed, or wrong signatures behind a prominent `BETA(M-5.1)` marker; owner is attributed via `resolveOwner(providerOrderUuid)` over the globally-unique `provider_order.providerOrderUuid`.
- **Areas changed:** `src/integrations/nautt/webhook-intake.ts` (marked bypass seam), `webhook-runtime.ts` (wiring), intake tests rewritten as beta-acceptance pins, new `webhook-intake-beta.test.ts` guard, registration trail (RESEARCHES.md, decision record, pre-production open question, Nautt spec caveats, root + nautt AGENTS.md overrides).
- **Verification:** Full critical yolo review APPROVE, round 1. 314 focused nautt tests, `pnpm check` (999 passed), guard test, diff/scope (exactly 10 files), PoP validation — all green; claim/lease/reconcile/finalize byte-identical to develop; signature module and tests byte-intact as the reversal target.
- **Final telemetry:** 002 planner; critical 003 critic approved round 1; cohesive 004 executor; full 005 critic approved with zero returns; no circuit breaker.
- **Durable decisions:** During the beta the intake is unauthenticated — the decision record states this plainly and closed-beta exposure is the accepted mitigation; forged bodies cannot inject false state because order status comes only from authoritative owner-bound reads. Protocol narrows to empty no-store 204/400/503 (401 suspended); unknown orders get 204 with no evidence. Reversal is one small marked diff: restore the signature gate and `verifyWebhookOwner` (module/tests kept intact) — the guard test fails if the marker disappears or verification silently returns.
- **Relevant deviations:** None beyond the documented beta ones.
- **Contract impact:** The pre-production gate is registered as an open question (`status: aberta`): HMAC verification MUST be restored at the human's explicit command before production; the authenticated HMAC research stays pending.

## Links

- **Spec:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *follow for the beta suspension and the permanent post-beta webhook contract.*
- **Commit:** `00b488cd18c76ce1532d8be957279eeec873c788` — *inspect the integrated delivery.*
