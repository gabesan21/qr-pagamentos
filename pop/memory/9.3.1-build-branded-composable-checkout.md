---
task: 9.3.1-build-branded-composable-checkout
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: 8de44ef5d1b21f94d2a41f715d71fea8e565ceef
pr:
---

# 9.3.1-build-branded-composable-checkout — Branded composable Commerce V2 checkout

- **Delivery:** Sessionless Commerce V2 public checkout end-to-end: reservation/replay/capability execution over `checkout_attempt_v2` behind additive V1-first branches on the existing `POST /api/payment-links/[identifier]/checkout` + `/checkout/status` routes (no new route, no rate-limit/request-log inventory edits), and the branded two-column `/pay/[identifier]` V2 experience (both compositions, five policy variants, exact server total, registry display code, owner theme/accent/logo independent of `storefront_enabled`, PIX QR/copy, capability polling with the V1-identical cadence).
- **Areas changed:** `src/checkout/public-checkout-v2.ts`, `payment-status-v2.ts`, `public-checkout-v2-presentation.ts` (+ unit/db tests), additive branches on the two V1 API routes and the `/pay/[identifier]` page (V1 render path byte-stable), `public-checkout-v2-page.tsx`/`-form.tsx`, `loading.tsx`/`error.tsx`, 6 dictionary keys × 2 locales, append-only globals.css checkout block + one token-whitelist entry, evidence pair (`tests/checkout.evidence.spec.ts`, run/verify scripts, `--checkout` mode in the shared harness, 52 captures), specs/AGENTS.md/DESIGN.md closeout.
- **Verification:** Critical task; full 005 critic — round 1 RETURN (the card's "two-column layout" never materialized: `.checkout-shell` 65ch cap < 696px two-track minimum). Rework: `.checkout-v2` widened to `var(--shell-max)` (72rem) with V1 untouched, copyState reset on terminal, evidence re-bound to run 20260726231945 proving two columns at 1440px and the stack at 375px. Round 2 APPROVE; critic re-ran typecheck/test/evidence-verify green and audited all fences (V1 byte-stability, redaction, reservation fences mirroring V1, SINGLE_USE claim rejection before replay, 24h TTL bounded by link expiry).
- **Final telemetry:** 002 plan; critical 003 approved; one executor (17 commits + 3 rework); full critic 1 return / 1 approve; no circuit breaker.
- **Durable decisions:** One justified fence deviation — `src/orders/order-v2.ts` `createFromAvailableLink` dropped the explicit `ownerId` on the nested lines create (Prisma 7 rejects a scalar duplicating the composite-relation-bound field; latent since 8.1.3, proven against disposable Postgres). Client state union pinned (`RESERVED`/`CREATING` + V1 vocabulary); browser body exactly `{ idempotencyKey, customer }`; `checkout-v2-` verifier namespace distinct from V1/standalone; consumed `SINGLE_USE` renders the one opaque unavailable (paid view reserved for 9.3.2).
- **Relevant deviations:** Additive-only touches to shared harnesses (`pop/scripts/db-test.mjs` stage, `--checkout` mode in `scripts/run-admin-evidence.mjs`) per the established extension pattern.
- **Contract impact:** Specs checkout-and-order-lifecycle (9.3.1 section), catalog-and-payment-links, storefront-and-customization (branding boundary amendment) updated; root AGENTS.md `src/checkout/` bullets + order-v2 wording delivered; `src/checkout/AGENTS.md` delivered line; DESIGN.md composition + evidence recorded.

## Links

- **Spec:** [[specs/checkout-and-order-lifecycle|Checkout and order lifecycle]] — *follow for the V2 checkout semantics and the 9.3.2 paid-view boundary.*
- **Commit:** `8de44ef5d1b21f94d2a41f715d71fea8e565ceef` — *inspect the integrated delivery (merge into develop).*
