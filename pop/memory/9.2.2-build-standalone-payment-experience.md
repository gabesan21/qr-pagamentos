---
task: 9.2.2-build-standalone-payment-experience
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: f4d559d2ab6a0672b86a83c4a2d0f99216e8b7e8
pr:
---

# 9.2.2-build-standalone-payment-experience — Standalone pay

- **Delivery:** sessionless standalone-payment experience at `/store/[slug]/pay`: canonical-grammar amount entry, policy-exact customer form over the five `CheckoutDataPolicy` variants, PIX QR/copy, status polling with retry, every terminal state, and return-to-store. The projection gained exactly one additive member, `checkoutDataPolicy`.
- **Areas changed:** `src/storefront/public-storefront.ts`, the new `src/app/store/[slug]/pay/` route, the storefront pay action, two globals.css rules plus a token-whitelist entry, and 67 captures.
- **Verification:** 005 critic round 1 RETURN on one blocking docs finding (a closeout edit had deleted a `DESIGN.md` lead sentence), restored byte-identical in `d974880`; round 2 APPROVE with test 1400/1400, lint, typecheck and evidence `:verify` green and frozen fences zero-diff.
- **Final telemetry:** 002 plan; non-critical yolo 002→004; one executor; 1 critic return then approve.
- **Durable decisions:** client state union pinned (`RESERVED`/`CREATING` plus the V1 terminals); body exactly `{ idempotencyKey, amount, customer }`; the amount grammar reuses `isStorefrontCartAmount` (client-safe twin of `order-v2.ts`); the V1 polling controller is imported unchanged; evidence makes zero provider calls.
- **Relevant deviations:** a pre-existing committed merge-conflict marker in the lifecycle spec had to be resolved; the additive `--standalone-payment` mode touches the shared harness; one accepted lint nit.
- **Contract impact:** specs: storefront-and-customization, checkout-and-order-lifecycle · DOX: root + DESIGN

## Links

- **Spec:** [[specs/checkout-and-order-lifecycle|Checkout and order lifecycle]] — *follow for standalone-payment semantics.*
- **Commit:** `f4d559d` — *inspect the merged delivery.*
