---
task: 9.2.2-build-standalone-payment-experience
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: f4d559d2ab6a0672b86a83c4a2d0f99216e8b7e8
pr:
---

# 9.2.2-build-standalone-payment-experience — Standalone payment buyer experience

- **Delivery:** Sessionless standalone-payment buyer experience at `/store/[slug]/pay`: amount entry under the canonical grammar, policy-exact customer form over the five `CheckoutDataPolicy` variants, PIX QR/copy with feedback, capability status polling with retry, every terminal/failure state, and return-to-store — bilingual and owner-themed. The public storefront projection gained exactly one additive member, `checkoutDataPolicy` (V1 `checkoutPolicy` precedent).
- **Areas changed:** `src/storefront/public-storefront.ts` (+test), new `src/app/store/[slug]/pay/` (page/loading/error/experience + tests, 41 new tests), `storefront-experience.tsx` pay action, 4 dictionary keys × 2 locales, 2 globals.css rules + 1 token-whitelist entry, evidence pair (`tests/standalone-payment.evidence.spec.ts`, run/verify scripts, `--standalone-payment` mode in the shared admin harness, 67 run-bound captures), specs/DESIGN.md/AGENTS.md closeout.
- **Verification:** Full 005 critic — round 1 RETURN (one blocking docs finding: a closeout edit had deleted the catalog-evidence paragraph's lead sentence in `DESIGN.md`); executor restored it byte-identical in `d974880`; round 2 APPROVE. Critic re-ran `pnpm test` 1400/1400, lint/typecheck, evidence `:verify`, token check — all green; zero diffs in the frozen fences (`src/checkout/`, `src/security/`, `src/observability/`, `src/app/pay/`, `src/app/api/`).
- **Final telemetry:** 002 plan; non-critical yolo 002→004; one executor (7 evidence-spec iterations, no app-code churn); full critic 1 return / 1 approve; no circuit breaker.
- **Durable decisions:** Client state union pinned (`RESERVED`/`CREATING` waiting + V1 terminals); submit body exactly `{ idempotencyKey, amount, customer }`; amount grammar reuses `isStorefrontCartAmount` (client-safe twin of the `order-v2.ts` pattern); polling controller imported unchanged from V1; zero provider calls in evidence (harness-computed capability HMAC).
- **Relevant deviations:** (1) resolved a pre-existing committed merge-conflict marker in `checkout-and-order-lifecycle.md` (union of Updated headers) — required to edit the file; (2) F03's additive `--standalone-payment` mode touches the shared `run-admin-evidence.mjs` per the established pattern; (3) one `no-img-element` lint warning, same accepted class as the storefront page.
- **Contract impact:** Specs `storefront-and-customization` (boundary amendment + slice) and `checkout-and-order-lifecycle` (buyer-experience section) updated; root `AGENTS.md` `/store/[slug]` line amended; `DESIGN.md` composition/states/whitelist/evidence recorded.

## Links

- **Spec:** [[specs/checkout-and-order-lifecycle|Checkout and order lifecycle]] — *follow for the standalone-payment semantics this page consumes.*
- **Commit:** `f4d559d` — *inspect the integrated delivery (merge into develop).*
