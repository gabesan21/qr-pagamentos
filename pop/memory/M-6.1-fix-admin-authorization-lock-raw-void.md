---
task: M-6.1-fix-admin-authorization-lock-raw-void
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: 9b9235e59be8bc1cfb8a2c6a70d364bbb9bc3d28
pr:
---

# M-6.1-fix-admin-authorization-lock-raw-void — Fix admin authorization advisory lock raw void deserialization

- **Delivery:** `withAuthorizationLock` (`src/auth/administration.ts`) switched `pg_advisory_xact_lock` from `$queryRaw` to `$executeRaw`, fixing the Prisma 7.8.0 P2010 that broke `deleteUser`/`changeStatus`/`changeRole` with opaque `change-failed`. New `acquireAuthorizationLock(transaction)` mirrors `src/auth/session.ts:33` (1.4.1 precedent); lock key, transaction scope, and opaque outcomes unchanged.
- **Areas changed:** `src/auth/administration.ts`; `src/auth/administration.test.ts` (lexeme + never-`$queryRaw` regression); new `src/auth/administration.database.test.ts` (happy locked paths); additive block in `pop/scripts/db-test.mjs`.
- **Verification:** 005 critic APPROVE round 1 — `pnpm check` exit 0 (1358 tests); `pnpm db:test` exit 0 incl. `PASS administration-database-locked-mutations`. Scope: four files + card; no spec/AGENTS drift.
- **Final telemetry:** Plan S, non-critical 002→004; one executor, zero returns; critic zero blockers (one coverage nit).
- **Durable decisions:** Defect surfaced by 10.2.1 production evidence. Future pg-adapter bumps must grep for `pg_advisory_xact_lock` + `$queryRaw`. First admin-service database test guards locked happy paths.
- **Relevant deviations:** Added service-level PostgreSQL coverage beyond the planned unit test — the plan's own risk fallback.
- **Contract impact:** None — behavior now matches the already-delivered administrative-foundation contract.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the admin lifecycle contract this repair restores.*
- **Commit:** `9b9235e` — *inspect the integrated delivery (merge into develop).*
