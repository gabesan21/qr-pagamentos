---
task: M-6.1-fix-admin-authorization-lock-raw-void
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: 9b9235e59be8bc1cfb8a2c6a70d364bbb9bc3d28
pr:
---

# M-6.1-fix-admin-authorization-lock-raw-void — Fix admin authorization advisory lock raw void deserialization

- **Delivery:** `withAuthorizationLock` (`src/auth/administration.ts`) no longer calls `transaction.$queryRaw` on the void-returning `SELECT pg_advisory_xact_lock(...)` — a P2010 under the Prisma 7.8.0 pg adapter that made every locked admin mutation (`deleteUser`/`changeStatus`/`changeRole`) fail at runtime with the opaque `change-failed`. New exported `acquireAuthorizationLock(transaction)` helper uses `$executeRaw`, byte-consistent with the 1.4.1 `src/auth/session.ts:33` precedent; lock key, transaction scope, and opaque outcomes unchanged.
- **Areas changed:** `src/auth/administration.ts`, `src/auth/administration.test.ts` (lexeme + never-`$queryRaw` regression), new `src/auth/administration.database.test.ts` (happy locked paths against disposable PostgreSQL), one additive registration block in `pop/scripts/db-test.mjs`.
- **Verification:** Full 005 critic APPROVE round 1 — gates re-run independently (`pnpm check` exit 0, 1358 tests; `pnpm db:test` exit 0 incl. `PASS administration-database-locked-mutations`); scope exactly the four code/test files + card; no spec/AGENTS.md changes (repair of already-specified behavior).
- **Final telemetry:** orchestrator plan (size S, non-critical 002→004); one executor, zero returns; full critic zero findings blocking (one coverage nit recorded).
- **Durable decisions:** The defect was found by 10.2.1's production evidence; the previously-missed file proves the 1.4.1 fix pattern must be grepped for (`pg_advisory_xact_lock` + `$queryRaw`) whenever the pg adapter is bumped. The new database test is the first administration-service coverage at that level and now guards the happy locked paths.
- **Relevant deviations:** Added service-level PostgreSQL coverage beyond the plan's minimum unit test — the plan's own documented risk fallback.
- **Contract impact:** None — behavior now matches the already-delivered administrative-foundation contract.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the admin lifecycle contract this repair restores.*
- **Commit:** `9b9235e` — *inspect the integrated delivery (merge into develop).*
