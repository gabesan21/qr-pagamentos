---
task: 15.1.1-remove-v1-application-surfaces
entry: 03-inventory-sweep
---

# Cross-cutting inventory and dictionary sweep

F04 rewrote `src/app/routes.test.ts` to V2-only assertions, dropped `paymentLinks`/`paymentLink` from `server-request-log.ts` (all 6 remaining buckets confirmed to still have a live caller), removed 39 dead pt-BR×en dictionary keys across five domains (re-verified for parity by the main agent), and trimmed `/orders/[id]`/`/admin/orders/[id]` targets from the evidence specs. Execution was interrupted mid-way by the user and resumed without loss.

## Evidence

- [[src/app/routes.test.ts]] — *the V2-only route assertions*.
