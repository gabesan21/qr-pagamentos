---
task: 15.1.6-remove-residual-v1-store-calls
entry: 01-residual-v1-removal-and-v2-only-storefront-projection
---

# Seven residual V1 Prisma calls removed; storefront `products` now V2-only

F01 deleted the last V1 delegate calls 15.1.4's C1 found failing `tsc`: admin soft-delete withdrawal, the V1 half of payment-link identifier uniqueness, and the whole `paymentLinkOrderId` surface (provider-order store, port, both checkout callers). Gave the public storefront `products` projection a V2-only rule — active, unexpired `REUSABLE` Commerce V2 link — syncing the storefront spec boundary line and the nautt `AGENTS.md` attach line. No schema, migration, or webhook change.

## Evidence

- [[pop/specs/storefront-and-customization]] — *records the V2-only `products` membership rule*.
