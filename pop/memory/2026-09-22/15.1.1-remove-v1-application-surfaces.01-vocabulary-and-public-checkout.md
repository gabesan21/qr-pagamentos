---
task: 15.1.1-remove-v1-application-surfaces
entry: 01-vocabulary-and-public-checkout
---

# Shared vocabulary relocated, public checkout V1 surface deleted

F01 relocated 7 V2 modules and 2 unnamed V1 tests that shared vocabulary with the deleted surfaces (accepted `Owns` deviation, confirmed by `pop_check_scope.py`). F02 deleted the public checkout V1 surface in parallel with F03. `PublicCheckoutForm`/its test were **not** deleted: `createPollingController` is the frozen contract from 14.6.1, still consumed by the V2 `standalone-payment-experience.tsx`, so the component stays a dead export by design, not an oversight.

## Evidence

- [[src/app/pay/[identifier]/checkout-experience.tsx]] — *the surviving V2-only checkout page branch*.
