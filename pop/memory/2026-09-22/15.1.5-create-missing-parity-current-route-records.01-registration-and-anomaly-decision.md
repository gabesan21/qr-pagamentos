---
task: 15.1.5-create-missing-parity-current-route-records
entry: 01-registration-and-anomaly-decision
---

# Three records registered; one pre-existing anomaly accepted, not fixed

F01 first hit BLOCKED before any write: 45/46 existing `current-route` ids reproduce under `obligationId('current-route', routeKind:route)`, but `current-route:86a91d7b9224e135` (`.../products/[id]/loading.tsx`, `e3264d60`) does not, no rename trail. Coordinator accepted it as a named, preserved-intact anomaly instead of widening scope; the reproducibility check now excludes just that id. The three missing records then registered cleanly (round 2).

## Evidence

- [[docs/frontend-template-parity/README.md]] — *records the dispositions and the accepted anomaly*.
