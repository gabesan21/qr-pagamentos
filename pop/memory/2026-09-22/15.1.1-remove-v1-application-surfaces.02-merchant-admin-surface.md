---
task: 15.1.1-remove-v1-application-surfaces
entry: 02-merchant-admin-surface
---

# Merchant/admin V1 surface deleted in two steps

F03 deleted the merchant/admin V1 surfaces (`legacy-order-table.tsx`, `/orders/[id]`, `/admin/orders/[id]`, `order-views.tsx`) in commit `9c8e396b`, then hit a partial BLOCKED: `src/auth/payment-link.ts` still served product/currency-pair catalogs to two live V2 forms. Resolved by the roadmap-anticipated fork: `listActiveProducts`/`listActiveCurrencyPairs` relocated to a new V2-owned module, `payment-link-v2-catalog.ts` (commit `5353eaa3`), completing the deletion without breaking V2.

## Evidence

- [[src/auth/payment-link-v2-catalog.ts]] — *the new V2-owned home for the relocated catalog queries*.
