---
task: 15.1.1-remove-v1-application-surfaces
entry: 04-deferred-findings
---

# Findings reported but deliberately not fixed here

Three out-of-scope items, recorded for follow-up rather than expanding this task: (a) stale prose comments naming already-deleted V1 files in `public-storefront.ts`, both dashboard files and `checkout-form-validation.ts` — nit, no behavior impact; (b) 6 `orders`-domain dictionary keys that look dead but are still asserted by `src/i18n/dictionaries.test.ts` (`adminStateKeys`, outside F04's `Owns`) — kept, test may be stale; (c) evidence specs still list `sourceInventory` for already-deleted `order-views.tsx`/`directory-copy.ts`.

## Evidence

- [[src/i18n/dictionaries.test.ts]] — *the test asserting the possibly-stale `adminStateKeys`*.
