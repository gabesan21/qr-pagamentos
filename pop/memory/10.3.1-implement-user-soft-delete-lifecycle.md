---
task: 10.3.1-implement-user-soft-delete-lifecycle
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: dc8e7b6d951995b7ff8d11d028d21d1b4082aa46
pr:
---

# 10.3.1-implement-user-soft-delete-lifecycle — Implement user soft-delete lifecycle

- **Entrega:** soft-delete terminal: `user.deleted_at` + `status='DISABLED'` com sign-in/principal/`countActiveAdmins` byte-idênticos; `user_deletion` append-only; `deleteUser` via `mutateAdminSafety` (último-admin guard, tx com advisory lock desativa storefront, links V1/V2, audita, revoga sessões); fences terminal status/role/password; rota `POST /admin/users/[id]/delete` (origin-guard, 401/403 vazios, redirects opacos) + 1 literal no request-log; DTO admin `deletedAt` por `toAdminUserDto` dedicado.
- **Áreas alteradas:** `prisma/` (schema, migration, bootstrap, AGENTS) · `src/auth/administration`, `identity` · rota admin delete · `src/observability/server-request-log` · specs admin-foundation, checkout-lifecycle, product-scope · root AGENTS.
- **Verificação:** 003 crítico aprovado rodada 1; 005 full 0 devoluções; `pnpm check` (1285), `db:test` (39), `db:migration-policy` (baseline=19 future=10), `db:contract-check`, `container:contract-check`, tudo exit 0.
- **Decisões duráveis:** `deleted_at`+`DISABLED` único modelo (CHECK `user_status_closed` no baseline); terminal, sem reativação; ids retidos; `toAdminUserDto` dedicado preserva `Principal` byte-idêntico e evita leak; não-admin não vê deleção.
- **Desvios relevantes:** DTO dedicado aceito no 005 como estritamente melhor; nits (`user.update` duplicado, erros genéricos no teste) para passes futuros.
- **Impacto em contratos:** specs: admin-foundation (lifecycle + route-table), checkout-lifecycle (preservação), product-scope (nota) · DOX: root AGENTS + prisma/AGENTS.

## Links

- **Spec afetada:** [[pop/specs/administrative-foundation|administrative-foundation]] — *siga para lifecycle/route-table*.
- **PR/commit:** dc8e7b6 (merge em develop) — *siga para o diff*.
