---
task: 10.3.1-implement-user-soft-delete-lifecycle
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: dc8e7b6d951995b7ff8d11d028d21d1b4082aa46
pr:
---

# 10.3.1-implement-user-soft-delete-lifecycle — Implement user soft-delete lifecycle

- **Entrega:** soft-delete terminal de usuários: `user.deleted_at` nullable + `status='DISABLED'` (sign-in scrypt, principal resolution e `countActiveAdmins` byte-idênticos); tabela append-only `user_deletion` (grants `SELECT`/`INSERT`, bootstrap re-pin relation-guarded); `deleteUser` via `mutateAdminSafety` — guard de último admin, uma tx com advisory lock (marker+DISABLED + storefront disable + `updateMany active=false` V1/V2 + audit row + revogação de sessões, rollback provado); fences de terminalidade em status/role/password; rota `POST /admin/users/[id]/delete` (origin-guard first, 401/403 vazios, redirects opacos) + 1 literal no request-log; DTO admin ganha `deletedAt: Date | null` via `toAdminUserDto` dedicado.
- **Áreas alteradas:** `prisma/` (schema, migration `20260726030000_user_soft_delete`, bootstrap, AGENTS.md) · `src/auth/administration.ts` + `identity.ts` · rota admin delete · `src/observability/server-request-log.ts` · specs administrative-foundation/checkout-and-order-lifecycle/product-scope · AGENTS.md raiz.
- **Verificação:** 003 crítico aprovado rodada 1; 005 full aprovado 0 devoluções — re-runs: `pnpm check` (1285), `db:test` (39), `db:migration-policy` (baseline=19 future=10), `db:contract-check`, `container:contract-check`, tudo exit 0.
- **Telemetria final:** 002: 1 planner · 003: 1 crítico strong, 0 devoluções · 004: 1 executor (3 fronts, 3 commits) · 005: 1 crítico strong full, 0 devoluções.
- **Decisões duráveis:** `deleted_at`+`DISABLED` é o único modelo expressível (CHECK `user_status_closed` está no baseline imutável); terminal, sem reativação; identificadores retidos para sempre, nunca reutilizáveis; `toAdminUserDto` dedicado (não estender `toUserDto` — manteria `Principal` byte-idêntico e reduziria superfície de leak a zero); surfaces públicas/merchant nunca expõem deleção.
- **Desvios relevantes:** DTO dedicado aceito no 005 como estritamente melhor que o plano original; nits (dois `user.update` poderiam ser um; Errors genéricos no teste de rota) ficam para passes futuros.
- **Impacto em contratos:** specs: administrative-foundation (lifecycle + route-table), checkout-and-order-lifecycle (fence de preservação de atribuição), product-scope (nota) · DOX: AGENTS.md raiz + prisma/AGENTS.md.

## Links

- **Spec afetada:** [[pop/specs/administrative-foundation|administrative-foundation]] — *siga para o lifecycle e a route-table*.
- **PR/commit:** dc8e7b6 (merge em develop) — *siga para o diff final*.
