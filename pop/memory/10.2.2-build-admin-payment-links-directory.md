---
task: 10.2.2-build-admin-payment-links-directory
project: pop
started: 2026-07-26
finished: 2026-07-27
commit: 48a1e338d7df769e0b85bb3e8392f8086c0fcfe0
pr:
---

# 10.2.2-build-admin-payment-links-directory — Administrator global payment-links directory

- **Delivery:** Read-only administrator-global V2 payment-links directory: `admin-payment-links-v2` registration over `queryAdministratorDirectory` (ADMIN_GLOBAL, sizes 10/20/50/100 default 50, `(createdAt,id)` keyset, state/type/kind/from/to filters + `q` with exact-identifier or bilingual-description/owner-username containment), rows = owner projection + exactly `{ username, deletedAt }`, `/admin/payment-links/v2/[id]` read-only detail with `/admin/orders?link=` drill-down. V2-only by design: no administrator V1 link projection exists; V1 files/trigger byte-frozen.
- **Areas changed:** `src/auth/payment-link-v2-admin-directory.ts` (+12 contract tests incl. a 25-row fail-closed filter↔`derivePaymentLinkV2State` matrix), `src/app/admin/payment-links/` (directory, query, copy, `v2/[id]` detail + 21 tests), 12 bilingual keys × 2 locales, evidence pair (`--admin-payment-links` mode, run 20260727035740, 48 captures), specs + root AGENTS.md.
- **Verification:** Critical; full 005 critic APPROVE round 1 (zero blocking; four nits — one accepted as a post-merge chore: a stale duplicate AGENTS.md bullet removed in the closeout). Critic re-ran typecheck/test (1534)/lint/evidence `:verify`/contract checks; `admin:source-check` failure list byte-identical to the 120 baseline; word-level diff proved the spec compaction lossless.
- **Final telemetry:** 002 plan; critical 003 approved; two executors (first killed for writing to the main checkout — work salvaged, relaunched with strict path discipline); full critic zero returns; no circuit breaker.
- **Durable decisions:** `pop/specs/administrative-foundation.md` compacted 169→139 lines (unwrap-only plus two stale scaffold sentences) re-establishing the ≤150 budget for the remaining Epoch-10 appends; the local Prisma state-condition mirror is byte-identical to the owner view's private mirror with a fail-closed evaluator; soft-delete honesty proven through the real `POST /admin/users/[id]/delete` route in evidence.
- **Relevant deviations:** Two mid-run rebases onto develop (M-6.1 fix + 10.2.1/9.3.2 closes) with additive conflict resolutions; stale `shellAdminLinksDescription` dictionary key left behind as a noted cleanup candidate (outside minimal write set).
- **Contract impact:** administrative-foundation (10.2.2 amendment + slice + compaction), catalog-and-payment-links/checkout-and-order-lifecycle/product-scope notes, root AGENTS.md bullet — all honored at merge.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the V2-only admin directory boundary and redaction contract.*
- **Commit:** `48a1e338d7df769e0b85bb3e8392f8086c0fcfe0` — *inspect the integrated delivery (merge into develop).*
