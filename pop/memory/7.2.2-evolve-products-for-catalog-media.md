---
task: 7.2.2-evolve-products-for-catalog-media
project: pop
started: 2026-07-24
finished: 2026-07-24
commit: eac54686fcd8ba09b8778b2eaf7cc54d8a7b81a0
pr:
---

# 7.2.2-evolve-products-for-catalog-media — Evolve products for catalog media

- **Delivery:** Products gained nullable currency (gated on active admin-registered mappings), nullable image through the canonical media boundary, and one-way terminal archival with edit lock — all additive, with V1 payment-link/order/checkout semantics byte-stable.
- **Areas changed:** migration `20260724220000_product_catalog_media` (`currency_code` AAA–ZZZ bounds, `image_media_id` varchar(43) no FK, `archived_at`), `src/auth/product.ts` (gating, image lifecycle, archive CAS), `src/app/products/route.ts`, focused service/route/db tests, catalog spec and root/prisma DOX.
- **Verification:** Full critical yolo review APPROVE, round 1. Migration lane green (baseline=19 intact), db:test 34/34, 34 focused tests, `pnpm check` (942 passed), V1 DTO byte-stability via byte-identical projections and unmodified suites, PoP validation valid.
- **Final telemetry:** 002 planner with 1 critical-003 return (archival enforcement boundary); cohesive 004 executor; full 005 critic approved with zero returns; no circuit breaker.
- **Durable decisions:** Archival is one-way `active=false` + `archived_at` in a single CAS — V1 deactivation semantics: new-link enforcement rides the existing baseline trigger and picker filter (never edited); preservation is data-level only (link rows and order snapshots byte-for-byte; checkout of links to archived products becomes unavailable). Currency re-gates only on change; unchanged/cleared values never gate, so products survive later mapping deactivation. `image_media_id` stores the opaque 43-char public identifier (internal UUIDs must never be exposed; activate/orphan are identifier-only). Category reassignment stays state-agnostic and moves archived products.
- **Relevant deviations:** `image_media_id` stores the opaque identifier rather than the internal UUID the plan wording suggested — required by the media contract; accepted by the critic with all lifecycle invariants proven.
- **Contract impact:** Catalog spec and root/prisma DOX synchronized; 7.2.3 owns the management UI over this contract.

## Links

- **Spec:** [[specs/catalog-and-payment-links|Catalog and payment links]] — *follow for product currency, image, and archival semantics.*
- **Commit:** `eac54686fcd8ba09b8778b2eaf7cc54d8a7b81a0` — *inspect the integrated delivery.*
