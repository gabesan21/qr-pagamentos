---
task: 2.1.2-onboard-nautt-key-and-wallet-balance
project: applications/qr-pagamentos
started: 2026-07-17
finished: 2026-07-17
commit: f3790b1010b92af0f0f0d877b134613c782a532f
pr:
---

# 2.1.2-onboard-nautt-key-and-wallet-balance — Onboard Nautt key and wallet balance

Delivered authenticated bilingual Nautt onboarding on `develop`. A forward migration adds a unique UUID credential revision so validate-before-save, ciphertext replacement, webhook registration, and wallet reads stay pinned to the same winning credential under concurrent requests. The server validates the owner key against the main-wallet balance before persistence, derives owner/locale/callback server-side, redacts provider failures, and preserves conservative `UNREGISTERED`, `REGISTERING`, `ACTIVE`, and `INDETERMINATE` recovery behavior. The home surface now presents key entry, configured balance, unavailable/manual retry, setup-changed, recovery-required, loading, and disabled states in `pt-BR` and `en` without exposing the key.

Verification passed migration backfill/ACL contracts, deterministic same-timestamp interleaving, installer/container contracts, PoP/DOX validation, visual/WCAG evidence at 375/768/1440 in light/dark themes, and `pnpm check` (444 passed, 1 skipped, production build passed). The first critical 005 review returned a mocked pending specimen; repair `f79d541` exercised live submissions with spinner, localized label, `aria-busy`, scoped disabling, and a single POST under double-click. Task commits were merged locally into `develop` as `f3790b1010b92af0f0f0d877b134613c782a532f`; remote push was policy-rejected.

- **Affected spec:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *follow when changing credential onboarding, revision claims, wallet balance, callback configuration, or recovery UI.*
