---
task: 2.3.2-recover-nautt-webhook-deliveries
project: applications/qr-pagamentos
started: 2026-07-17
finished: 2026-07-18
commit: bc5a9bb965d9ec5e3f93989219d260e46363266e
pr:
---

# 2.3.2-recover-nautt-webhook-deliveries — Recover Nautt webhook deliveries

Delivered explicit server-only recovery for permanently failed Nautt deliveries on `develop`. A strict injected history port exposes only the eight delivery fields evidenced by the supplied Nautt material; the service makes no unsupported HTTP envelope or pagination claim. Recovery resolves trusted owner/local-order identity, stops known specific UUIDs before lease or side effects, bounds list mode to 128 records, owns a cancelable 10-second port deadline, and makes late resolution/rejection inert.

A forward PostgreSQL migration adds typed `RECOVERY` evidence and a fenced 30-second owner/order lease around a 25-second accepted-work budget. Recovery evidence never fabricates an intake HMAC digest; collisions preserve existing delivery and ordered-attempt tuples exactly. Terminal orders import evidence with zero authoritative reads, while every actionable batch shares at most one owner-key order GET and the existing versioned CAS. Verification proved the 9.999/19.999/25.000-second success timeline, 30-second reclaim, stale-token fencing, atomic rollback, collision immutability, DML-only ACL, and N-to-one economics. `pnpm db:test`, database contracts, `pnpm check` (502 passed, 4 skipped), build, PoP, fresh verification, and critical yolo review passed. The branch was integrated and pushed to `develop` as `bc5a9bb` without a task PR.

The exact Nautt delivery-history HTTP response contract remains undocumented in the supplied research, so production wire decoding stays intentionally outside this task and the domain operation requires an injected normalized port.

- **Affected spec:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *follow when changing delivery-history recovery, normalized port decoding, recovery evidence, or lease economics.*
