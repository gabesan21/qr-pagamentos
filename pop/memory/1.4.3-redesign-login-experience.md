---
task: 1.4.3-redesign-login-experience
project: applications/qr-pagamentos
started: 2026-07-16
finished: 2026-07-17
commit: fd71e1f
pr:
---

# 1.4.3-redesign-login-experience — Redesign the login experience

Recomposed the unprefixed `/login` presentation exclusively from the owned
shadcn inventory — credential `Card`, `Field`/`Input` with labels above native
autofill-enabled controls, destructive `Alert` for the generic
invalid-credential recovery, and a page-local `Button`+`Spinner` submit control
— preserving the `/login/submit` POST contract, opaque failures, bilingual
dictionaries, and persisted-locale behavior. Added focused contract tests,
including a static source-import inventory permitting only the six named owned
sources, plus a dedicated production Playwright evidence runner with eight
hash-bound light/dark responsive captures, keyboard-traversal, 44px target,
label/autofill, recovery-alert, and axe assertions, verified by a manifest-bound
review. Independent verification and the aggregate gate passed; the yolo critic
integrated the task locally into `develop` without a PR. One severity-2 advisory
follow-up remains for the phase backlog: the plan-mandated `useFormStatus`
wiring cannot fire pending in production with a native string form action
(repair requires a client-driven pending state and deviates from this task's
approved plan).

- **Specs affected:** [[pop/specs/administrative-foundation|Administrative foundation]] and [[pop/specs/administrative-design-system|Administrative design system]] — *follow for the preserved sign-in/i18n boundary and the login consumer/evidence contract.*
