---
task: 14.7.2-phase-verification
entry: 01-realignment-and-bem-pin
---

# The realignment that did not weaken, and the pin that had to bite

`tests/login.evidence.spec.ts:169-175` stopped selecting the brand by `.auth-brand` and scopes it to `[data-slot="card-header"]`. Proof of identity: the `measured` blocks are byte-identical to the prior baseline at all eight captures. Zero `.skip`/`.todo` added. The new pin failed round 1: a trailing lookahead made `settings-surface__` unmatchable and truncated every family to its block name; the 900px assertion never read the elements. The directed repair moved it to family-prefix matching over className/CSS tokens and a balanced-brace parse of the block.

## Evidence

- [[src/app/globals-bem-retirement.test.ts]] — *the pin as it now stands*.
