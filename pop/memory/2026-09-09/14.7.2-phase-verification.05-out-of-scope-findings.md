---
task: 14.7.2-phase-verification
entry: 05-out-of-scope-findings
---

# Two failing scripts, correctly reported instead of repaired

`scripts/verify-brand-assets-evidence.mjs:87` hardcodes 17 sources / 28 derivatives against a manifest that has held 14/25 since `12.2.2` (`a674ade7`). `scripts/check-admin-ui-inventory.mjs`'s `allowedClasses` never covered `src/app-shell/app-shell.tsx`'s rail classNames, introduced by `14.4.1` (`c7d1a746`); 14.7.1 F03 pruned that allowlist but it never held those entries, so the gap predates Phase 14.7. Both fail outside `pnpm check`, so neither blocks the gate or the PR. Left untouched under the fallout-only rule, carried into the PR follow-ups.

## Evidence

- [[scripts/check-admin-ui-inventory.mjs]] — *the allowlist that never grew rails*.
