---
task: 14.7.2-phase-verification
entry: 03-gate-result
---

# The four gates, in numbers the judge re-ran

From the worktree with direct binaries, never `pnpm <script>` and never a container: `vitest run` **2228 passed / 0 failed / 28 skipped** across 269 files against the 2197 floor from 14.5.4 — passing grew, skipped held; `tsc --noEmit` 0; the lint chain 0 errors with 62 pre-existing warnings; `next build` 0. The judge reproduced the suite, the typecheck, the lint chain and both evidence verifiers independently, and confirmed the source-only diff is empty: this task changed no application behaviour. 14.6.3's qualified `2169+43` accounting is superseded.

## Evidence

- [[14.7.2-phase-verification.verify]] — *the reproduced counts*.
