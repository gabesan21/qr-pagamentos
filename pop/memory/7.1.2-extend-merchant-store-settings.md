---
task: 7.1.2-extend-merchant-store-settings
project: pop
started: 2026-07-24
finished: 2026-07-24
commit: b1dcaecf5d8c7d41cc6ee9e59d3c767e8e12c586
pr:
---

# 7.1.2-extend-merchant-store-settings — Extend merchant store settings

- **Delivery:** Persisted store theme (closed six-theme set), layout, logo (media lifecycle), standalone-payments toggle, and gated default currency on top of the existing slug/display-name/accent storefront-settings save, with absent=unchanged merge semantics and a redacted public projection.
- **Areas changed:** additive migration `20260724210000_store_settings_extension` (five `app.user` columns), media `activateOwned`/`orphanOwned` owner/purpose-fenced ops, storefront settings service + route, `src/design-system/themes.ts` single-source export, public store projection, storefront/media/catalog specs, root and prisma AGENTS.md.
- **Verification:** Full critical yolo review APPROVE, round 1. Migration lane (db:generate, contract-check, disposable db:test 30/30), 40 focused tests, `pnpm check` exit 0, media decoder preflight, scope/diff, and PoP validation passed.
- **Final telemetry:** 002 planner with 1 critical-003 return (regex CHECK → AAA–ZZZ bounds); cohesive 004 executor; full 005 critic approved with zero returns; no circuit breaker.
- **Durable decisions:** Currency assignment gates on `requireActivePair` (typed unavailable → opaque `?storefront=failed`); the stored code survives later mapping deactivation for reads. Logo replace is activate-new → save → orphan-old with compensating orphan on save failure; no DB FK on the logo identifier. Absent fields are unchanged on save, so the legacy dashboard form cannot wipe new fields; explicit blank clears. The public projection exposes only resolved theme/layout + logo identifier — never currency, toggles, or owner data. Re-submitting an unchanged stored currency re-gates on the registry (7.1.3 should omit unchanged fields).
- **Relevant deviations:** None.
- **Contract impact:** Storefront/media/catalog specs and root/prisma DOX synchronized; 7.1.3 owns the UI over this contract.

## Links

- **Spec:** [[specs/storefront-and-customization|Storefront and customization]] — *follow for the store settings persistence and projection contract.*
- **Commit:** `b1dcaecf5d8c7d41cc6ee9e59d3c767e8e12c586` — *inspect the integrated delivery.*
