---
task: 12.2.4-rebuild-design-system-specimen
project: applications/qr-pagamentos
date: 2026-08-03
---

# 12.2.4.02 — Evidence and parity binding

Updated the evidence runner, coverage checker, and verifier to bind every fixture state and protect against drift.

## Changed

- `tests/design-system.evidence.spec.ts` — `captureRenderedCoverage` records per-binding `semanticWitness`; `probeActionColors` runs for all six themes and persists `actionContrast` in every assertion record; deterministic cleanup before screenshots.
- `scripts/check-design-system-coverage.mjs` — validates rendered witnesses and action-contrast fields against the current evidence manifest.
- `scripts/verify-design-system-evidence.mjs` — checks `semanticWitness`, `actionContrast`, operated-binding set, source hashes, matrix, and deterministic repeat.
- `scripts/verify-design-system-evidence.test.ts` — one temporary directory per test with `afterEach` cleanup and `afterAll` sweep of `/tmp/design-system-evidence-*`; mutation tests for witnesses and action contrast.

## Note

A fresh 48-capture evidence run was not produced because the user requested to leave tests aside. The previous run `20260803193353` remains in `artifacts/design-system/current.json` but predates the final F01/F02 source changes.
