---
task: 12.2.4-rebuild-design-system-specimen
entry: 01-state-semantic-fixtures
---

# F01 — State-semantic fixtures

Rebuilt the server-first `/design-system` route with a narrow client island so every applicable state maps to a real DOM fixture: per-state directory specimens, controlled CopyField/Modal/ConfirmDialog/toast/OTP fixtures, persistent hover/focus via a CSS module, and an executable 20-entry coverage map (101 applicable states, 72 explicit N/A). Each binding carries a `semanticWitness` (`data-probe`, `aria-busy`, `data-state`…); no shared owner, token, route or dictionary was edited.

## Evidence

- [[src/app/design-system/page.tsx]] — *the rebuilt server-side specimen sections*.
- [[src/app/design-system/coverage.ts]] — *the executable coverage map*.
