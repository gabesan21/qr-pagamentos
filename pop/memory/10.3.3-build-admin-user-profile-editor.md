---
task: 10.3.3-build-admin-user-profile-editor
project: pop
started: 2026-07-26
finished: 2026-07-27
commit: d06fa5546fb3268f4966be88a384aba9ee5b5d07
pr:
---

# 10.3.3-build-admin-user-profile-editor — Administrator profile editor

- **Delivery:** `/admin/accounts/[id]` extended in place into the bilingual administrator profile editor for non-deleted users: facts card plus editor cards for identity (expected-version CAS), access (re-housed byte-frozen role/status/password routes), locale, checkout policy and storefront, plus the delivered delete form; deleted users get only the facts card with its localized non-color badge. The four new POSTs are origin-guarded, re-authorized, and answer only empty `401`/`403` or opaque `?editor=` notices.
- **Areas changed:** the page, the four new `/admin/users/[id]/*` routes, `src/auth/admin-user-profile.ts`, the directory/storefront services, `server-request-log.ts`, and the 48-capture evidence run 20260727110719.
- **Verification:** critical; 005 critic APPROVE on round 3 after two returns (evidence rebinding, spec overlap with 10.4.1, route redaction); `pnpm check` green on develop after merge.
- **Final telemetry:** 002 plan; critical yolo; one executor; two critic returns then APPROVE.
- **Durable decisions:** the branch predated 10.4.1 — the merge kept develop's settings-hub and default-theme spec paragraphs and re-applied the 10.3.3 paragraph and slice (spec 148/150 lines); the four new request-log keys coexist with `adminSettingsDefaultTheme`.
- **Relevant deviations:** the merge manually resolved `AGENTS.md`, the spec and `server-request-log.ts` without behavioral conflict; the card reached `006_done` before the closing merge commit.
- **Contract impact:** specs: administrative-foundation · DOX: root `AGENTS.md`.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the editor contract and redaction rules.*
- **Commit:** `d06fa55` — *inspect the merged delivery.*
