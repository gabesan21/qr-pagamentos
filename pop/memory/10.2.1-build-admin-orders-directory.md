---
task: 10.2.1-build-admin-orders-directory
project: pop
started: 2026-07-26
finished: 2026-07-27
commit: bbc7b63972c95d38bff1b2ed57832a51ba716242
pr:
---

# 10.2.1-build-admin-orders-directory — Administrator global orders directory

- **Delivery:** Read-only administrator-global Commerce V2 order directory: `admin-order-v2` registration over the delivered `queryAdministratorDirectory`/`ADMIN_GLOBAL` foundation (sizes 10/20/50/100 default 50, `(createdAt, id)` keyset, source/money/date/link filters, payer-text + local/provider-UUID `q`), rows reusing `toOrderV2Summary` plus owner attribution `{ username, deletedAt }` with interim `/admin/accounts` navigation and a localized non-color deleted badge, and the `/admin/orders/v2/[id]` read-only detail with one opaque unavailable outcome. V1 ledger byte-frozen below.
- **Areas changed:** `src/orders/order-v2-admin-directory.ts` (+test), `src/app/admin/orders/` (page, directory-query, directory-copy, `v2/[id]` detail + tests), 8 bilingual keys × 2 locales, evidence pair (`tests/admin-orders.evidence.spec.ts`, run/verify scripts, `--admin-orders` mode in the shared harness, 48 captures run 20260727030000), append-only spec amendments, root AGENTS.md bullet.
- **Verification:** Critical; full 005 critic APPROVE round 1 with zero blocking findings (three nits). Critic re-ran everything: typecheck/lint/test (1429)/build, `pnpm db:test` (15/15), evidence `:verify` — all exit 0. Byte-level absence of every frozen file confirmed; role/status re-authorization at three layers; soft-deleted owners proven end-to-end through the real `POST /admin/users/[id]/delete` (`/admin?success=deleted`).
- **Final telemetry:** 002 plan; critical 003 approved; one executor; evidence initially BLOCKED by a delivered defect (see below), resumed after the fix merged; full critic zero returns; no circuit breaker.
- **Durable decisions:** The evidence harness surfaced a real production defect — `withAuthorizationLock` (`src/auth/administration.ts`) used `$queryRaw` on the void advisory lock, breaking every locked admin mutation under the Prisma 7.8.0 pg adapter (P2010). Reported without improvising; originated modification **M-6.1** (`$executeRaw` + regression tests), whose merge unblocked this task's evidence. Administrator directory precedent: V2-only, derived read-time values, no admin mutation surface, V1 data never read.
- **Relevant deviations:** Post-merge fixes within Owns (owner-cell ghost Button-link for the 44px target, pagination distinctness on detail hrefs, comment rewording for token lint).
- **Contract impact:** `pop/specs/administrative-foundation.md` (directory paragraph + payer/deleted-owner amendments + slice), `checkout-and-order-lifecycle.md`, `product-scope.md`, root AGENTS.md bullet — all append-only.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the admin global-read scope and redaction contract.*
- **Commit:** `bbc7b63972c95d38bff1b2ed57832a51ba716242` — *inspect the integrated delivery (merge into develop).*
