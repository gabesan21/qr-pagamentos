# 12.5.3-remodel-merchant-payment-links — Aprovação no 005_closing (2ª rodada)

- **Data:** 2026-08-05
- **Gate:** 005_closing — Judge Dredd
- **Veredito:** aprovado para integração

## Critérios avaliados

| # | Critério | Resultado |
|---|----------|-----------|
| 1 | Diretório `/links` segue template e mantém estados derivados V2, filtros, busca e legado V1 | ✅ |
| 2 | Detalhe, sumário, badges, timeline e indisponíveis seguem template e preservam redação | ✅ |
| 3 | Criação `/links/new` segue template e preserva kind/type/pair e editor de linhas | ✅ |
| 4 | Edição segue template, preserva CAS, dirty-field omission e fork de nova versão | ✅ |
| 5 | Lifecycle e compartilhamento seguem template e preservam confirmação/redação | ✅ |
| 6 | Drill-down de pedidos segue template e mantém vínculo forçado ao identificador do link | ✅ |
| 7 | Nenhum serviço, rota, schema ou fronteira de redação alterada | ✅ |
| 8 | Componentes compartilhados são usados por todas as páginas merchant de links | ✅ |

## Evidências

- Diff integrado restrito a `src/app/(merchant)/links/**`, componentes declarados no plano (`link-v2-views.tsx`, `link-v2-form.tsx`, `link-lines-editor.tsx`, `link-v2-actions.tsx`, `share-copy.tsx`, `dirty-input.tsx`) e `src/i18n/dictionaries/payment-links-directory/**`.
- `git status` de `src/app/(merchant)/orders/**`, `src/app/orders/**`, `src/app/store/[slug]/**`, `src/app/globals.css` e `src/i18n/dictionaries/orders-directory/**` retorna vazio — invasões de ownership da 1ª rodada foram descartadas.
- `src/components/ui/input-otp.test.tsx` não consta no diff; erro pré-existente e fora do escopo.
- Checks reportados pelo executor: `pnpm lint` 0 errors / 57 warnings; `pnpm typecheck` pass; `pnpm build` pass; `pnpm test -- --run` 1980 passed / 28 skipped (exit code 1 por unhandled error pré-existente).

## Achados

- Nenhum achado bloqueante.
- Sugestão não bloqueante: `OrdersSummaryCard` em `v2/[id]/page.tsx` usa valores estáticos para `confirmed` e `volume`; o escopo atual não exige analytics reais, mas um follow-up futuro pode enriquecer esses dados.

## Decisão

Task aprovada. O delta da devolução anterior foi integralmente endereçado e o escopo permanece dentro dos contratos linkados.
