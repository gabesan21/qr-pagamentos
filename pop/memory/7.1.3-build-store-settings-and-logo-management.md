---
task: 7.1.3-build-store-settings-and-logo-management
project: pop
started: 2026-07-24
finished: 2026-07-24
commit: 6e4c7a98d01ff707cbc760c39ed3e018bf4c4164
pr:
---

# 7.1.3-build-store-settings-and-logo-management — Build store settings and logo management

- **Delivery:** Bilingual sectioned store settings workspace (Identity / Appearance with live theme preview / Payments / Currency) in the merchant `/settings` route, with logo upload through a guarded staging route, official brand fallback, and a disabled-with-explanation currency control when no active mapping exists.
- **Areas changed:** `src/app/storefront-settings-management.tsx` + preview/form-preparation/loading companions, `POST /storefront/logo` staging route, per-theme `[data-theme-preview]` token blocks with the color-alias layer, pt-BR/en dictionaries, `store-settings:evidence` pair, DESIGN.md, root AGENTS.md, storefront spec.
- **Verification:** Full critical yolo review APPROVE, round 1. `pnpm check` (967 passed), tokens:check + token tests, 62 focused tests, evidence verify with own-vision screenshot review, PoP validation — all green; write-set guards held with zero forbidden diffs.
- **Final telemetry:** 002 planner with 1 critical-003 return (dirty-field omission + real disabled-state source); cohesive 004 executor; full 005 critic approved with zero returns; no circuit breaker.
- **Durable decisions:** Dirty-field omission via the native `formdata` event deletes extended fields equal to prefilled values before submission, so a save with an unchanged (since-deactivated) currency succeeds while a changed one still gates; emptied logo still submits (explicit-empty clears). The currency select derives its disabled state from an empty `listActiveChoices` array — a normal explained empty state, never a thrown render path. Logo uploads stage via `POST /storefront/logo` (origin-guard first, re-authorized, opaque outcomes, one request-log entry); the save activates/orphans through 7.1.2; staged-never-saved uploads fall to media reconciliation. Theme previews use additive `[data-theme-preview]` blocks byte-identical to `:root` colors plus the re-declared alias layer (aliases resolve where declared).
- **Relevant deviations:** Evidence spec seeds/deactivates the BRL mapping via the admin mutation through browser-native fetch; `scripts/run-admin-evidence.mjs` gained the `--store-settings` runner flag — both within established conventions. Two severity-1 nits carried: live `no-img-element` warnings (precedent uses explicit eslint-disable) and one stale DESIGN.md sentence about the removed 5.1.1 storefront card.
- **Contract impact:** Root AGENTS.md storefront bullet and the storefront spec synchronized; Phase 7.1 complete.

## Links

- **Spec:** [[specs/storefront-and-customization|Storefront and customization]] — *follow for the store settings UI and logo management contract.*
- **Commit:** `6e4c7a98d01ff707cbc760c39ed3e018bf4c4164` — *inspect the integrated delivery.*
