---
task: 8.4.2-build-merchant-dashboard-ui
project: pop
started: 2026-07-25
finished: 2026-07-25
commit: 74ae82e7f2b8ec42d7e30c59bca1d41fae48e340
pr:
---

# 8.4.2-build-merchant-dashboard-ui — Build merchant dashboard UI

- **Entrega:** dashboard bilíngue do merchant em `/`: composição server-rendered read-only sobre `merchant-analytics.getForOwner` + storefront settings em paralelo; `?period=` fechado (today/7d/30d, default 7d, GET links com `aria-current` não-cor); vendas confirmadas vs localmente finalizadas em grupos separados (nunca somadas); rates decimais exatos com n/a; View Store só quando `storefrontEnabled && slug` → `/store/[slug]`; empty states por seção + skeletons; domínio i18n `merchant-dashboard`; par de evidence (47 captures).
- **Áreas alteradas:** `src/app/(merchant)/{page,dashboard,loading}.tsx` + testes · dicionários · bloco token-only em globals.css · evidence pair + harness mode · DESIGN.md/AGENTS.md/spec.
- **Verificação:** `pnpm check` verde + `merchant-dashboard:evidence`/`:verify` verdes; re-run do revisor (differential); projeção 8.4.1 byte-unchanged.
- **Telemetria final:** 002: 1 planner · 004: 1 executor (2 commits) · 005: 1 crítico differential, 0 devoluções.
- **Decisões duráveis:** sem trend charts (DTO sem buckets temporais — abort no plano em vez de patch na projeção); sem rotas novas.
- **Desvios relevantes:** chamada bare de storefront getForOwner (linha ausente impossível pós principal); modo `--merchant-dashboard` no harness compartilhado (padrão estabelecido).
- **Impacto em contratos:** specs: implemented-slice 8.4.2 · DOX: AGENTS.md (consumidor único do analytics + linha do dashboard) + DESIGN.md.

## Links

- **Spec afetada:** [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]] — *siga para as definições de analytics consumidas*.
- **PR/commit:** 74ae82e7f2b8ec42d7e30c59bca1d41fae48e340 (merge em develop) — *siga para o diff final*.
