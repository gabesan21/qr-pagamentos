---
task: 2.1.3-register-owner-webhooks
project: applications/qr-pagamentos
started: 2026-07-17
finished: 2026-07-17
commit: 7198dd946069ec6b4bf03d3989e07f4e3971adc7
pr:
---

# 2.1.3-register-owner-webhooks — Register owner webhooks

Delivered an owner-scoped Nautt webhook registration boundary on `develop`. A forward migration adds constrained `UNREGISTERED`, `REGISTERING`, `ACTIVE`, and `INDETERMINATE` states plus encrypted one-time-secret storage. `src/integrations/nautt/` now owns the HTTPS-only provider adapter and the orchestration service; an atomic database claim prevents concurrent duplicate registration, API-key replacement is blocked outside `UNREGISTERED`, and every outcome after dispatch that cannot prove a usable registration remains indeterminate with no automatic retry. Public results and errors are redacted. The root and integration DOX contracts were updated.

Verification passed `pnpm db:test`, `pnpm db:contract-check`, focused transition tests, `python3 pop/scripts/pop_validate.py --standalone`, and `pnpm check` (249 passed, 1 skipped, production build passed). The independent verifier and yolo critic reported no blocking findings. Task commit `9270215ee83b893c01ae904bf44305fc711dbfa4` was merged locally into `develop` as `7198dd946069ec6b4bf03d3989e07f4e3971adc7`; remote push was blocked by the environment's export policy.

- **Affected spec:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *follow when changing credential replacement, webhook registration, secret handling, or lifecycle recovery.*

