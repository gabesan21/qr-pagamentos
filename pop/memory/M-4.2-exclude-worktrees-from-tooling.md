---
task: M-4.2-exclude-worktrees-from-tooling
project: pop
started: 2026-07-25
finished: 2026-07-25
commit: 5a738db690a2738de460d49dc942cc3e3af1aa0e
pr:
---

# M-4.2-exclude-worktrees-from-tooling — Exclude task worktrees from tooling

- **Delivery:** `tsconfig.json` now excludes `pop/worktrees` (matching the M-4.1 Vitest exclusion and the pre-existing ESLint ignore), and `scripts/check-epoch1-source.mjs` gained a regression guard pinning that exclusion.
- **Areas changed:** `tsconfig.json`, `scripts/check-epoch1-source.mjs`.
- **Verification:** Differential yolo review APPROVE, round 1. Mutation probe proved the guard fires if the exclusion is removed and that broken TypeScript under `pop/worktrees/` is invisible with it; `pnpm check` green both with and without a synthetic worktree; `git ls-files pop/worktrees` shows only `.gitkeep`.
- **Final telemetry:** 002 planner (leak scoped to tsconfig only); single 004 executor; differential 005 critic approved with zero returns.
- **Durable decisions:** Only `tsconfig.json` leaked; ESLint, Vitest, repo scripts, and pop_validate were already clean. The epoch1 source-check guard mirrors the existing ESLint-ignore guard style.
- **Relevant deviations:** None. The pre-existing `epoch1:source-check` ENOENT found during execution became sibling task M-4.3.
- **Contract impact:** None — tooling configuration only.

## Links

- **Commit:** `5a738db690a2738de460d49dc942cc3e3af1aa0e` — *inspect the integrated delivery.*
