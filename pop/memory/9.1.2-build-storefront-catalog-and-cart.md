---
task: 9.1.2-build-storefront-catalog-and-cart
project: pop
started: 2026-07-26
finished: 2026-07-26
commit: a9ca21fd9a4da1931e805880aeda7eea779bb66e
pr:
---

# 9.1.2-build-storefront-catalog-and-cart — Build storefront catalog and cart

- **Entrega:** página sessionless `/store/[slug]` reconstruída como catálogo agrupado e brandeado (layouts boxed/table por settings, tema via `data-theme-preview`, logo/fallback) + cart browser-local versionado por slug (`qr-pagamentos:storefront-cart:v1:<slug>`, envelope v1 com reference+quantity/amount, reconciliação de stale na hidratação com um notice) + totais exatos BigInt 6 casas por moeda, nunca somados cross-currency; item standalone custom-amount; quantidades 1–9.999; zero controle de checkout (9.1.3); novo par `storefront:evidence`/`:verify` (run 20260726031015, 55 captures hash-bound, axe limpo nos 6 temas).
- **Áreas alteradas:** `src/storefront/public-storefront.ts` (+`standalonePaymentCurrencyCode`) · novo `src/storefront/cart.ts` puro · `src/app/store/[slug]/` (page/experience/error/loading) · dicionários (19 chaves ×2 locales) · `globals.css` (bloco storefront) · harness evidence (`tests/storefront.evidence.spec.ts`, `scripts/run|verify-storefront-evidence.mjs`, modo `--storefront`) · spec storefront + AGENTS.md raiz + DESIGN.md.
- **Verificação:** 005 differential aprovado 0 devoluções — re-runs `pnpm check` (1234 testes) e `storefront:evidence:verify` exit 0; `db:test` auditado (zero arquivos prisma no diff); ownership limpo (nada de rate-limit/request-log/rotas sessionless; V1 `products` byte-frozen).
- **Telemetria final:** 002: 1 planner · 004: 1 executor (4 fronts sequenciais, 6 commits) · 005: 1 crítico strong differential, 0 devoluções.
- **Decisões duráveis:** catálogo agrupado substitui a listagem V1 na página (membro DTO V1 byte-frozen); cart é 100% browser-local e reconciliado contra snapshot do servidor; totais exatos em módulo puro client-safe (BigInt, nunca `Number`); `standalonePaymentCurrencyCode` como emenda única de redação da task.
- **Desvios relevantes:** nenhum ao plano; 2 bugs de evidence-spec corrigidos no F03 + 1 bug real de contraste dark-theme achado pelo axe (surface full-bleed); nits do 005 (fallback morto em cart.ts, gitHead do manifest, filtro `-logo` do verifier) ficam para passes futuros.
- **Impacto em contratos:** specs: storefront-and-customization (slice 9.1.2 + emenda) · DOX: AGENTS.md raiz (`/store/[slug]`) + DESIGN.md.

## Links

- **Spec afetada:** [[pop/specs/storefront-and-customization|storefront-and-customization]] — *siga para o slice 9.1.2 e o contrato durável do cart*.
- **PR/commit:** a9ca21f (merge em develop) — *siga para o diff final*.
