---
task: 15.4.3-phase-verification
entry: 02-out-of-owns-dictionary-debt-repair
---

# Out-of-owns dictionary-debt repair

`tsc --noEmit` failed on `tests/merchant-dashboard.evidence.spec.ts`, which referenced two dictionary keys 15.4.2 retired. `owns` was widened pointwise to this one file (coordinator decision, same pattern as 15.2.4 round 2): `dictionary.merchantDashboardSalesEmpty` → `dictionary.merchantDashboardNoSales` (the real key `dashboard.tsx` uses), and the dead `dictionary.merchantDashboardLinksEmpty` assertion removed outright (no current equivalent). `tsc --noEmit` exit 0 and `vitest run` 2179 passed/28 skipped confirmed afterward.

## Evidence

- Commit `2d92f612`.
- [[tests/merchant-dashboard.evidence.spec.ts]] — the repaired assertions.
