---
task: 15.3.2-checkout-customer-block-only-when-required
entry: 01-conditional-customer-block
---

# Conditional customer block (F01)

`public-checkout-v2-form.tsx` now renders the customer heading, `role="status"` notice and field group only when the policy requires fields (template `needsForm` split); pay action and privacy line keep their position. `standalone-payment-experience.tsx` drops only the redundant `NONE` notice line. `checkoutNoCustomerData` is deleted from both dictionaries after a repo-wide grep found no other reference. Both test files gain markup assertions for `NONE` and a with-fields policy.

## Evidence

- [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]] — policy field set, unchanged.
- Commit dbf8a815 (PR #22).
