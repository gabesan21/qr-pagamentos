---
task: 15.3.2-checkout-customer-block-only-when-required
entry: 01-conditional-customer-block
---

# Conditional customer block (F01)

`public-checkout-v2-form.tsx` now gates the customer heading, `role="status"` notice, and field group on the policy actually requiring fields, mirroring the template's `needsForm` split; the pay action and privacy line keep their exact position. `standalone-payment-experience.tsx` drops only the redundant `NONE` notice line, leaving heading/amount field/footer untouched. `checkoutNoCustomerData` is deleted from both dictionaries after a repo-wide grep confirmed zero remaining references outside `pop/`. Both test files gain markup assertions for `NONE` and a with-fields policy.

## Evidence

- [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]] — policy-required field set this diff implements without changing.
- Commit dbf8a815 (PR #22).
