---
task: 15.4.2-fix-target-sizes-and-dictionary-debt
entry: 02-dictionary-debt
---

# 117 unreferenced dictionary keys retired, product count pluralized

Confirmed each of the 143 keys flagged by 15.1.4's F4 with a two-stage literal + dynamic-access search; 117 were genuinely unreferenced (incl. the dead `merchantDashboardSalesEmpty`) and removed symmetrically across `en`/`pt-BR` in 27 files, while keys reachable only via the live `designSystemCopy${state}` template literal were retained. The `{count} products` copy now selects an `Intl.PluralRules` one/other pair, not a hardcoded plural noun.

## Evidence

- [[src/i18n/dictionaries/domains.ts]] — *per-domain key ownership the removal respected*.
- [[src/i18n/dictionaries.test.ts]] — *key-parity test the removal keeps green*.
