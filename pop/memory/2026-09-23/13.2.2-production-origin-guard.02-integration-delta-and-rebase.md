---
task: 13.2.2-production-origin-guard
entry: 02-integration-delta-and-rebase
---

# Integration delta and rebase before merge

`pop_check_scope` caught `install/lib-operations.sh` outside `owns`: reverted, classifier inlined per script in its `node -e` idiom; also fixed a stale line ref, reused `readSecret`, corrected `install/.env.example`. The branch was rebased onto `origin/develop` after #31 so PR #32 carried only code (no card-path conflict).

## Evidence

- `install/install.sh` · `install/update.sh` · `install/restore.sh` — *inlined `origin_is_loopback_http`*.
- PR #32 — *squash 9ec162eb*.
