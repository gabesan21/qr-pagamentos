---
task: 15.4.1-migrate-template-vocabulary
entry: 01-three-front-vocabulary-migration
---

# Three fronts migrate the closed vocabulary map, F6 resolved

F01 (merchant/profile, 21 files) and F02 (admin, 14 files) applied the same closed map (`text-muted-foreground`→`text-text-2`, `bg-card`→`bg-surface`, `rounded-lg`→`rounded-card`, `font-[family-name:var(--font-display)]`→`font-display`, `max-w-[var(...)]`→`max-w-app/checkout/auth-form`) with exceptions preserved (`bg-primary`, `max-w-[var(--layout-max)]`, `bg-muted-foreground`). F03 (shell/auth/checkout/public/gate, 12 files) resolved F6: `language-switcher.tsx` composes owned `NativeSelect`, the shell account-panel submit composes owned `Button`, `app-shell__account-panel*` added to the allow list, and three typed/commented gate exemptions repaired `admin:source-check` classification (`--storefront-accent` already allow-listed by `check-design-tokens.mjs`).

## Evidence

- [[src/app/language-preference/language-switcher.tsx]] — *NativeSelect composition (F6)*.
- [[src/app-shell/shell-navigation.tsx]] — *Button composition pattern reused for account-panel submit*.
- [[pop/specs/application-frontend-system|application-frontend-system]] — *vocabulary/gate contract unchanged by this rename*.
