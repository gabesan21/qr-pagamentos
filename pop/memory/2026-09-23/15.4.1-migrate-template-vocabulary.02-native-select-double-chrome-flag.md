---
task: 15.4.1-migrate-template-vocabulary
entry: 02-native-select-double-chrome-flag
---

# Deviation flagged: NativeSelect may double-chrome in language-switcher

`NativeSelect` does not expose a `className` on its inner `<select>`, so composing it in `language-switcher.tsx` may render visible double chrome. Left as-is and flagged for human attention at criterion 8 (rendered-capture review, Docker-exclusive, user-owned) rather than guessed at — no visual regression tooling ran locally.

## Evidence

- [[src/app/language-preference/language-switcher.tsx]] — *composition site to inspect visually*.
