---
task: 15.3.3-checkout-template-and-a11y-convergence
entry: 01-template-and-a11y-convergence
---

# Template and a11y convergence (F01)

`checkout-shell.tsx`, `checkout-payment-views.tsx`, `checkout-privacy-notice.tsx`, `checkout-experience.tsx`, both `loading.tsx` and `public-checkout-v2-page.tsx` now carry a single `aria-live="polite"` region around the state badge, the `INDETERMINATE` line on `text-warning-on-soft`/`bg-warning-soft`, `max-w-checkout`/`font-display`/`bg-accent` in place of ad-hoc forms, a 44 px privacy trigger, and a reduced-motion-gated scroll to the first invalid field. Both `loading.tsx` resolve locale from the `qr_locale` cookie. No retry affordance or `NONE` customer block reintroduced. C2's `INDETERMINATE`/`EmptyState` pairing was already AA-correct via `StatusPill`, confirmed by inspection with no diff needed.

## Evidence

- [[pop/specs/application-frontend-system|application-frontend-system]] — utility vocabulary and AA rules this diff implements.
- Commit 7f46ec9f (PR #23).
