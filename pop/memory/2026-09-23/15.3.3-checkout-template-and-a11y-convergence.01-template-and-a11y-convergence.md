---
task: 15.3.3-checkout-template-and-a11y-convergence
entry: 01-template-and-a11y-convergence
---

# Template and a11y convergence (F01)

Shell, payment views, privacy notice, experience, both `loading.tsx` and v2 page carry one `aria-live="polite"` region on the state badge, the `INDETERMINATE` line on `text-warning-on-soft`/`bg-warning-soft`, `max-w-checkout`/`font-display`/`bg-accent` over ad-hoc forms, a 44 px privacy trigger and reduced-motion-gated scroll to the first invalid field. Both `loading.tsx` read locale from `qr_locale`. No retry or `NONE` block reintroduced. `INDETERMINATE`/`EmptyState` (C2) was already AA via `StatusPill`.

## Evidence

- [[pop/specs/application-frontend-system|application-frontend-system]] — token vocabulary and AA rules.
- Commit 7f46ec9f (PR #23).
