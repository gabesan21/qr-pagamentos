---
task: 15.2.2-fail-closed-on-documented-creation-errors
entry: 02-checkout-services-fail-closed-write
---

# Checkout services' fail-closed write

Both `public-checkout-v2.ts` and `standalone-checkout.ts` mark the attempt `FAILED` and the order `REJECTED` on a creation refusal, revoke the capability, and the replay path answers the same redacted `provider-unavailable` outcome as first submit — no provider body/code/message crosses the HTTP boundary. Unit tests added in both `*.test.ts` siblings; not run (deferred to [[15.2.4-phase-verification]]).

## Evidence

- `src/checkout/public-checkout-v2.ts`, `src/checkout/standalone-checkout.ts` — `FAILED`/`REJECTED` write and redacted replay outcome.
