---
task: 15.4.2-fix-target-sizes-and-dictionary-debt
entry: 03-parity-gate-hygiene
---

# Parity derivation bound to the tracked tree, three stale routes refreshed

`check-frontend-template-parity-contract.mjs` discovers current-tree files via `git ls-files`, not a raw walk, closing 15.1.4's F2: a gitignored `src/generated/prisma` can no longer bake into `obligations.ndjson`. A new `generated-tree-excluded` probe proves it (27/27 green). `--refresh-semantic-contract` repaired the three routes staled by F01/F02 (the two checkout `loading.tsx` routes of 15.3.4's F5, plus `admin/payment-links/page.tsx`) — `sha256` only, count unchanged (2226).

## Evidence

- [[docs/frontend-template-parity/README.md]] — *documents the boundary and probe*.
