---
task: 0.1.1-configurable-nautt-base-url
project: applications/qr-pagamentos
started: 2026-07-20
finished: 2026-07-20
commit: a3a82ae
pr:
---

# 0.1.1-configurable-nautt-base-url — Configurable Nautt base URL

- **Entrega:** a base URL da Nautt saiu da constante hardcoded para a ENV server-only `NAUTT_API_BASE_URL` (opcional), validada como HTTPS absoluto canônico sem credenciais/fragmento; produção segue como default quando unset.
- **Áreas alteradas:** `src/integrations/nautt/` — loader em `config.ts` consumido por chamada pelos três clientes; `install/`, `compose.yaml`, `.env.example` — ENV documentada e validada-se-set; `AGENTS.md` raiz, `src/integrations/nautt/AGENTS.md`, spec — contrato atualizado.
- **Verificação:** `pnpm check` (509 passed, 4 skipped), `install/test.sh`, `container:contract-check` e `db:contract-check` verdes; reexecutados na revisão independente (yolo).
- **Decisões duráveis:** ENV opcional com default de produção (não quebra deploys existentes); mesma classe de config não-secreta de `NAUTT_WEBHOOK_CALLBACK_URL`.
- **Desvios relevantes:** `uninstall.sh` entrou no escopo (allowlist de ENV); nit registrado — barra final no override gera `//` no path; sugestão — ENV inválida no registro de webhook classifica como `INDETERMINATE` (conservador).

## Links

- **Spec afetada:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *siga para conhecer a regra da ENV `NAUTT_API_BASE_URL`*.
- **Commit:** `a3a82ae` (merge) / `ae5e239` (diff) — *siga para inspecionar o diff final*.
