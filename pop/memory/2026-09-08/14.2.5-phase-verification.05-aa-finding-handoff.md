---
task: 14.2.5-phase-verification
entry: 05-aa-finding-handoff
---

# AA defect above the phase's reach

C13 could not run: soft pills draw `text-<tone>` over `bg-<tone>-soft` at 12px and four pairs fall under 4.5:1 — pix-paper `danger` 4.21 and `info` 4.40, cashier-daylight `success` 4.41 and `warning` 4.41 — while the spec puts AA above byte-identical rendering. The tints came from the template in 14.2.1, so the fix is token/generator work outside this front: recorded, not repaired; handed to [[14.3.3-project-aa-soft-tone-foregrounds]]. Bound evidence stays stale.

## Evidência

- [[pop/specs/application-frontend-system|application-frontend-system]] — *the AA precedence and the `k`-step method*.
- [[src/app/globals.css]] — *the failing pix-paper and cashier-daylight blocks*.
