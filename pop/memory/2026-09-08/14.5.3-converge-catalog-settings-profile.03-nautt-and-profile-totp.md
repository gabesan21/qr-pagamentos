---
task: 14.5.3-converge-catalog-settings-profile
entry: 03-nautt-and-profile-totp
---

# Nautt actions and the three-step TOTP order

The credential surface turned client-side with a status badge, the projected `updatedAt` labelled as a credential update, a Validate that only re-reads status, and a reset behind `ConfirmDialog` restricted to the recoverable states. `/profile` collapsed to one `max-w-3xl` stack with a dirty-gated identity save and a per-character password meter. Enrollment follows the template order: QR plus manual secret, a `fetch`-bridged confirm with an inline wrong-code error, then codes with copy-all and download — never persisted.

## Evidência

- [[src/app/profile/totp-section.tsx]] — *the three-step enrollment*.
