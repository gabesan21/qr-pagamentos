---
task: 14.4.1-converge-shell-and-auth-pages
entry: 01-shell-chrome
---

# Shell chrome converged with the template

The top-bar title stopped being a static per-role eyebrow: each layout supplies an inert route→label registry matched with `isActiveRoute`, plus a dashboard fallback (merchant also carries `/profile`). The rail/drawer switch moved from 768px to the `lg` law (`max-width: 63.9375rem`), the rail footer became monogram + username + role pill + `by Nautt Finance` — its duplicated sign-out removed, the account menu owns it — and the merchant top bar links `/store/<slug>` only when settings resolve enabled with a slug.

## Evidência

- [[src/app-shell/app-shell.tsx]] — *rail footer and title props*.
- [[src/app/(merchant)/shell-context.ts]] — *the storefront link*.
