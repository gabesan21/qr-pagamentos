---
task: 14.3.3-project-aa-soft-tone-foregrounds
entry: 03-spec-and-docs
---

# Contratos duráveis do on-soft

A spec do sistema de frontend passou a declarar o método `k` para texto sobre superfície soft e a tabela de 30 linhas (tema, papel, `k`, hex, razão); `DESIGN.md` espelha a tabela em forma compacta e lista as utilities novas; o `AGENTS.md` de `src/components/ui` proíbe `text-<tone>` sobre `bg-<tone>-soft`. A precedência de AA sobre paridade byte-a-byte já existia: os primitivos de auditoria e a linha do SHA do snapshot ficaram intactos.

## Evidência

- [[pop/specs/application-frontend-system|application-frontend-system]] — *o método e a tabela on-soft*.
- [[src/components/ui/AGENTS.md]] — *a regra de classe tonal do subtree*.
