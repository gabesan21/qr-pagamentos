---
task: 14.5.2-converge-merchant-payment-links
entry: 04-contracts-and-gate
---

# Contracts recorded, one DOX exception, gate in two rounds

A spec registrou o slice 14.5.2: partição de eras, os três membros aditivos (`confirmedOrderCount`, `confirmedVolume`, `available`), o `hasCheckoutAttempt` do prefill e a descrição mantida presa a `FIXED_AMOUNT`. `DESIGN.md` guardou a razão da partição e `link-money.ts` como dono da aritmética. Uma exceção escopada entrou no DOX de `data-directory` para a filtragem client-side da lista V1 congelada e sem cursor — pendente de ratificação humana. O gate devolveu na rodada 1 (foco invisível, `tsc` exit 1, badges genéricos) e aprovou na 2.

## Evidência

- [[pop/specs/catalog-and-payment-links]] — *o slice 14.5.2 e seus membros*.
