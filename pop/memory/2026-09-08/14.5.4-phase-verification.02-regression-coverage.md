---
task: 14.5.4-phase-verification
entry: 02-regression-coverage
---

# The regression the three checklists owed

Twelve new cases closed what 14.5.1, 14.5.2 and 14.5.3 accumulated: the `hasCheckoutAttempt` doubles on both branches and the negative attempt-lock banner, the resolved link's domain lifecycle badge and drill-down href in the order detail, the link's own `>Paid<` badge in the per-link drilldown, the currency-badge column with the pair filter registered only when the owner has active pairs, and the owner-only `confirmedOrderCount`/`confirmedVolume` with per-line availability. Four more pins arrived in round 2 for the directed repairs earlier judges had extracted.

## Evidência

- [[14.5.4-phase-verification.05-round-2-pins]] — *the four pins the first round owed*.
