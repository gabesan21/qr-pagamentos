---
task: 14.6.2-converge-storefront-and-standalone-pay
entry: 05-checklist
---

# Handoff to 14.6.3 and the human

Suites touched, needing the phase run: `store/[slug]/page.test.tsx`, `pay/page.test.tsx`, `pay/standalone-payment-experience.test.tsx` (realigned to `Monogram`, `CheckoutPaymentView`, `EmptyState`), plus `storefront-experience.test.tsx` over new markup; 4 files / 61 tests green here. Leftovers for 14.7.1: `.receipt-rail*` and the unreferenced `storefrontIntroduction`/`storefrontLogoAlt` keys. Human check (C10): both routes across six themes, both locales, 320/375/768/1440 — no overflow in `boxed|table`, visible focus, identity and tones equal to `/pay`, language switch.

## Evidence

- [[14.6.2-converge-storefront-and-standalone-pay.verify]] — *criteria and verdict*.
