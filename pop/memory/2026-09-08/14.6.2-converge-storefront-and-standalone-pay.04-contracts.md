---
task: 14.6.2-converge-storefront-and-standalone-pay
entry: 04-contracts
---

# The converged public surface written down

The storefront spec gains one `Task 14.6.2` line in "Implemented slices": both store routes on the shared shell, `EmptyState` instead of `Card` + `Alert`, the standalone phases through `CheckoutPaymentView`, the controller deviation, the BEM retirement with its `.receipt-rail*` exception — and that no route, body or cart contract changed. `DESIGN.md` rewrites the two extrapolation bullets on the same terms. The sessionless boundary is byte-identical: `src/checkout/**`, `src/app/api/**`, `src/app/pay/**`, `src/components/ui/**` and `src/i18n/**` untouched.

## Evidence

- [[pop/specs/storefront-and-customization|storefront spec]] — *the durable slice line*.
