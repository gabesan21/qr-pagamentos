---
task: 14.3.2-keep-mutations-on-page
entry: 05-destination-contract
---

# Post-mutation destination written into the contracts

The delivered destinations became contract text: the merchant redirect map names the order, payment-link, profile, and Nautt targets, the password paragraph states rotation with the `/profile` landing, and the V2 link section records create-to-detail with form-scoped failures. `DESIGN.md` adds the rule — a mutation returns to the page that submitted it, the destination derived server-side from the route param or the submitted `action`/`id`, never from a client path.

## Evidence

- [[pop/specs/administrative-foundation|administrative-foundation]] — *redirect map and rotation*.
- [[pop/specs/catalog-and-payment-links|catalog-and-payment-links]] — *link targets*.
