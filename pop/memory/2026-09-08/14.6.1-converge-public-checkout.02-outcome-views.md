---
task: 14.6.1-converge-public-checkout
entry: 02-outcome-views
---

# Payment phase and the eight outcome views

`checkout-payment-views.tsx` holds the presentational view: one live composition for the waiting states (badge, poll-failure banner with "check again", `QrDisplay` from the PIX payload with the merchant mark centre-cut, compact `CopyField`, amount due) plus five terminals — confirmed with an icon marker, refunded neutral, and rejected/cancelled/expired as error `EmptyState` with start-over. The provider image is no longer the QR source and no new asset entered the closed inventory.

## Evidence

- [[src/components/ui/qr-display.tsx]] — *the `payload`/`identity` contract raising error correction to `H`*.
