---
task: 14.7.1-retire-bem-and-refresh-contracts
entry: 04-contracts-and-dox
---

# One system, written down

`DESIGN.md` gains "BEM retirement (14.7.1)": the deletion list and, per survivor, why it cannot be a utility — the 900px breakpoint cannot move (`min-[900px]:` breaks the raw-value gate, `var()` is illegal in a media query), `brand-identity*` is component-owned geometry, `.sr-only` has no byte-equal replacement, `app-shell.css` is shell chrome. The spec says the same in one line, still 132 lines. Four DOX files gain a contract line each.

## Evidence

- [[pop/specs/application-frontend-system|application-frontend-system]] — *the single-system clause and its exceptions*.
