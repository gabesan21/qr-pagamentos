---
task: 14.6.2-converge-storefront-and-standalone-pay
entry: 01-storefront-shell
---

# The storefront drops its own rail for the checkout shell

Page, loading and error of `/store/[slug]` compose 14.6.1's `CheckoutShell`: merchant header (logo or `Monogram`, name, trust), `data-theme-preview` + `--storefront-accent`, public footer with `LanguageSwitcher`. `PublicStorefront` is structurally identical to `PublicCheckoutBranding`, so it goes straight in as `branding` — no fork. The page-local `receipt-rail storefront-rail` header and the introduction died with it; markup moved to projected utilities with zero new dictionary keys, and a name without a logo now renders `Monogram` initials.

## Evidence

- [[src/app/store/[slug]/page.tsx]] — *the shell composition and header precedence*.
