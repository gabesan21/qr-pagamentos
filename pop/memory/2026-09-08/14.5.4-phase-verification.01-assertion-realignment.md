---
task: 14.5.4-phase-verification
entry: 01-assertion-realignment
---

# Fourteen merchant suites realigned to the delivered composition

Every suite the run failed was rewritten against the delivered source, never around it: retired BEM gave way to `data-slot="card"` and `aria-current`, the theme `<select>` to a swatch `radiogroup`, the link directory to the era partition, and the dashboard rows to the owner's own order id plus the redacted payer name. Several assertions came out stronger than the ones they replaced. Zero `.skip`/`.todo` added; no case dropped without a same-behaviour replacement. `merchantDashboardSalesEmpty` was correctly dropped — it is rendered nowhere and stays a follow-up.

## Evidência

- [[14.5.4-phase-verification.verify]] — *the sampled realignment diffs*.
