---
task: 14.3.4-phase-verification
entry: 01-assertion-realignment
---

# The assertions Phase 14.3 falsified, rewritten and never deleted

Nine `page.test.tsx` mounting `DataDirectory`, plus `specimen-mounted`, gained the `useRouter` mock and assert the reset redirect `?filters=ignored` where they asserted an `invalid-query` render. The mutation suites moved to the on-page targets: orders-v2, payment-links-v2, V1 links, the merchant home, the Nautt routes with their `#settings-connection` anchor, and `/profile/password` asserting a fresh `qr_session`, never `/login`. No test deleted, no skip.

## Evidência

- [[src/app/profile/password/route.test.ts]] — *session rotation instead of `/login`*.
- [[src/app/admin/orders/page.test.tsx]] — *the reset redirect a directory page performs*.
