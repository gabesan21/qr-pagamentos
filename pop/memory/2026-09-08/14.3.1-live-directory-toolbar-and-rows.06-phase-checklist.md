---
task: 14.3.1-live-directory-toolbar-and-rows
entry: 06-phase-checklist
---

# What the gate left registered for the phase

No test ran in the task. The nine consumer `page.test.tsx` need the `next/navigation` mock and redirect assertions where they asserted `invalid-query`; `specimen-mounted.test.tsx` and the admin `directory-query.test.ts` cases for the stripped key also need realignment — all handed to the phase-verification task. Round 1 also left four non-blocking follow-ups: per-keystroke commits on free-text filters, the default `pageSize` emitted on every commit, the footer unmounting while pending, and the clear button borrowing the reset label.

## Evidência

- [[14.3.1-live-directory-toolbar-and-rows]] — *ledger; round-1 verify with repair addendum is in git history*.
