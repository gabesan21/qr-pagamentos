---
task: 14.7.1-retire-bem-and-refresh-contracts
entry: 02-auth-consumers
---

# Authentication drops its BEM sheets

`/login`, `/reset-password`, the TOTP challenge and `AuthCard` render without `auth-page`, the `auth-card__*` leaves, `auth-password-field*`, `auth-forgot*`, `auth-totp-actions`, `auth-mode-toggle`, `login-*` and `reset-password-*`; only the four selectors of the `(min-width: 900px)` split card survive. Colour intent holds — `hover:text-accent` resolves to the old `--action-primary` in all six themes. `.auth-brand` was refused, not forced: retiring it needs a `BrandIdentity` size prop (open gap).

## Evidence

- [[src/app/auth-card.tsx]] — *the surviving 900px selectors and the migrated panel*.
