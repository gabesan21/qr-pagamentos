---
task: 14.6.3-phase-verification
entry: 02-owed-regression
---

# The regression the two Phase 14.6 tasks owed

Five new suites under `src/app/pay/[identifier]/`: validation (masks, e-mail, CPF check digits identical to the server, CEP in both grammars); ten provider states projecting to eight distinct views, RESERVED/CREATING/CREATED sharing one live shell, refunded neutral without start-over, poll banner only on `pollFailed`; the 560px shell with logo→`Monogram`→fallback precedence; the footer switcher on the persisted locale; and the state machine — typing has no side effect, `startOver` is the only reset, `inputMode` numeric on CPF/CEP, focus to the first invalid field.

## Evidence

- [[pop/specs/checkout-and-order-lifecycle|checkout spec]] — *the states and rules pinned*.
