---
task: 14.3.2-keep-mutations-on-page
entry: 04-profile-settings-on-page
---

# Password change rotates the session, no sign-out

`POST /profile/password` leaves revocation untouched in the service — every prior session still dies atomically inside the user lock — and only then issues one fresh session for the actor, `createMfaVerified` when TOTP is enrolled so the MFA fact holds. The `qr_session` cookie is replaced with login's attributes and the merchant lands signed in on `/profile?password=changed`; the `qr_locale` write went with the old anonymous landing. Nautt returns to `/settings#settings-connection`.

## Evidence

- [[src/app/profile/password/route.ts]] — *rotation and cookie*.
- [[pop/specs/administrative-foundation|administrative-foundation]] — *rotation contract*.
