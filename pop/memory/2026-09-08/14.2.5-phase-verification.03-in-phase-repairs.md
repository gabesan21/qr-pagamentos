---
task: 14.2.5-phase-verification
entry: 03-in-phase-repairs
---

# The six cheap repairs inside the phase's reach

The root `Toaster` took the template's `position="top-right"` and `/design-system` lost its duplicate `ToastViewport` mount; a rejected `QRCode.toString` now records the failed (payload, level) pair so `aria-busy` clears instead of pulsing forever; the theme picker returns `null` for an empty registry rather than an unlabelled empty group; the dead `strikethrough` prop left `StatusPill`; and `DESIGN.md` says "unauthenticated public surfaces", matching what the layout actually trusts.

## Evidência

- [[src/components/ui/qr-display.tsx]] — *the failed-generation state*.
- [[src/components/ui/toast.tsx]] — *the single, positioned viewport*.
