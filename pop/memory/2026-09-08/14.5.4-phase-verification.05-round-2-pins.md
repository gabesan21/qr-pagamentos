---
task: 14.5.4-phase-verification
entry: 05-round-2-pins
---

# Four directed repairs finally pinned, structurally

Round 1 was returned because three repairs earlier judges had extracted still had no test, two of them reported as covered when they were not. Round 2 pinned all four so that undoing the fix turns them red: the hidden `currencyCode` mirror carrying the preselected currency into the create POST, the `<noscript>` logo controls bound by `form=` to a sibling form — held by an exact form count plus DOM order, so re-nesting fails — the `showV2Details` opt-in contrasted on both merchant and admin V1 details, and the `from`/`to` calendar-day validation.

## Evidência

- [[14.5.4-phase-verification.verify]] — *the round-1 return and round-2 approval*.
