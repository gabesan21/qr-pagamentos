---
task: 14.6.1-converge-public-checkout
entry: 06-checklist
---

# Handoff to 14.6.3 and the human

Suites falsified, needing execution: `public-checkout-form.test.tsx` and `public-checkout-v2-form.test.tsx` (markup moved to `CheckoutPaymentView`); re-grep tests for retired classes. Correcting the F04 misrecord: `.checkout-card`, `.checkout-form`/`.checkout-payment` and `.checkout-copy` survive because `standalone-payment-experience.tsx` alone consumes them. Human check: both eras, locales and themes at 320-1440 — masks, error focus, total on the submit, QR scan with the centred mark, retry on the first poll failure, modal focus, forced `404`.

## Evidence

- [[pop/kanban/005_closing/14.6.1-converge-public-checkout/14.6.1-converge-public-checkout.verify]] — *criteria and checklist*.
