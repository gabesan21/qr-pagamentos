---
task: 14.4.1-converge-shell-and-auth-pages
entry: 05-contracts
---

# Contracts record the shipped rules and the reset limit

`DESIGN.md`, the frontend spec and the shell DOX now state the shipped laws: title registry with dashboard fallback, the 1024px rail/drawer switch, the rail footer with sign-out owned solely by the account menu, the `/store/<slug>` target, and the shared auth-card composition. They also record why reset shows two states and not three: `validateResetChallenge`/`findValidToken` fold consumed, expired and unknown into one `null`, so the three-way split needs a new service outcome, not presentation.

## Evidência

- [[pop/specs/application-frontend-system|application-frontend-system]] — *the recorded reset limit*.
- [[src/app-shell/AGENTS.md]] — *the shell subtree contract*.
