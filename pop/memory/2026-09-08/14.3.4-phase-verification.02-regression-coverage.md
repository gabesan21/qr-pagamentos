---
task: 14.3.4-phase-verification
entry: 02-regression-coverage
---

# The regression the three checklists owed

A new `data-directory-client.test.tsx` pins the URL-state controller: debounced search commit, immediate commit on change, `cursor` dropped on every new query, chip labels resolved from the registered options (never the raw value) and guarded `getRowHref` navigation. `form-draft.test.tsx` pins textarea/select capture, dirty-only link drafts and the closed notice set. These are behaviours Phase 14.3 delivered that no earlier suite could observe.

## Evidência

- [[src/data-directory/ui/data-directory-client.test.tsx]] — *the controller, chips and row navigation*.
- [[src/app/form-draft.test.tsx]] — *what the draft guard captures and what it refuses*.
