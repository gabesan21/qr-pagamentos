---
task: 14.3.1-live-directory-toolbar-and-rows
entry: 05-contracts
---

# The owner contract was revised to authorize the live behaviour

`src/data-directory/AGENTS.md` now describes the client shell as a thin URL-state controller over the native GET form, the chip resolution rule, the row href with its interactive-descendant guard, the documented `interactive={false}` opt-out, and the invalid-input redirect — while keeping every prohibition intact (no total count, page numbers, arbitrary sort or parallel table owner). `DESIGN.md` and both specs were synced; the query grammar itself did not change, only the reserved notice pair was added to it.

## Evidência

- [[pop/specs/administrative-foundation|administrative-foundation]] — *the reserved-pair clause and the unchanged grammar*.
