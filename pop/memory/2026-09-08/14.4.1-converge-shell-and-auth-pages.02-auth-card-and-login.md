---
task: 14.4.1-converge-shell-and-auth-pages
entry: 02-auth-card-and-login
---

# Shared auth card and the login form

The new `src/app/auth-card.tsx` composes every auth surface: the brand panel moved to the leading side with tagline, product caption and six swatches from the closed theme-id registry, `aria-hidden` and hidden below 900px; the form column keeps its 420px cap and a fixed language slot. Login became a client form with a forgot-password link to `/reset-password` noting truthfully that an administrator-issued link is required, a 44px show/hide toggle, and per-field required copy with `aria-invalid`; the invalid alert stays generic.

## Evidência

- [[src/app/auth-card.tsx]] — *the shared composition*.
- [[src/app/login/login-form.tsx]] — *the credentials panel*.
