---
task: 14.5.3-converge-catalog-settings-profile
entry: 04-contracts-gaps-and-bem
---

# Specs amended, four gaps recorded, merchant BEM retired

Four specs record the delivered composition and four gaps kept as facts, never dead controls: no active-state control on create, no inline edit for an inactive category, no credential replace outside `UNREGISTERED`, no enrolled-since date. `DESIGN.md` records the merchant-local `SegmentedControl`, the new `ImageUploader` call sites and the single-column profile. `globals.css` lost the retired `.storefront-workspace*`, `.storefront-logo-*`, `.storefront-preview*` and `.profile-*` blocks; public and `.settings-surface*` rules stay.

## Evidência

- [[specs/catalog-and-payment-links|catalog-and-payment-links]] — *the amended contract*.
