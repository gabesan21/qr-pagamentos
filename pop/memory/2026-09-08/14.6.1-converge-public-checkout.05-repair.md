---
task: 14.6.1-converge-public-checkout
entry: 05-repair
---

# Round 1 returned three blockers; round 2 approved the repair

The judge returned the first head to 004: `CheckoutPaymentView` accepted `merchantIdentity` but no call site fed it, so the centre-cut never rendered while `DESIGN.md` already claimed it; the opaque `404` lost its only render site when the form started yielding, freezing the QR on an expired capability; and `/pay` still emitted four retired `checkout-*` classes. Commit `508e21d8` wired the mark from both pages, rendered the unavailable warning in the payment phase, and left every surviving `.checkout-*` rule with one referrer.

## Evidence

- [[pop/kanban/005_closing/14.6.1-converge-public-checkout/14.6.1-converge-public-checkout.verify]] — *both judged rounds*.
