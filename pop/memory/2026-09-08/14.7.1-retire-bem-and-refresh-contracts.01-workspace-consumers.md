---
task: 14.7.1-retire-bem-and-refresh-contracts
entry: 01-workspace-consumers
---

# Workspace consumers leave BEM for utilities

Merchant settings, storefront settings, the Nautt facts block and `/design-system` compose projected utilities: the two-column hub as `lg:grid-cols-[calc(var(--space-12)*4)_1fr]` mirroring 14.4.3's admin hub, sections as `grid gap-4 scroll-mt-[…]`, prose as `max-w-[var(--layout-max)] text-muted-foreground` — every swap token-exact (`--muted-foreground` ≡ `--text-secondary`, `min-h-11` = 44px). Dead `OrderListCard` and `admin/payment-link-management.tsx` left with zero importers. The nav-link active state adopts the hub's `bg`+weight.

## Evidence

- [[src/app/(merchant)/settings/settings-surface.tsx]] — *the migrated hub composition*.
