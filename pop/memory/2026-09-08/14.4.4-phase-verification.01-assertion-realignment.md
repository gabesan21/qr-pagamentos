---
task: 14.4.4-phase-verification
entry: 01-assertion-realignment
---

# The assertions the three 14.4 tasks falsified

Eighteen suites were realigned to the converged surfaces: shell title registry and rail footer, login/reset/404, the admin dashboard grid, the directories and their new filters, the admin route returns and the account editor. `page.test.tsx` of `accounts/[id]` shrank because its cases moved into four per-section suites; `account-mutation-form.test.ts` stays deleted. No suite was weakened: no `.skip`/`.todo` added, every deletion re-covered elsewhere.

## Evidência

- [[src/app/admin/accounts/[id]/page.test.tsx]] — *the page suite reduced to what still belongs to it*.
- [[src/app-shell/app-shell.test.tsx]] — *the shell contract read off the current sources*.
