---
task: 14.6.1-converge-public-checkout
entry: 01-shell-and-branding
---

# One branded 560px shell for both checkout eras

`checkout-shell.tsx` is now the single `/pay` composition: merchant header (logo, `Monogram` or official fallback), trust line, `data-theme-preview` plus validated `--storefront-accent`, one column at 560px, and a footer with powered-by, privacy `Modal` link and `LanguageSwitcher`. Page, loading, error and both V2 branches compose it. The V1 DTO gained the additive `branding` member V2 already shipped, resolved independent of `storefront_enabled`; the 1280px rule and the two-column split died with it.

## Evidence

- [[src/checkout/public-checkout-presentation.ts]] — *the additive V1 `branding` projection*.
