---
task: 14.3.4-phase-verification
entry: 05-round-2-pins-and-loop-coverage
---

# Round 2: pins that no longer pass by accident; reset loop covered

Round 1 returned two blockers. The token pins encoded the pre-14.3.3 contract and matched by substring; now they compare class tokens, demand `text-<tone>-on-soft`, forbid `text-<tone>`, count the 27 `@theme inline` utilities and fix hex, step and ≥4.5:1 for the 30 soft foregrounds. The strip-before-canonicalize invariant, untested, gained the same trio in the six resolvers: stripped-pair equivalence, a `location` without the pair, and the reset location replaying `ready`.

## Evidência

- [[scripts/design-tokens.test.ts]] — *the 30 primitives, 5 roles × 6 themes*.
- [[src/app/admin/orders/directory-query.test.ts]] — *the reserved trio*.
