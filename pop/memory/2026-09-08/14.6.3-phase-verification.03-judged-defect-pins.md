---
task: 14.6.3-phase-verification
entry: 03-judged-defect-pins
---

# A pin that cannot go red is not a pin

Round 1 was returned: two of the three pins for 14.6.1's judged defects stayed green with the repair reverted — the identity pin passed `merchantIdentity` in by hand, and the class pin rendered `CheckoutPaymentView`, which never emitted them. Round 2 fixed both. `page.identity-wiring.test.tsx` mocks the two form modules and captures the prop, so deleting either wiring line reddens; the class pin moved onto the real emitters (both forms, plus the payment phase) with an exact class list instead of a prefix regex that would false-positive on the `--checkout-max` token.

## Evidence

- [[14.6.3-phase-verification.verify]] — *both rounds, with the redness argument*.
