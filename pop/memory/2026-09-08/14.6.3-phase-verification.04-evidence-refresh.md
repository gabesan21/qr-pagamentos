---
task: 14.6.3-phase-verification
entry: 04-evidence-refresh
---

# Design-system evidence regenerated after the CSS retirement

Both 14.6.1 and 14.6.2 edited `src/app/globals.css`, which the manifest binds, so the run was stale. Regenerated locally with direct binaries — `next build`, a pinned `ADMIN_EVIDENCE_BASE_URL`, Playwright with one worker — producing run `20260909010850`, 48 captures, coverage 20/101, superseding `20260908212142`. The judge re-verified it offline: `verify-design-system-evidence.mjs` exits 0, `current.json` points at it, and its `gitHead` is an ancestor of HEAD. Artifacts written only by the script.

## Evidence

- [[artifacts/design-system/current.json]] — *the current run and its manifest hash*.
