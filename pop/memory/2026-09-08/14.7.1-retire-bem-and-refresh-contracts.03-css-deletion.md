---
task: 14.7.1-retire-bem-and-refresh-contracts
entry: 03-css-deletion
---

# 81 rule-lines leave globals.css

Deleted after a grep proving zero referrers in `src/**` and `tests/**`: `admin-shell*`, `admin-account*`, `admin-product*`, `receipt-rail*`, `ds-*` + `[data-ds-prose]`, `nautt-facts*`, `settings-surface*` with its `64rem` block, the auth leaves and `login-*`/`reset-password-*`. Below the tokens only base rules and four sanctioned exceptions remain: `.sr-only`, `.brand-identity*`/`.auth-brand`, the `.auth-card*` 900px block, and `app-shell.css` outside this file. The inventory allowlist lost 22 dead entries, closing 14.4.4's follow-up.

## Evidence

- [[src/app/globals.css]] — *what was retired and what survives*.
