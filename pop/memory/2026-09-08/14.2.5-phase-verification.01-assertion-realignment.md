---
task: 14.2.5-phase-verification
entry: 01-assertion-realignment
---

# The 17 assertions the phase falsified

Six files encoded behavior that 14.2.1–14.2.4 replaced: the badge's `<svg>` marker (now a `bg-current` dot), `bg-destructive` on the four destructive terminals (now `bg-danger-soft text-danger`), four `next/navigation` mocks missing `useSearchParams`, the closed settings-return allowlist (now host-validated, query kept and fragment stripped) and the anonymous `401` on the locale route (now `303` + `qr_locale`). Each stale test was rewritten in place, never deleted.

## Evidência

- [[src/app/settings-return-target.test.ts]] — *the allowlist turned into origin validation*.
- [[src/components/ui/status-badge.test.tsx]] — *the dot marker replacing the icon assertion*.
