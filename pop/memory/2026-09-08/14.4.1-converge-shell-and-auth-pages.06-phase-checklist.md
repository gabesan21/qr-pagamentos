---
task: 14.4.1-converge-shell-and-auth-pages
entry: 06-phase-checklist
---

# Handoff to 14.4.4 and the human

Five test files were invalidated by the new DOM and belong to `14.4.4-phase-verification`: `shell-account-menu.test.tsx`, `shell-context.test.ts` (needs a storefront-settings mock), `login/page.test.tsx`, `reset-password/page.test.tsx`, `reset-password/submit/route.test.ts`; the same run must evidence `pnpm lint` and confirm the build with the now-dynamic `/_not-found`. Open follow-ups from the gate: the strength meter duplicates the profile one, and the merchant layout reads the owner row twice.

## Evidência

- [[14.4.1-converge-shell-and-auth-pages.verify]] — *the gate round that named them*.
- [[14.4.4-phase-verification]] — *where they are due*.
