---
task: 14.5.1-converge-merchant-dashboard-and-orders
entry: 01-analytics-projection-and-dashboard
---

# Owner projection extended, dashboard converged

Counting-only additive fields entered the owner projection: orders in period split by provider state and origin, link and product counts, owner-own order/product ids, `isFirstRun`, plus a payer name-or-email from the snapshot the owner already reads. Both new store reads filter `ownerId` and amounts stay exact decimals. The page gained a client period control over the owned `Tabs`, state/source tabs, four inventory cards, clickable rows, storefront banner, first-run and `error.tsx` states; `.merchant-dashboard__*` left `globals.css`.

## Evidência

- [[src/orders/merchant-analytics.ts]] — *the owner-scoped projection*.
