---
task: 14.4.4-phase-verification
entry: 03-repairs-and-caught-bugs
---

# Nine in-phase repairs, two real bugs

Fixed inside the phase's reach: OTP `pattern`, period `TabsList` `aria-label`, password help `aria-describedby`, a dangling `htmlFor`, a dead `48rem` token-checker entry, a dead generator fallback, the `.auth-card__footer` rule, and "Vendedor"→"Lojista". Two were product bugs, not test drift: the associated-orders card swallowed Next's `redirect()`/`notFound()`, and a client boundary imported `@/auth/checkout-policy` at runtime (it pulls the database client) instead of type-only.

## Evidência

- [[src/app/admin/payment-links/associated-orders-card.tsx]] — *control flow rethrown*.
- [[src/app/admin/accounts/[id]/preferences-section.tsx]] — *boundary made type-only*.
