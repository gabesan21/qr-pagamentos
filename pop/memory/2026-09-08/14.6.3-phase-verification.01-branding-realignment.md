---
task: 14.6.3-phase-verification
entry: 01-branding-realignment
---

# The V1 projection realigned to the additive branding

`public-checkout-presentation.test.ts` stays on `toEqual` (closed shape) and gains the `branding` member plus a new case: a bare owner falls back to `DEFAULT_STOREFRONT_THEME_ID`, independent of `storefront_enabled`. `page.test.tsx`'s three fixtures gained a `branding` object and one added assertion. Nothing weakened: no `.skip`/`.todo`, no case deleted, every assertion read from the delivered `projectBranding`, not from the old test.

## Evidence

- [[src/checkout/public-checkout-presentation.ts]] — *the projection the assertions mirror*.
