---
task: 14.5.2-converge-merchant-payment-links
entry: 05-phase-checklist
---

# Falsified link suites handed to 14.5.4

Julgado por leitura; nenhuma suíte rodada. Ficam para o gate da phase: `links/directory-query` (filtros novos), `links/page` (eras e coluna de moeda), `links/new/page` e `v2/[id]/edit/page` (formulário reescrito, banners sob `hasCheckoutAttempt`), `v2/[id]/page` (action row, captions, resumo real), os dois `orders` (breadcrumbs, `CopyField`, pagador, badges de domínio), `payment-link-v2-prefill` (dublês ampliados; asserções de shape pendentes) e `payment-link-v2-view` (sem asserção para os membros novos). C14 vai ao humano.

## Evidência

- [[14.5.2-converge-merchant-payment-links.verify]] — *as duas rodadas do gate*.
