---
task: 11.1.2-add-admin-password-reset-action
project: applications/qr-pagamentos
started: 2026-07-28
finished: 2026-07-28
commit: 7a9a871
pr: ""
---

# 11.1.2-add-admin-password-reset-action

**What:** Added the administrator-initiated password-reset audit action.

**Why:** The user-management workflow needs a support action that never exposes whether a challenge or message was created.

**Done:**
- Added append-only `password_reset_request` table/model with restrictive FK on `user_id`, plain `actor_id` UUID, `outcome` varchar, and `created_at`.
- Generated safe-language migration `20260728210000_password_reset_request` and re-pinned `SELECT`/`INSERT` runtime grant in `prisma/bootstrap.sql`.
- Integrated audit writes into `src/auth/admin-password-reset.ts` for the five outcome classes: `SENT`, `NO_EMAIL`, `UNAVAILABLE`, `RATE_LIMITED`, `DELIVERY_FAILED`.
- Wired the resolved admin principal into `POST /admin/users/[id]/reset-password` while preserving origin-guard, re-authorization, request-log, and the closed `?reset=requested|failed` redirect set.
- Added/updated unit and route tests covering every outcome, secret non-exposure, and opacity.

**Gates:**
- `pnpm test src/auth/admin-password-reset.test.ts src/app/admin/users/[id]/reset-password/route.test.ts` — PASS (11 tests)
- `pnpm db:test` — PASS
- `pnpm check` — PASS
- `pnpm admin-users:evidence` / `:verify` — PASS (run 20260728210031)

**Decisão:** Aprovado pelo crítico independente do fluxo yolo em 2026-07-28.
