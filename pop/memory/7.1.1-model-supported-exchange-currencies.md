---
task: 7.1.1-model-supported-exchange-currencies
project: pop
started: 2026-07-23
finished: 2026-07-24
commit: cfe97c84ecdf906f528004f1c7688681709546d7
pr:
---

# 7.1.1-model-supported-exchange-currencies — Model supported exchange currencies

- **Delivery:** Dynamic admin-registered exchange-currency registry: `supported_exchange_currency` pointer table (ISO code PK with bounds check, unique `pair_id` FK `ON DELETE RESTRICT` to retained `catalog_currency_pair`), server-only registry/resolution service with typed gating, and the re-authorized `POST /admin/exchange-currencies` mutation route. No UI surface.
- **Areas changed:** additive safe migration `20260724120000_supported_exchange_currencies`, schema/bootstrap ACLs, `src/auth/supported-exchange-currency.ts`, admin route, db-test/db-contract assertions, catalog/storefront/Nautt specs, roadmap Phase 7.1 wording, root AGENTS.md boundary, RESEARCHES.md (currency-mappings research removed per human directive).
- **Verification:** Full critical yolo review APPROVE, round 1. Migration lane (db:generate, policy/contract checks, disposable db:test), 24 focused tests, typecheck/lint/build, scope/diff, and PoP validation passed; the two remaining suite failures were M-4.1's pre-existing baselines.
- **Final telemetry:** 002 planner with 1 critical-003 return (pointer-table enforcement, duplicate-pair semantics); cohesive 004 executor; full 005 critic approved with zero returns; no circuit breaker.
- **Durable decisions:** Provider UUIDs are administrator-registered runtime data, never build-time or inferred; ISO codes are dynamic (format check only). One ACTIVE mapping per code enforced by the database pointer table; re-registering an existing pair yields a typed duplicate-pair conflict or a sanctioned re-point restore — never an insert; `catalog_currency_pair_uuids_key` is preserved. Default resolves the active BRL mapping or explicit none. Missing configuration is a normal gated state: typed `NoActiveExchangeCurrencyMapping` for backends and a discriminated UI-prohibition signal for 7.1.2/7.2.2.
- **Relevant deviations:** bootstrap.sql gained a relation-guarded no-DELETE ACL block mirroring product_category (beyond the plan's must-not-edit; accepted by the critic as matching the Prisma doctrine); two pre-existing db-test bugs fixed en route (verified pre-existing at merge-base).
- **Contract impact:** The `nautt-supported-exchange-currency-mappings` authenticated research is permanently removed as a build dependency; provider mapping values are operational data registered by administrators.

## Links

- **Spec:** [[specs/catalog-and-payment-links|Catalog and payment links]] — *follow for the currency registry, gating, and history contract.*
- **Commit:** `cfe97c84ecdf906f528004f1c7688681709546d7` — *inspect the integrated delivery.*
