---
task: 8.3.3-build-owner-orders-management-ui
project: pop
started: 2026-07-25
finished: 2026-07-25
commit: c31b32ca21ab7dd8351e281afd3accc0d352c152
pr:
---

# 8.3.3-build-owner-orders-management-ui — Build owner orders management UI

- **Entrega:** UI de orders do merchant: `/orders` com diretório V2 (query 8.3.1, notice strip, 6 estados, sizes 10/20/50/100 default 20) acima da seção V1 byte-frozen; `/orders/v2/[id]` com thread de comentários (append + edit author-only CAS) e outcomes locais guardados postando `lifecycleVersion`; exposures aditivos sanctionados em `order-v2-view.ts` (payer na summary, lifecycleVersion no detail); toolbar genérica (pageSizes/textFilters); page-size localStorage observation-only; domínio i18n `orders-directory`; par `orders:evidence` (48 captures).
- **Áreas alteradas:** `src/app/(merchant)/orders...` + `/orders/v2/[id]` · `src/orders/order-v2-view.ts` (2 campos) · data-directory toolbar + min-w-0 · dicionários · evidence · DESIGN.md/AGENTS.md/spec.
- **Verificação:** `pnpm check` verde + `orders:evidence`/`:verify` verdes; re-run do revisor (differential); diff zero nos contratos entregues.
- **Telemetria final:** 002: 1 planner · 004: 1 executor (11 commits) · 005: 1 crítico differential, 0 devoluções.
- **Decisões duráveis:** localStorage nunca vence URL explícita nem viola canonicalização; exposures de projeção estritamente aditivos e fail-closed.
- **Desvios relevantes:** fixtures `payer` em 2 test files (fallout de typecheck); modo `--orders` no harness (staleava hash do links-evidence → re-bind no closeout); min-w-0 no data-directory (overflow 768px); dívida upstream registrada (enum filter "All" posta `filter.x=` vazio → invalid-query; task futura).
- **Impacto em contratos:** specs: slice 8.3.3 · DOX: AGENTS.md raiz + data-directory + DESIGN.md.

## Links

- **Spec afetada:** [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]] — *siga para o slice 8.3.3 e os exposures*.
- **PR/commit:** c31b32c (merge 42d2358 + fixture fix em develop) — *siga para o diff final*.
