---
task: 10.4.1-build-admin-settings-hub
project: pop
started: 2026-07-25
finished: 2026-07-27
commit: 5b28444e3c517dde0a1a1552b8f9f24d0bf41a37
pr:
---

# 10.4.1-build-admin-settings-hub — Administrator settings hub

- **Delivery:** bilingual `/admin/settings` hub with six anchored sections (registry, currency pairs, payment methods, settings, appearance, language) composing the delivered catalog, registry, payment-settings and language mutations, whose redirects now land on `/admin/settings`. Added the `system_settings` singleton (`id=1`, nullable closed-six-theme `default_theme_id`) behind `POST /admin/settings/default-theme`, plus creation-time USER-only stamping of the effective default theme (ADMIN rows NULL, no backfill).
- **Areas changed:** `src/app/admin/settings/`, `src/auth/system-settings.ts`, the `createUser` stamping, the redirects, migration `20260727050000_system_settings`, and 48 evidence captures (run 20260727053634) — all with tests.
- **Verification:** full 005 critic APPROVE round 1 (3 nits); critic re-ran typecheck, test, lint, evidence `:verify` and contract checks, source byte-identical to the baseline.
- **Final telemetry:** 002 plan; non-critical yolo 002→004; one executor (8 commits); zero returns.
- **Durable decisions:** the branch predated 10.3.2 — the merge preserved develop's `/admin/accounts` page and `AdminAccountsSurface` contract while the hub stayed extracted in `settings-surface.tsx`, retiring the moved legacy surfaces; the merged spec kept develop's compaction plus the additions (146 lines).
- **Relevant deviations:** the card moved to `006_done` before worktree removal, whose untracked artifacts required `--force`.
- **Contract impact:** specs: administrative-foundation, catalog-and-payment-links · DOX: root.

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the hub contract and default-theme mutation.*
- **Commit:** `5b28444` — *inspect the merged delivery.*
