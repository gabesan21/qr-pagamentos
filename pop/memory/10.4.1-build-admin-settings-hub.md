---
task: 10.4.1-build-admin-settings-hub
project: pop
started: 2026-07-25
finished: 2026-07-27
commit: 5b28444e3c517dde0a1a1552b8f9f24d0bf41a37
pr:
---

# 10.4.1-build-admin-settings-hub — Administrator settings hub

- **Delivery:** Bilingual `/admin/settings` hub with six anchored sections (exchange currencies, Nautt currency pairs, Nautt payment methods, global payment settings, appearance, language) composing the delivered catalog/registry/payment-settings/language mutations; redirects repointed to `/admin/settings?…` with closed bilingual notice set. Persisted `system_settings` singleton (`id=1`, nullable closed-six-theme `default_theme_id`) behind `POST /admin/settings/default-theme` with one literal request-log entry; creation-time USER-only stamping of the effective default theme via `src/auth/administration.ts` `createUser` (ADMIN rows NULL, no backfill, legacy NULL resolution unchanged).
- **Areas changed:** `src/app/admin/settings/` (page, `settings-surface.tsx`, `default-theme` route + tests), `src/auth/system-settings.ts` (+ tests/database test), `src/auth/administration.ts` stamping (+ test), catalog/registry/payment-settings route redirects and tests, evidence pair (`--admin-settings` mode, 48 captures, run 20260727053634), `scripts/run-admin-evidence.mjs` `--admin-settings` chain, `package.json` evidence script pair, dictionary administration slice, migration `20260727050000_system_settings`, specs + root AGENTS.md.
- **Verification:** Full 005 critic APPROVE round 1 (3 nits, no blocking). Critic re-ran typecheck/test/lint/evidence `:verify`/contract checks; source-check byte-identical to the branch baseline; fences zero-diff. Merge resolved develop-vs-branch spec drift by keeping develop's compacted administrative-foundation.md (144 lines) and re-applying the four 10.4.1 additions; AGENTS.md union of existing bullets plus settings-hub and system-settings bullets; `admin-surface.tsx` reconciled to keep develop's `AdminAccountsSurface` children contract while retiring the moved `AdminSurface`/`AdminSettingsSurface` legacy functions.
- **Final telemetry:** 002 plan; non-critical yolo 002→004; one executor (8 commits); full critic zero returns; no circuit breaker.
- **Durable decisions:** Branch was based on pre-10.3.2 develop; at merge, develop's `/admin/accounts` page and `AdminAccountsSurface` children contract were preserved, while the settings hub surface was extracted to `src/app/admin/settings/settings-surface.tsx` as delivered. The 144-line administrative-foundation.md spec cap held after the four additions (final 146 lines).
- **Relevant deviations:** Card directory moved to 006_done before worktree removal; worktree removal left artifacts untracked and required `--force` plus branch deletion.
- **Contract impact:** administrative-foundation (settings-hub route table, mutation inventory, implemented slice, Updated line), catalog-and-payment-links (registry UI mention), root AGENTS.md (`src/app/admin/settings/` + `src/auth/system-settings.ts` bullets, request-log 10.4.1 extension, exchange-currency UI clause).

## Links

- **Spec:** [[specs/administrative-foundation|Administrative foundation]] — *follow for the admin settings-hub contract and default-theme mutation.*
- **Commit:** `5b28444` — *inspect the integrated delivery (merge into develop).*
