---
task: 14.1.3-repair-admin-drilldown-and-confirmations
entry: 04-phase-handoff
---

# What the gate handed to 14.1.4 and to the human

`src/app/admin/payment-links/v2/[id]/page.test.tsx:83` still asserts the old `?link=` href and is updated in `14.1.4-phase-verification`, which also runs `pnpm check` and proves the filtered directory at runtime. The human checklist keeps criterion 10: keyboard operation and visible focus for the dialogs and the menu. Documentation sync stays with the orchestrator: the spec's `?link=` line and the app-shell username-block rule.

## Evidência

- [[pop/specs/administrative-foundation|Administrative foundation]] — *a linha que ainda cita `?link=`*.
- [[src/app-shell/AGENTS.md]] — *a regra do bloco de usuário a atualizar*.
