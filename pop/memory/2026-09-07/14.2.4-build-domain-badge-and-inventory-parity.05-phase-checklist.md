---
task: 14.2.4-build-domain-badge-and-inventory-parity
entry: 05-phase-checklist
---

# Assertions handed to the phase

Soft-tint pills invalidate two suites asserting rendered classes, and editing owners invalidates the design-system hashes. Left to 14.2.5: `status-badge.test.tsx`, expecting an `<svg>` per tone where a dot now draws; `standalone-payment-experience.test.tsx:152`, expecting `bg-destructive` on the four destructive terminals; and one run of `design-system:evidence` plus `:verify`. `primitive-inventory.test.tsx` was extended in-task — the only test this task could touch.

## Evidência

- [[src/components/ui/primitive-inventory.test.tsx]] — *the one in-task assertion change*.
- [[pop/roadmap/14-template-fidelity-convergence|Phase 14.2]] — *where the suite runs*.
