---
task: 14.2.4-build-domain-badge-and-inventory-parity
entry: 03-numeric-identity-components
---

# Five numeric and identity gaps closed

`CopyField` gained a compact chip variant repeating the button variant's a11y; `Monogram` an `xl` 48px accent-soft size; `StatCard` an `aria-hidden` SVG sparkline, no motion library; `MoneyText` a tokenized pair pill instead of `Badge`. `QrDisplay` became a client component generating the QR from an optional `payload` with the already-pinned `qrcode`, raising correction to `H` when an identity centre-cut is present. `graphic` still wins: no call site changed, no dependency added.

## Evidência

- [[src/components/ui/qr-display.tsx]] — *the client boundary and generation path*.
- [[src/components/ui/copy-field.tsx]] — *the compact variant*.
