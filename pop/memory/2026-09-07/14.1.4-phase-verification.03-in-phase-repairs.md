---
task: 14.1.4-phase-verification
entry: 03-in-phase-repairs
---

# Three pinpoint repairs inside the phase's reach

The 14.1.1–14.1.3 judgments left three cheap findings, all repaired here. The product id interpolated into the failure `Location` is percent-encoded, so an id with a slash or space can no longer forge a path. The sign-out `<form>` nested between `role="menu"` and its `role="menuitem"` carries `role="none"`, keeping the menu tree intact. The dashboard's `storefront` and `checkout-policy` notice branches were dropped from the `searchParams` type and from `ownerNotice`: no route emits those codes to `/` any more.

## Evidência

- [[src/app/products/route.ts]] — *the encoded failure target*.
- [[src/app/(merchant)/page.tsx]] — *the dead branches removed*.
