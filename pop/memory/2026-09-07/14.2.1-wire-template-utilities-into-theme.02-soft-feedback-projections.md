---
task: 14.2.1-wire-template-utilities-into-theme
entry: 02-soft-feedback-projections
---

# Feedback soft roles projected per theme

`THEME_PROJECTION` gained `color-feedback-{success,warning,danger,info}-soft`, so the DTCG `color.feedback.<role>.soft` tokens — which already resolved but were never emitted — now appear in all 15 theme blocks and preview twins of the generated region. Values are byte-equal to `docs/template/app/src/index.css` in the six themes. No token source was edited; the region was rewritten only by running the generator.

## Evidência

- [[scripts/generate-design-tokens.mjs]] — the four projection entries, lines 107-110.
