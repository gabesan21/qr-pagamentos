---
task: 14.2.3-mount-toasts-and-origin-locale-switch
entry: 05-phase-checklist
---

# What 14.2.5 inherits

Executors were barred from editing tests, so the assertions this task invalidated moved to the phase verification: four `page.test.tsx` mock `next/navigation` without `useSearchParams`; `settings-return-target.test.ts` still asserts the closed allowlist; `route.test.ts` still asserts the anonymous `401`. Two follow-ups stay outside this task's ownership: `/design-system` keeps a second `ToastViewport`, and the toast owner sits bottom-right while the template is top-right.

## Evidência

- [[src/components/ui/toast.tsx]] — *the diverging position*.
- [[src/app/design-system/interactive-specimens.tsx]] — *the redundant mount*.
