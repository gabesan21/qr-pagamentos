---
task: 14.1.4-phase-verification
entry: 02-regression-coverage
---

# The regression coverage the three verdicts owed

Twenty-nine cases now pin what the phase repaired: the Referer allowlist and every fallback it rejects; the catalog failure return per action with the percent-encoded id; the lifecycle form ids and the archived read-only view with no surviving mutation field; the account panel Sign out queried through the accessibility tree for both roles; the settings surface rendering each of seven sections once, in both locales; and the session draft that carries the typed values back.

## Evidência

- [[src/app/settings-return-target.test.ts]] — *the allowlist and its fallbacks*.
- [[src/app-shell/shell-account-menu.test.tsx]] — *Sign out for both roles*.
