---
task: 13.3.2-close-admin-source-check-gate
entry: 02-gate-precision
---

# Checker allow-list replaced by two rule-anchored typed exemptions

`scripts/check-admin-ui-inventory.mjs` no longer allow-lists literals; it now sanctions a shell `__` class only when `app-shell.css` defines a matching selector, and a `--` token only inside a `var(--…)` reference (mirrors `check-design-tokens.mjs`). Dead/redundant allow-list entries pruned after confirming no current file depends on them. `scripts/check-admin-ui-inventory.test.ts` gained one regression case per exemption plus a backing fixture.

## Evidence

- [[pop/roadmap/13-pre-production-hardening|Phase 13.3]] — *follow for the phase this front closed*.
- `scripts/check-admin-ui-inventory.mjs`, `scripts/check-admin-ui-inventory.test.ts` — *the edited files, commit 3033a842*.
