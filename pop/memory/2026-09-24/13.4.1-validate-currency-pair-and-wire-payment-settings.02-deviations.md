---
task: 13.4.1-validate-currency-pair-and-wire-payment-settings
entry: 02-deviations
---

# Recorded deviations

1. No link-edit gate: the pair is immutable on edit, so only `store.create` is gated.
2. `src/app/currency-pair-probe/route.ts` is not registered in `src/observability/server-request-log.ts` — open follow-up (modification proposal).
3. Admin evidence column shows the latest `(owner, pair)` row per code.
4. A parallel session's `develop` reset dropped this card's kanban commits; restored by cherry-pick before PR.

## Evidence

- [[pop/specs/catalog-and-payment-links|catalog-and-payment-links]] — *follow for the selection gate contract*.
- `src/auth/currency-pair-verification.ts` — *the gate and probe, commit bb5d0bb2*.
