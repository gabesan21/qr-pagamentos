---
task: 0.1.2-reset-nautt-credential-onboarding
project: applications/qr-pagamentos
started: 2026-07-20
finished: 2026-07-20
commit: e80dae2
pr:
---

# 0.1.2-reset-nautt-credential-onboarding — Reset Nautt credential onboarding

- **Entrega:** reset explícito, local-only e owner-initiated do onboarding Nautt travado: CAS único `REGISTERING`/`INDETERMINATE` → `UNREGISTERED` anulando `providerWebhookId`/`encryptedWebhookSecret`/`webhookRegisteredAt` (nunca `ACTIVE`, zero provider calls, zero decriptação, API key preservada); rota `POST /nautt-credentials/reset` com 401 vazio e redirects opacos; affordance bilíngue com disclosure do webhook órfão (inerte: segredo desconhecido → HMAC 401) exibido só no ramo stuck.
- **Áreas alteradas:** `src/integrations/nautt/owner-webhook-registration.ts` + `owner-onboarding.ts` — `reset()`/`resetRegistration`; `src/app/nautt-credentials/reset/route.ts` — nova mutação; `src/app/nautt-credential-surface.tsx` + dicionários `en`/`pt-BR` — UI; spec e ambos AGENTS.md — contrato evoluído (reset local documentado é o único outro retorno a `UNREGISTERED`).
- **Verificação:** `pnpm check` verde (527 testes); 58/58 testes-alvo (matriz de estados, corrida, auth de rota, superfície bilíngue); `git diff develop -- prisma/` vazio; revisão independente critical aprovada sem bloqueantes.
- **Decisões duráveis:** sem recuperação provider-side (gap RECON da Epoch 2 permanece); ambiguidade de duplo registro tratada por disclosure, não por retry automático.
- **Desvios relevantes:** nenhum. Sugestões registradas para follow-up: copy `nauttRecoveryRequired` obsoleta ao lado do botão; `activate()` sem claim-token (resultado seguro, rastreável).

## Links

- **Spec afetada:** [[specs/nautt-finance-integration|Nautt Finance integration]] — *siga para conhecer a semântica do reset local e o gap provider-side ainda aberto*.
- **Commit:** `e80dae2` (merge) / `ce7d76f` + `a14add2` (diffs) — *siga para inspecionar o diff final*.
