---
task: M-4.1-repair-test-baseline-determinism
project: pop
started: 2026-07-24
finished: 2026-07-24
commit: 570e251024780c4fd270732fc8e475684127b8fe
pr:
---

# M-4.1-repair-test-baseline-determinism — Repair test baseline determinism

- **Delivery:** Repaired the three carried develop test-baseline failures and stopped nested task worktrees from leaking into main-checkout `pnpm test` runs; zero production source changed.
- **Areas changed:** `src/auth/sign-in.test.ts` (`vi.mock("server-only")` idiom), `src/app/store/[slug]/page.test.tsx` (pt-BR sessionless expectation), `pop/scripts/db-test.mjs` (7-column/7-value invalid-price INSERT so 23514 surfaces instead of 42601), `vitest.config.ts` (`configDefaults.exclude` + `tests/**` + `pop/worktrees/**`).
- **Verification:** Differential yolo review APPROVE, round 1. `pnpm test` 880 passed / 7 skipped and `pnpm check` exit 0 in the worktree; leak probe collected zero files under `pop/worktrees/**` or nested `node_modules/**`; PoP validation valid.
- **Final telemetry:** 002 planner (root-cause diagnosis, correction sides proven); single 004 executor; differential 005 critic approved with zero returns.
- **Durable decisions:** The product-price fixture was wrong and production SQL correct (Postgres 42601 masked the intended 23514); the storefront sessionless locale default is pt-BR and the test now pins it.
- **Relevant deviations:** None.
- **Contract impact:** None — test/config only.

## Links

- **Commit:** `570e251024780c4fd270732fc8e475684127b8fe` — *inspect the integrated delivery.*
