---
task: 10.4.1-build-admin-settings-hub
entry: 03-creation-stamping
---

# Creation-time theme stamping

Updated `src/auth/administration.ts` `createUser` to stamp the effective default theme into `storefront_theme_id` only for role `USER` at creation time; ADMIN rows keep NULL and existing rows are never backfilled.

## Evidência

- [[src/auth/administration.ts]]
