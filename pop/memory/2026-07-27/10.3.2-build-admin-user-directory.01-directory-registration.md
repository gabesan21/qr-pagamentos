---
task: 10.3.2-build-admin-user-directory
entry: 01-directory-registration
---

# Admin-users registration

Registered `admin-users` over the data-directory foundation with `ADMIN_GLOBAL` scope: sizes 10/20/50/100 default 50, `(createdAt,id)` keyset, role/state/date/`q` filters, derived `deleted`>`disabled`>`active`, store state and `lastActivityAt`.

## Evidência

- [[src/auth/admin-user-directory.ts]]
