---
task: 10.3.3-build-admin-user-profile-editor
entry: 02-new-post-routes
---

# Four new POST subroutes

Added origin-guarded, re-authorized POST routes for identity CAS (`/admin/users/[id]/identity`), locale (`/locale`), checkout policy (`/checkout-policy`) and storefront (`/storefront`), returning only empty `401`/`403` or opaque `?editor=` redirects.

## Evidência

- [[src/auth/admin-user-profile.ts]]
