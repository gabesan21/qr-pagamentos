---
task: 11.1.1-build-self-hosted-password-reset
entry: 01-mail-config
---

# Mail config and sender

Added `src/auth/mail-config.ts` with `*_FILE` secrets that fail closed when set but unreadable; legacy env vars serve only when `*_FILE` is absent. Implemented `src/auth/mail-sender.ts` wrapping nodemailer with file-backed transport and bilingual templates.

## Evidência

- [[src/auth/mail-config.ts]]
- [[src/auth/mail-sender.ts]]
