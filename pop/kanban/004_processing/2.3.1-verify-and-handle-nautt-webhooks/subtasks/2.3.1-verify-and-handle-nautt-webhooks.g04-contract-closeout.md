# Group g04 — Contract closeout — [[2.3.1-verify-and-handle-nautt-webhooks]]

- **Order:** 4
- **Depends on:** g01, g02, g03
- **Description:** Re-run gates, review DOX, and leave auditable evidence/spec handoff.

## Movements

- [x] (agent) Review every changed path against root, Prisma, and Nautt DOX; update those contracts only where route ownership, durable invariants, verification, or indices changed.
  - Expected: root routes the central callback, Prisma names delivery constraints/ACL invariants, Nautt records pre-parse HMAC/dedupe/reconciliation; no unnecessary child `AGENTS.md` is added.
  - If it fails: a local rule cannot be understood from the nearest contract + parents → add the smallest triggered child contract and refresh parent index, staying within DOX reference/line limits.
- [x] (agent) Re-run `pnpm db:generate`, `pnpm db:contract-check`, `pnpm db:test`, focused webhook tests, and `pnpm check`; save concise PASS evidence here.
  - Expected: migration replay/ACL/races, focused security/timing matrix, lint/typecheck/tests/build all pass.
  - If it fails: relevant regression → return to the owning group; unrelated pre-existing failure → record command/output and stop for orchestrator triage, never suppress it.
- [x] (agent) Run `python3 pop/scripts/pop_validate.py --standalone` and `git diff --check`; review the linked spec against reality.
  - Expected: PoP and whitespace validation pass; no silent spec divergence exists.
  - If it fails: planned behavior diverged materially → write the divergence in the spec **Open** and card notes, then return to 002; minor exact delivered detail remains queued for 006 implemented-slice sync.

## Execution notes

- In 006, update the linked Nautt spec with the delivered slice/outcome matrix in the same closeout as memory; do not mark it implemented during 004.
- Evidence: generated client/static contract/disposable DB all PASS; lint, typecheck, 477 tests (3 skipped), and production build pass with `/api/nautt/webhooks`; no 004 spec divergence, so implemented-slice sync remains correctly queued for 006.
- Remediation evidence: all returned 005 findings are now executable regressions; every focused acceptance command passes sequentially, disposable DB markers pass, and `CI=true pnpm check` passes with 484 tests (3 skipped) plus the production webhook route build.
