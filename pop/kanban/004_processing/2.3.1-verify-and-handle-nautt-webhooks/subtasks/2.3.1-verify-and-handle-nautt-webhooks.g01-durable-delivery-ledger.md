# Group g01 — Durable delivery ledger — [[2.3.1-verify-and-handle-nautt-webhooks]]

- **Order:** 1
- **Depends on:** none
- **Description:** Add constrained delivery/attempt persistence and a race-safe claim port.

## Movements

- [ ] (agent) Walk root and Prisma DOX, then add `WebhookDelivery`/`WebhookDeliveryAttempt`, owner/order relations, and one new forward-only migration with deterministic checks, unique delivery identity, indexes, and runtime DML only.
  - Expected: schema and SQL encode owner binding, normalized evidence, closed decisions/outcomes, lease/timestamps, positive attempts, and lowercase payload digest without storing raw bodies or secrets.
  - If it fails: optional composite FK is not representable → keep the relation enforced in reviewed SQL and expose only typed scalar fields to Prisma; do not remove the FK.
- [ ] (agent) Add `webhook-delivery-store.ts` with atomic insert/load + attempt creation + conditional claim, terminal replay, retryable failure, expired-lease reclaim, and durable finalize operations; provide an injected in-memory double with identical transitions.
  - Expected: two concurrent claims yield one worker; terminal replay cannot reclaim; a crashed lease becomes reclaimable; attempt numbers/evidence are monotonic.
  - If it fails: Prisma multi-call race leaves a gap → use one narrow transaction/raw statement and prove row counts; never hold a transaction over provider I/O.
- [ ] (agent) Extend DB static/runtime/disposable probes for migration count/checksum, constraints, cascades, collision race, reclaim, DML/sequence needs, and forbidden ownership/DDL privileges.
  - Expected: `pnpm db:generate && pnpm db:contract-check && pnpm db:test` prints all existing PASS markers plus webhook-delivery PASS markers.
  - If it fails: ACL or constraint mismatch → correct only the new migration/harness before it ships; never rewrite older migration history.

## Execution notes

- Capture migration name, constraint names, race winner counts, lease times, and ACL PASS output for 005.

