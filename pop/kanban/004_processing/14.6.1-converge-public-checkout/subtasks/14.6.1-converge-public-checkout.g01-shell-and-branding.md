# Frente F01 — Shell, V1 branding, copy and CSS — [[14.6.1-converge-public-checkout]]

- **Entrega:** the shared branded checkout shell for both eras (merchant header, scoped theme and accent, one 560px column, public footer), the privacy notice with its `Modal`, the additive V1 branding projection and page wiring, the V2 page recomposed on the same shell, every dictionary key this task needs in both locales, the retired `.checkout-*` rules and the updated accent whitelist.
- **Escopo:** the public `/pay/[identifier]` composition surface and the copy/CSS/token surfaces the other fronts consume read-only. No form logic, no payment or outcome view, no service semantics.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** o card, esta fatia, o [[14.6.1-converge-public-checkout.plan|plano]] (Objetivo, Estratégia, Critérios), `pop/researches/template-fidelity-convergence/public-checkout-and-inventory.md`.
- **Owns:** `src/app/pay/[identifier]/{page.tsx,loading.tsx,error.tsx,public-checkout-v2-page.tsx}`, os arquivos novos `src/app/pay/[identifier]/{checkout-shell.tsx,checkout-footer.tsx,checkout-privacy-notice.tsx}`, `src/checkout/public-checkout-presentation.ts`, `src/i18n/dictionaries/checkout/{en.ts,pt-BR.ts}`, `src/app/globals.css`, `scripts/check-design-tokens.mjs`, e o campo `Resultado` desta fatia.
- **May read:** `docs/template/app/src/pages/checkout/CheckoutPage.tsx`, `docs/template/app/src/components/{Footer.tsx,ui/Modal.tsx,ui/Monogram.tsx}`, `docs/template/app/src/i18n/{en,pt-BR}.ts`, `src/app/pay/[identifier]/**`, `src/app/store/[slug]/{page.tsx,pay/page.tsx}`, `src/app/{auth-card.tsx,language-preference/language-switcher.tsx}`, `src/components/ui/**`, `src/brand/**`, `src/checkout/**`, `pop/specs/{checkout-and-order-lifecycle,storefront-and-customization,media-storage,application-frontend-system}.md`, `DESIGN.md`, os `AGENTS.md` do DOX.
- **Must not edit:** `src/app/pay/[identifier]/{public-checkout-form.tsx,public-checkout-v2-form.tsx}` e os arquivos novos de F02/F03; `src/app/store/**`; `src/app/api/**`; `src/checkout/**` exceto `public-checkout-presentation.ts`; `src/components/ui/**`, `src/brand/**`, `public/**`, `prisma/**`; qualquer `*.test.*`; `tests/**`; specs, `DESIGN.md`, DOX; o card, o plano e as demais fatias.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] — *use para inventário anti-drift, estados obrigatórios e leis numéricas*; [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para declarar o contrato do DTO aditivo antes de codar*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** relatório com os arquivos alterados, a lista exata das chaves de dicionário publicadas (o contrato de copy que F02 e F03 consomem), as regras `.checkout-*` removidas e as mantidas com seu referenciador, status `concluída | BLOCKED`.
- **Critérios:** C01, C02, C08, C09, C10, C11.

## Contrato de execução

- **Shell:** one server composition shared by the V1 branch, the V2 checkout branch, the V2 paid branch, the unavailable view, `loading` and `error`. It renders the merchant header (owner logo through `/media/<identifier>` when present, otherwise `Monogram` `xl` over the display name, with the official merchant fallback kept for the unbranded case), the display name, the trust line, one column at the checkout cap, and the footer. It is the **only** file declaring `--storefront-accent`, so `scripts/check-design-tokens.mjs` gains exactly one entry for its path with exactly one match, and the entry of `public-checkout-v2-page.tsx` is removed if that file stops declaring it.
- **V1 branding:** extend `PublicCheckoutPresentation` with a `branding` member of the same shape and resolution rules already delivered for V2 (localized display name, accent, theme id with the design-system default, logo media identifier), selected from the owner row the existing query already joins, independent of `storefront_enabled`. Add nothing else: no link expiry, type, state, version or identifier enters the DTO, and the availability gates, the identifier pattern and the opaque miss stay byte-equivalent.
- **Footer and privacy:** the footer carries powered-by copy, the privacy link and `LanguageSwitcher` (the anonymous `qr_locale` cookie keeps deciding the locale — never `defaultLocale` for an anonymous buyer). The privacy notice is a small client boundary: the short line plus a trigger that opens `Modal` with the expanded text split from a dictionary key; F03 mounts it under the submit and F01 also places the link in the footer.
- **Copy:** publish every key this task needs — trust line, single-column headings, buyer-data legend, format error messages, submit with the total placeholder, start-over, generating-QR, scan caption, amount due, poll error and check-again, confirming-with-bank, the five terminal titles/bodies, success, refunded, powered-by, privacy line, privacy modal title and body — in `en` and `pt-BR` under the same names, following the interpolation form already used by the neighbouring dictionaries. F02 and F03 must not need a key you did not publish.
- **CSS:** delete only `.checkout-*` rules with no remaining referrer after this task's markup (`rg` before each removal); keep every rule `/store/[slug]/pay` still uses and name them in your report for 14.6.2. Emit no `checkout-` class name from any file you write — layout comes from the owned primitives and semantic utilities.
- Anything real but outside the card's "O quê / Por quê" is a follow-up proposal in your report — never a new criterion or a new front.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `9af00bd3` on `task/14.6.1-converge-public-checkout--f01`.
- **Arquivos alterados:** `src/app/pay/[identifier]/{page.tsx,loading.tsx,error.tsx,public-checkout-v2-page.tsx,checkout-shell.tsx,checkout-footer.tsx,checkout-privacy-notice.tsx}`, `src/checkout/public-checkout-presentation.ts`, `src/i18n/dictionaries/checkout/{en.ts,pt-BR.ts}`, `src/app/globals.css`, `scripts/check-design-tokens.mjs`.
- **Desvios:** nenhum.
- **Evidência:**
  - **Chaves de dicionário publicadas (en/pt-BR, mesmo nome):** `checkoutEmailFormatError`, `checkoutCpfFormatError`, `checkoutCepFormatError`, `checkoutPayWithTotal` (`{total}`), `checkoutStartOver`, `checkoutGeneratingQr`, `checkoutScanCaption`, `checkoutAmountDueLabel`, `checkoutPollErrorBanner`, `checkoutCheckAgain`, `checkoutConfirmingWithBank`, `checkoutOutcomeConfirmedTitle/Body`, `checkoutOutcomeRejectedTitle/Body`, `checkoutOutcomeCancelledTitle/Body`, `checkoutOutcomeExpiredTitle/Body`, `checkoutOutcomeRefundedTitle/Body`, `checkoutPoweredBy`, `checkoutPrivacyLine`, `checkoutPrivacyLinkLabel`, `checkoutPrivacyModalTitle`, `checkoutPrivacyModalBody`. Existing keys reused as-is: `checkoutTrustLine`, `checkoutSummaryHeading`, `checkoutCustomerHeading`, `checkoutPaymentHeading`, `checkoutTotalLabel`, `checkoutMerchantLogoAlt`, `checkoutMerchantFallbackAlt`, `storefrontFallbackName`, `languageLabel`, `close`.
  - **Regras `.checkout-*` removidas (sem referenciador restante):** `.checkout-shell`, `.checkout-shell > *`, `.checkout-main`, `.checkout-v2`, `.checkout-v2__rail`, `.checkout-v2__logo`, `.checkout-v2__name`, `.checkout-v2__columns`, `.checkout-v2__columns > *`, `.checkout-v2__summary`, `.checkout-v2__heading`, `.checkout-v2__lines`, `.checkout-v2__line`, `.checkout-v2__line-title`, `.checkout-v2__line-description`, `.checkout-v2__line-price`.
  - **Regras mantidas (referenciador ativo):** `.checkout-description`, `.checkout-card`, `.checkout-form`/`.checkout-payment`, `.checkout-copy` (+ nested), `.checkout-qr`, `.checkout-privacy` — todas ainda consumidas por `public-checkout-form.tsx`/`public-checkout-v2-form.tsx` (F03) e `src/app/store/[slug]/pay/standalone-payment-experience.tsx`; `.checkout-v2__total` (+ `span`) mantida só por `standalone-payment-experience.tsx`.
  - `node scripts/check-design-tokens.mjs` → exit 0, um único whitelist novo (`src/app/pay/[identifier]/checkout-shell.tsx`), a entrada antiga de `public-checkout-v2-page.tsx` removida (esse arquivo não declara mais `--storefront-accent`).
  - `./node_modules/.bin/tsc --noEmit` → 0 erros nos arquivos tocados; 2 erros pré-existentes e não relacionados (`src/app/nautt-credential-surface.test.tsx`, `src/auth/payment-link-v2-view.test.ts`) confirmados fora do diff desta frente.
  - `./node_modules/.bin/eslint` nos arquivos alterados → 0 erros.
  - `src/app/pay/**` (arquivos desta frente) não emite nenhuma classe `checkout-` — confirmado por `rg`.
