# Frente F02 — Contract checks, fixtures and database documentation — [[15.1.2-remove-v1-persistence-and-rebase-migrations]]

- **Entrega:** the static database contract, the database fixtures and the database documentation describe the re-pinned V2-only baseline, with no V1 assertion, fixture or grant residue.
- **Escopo:** the V1 assertions of `db-contract-check.mjs` (four models, three V1 migration blocks, baseline minimum), the V1 `payment_link` fixture block of `db-test.mjs`, the V1 grant residue check in `bootstrap.sql`, the baseline sentences in `prisma/AGENTS.md` and README's *Database foundation*, and the stale verifier stub line in `install/test.sh`. No behavior change in the installer or the containers.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card and the plan's *Estratégia*, *Riscos* and *Critérios* sections; F01's reported numbers; [[pop/notes/decisions/2026-09-22-v1-removal-and-checkout-decisions|2026-09-22 decisions]]; esta fatia.
- **Owns:** `pop/scripts/db-contract-check.mjs`, `pop/scripts/db-test.mjs`, `prisma/bootstrap.sql`, `prisma/AGENTS.md`, `README.md`, `install/test.sh`.
- **May read:** `prisma/schema.prisma`, `prisma/migrations/**`, `prisma/migration-policy-baseline.json`, `pop/scripts/migration-policy.mjs`, `pop/scripts/container-contract-check.mjs`, `install/update.sh`, `container/AGENTS.md`, `install/AGENTS.md`, `pop/specs/administrative-foundation.md`.
- **Must not edit:** `prisma/schema.prisma`, `prisma/migrations/**`, `prisma/migration-policy-baseline.json`, `pop/scripts/migration-policy*.mjs` (F01); `pop/specs/**`, o `AGENTS.md` da raiz e os demais `AGENTS.md` (15.1.3); `src/**`, `container/**`, `install/**` fora de `install/test.sh`.
- **Depends on:** F01 · **Entrada esperada:** a nova contagem de baseline, o digest âncora e os ids de diretório removidos/editados reportados por F01. Sem eles, `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use antes da primeira edição e a cada decisão de contrato*.
- **Web:** deny · **Gate/delta:** 003_human_approval (task `critical`) · nenhum delta.
- **Saída:** relatório curto ao agente principal com arquivos alterados, o delta de cobertura deixado pela remoção das fixtures V1 e o que ficou para a checklist humana; status `concluída | BLOCKED`.
- **Critérios:** C04, C05, C06, C07, C09 do [[15.1.2-remove-v1-persistence-and-rebase-migrations.plan|plano]].

## Contrato de execução

- Caminhe [`prisma/AGENTS.md`](../../../../../prisma/AGENTS.md), [`container/AGENTS.md`](../../../../../container/AGENTS.md) e [`install/AGENTS.md`](../../../../../install/AGENTS.md) antes da primeira edição de cada área.
- Em `prisma/AGENTS.md` e no README, reescreva **somente** a frase do baseline pinado (nova contagem e último id) e registre a rebase como a única exceção histórica autorizada; a proibição de DDL destrutiva em migração futura permanece literal.
- Em `bootstrap.sql`, o esperado é ausência de bloco ACL nomeando tabela V1: confirme e preserve byte a byte os blocos relation-guarded V2; grant genérico de tabelas não é resíduo.
- Ao remover as fixtures V1 de `db-test.mjs`, registre no Resultado o delta de cobertura (a prova de arquivamento/dependência ativa que rodava pelo trigger V1) — não crie trigger, tabela ou fixture nova para compensar.
- Não rode Docker, instalador nem nenhum comando do projeto sob container; `pnpm db:test` e `install/test.sh` são verificação humana.
- Entregar somente o escopo e os critérios desta frente.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `85ca5858` em `task/15.1.2-remove-v1-persistence-and-rebase-migrations`.
- **Arquivos alterados:** `pop/scripts/db-contract-check.mjs`, `pop/scripts/db-test.mjs`, `prisma/AGENTS.md`, `README.md`, `install/test.sh`. `prisma/bootstrap.sql` não precisou de edição — nenhum bloco ACL nomeando tabela V1 existia (a permissão `payment_link` sempre veio do `GRANT ... ON ALL TABLES` genérico, sem carve-out dedicado).
- **Delta de cobertura:** a fixture `payment-link-schema-and-locking` provava, contra um Postgres real, (a) as nove constraints de `payment_link`; (b) a trigger `payment_link_require_active_dependencies` rejeitando criação com produto/par de moeda inativo, inclusive sob corrida de lock concorrente (criação vs. desativação nas duas ordens); (c) rejeição de link para produto já arquivado. Nada disso é reproduzido para V2: a ativa-dependência V2 é service-fenced por contrato (`prisma/AGENTS.md`, bloco `payment_link_v2`), sem trigger equivalente — decisão já registrada no plano, não uma lacuna nova desta frente. Nenhuma trigger, tabela ou fixture nova foi criada para compensar.
- **Desvios:** removido também o helper `waitForLock` de `db-test.mjs` (ficou sem nenhum chamador após a fixture V1 sair — código morto local, seguro dentro do escopo do arquivo já possuído por esta frente).
- **Evidência:** `node pop/scripts/db-contract-check.mjs` → `PASS documentation-contract`; `pnpm db:contract-check` → `PASS migration-policy baseline=16 future=16` + `PASS migration-policy-language` + `PASS migration-policy-fixtures` + `PASS documentation-contract`; `node --check pop/scripts/db-test.mjs` e `bash -n install/test.sh` → sintaxe limpa. `db-test.mjs` continua Docker-dependente (`verify: user`, não executado por este agente) — cobertura real das mudanças fica para a checklist humana (C10/C11 do card).
