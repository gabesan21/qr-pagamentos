# Verification — [[6.1.1-define-panel-architecture-and-permissions]]

- **Stage:** 005_verifying
- **Reviewer:** independent yolo critic
- **Mode:** full — mandatory because the task is critical; reviewed the original objective, both linked durable specs, the complete changed surface, the current App Router inventory, related authorization contracts, and repository gates.
- **Delivery:** `291b4bd` (`docs: define exclusive panel architecture`)

## Original request

**Not yet met.** The delivery establishes the intended role-exclusive direction and updates only the authorized contract files, but the capability matrix is internally contradictory and omits compatibility decisions for two current authenticated routes. It therefore does not yet provide the single unambiguous target contract required before later panel implementation.

## Scope and contract evidence

- Changed surface is exactly `AGENTS.md`, `pop/specs/administrative-foundation.md`, and `pop/specs/administrative-design-system.md`; no ownership invasion or application behavior change was found.
- Root DOX and the design-system spec agree that shared visual sources do not create a shared shell or business capability.
- Public/sessionless identifiers, projections, opaque outcomes, unprefixed locales, and `[lang]/*` absence are preserved in the proposed text.

## Findings

1. **Blocking — the data boundary contradicts approved administrator inspection.** `pop/specs/administrative-foundation.md:53-59` says no administrator capability may read merchant-owned data, while the same matrix assigns administrator global order, catalog/link-directory, and analytics reads. Existing durable contracts explicitly preserve protected administrator order inspection (`pop/specs/checkout-and-order-lifecycle.md:15`, `pop/specs/product-scope.md:16`, `AGENTS.md:89-91`). Later tasks cannot tell whether the intended denial is access to merchant-owned routes/mutations or all merchant-owned records. Narrow the prohibition and explicitly preserve the approved read-only/global projections.
2. **Blocking — the current authenticated route inventory is not fully dispositioned.** `src/app/admin-access/route.ts:6-12` is an administrator-only endpoint outside `/admin*`, contradicting the new “administrative routes stay under `/admin`” rule without a retain/relocate/remove decision. `src/app/admin/users/[id]/nautt-credentials/route.ts:8-25` is an existing administrator mutation of a user's Nautt credential, while the matrix assigns Nautt credentials to merchant `USER` only and does not state the compatibility outcome for this route. AC1/AC2 and the card's requested compatibility decisions remain incomplete until both routes have explicit target behavior and denial semantics.

## Checks

- `python3 pop/scripts/pop_validate.py` — pass.
- `git diff --check develop...HEAD`; changed-file/scope inspection; acceptance `rg` probes — pass.
- `pnpm lint` — pass with 11 pre-existing warnings in unchanged `src/orders/order-view.ts`.
- `pnpm build` — pass outside the sandbox after the sandboxed run was unable to bind Turbopack's local helper port.
- `pnpm check` / `pnpm typecheck` — baseline failure at unchanged `src/app/storefront/route.test.ts:29` (`HeadersInit` inference); the delivery commit does not change that file.
- `pnpm test` — 723 passed, 6 skipped, with two baseline failures in unchanged surfaces: unresolved `server-only` during `src/auth/sign-in.test.ts` loading and the existing `pt-BR`/English expectation mismatch in `src/app/store/[slug]/page.test.tsx:56`. `git diff HEAD^ HEAD` is empty for all failure files, `server-request-log.ts`, and `package.json`.

## Decision

**Return to 004 — yolo 005 return 1 of 2.** Repair the two contract blockers, then repeat a full critical review.
