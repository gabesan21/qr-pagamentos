# Front F04 — Retire transitional font provenance — [[12.2.2-reconcile-template-identity-and-assets]]

- **Delivery:** make the installed-font contract exactly Sora, Inter, and IBM Plex Mono by removing the obsolete IBM Plex Sans transition from provenance, enforcement, package metadata, and lock state.
- **Scope:** narrow execution-plan-gap cleanup; F01/F02 functional identity, generator, derivatives, and outputs remain intact.
- **Owner:** agent.
- **Owns:** `src/design-system/fonts/provenance.json`, `scripts/check-font-provenance.mjs`, `scripts/font-provenance.test.ts`, `package.json`, `pnpm-lock.yaml`.
- **May read:** delivered F01/F02 results; `src/brand/template-asset-sources.ts`; `src/brand/assets.manifest.json`; generator/checker; DESIGN; application frontend spec; font package metadata and licenses.
- **Must not edit:** `src/brand/**`, `scripts/{brand-contract,generate-brand-assets}*`, `public/**`, `src/app/favicon.ico`, `docs/template/**`, target font files/licenses/tokens/imports, F03 paths, route UI, specs, media, DB/API/auth/security.
- **Depends on:** delivered F01 and F02.
- **Expected input:** runtime/generator/manifest contain no IBM Plex Sans consumption; the three target families and ten exact Latin WOFF2 weights are pinned and passing.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/sync-specs/SKILL|sync-specs]].
- **Criteria:** C06 and C09 in the [[12.2.2-reconcile-template-identity-and-assets.plan|plan]].

## Contract boundaries

- Delete `transitionalDependency` and the check that requires it; retain fail-closed package/version/integrity/license/CSS/WOFF2 checks for all three target families.
- Remove only `@fontsource-variable/ibm-plex-sans` from direct dependencies and its lock importer/package/snapshot entries. Preserve Sora, Inter, IBM Plex Mono, every unrelated dependency, and their exact pins.
- Extend focused mutations to prove the checker rejects target-family package/version/lock/provenance drift and any reintroduced IBM Plex Sans runtime/CSS consumption; removing the transition must not weaken supply-only or complete audits.
- Exact repository search must distinguish historical prose/evidence from executable provenance, package, lock, generator, manifest, and runtime references; no production contract may consume or require IBM Plex Sans.
- Any change to `src/brand/template-asset-sources.ts` or font provenance requires F02 to replay deterministic manifest generation/check after F04. The replay owns only `src/brand/assets.manifest.json` and generated verification, not functional generator/output changes.

## Verification and handoff

- Run focused font-provenance tests, `pnpm fonts:check`, exact searches, `pnpm brand:generate`, and `pnpm brand:check`; prove package/lock removal is isolated and `git diff --check` passes.
- Report F04 complete only with the three-family/ten-weight result and mutation evidence. Then hand off to narrow F02 manifest replay; F03 remains pending until that replay passes.

## Result

- **Status:** complete — the executable font contract now admits exactly Inter
  400/500/600, Sora 400/500/600/700, and IBM Plex Mono 400/500/600.
- **Delivery:** removed `transitionalDependency`, the direct
  `@fontsource-variable/ibm-plex-sans` pin, and only its importer/package/snapshot
  lock entries. The checker now rejects a reintroduced transitional key,
  dependency, lock record, runtime import, or CSS family while preserving the
  package/version/integrity/license/CSS/WOFF2 audit for all ten target weights.
- **Mutation evidence:** direct Vitest execution passed 25/25 cases, including
  every target package pin, every target family/weight import, lock integrity,
  provenance shape, missing/duplicate approved imports, remote hosts, legacy
  package/CSS consumption, and unapproved subsets.
- **Verification:** direct complete and supply-only provenance checks each
  reported `3 families, 10 exact Latin WOFF2 weights`; focused ESLint, repository
  TypeScript, repository ESLint (0 errors; 58 pre-existing warnings), and
  `git diff --check` passed. Exact executable search found legacy names only in
  rejection guards/tests, never package, lock, provenance, generator, manifest,
  or runtime consumption.
- **F02 handoff:** the pre-replay `brand:check` fails only on
  `src/brand/assets.manifest.json`. A temporary `brand:generate` followed by
  `brand:check` verified 17 sources and 28 deterministic derivatives; its sole
  generated manifest delta was discarded so F04 commits no `src/brand/**` path.
  F02 must now commit the planned manifest-only replay.
- **Environment note:** invoking `pnpm` after the lock change attempted a store
  sync and failed to open the environment-owned SQLite database. The same
  installed Node/Vitest/ESLint/TypeScript entrypoints were executed directly;
  the aggregate `pnpm` commands remain for the integrated gate after F02 replay.
