# Front F03 — Independent asset evidence — [[12.2.2-reconcile-template-identity-and-assets]]

- **Delivery:** focused mutation coverage and fresh run-bound Chromium evidence proving the integrated asset family without taking route/specimen ownership.
- **Scope:** test/evidence harness and generated evidence artifacts only.
- **Owner:** agent.
- **Owns:** new `tests/brand-assets.evidence.spec.ts`, new `scripts/run-brand-assets-evidence.mjs`, new `scripts/verify-brand-assets-evidence.mjs`, focused new/updated asset evidence tests, `artifacts/brand-assets/**`.
- **May read:** integrated F01/F02 output, existing Playwright/evidence conventions, parity evidence protocol, DESIGN, application frontend spec.
- **Must not edit:** `src/brand/**`, generator/contract files, `public/**`, favicon, `docs/template/**`, `src/media/**`, route/UI sources including `/design-system`, specs, tokens/fonts, `package.json`, `pnpm-lock.yaml`, dependencies, unrelated evidence.
- **Depends on:** F02.
- **Expected input:** checked manifest and byte-stable complete generated output inventory.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/sync-specs/SKILL|sync-specs]].
- **Criteria:** C02-C08 in the [[12.2.2-reconcile-template-identity-and-assets.plan|plan]].

## Evidence contract

- Start from a clean generated inventory and create a fresh run ID; never reuse a prior manifest, screenshot, or review.
- Render source target beside production derivative in repository Chromium with external requests blocked, fonts locally settled, animations/caret disabled, device scale factor 1, and deterministic background/context.
- Contact sheets cover 320, 375, 768, and 1440 widths; positive/reversed identity modes; all 17 source roles; all six swatches; favicon frames at native and magnified sizes; and representative fallback/illustration sizes.
- Raster comparison uses the frontend parity threshold `0.1` and max differing-pixel ratio `0.001`; explicitly documented derivations such as outlined wordmark and favicon use approved target masks while independent geometry/color assertions still apply.
- Assertions separately cover output inventory/hash/dimensions, accessible inline identity semantics, decorative asset contract, no overflow, no external request, and no console failure. Raster tolerance never waives them.
- Mutation tests prove missing/stale/unknown asset, altered source/output hash, wrong viewBox/frame/theme ID, duplicate bytes, unsafe SVG, live font/text, absent grant, and merchant-media classification fail closed.
- Run `node scripts/run-brand-assets-evidence.mjs`, `node scripts/verify-brand-assets-evidence.mjs`, the focused test file directly through the existing test runner, `pnpm frontend-parity:check`, `pnpm check`, and `git diff --check` at exact HEAD.

## Result

- **Status:** completed at task HEAD by the F03 executor; task intentionally remains in `004_processing` for aggregate ownership and closing gates.
- **Fresh run:** `20260803032854`; current manifest SHA-256 `5d1b08ba310f62c6f7d1950f5e76f31cb7b11e79dc0696487c4385d3fa18a1b2`; verifier passed against the current pointer, closed run directory, and every bound source/output/harness hash.
- **Chromium matrix:** four fresh contact sheets at `320`, `375`, `768`, and `1440` CSS pixels (device scale factor 1), covering all 17 source roles, 16 direct target/production pairs, all eight positive/reversed static identity variants, all six exact swatches, and `16/32/48` favicon frames at native and 128px magnified sizes. Screenshot hashes are `41701da6…c873`, `652d7242…82da`, `e94feb5c…1b9a`, and `a6b13c20…b710` in width order.
- **Independent assertions:** every direct Chromium raster pair and the approved left-`32x32` QR mask for the outlined logo passed threshold `0.1` / maximum differing-pixel ratio `0.001` with observed ratio `0`; the wordmark additionally proved outlined `160x32` geometry with no live text/font. Exact source/output bytes, hashes, SVG viewBoxes, PNG/ICO dimensions and frame hashes, inline identity semantics, decorative/adjacent naming modes, no horizontal overflow, zero external requests, zero console/page errors, ready images, and zero serious/critical axe findings passed. Visual inspection of the `320` and `1440` captures found no unresolved severity 2–4 issue.
- **Fail-closed evidence:** 13/13 probes passed for missing, stale, unknown, altered source hash, altered output hash, wrong viewBox, wrong favicon frame, wrong theme ID, duplicate bytes, unsafe SVG, live text/font, absent grant, and merchant-media classification. Probe artifact SHA-256: `49c61779…b3f7`; assertions SHA-256: `08aa5afc…a033`.
- **Gates:** direct runner/focused Playwright test passed (`1` test); `node scripts/verify-brand-assets-evidence.mjs`, `node scripts/generate-brand-assets.mjs --check` (`17` sources / `28` derivatives), `node scripts/check-font-provenance.mjs` (`3` families / `10` exact weights), and frontend parity (`2,230` records / `69` reachable files / `454` import edges / `48` routes) passed. `pnpm check` passed with `210` test files and `1,903` tests (`9` files / `28` tests skipped), a 51-page production build, zero lint errors, and 58 pre-existing warnings. `git diff --check` passed. The environment warning remains limited to current Node `26.5.1` versus pinned `26.4.0`.
- **Declared derivation limits:** outlined wordmark fidelity uses the approved QR mask plus independent outline/no-live-font geometry rather than treating a live-font browser raster as byte-equivalent; favicon fidelity uses exact generated frame hashes/dimensions plus native/magnified browser review. Route migration, `/design-system`, runtime UI, specs, media, tokens/fonts, dependencies, generators, and production asset bytes were not changed by F03.
