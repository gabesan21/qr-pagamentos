# Front F02 — Deterministic derivatives and contract — [[12.2.2-reconcile-template-identity-and-assets]]

- **Delivery:** one safe deterministic generator/checker with a closed manifest for identity, application artwork, theme swatches, and favicon derivatives.
- **Scope:** generated bytes, their contract, and brand DOX only; no consumer or evidence migration.
- **Owner:** agent.
- **Owns:** `scripts/brand-contract.mjs`, `scripts/brand-contract.test.ts`, `scripts/generate-brand-assets.mjs`, `src/brand/assets.manifest.json`, `src/brand/AGENTS.md`, `public/brand/**`, `public/application-assets/**`, `src/app/favicon.ico`.
- **May read:** F01 outputs, 12.2.1 token/font outputs, current generator/tests, application frontend spec, DESIGN, immutable source assets, parity obligations.
- **Must not edit:** F01 canonical/component files, `docs/template/**`, `src/media/**`, route UI, specs, token/font/package files, tests/evidence/artifacts owned by F03.
- **Depends on:** F01.
- **Expected input:** closed source register, canonical glyph, reviewed Sora outlines, identity API/fallback roles.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/sync-specs/SKILL|sync-specs]].
- **Criteria:** C01, C03, C04, C05, C06 in the [[12.2.2-reconcile-template-identity-and-assets.plan|plan]].

## Contract boundaries

- Keep the four identity IDs and positive/reversed static variants closed; the manifest may add application-art roles but cannot remap identities or treat every SVG as a brand lockup.
- `public/brand/**`, `public/application-assets/**`, the manifest, and favicon are generated only. A second generation must reproduce every byte and reject undeclared files.
- SVG validation uses a closed element/attribute inventory; reject script, image, foreignObject, style/event handlers, external/data/network references, live text/font dependencies, malformed geometry, duplicate bytes, and unsupported color semantics.
- Bind each source and derivative separately: immutable source SHA/bytes/viewBox/parity ID, derivation kind, output SHA/MIME/intrinsic dimensions/modes/accessibility, and exact provenance/grant references.
- Favicon frames derive only from the new QR glyph and approved semantic palette; missing sizes or incoherent ICO directory/frame bytes fail.
- Six swatches retain exact stored IDs and validate their visual colors against 12.2.1's canonical values; do not branch identity/component code by theme.
- Application fallbacks and illustrations remain static presentation resources. They never create upload capability, media ownership, storage identifiers, quota, or lifecycle state.

## Verification

- Run focused generator/contract/component tests after each coherent transformation; then `pnpm brand:generate`, repeat generation, and `pnpm brand:check`.
- Add negative probes for every fail-closed dimension and inspect `git diff --name-only` against `Owns`.

## Result

- **Status:** delivered.
- Replaced the superseded generator with one closed, fail-closed pipeline that verifies all 17 immutable template SVG records before emitting 28 unique derivatives: eight positive/reversed outputs for the four identity IDs, 16 application-owned presentation assets, three QR-glyph PNG frames, and one ICO bound to exact 16/32/48 frame hashes. The six swatches retain stored theme IDs and prove authored page/surface/border/accent colors against canonical 12.2.1 audit tokens.
- `assets.manifest.json` binds each source and derivative to parity ID, SHA-256, bytes or geometry, role, derivation, MIME, color/accessibility modes, source commit, and factual project origin “Os assets foram todos construídos para nosso projeto”. Static lockups use reviewed Sora outlines without live font; application fallbacks remain outside merchant media capability and lifecycle.
- The checker rejects inventory drift, unknown/duplicate outputs, incomplete provenance, unsafe/active SVG, unsupported colors, non-local/unresolved references, live text/font, malformed geometry/nesting, wrong swatches, remapped identities, and incoherent ICO bytes. Focused Vitest `25/25`, ESLint, TypeScript, `pnpm brand:check`, parity (`2230` records, `17` assets), and diff-check passed; repeated generation kept `79d7784c…` public and `84622b82…` ICO-plus-manifest aggregate hashes.
- Aggregate `pnpm lint` reached the pre-authorized F01 follow-up: `fonts:check` treated the literal Sora supply path in the F01 register as a runtime import. Obsolete IBM package/lock cleanup and source-register/checker reconciliation remained outside F02; no F02 generator, manifest, output, or DOX IBM Plex Sans reference remains.
- Replay after the F01/F04 provenance fixes regenerated only the manifest at SHA-256 `9d6479c9…`; a second generation and the direct `brand:check` entrypoint reproduced and verified the same bytes.
