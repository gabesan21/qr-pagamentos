# Front F01 — Audit schema and migration — [[11.1.2-add-admin-password-reset-action]]

- **Deliverable:** `password_reset_request` table/model with restrictive FK on `user_id`, plain `actor_id` UUID (no actor FK), `outcome` enum/varchar, and `created_at`; safe-language migration; bootstrap grant re-pin.
- **Scope:** add the audit persistence that records every administrator-initiated reset attempt without exposing secrets.
- **Responsible:** agent.
- **Owns:** `prisma/schema.prisma`, new `prisma/migrations/20260728*_password_reset_request/migration.safe.json` plus generated `migration.sql`, `prisma/bootstrap.sql`.
- **May read:** `prisma/AGENTS.md`, `prisma/migrations/20260726030000_user_soft_delete/`, `prisma/migrations/20260727233000_password_reset_token/`.
- **Must not edit:** any existing migration directory, generated Prisma client.
- **Depends on:** none.
- **Skills:** [[pop/skills/clean-code-change|clean-code-change]].
- **Criteria:** 1, 2, 7.

## Execution contract

- Add `PasswordResetRequest` to `schema.prisma` with fields `id`, `userId`, `actorId`, `outcome`, `createdAt`; FK `userId` → `User` on delete restrict; no FK on `actorId`.
- Create the next migration directory after the latest one, using `node pop/scripts/migration-policy.mjs generate` from a `migration.safe.json` that only creates the table, constraints, index, and grants `SELECT`/`INSERT` to `qr_runtime`.
- Re-pin the grant in `prisma/bootstrap.sql` inside the existing relation-guarded block so runtime receives exactly `SELECT`/`INSERT` and never `UPDATE`/`DELETE`.
- Run `pnpm db:generate`, `pnpm db:migration-policy`, and `pnpm db:contract-check`.

## Result

- **Status:** done
- **Commit/artifact:** —
- **Changed files:** `prisma/schema.prisma`, `prisma/migrations/20260728210000_password_reset_request/`, `prisma/bootstrap.sql`, `pop/scripts/db-contract-check.mjs`
- **Deviations:** Updated `pop/scripts/db-contract-check.mjs` to match the current `.env.example` (11.1.1 SMTP additions) and relaxed a whitespace-sensitive schema assertion; these were pre-existing contract drift uncovered by the required gate, not schema changes.
- **Evidence:** `pnpm db:generate` PASS, `pnpm db:migration-policy` PASS (baseline=19 future=14), `pnpm db:contract-check` PASS.
