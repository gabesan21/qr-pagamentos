# Verificação — [[15.1.4-phase-verification]]

- **Etapa:** 004_processing · **Papel:** `pop-phase-verifier` · **Rodada:** 2 — 2026-09-22
- **Base:** merge de `develop` @ `e1368da0` (traz 15.1.6, PR #16, `70b570f5`) · **Branch:** `task/15.1.4-phase-verification` · **Reparo rodada 2:** `30185c9d` (repara reparo direcionado do coordenador em `f724fca9`, superseded)
- **Rodada 1 (resumo):** BLOCKED em C1 — 7 erros de `tsc` por chamadas Prisma V1 residuais fora do `Owns`; proposta F1 → task `15.1.6` (fechada, PR #16). C2/C4/C5 já verdes; C3 verde na árvore rastreada. Reparos in-phase `70015161` (C6) preservados.
- **Ambiente:** Node 26.9.0 vs pin 26.4.0 (engine warning only; direct pnpm ran, `SHARP_IGNORE_GLOBAL_LIBVIPS=1`). No reinstall needed (`node_modules` already present).
- **Resultado:** **concluída** — C1–C8 all pass.

## Gates (C1–C5)

| # | Result | Evidence |
|---|--------|----------|
| C1 | **pass** | `pnpm check` exit 0. `tsc --noEmit`: 0 errors. `eslint`: 0 errors, 51 pre-existing warnings (unchanged). `vitest run`: **2142 passed / 28 skipped** (258 files, 20.27s). `next build`: compiles, typechecks, generates all 50 static pages. |
| C2 | **pass** | `db:migration-policy` → `PASS migration-policy baseline=16 future=16` + language/fixtures PASS; `db:contract-check` → `PASS documentation-contract`; `db:generate` → Prisma Client 7.8.0 generated, exit 0. |
| C3 | **pass** | `frontend-parity:check` with `src/generated/prisma` **absent** → `FRONTEND_PARITY_OK records=2226 reachable_files=69 import_edges=454 current_routes=47`, exit 0. No `PARITY_CURRENT_ROUTE_MISSING`, no class attributable to V1 removal or the 15.1.5 routes; anomaly `current-route:86a91d7b9224e135` intact. With the generated client **present** (e.g. right after `pnpm check`/`db:generate`): exit 1, 3× `PARITY_STATE_FIXTURE_INVALID` + 3× `PARITY_STATE_TARGET_INVALID` on `state:29b73275…/9b452e83…/e83683e0…` — pre-existing F2 (below), not repaired, not V1-attributable; the pass criterion is read on the tracked-only tree. |
| C4 | **pass** | `FRONTEND_PARITY_MUTATIONS_OK probes=26` (26/26 OK lines, matches 15.1.3 ledger and round-1 count). |
| C5 | **pass** | `ADMIN_CONTRACT_SYNC_OK task=1.4.4 adapters=0 index=absent implemented_slices=2 planned_slices=0 design=current`. |

## Repair this round — parity self-desync from round-1's own C6 repair

Round-1's C6 repair `70015161` removed a dead-V1-file comment inside `src/app/admin/payment-links/page.tsx` (owned, in-reach), which changed that file's sha256 and desynced the tracked `current-route` parity record (`current-route:583ee7d24053d9af`) built on the pre-repair hash — reported by C3 as `PARITY_CURRENT_ROUTE_MISSING` (new sha) + `PARITY_CURRENT_ROUTE_STALE`/`PARITY_SOURCE_HASH_STALE` (old sha). Not V1-attributable and not a 15.1.6 defect; self-inflicted by the phase's own in-reach edit, so repaired here rather than deferred.

**Directed repair (coordinator):** the first attempt (`f724fca9`, superseded) ran the closed `--refresh-semantic-contract` operation with `src/generated/prisma` present, which let the checker derive `state:29b73275…/9b452e83…/e83683e0…`'s `target.currentComponents`/`fixture.currentSurfacePaths` from the gitignored generated client and bake that into the tracked `obligations.ndjson` — a clean checkout without the generated client would then fail C3. Redone (`30185c9d`): reverted the two parity files to `develop`'s baseline, moved `src/generated/prisma` out of `src/` entirely, ran `node scripts/check-frontend-template-parity-contract.mjs --refresh-semantic-contract` (→ `FRONTEND_PARITY_REFRESH_OK records=2226 semantic_records=2010 current_routes=47`), then restored the generated dir — never a hand edit to `docs/frontend-template-parity/**`. Confirmed via `git diff --cached develop -- docs/frontend-template-parity/`: the only changed record is `current-route:583ee7d24053d9af` (plus the manifest's obligations sha/count it implies), zero `src/generated` references anywhere in the diff. Re-ran C3 both ways (row above) and C4 (unaffected, still 26 probes, below). No `src/**`/`tests/**` file changed this round, so `pnpm check` was not re-run (round-1's C1 result stands).

## C6 — round-1 repairs still valid after the merge

`70015161`'s repairs (i18n dead keys, `sourceInventory` prunes, dead-V1-comment removals, `check-design-system-coverage.mjs` count 2226) all still hold: C1's clean vitest run (2142/28) and the parity refresh above (records=2226) confirm no falsification survived the 15.1.6 merge. No new falsification found in `owns` from 15.1.6's diff (verified by grep: no `paymentLinkOrderId`/`client.paymentLink`/`db.paymentLink` residue in `src/**`, no comment naming a deleted V1 file).

## C7 — no new out-of-reach defect this round

F1 (round 1) closed via 15.1.6. F2–F4 (round 1) carry forward unchanged, still out of this front's reach:

- **F2 (suggestion, modification):** the parity checker's semantic state derivation reads whatever is on disk under `src/generated/prisma` (gitignored) when it runs, not just the tracked tree. It reproduces `PARITY_STATE_{FIXTURE,TARGET}_INVALID` on `state:29b73275…/9b452e83…/e83683e0…` whenever the generated client is present — which includes right after `pnpm check` (its `precheck` runs `db:generate`) or any local `db:generate`, since nothing removes the client afterward. Confirmed both ways this round: exit 0 with the generated dir absent, exit 1 present (C3 row above). Worse, `--refresh-semantic-contract` run with the client present writes that derivation into the **tracked** `obligations.ndjson` (discovered mid-round-2 by the coordinator, repaired in `30185c9d`), so a careless refresh converts a transient false failure into a permanent tracked-record defect. Limit discovery to tracked files, or exclude `src/generated/**` from the checker's derivation inputs outright.
- **F3 (suggestion, 15.3.1):** delete `PublicCheckoutForm` and relocate `createPollingController` when 15.3.1 removes the backoff/polling.
- **F4 (nit, 15.4.2):** 143 dictionary keys unreferenced since before the phase; fold into 15.4.2's dictionary debt.

## C8 — review pass (clean-code-review · ui-review, static, no Docker/screenshots)

Reviewed the 15.1.6 diff (25 files, PR #16) integrated on top of round-1's repair. All seven residual V1 calls are gone (grep confirms); the storefront `products` projection is now V2-only (active, unexpired `REUSABLE` link) with the spec (`storefront-and-customization.md`) and DOX (`src/integrations/nautt/AGENTS.md`) lines synced to match. No new blocking finding. Remaining historical "V1" comments in `src/checkout/standalone-checkout.ts`, `standalone-payment-status.ts`, `standalone-payment-experience.tsx`, `checkout-payment-views.tsx`, `checkout-experience.tsx`, `public-checkout-form.tsx` name **live** parity/type concepts (`CustomerSnapshotV1`, "V1 status vocabulary" as an analogy), not deleted files — out of C6's "no comment names a deleted V1 file" scope, left untouched. `336953de` (user commit: loopback HTTP for operator origins, Nautt webhook callback validator) remains untouched; no webhook call, no webhook registration, no Nautt network touched this round.

## Human checklist (carried to the PR "Verificação humana" section)

- **C9** `pnpm db:test` (Docker) → green; `to_regclass` of the four V1 tables is null.
- **C10** `install/test.sh` or a clean-checkout install → succeeds, no V1 table created.
- **C11** recapture the affected `tests/*.evidence.spec.ts` → V2-only captures, no missing screen, no V1 page targeted.
