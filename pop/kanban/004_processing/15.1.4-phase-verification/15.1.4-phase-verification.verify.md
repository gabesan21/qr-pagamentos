# Verificação — [[15.1.4-phase-verification]]

- **Etapa:** 004_processing · **Papel:** `pop-phase-verifier` · **Rodada:** 1 — 2026-09-22
- **Base:** `develop` @ `8065c35d` · **Branch:** `task/15.1.4-phase-verification` · **Reparo:** `70015161`
- **Ambiente:** Node 26.9.0 vs pin 26.4.0 (engine warning only; direct pnpm ran). `pnpm install` needs `SHARP_IGNORE_GLOBAL_LIBVIPS=1` on this host — see [[pop/notes/references/limites-de-verificacao|verification limits]].
- **Resultado:** **BLOCKED** — C1 cannot pass inside the front's `Owns`; the fix needs `src/auth`, `src/integrations/nautt`, `src/storefront`, one DOX line and one spec line (plan abort condition).

## Gates (C1–C5)

| # | Result | Evidence |
|---|--------|----------|
| C1 | **fail** | `tsc --noEmit`: 7 errors in 4 files (V1 Prisma calls on dropped tables/column, below). `pnpm lint`: exit 0 (0 errors, 51 pre-existing warnings). `vitest run`: 8 failures before repair → after repair **2142 passed / 28 skipped, exit 0**. `next build`: compiles, then fails at the same type check. |
| C2 | pass | `db:migration-policy` → `PASS migration-policy baseline=16 future=16`; `db:contract-check` → `PASS documentation-contract`; `db:generate` exit 0. |
| C3 | pass* | Exit 0 on the tracked tree (generated client absent). With `src/generated/prisma` present: 3 × `PARITY_STATE_{TARGET,FIXTURE}_INVALID` (`state:29b73275…`, `9b452e83…`, `e83683e0…`), all from the checker deriving the gitignored `src/generated/prisma/models/Session.ts` as a current component. Pre-existing, not V1-attributable; no `PARITY_CURRENT_ROUTE_MISSING`; anomaly `current-route:86a91d7b9224e135` intact. |
| C4 | pass | `FRONTEND_PARITY_MUTATIONS_OK probes=26` (26 OK lines, matching the 15.1.3 ledger). |
| C5 | pass | `ADMIN_CONTRACT_SYNC_OK task=1.4.4 … design=current`. |

## In-phase repairs (C6) — `70015161`, scope check green (16 paths)

- `scripts/check-design-system-coverage.mjs`: parity record count 2225 → 2226 (15.1.5 +3, 15.1.3 −2). This was the only test failure (8 cases in `verify-design-system-evidence.test.ts`); `design-system:source-check` → `obligations=2226 shared=187`.
- `src/i18n`: removed the two keys that died with the V1 order views (`orderBackToList`, `orderProduct`) from both locales and the test. "Only live keys" is read as "no key the removal killed". The other 4 suspect `orders` keys were already dead before the phase (F4).
- `tests/*.evidence.spec.ts`: removed `sourceInventory` entries for deleted files (`order-views.tsx` plus seven `directory-copy.ts`). Each one would crash `readFile` at recapture.
- Comments naming deleted V1 modules, or describing V1 surfaces as live: both dashboards, `checkout-form-validation.ts`, `public-storefront.ts`(+test) and `admin/payment-links/page.tsx`.

## Review (C8) — clean-code-review · ui-review (static, no Docker/screenshots)

- **Blocking — V1 persistence calls survive the 15.1.1 × 15.1.2 integration.** Each is a type error and, by inference (not executed, no DB), a runtime fault:
  - `src/auth/administration.ts:20,137,157` — `deactivatePaymentLinks` calls `client.paymentLink` (undefined), so the admin user deletion throws.
  - `src/auth/payment-link-v2.ts:356` — `identifierTaken` calls `db.paymentLink.count`, so V2 link creation throws.
  - `src/integrations/nautt/provider-order-store.ts:27,35,74,81,197,216` + `quote-ownership.ts:14` + `owner-pricing-orders.ts:111,127` — `paymentLinkOrderId` on the dropped column.
  - `src/storefront/public-storefront.ts:40-49,96-106,160-180,215-232` — the V1 `products` projection (products with an active V1 link) is still queried. Prisma rejects the `paymentLinks` relation, so `/store/[slug]` fails (UI severity 4). The page no longer renders that list (`page.test.tsx:98`).
- **Suggestion:** `PublicCheckoutForm` (V1 form) is alive only through tests; `createPollingController` still lives in its module (`public-checkout-form.tsx`), imported by the V2 and standalone experiences.
- **Out of scope, preserved:** `336953de` (user commit: loopback HTTP for operator origins, incl. the Nautt webhook callback validator) is not a phase change. It was untouched, and no webhook was tested.

## Follow-ups (C7) — zero change outside `Owns`

- **F1 (blocking, above reach):** proposed task `15.1.6-remove-residual-v1-store-calls`. It removes the calls above (+ test fakes in `administration.test.ts`, `profile-concurrency.test.ts`, nautt tests) and decides the storefront `products` member. It syncs `pop/specs/storefront-and-customization.md:27` ("active products that have at least one active payment link") and `src/integrations/nautt/AGENTS.md:35` (`paymentLinkOrderId`). 15.1.4 then gains `depends_on` on it and re-runs C1.
- **F2 (suggestion, modification):** the parity checker's semantic derivation reads gitignored `src/generated/**`, so C3 fails after every `pnpm check` (its `precheck` runs `db:generate`). Limit discovery to tracked files.
- **F3 (suggestion, 15.3.1):** delete `PublicCheckoutForm` and relocate `createPollingController` when 15.3.1 removes the backoff/polling.
- **F4 (nit, 15.4.2):** 143 dictionary keys have been unreferenced since before the phase (static name search; dynamic access not ruled out), incl. `ordersDescription`, `ordersEmpty`, `ordersEmptyDescription`, `orderCurrencyPair`. Fold into 15.4.2's dictionary debt.

## Human checklist (carried to the PR once unblocked)

- C9 `pnpm db:test` (Docker) → green; `to_regclass` of the four V1 tables is null.
- C10 `install/test.sh` or a clean-checkout install → succeeds, no V1 table.
- C11 recapture the affected `tests/*.evidence.spec.ts` → V2-only captures, no missing screen.
