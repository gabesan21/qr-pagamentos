# Front F04 — InputOTP visible-focus remediation — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** repair the existing InputOTP owner's applicable visible-focus state and prove it without changing its contract.
- **Scope:** one shared owner and its matching focused regression test only.
- **Owner:** agent.
- **Owns:** `src/components/ui/input-otp.tsx`, `src/components/ui/input-otp.test.tsx`.
- **May read:** C04/C10, F01 specimen/coverage output, paused F02 evidence source and finding, shared UI DOX/inventory, generated semantic Tailwind CSS, owned InputOTP/Radix implementation.
- **Must not edit:** every other `src/components/ui/**` path; all F01 specimen/dictionary files; paused F02 `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`, `package.json`, `scripts/check-design-system-coverage.mjs`, `scripts/verify-design-system-evidence.mjs`, `tests/design-system.evidence.spec.ts`; artifacts, specs and DOX.
- **Depends on:** completed F01 and the F02 finding that real Tab at `pt-BR`/320 yields `:focus-visible` with 44px height but computed outline `0` and box-shadow `none`.
- **Expected input:** `input#specimen-otp` exercises the production InputOTP owner through the integrated specimen; F02 browser proof remains paused and preserved.
- **Skills:** `ui-change`, `clean-code-change`, `shadcn`.
- **Criteria:** C04, C10.

## Execution contract

- Preserve exports, props, DOM/ARIA ownership, Radix/input-otp keyboard entry, active/invalid/disabled behavior and ≥44px target geometry; change only the visible-focus defect.
- Use the existing semantic focus token and owned Tailwind composition; do not add raw color, theme branch, page-local override, dependency or alternate primitive.
- The focused test must deterministically protect API/DOM/OTP behavior, target geometry contract and emitted semantic focus styling; the resumed F02 real-Tab probe supplies computed browser proof of a 3px-equivalent visible ring/outline.
- Validate the diff against the two exact `Owns` paths. Missing semantic projection or incompatible owner contract → `BLOCKED`; never edit another front.

## Result

- **Status:** pending.
- **Commit/artifact:** pending.
- **Files changed / deviations / evidence:** pending.
