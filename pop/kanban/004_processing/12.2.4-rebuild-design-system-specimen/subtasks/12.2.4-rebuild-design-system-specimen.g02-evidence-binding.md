# Front F02 — Evidence and parity binding — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** fresh, mutation-tested proof for complete coverage, real states and the 6×2×4 matrix at exact task HEAD.
- **Scope:** specimen coverage checker, runtime evidence runner/verifier, generated parity identity refresh and run artifacts.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, design-system evidence/checker scripts and tests, design-system evidence script entries in `package.json`, `artifacts/design-system/**`, canonically refreshed `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`.
- **May read:** F01 output, coverage/shared inventories, all bound owner sources, dictionaries, tokens/fonts/assets, parity checker/README, project Playwright config.
- **Must not edit:** specimen/runtime source, shared owners, `pnpm-lock.yaml`, other evidence families, template snapshot, product routes, specs/DOX.
- **Depends on:** F01.
- **Expected input:** integrated closed specimen and coverage map whose fixture identifiers are stable.
- **Skills:** `clean-code-change`, `ui-change`, `vercel-react-best-practices`.
- **Criteria:** C01-C08.

## Execution contract

- Derive assertions and source inventory from executable authorities; do not duplicate an unverified hand-maintained list in the test.
- Record exactly 48 base captures and bound real interaction/state probes; each manifest key includes locale, theme, viewport, task HEAD, source hashes and result.
- Assert both full/reduced motion, actual keyboard focus, hit targets, responsive modes, complete locale content, WCAG/axe, console/request cleanliness and final-state equivalence.
- Bind the 187 shared obligations and the current-only `/design-system` record without inventing a direct template route or raster golden; parity refresh is canonical and preserves protected fields/counts.
- Verifier mutation tests must reject owner, fixture/state, locale/theme/viewport, artifact, authority or source drift. Missing F01 input → `BLOCKED`; never edit it.

## Result

- **Status:** complete.
- **Commit/artifact:** initial atomic F02 commits `81e51fb47fe26af2ed039a759c92141da79c1903` and `15037c4`; final repair commit recorded after this result. Current evidence run `20260803193353`, manifest SHA-256 `d326a2374f59ae9b325d0849ebf6c7e64509caa9275c04ed148964a02ba5052f`, captured from integrated source HEAD `33404e99414bb0f2a49d717074ee38ec25731205`.
- **Files changed:** `tests/design-system.evidence.spec.ts`; `scripts/check-design-system-coverage.mjs`; `scripts/verify-design-system-evidence.{mjs,test.ts}`; `artifacts/design-system/{current.json,20260803193353/**}`; canonical `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`; this result and task telemetry.
- **Deviations:** none in F02 scope. Evidence resumed only after the approved F01/F05 dependency repairs landed, then all 67 bound-source hashes and the canonical parity contract were frozen before the single final replacement capture.
- **Evidence:** exactly 48 base captures (`2` locales × `6` themes × `4` viewports), 48 full/reduced-motion interaction probe pairs, 4,848 rendered state witnesses (`101` per capture), 1,104 rendered primitive witnesses (`23` per capture), no serious/critical axe findings, no horizontal overflow, and no console, page, or external-request failures. The suspicious `en`/`midnight-clearing`/`375` repeat is byte-identical to its base (`d6f66e2eb95f15e84ee22961513d37baf4f790ed8d4c39544af03d7f2af5807f`) with zero lingering toasts. Preflight probes verified the actual Button and linked Badge default/hover/active computed colors and >=4.5:1 foreground contrast in all six themes. Coverage independently verifies `20` entries, `101` applicable states, `72` explicit N/A states, `23` primitives, `2,230` parity records, and `187` shared obligations. Visual review covered final-run narrow light/dark and wide captures with no severity 2–4 finding. Mutation/coverage tests passed `20/20`, including deleted, duplicated, stale, hidden, mismatched, divergent-history, deterministic-repeat, authority, artifact, and bound-source failure cases. The verifier requires the captured source HEAD to remain an ancestor and all 67 bound sources to retain exact hashes, so later evidence/docs commits remain verifiable while source changes fail closed. Source inventory, parity, typecheck, lint (0 errors; 58 pre-existing warnings), production build, and the full `pnpm check` gate passed (`231` test files passed, `9` skipped; `1,972` tests passed, `28` skipped). One initial full-gate attempt exposed a transient upstream `input-otp` jsdom teardown exception after all tests passed; an unchanged clean rerun passed the entire gate.
