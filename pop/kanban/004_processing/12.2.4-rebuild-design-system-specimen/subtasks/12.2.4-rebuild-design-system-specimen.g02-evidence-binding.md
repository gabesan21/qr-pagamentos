# Front F02 — Evidence and parity binding — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** fresh, mutation-tested proof for complete coverage, real states and the 6×2×4 matrix at exact task HEAD.
- **Scope:** specimen coverage checker, runtime evidence runner/verifier, generated parity identity refresh and run artifacts.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, design-system evidence/checker scripts and tests, design-system evidence script entries in `package.json`, `artifacts/design-system/**`, canonically refreshed `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`.
- **May read:** F01 output, coverage/shared inventories, all bound owner sources, dictionaries, tokens/fonts/assets, parity checker/README, project Playwright config.
- **Must not edit:** specimen/runtime source, shared owners, `pnpm-lock.yaml`, other evidence families, template snapshot, product routes, specs/DOX.
- **Depends on:** F01.
- **Expected input:** integrated closed specimen and coverage map whose fixture identifiers are stable.
- **Skills:** `clean-code-change`, `ui-change`, `vercel-react-best-practices`.
- **Criteria:** C01-C08.

## Execution contract

- Derive assertions and source inventory from executable authorities; do not duplicate an unverified hand-maintained list in the test.
- Record exactly 48 base captures and bound real interaction/state probes; each manifest key includes locale, theme, viewport, task HEAD, source hashes and result.
- Assert both full/reduced motion, actual keyboard focus, hit targets, responsive modes, complete locale content, WCAG/axe, console/request cleanliness and final-state equivalence.
- Bind the 187 shared obligations and the current-only `/design-system` record without inventing a direct template route or raster golden; parity refresh is canonical and preserves protected fields/counts.
- Verifier mutation tests must reject owner, fixture/state, locale/theme/viewport, artifact, authority or source drift. Missing F01 input → `BLOCKED`; never edit it.

## Result

- **Status:** pending.
- **Commit/artifact:** pending.
- **Files changed / deviations / evidence:** pending.
