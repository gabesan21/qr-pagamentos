# Verificação — [[11.2.2-verify-six-theme-bilingual-experience]]

- **Etapa:** 005_closing · **Responsável:** crítico independente (yolo)
- **Modo:** `full` — task `critical: true` (revisão integral obrigatória mesmo após devolução por `execucao`; verificação independente em contexto fresco, sem reutilizar conclusões da rodada anterior).
- **Rodada:** 2 (`yolo_005_returns: 1`).

## Critérios verificados

| # | Critério | Resultado |
|---|----------|-----------|
| F01 | Coverage audit: runners vs requisitos | Atendido — matriz cobre shells admin/merchant, dashboards, tabelas, formulários, media, storefront, carrinho, checkout, mobile, teclado, reduced motion, WCAG 2.2 AA. |
| F02 | Evidence execution: runners prioritários locais | **Não atendido** — os runs de 2026-07-29 existem, mas estão stale em relação ao código final da task (ver achado bloqueante 1). |
| F03 | Severity-2+ fixes | Atendido — contraste `text-secondary`→`text-muted-foreground` e alias `--color-text-secondary` corretos no diff; delta da devolução anterior (totp key independente) corretamente implementado em `b04522a`. |
| F04 | Consolidation and gates (`:verify` verde) | **Não atendido** — 12 dos 18 `:verify` falham no estado integrado (ver achado bloqueante 1). |

## Evidências

- **Diff integral auditado** (`aab2575^..ad625f7`, sem contar artifacts): `package.json`, `scripts/generate-design-tokens.mjs`, `scripts/run-admin-evidence.mjs`, `scripts/run-reset-password-evidence.mjs`, `src/app/globals.css`, `src/app/profile/totp-section.tsx`, `src/app/admin/accounts/[id]/page.tsx`, `tests/admin.evidence.spec.ts` (removido), `tests/app-shell.evidence.spec.ts`, `tests/store-settings.evidence.spec.ts`.
- **Delta da devolução anterior — verificado e correto:** `b04522a` gera `totpEncryptionKey` independente via `randomBytes(32).toString("base64url")` nos dois runners, inclui o valor em `sensitiveValues` (redação de logs) e o escreve em `totp_encryption_key`; nenhuma outra reutilização da chave Nautt resta em `scripts/` (grep sobre `totp_encryption_key|totpEncryptionKey`); `install/install.sh` já gerava chave TOTP própria. Spec `pop/specs/identity-security.md:29` agora respeitada. Referência de commit no verify anterior (`9ed32b4`) confere com o log.
- **Demais mudanças de código — corretas:** alias `--color-text-secondary: var(--text-secondary)` adicionado a `SCOPED_PREVIEW_COLOR_ALIASES` e aos 7 blocos de tema de `globals.css` (6 temas + raiz), mantendo paridade gerador/CSS; `text-secondary`→`text-muted-foreground` em `totp-section.tsx` (6 ocorrências) e `admin/accounts/[id]/page.tsx`; remoção de `tests/admin.evidence.spec.ts` justificada (testava fluxo obsoleto de criação de produto em `/admin`) com `admin:evidence` remapeado para `admin-dashboard.evidence.spec.ts` + `verify-admin-dashboard-evidence.mjs` (wiring conferido em `scripts/run-admin-evidence.mjs:105-131`); `mobileLinkCount` 6/5 no app-shell corresponde ao contrato (5 primárias + link secundário `/profile` só no merchant, `src/app-shell/AGENTS.md:11`); redirect `/admin/settings?success=exchange-currency` em `store-settings.evidence.spec.ts` corresponde ao hub de settings entregue.
- **Re-execução independente dos 18 `:verify` (2026-07-31):**
  - Passam (6): `design-system`, `login`, `app-shell`, `catalog`, `profile`, `reset-password`.
  - **Falham (12):** `admin` (admin-dashboard), `merchant-dashboard`, `storefront`, `store-settings`, `checkout`, `standalone-payment` — `source inventory is stale: src/app/globals.css`; `orders`, `links`, `admin-orders`, `admin-users`, `admin-payment-links`, `admin-settings` — `source inventory is stale: scripts/run-admin-evidence.mjs`.
- **Causa raiz confirmada por hash:** o manifest pinado de `artifacts/admin-dashboard/20260729014605` registra `globals.css` com sha256 `7117fcfc…` (blob de `08b71d4`); o arquivo final commitado em `09aa4b3` tem sha256 `1f819348…`. Os runs terminaram 2026-07-28 23:36 local; a edição final de `globals.css` (alias) entrou às 23:39 e `09aa4b3` às 23:42 — **nenhum runner foi re-executado depois da mudança final de `globals.css`**, e nenhum foi re-executado depois de `b04522a` (2026-07-31), que alterou `run-admin-evidence.mjs`. O plano (F04) exigia explicitamente "re-run dos runners afetados".

## Achados

### Bloqueantes

- **B1 — Contrato de evidência run-bound quebrado em 12 escopos.** Os manifests pinam hashes de fonte; o código final da task (`globals.css` com o alias, `run-admin-evidence.mjs` com a totp key independente) nunca foi re-evidenciado. Consequência: `pnpm <scope>:evidence:verify` falha em 12 dos 18 escopos no estado integrado em `develop`, violando o critério 2 do plano ("runners passam: `pnpm *:evidence` + `:verify`"), o requisito F04 de re-run, e os contratos duráveis do AGENTS.md raiz ("`pnpm <scope>:evidence` / `:verify` hold the run-bound production evidence contract"). A evidência visual atual não cobre o código entregue — incluindo o próprio fix de contraste WCAG que motivou a mudança em `globals.css`. Nota: a afirmação da rodada anterior de que "seus `:verify` passaram" já não era verdadeira após `09aa4b3`; este gate só é válido com verificação verde no commit final.

### Sugestões / nits

- Nenhum adicional; o conteúdo dos fixes está correto — falta apenas re-evidenciar.

## Decisão

**Devolvido a 004_processing (`execucao`).** O executor não cumpriu o critério que recebeu (F04/critério 2: re-run dos runners afetados e `:verify` verde no estado final). Não é `lacuna` nem `premissa`: o plano cobria o pedido e a estratégia está certa; falta executar o re-run. Correção mecânica: re-executar os 12 runners stale contra o código atual (que já inclui o alias e a totp key independente), comitar os artifacts atualizados e confirmar os 18 `:verify` verdes. Não despacho a correção — quem relança é o orquestrador.

## Delta da devolução

- **Tipo:** `execucao` (volta a 004_processing).
- **Critérios/frentes afetadas:** critério 2 (runners/`:verify`) e F04 (consolidação); F02 reentra apenas na parte de **re-executar** os 12 escopos stale: `admin` (admin-dashboard), `merchant-dashboard`, `storefront`, `store-settings`, `checkout`, `standalone-payment`, `orders`, `links`, `admin-orders`, `admin-users`, `admin-payment-links`, `admin-settings`.
- **Frentes intactas (não reexecutar nem alterar):** F01 (matriz de cobertura); todos os fixes de F03 já aplicados e verificados — contraste `text-secondary`→`text-muted-foreground`, alias `--color-text-secondary` em `generate-design-tokens.mjs`/`globals.css`, totp key independente (`b04522a`), remapeamento admin→admin-dashboard, `mobileLinkCount` 6/5, redirect do store-settings. Os 6 `:verify` que passam (`design-system`, `login`, `app-shell`, `catalog`, `profile`, `reset-password`) não precisam de re-run. Nenhuma mudança de código de aplicação é necessária — somente re-run de evidência + refresh de artifacts + `:verify` verde.

---

# Reentrada 004 (2026-07-31) — delta executado

## O que foi feito

1. **Re-run dos 12 escopos stale** contra o código atual (`b0f1117`): 11/12 passaram de primeira; `merchant-dashboard` travou 2× (30 min) no `waitForURL('load')` após clique no switcher de período.
2. **Fix de race nos specs de evidência (F03, severity-2):** o switcher de período usa `<Link>` do Next.js (navegação soft — sem evento `load`); `waitForURL(url, 'load')` corria contra a transição client e podia nunca resolver. Substituído por polling web-first `expect(page).toHaveURL(...)` em `tests/merchant-dashboard.evidence.spec.ts` (2 pontos) e `tests/admin-dashboard.evidence.spec.ts` (2 pontos, mesmo componente compartilhado `DashboardPeriodNavigation`). Evidência do mecanismo: trace do run `20260731143425` mostra zero atividade de rede após o clique e snapshot com URL inalterada.
3. **Re-runs pós-fix:** `merchant-dashboard` OK em 3,5 min (antes: 2 timeouts de 30 min); `admin` falhou 1× com a nova asserção (5s, URL ficou em `?period=today` — prova de que o clique foi descartado pelo router) e passou no re-run. Runs falhos/parciais descartados (sem `manifest.json`); somente runs verdes comitados.
4. **18/18 `:verify` verdes** na varredura final (2026-07-31 12:33 local).

## Risco aceito (severity-3)

- **Flake intermitente do router Next 16.2.10:** raramente o clique em `<Link>` de search-param não inicia navegação (router drop, sem rede nem erro de console). Antes mascarado pelo `waitForURL('load')` que pendurava 30 min; agora falha rápido (5s) e visível. Sem evidência de impacto a usuário real fora do ambiente de evidência; se recorrente, avaliar `prefetch={false}` no `DashboardPeriodNavigation` em task própria.

## Estado final

- 18/18 `:verify` verdes; artifacts atualizados para o código final (globals.css com alias, runners com totp key independente); B1 resolvido.
