# Front F01 — Pinned font supply — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** an offline-capable, exact-weight font dependency set with committed OFL license and provenance evidence.
- **Scope:** replace UI consumption of IBM Plex Sans with Inter, Sora and IBM Plex Mono sources; retain its package only as transitional brand provenance for 12.2.2; no CSS consumption or UI edits.
- **Owner:** agent.
- **Owns:** `package.json`, `pnpm-lock.yaml`, `src/design-system/fonts/**`, `scripts/check-font-provenance.mjs`, `scripts/font-provenance.test.ts`.
- **May read:** `DESIGN.md`, application frontend spec, template `index.html`/`index.css`, current Fontsource package metadata and lock importer.
- **Must not edit:** `src/app/**`, `src/components/**`, token JSON/resolver, design-token scripts/tests, evidence tests/artifacts, specs and task files.
- **Depends on:** none.
- **Expected input:** exact families/weights and no-remote/OFL contract from `DESIGN.md`.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Criteria:** C04, C07.

## Execution contract

- Pin exact package versions; install only Latin production CSS/WOFF2 needed for `pt-BR` and `en` and the admitted weights.
- Commit human-readable OFL texts and a deterministic manifest/check binding packages, versions, lock integrity, imports, weights, license IDs/hashes, and local asset paths.
- Preserve `@fontsource-variable/ibm-plex-sans` only because the current brand manifest still references its provenance and 12.2.2 owns that migration. Prove zero IBM Plex Sans runtime/CSS import/request usage; do not introduce remote URLs, runtime loaders, unapproved weights, or copied font bytes without provenance.
- Missing/incompatible package or license is `BLOCKED`; do not substitute another family.
- Deliver only this scope; report altered files and focused test evidence without staging or committing.

## Result

- **Status:** complete — pinned local supply and fail-closed provenance gate delivered; ready for F02 runtime imports.
- **Commit/artifact:** uncommitted F01 changes in `pop/worktrees/12.2.1-rebuild-template-tokens-and-typography`; `src/design-system/fonts/provenance.json` binds 3 packages, 10 admitted weights, package-lock integrities, CSS/WOFF2 bytes and committed licenses.
- **Files changed:** `package.json`; `pnpm-lock.yaml`; `src/design-system/fonts/provenance.json`; three byte-identical files under `src/design-system/fonts/LICENSES/`; `scripts/check-font-provenance.mjs`; `scripts/font-provenance.test.ts`.
- **Deviations:** none from F01 ownership. The complete `pnpm fonts:check` intentionally remains red until sequential F02 replaces the existing `src/app/globals.css` IBM Plex Sans import with the ten approved Latin entrypoints; `--supply-only` is the F01 boundary. `@fontsource-variable/ibm-plex-sans@5.2.8` remains pinned and UI-inert-after-F02 solely for the current brand manifest until task 12.2.2. The runner reports Node `26.5.1` versus the pinned `26.4.0`, without affecting focused results.
- **Evidence:** registry metadata confirmed `@fontsource/inter@5.3.0`, `@fontsource/sora@5.3.0`, and `@fontsource/ibm-plex-mono@5.3.0` as `OFL-1.1`; admitted weights are Inter 400/500/600, Sora 400/500/600/700, and IBM Plex Mono 400/500/600. `node scripts/check-font-provenance.mjs --supply-only` passed (3 families, 10 Latin WOFF2 weights); `pnpm exec vitest run scripts/font-provenance.test.ts` passed 7/7 including legacy/remote/subset/missing/duplicate mutations; focused ESLint passed; `pnpm db:generate && pnpm typecheck` passed; `pnpm brand:check` verified 12 deterministic assets; all three committed licenses are byte-identical to installed package licenses; `git diff --check` and F01 allowlist scope passed. The complete checker failed only on the expected pre-F02 legacy import plus ten not-yet-installed runtime imports.
