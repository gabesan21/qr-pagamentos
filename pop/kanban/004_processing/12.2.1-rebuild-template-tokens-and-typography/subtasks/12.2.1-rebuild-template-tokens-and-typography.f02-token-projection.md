# Front F02 — DTCG graph and CSS projection — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** the exact, accessible, layered DTCG source and byte-stable CSS consumed by current and future UI.
- **Scope:** token files, resolver/validation/generation, theme-ID adapter, CSS imports/variables and static tests; no page/component redesign.
- **Owner:** agent.
- **Owns:** `src/design-system/tokens/**`, `src/design-system/themes.ts`, `src/design-system/themes.test.ts`, `scripts/design-token-graph.mjs`, `scripts/generate-design-tokens.mjs`, `scripts/check-design-tokens.mjs`, `scripts/design-tokens.test.ts`, `src/app/globals.css`.
- **May read:** F01 font manifest/imports, `DESIGN.md`, application frontend spec, template CSS/config, parity obligations, existing UI consumers and DOX.
- **Must not edit:** F01-owned files, route/component source, `src/app-shell/**`, evidence tests/artifacts, backend/API/database, specs and task files.
- **Depends on:** F01.
- **Expected input:** verified pinned family imports and provenance manifest.
- **Skills:** [[.agents/skills/design-tokens/SKILL|design-tokens]], [[.agents/skills/color-expert/SKILL|color-expert]], [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Criteria:** C01, C02, C03, C05, C07.

## Execution contract

- Use DTCG 2025.10 valid types/references and resolver ordering; validate nested/composite aliases, external sources, names, units, values, cycles and identical theme paths before generation.
- Preserve the template CSS hash and exact sRGB audit objects; encode the fixed tertiary projections and objective contrast/gamut checks at the semantic boundary.
- Project semantic/component variables plus value-free current aliases, exact local typography roles, geometry/elevation/focus/motion, safe light/dark fallbacks and scoped theme previews.
- Keep the six stored IDs/order/default and do not implement template localStorage state or branch components by ID.
- Expand raw-value lint only within the established frontend/token boundary; do not repair unrelated routes or components.
- Missing F01 input or a contract-incompatible consumer is `BLOCKED`; do not cross ownership.
- Deliver only this scope; report altered files and focused gates without staging or committing.

## Result

- **Status:** complete — the six-context DTCG 2025.10 graph, strict resolver/validator, deterministic CSS projection, local typography consumption, and value-free compatibility bridge now include the F03 contrast delta and are ready for the visual-evidence retry.
- **Commit/artifact:** uncommitted F02 changes in `pop/worktrees/12.2.1-rebuild-template-tokens-and-typography`; `src/design-system/tokens/resolver.json` resolves primitive → theme → semantic → component → motion sources, and the generated block in `src/app/globals.css` is current.
- **Files changed:** `src/design-system/tokens/**`; `src/design-system/themes.ts`; `src/design-system/themes.test.ts`; `scripts/design-token-graph.mjs`; `scripts/generate-design-tokens.mjs`; `scripts/design-tokens.test.ts`; `src/app/globals.css`. The contrast reentry changed only `src/design-system/tokens/**`, `scripts/design-token-graph.mjs`, `scripts/generate-design-tokens.mjs`, `scripts/design-tokens.test.ts`, and generated `src/app/globals.css`; no F01 package/provenance, route, component, shell, evidence, backend, spec, or task source was edited in the task worktree.
- **Deviations:** the reentry gate `pnpm frontend-parity:check` is red on inherited HEAD `1593f91`, outside F02 ownership: `/design-system` reports current-route missing/stale and source hash stale for `src/app/design-system/page.tsx`; the F02 diff does not touch that page or parity artifacts. The transitional IBM Plex Sans package remains installed solely for the F01-declared brand-provenance handoff to 12.2.2, but has no runtime/CSS import. The runner reports Node `26.5.1` versus the pinned `26.4.0`.
- **Evidence:** the contrast delta preserves all 120 immutable audit values, derives every strong success/warning/danger/info role toward primary text until it passes `4.5:1` against page/raised/secondary surfaces, and selects black or white as the deterministic highest-contrast on-strong role. Value-free compatibility aliases now map `destructive`, `warning`, and `success` to strong feedback colors, while explicit soft and soft-foreground roles remain paired. The lowest strong/surface ratio is `4.505:1`; the lowest on-strong ratio is `5.134:1`; the lowest soft pair is `10.754:1`. The `pix-paper` danger projection is `#c13b3a` at step 7 so the existing `hover:bg-destructive/90` stays at least `4.510:1` across all three surfaces. `pnpm tokens:generate`, `pnpm tokens:check`, focused Vitest 30/30, complete `pnpm fonts:check`, `pnpm db:generate && pnpm typecheck`, relevant ESLint, `pnpm brand:check`, `git diff --check`, and the F02 owner allowlist passed. The earlier full parity pass remains historical evidence; the current inherited parity drift is recorded above for its owner. Mutation coverage rejects missing external sources/aliases, cycles, invalid names/types/units, out-of-gamut sRGB, and hex/component drift; all six tertiary projections, strong/soft feedback pairs, deterministic foreground choices, destructive hover composites, exact geometry/elevation/focus/type values, 13 reachable motion timings, three easings, and reduced `0.01ms`/one-iteration resolution are pinned.

## Reentry r1 result

- **Status:** completed at task commit `cd59856`; only the judgment's F02 focus delta changed.
- **Delivery:** immutable translucent audit-ring bytes remain exact, while each theme aliases `color.focus.ring` to an opaque accessibility projection. Minimum page/raised/secondary ratios are `3.199/3.512/3.006` (pix), `5.462/5.918/5.210` (cashier), `4.342/4.734/4.001` (sand), `12.770/11.650/10.383` (midnight), `5.849/5.417/4.773` (vault), and `10.752/10.051/9.241` (amber).
- **Evidence:** deterministic tests assert every semantic projection at `>=3:1` and prove the original alpha composite still fails at least one adjacent surface in every theme. Token generation/check, 29 focused tests, lint with the unchanged 58-warning baseline, diff-check and the nine-file F02 allowlist passed; F01/F04/F05 and F03-owned files remained intact.
