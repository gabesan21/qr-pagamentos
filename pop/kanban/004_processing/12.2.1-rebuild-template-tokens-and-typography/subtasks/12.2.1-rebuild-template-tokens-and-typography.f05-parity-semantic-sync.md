# Front F05 — Canonical parity source sync — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** a fail-closed canonical refresh that synchronizes current-route source identity and binds the authorized F04 `/design-system` delta.
- **Scope:** extend, document, test and run the existing refresh narrowly; no hand edit, route remapping, production change, or evidence capture.
- **Owner:** agent.
- **Owns:** `scripts/check-frontend-template-parity-contract.mjs`, `docs/frontend-template-parity/README.md`, focused parity refresh tests/probes if needed, `docs/frontend-template-parity/obligations.ndjson`, `docs/frontend-template-parity/manifest.json`.
- **May read:** parity README/checker, current contract, `src/app/design-system/page.tsx`, F04 result and F02 contrast result.
- **Must not edit:** application/CSS/token/font/component source, unrelated scripts, package/lock, specs, evidence harness/artifacts, F01/F02/F03/F04 outputs and task files.
- **Depends on:** completed F04 and F02 contrast commit `0724674`.
- **Expected input:** only the route source/hash drift reported by `pnpm frontend-parity:check`; all approved mappings and production changes remain intact.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/sync-specs/SKILL|sync-specs]].
- **Criteria:** C09, C05, C07.

## Execution contract

- Snapshot record count plus every obligation ID, target, owner, disposition, reason and evidence before writing.
- Extend the existing refresh so each exact current-route inventory entry updates only its matched record's `source.path` and `source.sha256`; fail on missing, duplicate or ambiguous identity rather than adding, deleting or remapping records. Document the expanded canonical behavior.
- Add focused proof that stale route source identity refreshes and invariant tampering/missing/duplicate mappings fail closed, reusing the mutation-probe style where practical.
- Run exactly `node scripts/check-frontend-template-parity-contract.mjs --refresh-semantic-contract`; never edit the NDJSON or manifest by hand.
- Compare structurally: preserve 2,230 records, stable IDs, targets, owners, dispositions, reasons and evidence; permit only current-route source identity, three regenerated semantic families and manifest obligation hash binding to differ.
- Run `pnpm frontend-parity:check`, the semantic mutation probes, `git diff --check`, and a scope allowlist before returning F03 to execution.
- If exact current inventory cannot match one existing stable current-route record or any invariant changes, report `BLOCKED` with the diagnostic and unchanged snapshots; never hand-edit.
- Deliver only this delta; report changed files and exact before/after invariants without staging or committing.

## Result

- **Status:** pending second-reentry execution; prior F01/F02/F04 and F03 harness results remain historical and intact.
