# Front F03 — Runtime visual evidence — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** fresh, verifier-bound browser proof for the installed theme and typography foundation.
- **Scope:** enhance and run existing `/design-system` evidence without changing the specimen/page or shared components.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, `scripts/verify-design-system-evidence.mjs`, `artifacts/design-system/**` (new run and current pointer only).
- **May read:** F01/F02 output, existing specimen, Playwright config, prior evidence format, `DESIGN.md`, application frontend spec.
- **Must not edit:** application/CSS/token/font source, package/lock, other evidence families, prior immutable evidence runs, specs and task files.
- **Depends on:** F02.
- **Expected input:** current generated CSS, passing focused token/font gates and locally available production fonts.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Criteria:** C06, C07.

## Execution contract

- Assert computed Inter body, Sora heading/tracking and IBM Plex Mono numeric role after `document.fonts.ready`, with exact admitted weights and no fallback result.
- Bind all six themes at 320/375/768/1440 to computed semantic color, tertiary, radius, focus, elevation and reduced-motion values; keep independent overflow, target, keyboard, axe, console and external-request assertions.
- Produce exactly 24 fresh PNGs plus hashed assertions/manifest/current pointer, inspect them, and bind a review with no unresolved severity 2+.
- Do not rebuild the component specimen or claim bilingual/state completeness owned by 12.2.4.
- Missing runtime/font or failed objective/visual assertion is `BLOCKED`; do not weaken the verifier.
- Deliver only this scope; report run ID/files/evidence without staging or committing.

## Reentry contract

- Retry only after F04 is complete. Keep this front's original evidence-only ownership and verify the real F04 ID/money consumers; do not add test-only DOM, page/CSS edits, or specimen structure.
- The Result below records the first attempt and remains historical evidence; replace it only with the retry outcome after executing the unchanged C06/C07 gate plus C08 computed-style assertions.

## Result

- **Status:** BLOCKED — the existing specimen does not expose the required IBM Plex Mono numeric role, so the F03 fail-closed rule prevents generating or claiming a fresh evidence run.
- **Commit/artifact:** none. No run directory was created and `artifacts/design-system/current.json` remains the prior immutable `20260729012804` pointer (SHA-256 `78aacc7b0fedf7914651fb035831a2987d057036f472348a2a879d67f46110b5`).
- **Files changed:** no F03-owned file. Only this workflow-root Result was updated; the task worktree evidence test, verifier, prior runs, and current pointer remain untouched.
- **Deviations:** none. The executor did not inject a test-only numeric node, weaken the verifier, edit the specimen, or cross into F02-owned CSS. The blocking repair belongs to an earlier owner: make a real existing numeric specimen element consume the numeric role and remove the visible legacy `IBM Plex Sans` claim before F03 is retried.
- **Evidence:** production `pnpm build` passed after the sandbox-only Turbopack port restriction was rerun with escalation (51/51 static pages). A headless Chromium probe at `/design-system`, after `document.fonts.ready`, measured body `Inter, system-ui, sans-serif` at weight 400; `h1` `Sora, sans-serif` at weight 400 with `-0.48px` tracking (the exact `-0.02em` projection at 24px); and the first money cell in `table.ds-facts` as `Inter, system-ui, sans-serif` at weight 400 with only `tabular-nums`, not IBM Plex Mono. Source inventory confirms the specimen contains no `.font-mono`, `.font-money`, `code`, or `kbd` consumer, while its visible receipt rail still says `IBM Plex Sans`. The same probe measured full motion `.18s` / one iteration and reduced motion `.01ms` (`1e-05s`) / one iteration. Because the mandatory numeric role is absent, the 24-capture matrix, review, verifier, and scope gates were intentionally not fabricated or claimed.
