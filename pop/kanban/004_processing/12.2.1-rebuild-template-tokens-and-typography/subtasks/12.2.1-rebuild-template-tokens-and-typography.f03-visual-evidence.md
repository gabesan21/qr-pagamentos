# Front F03 — Runtime visual evidence — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** fresh, verifier-bound browser proof for the installed theme and typography foundation.
- **Scope:** enhance and run existing `/design-system` evidence without changing the specimen/page or shared components.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, `scripts/verify-design-system-evidence.mjs`, `artifacts/design-system/**` (new run and current pointer only).
- **May read:** F01/F02 output, existing specimen, Playwright config, prior evidence format, `DESIGN.md`, application frontend spec.
- **Must not edit:** application/CSS/token/font source, package/lock, other evidence families, prior immutable evidence runs, specs and task files.
- **Depends on:** F02 initially; F04 and F05 before the current retry.
- **Expected input:** current generated CSS, passing focused token/font gates and locally available production fonts.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Criteria:** C06, C07.

## Execution contract

- Assert computed Inter body, Sora heading/tracking and IBM Plex Mono numeric role after `document.fonts.ready`, with exact admitted weights and no fallback result.
- Bind all six themes at 320/375/768/1440 to computed semantic color, tertiary, radius, focus, elevation and reduced-motion values; keep independent overflow, target, keyboard, axe, console and external-request assertions.
- Produce exactly 24 fresh PNGs plus hashed assertions/manifest/current pointer, inspect them, and bind a review with no unresolved severity 2+.
- Do not rebuild the component specimen or claim bilingual/state completeness owned by 12.2.4.
- Missing runtime/font or failed objective/visual assertion is `BLOCKED`; do not weaken the verifier.
- Deliver only this scope; report run ID/files/evidence without staging or committing.

## Reentry contract

- Retry only after F04 and the canonical F05 parity sync are complete. Keep this front's original evidence-only ownership and verify the real F04 ID/money consumers; do not add test-only DOM, page/CSS/parity edits, or specimen structure.
- The Result below records the first attempt and remains historical evidence; replace it only with the retry outcome after executing the unchanged C06/C07 gate plus C08 computed-style assertions.

## Result

- **Status:** BLOCKED — the post-F04 retry proves the numeric role but now fails closed on a serious runtime contrast regression owned by the token projection: `text-destructive` resolves to the danger-soft surface color on the page surface.
- **Commit/artifact:** none. No valid run was selected and no capture was claimed. Incomplete pre-capture run directories were removed; `artifacts/design-system/current.json` still names the prior immutable `20260729012804` run. The replacement harness remains uncommitted in the task worktree.
- **Files changed:** only the F03-owned `tests/design-system.evidence.spec.ts` and `scripts/verify-design-system-evidence.mjs`, plus this workflow-root Result. The harness now requires the exact 6-theme × 4-viewport matrix, atomic final-only pointer replacement, CSSOM-canonical exact semantic tokens including tertiary/radius/focus/elevation, all admitted local font faces and the four real F04 numeric consumers after `document.fonts.ready`, full/reduced motion, keyboard/target/overflow/isolation/console checks, serious/critical axe closure, and hashes for assertions, PNGs, token files, font provenance, brand manifest, source inventory, manifest, and current pointer.
- **Deviations:** none. The executor did not exclude axe rules, weaken contrast thresholds, inject test-only DOM, edit the specimen/CSS/tokens/components, overwrite a prior run, or publish a partial pointer. Repair requires an F02/token-projection owner to separate the destructive foreground used by text from the soft destructive surface before F03 can retry.
- **Evidence:** ESLint on both F03 files, `pnpm typecheck`, and `git diff --check` passed repeatedly. Production `pnpm build` passed with 51/51 static pages. The browser loaded all pinned Inter 400/500/600, Sora 400/500/600/700 and IBM Plex Mono 400/500/600 Latin faces; computed body was Inter 14/20/400, `h1` was Sora 24/32/400 with `-0.48px` tracking, and all four PIX/BRL cells were quoted `IBM Plex Mono`, 14/20/400, `tabular-nums`. Exact palette/motion serializations were canonicalized without changing values. At the first `pix-paper` 320px axe gate, `color-contrast` reported five serious nodes: invalid field labels, two error messages, and the invalid textarea use computed `#fadfda` over `#f7f4ee`, ratio 1.15:1 versus required 4.5:1. This follows `--destructive: var(--color-feedback-danger-soft)` while current components use `text-destructive`. Exactly zero valid screenshots were retained, so visual review and verifier were correctly not claimed.
- **Attempt history:** the first F03 attempt remains superseded evidence: before F04, the specimen had no real numeric-role consumer and still named IBM Plex Sans. F04 resolved that blocker exactly; this retry exposed the independent rendered-contrast blocker above.
