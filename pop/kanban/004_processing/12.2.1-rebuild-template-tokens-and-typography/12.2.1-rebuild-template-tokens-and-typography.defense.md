# Plan defense — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Stage:** 002_planning · **Owner:** agent planner

## Contestable decisions

| # | Decision | Adopted choice | Rejected alternative | Why | What would falsify it |
|---|---|---|---|---|---|
| 1 | Token architecture | Separate DTCG primitive/audit, semantic, component and modifier sources resolved by the repository generator | Keep the monolithic legacy theme file or add runtime styling | Stable aliases preserve one source and six contexts without a runtime dependency | Custom resolution cannot validate or resolve the composite/alias graph required by C01 |
| 2 | Exactness versus accessibility | Preserve every template byte as audit data and change only constrained rendered semantics | Mutate source colors or render known failing pairs exactly | The active contract makes WCAG win while retaining parity evidence | C02 cannot produce accessible semantics without losing the immutable audit values |
| 3 | Font source and legacy provenance | Exact pinned static-weight Fontsource packages with OFL files/provenance; retain `@fontsource-variable/ibm-plex-sans` UI-inert until 12.2.2 migrates its brand-manifest reference | Remove the legacy package before its provenance consumer migrates, use Google Fonts runtime, system substitution, or an unrestricted variable range | Target UI admits only approved weights and builds offline, while the explicit transition avoids breaking the dependent brand task | A target package fails license/glyph/weight/integrity preflight, or IBM Plex Sans remains reachable through runtime CSS/imports/requests |
| 4 | Migration bridge | Generate value-free compatibility aliases from target semantics | Break all existing consumers now or retain duplicated legacy literals | Later route tasks need stable runtime while one token graph remains authoritative | A required current consumer cannot work through aliases without a duplicate raw value |
| 5 | Evidence scope | Enhance the existing specimen for a 24-capture token/type matrix | Rebuild the specimen or duplicate the final bilingual component matrix now | Task 12.2.4 owns specimen/state expansion; this task needs only foundation proof | Existing elements cannot expose all three type roles and computed theme/focus/motion values |
| 6 | Numeric-role reentry | Mark the existing PIX IDs and BRL amounts as real numeric-role consumers and replace only the obsolete family label | Inject a test-only node, broaden numeric typography across unrelated routes, or rebuild the specimen/components | It closes the observed runtime gap at the smallest truthful production boundary while preserving 12.2.4 ownership | Existing facts cannot consume the role accessibly without changing component structure or token semantics |
| 7 | Parity reentry | Extend the canonical refresh narrowly to recompute current-route source identity, document it, then use that command | Hand-edit the stale route hash, rebuild route mappings, or broaden the evidence harness | The source inventory changed legitimately, but every stable mapping remains an independently checked invariant | The route refresh cannot update only inventory-derived path/hash while preserving IDs, targets, owners, dispositions, reasons and evidence |
