# Frente F01 — Workspace consumers off BEM — [[14.7.1-retire-bem-and-refresh-contracts]]

- **Entrega:** merchant settings, storefront settings, the Nautt credential block, `/design-system` and the two dead components leave every route-scoped BEM class name behind, on the projected utility vocabulary. The CSS rules themselves are deleted by F03, once this front has removed their last referrer.
- **Escopo:** presentation only. No route, dictionary key, form body, server action, authorization, redaction or money path changes; rendered geometry stays equivalent.
- **Responsável:** agent. · **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.7.1-retire-bem-and-refresh-contracts.md`; this slice; sections "Objetivo", "Estratégia", "Inventário", "Riscos", "Critérios" of [[14.7.1-retire-bem-and-refresh-contracts.plan|the plan]]; `src/app/globals.css` (read-only — the rules you are freeing); `scripts/check-design-tokens.mjs` (read-only — the gate that forbids raw literals); `src/app/admin/settings/{page,settings-nav,settings-section-notice}.tsx` (read-only — 14.4.3's utility implementation of the same two-column settings hub, the reference to mirror); `DESIGN.md` "Projected utility vocabulary", "Responsive shells", "State contract"; [[pop/specs/application-frontend-system|frontend spec]]; [[researches/template-fidelity-convergence/admin-area|admin-area audit]] (the BEM finding); `src/components/ui/**` + [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md).
- **Owns:** `src/app/(merchant)/settings/settings-surface.tsx`, `src/app/storefront-settings-management.tsx`, `src/app/nautt-credential-surface.tsx`, `src/app/design-system/page.tsx`, `src/app/design-system/interactive-specimens.tsx`, `src/app/orders/order-views.tsx`, `src/app/admin/payment-link-management.tsx`, `src/app/(merchant)/links/legacy-links.tsx` (only the comment line naming the deleted file), `src/app/(merchant)/settings/settings-surface.test.tsx` and `src/app/storefront-settings-management.test.tsx` (only to retire assertions on removed classes/markup — never new coverage, never a run).
- **May read:** every path under "Paths de entrada"; any file in `src/**` for grep evidence.
- **Must not edit:** `src/app/globals.css`, `src/app/auth-card.tsx`, `src/app/login/**`, `src/app/reset-password/**` (F02), `DESIGN.md`, `pop/specs/**`, every `AGENTS.md`, `scripts/**`, `src/components/ui/**`, `src/app-shell/**`, `src/brand/**`, `src/app/pay/**`, `src/app/store/**`, `src/app/api/**`, `docs/**`, `artifacts/**`.
- **Depends on:** nenhuma. Runs in the same wave as F02 (disjoint write set).
- **Entrada esperada:** the admin settings hub as the composition reference. Missing or divergent → migrate anyway with plain utilities and record the deviation; never `BLOCKED` on it.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep one intention per diff*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the token contract, the anti-drift inventory and the six component states*.
- **Web:** deny. · **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (class names dropped per file with the grep proof, exports/files deleted with the importer grep, deviations and gaps), evidence from the three static commands, status `concluída | BLOCKED`.
- **Critérios:** C1, C3, C5, C6, C8 do [[14.7.1-retire-bem-and-refresh-contracts.plan|plano]].

## Contrato de execução

- **Re-grep first.** 14.6.2 is landing in parallel and may already have removed `.receipt-rail*` or left it alive for `/design-system`. Before the first edit, grep `src/**` for every class name you intend to drop and treat the tree you find as truth; report any divergence from the plan's inventory.
- Class names in scope here: `settings-surface{,__layout,__nav,__nav-inner,__nav-link,__sections,__section,__section-heading,__section-description}`, `nautt-facts`, `ds-{ledger,section,section__heading,row,facts}` and the `data-ds-prose` attribute, `admin-shell{,__intro}`, `receipt-rail{,__label,__facts}` on `/design-system`, `admin-account{,-list,__facts,__actions}`, `admin-product{,-list,-description,__facts}`.
- **Settings layout:** the `@media (min-width: 64rem)` block is exactly Tailwind's `lg:`, so `settings-surface__layout` becomes `grid gap-6 lg:grid-cols-[calc(var(--space-12)*4)_1fr] lg:items-start`, `__nav` becomes `hidden lg:block`, `__nav-inner` keeps `lg:sticky` with `top-[calc(var(--top-bar-height)+var(--space-6))]`, and `__nav-link[aria-current="true"]` becomes the `aria-[current=true]:` variant. Mirror the admin hub rather than inventing a second idiom.
- **No raw literal.** `check-design-tokens.mjs` rejects any `px|rem|em|ch|#hex` in `src/app/**`; write `var(--space-*)`, `var(--layout-max)`, `var(--type-*)` inside arbitrary values, use the Tailwind spacing scale where it already matches `--space-*`, and never add an inline `style=`.
- **Dead code:** `OrderListCard` (`order-views.tsx`) and the whole of `src/app/admin/payment-link-management.tsx` currently have zero importers. Delete each only after a grep over `src/**` and `tests/**` returns zero; a single positive hit means you keep it and migrate its classes instead. Report which path each proof took.
- Keep `data-ds-section`, `data-specimen-owner`, `data-design-system-locale`, `data-directory-specimen-state`, every `id`, every `aria-*` and every `role` byte-identical — the specimen evidence and `routes.test.ts` bind to them, not to classes.
- Touching `/design-system` invalidates the hash-bound evidence in `artifacts/design-system/current.json`. That is expected and is 14.7.2's regeneration; do **not** run, edit or regenerate any artifact, and do not touch `docs/` or `artifacts/`.
- Colocated tests are edited only to realign an assertion on a class or markup you changed. No new coverage, no `vitest` run — tests belong to 14.7.2.
- Evidence commands, direct binaries only, never `pnpm <script>` and never a container: `./node_modules/.bin/tsc --noEmit`, `node scripts/check-design-tokens.mjs`, `./node_modules/.bin/eslint <changed files>`.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** _(preencher na execução)_
- **Commit/artefato:** _(preencher)_
- **Arquivos alterados:** _(preencher)_
- **Desvios:** _(preencher)_
- **Evidência:** _(preencher)_
