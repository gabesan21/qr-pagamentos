# Frente F02 — Authentication consumers off BEM — [[14.7.1-retire-bem-and-refresh-contracts]]

- **Entrega:** `/login`, `/reset-password` and the shared `AuthCard` drop every auth BEM leaf class in favour of the projected utilities, keeping intact the four selectors that live inside the gate-sanctioned `(min-width: 900px)` block and naming them explicitly as the surviving exception.
- **Escopo:** presentation only. Login, MFA and recovery behaviour is frozen — same routes, same form bodies and field names, same server actions, same opaque outcomes, same dictionary keys, same visible `id`s.
- **Responsável:** agent. · **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.7.1-retire-bem-and-refresh-contracts.md`; this slice; sections "Objetivo", "Estratégia", "Inventário", "Riscos", "Critérios" of [[14.7.1-retire-bem-and-refresh-contracts.plan|the plan]]; `src/app/globals.css` (read-only — the auth rules and the 900px block); `scripts/check-design-tokens.mjs` (read-only — it sanctions `(min-width: 900px)` only inside `globals.css`); [[researches/template-fidelity-convergence/foundation-shell-auth|foundation-shell-auth audit]]; [[pop/specs/identity-security|identity-security]] (login/MFA/recovery contract, frozen); [[pop/specs/application-frontend-system|frontend spec]]; `DESIGN.md` "Projected utility vocabulary", "Responsive shells → Authentication"; `src/brand/brand-identity.tsx` + [`src/brand/AGENTS.md`](../../../../../src/brand/AGENTS.md) (read-only); `src/components/ui/**` + [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md); the memory of 14.4.1 (`14.4.1-converge-shell-and-auth-pages.02-auth-card-and-login`) for what the converged card already promises.
- **Owns:** `src/app/auth-card.tsx`, `src/app/login/page.tsx`, `src/app/login/login-form.tsx`, `src/app/login/totp-challenge-form.tsx`, `src/app/reset-password/page.tsx`, `src/app/reset-password/reset-password-form.tsx`, and `src/app/login/page.test.tsx` + `src/app/reset-password/page.test.tsx` (only to realign the class assertions listed below — never new coverage, never a run).
- **May read:** every path under "Paths de entrada"; any file in `src/**` for grep evidence.
- **Must not edit:** `src/app/globals.css`, every path owned by F01, `src/brand/**`, `DESIGN.md`, `pop/specs/**`, every `AGENTS.md`, `scripts/**`, `src/components/ui/**`, `src/app-shell/**`, `src/app/api/**`, identity actions/services, `src/app/login/{login-submit,reset-password-submit}`-style submit modules unless a class name lives there, `docs/**`, `artifacts/**`.
- **Depends on:** nenhuma. Runs in the same wave as F01 (disjoint write set).
- **Entrada esperada:** nenhuma externa.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep one intention per diff*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the token contract, the anti-drift inventory and the six component states*.
- **Web:** deny. · **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (class names dropped per file with the grep proof, the surviving selectors and the reason, assertions realigned, gaps recorded), evidence from the three static commands, status `concluída | BLOCKED`.
- **Critérios:** C2, C3, C5, C6, C8 do [[14.7.1-retire-bem-and-refresh-contracts.plan|plano]].

## Contrato de execução

- **Re-grep first**, then drop these leaves: `auth-brand`, `auth-card__{tagline,caption,strip,swatch,footer}`, `auth-card__form--tight`, `auth-password-field{,__input,__toggle}`, `auth-forgot{,__link,__note}`, `auth-totp-actions`, `auth-mode-toggle`, `login-page`, `login-form`, `reset-password-page`, `reset-password-form`, `auth-page`.
- **The 900px block stays.** `.auth-card`, `.auth-card__panel`, `.auth-card__form` and `.auth-card__language` carry the split-card responsive contract through `@media (min-width: 900px)`. `check-design-tokens.mjs` sanctions that literal **only inside `globals.css`**; `min-[900px]:` in a `.tsx` is a raw-value violation and Tailwind cannot express the breakpoint through a `var()` (CSS forbids `var()` in a media query). So keep those four class names on their elements, migrate nothing that depends on them, and name them in your report as the exception F03 must document. Attempting to move them out is out of contract.
- `.auth-brand` is a descendant override reaching into `BrandIdentity`'s internal `brand-identity__mark`/`__name`. Swapping it for the `compact-role-lockup` variant would change the rendered `data-brand-identity` value — a component-semantics change, out of scope. Either express the remaining margin with a utility while keeping the mark sizing identical, or keep `.auth-brand` and **record it as a gap** with one line saying what a fix would need. Never change `BrandIdentity`'s API, variants or props.
- **No raw literal, no inline style.** Use `var(--space-*)`, `var(--auth-form-max)`, `var(--target-min-size)`, `var(--type-*)`, `var(--disabled-opacity)` inside arbitrary values; `50%` and `translate-y-1/2` are fine (the gate matches `px|rem|em|ch|#hex` only).
- Preserve every `id` (`login-form` is read by `login-form.test.tsx` through `getElementById`), every `name`, `type`, `autoComplete`, `aria-*`, `role` and the no-JS `<form>` submission path exactly.
- Test realignment is bounded to these lines: `login/page.test.tsx` 37, 38, 45 and `reset-password/page.test.tsx` 41, 42, 43, 73. Assertions on `auth-card__panel`/`auth-card__form` stay valid (those classes survive); replace only the ones whose class you removed, and prefer asserting a role/label/`id` over a utility string. Remove no assertion without replacing what it proved.
- Auth pages are pixel-evidence surfaces (`tests/login.evidence.spec.ts`, `artifacts/login`, `artifacts/reset-password`). Regeneration belongs to 14.7.2; do not run, edit or regenerate any artifact.
- Evidence commands, direct binaries only, never `pnpm <script>` and never a container: `./node_modules/.bin/tsc --noEmit`, `node scripts/check-design-tokens.mjs`, `./node_modules/.bin/eslint <changed files>`.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** _(preencher na execução)_
- **Commit/artefato:** _(preencher)_
- **Arquivos alterados:** _(preencher)_
- **Desvios:** _(preencher)_
- **Evidência:** _(preencher)_
