# Frente F01 — Catalog products and categories — [[14.5.3-converge-catalog-settings-profile]]

- **Entrega:** flat product create/edit form with the drag-and-drop `ImageUploader`, the store's default currency pre-selected, inline validation and a currency-aware sticky preview; neutral archived badges and the product-fallback asset on list and detail; categories with primitive fields, template deactivation dialogs and an archived-free reference count; plus the merchant-local `SegmentedControl` the settings front consumes.
- **Escopo:** presentation and client behaviour inside `/catalog/**` over the unchanged `/products`, `/products/images` and `/product-categories` mutations.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.5.3-converge-catalog-settings-profile.md`; seções Objetivo, Estratégia e Critérios de [[14.5.3-converge-catalog-settings-profile.plan|plano]]; esta fatia; [[researches/template-fidelity-convergence/merchant-catalog-settings-profile|audit]]; [[pop/specs/catalog-and-payment-links|catalog-and-payment-links]]; [[pop/specs/media-storage|media-storage]]; [[pop/specs/application-frontend-system|application-frontend-system]].
- **Owns:** `src/app/(merchant)/catalog/**` exceto `*.test.tsx`, `src/app/(merchant)/merchant-controls.tsx` (novo), `src/i18n/dictionaries/products/{en,pt-BR}.ts`.
- **May read:** `docs/template/app/src/pages/catalog/*`, `src/components/ui/**`, `src/components/ui/AGENTS.md`, `src/media/AGENTS.md`, `src/data-directory/**`, `src/auth/product.ts`, `src/auth/product-category.ts`, `src/app/products/**`, `src/app/product-categories/route.ts`, `src/app/form-draft.tsx`, `src/brand/**`, `DESIGN.md`.
- **Must not edit:** `src/app/products/**`, `src/app/product-categories/route.ts`, qualquer outro `route.ts`, `src/auth/**`, `src/integrations/**`, `src/media/**`, `src/data-directory/**`, `src/components/ui/**`, `src/brand/**`, `src/app/globals.css`, `src/app/(merchant)/{settings,profile}/**`, `src/app/profile/**`, qualquer `*.test.ts(x)`, dicionário de outra frente, `pop/specs/**`, `DESIGN.md`.
- **Depends on:** nenhuma.
- **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao alterar código*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao compor qualquer tela*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff integrado nos paths de `Owns` + relato ≤10 linhas no bloco Resultado, evidência por leitura de arquivo, status `concluída | BLOCKED`.
- **Critérios:** C01, C02, C03, C04, C11, C13 do [[14.5.3-converge-catalog-settings-profile.plan|plano]].

## Contrato de execução

- Read the template's `CatalogPage.tsx`, `ProductNewPage.tsx`, `ProductDetailPage.tsx`, `ProductForm.tsx`, `fields.tsx` and `CategoriesPage.tsx` before composing; reproduce composition and vocabulary, never the mock data layer, framer-motion or the mock validation rules.
- The form is **flat**: one field stack, no `SectionCard` inside `SectionCard`, one submit per page (the create page's own button is the only one), `LocalizedFieldGroup` for titles/descriptions kept, hidden mirrors and the `/products` field names unchanged.
- Default currency comes from the merchant's stored storefront default; keep the mapped, stored-but-unmapped and no-mapping states already delivered. **No active-state control on create** — the route and service accept no state field; record the finding for F04 instead of rendering a dead control.
- Replace `ImageField` with `src/components/ui/image-uploader`, injecting a `stage` that posts the same `image` multipart field to `/products/images` and resolves `{ identifier, previewUrl: "/media/<identifier>" }`; delete the legacy field once no consumer remains. Never re-point the route or read a storage key.
- Preview reflects the selected currency through `MoneyText`'s pair line and falls back to the generated product-fallback asset; archived rows and the detail header use the neutral tone.
- Client validation blocks submit and renders inline `role="alert"` messages; keep `FormDraftGuard`/`saveFormDraft` behaviour under a failure notice untouched.
- Categories: rebuild the create card with `Field`/`Input`, keep the two deactivation dialogs but fix the close label, count references over non-archived products only, and leave inactive rows without a mutation control — editing an inactive category is refused by the store (`product-category.ts:180`) and by the spec's opaque-conflict rule; state the reason in copy.
- `merchant-controls.tsx` receives only the existing `SegmentedControl` (token classes, `role="radiogroup"`, `aria-checked`, ≥44 px targets); re-point the catalog imports and never create a second control style.
- Every new string lands in `src/i18n/dictionaries/products/{en,pt-BR}.ts` with both locales in parity.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <observação relevante>.
