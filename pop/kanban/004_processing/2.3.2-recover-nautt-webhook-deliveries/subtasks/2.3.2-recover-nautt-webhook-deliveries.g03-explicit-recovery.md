# Group g03 — Explicit recovery — [[2.3.2-recover-nautt-webhook-deliveries]]

- **Order:** 3
- **Depends on:** g01, g02
- **Description:** Orchestrate one owner-bound explicit recovery with dedupe, terminal safety, and one authoritative read.

## Movements

- [ ] (agent) Add `webhook-delivery-recovery.ts` as an injected server-only operation accepting trusted owner/local order plus list-or-specific selector; resolve exact owner/provider target, classify a specific UUID globally against that binding, return on either known classification, then claim lease only for absent/list work and load that owner's active API key + registered webhook UUID.
  - Expected: no route/UI/scheduler exists; known specific performs zero lease claim/release/complete, evidence write, credential decrypt, normalized history call, or order GET; unknown/cross-owner target and live lease also perform zero secrets/network.
  - If it fails: owner/provider identity must come from input beyond trusted owner/local IDs → stop and return to 002; do not accept browser provider IDs.
- [ ] (agent) Create a fixed 10-second abort deadline after lease claim, pass its signal to exactly one selected normalized history-port method, and race the method against the deadline. On timeout, abort and release/fence the matching lease; consume but ignore any late resolution/rejection so it cannot validate, reconcile, persist, or complete.
  - Expected: cooperative and cancellation-ignoring ports both return redacted unavailable at 10,000ms with one call, abort observed, 0 order GET/evidence writes, no partial batch, and no late/unhandled side effect.
  - If it fails: the late port promise can resume work or reject unhandled → fix the orchestration boundary before proceeding; never extend the lease or add retry.
- [ ] (agent) For an in-deadline list result, synchronously reject more than `NORMALIZED_HISTORY_MAX_RECORDS = 128` before per-record validation/dedupe, reconciliation, or evidence writes; then validate accepted identity, keep only new permanent failures, and branch: all-known/nonfailed no-op; terminal records `IGNORED` with 0 order GET; actionable calls existing `reconcileWebhookOrder` once then records `PROCESSED`.
  - Expected: exactly 128 records may proceed; 129 returns redacted unavailable, releases/fences the lease, performs 0 order GET/evidence writes, and persists no prefix. This bound is application-local, not a Nautt cardinality/pagination claim. Accepted N causes one history call plus ≤1 10s order GET, inserts atomically after CAS, preserves collisions, clears plaintext key references, and returns only redacted metadata.
  - If it fails: provider/CAS/persistence fails → matching token releases where possible and no partial batch remains; a later explicit call may reclaim/retry, never loop internally.
- [ ] (agent) Add service matrices for list/specific, pre-lease same/conflicting-known, exact 128/129 local bounds, enforced/ignored-cancellation deadlines and late settlement, nonfailed/all-known, owner/webhook/order mismatch, terminal, active/indeterminate, provider error, concurrency, N-to-one economics, CAS stale/final, rollback, and sensitive sentinel absence.
  - Expected: focused recovery/reconciliation commands pass with exact lease/decrypt/history/order/write counts, one selected port call, signal/abort observations, and no invented wire shape/provider cardinality.
  - If it fails: any known UUID or final order reaches order GET/mutation → fix orchestration/store predicate before aggregate gates; do not rely on CAS alone.
- [ ] (agent) Add deterministic clock/barrier tests for the full lease: an in-deadline history result settles by 10s, order GET by 20s, and durable completion by 25s; a duplicate at 24,999ms remains busy with zero second calls, crash reclaim starts only at ≥30,000ms, and the old token cannot insert or complete. Separately hold history past 10,000ms to prove abort/release and inert late settlement.
  - Expected: accepted work has 5s margin, every duplicate stays excluded until completion/expiry, and all provider barriers run outside store transactions.
  - If it fails: lease expires during accepted work or duplicate dispatches → increase safety design and return to 002; do not hide it with retries or a scheduler.
- [ ] (agent) Review/update Nautt and Prisma DOX (root only if cross-cutting routing changed), run focused DB/tests and `pnpm check`, then PoP/diff validation; review the linked spec for divergence.
  - Expected: all gates pass; contracts describe explicit invocation, history endpoints, lease/evidence, dedupe, terminal/no-scheduler rules; spec remains truthful and queued for 006 implemented-slice sync.
  - If it fails: material behavior diverges → record it in spec **Open** and card notes, return to 002; do not silently rewrite the approved contract.

## Execution notes

- Record selector, normalized count/limit decision, known/new/nonfailed counts, lease result, deadline signal/abort/late-settlement result, history/order GET counts, CAS result, persisted decision, redaction, and every gate PASS marker.
