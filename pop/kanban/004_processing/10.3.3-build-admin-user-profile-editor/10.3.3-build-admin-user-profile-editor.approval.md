# Approval — [[10.3.3-build-admin-user-profile-editor]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read of the card, plan, linked spec, roadmap, and the code anchors. Reality checks performed:

- **Schema (no-migration claim verified).** Every pinned editable field already exists on `user` in `prisma/schema.prisma:21-40`: `username`, `email`, `preferredLocale`, `profileVersion`, `checkoutDataPolicy`, `storefrontSlug` (+ unique), both display names, `storefrontAccentColor`, `storefrontEnabled`, `storefrontThemeId`, `storefrontLayout`, `storefrontStandalonePaymentsEnabled`, `storefrontDefaultCurrencyCode`, and `deletedAt` for the terminality fence. The abort-if-missing-column condition is real but correctly pre-checked — it will not fire.
- **Frozen legacy routes exist.** `src/observability/server-request-log.ts:32-37` carries the literal inventory for `/admin/users/[id]/{password,role,status,delete,nautt-credentials}` and `src/auth/administration.ts` exports the delivered service; decision 4 (re-house role/status/password forms on those byte-frozen routes, accept the `/admin` redirect) preserves the frozen contract exactly as 10.3.2 already did for delete.
- **Extension point is real and pinned upstream.** 10.3.2's plan (currently in `002_planning`) explicitly pins `getAdminUserDetail` in `src/auth/admin-user-directory.ts`, the `/admin/accounts/[id]` route, the `admin-users:evidence` pair, and the interim legacy-form window that this task re-houses — with the reciprocal instruction that 10.3.3 extends that same route and must not move it. The two plans interlock cleanly.
- **Editable set matches the card and roadmap.** Card: "approved identity, role, status, locale, checkout, and store fields plus a safe storefront link, while excluding secrets and direct cross-owner commerce mutations". Roadmap 10.3.3: "protected user profile editor for approved account/store fields and a safe public-store link". The plan's pinned set is exactly that — no more (logo/media excluded on the documented owner-fencing ground, no credentials/provider/session/audit/hash exposure), no less (all nine storefront fields the schema holds except the owner-fenced logo identifier). The exact set is flagged as a spec amendment candidate, which is the honest route since spec line 37/92 currently sanctions admin user management but not yet admin-over-user storefront/checkout writes.
- **Temporal gate honored by construction.** 10.3.2 is in `002_planning`, unmerged; the plan forbids 004 until its merge and aborts if the merged contract differs from the pinned extension point.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result plus seven acceptance criteria, each with a concrete command and a pass condition (test suites, evidence pair, `pnpm check`, `admin:source-check`, `admin:contract-check`, `db:test`).
2. **Sufficient lean brief — pass.** Ten pinned decisions the executor must not revisit, five fronts with owns/may-read/must-not-edit boundaries, explicit concurrency map against 9.1.3/9.3.1/10.1.1/10.2.x, abort conditions. 130 lines, no implementation fragments.
3. **Safe execution — pass.** Delivered services (`administration.ts`, `identity.ts`, `profile.ts`, `checkout-policy.ts`, `locale-preference.ts`) and the five legacy routes byte-frozen; new capability isolated in a new admin-scoped module with an explicit ban on forged-principal calls into owner services; `storefront-settings.ts` touched only by an additive export with a byte-identical-behavior claim that 005 can diff; request-log growth is append-only distinct keys with a serialize-behind-9.1.3 rule; terminality fences reuse the 10.3.1 opaque-failure shape.
4. **Proportional specs — pass.** Append-only amendment to `administrative-foundation.md` (field set, routes, CAS/revocation semantics, redaction boundary, slice) plus notes-only on the two other linked specs and a root `AGENTS.md` bullet update; no rewording of sibling-edited sections.
5. **No avoidable `(user)` item — pass.** The plan contains no human-owned work; the card's only `(user)` item ("Pronto para planejar") is already checked.
6. **Proportional to risk/size — pass.** L, critical, cross-layer admin mutation surface: strictly sequential single executor with five fronts, full-gate verification including the disposable-Postgres contract and run-bound evidence. Not over- or under-engineered.
7. **Economic verification — pass.** Every criterion verifies by local re-run (`pnpm test`, `pnpm check`, evidence pair, `db:test`, diff reads); no web anywhere.

#### Risk assessment

The 005 gate (full verification, mandatory for critical) must specifically verify:

- **Capability boundary is the amendment's, not the executor's.** The admin-over-user identity/locale/checkout-policy/storefront write is a new capability beyond the spec's current admin list; it is legitimate only if the append-only amendment lands and matches the delivered set exactly. Diff the delivered editable set and forms against the amended boundary — logo/media field absent from every form, DTO, and redirect payload.
- **Byte-frozen claims.** Diff `administration.ts`, `identity.ts`, `profile.ts`, `checkout-policy.ts`, `locale-preference.ts`, and the five legacy routes against their pre-task state; confirm `storefront-settings.ts` gained only the additive export with zero behavior change to the merchant path.
- **No forged principal.** The new service must re-authorize the admin and write by target id directly; grep for any call into owner-scoped services (`profile.ts`, `storefront-settings.ts` service functions, `checkout-policy.ts`) with a fabricated owner principal.
- **Terminality opacity.** A `deletedAt` target is indistinguishable from unknown across all four new routes and all four mutations; identity edit must not free or collide with a deleted user's retained username/email/slug (uniqueness maps to the one conflict outcome).
- **Session semantics.** Identity CAS retains sessions and revokes nothing; locale/checkout/storefront edits revoke nothing; only the frozen routes revoke.
- **Request-log inventory.** Exactly four new literal keys (`identity`, `locale`, `checkout-policy`, `storefront` under `/admin/users/[id]/`); no edit to 9.1.3's keys; if 9.1.3 was still open at 004, this landed after its merge.
- **Temporal gate.** Confirm 10.3.2 actually merged before 004 started and that `getAdminUserDetail`/the detail route match the pinned contract (if not, the task should have aborted, not re-plumbed).
- Minor plan tension to resolve in execution, not worth a return: the top-level gate says 004 starts only after 10.3.2 merges, while F01's dependency note suggests F01/F02 could run pre-merge. The top gate and the Order section are stricter and prevail — the orchestrator must not start F01 early.

aprovado por revisor independente (yolo) — 2026-07-26
