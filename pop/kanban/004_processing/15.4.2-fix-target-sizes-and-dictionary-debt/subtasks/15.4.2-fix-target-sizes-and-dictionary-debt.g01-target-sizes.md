# Frente F01 — Targets at the 44 px minimum — [[15.4.2-fix-target-sizes-and-dictionary-debt]]

- **Entrega:** every interactive target listed in A11 meets the `44×44` CSS-pixel minimum, built only from the declared token family; sites already compliant stay byte-identical and are named with evidence.
- **Escopo:** the merchant link-line controls and the merchant segmented control. Confirm — do not assume — the state of each A11 site before editing: `admin-controls.tsx` and `checkout-privacy-notice.tsx` were already raised by earlier Phase 15 work. Any further sub-minimum control revealed by the 15.4.1 ledger enters only if it lives inside this front's `Owns`; anything else is reported as a follow-up, never silently fixed.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `15.4.2-fix-target-sizes-and-dictionary-debt`, seções "Objetivo", "Estratégia" e "Critérios" de [[15.4.2-fix-target-sizes-and-dictionary-debt.plan]], esta fatia, `DESIGN.md` (geometria e alvo mínimo), [[pop/researches/pix-checkout-review/2026-09-22-review-findings|review findings]] (A11), [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md).
- **Owns:** `src/app/(merchant)/links/link-lines-editor.tsx`, `src/app/(merchant)/merchant-controls.tsx`.
- **May read:** `src/**`, `docs/template/app/src/components/**`, `DESIGN.md`, `pop/specs/application-frontend-system.md`, `pop/memory/**`.
- **Must not edit:** `src/components/ui/**` (frozen owners set, no insufficiency finding here), `src/app/admin/**`, `src/app/pay/**`, `src/i18n/**`, `scripts/**`, `docs/**`, specs, DOX, `DESIGN.md`.
- **Depends on:** nenhuma frente; a task inteira depende de 15.4.1 ter ledger em `pop/memory/`.
- **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] — *use para tokens como contrato, estados obrigatórios e anti-drift*; [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para contrato antes do código e refatoração segura*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval (não-yolo); sem delta nesta rodada.
- **Saída:** diff nos dois arquivos + preenchimento da seção "Resultado" desta fatia, teto 50 linhas, evidência `arquivo:linha` por alvo (antes/depois) e a lista dos sites já conformes, status `concluída | BLOCKED`.
- **Critérios:** C1, C2, C9 do [[15.4.2-fix-target-sizes-and-dictionary-debt.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- Use exclusivamente a família declarada (`min-h-11`, `min-w-11`, `size-11` e equivalentes de token). Valor cru em px, `style` inline ou classe arbitrária `[...]` é reprovação, mesmo que renderize certo.
- Preserve semântica e rótulos existentes: `role="group"`/`role="radiogroup"`, `aria-label`, estados `disabled` e o anel de foco continuam como estão. Aumentar o alvo não pode mudar a ordem de tabulação nem o texto.
- O ícone dentro do botão mantém o tamanho visual atual; quem cresce é a área clicável.
- Não introduza um segundo estilo de botão nem um componente novo: o controle segmentado merchant e o admin seguem sendo dois módulos locais distintos (a consolidação é follow-up rastreado, fora desta task).
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Nenhum teste roda aqui: `pnpm test`/`pnpm check` são critérios `phase` da task de verificação da phase.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** diff não commitado, staged/unstaged no worktree (integração pelo agente principal).
- **Arquivos alterados (= `Owns`):** `src/app/(merchant)/links/link-lines-editor.tsx`, `src/app/(merchant)/merchant-controls.tsx`.
- **Alvos corrigidos:**
  - `link-lines-editor.tsx:197` (decrease stepper) — `size-8` → `min-h-11 min-w-11` (mesmo `flex items-center justify-center`, ícone `size-3.5` inalterado).
  - `link-lines-editor.tsx:207` (increase stepper) — `size-8` → `min-h-11 min-w-11`, ícone inalterado.
  - `link-lines-editor.tsx:225` (remove button) — `p-1.5` → `flex min-h-11 min-w-11 items-center justify-center`, ícone `size-4` inalterado.
  - `merchant-controls.tsx:32` (segmented control option) — `px-3 py-1.5` → `min-h-11 min-w-11 px-3 py-2` (alinhado ao irmão já conforme `admin-controls.tsx:49`).
- **Já conformes (sem diff):** `src/app/admin/admin-controls.tsx:49` (`min-h-11 min-w-11 rounded px-3 py-2`, confirmado por leitura antes de editar); `src/app/pay/[identifier]/checkout-privacy-notice.tsx:20` (`inline-flex min-h-11 min-w-11 items-center justify-center`, confirmado por leitura). Nenhum dos dois recebeu diff.
- **Desvios:** nenhum. Toda classe aplicada vem da família declarada (`min-h-11`/`min-w-11`), nenhum valor px cru, `role="group"`/`role="radiogroup"`, `aria-label`, `disabled` e o anel de foco preservados; ordem de tabulação inalterada; ícones mantêm o tamanho visual atual.
- **Evidência:** `git diff --stat` mostra só os dois arquivos de `Owns` (4 inserções/4 deleções); `grep -n "size-8\|p-1.5\|py-1.5"` nos dois arquivos não retorna ocorrência após o diff.
