# Aprovação — [[5.3.1-security-headers-and-origin-checks]]

- **Etapa:** 003_human_approval · **Responsável:** revisor independente em yolo

## Rodada 1 — 2026-07-21

### Brief para decisão

- **Entrega:** five security headers from `next.config.ts` `headers()` on `/:path*`; fail-closed origin verification returning the existing opaque empty `403` on every cookie-authenticated POST route.
- **Estratégia:** static `headers()` entry (no middleware, CSP deferred with recorded reason: Next 16 inline hydration scripts + no-middleware decision); shared `src/app/origin-guard.ts` helper (Origin host vs Host, X-Forwarded-Host fallback) wired before auth/service work on browser form-post mutations; webhook, public checkout APIs, `/api/health`, and `[lang]/*` stubs untouched.
- **Topologia:** executor único, F01 (headers) then F02 (origin guard), write sets disjuntos.
- **Risco principal:** false rejections behind the TLS reverse proxy — mitigated by Host/X-Forwarded-Host comparison plus runbook note deferred to task 5.5.1.
- **Critérios principais:** 1–4, all `re-run` (config unit test, focused route tests for reject/accept, excluded-routes green, `pnpm check`).
- **Plano:** [[5.3.1-security-headers-and-origin-checks.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_

### Resposta do crítico (yolo)

- **Contexto:** strong independente.
- **Devolução:** 0 de 2.
- **Decisão:** aprovado → 004.
- **Motivo/evidência:** route classification verified against the actual `src/app/**/route.ts` POST inventory — the guarded list (login/submit, logout, language-preference, products, payment-links(, [id]), checkout-policy, nautt-credentials(, /register, /reset), admin/*) is complete and matches cookie-authenticated mutations; webhook, public `api/payment-links/*`, `/api/health`, and `[lang]/*` stubs are correctly excluded and in F02's `must not edit`. Origin rule (require Origin, host vs Host with X-Forwarded-Host fallback, empty `403` fail-closed before service work) is sound behind the documented TLS proxy and consistent with the AGENTS.md empty 401/403 contract. CSP deferral is justified by the epoch decision (headers in `next.config.ts`, no middleware) and recorded in the plan, not hidden. Criteria 1–4 are executable and all `re-run`; no `(user)` items; topology proportional to size M.

- [x] Feito

aprovado por revisor independente (yolo) — 2026-07-21
