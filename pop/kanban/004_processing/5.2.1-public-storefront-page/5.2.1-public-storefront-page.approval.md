# Approval — [[5.2.1-public-storefront-page]]

- **Stage:** 003_human_approval · **Owner:** independent yolo critic

## Round 1 — 2026-07-21

### Brief for decision

- **Delivery:** A sessionless, bilingual `/store/[slug]` page with a server-only redacted projection of enabled storefronts and eligible active products/payment links.
- **Strategy:** Keep owner configuration immutable here; validate slug before a closed Prisma projection, emit one opaque unavailable outcome, and expose only display data plus a checkout identifier.
- **Dependencies:** 5.1.1 must close before 004; F01 projection precedes F02 page/UI.
- **Risk:** Accidental public data exposure or unsafe branded CSS; mitigated by structural `select`, negative redaction assertions, and one validated scoped accent value.
- **Verification:** Source/static checks only; runtime test, build, and database execution are skipped by explicit user instruction.

### Human response

_(write here: approved, or requested changes)_

- [ ] Done

### Agent decision

_(after Done: `approved → 004` or `changes requested → 002: <summary>`)_

### Yolo critic response

- **Decision:** changes requested → 002.
- The proposed dynamic accent requires a React `style` custom-property assignment on the page, but `scripts/check-design-tokens.mjs` currently rejects every inline `style=` in `src/app/**`. The plan promises both that assignment and no inline-style violation, while omitting the narrowly scoped checker/`DESIGN.md` contract change (or an alternative feasible delivery mechanism). Resolve this before 004 so criterion 4 is objectively achievable without weakening the design-token boundary.
- The dependency is correctly treated as a pre-004 gate; it is not a planning blocker. All other redaction, sequencing, and source/static-only verification provisions are proportionate.

## Round 2 — 2026-07-21

### Brief for decision

- **Revision:** dynamic branding is now an exact, one-purpose root declaration for `--storefront-accent`, supplied only by the validated projection field.
- **Boundary:** the token checker must allow that exact path/property/expression only; raw-value detection remains unconditional and all other inline styles—including near misses—remain rejected by tests.
- **Delivery and dependency:** the redacted sessionless projection/page remain unchanged; 5.1.1 is now integrated and the sequential F01 → F02 execution topology remains valid.
- **Verification:** static source/lint/typecheck/checker evidence only; runtime test, build and database execution remain explicitly skipped by user instruction.

### Yolo critic response

- **Decision:** approved → 004.
- The dynamic-accent exception is objectively narrow: the plan limits it to `src/app/store/[slug]/page.tsx`, the sole `--storefront-accent` property, and exactly `storefront.accentColor`. It requires checker fixtures that reject a changed path, property, expression, sibling property, every other `style=`, and raw visual values; the raw-value scan remains unconditional.
- The remaining plan is sufficient and proportionate: the integrated 5.1.1 contract supplies the validated nullable accent and public settings, F01 precedes the UI consumer, the projection is structurally redacted, and the route has no public mutation or checkout expansion. Source/static verification remains consistent with the user-directed runtime-test skip.

- [x] Done

aprovado por revisor independente (yolo) — 2026-07-21
