---
reviewer: yolo-critic
role: gate-003 reviewer
date: 2026-07-31
outcome: approved
---

# 11.2.3-rehearse-production-upgrade-and-recovery — Gate 003 Approval

The plan is **approved** for `004_processing`.

## Rationale

- The objective is fully covered: clean install, identity seed, SMTP-bound self-hosted password reset, media persistence, representative Commerce V2 journeys, update, backup, restore, liveness, and exact-revision release ledger are all represented.
- Work fronts are well-scoped and sequential; front 6 is correctly gated as a conditional add-on pending closure of `11.2.2-verify-six-theme-bilingual-experience`.
- Dependencies (`6.3.3`, `11.1.1`, `11.2.2`) are explicitly stated and respected.
- Risks are realistic and mitigated: Docker/host flakiness, mock SMTP orchestration/leak, `11.2.2` timing, and timeout pressure are all acknowledged.
- Acceptance criteria are concrete and verifiable: container scenario pass, installer/check gates, ledger/evidence assertions, post-restore inventory/health check, AGENTS.md freshness, and the `11.2.2` integration condition.
- The plan avoids production data, avoids secret exposure, and exercises the existing install/update/backup/restore scripts rather than restructuring them.

## Non-blocking observations

1. Mock SMTP capture introduces a new secret-bearing token path; implementation must ensure the reset token never appears in logs, artifacts, or test evidence. The plan flags this risk; treat it as a hard acceptance criterion in `005_closing`.
2. Ensure the `11.2.2` integration gate is implemented as a documented blocker, not as placeholder code that could be accidentally enabled before `11.2.2` closes.
3. The rehearsal relies on `11.1.1-build-self-hosted-password-reset` already exposing a working SMTP-driven reset route; verify that contract is present before exercising it in `004`.
