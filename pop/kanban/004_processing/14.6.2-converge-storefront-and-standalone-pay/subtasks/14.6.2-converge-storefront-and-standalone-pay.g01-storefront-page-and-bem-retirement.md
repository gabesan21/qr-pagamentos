# Frente F01 — Storefront page and its BEM usage — [[14.6.2-converge-storefront-and-standalone-pay]]

- **Entrega:** `/store/[slug]` (page, experience, loading, error) recomposed on the converged public checkout vocabulary delivered by [[14.6.1-converge-public-checkout]], with every public `storefront-*`/`receipt-rail*` class name dropped from the markup in favour of the projected utilities, plus the storefront copy delta in both locales. The CSS rules themselves are deleted by F02, once this front has removed their last storefront referrer.
- **Escopo:** presentation of the public storefront route and its copy. Cart semantics (browser-local key/envelope, 1–9,999 quantities, BigInt per-currency totals, stale-item recovery notice), the `POST /api/store/[slug]/cart/checkout` body, the `201` clear-and-redirect to `/pay/[identifier]`, the `?amount=` prefill grammar and the `boxed|table` layouts keep their behaviour exactly.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.6.2-converge-storefront-and-standalone-pay.md`; this slice; sections "Objetivo", "Estratégia", "Riscos", "Critérios" of [[14.6.2-converge-storefront-and-standalone-pay.plan|the plan]]; [[researches/template-fidelity-convergence/public-checkout-and-inventory|the audit]] (Part A `/store/[slug]` row and the shared-vocabulary question); [[pop/specs/storefront-and-customization|storefront spec]] "Public redaction boundary" and "Public page and browser cart V1"; [[pop/specs/application-frontend-system|frontend spec]]; `DESIGN.md` "Public payment and storefront surfaces", "Shared component ownership", "State contract"; [[14.6.1-converge-public-checkout.plan|the 14.6.1 plan]], its slices and its memory ledger (read before the first edit — they name the shell, header, footer and `EmptyState` owners to consume); `src/app/pay/[identifier]/**` as delivered; `src/components/ui/**` + [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md); `src/i18n/dictionaries/checkout/**`; `src/storefront/**`; `src/app/globals.css` and `scripts/check-design-tokens.mjs` (read-only).
- **Owns:** `src/app/store/[slug]/page.tsx`, `src/app/store/[slug]/storefront-experience.tsx`, `src/app/store/[slug]/loading.tsx`, `src/app/store/[slug]/error.tsx`, `src/app/store/[slug]/page.test.tsx` and `src/app/store/[slug]/storefront-experience.test.tsx` (only to retire assertions on removed classes/props — never new coverage, never a run), `src/i18n/dictionaries/storefront/en.ts`, `src/i18n/dictionaries/storefront/pt-BR.ts`.
- **May read:** every path listed under "Paths de entrada"; `src/app/(merchant)/settings/**`, `src/app/storefront-preview.tsx`, `src/app/storefront-settings-management.tsx` (only to confirm which `storefront-*` classes belong to merchant settings and must keep working).
- **Must not edit:** `src/app/store/[slug]/pay/**`, `src/app/globals.css`, `DESIGN.md`, `pop/specs/**` (all F02), `src/components/ui/**`, `src/components/ui/inventory.json`, `src/checkout/**`, `src/storefront/**`, `src/app/pay/**`, `src/i18n/dictionaries/checkout/**`, every admin/merchant surface, `scripts/**`, `src/app/api/**`.
- **Depends on:** nenhuma (14.6.1 closed is the task-level dependency).
- **Entrada esperada:** 14.6.1's shell/merchant header/public footer with `LanguageSwitcher`, its `EmptyState` unavailable/error vocabulary and the checkout dictionary keys behind that furniture. Missing or unusable → `BLOCKED` with evidence; never fork a second rail or footer.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep one intention per diff*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the token contract, the anti-drift inventory and the six component states*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (compositions consumed from 14.6.1, class names dropped, copy keys added per locale, deviations), evidence `pnpm typecheck` + `pnpm lint` output, status `concluída | BLOCKED`.
- **Critérios:** C1, C2, C6, C7 do [[14.6.2-converge-storefront-and-standalone-pay.plan|plano]].

## Contrato de execução

- Consume, never re-create: the shell, the identity rail (`Monogram` or the merchant logo + display name + trust line), the `EmptyState` vocabulary for unavailable/empty/error and the public footer are 14.6.1's owners.
- Leave the storefront markup with zero `storefront-*`/`receipt-rail*` class names, so F02 can delete the rules against a clean grep; report in the front's output which class names disappeared.
- Keep `<main data-theme-preview=… style={{ "--storefront-accent": storefront.accentColor } as CSSProperties}>` verbatim, unless the page delegates the accent to 14.6.1's shell and that shell already owns an allowlisted declaration: `scripts/check-design-tokens.mjs` is path-bound and this front may not edit it.
- Replacement styling uses only the projected utilities (`bg-surface`, `text-text-2`, `rounded-card`, `shadow-card`, `max-w-app`, `font-display`, tone-soft pairs); no raw hex/px/rem/ch value, no new inline style, no second visual variant of an owned primitive.
- Both locales change together; copy that already exists in the checkout dictionary is reused, never duplicated into the storefront dictionary.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:**
- **Commit/artefato:**
- **Arquivos alterados:**
- **Desvios:**
- **Evidência:**
