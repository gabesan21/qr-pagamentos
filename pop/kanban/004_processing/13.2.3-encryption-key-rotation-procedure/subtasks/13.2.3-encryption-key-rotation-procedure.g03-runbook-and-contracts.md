# Frente F03 — Runbook, specs and subtree contracts — [[13.2.3-encryption-key-rotation-procedure]]

- **Entrega:** the operator rotation procedure in the runbook and the durable contracts it rests on (spec `nautt:69` closed, TOTP rotation stated, affected subtree boundaries updated).
- **Escopo:** documentation and contracts only. No source, container, compose, installer or schema change.
- **Responsável:** agent. **Papel:** `pop-executor`.
- **Paths de entrada:** card `13.2.3-encryption-key-rotation-procedure.md`; seções "Estratégia", "Riscos", "Critérios de aceite" e "Specs e contratos" do [[13.2.3-encryption-key-rotation-procedure.plan|plano]]; esta fatia; os diffs entregues por F01 e F02; [[pop/notes/decisions/2026-09-23-epoch-13-decisions|decisões de 2026-09-23]].
- **Owns:** `docs/production-runbook.md`, `pop/specs/nautt-finance-integration.md`, `pop/specs/identity-security.md`, `install/AGENTS.md`, `container/AGENTS.md`, `src/integrations/nautt/AGENTS.md`.
- **May read:** `src/**`, `container/**`, `install/**`, `compose.yaml`, `prisma/**`, `docs/**`, `pop/specs/**`, `pop/notes/**`.
- **Must not edit:** todo o write set de F01 e F02, root `AGENTS.md`, `prisma/AGENTS.md`, `pop/ROADMAP.md`, `pop/roadmap/**`, o card e demais specs.
- **Depends on:** F01, F02. **Entrada esperada:** the delivered env/secret names and the one-shot invocation — document what exists, never a name you assume.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para manter o contrato antes da redação*.
- **Web:** deny.
- **Gate/delta:** 003 antes da execução (task `critical`); nenhum delta.
- **Saída:** diff no write set + esta fatia preenchida, teto de resumo 15 linhas, evidência: paths e leitura, status `concluída | BLOCKED`.
- **Critérios:** 7, 8 do plano.

## Contrato de execução

- Runbook: add a rotation section under the secret posture, with the ordered procedure — verified backup first; generate the new key; stage it as the current secret and the outgoing one as the previous secret; restart so both are mounted; run the one-shot with the app stopped; re-run until zero remaining; drop the previous secret and restart only after the window outlived the 24 h capability TTL.
- The runbook must state, in operator language: a half-finished rotation never loses access while both keys are configured; a backup is bound to the key set of its creation, so the old key is retained while such backups live; in-flight checkout capabilities and directory cursors survive only while the previous key is configured; TOTP recovery codes are digests and are untouched; the procedure never prints or records key material and never touches webhook registration or `NAUTT_API_BASE_URL`.
- `nautt-finance-integration.md`: add one Requirements bullet with the rotation contract and one Implemented-slices bullet crediting task 13.2.3; remove the `nautt:69` Open bullet; rewrite `nautt:70` so it keeps only the provider-side list/delete/orphan gap, without claiming the local key-rotation procedure is missing. Update the `Updated:` line following the existing "Earlier:" convention.
- `identity-security.md`: state that `TOTP_ENCRYPTION_KEY` is rotatable through the same procedure and that recovery-code digests are unaffected; add the matching acceptance item. **Do not create the missing `## Open` section** — that is recorded housekeeping (decision 7), not this task.
- Subtree contracts: `install/AGENTS.md` records that the explicit operator-invoked rotation is the only exception to "never rotate continuity material" and stays out of install/update; `container/AGENTS.md` records the one-shot and its counts-only, non-retrying output; `src/integrations/nautt/AGENTS.md` records that the stored webhook secret is rewrapped locally while provider-side recovery stays open.
- Contracts are described, never duplicated: no procedure copied into a spec, no spec text copied into the runbook, no code listing anywhere. Keep notes/specs near their existing size and use the project's link convention.
- **Do not run any command.** No test, installer or Docker invocation.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência. Não implementar, simular ou corrigir dependências.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
