# Aprovação — [[15.1.1-remove-v1-application-surfaces]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-22

### Brief para decisão

- **Entrega:** remove the V1 payment-link/checkout/order line from the application layer, so `src/` and `tests/` describe Commerce V2 only — no V1 module, route, page, DTO branch, dictionary key or test left behind.
- **Estratégia:** V1 and V2 are entangled by **shared vocabulary**, not by behavior: `src/orders/payment-link-order.ts` owns `CHECKOUT_DATA_POLICIES`, `PAYMENT_LINK_ORDER_STATES`, `CustomerSnapshotV1` and `normalizeCustomerSnapshotV1`; `src/app/orders/order-views.tsx` owns `orderStateLabel`/`OrderStateBadge`; `src/auth/payment-link.ts` owns `PAYMENT_LINK_TYPES`. More than ten V2 modules import them. Wave 1 therefore relocates that voc…
- **Topologia:** `pop-execution-orchestrator` em ondas híbridas — há DAG (F01 → F02‖F03 → F04) e quatro conjuntos de escrita distintos; cada frente é um `pop-executor` com uma skill predominante.
- **Risco principal:** a V2 module still imports a V1 helper after F01 (the epoch's declared fork) — mitigação: F02/F03 relocate nothing on their own; a surviving import is reported to the principal, never solved by keeping a V1 file alive.
- **Critérios principais:** C01, C02, C03, C04, C05, C06, C07, C08, C09, C10, C11, C12.
- **Plano:** [[15.1.1-remove-v1-application-surfaces.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

Aprovado por comando explícito do coordenador em sessão (2026-09-22): "Assuma o gate 003 como aprovado pelo coordenador" — desvio registrado sob a regra 20 do [[AGENTS|AGENTS]] do projeto; escopo do comando é só esta task, só este gate.

- [x] Feito

### Decisão do agente

`aprovado → 004`
