# Frente F01 — Relocate the shared V2 vocabulary — [[15.1.1-remove-v1-application-surfaces]]

- **Entrega:** the vocabulary V2 shares with V1 lives in V2-owned modules, and every V2 consumer imports it from there. No V1 file is deleted in this front; nothing changes behavior.
- **Escopo:** `CHECKOUT_DATA_POLICIES` / `CheckoutDataPolicy`, `PAYMENT_LINK_ORDER_STATES` / `PaymentLinkOrderState`, `CustomerAddressV1` / `CustomerSnapshotV1` / `normalizeCustomerSnapshotV1` (`src/orders/payment-link-order.ts`); `orderStateLabel` / `OrderStateBadge` (`src/app/orders/order-views.tsx`); `PAYMENT_LINK_TYPES` / `PaymentLinkType` (`src/auth/payment-link.ts`). Members move with identical values, order and semantics; the V1 stores, services and views stay in place for F02/F03 to delete. A V1 file may import the new home; the new home must never import a V1 file.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card [[15.1.1-remove-v1-application-surfaces]]; seções "Objetivo", "Estratégia" e "Critérios" de [[15.1.1-remove-v1-application-surfaces.plan]]; esta fatia; [[.agents/skills/clean-code-change/SKILL|clean-code-change]]; [`src/checkout/AGENTS.md`](../../../../../src/checkout/AGENTS.md) (leitura).
- **Owns:** the new V2-owned home module(s) under `src/orders/` and/or `src/app/orders/`; the import lines of the V2 consumers only — `src/checkout/{public-checkout-v2,public-checkout-v2-presentation,payment-status-v2,standalone-checkout,standalone-payment-status,storefront-cart-checkout}.ts`, `src/storefront/public-storefront.ts`, `src/app/orders/order-v2-views.tsx`, `src/app/(merchant)/links/v2/[id]/orders/order-v2-views.tsx`, `src/app/(merchant)/links/link-v2-actions.tsx`, `src/app/admin/payment-links/associated-orders-card.tsx`, `src/app/pay/[identifier]/{checkout-experience,public-checkout-v2-form}.tsx`, `src/app/store/[slug]/pay/standalone-payment-experience.tsx`, plus the import lines of the V1 files that consume the same members; the matching import lines in the V2 test files of those modules.
- **May read:** `src/**`, `tests/**`, `prisma/schema.prisma`, `pop/specs/checkout-and-order-lifecycle.md`, `pop/specs/catalog-and-payment-links.md`.
- **Must not edit:** any V1 module, route, page or test (F02/F03 delete them); the inventories and dictionaries reserved for F04; `prisma/**`; `pop/specs/**`; any `AGENTS.md`; `docs/**`; the card and the plan.
- **Depends on:** nenhuma.
- **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para o contrato antes de mover e para manter o diff com uma única intenção estrutural*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval já cumprido pela task; nenhum delta de reentrada.
- **Saída:** relato curto no bloco "Resultado" desta fatia, teto 12 linhas, evidência: lista dos módulos novos e dos consumidores religados, status `concluída | BLOCKED`.
- **Critérios:** C06, C07, C10 do [[15.1.1-remove-v1-application-surfaces.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- Relocation is a move, not a redesign: no rename, no signature change, no new member, no member dropped. If a member looks V1-only, leave it in the V1 file — F02/F03 delete it with the file.
- Report the exact list of relocated members and their new home; F02 and F03 assume it verbatim.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente (sandbox, permissão, flakiness); na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Nenhum teste roda nesta task — `pnpm check` é critério `phase` da [[15.1.4-phase-verification]].
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `8db3d850` em `task/15.1.1-remove-v1-application-surfaces`.
- **Arquivos alterados:** 3 módulos novos (`src/orders/order-v2-policies.ts`, `src/orders/payment-link-v2-type.ts`, `src/app/orders/order-state-views.tsx`) + 37 arquivos com edição só de linhas de import/reexport, incluindo os três donos originais.
- **Desvios:** ampliou o conjunto explícito de "Owns" para religar também `order-v2.ts`, `order-v2-view.ts`, `order-v2-directory.ts`, `order-v2-admin-directory.ts`, `payment-link-v2.ts`, `payment-link-v2-view.ts`, `payment-link-v2-admin-directory.ts` e os testes V1 (`order-view.test.ts`, `payment-link-order.test.ts`) — necessário porque esses módulos V2 também importavam os membros movidos e ficariam quebrados; mudança só de linha de import, mesmos membros, autorizado pelo agente principal após conferência do diff (`git show --stat 8db3d850`, `pop_check_scope.py --allow 'src/**' --deny <inventários/tests/prisma/specs/AGENTS/docs>` → ownership válido).
- **Evidência:** `CHECKOUT_DATA_POLICIES`/`CheckoutDataPolicy`, `PAYMENT_LINK_ORDER_STATES`/`PaymentLinkOrderState`, `CustomerAddressV1`/`CustomerSnapshotV1`/`normalizeCustomerSnapshotV1` → `src/orders/order-v2-policies.ts`; `orderStateLabel`/`OrderStateBadge` → `src/app/orders/order-state-views.tsx`; `PAYMENT_LINK_TYPES`/`PaymentLinkType` → `src/orders/payment-link-v2-type.ts`. Grep pós-edição: zero imports remanescentes desses membros a partir de `payment-link-order.ts`, `order-views.tsx` ou `payment-link.ts`.
