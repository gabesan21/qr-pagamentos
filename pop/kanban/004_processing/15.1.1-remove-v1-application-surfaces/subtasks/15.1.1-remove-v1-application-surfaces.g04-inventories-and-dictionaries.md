# Frente F04 — Sweep the cross-cutting inventories — [[15.1.1-remove-v1-application-surfaces]]

- **Entrega:** every inventory that still enumerates V1 — route contract, request-log route names, rate-limit buckets, both dictionaries and the evidence specs — names V2 routes, keys and eras only.
- **Escopo:** rewrite the V1-first assertions of `src/app/routes.test.ts` so the remaining ones describe the V2-only routes (`/pay/[identifier]`, the public API routes, the standalone page); remove the dead entries from `src/observability/server-request-log.ts` (`paymentLinks`, `paymentLink`) and its test; remove any rate-limit bucket in `src/security/public-rate-limit.ts` left without a caller; delete from `src/i18n/dictionaries/**` (root and per-domain, both `pt-BR` and `en`) every key whose only consumers were the surfaces F02/F03 deleted, keeping the two locales at exact key parity; reduce `tests/checkout.evidence.spec.ts` to a single-era grid and drop the `/orders/[id]` and `/admin/orders/[id]` targets from `tests/orders.evidence.spec.ts` and `tests/admin-orders.evidence.spec.ts`. Nothing else in the evidence runners changes, and none of them is executed here.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card [[15.1.1-remove-v1-application-surfaces]]; seções "Objetivo", "Estratégia" e "Critérios" de [[15.1.1-remove-v1-application-surfaces.plan]]; esta fatia; os relatos de F02 e F03; [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Owns:** `src/app/routes.test.ts`; `src/observability/server-request-log.ts` e `server-request-log.test.ts`; `src/security/public-rate-limit.ts` e seu teste; `src/i18n/dictionaries/**`; `tests/checkout.evidence.spec.ts`, `tests/orders.evidence.spec.ts`, `tests/admin-orders.evidence.spec.ts`.
- **May read:** `src/**`, `tests/**`, `pop/specs/checkout-and-order-lifecycle.md`, `pop/specs/catalog-and-payment-links.md`.
- **Must not edit:** todo o restante de `src/` (superfícies de F01/F02/F03 já integradas); os demais `tests/*.evidence.spec.ts`; `prisma/**` (15.1.2); `pop/specs/**`, qualquer `AGENTS.md`, `docs/**` (15.1.3); o card e o plano.
- **Depends on:** F02, F03.
- **Entrada esperada:** os relatos de F02 e F03 com a lista de arquivos apagados e seções removidas — é ela que prova quais rotas e chaves ficaram sem consumidor. Sem ambos, `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para tratar cada inventário como contrato: prove o consumidor antes de apagar a entrada*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval já cumprido pela task; nenhum delta de reentrada.
- **Saída:** relato curto no bloco "Resultado" desta fatia, teto 12 linhas, evidência: entradas removidas por inventário, diff de chaves `pt-BR` × `en` e varredura final de imports/menções V1 em `src/` e `tests/`, status `concluída | BLOCKED`.
- **Critérios:** C02, C09, C10, C12 do [[15.1.1-remove-v1-application-surfaces.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- Uma chave ou entrada só é apagada quando a varredura prova que não sobrou consumidor; chave usada por superfície V2 fica, mesmo que o nome cite "legacy" — nome não é prova.
- `pt-BR` e `en` saem com o mesmo conjunto de chaves; divergência é defeito, não detalhe.
- As assertivas que sobram em `src/app/routes.test.ts` descrevem o contrato V2; não invente cobertura nova nem crie arquivo de teste — escrever suíte é da [[15.1.4-phase-verification]].
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Nenhum teste roda nesta task, e nenhum runner de evidência é executado pelo agente (C12 é `verify: user`).
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `9fcf7118` (branch `task/15.1.1-remove-v1-application-surfaces`).
- **Arquivos alterados:** `src/app/routes.test.ts`; `src/observability/server-request-log.ts`; `src/i18n/dictionaries/{checkout,orders-directory,payment-links,payment-links-directory,payment-links-form}/{en,pt-BR}.ts`; `tests/{checkout,orders,admin-orders}.evidence.spec.ts`.
- **Desvios:** `public-rate-limit.ts` intocado — todos os 6 buckets têm chamador vivo. `orders/{en,pt-BR}.ts` sem mudança líquida: 6 chaves pareciam mortas por superfície, mas `src/i18n/dictionaries.test.ts` (fora do owns) as afirma em `adminStateKeys` — mantidas para não quebrar esse teste. Achado fora do escopo, não corrigido: `src/app/orders/order-views.tsx` e ambos `.../orders/directory-copy.ts` seguem no `sourceInventory` de `orders.evidence.spec.ts`/`admin-orders.evidence.spec.ts` mas não existem no disco (fora do alvo dirigido desta frente, que era só `/orders/[id]` e `/admin/orders/[id]`).
- **Evidência:** removidas 14 chaves de `payment-links-directory`, 14 de `payment-links` (admin*), 5 de `orders-directory`, 2 de `payment-links-form`, 4 de `checkout` — todas com grep -w zero-consumidor em `src/`+`tests/`; `orders`/`checkout-policy` sem chave morta. Paridade `pt-BR`×`en` confirmada em todos os domínios tocados. Grep final por `orders/[id]/page`, `admin/orders/[id]/page` em `src/`+`tests/`: limpo.
