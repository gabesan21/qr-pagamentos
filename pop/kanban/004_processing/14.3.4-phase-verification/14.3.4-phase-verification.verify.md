# Julgamento — [[14.3.4-phase-verification]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** full — phase-verification task in round 1: the surface is the whole phase checklist, not a delta.
- **Superfície:** integrated diff `01b8e1ac..6dcad432` (26 code files plus the regenerated `artifacts/design-system/**`), the three checklists in `pop/memory/2026-09-08/14.3.{1,2,3}-*phase-checklist`, `src/data-directory/AGENTS.md`, the gate evidence recorded in the card, and one targeted run of the two disputed token test files.
- **Devoluções:** execução 0 de 2 · plano 0 de 2 — 3ª da mesma rota ativa circuit breaker.

### Conformidade com objetivo e specs

**Original request:** met in two thirds. The gate ran green, the mutation/directory assertions were realigned in place with no test deleted, the cheap repairs landed, a real bug (row clicks swallowed) was found and fixed, and the evidence is current. Three items the card's "O quê" names explicitly — the on-soft token pins, the 27-utility count, and the stripped reserved key — were never discharged, and this is the only stage where they run.

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Aggregate gate green | evidência | recorded run: tsc 0, lint 0 errors / 66 pre-existing warnings, vitest 2116/28 (×2), build 0 | passou | card §Log; no environment failure |
| C02 | Suite not weakened | evidência | diff: no test file deleted, no new `skip`, 2116 ≥ 2104 | passou | but see B1: one pin is green and void |
| C03 | Directory suites realigned | evidência | 9 `page.test.tsx` + `specimen-mounted` | falhou (parcial) | mocks and `?filters=ignored` redirects are right; the three admin `directory-query.test.ts` are untouched and no test anywhere asserts the stripped reserved key (B2) |
| C04 | Mutation destinations realigned | evidência | orders-v2, links-v2, V1 links, `(merchant)/page`, password, nautt | passou | on-page targets, session rotation instead of `/login`, `#settings-connection` anchor all asserted |
| C05 | Token pins moved to the on-soft contract | evidência | reading the three named files | falhou | none of the three was touched: 30 primitives and 27 utilities unpinned, pill map still on `text-<tone>` (B1) |
| C06 | 14.3.1 regression covered | evidência | new `data-directory-client.test.tsx` | falhou (parcial) | debounce, immediate commit, cursor drop, chip labels and row href are covered; the reserved-pair strip across the eight resolvers and the pending skeleton are not (B2); the footer case does not pin (B3) |
| C07 | 14.3.2 regression covered | evidência | `form-draft.test.tsx` + route/page suites | passou | textarea/select round-trips, dirty-only drafts, closed notice set, fresh `qr_session` on password change; password/file exclusion is structural (N1) |
| C08 | 14.3.3 regression covered | evidência | `scripts/design-tokens.test.ts:154-177` | falhou (parcial) | a pre-existing generic ratio assertion covers 4 roles × 6 themes; `accent` soft-foreground is uncovered and nothing was added here (B1) |
| C09 | Cheap repairs applied and pinned | evidência | reading the repaired files and their tests | passou (parcial) | encode, uploader contrast and footer applied; debounce and default `pageSize` dropped as follow-ups under the criterion's own clause; `tokens:check` green inside `lint` |
| C10 | Design-system evidence current | evidência | manifest audit: 48 captures, `gitHead` ancestor of HEAD, 68 source hashes | passou | 0 hash mismatch when recomputed; see S1 |
| C11 | Directory filtering feels live | humano | — | checklist humana | — |
| C12 | Invalid URL and no-JS paths | humano | — | checklist humana | — |
| C13 | Typed input survives failure | humano | — | checklist humana | — |
| C14 | Password change keeps the session | humano | — | checklist humana | — |
| C15 | On-soft pills legible | humano | — | checklist humana | — |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante (B1) | The token pins still encode the pre-14.3.3 contract and pass by accident: the test's mirror map says `text-danger` while the component renders `text-danger-on-soft`, so `toContain` matches on a substring; the utility test lists 22 names and calls itself "exactly the 22" while `@theme inline` has 61 entries including the five `--color-*-on-soft`; the 30 `*-soft-foreground` primitives have neither hex nor `interpolationStep` pinned | `src/components/ui/status-badge.test.tsx:11-25` vs `status-badge.tsx:10-14`; `src/app/globals-theme-tokens.test.ts:26-50`; `scripts/design-tokens.test.ts:161-177`; targeted run of both files: 44 passed — green and void | Mirror `text-<tone>-on-soft`, pin the 27 utilities and the 30 primitives (hex + step), including the `accent` role |
| bloqueante (B2) | The phase's most fragile invariant — every resolver stripping `filters=ignored` **before** canonicalizing, so the reset redirect never loops — has no test at all: `src/data-directory/server/notice.ts` has no test file and the three admin `directory-query.test.ts` never got the reserved-key case 14.3.1 registered | `src/data-directory/server/notice.ts:12-15` (the written contract); eight strip sites (6 resolvers + `links/v2/[id]/orders/page.tsx:39` + `directory-support.tsx`); grep for `filters=ignored` finds only redirect-target assertions | One case per site: input carrying the reserved pair resolves `ready` with the notice and no further redirect |
| sugestão (B3) | The footer case does not pin the repair: `router.replace` is a synchronous mock, so the transition has already settled and `isPending` is `false` when the assertion runs — the footer would be mounted against the pre-repair code too | `src/data-directory/ui/data-directory-client.test.tsx:159-176` | Hold the transition (a pending replace) or drop the claim that it pins |
| sugestão (S1) | The evidence manifest binds 68 sources but not `data-directory-client.tsx`: the row-click fix landed after the capture and the verifier still accepts the run as current | `artifacts/design-system/20260908182111/manifest.json` (`gitHead` 1ef803b0) | Follow-up: add the client shell to the bound source list |
| nit (N1) | `FormDraftGuard` captures only declared `fieldNames`, so password/file are excluded by construction and have no explicit case | `src/app/form-draft.test.tsx:50-70` | No action required |

Real-bug fix audited: bounding `isInteractiveDescendant` to `event.currentTarget` keeps the DOX rule (an interactive descendant still blocks; the table's `role="region"` is an ancestor, not a descendant) and leaves the keyboard path — the explicit row action — untouched. `encodeURIComponent` is the identity for valid ids: the `Location` values asserted in both `[id]/route.test.ts` did not change.

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente (specs, prisma, docs and i18n untouched; `artifacts/` only regenerated by its script).
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [ ] Critérios `verify: phase` registrados na checklist da phase — C05, C06 and C08 were not discharged, and this stage is the only place they run.
- [x] Specs, documentação e contratos DOX afetados estão coerentes (no contract text touched, exactly as the plan required).

## Veredito

- **Decisão:** bloqueante de execução → 004_processing.
- **Bloqueantes:** B1 (on-soft pins not migrated — C05/C08) · B2 (reserved-pair strip uncovered — C03/C06).
- **Defeito de plano:** nenhum — the criteria did cover the card's request; the delivery did not meet them.
- **Sugestões/nits:** B3 (footer pin is inert) · S1 (client shell outside the bound sources) · N1.
- **Checklist humana:** C11 live filtering with no Apply, localized chips, row opens its detail · C12 malformed query resets with one informational toast and no loop, and with JS disabled Apply/Reset work with no "filters ignored" alert · C13 typed text survives a failure on comment, outcome, identity and link forms · C14 password change keeps the session on `/profile` · C15 on-soft pills legible at 12px in the six themes.
- **Resumo:** realignment, cheap repairs, the row-click fix and the refreshed evidence are correct and green; what the phase owed on the token contract and on the reserved key was not delivered, and the suite stays green precisely because it still encodes the old contract — the risk the plan's own strategy names.

## Delta da devolução

- **Tipo:** execucao.
- **Critérios afetados:** C05 (none of the three token files touched) · C08 (`accent` uncovered, 30 primitives unpinned) · C03/C06 (reserved key unasserted in the three admin `directory-query.test.ts` and across the eight strip sites).
- **Frentes afetadas:** `F01` — re-enters 004 with these items only.
- **Frentes intactas:** there is no second front; **approved and not to be redone**: the nine `page.test.tsx`, `specimen-mounted`, the mutation suites, `data-directory-client.test.tsx`, the three code repairs and the regenerated evidence.
- **Ação esperada:** pin `text-<tone>-on-soft`, the 27 utilities and the 30 primitives (including `accent`), and cover the `filters=ignored` strip before canonicalization at the eight sites; optionally harden the footer pin (B3).

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=scripts/design-tokens.test.ts,src/app/globals-theme-tokens.test.ts,src/components/ui/status-badge.test.tsx,src/data-directory/server/notice.ts,src/app/admin/accounts/directory-query.test.ts,src/app/admin/orders/directory-query.test.ts,src/app/admin/payment-links/directory-query.test.ts frentes=F01 intactas=nenhuma -->
