# Front F01 — Activate application frontend contract — [[12.1.2-specify-application-frontend-system]]

- **Delivery:** Finalize and activate the canonical frontend spec, record truthful implementation state, and close reciprocal supersession with the historical administrative visual spec.
- **Scope:** Durable presentation contract and spec lifecycle only; no `DESIGN.md` or production change.
- **Owner:** agent.
- **Owns:** `pop/specs/application-frontend-system.md`, `pop/specs/administrative-design-system.md`.
- **May read:** task card, parity README/manifest/obligations, template samples, roadmap/research, all linked specs, root/DOX contracts.
- **Must not edit:** `DESIGN.md`, `docs/frontend-template-parity/**`, `docs/template/**`, `src/**`, tests, manifests, dependencies, other specs.
- **Depends on:** none.
- **Expected input:** the 002 `draft`/`planned` application spec and integrated develop parity contract.
- **Skills:** [[.agents/skills/write-spec/SKILL|write-spec]], [[.agents/skills/sync-specs/SKILL|sync-specs]], [[.agents/skills/ui-change/SKILL|ui-change]] — *use for canonical scope, truthful lifecycle, and design-contract completeness*.
- **Criteria:** C01, C02, C06 in [[12.1.2-specify-application-frontend-system.plan|plan]].

## Execution contract

- Preserve the one-question scope: how the supplied template governs application presentation without changing behavior.
- Keep exact numeric rules or bind their hash-derived parity records; do not replace them with adjectives.
- Preserve history; update both supersession ends rather than rewriting prior delivered slices.
- Set implementation truth to `partial`, not `implemented`, because later Epoch 12 tasks own production conformance.
- Missing legal/provenance evidence blocks only the relevant font/asset lane and must not be silently waived.
- Run the assigned criteria and report `BLOCKED` if parity conflicts with an authoritative business/security contract.
- Do not alter paths outside `Owns`; a new need returns to the orchestrator.

## Result

- **Status:** pending.
- **Commit/artifact:** pending.
- **Files changed:** pending.
- **Deviations:** pending.
- **Evidence:** pending.
