# Group g02 — Repair and regress — [[1.4.1-repair-login-submit-reliability]]

- **Order:** 2
- **Depends on:** g01
- **Description:** Apply the smallest observed root-cause fix while preserving authentication semantics.

## Movements

- [ ] (agent) Walk the DOX path for every confirmed edit, then implement the smallest fix at the boundary named by g01; use only the triggered fork from the plan.
  - Expected: the characterization test changes from fail to pass with one coherent behavioral intention and no login-page redesign or unrelated refactor.
  - If it fails: repair requires a new product contract, broad refactor, or another subtree → record the divergence and return to 002 instead of widening scope.
- [ ] (agent) (after previous) Strengthen `src/auth/sign-in.test.ts` and the owning service/adapter test for valid login, invalid login, cookie absence/presence, locale call, and infrastructure-failure distinction.
  - Expected: tests assert generic invalid credentials while ensuring thrown infrastructure faults are not converted into that outcome or an authenticated cookie.
  - If it fails: mocks cannot represent the confirmed defect → keep only route semantics in the mocked test and protect the defect at the owning boundary plus container scenario.
- [ ] (agent) (after previous) Run the focused test command from the diagnosis and `pnpm test -- src/auth/sign-in.test.ts` after reviewing the diff for names, explicit exceptional paths, and unnecessary abstractions.
  - Expected: both exit 0 and a new reader can identify the happy path, invalid-credential path, and infrastructure-failure path locally.
  - If it fails: regression or unclear control flow → revert the last structural transformation, restore the characterized contract, and repair incrementally.

## Execution notes

- 2026-07-16 — Planned from an unresolved runtime exception; g01 controls the exact fork.
