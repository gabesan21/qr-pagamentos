# Group g03 — Converge and evidence — [[1.4.1-repair-login-submit-reliability]]

- **Order:** 3
- **Depends on:** g02
- **Description:** Prove the repair in disposable production, database, application, and documentation gates.

## Movements

- [ ] (agent) Commit the candidate branch, verify the worktree is clean, then run `pnpm container:test --clean-clone --scenario login`.
  - Expected: exit 0 with all four named login PASS markers, automatic resource cleanup, and no printed secret/database URL.
  - If it fails: valid POST still returns 500 or assertions fail → collect sanitized logs, return to the owning g01 fork, and amend only after the focused regression explains the failure.
- [ ] (agent) (after previous) Run `pnpm db:test` and `pnpm check` independently.
  - Expected: both exit 0; database migrations/privileges and database-free typecheck, lint, tests, and build pass.
  - If it fails: relevant regression → repair within the confirmed boundary; unrelated pre-existing failure → document exact command/evidence and abort rather than suppressing it.
- [ ] (agent) (after previous) Finalize the diagnosis with pre-fix/post-fix evidence, verify it contains no generated credential, cookie value, connection string, or internal exception detail exposed to a client, and review affected DOX contracts.
  - Expected: artifact names root cause and final commit/test commands; contracts remain accurate or are updated with the code; `python3 pop/scripts/pop_validate.py` exits 0.
  - If it fails: redaction or DOX drift → remove the unsafe evidence/update the owning contract and rerun validation before 005.

## Execution notes

- 2026-07-16 — Planned; clean-clone execution requires committed HEAD by harness contract.
