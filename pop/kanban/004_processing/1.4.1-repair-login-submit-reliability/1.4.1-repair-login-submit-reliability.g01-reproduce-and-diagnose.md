# Group g01 — Reproduce and diagnose — [[1.4.1-repair-login-submit-reliability]]

- **Order:** 1
- **Depends on:** none
- **Description:** Establish a deterministic real-runtime failure before authorizing a fix.

## Movements

- [ ] (agent) In the task worktree, extend `pop/scripts/container-test.mjs` with a `login` scenario and an HTTP POST helper using its generated `admin.user` and password, loopback published port, cleanup, and redaction machinery.
  - Expected: the probe can distinguish invalid and valid 303 redirects, parse `Set-Cookie` attributes without printing its value, and query only non-secret session count/preferred-locale state.
  - If it fails: harness cannot reach the app or safely inspect state → reuse `startHappy`, the existing port resolver, and a runtime-role/DB-container probe; do not introduce a host dependency or public DB port.
- [ ] (agent) (after previous) Run the login scenario against the pre-fix application and capture exit/status plus sanitized relevant Compose/database evidence in `1.4.1-repair-login-submit-reliability.diagnosis.md`.
  - Expected: valid login reproduces the reported failure and the evidence identifies whether credentials, session transaction, validation, locale persistence, schema, privilege, or configuration failed.
  - If it fails: current clean clone returns success → execute the plan's environment-drift fork; if no reported-state evidence confirms a defect, trigger the no-reproduction abort.
- [ ] (agent) (after previous) Add a focused test that characterizes the confirmed failure and demonstrate it fails before the repair.
  - Expected: the diagnosis records the exact test command, pre-fix nonzero/assertion evidence, root-cause hypothesis, and discarded alternatives without secret values or usable URLs.
  - If it fails: test cannot isolate the observed boundary → narrow it to the owning adapter/service before editing production behavior; do not encode incidental log text.

## Execution notes

- 2026-07-16 — Planned; no runtime diagnosis exists yet.
