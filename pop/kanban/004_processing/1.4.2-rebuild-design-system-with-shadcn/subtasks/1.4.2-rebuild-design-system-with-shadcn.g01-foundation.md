# Group g01 - Foundation and toolchain - [[1.4.2-rebuild-design-system-with-shadcn]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Prove the exact official shadcn and browser foundation before visual implementation begins.

## Movements

- [ ] (agent) In the task worktree, re-run project runtime versions, ensure the baseline is clean, and run `pnpm install --frozen-lockfile` plus characterization tests for current primitives/specimen.
  - Expected: pinned Node/pnpm match, install is unchanged, and current tests pass before migration.
  - If it fails: dirty baseline or failing characterization -> preserve user changes, record evidence, and stop before attributing the failure to this task.
- [ ] (agent) Resolve exact current versions with `pnpm view` for `shadcn`, the dependencies generated by its official preset, Tailwind/PostCSS, `@fontsource-variable/ibm-plex-sans`, `@playwright/test`, and `@axe-core/playwright`; record the lookup and save every direct dependency with `--save-exact`.
  - Expected: package.json contains no ranges for new direct packages and the lockfile resolves one version each.
  - If it fails: registry/DNS/auth error -> retry once with the approved package runner; persistent failure means `blocked`, never infer versions.
- [ ] (agent) Run documented `pnpm exec shadcn init --preset nova --yes --no-monorepo`, then `pnpm exec shadcn info` and `pnpm exec shadcn preset resolve --json`; capture both outputs before any component add.
  - Expected: info reports Next App Router, RSC, `@/*`, CSS variables, and `src/app/globals.css`; resolved JSON reports base `radix` and style `nova`.
  - If it fails: incompatible CSS/config or any different base/style -> inspect the CLI output and abort; never use `info --json`, silently switch to base, or hand-author a preset URL.
- [ ] (agent) Use official registry `search/view` and `add --dry-run` before adding the planned inventory; inspect every added source and dependency before accepting it.
  - Expected: only official items and their required files enter the tree; imports match `@/*`, group composition and RSC boundaries are valid, and no community registry appears.
  - If it fails: missing item -> take the official-composition fork; hardcoded/missing imports -> repair against `pnpm exec shadcn info` and re-run typecheck.
- [ ] (agent) Add `playwright.config.ts` and named `tests/browser-launch.smoke.spec.ts`, run the agent-owned `pnpm exec playwright install chromium`, then run `pnpm exec playwright test tests/browser-launch.smoke.spec.ts --project=chromium --workers=1`.
  - Expected: Chromium launches headlessly and loads the specimen with no browser install prompt.
  - If it fails: missing host library or forbidden download -> use Playwright diagnostics once; any required human/system install marks the task blocked with no fallback.

## Execution notes

- 2026-07-16 - Planning preflight found no browser executable or local browser automation package; this group is a hard dependency for g02-g04.
