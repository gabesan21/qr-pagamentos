# Frente F03 — Payment-links directory and detail — [[14.4.2-converge-admin-dashboard-and-directories]]

- **Entrega:** `/admin/payment-links` with the identifier `CopyField`, amount-or-products and created columns plus merchant and money filters; `/admin/payment-links/v2/[id]` with the read-only public URL, the associated-orders list and the illustrated unavailable state; and the additive administrator lookup by link identifier that F02 consumes.
- **Escopo:** the administrator payment-link surfaces and their query contract, plus the shared unavailable composition. The owner projection, merchant link pages and every order surface are out.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this slice, the plan's "Estratégia" and "Critérios de aceite", `pop/researches/template-fidelity-convergence/admin-area.md` (rows `/admin/payment-links` and `/admin/payment-links/v2/[id]`), `docs/template/app/src/pages/admin/{AdminLinks,AdminLinkDetail,unavailable,shared}.tsx`.
- **Owns:** `src/auth/payment-link-v2-admin-directory.ts`, `src/app/admin/payment-links/**` (page, `directory-query.ts`, the detail page, new route skeletons, one new admin-local associated-orders card), the unavailable composition in `src/app/(merchant)/links/link-v2-views.tsx`, `src/i18n/dictionaries/payment-links-directory/{en,pt-BR}.ts`.
- **May read:** `src/data-directory/**` and its `AGENTS.md`, `src/auth/payment-link-v2-view.ts`, `src/orders/order-v2-admin-directory.ts`, `src/components/ui/**`, `pop/specs/{administrative-foundation,catalog-and-payment-links}.md`, `DESIGN.md`.
- **Must not edit:** `src/auth/payment-link-v2-view.ts` and the owner link services, `src/orders/**`, `src/app/admin/orders/**`, `src/app/admin/{page,dashboard,loading,error}.tsx`, the merchant link pages and every other part of `link-v2-views.tsx` beyond optional props and the unavailable composition, `src/components/ui/**`, `src/app/globals.css`, other dictionary domains, `**/*.test.*`, `pop/specs/**`, `DESIGN.md`, any `AGENTS.md`.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use before touching the query contract*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the columns, detail cards and states*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** execution report naming the registered filters, the columns, the lookup signature F02 consumes and any gap recorded, status `concluída | BLOCKED`.
- **Critérios:** C07, C08, C10, C11.

## Contrato de execução

- **Query contract:** register `merchant` (text; exact, case-insensitive owner-username equality, the same semantics F02 registers on orders) and `money` (`USD`/`FIAT`, resolved through the active exchange-currency pointer on the link's currency pair, mirroring the order directory's bucket) beside the delivered `state`/`type`/`kind`/`from`/`to` — 7 filters, inside the cap of eight. The template's currency-pair select is not reproducible: registered filters are closed static enums and pair labels are dynamic registry rows — record that gap for F04. Page sizes, keyset, cursor digest inputs, `q` semantics and the row DTO stay as delivered.
- **Identifier lookup:** add one bounded administrator read that resolves a link by its 24-character identifier and returns the same found/unavailable shape as the delivered `getForAdmin` (re-authorized administrator, no owner scoping, one opaque outcome for malformed and missing identities, the same select). It is a read: no mutation, no new store beyond one `findUnique`. Report its exact signature — F02 is blocked without it.
- **Directory:** columns identifier (compact `CopyField`), merchant, composition, type, state (`LinkLifecycleBadge` with dictionary labels), amount when fixed or the localized "N products" count, orders count, created with expiry beneath it, actions. Keep the row href, the explicit action, the invalid-filters notice and the chip resolution delivered by 14.3.1.
- **Detail:** render the public-URL `CopyField` (read-only sharing of an already public path — stop passing the flag that hides it) beside the identifier, keep the owner card and the timeline, and add an admin-local associated-orders card: query the delivered administrator order directory with `filter.link=<identifier>` at a registered page size, render at most five rows with provider and outcome badges, amount and a link to `/admin/orders/v2/<id>`, a localized empty line when there is none, and a view-all link to `/admin/orders?filter.link=<encoded identifier>`. No mutation, no owner-only surface.
- **Unavailable:** replace the destructive alert with the `EmptyState` unavailable illustration plus the back action, keeping the same opaque copy. The float `Number()` subtotal in the shared composition card is out of this front's scope — record it as a follow-up in the report.
- Every new or changed user-facing string lives in `payment-links-directory/en.ts` and `payment-links-directory/pt-BR.ts` under the same key.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <lookup signature, registered filters, `pnpm typecheck`/`pnpm lint` exit codes, follow-ups recorded>.
