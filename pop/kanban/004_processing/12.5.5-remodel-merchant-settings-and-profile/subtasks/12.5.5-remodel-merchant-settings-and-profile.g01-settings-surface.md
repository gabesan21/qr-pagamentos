# Frente F01 — Settings surface — [[12.5.5-remodel-merchant-settings-and-profile]]

- **Entrega:** `/settings` remodelada com layout de template (navegação ancorada, cards por seção, preview), mantendo native POSTs e contratos de negócio.
- **Escopo:** Recompor a página merchant `/settings` e seus componentes auxiliares; preservar rotas, serviços e validações existentes.
- **Responsável:** agent.
- **Owns:** `src/app/(merchant)/settings/page.tsx`, `loading.tsx`, `page.test.tsx`; `src/app/checkout-policy-management.tsx`; `src/app/storefront-settings-management.tsx`; `src/app/nautt-credential-surface.tsx`; `src/app/language-preference/language-preference-form.tsx`.
- **May read:** `docs/template/app/src/pages/settings/SettingsPage.tsx`; `src/app/admin/settings/**` (referência de padrões admin); specs de storefront, Nautt, checkout e frontend.
- **Must not edit:** rotas em `src/app/storefront/**`, `src/app/checkout-policy/**`, `src/app/nautt-credentials/**`, `src/app/language-preference/route.ts`; serviços em `src/auth/**`, `src/integrations/nautt/**`; schemas.
- **Depends on:** nenhuma.
- **Skills:** ui-change, clean-code-change.
- **Critérios:** 1, 2, 3, 7.

## Contrato de execução

- Preservar native POST e redirects opacos; notícias continuam vindas de query params resolvidos server-side.
- Não reimplementar validação no cliente além do necessário para estados visuais; validações permanecem no servidor.
- Dependência ou entrada ausente → `BLOCKED` ao orquestrador.

## Resultado

- **Status:**
- **Commit/artefato:**
- **Arquivos alterados:**
- **Desvios:**
- **Evidência:**
