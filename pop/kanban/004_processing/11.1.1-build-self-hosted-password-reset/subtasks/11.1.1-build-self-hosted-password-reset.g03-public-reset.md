# Front F03 — Public reset consume flow and bilingual mail evidence — [[11.1.1-build-self-hosted-password-reset]]

- **Deliverable:** Public `/reset-password` page consumes the challenge, rotates the password, and revokes sessions; `/reset-password` evidence spec covers valid/invalid/error states, bilingual copy, and a11y.
- **Scope:** existing page/submit, bilingual message, evidence spec, verify scripts.
- **Responsible:** agent.
- **Owns:** `src/app/reset-password/page.tsx`, `src/app/reset-password/submit/route.ts`, `src/app/reset-password/reset-password-submit.tsx`, `src/auth/admin-password-reset.ts` (message), `tests/reset-password.evidence.spec.ts`, evidence verify scripts, `package.json`.
- **May read:** `src/auth/password-reset.ts`, `src/i18n/dictionaries`, `tests/*.evidence.spec.ts`, `scripts/run-*-evidence.mjs`, `scripts/verify-*-evidence.mjs`.
- **Must not edit:** `src/auth/mail-config.ts`, `compose.yaml`, install scripts.
- **Depends on:** F01, F02.
- **Skills:** [[pop/skills/ui-change|ui-change]], [[pop/skills/ui-review|ui-review]].
- **Criteria:** 5, 6, 7.

## Execution contract

- Verify `src/app/reset-password/page.tsx` and `submit/route.ts` already meet consumption, rotation, and revocation; adjust only gaps.
- Ensure the email message (`buildResetMessage`) is bilingual (`pt-BR`/`en` in the same body) and does not expose internal identity.
- Add `tests/reset-password.evidence.spec.ts` with scenarios: valid token (form visible), invalid token (alert), submit error, pt-BR/en locales, empty serious/critical axe violations.
- Add `run-reset-password-evidence.mjs` / `verify-reset-password-evidence.mjs` and `reset-password:evidence` / `:verify` entries in `package.json`.
- Reuse/adapt `33f796e` to keep `admin-users` evidence with the reset button and notices.

## Result

- **Status:** —
- **Commit/artifact:** —
- **Changed files:** —
- **Deviations:** none.
- **Evidence:** `pnpm reset-password:evidence:verify` and `pnpm admin-users:evidence:verify`.
