# Front F04 — Container and installer integration — [[11.1.1-build-self-hosted-password-reset]]

- **Deliverable:** Container and installer stage/mount the new SMTP/public-origin secrets; container and installer tests cover file-backed configuration; runbook updated.
- **Scope:** `container:test`, `install/test.sh`, operational documentation.
- **Responsible:** agent.
- **Owns:** `pop/scripts/container-test.mjs`, `install/test.sh`, `docs/production-runbook.md`.
- **May read:** `compose.yaml`, `pop/scripts/container-prepare-secrets.mjs`, `install/install.sh`, `install/lib-operations.sh`, `container/AGENTS.md`, `install/AGENTS.md`.
- **Must not edit:** application code, schema, migrations.
- **Depends on:** F01.
- **Skills:** [[pop/skills/clean-code-change|clean-code-change]].
- **Criteria:** 2, 8.

## Execution contract

- Update `pop/scripts/container-test.mjs` to generate/validate SMTP/public-origin files in scenarios that build secrets (e.g., `config`, `happy`, `install-lifecycle`).
- Update `install/test.sh` to provide `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`, `SMTP_TLS_MODE`, `PUBLIC_ORIGIN` in `install/.env` and verify the staged files are mounted.
- Update `docs/production-runbook.md` describing the new file-backed secrets and that the app consumes them via `/run/secrets/`.
- Ensure disposable scenarios validate permissions (`0600` source, `0400` staged, UID/GID `1000:1000`).

## Result

- **Status:** —
- **Commit/artifact:** —
- **Changed files:** —
- **Deviations:** none.
- **Evidence:** `pnpm container:test --clean-clone --scenario config` and `install/test.sh`.
