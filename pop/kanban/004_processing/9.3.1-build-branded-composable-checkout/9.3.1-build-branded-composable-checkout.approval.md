# Approval — [[9.3.1-build-branded-composable-checkout]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read of the card, plan, linked contracts, and the delivered code base. Every load-bearing claim in the plan was checked against reality:

- **Depends_on merged.** 006_done is cleaned per workflow; memories exist for 8.1.2, 8.1.3, and 9.1.1 (`pop/memory/`). 9.1.3 and 9.2.2 sit in 003 alongside this task and are correctly treated as parallel consumers, not prerequisites.
- **Seams exist.** `totalFromLines` (`src/orders/order-v2.ts:168`), `createFromLink` (service method, `src/orders/order-v2.ts:276`), `normalizeCustomerSnapshotV1` (`src/orders/payment-link-order.ts:125`), `createPollingController` exported (`src/app/pay/[identifier]/public-checkout-form.tsx:34`). The V1-first additive-branch precedent is real (`src/auth/public-payment-link-v2.ts`).
- **Zero-inventory-edit claim verified.** `src/app/api/payment-links/[identifier]/checkout/route.ts` wraps the literal template in `withServerRequestLog` and runs the limiter before parsing; the request-log inventory already carries both `publicCheckout` templates (`src/observability/server-request-log.ts:38-39`). An in-handler V2 branch genuinely requires no inventory or limiter edits.
- **Token whitelist mechanism matches.** `scripts/check-design-tokens.mjs:10-11` is an exact per-path declaration whitelist; one appended entry for the `/pay/[identifier]` V2 branch is the established pattern (9.2.2's approved plan does the same).
- **Cross-task coordination is honest.** 9.2.2's plan adds keys only to `storefront/*` and reuses `checkout/*` read-only — exactly the split 9.3.1's decision 7 asserts. 9.1.3's plan explicitly records that `/pay/[identifier]` renders V2 only via 9.3.1 and redirects there — decision 1's "no 9.1.3 change" holds.
- **Branding independence (decision 5) is contract-honest.** `src/storefront/public-storefront.ts:187` gates only the slug-based public store read on `storefrontEnabled`; the branding resolution itself (accent, `themeId ?? DEFAULT_STOREFRONT_THEME_ID`, logo — lines 162-165) is owner-record data with no enabled-flag dependency. No contract ties theme/accent/logo to store publication; the flag gates the slug surface, not the owner's persisted theme preference. A merchant with storefront disabled keeps branded checkout on their own V2 links without violating any written boundary. The plan also flags the decision with an explicit replan fallback, which is the right posture.
- **Evidence and gates exist.** `merchant-dashboard:evidence`/`:verify` and `orders:evidence`/`:verify` set the pair pattern; `pnpm check` and `pnpm db:test` exist.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result is a concrete buyer journey plus byte-identical V1 and named green gates (`pnpm check`, `checkout:evidence` pair, `pnpm db:test`); each acceptance criterion binds a command, a pass condition, and a 005 mode.
2. **Sufficient lean brief — pass.** Nine pinned decisions, per-front owns/may-read/must-not-edit, risks with abort conditions, specs with expected changes; 115 dense lines, no implementation dump.
3. **Safe execution — pass.** F01 (2 service files + tests, 4 route files) ∥ F02 (1 presentation file + test) have disjoint write sets and disjoint read lists; F03 depends on both, F04 on F03, F05 last. Single executor, orchestrator-validated ownership, diff-based forbidden-path criterion (5). The parallel claim is sound.
4. **Proportional specs — pass.** Three linked specs each carry an expected change owned by F05, plus root AGENTS.md, `src/checkout/AGENTS.md` (whose line 15 forward-reference this task fulfills), and DESIGN.md.
5. **No avoidable `(user)` item — pass.** Card carries only the checked "Pronto para planejar"; the plan assigns no human execution item.
6. **Proportional to risk/size — pass.** Critical + L justifies five fronts, eight criteria, disposable-Postgres contract tests, and a run-bound evidence pair. The three risky decisions (single canonical route, branding-when-disabled, zero inventory edits) are explicitly flagged with fallbacks.
7. **Economic verification — pass.** Criteria 1, 2, 3, 4, 6, 7 are `re-run`; 5 and 8 are evidence reads; no web anywhere.

#### Risk assessment

005 (always full for critical) must verify, with re-runs not reads:

- **V1 byte-stability in practice:** page.tsx gains a branch — confirm V1 identifiers render through the untouched V1 path (V1 page/form tests unmodified and green), V1-first resolution order on both API routes, unknown identifier → the one opaque unavailable, and zero diffs in the six delivered `src/checkout/` files, both inventories, `public-checkout-form.tsx`, `src/orders/*`, `src/auth/*`, `prisma/`.
- **Sessionless trust boundary:** browser body exactly `{ idempotencyKey, customer }`; totals from `totalFromLines`/fixed amount over locked link state; policy from the owner's locked `checkoutDataPolicy`; absence assertions proving no owner identity, pair UUIDs, state/version/timestamps, or provider data in the presentation DTO.
- **Reservation fences over `checkout_attempt_v2`:** single transaction, link `FOR UPDATE`, `payment_link_v2_single_use_settlement` claim rejection, replay reissue-only (no re-quote/re-dispatch), 24h TTL bounded by link expiry, `INDETERMINATE` on ambiguous dispatch, no transaction across provider I/O, orders shaped only via `createFromLink` — via `pnpm db:test`.
- **Branding independence implemented as specified:** owner-settings read (not the slug-gated public read), `themeId ?? DEFAULT_STOREFRONT_THEME_ID`, accent null → no override, logo via `/media/[identifier]` with `BrandIdentity` fallback; exactly one appended whitelist entry and an append-only `globals.css` checkout block.
- **Client contract:** pinned state union, V1-identical polling cadence, `404` → one opaque unavailable, consumed SINGLE_USE → opaque unavailable with no paid view (9.3.2 boundary respected).
- **Cross-task hygiene:** checkout-namespace keys only in `dictionaries/checkout/*` with parity tests green; if 9.2.2 or 9.1.3 merged first, confirm only append-only rebases touched shared files (whitelist, `package.json`, docs).
- **Evidence:** `checkout:evidence` then `checkout:evidence:verify` both exit 0 with manifest covering compositions × themes × locales × widths and the decision-8 states; `pnpm check` green.

aprovado por revisor independente (yolo) — 2026-07-26
