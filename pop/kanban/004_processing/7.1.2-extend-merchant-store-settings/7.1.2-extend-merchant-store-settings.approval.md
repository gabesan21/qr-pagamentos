# 7.1.2-extend-merchant-store-settings — Approval

## Round 1 (yolo critic, 003 plan gate)

### Resposta do crítico (yolo)

**Verdict: RETURN** (devolução 1/2 → 002).

Grounding verified against repo reality: existing save contract in
`src/auth/storefront-settings.ts` + `src/app/storefront/route.ts` (opaque
`?storefront=changed|failed|conflict`, empty 401/403) matches the plan;
`requireActivePair` / `NoActiveExchangeCurrencyMappingError` /
`getSupportedExchangeCurrencyService()` exist in
`src/auth/supported-exchange-currency.ts:112-121,240-242`; the media service
(`src/media/media-service.ts:129-142`) already fences activate/orphan through
`store.transition` and `findByIdentifier` exists, so identifier-based
`activateOwned`/`orphanOwned` fit the lifecycle contract; `STOREFRONT_LOGO`
exists (`src/media/types.ts:1`); the six-theme closed set with default
`pix-paper` is confirmed in `src/design-system/tokens/resolver.json`;
`app.user` runtime grants are table-level (`prisma/bootstrap.sql:31`), so new
columns need no ACL change; dependency commits `cfe97c8`, `56db7c4`,
`f99ba93` exist; the logo activate→save→orphan + compensation pattern is
race-bounded (revision fencing makes concurrent saves fail or leak a
quota-counted ACTIVE object, never a broken reference) and is documented as
accepted risk; absent=unchanged merge semantics are implementable against the
current form contract and correctly protect the legacy dashboard card; the
public projection redaction boundary is consistent with
`src/storefront/public-storefront.ts` and `readPublic`'s ACTIVE-only reads.

**Blocking reason:**

1. **The currency-code check is not expressible in the closed migration safe
   language.** The plan specifies `storefront_default_currency_code` with
   `CHECK (~ '^[A-Z]{3}$')`, but `pop/scripts/migration-policy.mjs`
   `checkSql` (lines 127-154) admits only comparison ops (`eq/ne/lt/lte/gt/gte`),
   `and`/`or`/`not`, `isNull`, and `in` — there is no regex/`~` operator, and
   raw SQL is forbidden. Executed as written, the migration either fails
   `pnpm db:migration-policy` or forces an improvisation outside the plan.
   Fix: express it as the established bounds pattern from
   `20260724120000_supported_exchange_currencies`
   (`code >= 'AAA' AND code <= 'ZZZ'`, see its `migration.safe.json`), matching
   the `prisma/AGENTS.md` "AAA–ZZZ bounds check as defense-in-depth behind
   service-boundary format validation" precedent; adjust the plan text and AC2
   wording accordingly.

**Non-blocking advisory (fix in the same 002 pass, not a return reason):**

2. The DB front's "must not edit" list names the 19 pinned baseline dirs and
   `20260724*_supported_exchange_currencies` but omits the other post-baseline
   migrations (`20260723233000_media_objects`,
   `20260724010000_owner_product_categories`,
   `20260724033000_merchant_profile_version`). The policy verifier is
   fail-closed on any history edit, so risk is low, but the deny list should
   read "all existing migration directories" for completeness.

— devolvido por revisor independente (yolo) — 2026-07-24

## Round 2 (yolo critic, 003 plan gate)

### Resposta do crítico (yolo)

**Verdict: APPROVE.**

Re-verified in fresh session:

1. **Round-1 blocking finding resolved.** The regex CHECK is gone; the plan
   (lines 33-39) now specifies the `code >= 'AAA' AND code <= 'ZZZ'` bounds
   check, expressible in `migration.safe.json` `checkSql` as `and` of
   `gte`/`lte` over column/string-literal operands — all admitted by
   `pop/scripts/migration-policy.mjs:127-154` — and structurally identical to
   the precedent in
   `prisma/migrations/20260724120000_supported_exchange_currencies/migration.safe.json:92-124`.
   AC2 wording updated to match ("currency `AAA`–`ZZZ` bounds check"). The
   theme/layout `IN` checks and the `BOOLEAN NOT NULL DEFAULT true` typed
   constant default are also safe-language-expressible (`in` op, line 148;
   non-null default rule, line 109). The known looseness of the bounds check
   (e.g. a lowercase third char passes) is precedent-consistent
   defense-in-depth; the plan keeps exact `[A-Z]{3}` enforcement at the
   service boundary.
2. **Round-1 advisory resolved.** The DB front deny list now reads "all
   existing migration directories" and explicitly names every post-baseline
   directory (`20260723233000_media_objects`,
   `20260724010000_owner_product_categories`,
   `20260724033000_merchant_profile_version`,
   `20260724120000_supported_exchange_currencies`).
3. **No new contradictions.** Six-theme closed set confirmed in
   `src/design-system/tokens/resolver.json` (default `pix-paper` first;
   longest id 17 chars ≤ `VARCHAR(24)`); media identifier pattern is exactly
   43 chars (`src/media/types.ts:3`) matching `VARCHAR(43)`; revision-fenced
   `store.transition` and `findByIdentifier` exist (`src/media/media-store.ts:20,29`),
   and `STOREFRONT_LOGO` exists (`src/media/types.ts:1`), so identifier-based
   `activateOwned`/`orphanOwned` fit the lifecycle; `requireActivePair`,
   `NoActiveExchangeCurrencyMappingError`, `getSupportedExchangeCurrencyService`
   confirmed at `src/auth/supported-exchange-currency.ts:31,112,240`; current
   `src/auth/storefront-settings.ts` shape (StorefrontSettingsData, slug
   uniqueness conflict, enable-requires-slug) matches the plan's extension
   contract and root AGENTS.md; every acceptance-gate script exists in
   `package.json` (`db:generate`, `db:migration-policy`, `db:contract-check`,
   `db:test`, `media:decoder-preflight`, `check`); the cited
   `migration-policy.mjs generate` subcommand exists
   (`pop/scripts/migration-policy.mjs:316`).

— aprovado por revisor independente (yolo) — 2026-07-24
