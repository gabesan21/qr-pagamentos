# Group g01 — Identity/password contract — [[1.2.2-establish-local-identities]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Deliver cohesive, server-only identity normalization and password credential primitives before persistence wiring.

## Movements

- [x] (agent) **g01.m1** Implement/tests the exact plan validator: reject NUL/CR/LF; trim edge SP/HT; ASCII lowercase; 6–254 total; one `@`; 1–64 allowed-character local with dot rules; 4–253 multi-label domain with 1–63 alnum/hyphen labels, hyphen-edge rule, and 2–63 letter final label; keep closed role/status and safe DTOs.
  - Expected: every named valid/boundary and invalid vector in the plan is asserted, canonical equality holds, and outward values omit hash/plaintext/internal fields.
  - If it fails: an input requires syntax outside this closed contract → return to 002; do not broaden or use locale-dependent behavior.
- [x] (agent) **g01.m2** Implement exactly the plan's ASCII `scrypt$v=1$N=131072,r=8,p=1$<22-char salt>$<43-char key>` grammar: 16-byte random salt, 32-byte key, `N=131072/r=8/p=1/maxmem=268435456`, ≤128-byte full-match parser, canonical base64url round-trip, and constant-time compare.
  - Expected: exact valid fixture and fresh-salt tests pass; malformed input returns one generic result before `scrypt`; plaintext and secrets are absent from errors.
  - If it fails: pinned Node/image rejects any exact parameter/resource/parser requirement → return to 002; do not tune or substitute.
- [x] (agent) **g01.m3** Add tests forbidding composition rules and covering whitespace/non-ASCII passwords plus record vectors for wrong prefix/version/parameters, field count/order, decimal spelling, padded/invalid/noncanonical base64url, decoded lengths, non-ASCII, trailing data, and >128 bytes.
  - Expected: approved 12/128-code-point passwords pass regardless of composition; every malformed record fails uniformly without a `scrypt` call or secret-bearing output.
  - If it fails: code-unit/byte counting drifts from the contract → centralize code-point counting and keep persistence/hashing behind the validated input type.

## Execution notes

- 2026-07-14 — Capture touched paths and focused test observations here; the full unit suite runs once through `pnpm check` in g04.
- 2026-07-14 — Added `src/auth/{identity,password}.ts` and 44 focused assertions. Failing-first observed both modules absent; the implemented run passed 2 files/44 tests on pinned tooling. Exact email boundaries, Unicode code-point limits, DTO selection, scrypt fixture/fresh salts, and malformed preflight/no-scrypt behavior passed; evidence is secret-free.
- 2026-07-14 — Verification correction expanded malformed scrypt observations to every promised class: missing/extra fields, parameter order and each wrong value, decimal spelling, padding/alphabet/canonicality, 15/17-byte salt encodings, 31/33-byte key encodings, non-ASCII, trailing data, and parser bound. Focused password run passed 27/27; final aggregate included all vectors.
