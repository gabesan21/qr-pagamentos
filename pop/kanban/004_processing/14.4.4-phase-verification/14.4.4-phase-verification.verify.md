# Julgamento — [[14.4.4-phase-verification]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** full — phase-verification task, round 1: the whole Phase 14.4 surface, and the plan declares the re-runs as `agent` criteria (ran here).
- **Superfície:** `59addba7..099a425c`, 52 source/test paths + regenerated `artifacts/{login,design-system}/20260908*`; own runs: `vitest run` and `tsc --noEmit`.
- **Devoluções:** execução 0 de 2 · plano 0 de 2 — 3ª da mesma rota ativa circuit breaker.

### Pedido original (card "O quê / Por quê")

Met on the gate itself: the aggregate check is green on the integrated tree, the falsified suites are realigned honestly (no suite weakened to go green, `account-mutation-form.test.ts` stays deleted, no new skip), both evidence runs are refreshed and bind the current sources, and the cheap repairs landed — including two real bugs the gate caught. **Not met** on "add the regression tests their checklists accumulated": five sites named in criteria 2–4 and the retired-BEM pin of criterion 5 have no assertion anywhere, so the phase's only test gate closes with those 14.4.2/14.4.3 surfaces unpinned.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| 1 | Falsified suites realigned | evidência | own `vitest run` + diff | passou | 252 files / 2179 passed / 28 skipped / 0 failed; no `.skip`/`.todo` added in the diff; `account-mutation-form.test.ts` absent; `page.test` deletions in `accounts/[id]` are genuinely re-covered by the four new section suites (logo fence, store link, unavailable states, TOTP disable, delete gate) |
| 2 | Shell/auth coverage added | evidência | new suites | **falhou** | title registry/fallback, `63.9375rem` (+`not.toContain("768px"/"48rem)")`), storefront link enabled+slugged, AuthCard, login toggle/forgot/required, OTP six cells+paste+auto-submit+`pattern`, reset states/meter/mismatch, bilingual 404 all present — but "storefront link … **never admin**" has no assertion (`admin/layout.tsx` never passes `storefrontLink`; nothing pins it, unlike the sibling `profileLink` pin at `app-shell.test.tsx:88`), and the rail-footer case asserts username/role pill/caption/no-sign-out but not the monogram (`app-shell.tsx:47`) |
| 3 | Admin dashboard/directories coverage | evidência | new suites | **falhou** | period pass-through, `lg:col-span-{5,4,3}`, both drill-downs, seven filters incl. `STATELESS`→null, `merchant`, `money`+no-pair, redaction and `getForAdminByIdentifier` all present — but "associated-orders card **badges/amount**" is unasserted: all three cases in `associated-orders-card.test.tsx` mock a rejection and the page test mocks `rows: []`, so `associated-orders-card.tsx:46-70,112` never renders a row |
| 4 | Accounts/settings coverage | evidência | new suites + grep | **falhou** | create-account modal+generator, `POST /admin/users` return, tab hash restore all present and strong; **absent:** §4 payment settings confirm + functional POST names and language apply-on-select (`grep -rln "payment-settings-section\|language-section\|confirm-toggle" src --include='*.test.*'` → empty; `settings/page.test.tsx` has 3 cases: anchors, notices, empty states). `src/app/admin/users/[id]/**` byte-identical ✔ |
| 5 | Pins would fail against pre-14.4 code | evidência | inspection | **falhou** | `titleFallback`/`titleRoutes` reject the retired `pageTitle` prop and `.app-shell__rail-caption` did not exist; `not.toContain("768px")`/`("48rem)")` reject the old media query; `/admin/accounts?success=created` rejects `/admin?success=created`; `/reset-password?status=changed` rejects `/login?password=changed`. **Missing:** nothing rejects the retired `.admin-dashboard*` — `grep -rn "admin-dashboard" src --include='*.test.*'` is empty; `page.test.tsx` merely stopped asserting them, and `scripts/check-admin-ui-inventory.mjs:11-14` still allowlists nine dead `admin-dashboard*` classes |
| 6 | Cheap in-phase repairs applied | evidência | diff | **falhou (8/9)** | OTP `pattern`, `TabsList aria-label`, `redirect()` no longer swallowed, dead `48rem` entry, `aria-describedby` id, `htmlFor` on the segmented group, dead generator fallback, "Vendedor"→"Lojista" all applied and covered; payer hint already behind a prop on `develop`. **Pending:** the login-form doc comment still claims native `required` keeps working with JS disabled, which `noValidate` disables (`login-form.tsx:38-43,66`) |
| 7 | Evidence refreshed and accepted | evidência | manifests | passou | both `gitHead` = `b63665ca`, ancestor of HEAD (`git merge-base --is-ancestor` ok); manifest source hashes match the current tree (`globals.css` `062dded4…`, incl. the later footer fix); `manifestSha256` in `design-system/current.json` matches the manifest byte-for-byte — the `68d18b28` pointer repoint is documented and truthful, not a fabricated artifact |
| 8 | Full aggregate gate green | evidência | own runs | passou | `vitest` 252/2179/28/0 (baseline 2140/28 → +39, nothing lost); `tsc --noEmit` exit 0; lint chain (0 errors, 72 pre-existing warnings) and `next build` from the executor's evidence, not re-run here |
| 9 | No edit outside the F01 write set | evidência | diff audit | passou (com nota) | `pop_check_scope` empty; specs/prisma/docs/frozen routes untouched; the shared-dictionary edit is the write-set extension logged in the card; `globals.css` and `preferences-section.tsx` are gate-caught repairs the card's "O quê" authorizes. Note: `admin/payment-links/v2/[id]/page.tsx` is not a criterion-6 repair source — see finding 1 |
| 10 | Above-reach defect proposed, not patched | evidência | diff | passou | no service, DTO or spec edit; both caught bugs stayed inside the phase's reach |
| 11 | Browser pass of Phase 14.4 | humano | — | checklist humana | ver veredito |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | Production render topology changed for test observability: the async card became a direct call so `renderToStaticMarkup` can see it. It is the only `{await <Component>()}` in the codebase, undocumented, and it removes the component boundary (no `<Suspense>` can ever wrap that read) — while the card already has its own suite | `src/app/admin/payment-links/v2/[id]/page.tsx:35` | restore `<AssociatedOrdersCard … />` and drop the two page-level assertions that depend on it, or keep it and state the reason in the file |
| sugestão | The dead-allowlist defect fixed for `48rem` survives for the retired dashboard BEM: nine `admin-dashboard*` classes (and several retired `app-shell__*`) still pass the inventory guard | `scripts/check-admin-ui-inventory.mjs:11-14,17-23` | prune the entries no `.tsx` renders anymore |
| nit | The new `totp-challenge-form.test.tsx` drops the semicolons the rest of the suite uses (lint does not flag it) | `src/app/login/totp-challenge-form.test.tsx` | match the file style when next touched |
| nit | `paymentLinkDirectoryProductsCount: "{count} products"` has no singular form and the new pin cements "1 products" | `src/i18n/dictionaries/payment-links-directory/en.ts:121` · `admin/payment-links/page.test.tsx:110` | follow-up outside this task: pluralize the label in both locales |

### Escopo e integração

- [x] Alterações respeitam `Owns`/`Must not edit`, com a nota do critério 9.
- [x] Dependências não foram implementadas oportunisticamente pelos consumidores.
- [x] Critérios `verify: phase` — não se aplica: esta **é** a task de verificação da phase; o critério 11 vai à checklist humana.
- [x] Specs, documentação e contratos DOX coerentes: nenhuma spec tocada, nenhuma mudança de contrato durável.

## Veredito

- **Decisão:** bloqueante de execução → 004_processing. Five named sites in criteria 2–4, the criterion 5 pin and one criterion 6 item were not delivered; they are distinct fronts in four files, above the two-adjustment ceiling of a directed repair.
- **Bloqueantes:** (a) no assertion pins "storefront link never admin"; (b) the associated-orders badges/amount are never rendered by any test; (c) payment-settings confirm + POST names and language apply-on-select have no assertion; (d) nothing rejects the retired `.admin-dashboard*`; (e) the `login-form` doc comment still contradicts `noValidate`.
- **Defeito de plano:** none — the criteria did cover the request; what failed was fulfilment.
- **Sugestões/nits:** see the table above; none blocks.
- **Checklist humana:** criterion 11, in the browser, in both locales and the six themes, at 320px and above 1024px: title per route while navigating, drawer below 1024px and rail above, storefront link target, OTP paste and auto-submit, reset states and success, bilingual 404, period control without scroll jump, both drill-downs, create-account modal and generator, tab hash restore on reload, scroll-spy and `aria-current`, payment-settings confirm, language apply-on-select, one toast per action, focus order and WCAG 2.2 AA. Also confirm `lint` and `build`, which were not re-run here.
- **Resumo:** the phase gate is green and the evidence is trustworthy; what is missing is the named coverage the phase itself commissioned — without it, 14.4 closes with four freshly converged surfaces unprotected.

## Delta da devolução

- **Tipo:** execucao.
- **Critérios afetados:** 2 (storefront "never admin"; rail-footer monogram), 3 (associated-orders badges/amount), 4 (payment-settings confirm + POST names; language apply-on-select), 5 (absence pin for `.admin-dashboard*`), 6 (`login-form` doc comment).
- **Frentes afetadas:** `F01` — reenters with these items only: one assertion in `app-shell.test.tsx` (sibling of the `profileLink` pin) plus the monogram; one case with a ready row in `associated-orders-card.test.tsx` (provider badge, neutral `StatusBadge` for `state === null`, formatted amount); assertions in `settings/page.test.tsx` (`type="submit" name="locale"` buttons with no Save; hidden `name="currencies" value="BRL"` and `name="paymentMethods" value="PIX"`, matching what `payment-settings/route.ts` reads) plus one jsdom case for the confirm when a row is switched off; `expect(html).not.toContain("admin-dashboard__")` in `admin/page.test.tsx`; one sentence in the `login-form.tsx` comment.
- **Frentes intactas:** everything else stays approved and must **not** be redone — the 18 realigned suites, the shell/login/reset/404/OTP coverage, the four account-editor section suites, the create-account modal, the directory filters, the nine repairs and both evidence runs (`gitHead` is already an ancestor and the hashes match; regenerate only if a manifest-bound source changes — no item in this delta changes one).
- **Ação esperada:** 004 adds the named assertions and the comment sentence, re-runs the aggregate gate and reports the counts; no strategy, product or evidence change.

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=src/app-shell/app-shell.test.tsx,src/app/admin/payment-links/associated-orders-card.test.tsx,src/app/admin/settings/page.test.tsx,src/app/admin/page.test.tsx,src/app/login/login-form.tsx frentes=F01 intactas=nenhuma -->
