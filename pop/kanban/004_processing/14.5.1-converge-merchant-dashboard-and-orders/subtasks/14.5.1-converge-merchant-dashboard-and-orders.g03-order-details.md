# Frente F03 — Order details — [[14.5.1-converge-merchant-dashboard-and-orders]]

- **Entrega:** `/orders/v2/[id]` with the template's outcome editor (select + note + confirmation), the monogram comment composer over a newest-first thread, and a link card carrying the lifecycle badge and the drill-down to `/links/v2/<id>`; both merchant detail routes gain a skeleton and keep one heading.
- **Escopo:** the merchant order detail surfaces, the shared order view files they compose, one additive owner link lookup and one additive `Timeline` slot. The directory page, the dashboard, the admin routes and the mutation routes are out.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this slice, the plan's "Estratégia" and "Critérios de aceite", `pop/researches/template-fidelity-convergence/merchant-commerce.md` (rows `/orders/[id]` and `/orders/v2/[id]`), `docs/template/app/src/pages/merchant/MerchantOrderDetail.tsx`.
- **Owns:** `src/app/orders/{order-v2-views,order-views}.tsx` (additive only), `src/app/(merchant)/orders/{[id],v2/[id]}/**` plus their new `loading.tsx`, one new client confirm wrapper under `src/app/(merchant)/orders/`, `src/auth/payment-link-v2-view.ts` (additive only), `src/components/ui/timeline.tsx` (additive only), `src/i18n/dictionaries/orders/{en,pt-BR}.ts`.
- **May read:** `src/orders/{order-v2-view,order-engagement-v2,order-view}.ts`, `src/app/orders-v2/[id]/route.ts`, `src/app/(merchant)/{orders/orders-notices.tsx,orders/directory-query.ts,links/**}`, `src/app/{admin/settings/confirm-toggle.tsx,(merchant)/catalog/categories/category-row-actions.tsx}` (confirm pattern), `src/components/ui/**`, `pop/specs/{checkout-and-order-lifecycle,application-frontend-system}.md`, `DESIGN.md`.
- **Must not edit:** `src/app/(merchant)/orders/{page.tsx,directory-query.ts,orders-notices.tsx,page-size-preference.tsx}`, `src/app/orders-v2/**` and every other route handler, `src/orders/**`, `src/app/admin/**`, `src/app/(merchant)/{page,dashboard,loading}.tsx`, `src/app/globals.css`, `src/components/ui/**` except `timeline.tsx`, other dictionary domains, `**/*.test.*`, `prisma/**`, `pop/specs/**`, `DESIGN.md`, any `AGENTS.md`.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use before touching shared views and the lookup*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the editor, thread and states*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** execution report naming the editor shape, the thread composition, the lookup added and the gaps recorded, status `concluída | BLOCKED`.
- **Critérios:** C08–C12.

## Contrato de execução

- **Outcome editor:** replace the two `<details>` forms with one `NativeSelect` over `LOCAL_FINALIZED`/`LOCAL_CANCELLED`, an optional note and a `ConfirmDialog` confirming the native form submit, posting the delivered `set-outcome` action with the lifecycle version to the unchanged route. **Do not gate on `confirmed` and do not invent an outcome:** the spec pins `AD_HOC` orders as stateless with the local outcome as their whole operator lifecycle and allows a new append after reconciliation, so the template's gate and its `in-progress`/`finalized` vocabulary do not map. Render a caption stating that a local outcome never writes, masks or shadows the payment state. Keep `FormDraftGuard` and the on-page notice delivered by 14.3.2.
- **Comments:** pair the composer textarea with `Monogram` for the signed-in owner and render the thread newest-first through `Timeline`. The author-only CAS edit is a delivered capability and stays: carry it in one additive optional `action` slot on `TimelineEntry`, absent by default so the chronology card and every other caller render exactly as delivered. Append-only semantics, the 1–2,000 body bounds and the version field are untouched.
- **Link card:** add an additive owner-scoped lookup by link identifier to `src/auth/payment-link-v2-view.ts`, returning the link id and derived lifecycle for the owner's own link and failing closed to the one opaque unavailable outcome otherwise. Render `LinkLifecycleBadge` and the link to `/links/v2/<id>` beside the existing identifier `CopyField`. If the shared card already carries an optional link prop from 14.4, reuse it verbatim rather than adding a second one, and keep every admin call site rendering as delivered.
- **Both detail routes:** keep the `EmptyState` illustration for the unavailable state, drop the heading duplicated above the breadcrumb, and add a `loading.tsx` whose geometry matches the final layout. **No payment-data card:** the projection exposes no payment reference and provider order UUIDs never render — record it as a gap, as with the V2 line-title column, which needs a merchant/admin-shared projection change and stays a follow-up.
- Every new or changed user-facing string lives in `orders/en.ts` and `orders/pt-BR.ts` under the same key; reuse the existing `orderV2*` keys read-only and add nothing to the `orders-directory` domain, which F02 owns this round.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <editor and thread composition, `pnpm typecheck`/`pnpm lint` exit codes, gaps recorded>.
