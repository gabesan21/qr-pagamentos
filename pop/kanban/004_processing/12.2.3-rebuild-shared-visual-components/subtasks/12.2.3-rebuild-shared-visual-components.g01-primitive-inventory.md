# Front F01 — Primitive inventory — [[12.2.3-rebuild-shared-visual-components]]

- **Delivery:** reconcile the existing owned primitive APIs and add only official sources/dependencies required by reachable parity responsibilities.
- **Scope:** primitive source, focused tests and dependency graph; no higher composition or consumer migration.
- **Owner:** agent.
- **Owns:** `src/components/ui/{alert,badge,button,card,checkbox,field,input,label,native-select,separator,skeleton,spinner,table,textarea,input-otp,switch,dialog,alert-dialog,tabs,avatar,empty,sonner,pagination}.tsx`, matching focused tests, `package.json`, `pnpm-lock.yaml`.
- **May read:** parity manifest/obligations and template reachable components; `components.json`; token sources; current consumers; shadcn rules/docs; frontend spec and DESIGN.
- **Must not edit:** F02/F03 composition files, `src/data-directory/**`, routes, shell, specimen, specs/DOX/evidence, template, tokens, brand/media/domain/server code.
- **Depends on:** tasks 12.2.1 and 12.2.2.
- **Expected input:** stable semantic token projection, identity family, 20 reachable component records and 49 generated exclusions.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/shadcn/SKILL|shadcn]], [[.agents/skills/react-best-practices/SKILL|vercel-react-best-practices]].
- **Criteria:** C01-C03, C06, C07.

## Execution contract

- Run official `pnpm dlx shadcn@latest info`, component `docs`, `add --dry-run`, and per-file `--diff` before any registry source change; never overwrite or fetch raw registry files.
- Preserve compatible exports used by current consumers unless a focused test proves a necessary migration inside this owned file set.
- Keep static primitives server-renderable; client directives belong only to official interactive primitives.
- Use semantic tokens and built-in variants; no manual dark color, theme branch, raw visual value, manual overlay z-index, or icon sizing override.
- Record one insufficiency finding and applicable/N/A states for each new primitive in the inventory input consumed by F05.
- Registry or dependency bytes unavailable after the required preflight is `BLOCKED`; do not hand-author an assumed API.
- Do not add `ImageUploader`, generated registry inventory, or any package not required by a reachable responsibility.

## Result

- **Status:** completed.
- **Commit/artifact:** atomic F01 commit on `task/12.2.3-rebuild-shared-visual-components`; `src/components/ui/primitive-inventory.test.tsx` is the focused executable inventory.
- **Files changed:** `package.json`, `pnpm-lock.yaml`; the 14 existing owned primitive sources (10 required reconciliation, while `alert`, `label`, `separator`, and `skeleton` remained byte-stable); nine new official primitive sources (`alert-dialog`, `avatar`, `dialog`, `empty`, `input-otp`, `pagination`, `sonner`, `switch`, `tabs`); focused inventory test; this result record.
- **Deviations:** the official Sonner dry-run proposed `next-themes`, but production deliberately omits it because the application owns a six-theme resolver and semantic token projection. Official raw visual values, manual dark branches, fixed overlay layers, undersized controls, and English-only labels were replaced by the established token, accessibility, and locale contracts. No registry file was overwritten.
- **Evidence:** official `shadcn@latest` `info --json` confirmed Next.js 16, RSC, Tailwind 4, `radix-nova`, Radix, and Lucide; official docs, aggregate dry-run, and all ten per-file diffs were inspected. `input-otp@1.4.2` and `sonner@2.0.7` are exact direct dependencies. Focused Vitest passed 8/8; `pnpm typecheck`, `pnpm lint` (0 errors; 58 pre-existing warnings), token check, 17-source/28-derivative brand check, 3-family/10-weight font provenance, and `git diff --check` passed. `pnpm frontend-parity:check` intentionally reports 16 authored-class and five interaction target/fixture bindings after the new `switch`, `input-otp`, and `pagination` paths appeared; F05 owns the required parity refresh and evidence binding, so F01 did not mutate those artifacts.

### New primitive insufficiency and state input for F05

| Primitive | Existing-inventory insufficiency | Applicable states | Explicitly N/A / delegated |
|---|---|---|---|
| `AlertDialog` | No owned destructive-confirmation surface with Radix focus containment and restoration. | closed/open, initial focus, cancel/action, destructive confirmation, pending/disabled action, retained-open failure | loading and empty are N/A; failure copy is composed by F03 |
| `Avatar` | No single image/monogram owner with deterministic fallback and size/group vocabulary. | image, fallback/error fallback, `sm`/default/`lg`, group, badge | loading, empty, hover/focus, and disabled are N/A for this display primitive |
| `Dialog` | No owned general modal with Radix focus containment, restoration, and localized close controls. | closed/open, initial focus, keyboard loop, permitted escape/overlay dismiss, optional header/footer close | loading and empty are N/A; pending/disabled/error content is composed by F03 |
| `Empty` | Existing alert/card sources cannot express a neutral empty result without conflating it with failure. | empty title, optional description, optional media, optional recovery/CTA content | loading, error, hover/focus, and disabled are N/A; CTA states belong to its Button |
| `InputOTP` | Plain `Input` cannot provide segmented OTP entry, active-slot caret, or OTP semantics. | empty, partially/fully populated, active/focus, invalid, disabled | loading and empty-result are N/A; descriptive validation copy belongs to `Field` |
| `Pagination` | No role-neutral, server-link-first cursor navigation vocabulary exists. | previous/next link, current link, hover/focus, localized nav/link labels, presentational ellipsis | loading/error are N/A; unavailable links are omitted or explained by F04 rather than fake-disabled |
| `Sonner` | `Alert` cannot provide stacked transient live feedback, dismissal, or optional recovery actions. | info, success, warning, destructive error, loading, action, cancel, close/dismiss | empty is N/A; retry and auto-dismiss policy are supplied by F03 callers |
| `Switch` | `Checkbox` does not express immediate boolean settings semantics or a track/thumb selected marker. | unchecked/checked, hover/focus, invalid, disabled | loading, empty, and request error are N/A; pending/error feedback belongs to the enclosing `Field`/form |
| `Tabs` | No owned keyboard tablist/panel primitive with a non-color active marker exists. | default, selected/active, hover/focus, disabled, horizontal/vertical, filled/line | loading/empty/error are N/A at primitive level and belong to each panel composition |
