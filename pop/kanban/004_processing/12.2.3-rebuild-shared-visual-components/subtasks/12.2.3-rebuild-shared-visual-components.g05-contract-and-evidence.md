# Front F05 — Contract, anti-drift and evidence — [[12.2.3-rebuild-shared-visual-components]]

- **Delivery:** bind the integrated inventory to executable anti-drift checks, synchronized contracts and fresh real-consumer evidence.
- **Scope:** independent audit/evidence and durable truth; no production component or route implementation.
- **Owner:** agent.
- **Owns:** `scripts/check-shared-ui-inventory.mjs`, matching test, `src/components/ui/inventory.json`, `src/components/ui/primitive-inventory.test.tsx`, `docs/frontend-template-parity/manifest.json`, `docs/frontend-template-parity/obligations.ndjson`, task-specific evidence scripts/tests, generated `artifacts/design-system/**`, `DESIGN.md`, `src/components/ui/AGENTS.md`, `src/data-directory/AGENTS.md`, `pop/specs/application-frontend-system.md`.
- **May read:** all integrated F01-F04 results, parity authority, current consumers, existing evidence runners/verifiers and superseded spec history.
- **Must not edit:** package/lock, any production TSX outside the inventory JSON, routes, shell, `/design-system` page/specimen, template, parity authority outside the two generated files and strict refresh below, token/brand/media/domain/server code, superseded spec.
- **Depends on:** F01-F04, including the integrated F01 pagination focus-ring re-entry delta.
- **Expected input:** complete integrated source inventory, focused test results and per-addition insufficiency/state records.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/sync-specs/SKILL|sync-specs]], [[.agents/skills/react-best-practices/SKILL|vercel-react-best-practices]].
- **Criteria:** C01-C09.

## Execution contract

- Map every 12.2.3 obligation once to a concrete production owner/disposition; enforce that all excluded generated files remain absent and dependency additions are closed.
- Update the F01-authored inventory test for the integrated tree so it validates primitive membership separately from the planned F02/F03 compositions instead of treating every UI TSX file as a primitive.
- Run the official checker with `--refresh-semantic-contract` only for the derived `currentComponents`, fixture and target bindings of the 187 task 12.2.3 obligations in the two owned generated parity files.
- Prove no obligation ID, source path, source hash, disposition, excluded count or global total of 2,230 changes; the refreshed checker plus mutation and summary gates must pass.
- Record owner/import path, public responsibility, applicable/N/A states and one-line existing-inventory insufficiency for every added source.
- Correct the spec/DESIGN contradiction that names excluded `ImageUploader`; keep `implementation: partial` and do not claim route or specimen conformance.
- Update both DOX contracts to the delivered inventory/client boundaries without restating domain behavior.
- After the F01 pagination delta and focused/typecheck gates pass, rerun current design-system evidence at exact integrated HEAD across six themes and four widths; bind fresh manifest/review and reject external requests, axe/focus/target/overflow or severity 2+ failures.
- Do not expand the route/specimen to display new inventory; record that complete component/state/locale specimen proof is owned by 12.2.4.
- Run focused tests, anti-drift mutations, parity check, full `pnpm check`, and diff/PoP validation.

## Result

- **Status:** complete.
- **Commit/artifact:** this atomic F05 commit; evidence run `20260803135032`, manifest
  `e89ae0a7209689ce55aa739706faf8a7dfac6b503cf254f34cf355247131827c`,
  bound to source HEAD `badc0c13a4bd00c0cc6f3fc40bcac1b23587c65d`.
- **Files changed:** executable shared inventory/checker and mutation tests;
  integrated primitive inventory test; generated parity manifest/obligations;
  current evidence test/verifier, pointer and 24-capture run; `DESIGN.md`, the
  two owned DOX contracts, and the active frontend spec.
- **Deviations:** three material integration gaps returned through 002 before
  execution resumed: aggregate primitive/composition test ownership, scoped
  semantic parity refresh, and pagination focus/44×44 browser contracts. The
  formal deltas are in this plan; F01 owns the production pagination fixes.
  Failed partial evidence runs were rejected and are not referenced as proof.
- **Evidence:** 187/187 obligations map once to 20 owners; 49 exclusions and 9
  focused additions are closed. The 2,230-record parity checker passes after
  invariant-guarded refresh (135 derived bindings changed only for 12.2.3), as
  do all 23 semantic mutation probes. Fresh 6-theme × 4-width evidence passes
  with no external request, serious/critical axe, focus, target, overflow,
  token, font, motion, or severity 2+ finding. `pnpm check` passes with 1,944
  tests, 28 skips, 51 built pages, and the unchanged 58-warning/0-error lint
  baseline; focused tests, evidence verifier, `git diff --check`, ownership and
  PoP validation pass.
