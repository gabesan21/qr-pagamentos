# Approval — [[9.3.2-complete-public-link-terminal-states]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read with reality checks against the merged tree and 9.3.1's approved plan:

- **Decision 1 (V2-only, V1 byte-frozen)** — confirmed feasible. Current `src/app/pay/[identifier]/page.tsx` renders only the V1 presentation with one opaque unavailable view; 9.3.1's approved plan adds the V2 branch and explicitly leaves consumed `SINGLE_USE` at the opaque view, reserving the explicit paid view for 9.3.2. The seam 9.3.2 needs exists by design.
- **Decision 2 (paid-view content boundary)** — sanctioned. Judged against the exposure surface: composition, exact total, and display currency were already public to any identifier holder while the link was active (`public-payment-link-v2.ts` exposes the full `{ composition, currencyPair }` superset publicly, pair UUIDs included — the paid view is *tighter*, excluding pair UUIDs). The only new disclosure is the single bit "this link was consumed", which the roadmap explicitly requires and which the reservation route already reveals implicitly (a consumed `SINGLE_USE` is rejected). The pinned never-expose list (order state, any timestamp including `claimedAt`, refund state, payer data, attempt/capability/verifier material, provider data, pair UUIDs, owner identity beyond branding, link internals) is exact and testable via absence assertions. Branding independent of the storefront-enabled flag follows 9.3.1's already-approved decision 5 precedent. The fallback minimal variant is recorded if this gate rejected it — it does not.
- **Decision 3 (claim-keyed, never state-keyed)** — confirmed against `prisma/schema.prisma`: `PaymentLinkV2SingleUseSettlement` persists independently of order state, and the spec pins claim-before-`CONFIRMED` atomic semantics with `CONFIRMED → REFUNDED` allowed afterward. Keying the paid branch solely to the claim row means a later `REFUNDED` never flips or annotates the view — correct, and the only choice that avoids leaking lifecycle. The plan correctly excludes `claimedAt` and `orderV2Id` from the DTO even though the claim row carries them.
- **Decision 4 (additive branch in 9.3.1's delivered presentation read)** — consistent with 9.3.1's plan (its F02 delivers `public-checkout-v2-presentation.ts` with the one-opaque-null outcome for consumed links; 9.3.2 upgrades that one case to the paid branch). Zero route/limiter/request-log edits is sound: the reservation already rejects claimed links, and the page is not a rate-limited API surface.
- **Decision 5 (evidence extends the delivered pair)** — verified `checkout:evidence` does not yet exist in `package.json` (9.3.1 creates it); extending it with no `package.json` change is correct given the temporal gate.
- **Decision 6 (temporal gate)** — pinned and necessary: every edited file (presentation service, page branch, checkout dictionaries, evidence spec) is created by 9.3.1, currently in 004.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result is concrete (branded localized paid view for consumed `SINGLE_USE` V2 links; all other non-payable states indistinguishable from a nonexistent link); seven acceptance criteria each name command, pass condition, and 005 mode.
2. **Sufficient lean brief — pass.** Objective, five pinned decisions, four fronts with owns/may-read/must-not-edit, risks with mitigations, abort conditions, specs/contracts with expected changes. No implementation snippets; executor binds to merged reality, not plan-guessed file names.
3. **Safe execution — pass.** Write sets disjoint from in-flight 10.2.1 and 9.2.2 (residual: append-only docs, rebase at 006); V1 render path byte-stable with orchestrator diff check (criterion 3); forbidden-path list explicit; no route/inventory/prisma edits; abort conditions for seam divergence and token-mechanism infeasibility instead of opportunistic workarounds.
4. **Proportional specs — pass.** Exactly one deliberate spec amendment (decision 2 content boundary + claim-keying) into `checkout-and-order-lifecycle.md`, a delivered-by note in `catalog-and-payment-links.md`, storefront spec explicitly read-only. F04 owns the five documents, nothing more.
5. **No avoidable `(user)` item — pass.** Card's only user gate ("Pronto para planejar") is checked; the plan schedules no human action.
6. **Proportional to risk/size — pass.** Critical M: four strictly sequential fronts, single executor, no fake parallelism; the disclosure decision is escalated to this gate with a recorded fallback — the right proportion of caution.
7. **Economic verification — pass.** Criteria 1, 2, 4, 5, 7 are re-run (`pnpm test`, evidence pair, `pnpm check`, `pnpm db:test`); 3 and 6 are evidence reads. No web.

#### Risk assessment

The 005 gate (always full for critical) must verify, with priority:

1. **Disclosure discipline (highest risk):** read the final paid DTO *and rendered markup* against decision 2's never-expose list — in particular that `claimedAt`, `orderV2Id`, and `ownerId` from the claim row never reach the DTO, and no settlement date or relative-time wording leaks through dictionary strings.
2. **Claim-keying proof:** the dedicated test must show REFUNDED-after-claim still renders paid, and a `SINGLE_USE` link with attempts but no claim still renders the normal checkout view (never paid-by-inference).
3. **V1 byte-stability:** `git diff --stat` vs task base shows zero diffs in the V1 render path, all routes, inventories, `src/orders/*`, `src/auth/*`, `prisma/`, `scripts/check-design-tokens.mjs`, `package.json`.
4. **Unavailable-set coherence:** after the change, "consumed `SINGLE_USE`" exits the opaque set while malformed/unknown/inactive/expired/line-product-unavailable stay in one indistinguishable view — confirm no status oracle was introduced (e.g., timing or markup difference between unknown and inactive).
5. **Temporal-gate compliance:** confirm 9.3.1 was actually merged to `develop` before 004 started and the executor bound to merged file names, not this plan's.
6. **Spec hygiene at F04:** `pop/specs/checkout-and-order-lifecycle.md` currently carries unresolved merge-conflict markers (`<<<<<<< HEAD` … `>>>>>>> task/9.1.3-...`) in its header in this working tree — confirm they are an artifact of the in-flight 9.1.3 integration and that the 9.3.2 amendment lands on a clean file without baking markers in.
7. **Whitelist/token discipline:** confirm the paid view needed no new `check-design-tokens.mjs` whitelist entry beyond 9.3.1's, and no inline style outside the sanctioned `--storefront-accent` mechanism.

aprovado por revisor independente (yolo) — 2026-07-26
