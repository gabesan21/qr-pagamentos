# 2.3.1-verify-and-handle-nautt-webhooks — Verification

## Summary

All acceptance criteria pass on critical-remediation commit `988a6e121e31ca2b43651abaa5286c007bcaeae3`. The 16-second lease now exceeds the 14.5-second accepted-work budget, held duplicates cannot issue an overlapping provider read, expired work is durably classified and fenced, and the production-equivalent capacity path is observed end to end.

## Criterion results

| # | Result | Command / evidence |
|---|--------|--------------------|
| 1 | PASS | `pnpm test -- src/integrations/nautt/webhook-signature.test.ts` exited 0. Exact bytes pass; whitespace mutation fails; malformed length/case/hex/comma grammar invokes `verifyWebhookOwner`, performs zero comparison calls, and zeroes every candidate buffer. Valid fixed-length signatures compare every candidate without early exit and reject ambiguity. The project wrapper ran `485 passed`, `3 skipped`. |
| 2 | PASS | `pnpm test -- src/integrations/nautt/webhook-intake.test.ts -t "authentication"` exited 0. Missing, malformed, forged, and ambiguous signatures return opaque `401`; parser/rejected-parser, delivery claim/bind/finalize, API-key decryption, provider fetch, and reconciliation spies remain at zero. Candidate cleanup is independently proved by criterion 1. |
| 3 | PASS | `pnpm db:test` printed `PASS webhook-delivery-runtime`, `PASS webhook-delivery-schema`, migration replay/idempotence, constraints, role separation, and runtime denials. PostgreSQL serialized one claim plus one busy duplicate, allowed reclaim only after 16 seconds, rejected stale finalization, cleared the final lease/processing owner, and persisted attempts 1/2/3 as `RETRYABLE`/`BUSY`/`PROCESSED` with provider-attempt and digest evidence intact. |
| 4 | PASS | `pnpm test -- src/app/api/nautt/webhooks/route.test.ts src/integrations/nautt/webhook-intake.test.ts` exited 0. Route tests prove exact chunks, early oversize cancellation, and empty no-store `204/400/401/413/503`. Intake tests cover malformed, duplicate, unknown/final, busy, retry, and no-op outcomes. A held first reconciliation remains active at 14,499 ms; its duplicate returns `503` with zero second GET. Reclaim occurs only at 16,001 ms, then attempt fencing makes the stale worker return `503`. |
| 5 | PASS | `pnpm test -- src/integrations/nautt/provider-order-reconciliation.test.ts src/integrations/nautt/webhook-intake.test.ts` exited 0. Unknown/final or wrong-owner lookup performs zero decrypt/GET; mismatched authoritative UUID performs zero CAS; stale/final state does not regress. Notification status `finished` is excluded while provider status `processing` alone reaches the shared CAS. |
| 6 | PASS | `pnpm test -- src/integrations/nautt/webhook-intake.test.ts -t "deadline\|capacity\|cost"` exited 0. The exercised path creates 1,000 AES-GCM ciphertexts, performs 1,000 real decryptions, 1,000 real HMAC-SHA256 plus `timingSafeEqual` comparisons, one injected 10-second provider advance, and claim/bind/finalize transitions as they execute. The deterministic operation timeline is exactly 12,600 ms, below 14,500 ms, with no post-request arithmetic constant. The 16,000 ms lease provides 1,500 ms of safety beyond that accepted budget. |
| 7 | PASS | `pnpm db:generate && pnpm db:contract-check` generated Prisma Client 7.8.0 and printed `PASS documentation-contract`. Manual review confirms the forward-only migration, closed constraints, composite owner/order FK, DML-only grants, and attempt-sequence usage. |
| 8 | PASS | `pnpm check` passed outside the sandbox so Next/Turbopack could create local workers. Typecheck, design-token lint/ESLint, `485 passed` / `3 skipped`, and production build passed; route inventory includes `/api/nautt/webhooks`. |
| 9 | PASS | `python3 pop/scripts/pop_validate.py --standalone && git diff --check` printed `standalone válido` and `Vault válido — nenhuma violação encontrada`, with no whitespace errors. Root, Prisma, and Nautt DOX state the safe-lease and malformed-signature cleanup contracts; stage-006 spec sync remains queued. |

## Critical findings resolved

1. `WEBHOOK_PROCESSING_LEASE_MS` is now 16,000 ms: the 14.5-second accepted window cannot overlap a reclaimed worker, while post-expiry crash recovery and attempt fencing remain executable.
2. The capacity gate no longer adds 10 seconds after completion. Real encrypted candidate loading/decryption and HMAC comparisons run inside intake, and injected time advances at each provider and persistence operation; disposable PostgreSQL separately proves the durable outcomes.
3. Every malformed signature grammar now exercises owner verification with a comparison spy, proves zero comparisons, and proves all provided secret buffers are cleared.

## Clean-code review

- **Behavior and security:** the safe lease prevents overlapping provider reads within the accepted request budget; expired claims preserve retryable evidence and stale workers cannot finalize. Raw bytes remain bounded and only the authoritative owner-bound GET reaches CAS.
- **Tests:** the timing model observes the same cryptographic operations as production and advances dependency time during execution. Intake and disposable-DB tests independently cover live exclusion, reclaim, fencing, and durable outcomes.
- **Complexity and coupling:** encrypted-candidate loading is isolated in a focused server-only factory with production defaults and narrow test seams. Lease constants express the accepted budget and safety margin directly; raw SQL remains confined to the serialized claim transaction.
- **DOX and consistency:** Prisma and Nautt contracts document the safe lease, expired-attempt evidence, and malformed-signature cleanup. No unrelated behavior or unnecessary child contract was introduced.

## Outcome

Task verification **passes** and is ready for a fresh critical yolo review. No blocking or non-blocking findings remain from this verification; the critic signature is intentionally unset.

### Yolo critic response

- [ ] Verified
