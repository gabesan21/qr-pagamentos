# Verification — [[7.3.1-build-merchant-profile-management]]

- **Stage:** 005_verifying
- **Mode:** full, mandatory for `critical: true` and after return to 004
- **Reviewer:** fresh strong independent yolo critic
- **Commit:** `d028919c85ad6cbc944623243552714f41a13e63`
- **Decision:** **APPROVE — round 2/2**

## Original request first

The delivery meets the original request. An active merchant has a distinct bilingual
profile workspace for username, nullable contact email, and password rotation.
Identity changes use own-row version CAS and opaque conflicts while preserving
sessions. Rotation proves the current password with exactly one instrumented
verification KDF for every authorized parsed attempt, serializes against sign-in
and administrator reset, atomically replaces the credential and revokes every
session, expires the current cookie, and retains username-only authentication.
Administrators and public projections gain no merchant identity capability.

The repaired browser path also meets the native interaction contract. It does not
call `preventDefault`, `fetch`, or `requestSubmit`; the submit observer changes
only presentational pending state, and the `formdata` observer disables the form
only after the browser has formed its payload. Chromium evidence proves
URL-encoded document-navigation POSTs, exact fields and profile CAS, click and
Enter coverage for both forms, one request, immediate pending feedback, cookie
expiry, complete session rejection, and old/new-password outcomes.

## Prior blocking findings

1. **Resolved — native POST interception.**
   `src/app/profile/profile-form.tsx` now observes native `submit` and `formdata`
   events without replacing browser navigation. The source gate and unit test
   reject `preventDefault`, `fetch`, and `requestSubmit`. Run
   `20260724082338` records document-resource navigation, URL-encoded payloads,
   exact identity/password fields, CAS, one request, and complementary
   click/Enter pending cases.

2. **Resolved — persisted-locale completion.**
   Successful rotation resolves the actor's validated persisted locale, sets the
   HttpOnly non-authentication `qr_locale` cookie beside the expired session
   cookie, and keeps the fixed `/login?password=changed` redirect. Login and root
   layout accept only `pt-BR`/`en`; screenshots and assertions prove matching
   visible copy and `<html lang>` with distinct hashes.

## Full verification evidence

- **AC1–AC4 security and concurrency:** 7 files, 49 tests passed. Inspection
  confirms origin → authorization → parse ordering, closed redirects/cookies,
  exact observed-credential CAS, one-KDF fallback paths, atomic all-session
  revocation, and shared per-user sign-in/reset/rotation locking.
- **AC5 compatibility:** 5 executable files, 33 tests passed. The sole
  `src/auth/sign-in.test.ts` collection failure is the unchanged repository
  `server-only` resolution baseline; username-only sign-in and public/store/order
  boundaries are otherwise covered by focused tests and source inspection.
- **AC6 UI and locale:** 6 files, 29 tests passed. Owned-component composition,
  fixed five-item navigation, finite notices, native pending semantics,
  dictionaries, locale-cookie validation, and bilingual login completion pass.
  The previously recorded pinned shadcn preflight remains source-drift-free.
- **AC7 rendered evidence:** `pnpm profile:evidence:verify` passed run
  `20260724082338`, binding exactly 50 PNGs and 53 run files to the closed source
  inventory. Manual inspection at 375, 768, and 1440 found no overlap, missing
  content, or hierarchy issue of severity 2 or greater. English and Portuguese
  completion captures render the correct distinct copy.
- **AC8 repository contracts:** migration policy/language/fixtures, database
  static contract, 105-path ownership check, `git diff --check`, lint,
  typecheck, production build, and standalone PoP validation passed. Lint has
  only the existing `order-view.ts` warnings. Disposable PostgreSQL replay
  passed `merchant-profile-schema`, then reproduced the unrelated pre-task
  malformed product-price fixture (`42601` where the fixture expects `23514`).
- **Full suite:** 116 files passed, 2 skipped, 873 tests passed, and 7 skipped.
  Its only failures are the unchanged `server-only` collection baseline and the
  pre-existing storefront test expecting English while default locale is
  Portuguese.

## UI review

- Nielsen visibility, feedback, consistency, error prevention, recovery, and
  minimalist composition pass for the profile workflow.
- WCAG evidence reports no serious/critical axe findings; labels, autocomplete,
  focus indication, 44px targets, 320px reflow, and announced notices/pending
  states pass.
- Final screenshots at 375, 768, and 1440 show no unresolved visual finding of
  severity 2 or greater.
- Known review limits remain touch hardware, motion timing, and exhaustive
  assistive-technology behavior; these do not contradict the delivered evidence.

## Findings and decision

No blocking findings, suggestions, or nits remain in the changed scope.

**APPROVED — round 2/2.**

verificado por revisor independente (yolo) — 2026-07-24
