# Verification — [[1.2.2-establish-local-identities]]

- **Stage:** 005_verifying · **Owner:** agent (+ user because `critical: true`)

> **Supersession:** Rounds 1 and 2 verified the former required-email identity contract. The human-approved username-first correction supersedes their verdicts; only the correction round below applies to the current baseline.

## Round 1 — 2026-07-14

| # | Criterion | Mode | Verification run / evidence audited | Result | Evidence |
|---|-----------|------|-------------------------------------|--------|----------|
| 1 | Every gate selects pinned tooling. | re-run | Exact plan command. | ✅ passed | Exact output was Node `v24.18.0` and pnpm `11.13.0`. |
| 2 | Migration enforces identities, immutable no-FK singleton locator, closed fields, credential relation, and least-grant DML without blocking ordinary user mutations. | re-run | `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH" pnpm db:contract-check && PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH" pnpm db:test` | ✅ passed | Static contract and disposable PostgreSQL replay passed identity constraints, immutable locator update/delete denials, allowed user lifecycle with tombstone, and runtime privilege denials. |
| 3 | Normal seed and helper-routed opt-in recovery use exact isolated secret graphs, immutable UUID targeting, and ordered startup. | re-run | Exact plan command: container contract check plus clean-clone `identity-seed` and `identity-recovery`. | ❌ failed | All commands exited 0 and proved startup, idempotence, UUID targeting, deleted-target abort, and redaction. However `pop/scripts/container-test.mjs:87,282` invokes two-file Compose directly; it never invokes `install/install.sh --recover-initial-admin` or its `compose()` helper, so the human-approved helper forwarding/interpolation path is not exercised. |
| 4 | Installer validates exact ASCII email and deterministic protected password/recovery files. | re-run | Exact `install/test.sh` exited 0; independent NUL fixture against `install/install.sh --dry-run --env-file <fixture>` exited 0. | ❌ failed | **Blocking:** `install/install.sh:36-47` reads an env value containing NUL as `admin@example.com`; Bash discards the NUL, so the installer succeeds instead of the plan-required abort. `install/test.sh:58-80` has no NUL regression and also does not execute successful candidate promotion/staged-candidate removal. |
| 5 | Exact email/password/scrypt vectors and all database-free gates pass once. | re-run | Exact `pnpm db:generate && pnpm check`. | ❌ failed | Generation/typecheck/lint/48 tests/build exited 0, but `src/auth/password.test.ts:21-30` does not cover every promised malformed-record class (complete field-count, parameter-value, and decoded salt/key length vectors), so the required unit observation is incomplete. |
| 6 | Evidence and root/Prisma DOX/operator/spec docs match reality. | evidence | Audited g04 notes, changed README/DOX/spec, tracked files, `git diff --check`, and immutable foundation migration hash. | ❌ failed | README, DOX, spec exclusions, tracked-artifact audit, and unchanged foundation migration align. The g03/g04 evidence nevertheless claims helper-routed recovery and complete exact-vector coverage that the harness/tests above do not demonstrate. No generated or secret artifact is tracked. |

### Review findings

- **Blocking — rejected NUL becomes accepted identity input:** `install/install.sh:36-47` cannot preserve/detect NUL while loading `INITIAL_ADMIN_EMAIL`; the value reaches validation without the forbidden byte. Impact: the explicit installer input contract is bypassed. Add a fail-closed raw env-file check and an executable NUL regression.
- **Blocking — approved recovery edge is not integration-tested:** `pop/scripts/container-test.mjs:87,282` bypasses the installer helper, while `install/test.sh:77-80` only checks manually printed dry-run text. Impact: regression of the exact Compose interpolation correction that required human intervention could pass every gate. Exercise the real helper-routed recovery path with disposable resources and assert promotion/removal behavior.
- **Blocking — evidence overstates vector coverage:** `src/auth/password.test.ts:21-30` and `install/test.sh:58-80` omit named plan vectors while g04 records complete coverage. Impact: criterion 4/5 regressions can remain invisible. Add the promised cases or narrow the plan only through a new approval.

**Verdict:** rejected → return to 004_processing to fix installer NUL rejection and complete the helper-path/vector evidence; no application behavior outside that approved scope is requested.

## Round 2 — 2026-07-14

| # | Criterion | Mode | Verification run / evidence audited | Result | Evidence |
|---|-----------|------|-------------------------------------|--------|----------|
| 1 | Every gate selects pinned tooling. | re-run | Exact plan version command. | ✅ passed | Exact output remained Node `v24.18.0` and pnpm `11.13.0`. |
| 2 | Migration enforces identities, immutable no-FK singleton locator, closed fields, credential relation, and least-grant DML without blocking ordinary user mutations. | evidence | Audited the passing Round 1 live DB run and correction range `c254ac6..ee8c5cf`. | ✅ passed | The correction changed no Prisma, migration, database harness, or runtime DB file; the immutable foundation migration hash remains `60cc00c150a32f27997b9fe6ad00ce618609df267e30727949caa6c8e138ff81`. |
| 3 | Normal seed and helper-routed opt-in recovery use exact isolated secret graphs, immutable UUID targeting, and ordered startup. | re-run + prior evidence | Exact `pnpm container:contract-check` and clean-clone `identity-recovery`; unchanged Round 1 clean-clone seed evidence audited. | ✅ passed | Recovery invoked the real installer/helper and passed all six helper values (`APP_PORT`, three base password-file paths, staged directory, recovery-file path), base interpolation, exact promotion/removal, failed retry retention, UUID targeting, deleted-target abort, and redaction. |
| 4 | Installer validates exact ASCII email and deterministic protected password/recovery files. | re-run | Exact `install/test.sh` plus live clean-clone recovery observations. | ✅ passed | Raw-byte NUL regression now fails closed before Bash parsing; installer tests passed. Live recovery proved mode-preserving exact candidate promotion, source/staged candidate removal after success, and source/staged retention after failure. |
| 5 | Exact email/password/scrypt vectors and all database-free gates pass once. | re-run | Sole post-correction `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH" pnpm check`. | ✅ passed | Prisma generation, typecheck, lint, 63 passed/1 skipped tests, and production build exited 0. Named malformed vectors now cover every field/parameter/order/decimal/padding/alphabet/canonicality/decoded-length/non-ASCII/trailing/parser-bound class without calling `scrypt`. |
| 6 | Evidence and root/Prisma DOX/operator/spec docs match reality. | evidence | Audited correction notes, full task diff, tracked files, ignore rules, `git diff --check`, and scope exclusions. | ✅ passed | Notes now distinguish unit installer evidence from live helper evidence; README/DOX/spec remain aligned. No generated client, local secret, reset/MFA/session/UI behavior, or unrelated artifact is tracked. |

### Clean-code review

- No blocking, suggestion, or nit findings remain in the correction range. Raw-byte validation is fail-closed and local to env loading; the Docker shim is test-only and observes paths without weakening the production secret boundary; the expanded table-driven vectors express the parser contract directly.

**Verdict:** agent verification approved → ready for required human verification; remain in 005_verifying with the human checkbox unchecked.

## Round 3 — Username-first correction verification — 2026-07-14

| # | Criterion | Mode | Verification run / evidence audited | Result | Evidence |
|---|-----------|------|-------------------------------------|--------|----------|
| 1 | Pinned tools and rewrite preconditions are executable without purging retained data. | re-run | Exact pinned version, ancestry, and non-mutating Docker-volume commands from Round 6. | ❌ failed | Node `v24.18.0`, pnpm `11.13.0`, and `PASS identity-migration-not-on-main` matched. The volume command instead printed `BLOCKED retained-old-identity-volume`; no purge or non-disposable apply was performed. |
| 2 | One rewritten baseline enforces required unique canonical username, nullable unique canonical email, unchanged credentials/roles/locator, and least privilege. | re-run | Exact `pnpm db:generate && pnpm db:contract-check && pnpm db:test`. | ✅ passed | Generation and static checks passed; disposable PostgreSQL passed replay/idempotence, username/email constraints, locator lifecycle, user DML, and privilege denials. Exactly one local-identity migration exists; its hash is `05249ddd65c3e746775b5e5d42195fdc693a2426d790a7ca1f26cec83f7445e2`, and the Phase 1.1 hash remains `60cc00c150a32f27997b9fe6ad00ce618609df267e30727949caa6c8e138ff81`. |
| 3 | Seed uses required username/optional email and recovery remains isolated, UUID/password-only, and identity-preserving. | re-run | Exact container contract plus clean-clone `identity-seed` and `identity-recovery` sequence. | ✅ passed | Pinned image/static contracts, absent/present email seed, invalid username abort, idempotence, no retarget, helper forwarding, candidate promotion/retention, UUID targeting, and deleted-target abort all passed. |
| 4 | Installer creates/stages the exact username, optional-email marker, password, and recovery files. | re-run | Exact `install/test.sh`. | ✅ passed | `PASS install-contract`; the audited tests cover username/email validation including NUL, bytes/modes, staging, reuse, promotion, redaction, and retention/purge flags. |
| 5 | DTO/identity primitives expose username and nullable email with no email login path; all database-free gates pass. | re-run | Exact `pnpm check`, immediately followed by the exact bounded `rg` command. | ❌ failed | Typecheck, lint, 83 tests (1 skipped), and build passed. The subsequent search matched `src/generated/prisma/**`, which `pnpm check` had just generated, and printed `BLOCKED email-login-path`. The approved command does not exclude ignored generated code or authorize deleting it between the two runs. |
| 6 | Docs/spec/roadmap/DOX/artifacts/evidence match the corrected baseline. | re-run + evidence | Exact diff, tracked/ignored artifact, four documentation-presence commands; audited all four subtask notes and the changed contracts. | ✅ passed | `git diff --check` was silent; both artifact checks and all four documentation/evidence checks printed their exact PASS lines. The g04 note discloses that executor evidence removed ignored generated code before its search, so it does not cure criterion 5's exact-sequence failure. |

### Clean-code review

- **Blocking — the no-email-login acceptance probe contradicts its producer gate:** `package.json` makes `pnpm check` generate `src/generated/prisma/**`, while criterion 5 immediately searches all `src/**/*.{ts,tsx,mjs}` and matches Prisma's generated examples. Impact: a correct username-first implementation cannot pass the approved exact sequence. Exclude `src/generated/**` in the bounded probe (or otherwise amend the approved verification contract) and rerun it without an interstitial cleanup.
- No additional blocking, suggestion, or nit finding was found in the username/nullable-email implementation, migration, seed/recovery, installer, DTOs, tests, or documentation. The focused and disposable gates exercise the promised behavior and security boundaries.

**Verdict:** rejected. Criterion 1 requires the human-owned retained volume to be resolved without verifier purge, and criterion 5 requires a corrected executable probe; remain in `005_verifying`/return through the orchestrator as appropriate. Human approval stays pending.

## Round 7 reverification — 2026-07-14

| # | Criterion | Mode | Result | Evidence |
|---|-----------|------|--------|----------|
| 1 | Pinned tools and rewrite preconditions are executable without purging retained data. | re-run | ❌ blocked | Node `v24.18.0`, pnpm `11.13.0`, ancestry, and Docker daemon access passed; the exact non-mutating check still printed `BLOCKED retained-old-identity-volume`. No purge or apply was performed. |
| 2 | Rewritten username/nullable-email baseline and database privileges hold. | carried forward | ✅ passed | No implementation, Prisma, migration, or database-harness file changed since Round 3; its exact disposable database evidence remains applicable. |
| 3 | Username-first seed and UUID/password-only recovery hold. | carried forward | ✅ passed | No container, Compose, installer, or recovery implementation changed since Round 3; its exact clean-clone evidence remains applicable. |
| 4 | Installer identity/password/recovery file contract holds. | carried forward | ✅ passed | No installer implementation or test changed since Round 3; `PASS install-contract` remains applicable. |
| 5 | Database-free gates pass and authored code contains no email-login path. | re-run | ✅ passed | Exact pinned `pnpm check` passed typecheck, lint, 83 tests with 1 skipped, and production build. With generated Prisma retained, the immediately following approved probe excluding only `src/generated/prisma/**` emitted exactly `PASS no-email-login-path`. |
| 6 | Docs/spec/roadmap/DOX/artifacts/evidence match the corrected baseline. | re-run + evidence | ✅ passed | `git diff --check` was silent; generated Prisma remained present and ignored; tracked/ignored artifact checks and all four documentation/evidence commands emitted their exact PASS lines. Round 7 executor evidence accurately records the same immediate sequence. |

### Round 7 clean-code review

- No blocking, suggestion, or nit finding remains in the Round 7 correction. The probe excludes only ignored generated Prisma output while continuing to scan every authored TypeScript/MJS file under `src` and `container`; this directly resolves Round 3's false positive without weakening the username-only login contract.

**Verdict:** criterion 5 correction approved, but overall agent verification remains blocked on criterion 1's human-owned retained volume. Stay in `005_verifying`; human approval remains unchecked.

## Round 8 — Uninstall identity hotfix verification — 2026-07-14

| Scope | Mode | Result | Evidence |
|-------|------|--------|----------|
| Default and `--purge-data` uninstall do not require initial-admin username/email. | re-run + review | ✅ passed | `install/test.sh` passed both modes using a temporary env file with both identity variables removed. Every uninstall invocation in the test uses `--dry-run`; the harness creates files only under its temporary directory, so it neither invokes Compose nor removes source/staged secrets or a real volume. `install/uninstall.sh` now requires only the four values forwarded to Compose down. |
| Installation still requires a valid username. | re-run + review | ✅ passed | `install/test.sh` retained missing/blank username rejection and invalid username vectors. The production non-dry-run path still sends the required username through `prepare-identity-secrets.mjs`; the hotfix did not change install validation. |
| Focused, aggregate, documentation, and smallest-fix quality gates hold. | re-run + review | ✅ passed | Node `v24.18.0`, pnpm `11.13.0`, `bash -n install/{install,uninstall,test}.sh`, `install/test.sh`, `pnpm check`, and `git diff --check 2f89fa2..HEAD` passed; 83 tests passed and 1 was skipped, with typecheck, lint, and build clean. The implementation is a one-item required-key deletion plus two focused regressions; README accurately distinguishes uninstall from creation-time identity inputs. No blocking, suggestion, or nit finding. |
| Existing retained-volume prerequisite. | read-only re-run | ❌ blocked | Exact `docker volume inspect qr-pagamentos_postgres-data` precondition still printed `BLOCKED retained-old-identity-volume`. No purge, Compose down, migration apply, or other volume mutation was performed. |

**Verdict:** uninstall hotfix approved with no clean-code findings. Overall task verification remains blocked only on criterion 1's human-owned retained volume; stay in `005_verifying` and keep human approval unchecked.

## Human approval (critical task)

### Human response

_Pending after agent verification passes._

- [ ] Done
