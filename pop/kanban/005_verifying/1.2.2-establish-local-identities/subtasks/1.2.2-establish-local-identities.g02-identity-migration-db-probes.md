# Group g02 — Immutable identity migration and DB probes — [[1.2.2-establish-local-identities]]

- **Order:** 2.
- **Depends on:** g01.
- **Description:** Persist identity invariants through a new immutable migration and executable least-privilege probes.

## Movements

- [x] (agent) **g02.m1** Add `User`, one-to-one `PasswordCredential`, and `DeploymentBootstrap(id=1, initialAdminUserId UUID, createdAt)` with no FK; new immutable migration checks singleton key and triggers rejection of every locator update/delete, never user mutation/deletion; never alter foundation history.
  - Expected: exactly one immutable locator can exist, its UUID cannot retarget, ordinary user disable/demotion/deletion succeeds leaving the audit UUID, other identity constraints hold, and Prisma generation succeeds.
  - If it fails: Prisma cannot declare a required check → apply the pre-authorized migration-SQL fork, keep the Prisma model representative, and assert the named constraint statically/live.
- [x] (agent) **g02.m2** Extend migration grants so `qr_runtime` has only demonstrated identity table DML and sequence use, with no DDL/ownership/migration-table/role privileges.
  - Expected: bootstrap remains the sole role/grant owner; migration applies grants to new objects without broad default privileges.
  - If it fails: runtime cannot perform a required identity operation → identify the exact missing table/sequence privilege and grant only it; any broader capability aborts.
- [x] (agent) **g02.m3** Probe replay, identity constraints, one locator only, locator update/delete denial, allowed user disable/demotion/delete with retained UUID, dangling recovery lookup, runtime DML, and forbidden DDL/role/migration access.
  - Expected: exact-PATH DB gates pass; locator never retargets, user lifecycle is not constrained here, and deleted UUID produces no recovery target.
  - If it fails: fixture cleanup/order masks a constraint or privilege → isolate each probe in deterministic transactions and repair the migration/harness, never weaken the invariant.

## Execution notes

- 2026-07-14 — Record migration directory, generated-client result, static output, disposable DB container identifier, and positive/denial summaries here.
- 2026-07-14 — Added `prisma/migrations/20260714190000_local_identities/` without touching foundation history; Prisma generation and static contract passed. Elevated disposable `qr-pagamentos-db-test-*` completed replay/idempotence, identity/FK/hash checks, immutable singleton update/delete denials, allowed user demotion/disable/delete with unresolved UUID tombstone, least-grant DML, and DDL/metadata/role denials; container was removed by the harness.
