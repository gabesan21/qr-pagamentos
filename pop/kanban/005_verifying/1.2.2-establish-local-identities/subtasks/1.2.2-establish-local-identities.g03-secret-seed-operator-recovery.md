# Group g03 — Username-first seed and preserved recovery — [[1.2.2-establish-local-identities]]

- **Order:** 3
- **Depends on:** g01, g02
- **Description:** Carry required username and optional email through protected files while preserving UUID-only password recovery.

## Movements

- [x] (agent) **g03.m1** Require `INITIAL_ADMIN_USERNAME`, allow absent/blank `INITIAL_ADMIN_EMAIL`, and write exact mode-0600 source files: canonical username+LF, optional canonical email+LF or zero-byte absence marker, unchanged generated password.
  - Expected: installer/env example/tests cover missing/blank/invalid username; absent/present/invalid/NUL email; exact paths, bytes, modes, reuse, and redaction.
  - If it fails: generic secret reading rejects the empty marker → use a seed-local optional identity reader; never weaken password/database secret reads or invent an email.
- [x] (agent) **g03.m2** Stage username/email/password files at mode 0400, mount exact base Compose targets, and seed required username plus SQL NULL/non-NULL email after migration.
  - Expected: fresh seed with absent and present email succeeds; collision/invalid username fails; rerun resolves UUID and mutates neither identifier.
  - If it fails: Compose requires identity values in env/argv or an optional mount → retain the always-present zero-byte file; secret-value transport outside files aborts.
- [x] (agent) **g03.m3** Preserve helper-routed recovery, candidate promotion/retry, role/status restoration, UUID-only targeting, and no username/email mutation; update clean-clone harness fixtures/assertions.
  - Expected: recovery observes all six helper path variables, rotates only password/restores role/status, and byte-compares username/email before/after.
  - If it fails: recovery needs identifier lookup or replacement user creation → abort; never select by username/email or retarget the locator.
- [x] (agent) **g03.m4** Replace static installer/container contracts and leakage searches for renamed files and nullable email; remove obsolete required-email assumptions.
  - Expected: base config requires username/password files, tolerates zero-byte email, normal startup never needs recovery input, and no email-login fallback appears.
  - If it fails: stale email-first string is documentation/evidence only → update it; executable email-as-login or required-email behavior returns to the owning movement.
- [x] (agent) **g03.m5** Decouple default and purge uninstall validation from install-only initial-administrator identity inputs.
  - Expected: both uninstall modes accept the deployment environment without `INITIAL_ADMIN_USERNAME` or `INITIAL_ADMIN_EMAIL`, while installation still rejects a missing or blank username.
  - If it fails: keep deployment values required by Compose `down`; never make deletion depend on creation-time identity values or files.

## Execution notes

- 2026-07-14 — Previous email-required seed evidence is superseded; preserve only password/recovery behavior after re-proving it with username and nullable-email fixtures.
- 2026-07-14 — Installer, clean-clone seed, and clean-clone recovery passed required username, nullable email, file modes, idempotence, helper forwarding, candidate lifecycle, UUID targeting, and no identity mutation.
- 2026-07-14 — Hotfix regression failed first with `required variable is missing: INITIAL_ADMIN_USERNAME`; after limiting uninstall requirements to its Compose-down inputs, `install/test.sh` passed both default and `--purge-data` without username/email while retaining install-side username rejection. Shell syntax/diff checks and `pnpm check` also passed (83 tests passed, 1 skipped; typecheck/lint/build passed).
