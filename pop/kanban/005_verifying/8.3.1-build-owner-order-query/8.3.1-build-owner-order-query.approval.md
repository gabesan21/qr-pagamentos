# Approval — [[8.3.1-build-owner-order-query]]

## Round 1 — 2026-07-25

### Resposta do crítico (yolo)

**Verdict: APPROVE.**

Adversarial reading of card + plan against the V2 spec
([[specs/checkout-and-order-lifecycle|Checkout and order lifecycle]]), the
data-directory foundation source (`src/data-directory/server/`), the
administrative-foundation spec, memory 6.3.1, and Epoch 8 roadmap:

- **Verifiable objective:** criteria 1–2 are executable (`pnpm check`; focused
  vitest for canonical `307`, zero-I/O invalid, cursor staleness, keyset both
  directions, `pageSize + 1`, every filter/`q` mode, cross-owner opacity);
  criteria 3–4 are objectively inspectable. At least one `re-run` criterion
  exists, as required for runtime tasks.
- **Fidelity to card and spec:** `q` (UUID → local `order_v2.id` /
  `provider_order.provider_order_uuid`; text → payer name/email/cpf), exact
  `money` USD|FIAT, `from`/`to` calendar-day bounds, `source` LINK|AD_HOC, and
  `link` fact match the card's "scoped pagination and search, USD/FIAT, date,
  payer, provider/local ID, and source filters with default 20" and the spec's
  index strategy ("Remaining 8.3.1 filters (date bounds, payer text,
  provider/local UUID equality) compose over the owner-scoped keyset indexes
  without new design"). Filter count (5) is within the foundation's eight-filter
  cap. Order `(created_at desc, id desc)` ending in `UNIQUE_IMMUTABLE_ID`
  matches the foundation contract.
- **Foundation change is coherent and safe:** the current pin is real
  (`query-contract.ts:5-6` `[25,50,100]`/default 25; `cursor.ts:94` envelope
  check; administrative-foundation spec line "page sizes 25/50/100"). F01 keeps
  the global closed set a superset (10/20/25/50/100) and moves the default to
  per-directory registration; the spec line sync is in the same delivery. The
  one existing consumer (`src/app/(merchant)/catalog/`) accepts 25/50/100 with
  default 25, all preserved by the superset design; its pinned test
  (`directory-query.test.ts` expecting default 25) plus criterion 1 force any
  behavioral drift to surface.
- **Boundaries respected:** no mutations (`order-v2.ts` /
  `order-engagement-v2.ts` untouched, 8.3.2 owns), no rendering/dictionary/UI
  (8.3.3 owns), no admin global variant, frozen V1 (`src/app/orders/**`,
  `order-view.ts`) untouched, no schema/migration work (abort condition if a
  filter ever demands one).
- **Risks covered:** page-size ripple into cursor/spec mitigated by superset +
  same-delivery spec sync; payer-text scan bounded by owner-scoped predicates;
  two explicit abort conditions (new index need; no durable pair→code
  resolution). The `RECON NEEDED` on `exchange_currency_uuid` → USD/FIAT
  mapping is a legitimate recorded uncertainty with a concrete check target,
  not a hidden decision.
- **No hidden decision, no `(user)` item, specs proportional** (two linked
  spec-line syncs), topology sound (single executor, F01 → F02 sequential).

**Residual risks for 005 to verify (non-blocking):**

1. F01 must not silently change the catalog directory's default (25) or its
   accepted set; per-directory registration must leave unregistered/existing
   consumers on the global default, and any intentional change must land in the
   spec sync (criterion 4).
2. The USD/FIAT server-side mapping must resolve through durable
   currency-pair registry data, never URL-supplied UUIDs/codes; if the RECON
   finds no durable read path, the executor must extend the spec coherently or
   abort per the plan, not improvise.
3. Cursor compatibility: the extended envelope validation must keep accepting
   only registered per-directory sizes; no weakening of the cursor binding.

aprovado por revisor independente (yolo) — 2026-07-25

### Resposta do humano

_(pendente)_
