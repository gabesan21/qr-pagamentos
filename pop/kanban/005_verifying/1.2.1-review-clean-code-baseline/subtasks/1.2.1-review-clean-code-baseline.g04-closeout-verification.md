# Group g04 — Closeout and verification — [[1.2.1-review-clean-code-baseline]]

- **Order:** 3.
- **Depends on:** g02, g03.
- **Description:** Consolidate the behavior-preserving diff, close documentation contracts, and produce acceptance evidence.

## Movements

- [x] (agent) From the worktree root, set `WORKTREE_ROOT=$(git rev-parse --show-toplevel)` and `TASK_DIR="$WORKTREE_ROOT/pop/kanban/004_processing/1.2.1-review-clean-code-baseline"`; require `test -d "$TASK_DIR/subtasks"`. Run `! rg -n '\| pending \|' "$TASK_DIR/subtasks/1.2.1-review-clean-code-baseline.g02-application-database.md" "$TASK_DIR/subtasks/1.2.1-review-clean-code-baseline.g03-deployment-installer.md"`, then copy the finalized rows into the consolidated table below in exact g02 table order followed by exact g03 table order. Run `test "$(awk '/^\| / && $0 !~ /Surface|---/ {count++} END {print count}' "$TASK_DIR/subtasks/1.2.1-review-clean-code-baseline.g04-closeout-verification.md")" -eq 9` and `! rg -n '\| pending \|' "$TASK_DIR/subtasks/1.2.1-review-clean-code-baseline.g04-closeout-verification.md"`; review the combined diff against that ledger for one baseline intent.
  - Expected: path check exits 0; consolidated ledger has exactly nine unique rows, no `pending` cell, and no unexplained file/hunk; larger debt is recorded rather than implemented.
  - If it fails: mixed intent or behavior change → reviewability/spec risk → split out task-sized work and abort if the remaining diff cannot preserve the approved contract.
- [x] (agent) Re-walk the root and applicable child AGENTS contracts for every changed path; update only contracts whose purpose, responsibility, structure, flow, inputs, outputs, or quality rules changed, then review parent/related indexes.
  - Expected: local context remains sufficient, hashes/links are valid, and unchanged contracts receive no cosmetic churn.
  - If it fails: contract cannot describe the resulting tree concisely or conflicts with a parent → DOX closeout failure → correct the contract or abort for redesign.
- [x] (agent) Apply `sync-specs`: compare reality with [[specs/administrative-foundation]], record any divergence in its `Open` section and card Notes rather than silently rewriting it; otherwise leave requirements unchanged for 006 to add the implemented slice.
  - Expected: the spec remains truthful and `aprovada`, with any divergence explicit.
  - If it fails: a material requirement change is needed → plan invalidation → return to 002; do not continue verification.
- [x] (agent) Run acceptance criteria 1–3 and 6, capture concise outputs in these Notes, and correct only regressions attributable to an approved remediation.
  - Expected: `pnpm check`, database/static container/installer contracts, and standalone PoP validation all exit 0 with expected PASS markers.
  - If it fails: attributable regression → return to the owning transformation; environment/unattributed failure → abort with captured evidence.
- [x] (agent) Commit the complete task diff, then run criterion 4's seven `--clean-clone` container scenarios, capture per-scenario result plus cleanup evidence, and commit that evidence-only Notes update before the final range audit.
  - Expected: clean HEAD guard passes, all scenarios exit 0, no labeled disposable resource remains, and the evidence commit leaves a clean worktree.
  - If it fails: scenario regression → return to owning transformation; cleanup/isolation uncertainty → stop immediately without touching non-task Docker resources.
- [x] (agent) Recompute `WORKTREE_ROOT=$(git rev-parse --show-toplevel)` and `TASK_DIR="$WORKTREE_ROOT/pop/kanban/004_processing/1.2.1-review-clean-code-baseline"`; require `test -d "$TASK_DIR/subtasks"`. Load `TASK_BASE=$(awk -F= '/^[- ]*TASK_BASE=/{print $2}' "$TASK_DIR/subtasks/1.2.1-review-clean-code-baseline.g01-baseline.md")`; run `printf '%s\n' "$TASK_BASE" | grep -Eq '^[0-9a-f]{40}$'` and `git merge-base --is-ancestor "$TASK_BASE" HEAD`. From `WORKTREE_ROOT`, run `git diff --check "$TASK_BASE"...HEAD`, `git diff --stat "$TASK_BASE"...HEAD`, `git log --oneline "$TASK_BASE"..HEAD`, and `git status --short`. Pipe `git diff --name-only "$TASK_BASE"...HEAD` through `awk '!/^(AGENTS\.md|README\.md|Dockerfile|compose\.yaml|\.env(\.compose)?\.example|src\/|prisma\/|container\/|install\/|pop\/scripts\/(db-(contract-check|test)\.mjs|db-runtime\.test\.ts|container-(contract-check|prepare-secrets|test)\.mjs)|pop\/specs\/administrative-foundation\.md|pop\/roadmap\/1-administrative-foundation\.md|pop\/kanban\/[^/]+\/1\.2\.1-review-clean-code-baseline\/)/ { print "UNPLANNED " $0; bad=1 } END { exit bad }'`.
  - Expected: base validation and whitespace/allowlist commands exit 0; the full range and commit log are captured; status is empty; every allowed changed file/hunk maps to the completed ledger, a protecting test, a required spec/DOX update, or a task-harness transition.
  - If it fails: invalid base, unplanned path, dirty/unexplained state, or missing evidence → close only task-owned gaps; base corruption or foreign/unplanned work aborts and returns to planning if scope must expand.

## Execution notes

- 2026-07-14 — Full clean-clone scenarios intentionally run only after a task commit because the harness requires a clean committed HEAD.
- 2026-07-14 — Consolidation check found exactly nine finalized rows in required g02-then-g03 order and no unexplained remediation hunk.
- 2026-07-14 — DOX closeout: root `AGENTS.md` remains sufficient for the changed installer/harness paths; no responsibility, structure, flow boundary, input/output contract, index, link, or hash changed, so cosmetic DOX churn was avoided. `prisma/AGENTS.md` was re-read for the reviewed but unchanged database subtree.
- 2026-07-14 — Spec sync: the implementation preserves every mounted administrative-foundation requirement; no `Open` divergence or requirement rewrite is needed in 004. The reviewed/remediated slice remains a 006 update.
- 2026-07-14 — Acceptance 1–3 and 6: `pnpm check` passed (4 tests, 1 skipped, production build); database contract and disposable test printed all replay/CRUD/constraint/separation/denial PASS markers; container digest/image contract and installer contract passed; standalone PoP validation exited 0 with no violations (two expected transition warnings: the legacy release-label wording and the not-yet-created 005 verification artifact).
- 2026-07-14 — Pre-scenario task commit: `0b7a6a4` (`refactor: review clean-code baseline`).
- 2026-07-14 — Clean-clone matrix: `build` passed context exclusion and all three non-root image contracts; `config` passed source/staged permissions and Compose ordering; `happy` passed build/bootstrap/migration/preflight/liveness/assets; `roles` passed separation; `failures` passed escaping, encoding, and all three fail-closed probes; `lifecycle` passed migration no-op, runtime preflight restart, no job rerun, persistent restart, and cleanup; `isolation` passed initialization identity, secret/log/port isolation. All seven commands exited 0.
- 2026-07-14 — Post-matrix Docker inventory found no `qrct*` Compose project among containers, networks, or volumes (`PASS no-qrct-containers-networks-volumes`).
- 2026-07-14 — Full-range audit from `d791b49cedeb8f33d94fff61131fd0274b907379` passed ancestry, whitespace, status, nine-row cohesion, and the approved path allowlist; the range contained only the implementation commit and evidence commit before this final audit record.

### Consolidated ledger (owned by g04; g02 rows, then g03 rows)

| Surface | Evidence | Disposition |
|---------|----------|-------------|
| `src/app/` | Closed locale routing and exact health characterization remain clear and protected. | no issue |
| `src/i18n/` | Closed locale typing plus compile-time and runtime dictionary parity remain explicit. | no issue |
| `src/db/` | Lazy runtime client and migration/runtime URL separation remain explicit and statically protected. | no issue |
| `prisma/` | Fixture-only schema, immutable constraints, ownership, grants, and denials match the child contract. | no issue |
| `pop/scripts/db-*` | Disposable replay, CRUD, constraints, ownership, denial, and scoped-cleanup evidence is complete. | no issue |
| `container/` | Direct-child lifecycle, preflight, URL separation, secret handling, and redacted failures remain clear. | no issue |
| `Dockerfile` + `compose.yaml` | Pinned, non-root, ordered, read-only, isolated production topology matches the spec. | no issue |
| `install/` + env examples + `README.md` | Aligned uninstall parsing/port validation and replaced undeclared host `curl` with the existing exact in-container healthcheck; regression-covered. | local remediation |
| `pop/scripts/container-*` | Removed redundant pre-start secret staging while preserving unique project/temp provenance, failure restaging, and scoped cleanup. | local remediation |
