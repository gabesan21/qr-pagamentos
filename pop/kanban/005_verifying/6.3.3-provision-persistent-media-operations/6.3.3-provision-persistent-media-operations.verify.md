# Verification — [[6.3.3-provision-persistent-media-operations]]

- **Stage:** 005_verifying
- **Mode:** full, mandatory because the task is critical and has two prior 005 returns
- **Reviewer:** fresh strong independent yolo critic
- **Worktree tip:** `69c3939ec84c1165a8920f5492b156ee7877dcb0`
- **Decision:** **REJECT — third 005 failure; circuit breaker requires human intervention**

## Original request first

The delivery now proves the dedicated non-root media volume through install,
retention, backup, exact-release restore, recovery, and paired purge. The four
prior round-two defects are materially repaired: the backup manifest binds
both exact images; restore resolves and revision-checks both; disposable
secrets are UID/GID `1000:1000`, mode `0400` in a labeled volume even under
the fixture's operator-UID-2001 simulation; inventory is streamed without a
host residual; and double-failure recovery passes exact migration, metadata,
identity-seed, and health proof before restart.

The original request is nevertheless not complete because the required update
rollback rehearsal is red. The task's own target-health fault does not produce
the required sealed target-health rollback result, so the promised full update
failure envelope and exact previous-image health recovery are not reproducibly
proved.

## Blocking finding

1. **Blocking — the mandatory update scenario fails its target-health rollback
   evidence gate.** `pop/scripts/container-test.mjs:919-939` publishes a
   target whose runtime throws, requires one protected evidence file containing
   `result=target-health-failed-previous-image-healthy`, and only then proves
   the exact previous image and healthy state. A fresh
   `pnpm container:test --clean-clone --scenario update` instead terminates at
   line 935 with `Error: target rollback evidence missing`. The implementation
   can classify a failed `compose up --force-recreate` as `target-recreate`
   before reaching the explicit health branch
   (`install/update.sh:401-407`), so this injection does not deterministically
   exercise or prove the promised target-health rollback. The same scenario
   therefore never reaches its complete update PASS inventory or final
   evidence-count assertion. This contradicts AC4 and the release ledger's
   PASS claim in `docs/release-evidence.md`.

## Independent evidence

| Check | Result |
|---|---|
| `pnpm container:test --clean-clone --scenario update` | **FAIL** after approximately eight minutes — `target rollback evidence missing` at fixture line 935 |
| `... --scenario media-restore` | PASS — all five markers, including adversaries, UID-2001 staging, exact dual images, automatic recovery, and double failure |
| `... --scenario media` | PASS — all 13 topology/startup/persistence/cleanup markers |
| `... --scenario install-lifecycle` | PASS — retention, credential continuity, zero-row adoption, paired purge |
| `... --scenario media-backup` | PASS — atomic paired capture |
| `pnpm container:contract-check -- --local-pins` | PASS |
| `install/test.sh` | PASS |
| decoder preflight and focused media/preflight/reconciliation Vitest | PASS — Sharp `0.34.5`; 28 tests |
| `pnpm typecheck` | PASS |
| `pnpm lint` | PASS with the 11 unchanged `order-view.ts` warnings |
| `pnpm build` | PASS outside the sandbox; the first sandbox run only denied Turbopack's temporary port |
| `pnpm test` / aggregate baseline | 847 passed, 9 skipped; only the unchanged `server-only` resolution and storefront-locale failures |
| scope, diff, and PoP validation | PASS — 27 owned paths, clean committed worktree, standalone vault valid |

Source inspection also confirms the final repair pins the previous image before
candidate build and routes candidate/preflight/promotion/proof failures through
sealed handlers. That inspection cannot replace the red mandatory runtime
rehearsal.

## Decision and circuit consequence

Reject. This is the **third 005 rejection**. Set `circuit_breaker: true` and
stop automatic yolo progression; do not return to 004 or integrate the task
without explicit human intervention.
