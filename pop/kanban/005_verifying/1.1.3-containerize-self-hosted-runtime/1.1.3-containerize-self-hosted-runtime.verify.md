# Verification — [[1.1.3-containerize-self-hosted-runtime]]

- **Stage:** 005_verifying · **Owner:** agent (+ user because `critical: true`)
- **Current verified HEAD:** `688c5a957e856894e6b0920897001807f8d81637`
- **Current merge-base:** `d544b45d309333976ee5919f9a9333220f122535` (`origin/develop`)

## Prior rounds — compacted 2026-07-14

Detailed evidence remains recoverable from this file's Git history at the listed commits; obsolete and superseded prose was compacted to keep the required task artifact within the PoP 150-line contract.

| Round | HEAD | Outcome |
|-------|------|---------|
| 1 | `f2e9656` | C1-C7 passed; C8 rejected symbolic `HEAD` in the committed range. |
| 2 | `796b541` | Range repair worked; live Node digest drift rejected C1/C8. |
| 3 | `2152c5c` | C1-C8 passed; human critical gate remained pending. |
| 4 | `b4a2181` | C1-C9 passed for the first installer, later superseded by the user-directed installer redesign. |
| 5 | `d5fa447` | Docker-context behavior passed, but the regression assertion was non-causal and moved `scripts/*` paths caused `MODULE_NOT_FOUND`; returned to 004. |

## Human critical verification

Do not perform or check this gate until a fresh automated round passes. Then, from the task worktree, use only disposable files and resources:

```sh
export VERIFY_DIR="$(mktemp -d)"
export COMPOSE_PROJECT_NAME="qr-human-113-$(date +%s)"
export APP_PORT=33013
umask 077
openssl rand -base64 48 > "$VERIFY_DIR/postgres-admin-password"
openssl rand -base64 48 > "$VERIFY_DIR/qr-migrator-password"
openssl rand -base64 48 > "$VERIFY_DIR/qr-runtime-password"
chmod 0600 "$VERIFY_DIR"/*
printf 'APP_PORT=%s\nPOSTGRES_ADMIN_PASSWORD_FILE=%s\nMIGRATOR_PASSWORD_FILE=%s\nRUNTIME_PASSWORD_FILE=%s\nSTAGED_SECRETS_DIR=%s\n' \
  "$APP_PORT" \
  "$VERIFY_DIR/postgres-admin-password" \
  "$VERIFY_DIR/qr-migrator-password" \
  "$VERIFY_DIR/qr-runtime-password" \
  "$VERIFY_DIR/staged" > .env.compose
chmod 0600 .env.compose
pnpm container:prepare-secrets -- --env-file .env.compose
docker compose --env-file .env.compose build --pull
docker compose --env-file .env.compose up -d
docker compose --env-file .env.compose ps
docker compose --env-file .env.compose logs --no-color bootstrap migrate app
```

Wait for liveness, then verify both locales and the exact health response:

```sh
until curl -fsS "http://127.0.0.1:${APP_PORT}/api/health" >/dev/null; do sleep 1; done
curl -fsS "http://127.0.0.1:${APP_PORT}/pt-BR" >/dev/null
curl -fsS "http://127.0.0.1:${APP_PORT}/en" >/dev/null
test "$(curl -fsS "http://127.0.0.1:${APP_PORT}/api/health")" = '{"status":"ok"}'
```

Verify successful one-shot jobs, non-root users, and loopback-only publication:

```sh
BOOTSTRAP_ID=$(docker compose --env-file .env.compose ps -a -q bootstrap)
MIGRATE_ID=$(docker compose --env-file .env.compose ps -a -q migrate)
test "$(docker inspect --format '{{.State.ExitCode}}' "$BOOTSTRAP_ID")" = 0
test "$(docker inspect --format '{{.State.ExitCode}}' "$MIGRATE_ID")" = 0
test "$(docker compose --env-file .env.compose exec -T app id -u)" = 1000
docker compose --env-file .env.compose exec -T db sh -eu -c 'uid=$(awk "/^Uid:/{print \$2}" /proc/1/status); test "$uid" -gt 0'
test "$(docker compose --env-file .env.compose port app 3000)" = "127.0.0.1:${APP_PORT}"
test -z "$(docker compose --env-file .env.compose port db 5432 2>/dev/null || true)"
```

Prove an app-only restart repeats runtime preflight without changing either job:

```sh
JOBS_BEFORE=$(docker inspect --format '{{.Id}}|{{.State.ExitCode}}|{{.State.StartedAt}}|{{.State.FinishedAt}}' "$BOOTSTRAP_ID" "$MIGRATE_ID")
PREFLIGHT_BEFORE=$(docker compose --env-file .env.compose logs --no-color app | grep -c 'PASS runtime-db-preflight')
docker compose --env-file .env.compose restart app
until curl -fsS "http://127.0.0.1:${APP_PORT}/api/health" >/dev/null; do sleep 1; done
PREFLIGHT_AFTER=$(docker compose --env-file .env.compose logs --no-color app | grep -c 'PASS runtime-db-preflight')
JOBS_AFTER=$(docker inspect --format '{{.Id}}|{{.State.ExitCode}}|{{.State.StartedAt}}|{{.State.FinishedAt}}' "$BOOTSTRAP_ID" "$MIGRATE_ID")
test "$PREFLIGHT_AFTER" -gt "$PREFLIGHT_BEFORE"
test "$JOBS_AFTER" = "$JOBS_BEFORE"
```

Finally, destroy only this disposable project and prove no resources/files remain:

```sh
docker compose --env-file .env.compose down --volumes --remove-orphans --rmi local
pnpm container:prepare-secrets -- --env-file .env.compose --clean
rm -f .env.compose
rm -rf "$VERIFY_DIR"
test -z "$(docker ps -aq --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker network ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker volume ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker image ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test ! -e .env.compose
test ! -e "$VERIFY_DIR"
```

- [ ] Done

## Round 6 — post-path repair — 2026-07-14

| Check | Result | Evidence |
|-------|--------|----------|
| Prior blockers | ✅ resolved | `pnpm container:contract-check` invoked `node pop/scripts/container-contract-check.mjs` and passed both live digest equalities plus `PASS image-contract`. The clean-clone build creates invalid TypeScript under `pop/worktrees` after `git archive`, then the real Compose build passed and emitted `PASS docker-context-exclusion`; this depends on `.dockerignore`, not Git ignore state. |
| Build and quality | ✅ passed | `pnpm container:test --clean-clone --scenario build` produced non-root bootstrap/migrate/app images; `pnpm check` passed typecheck, lint, 4 tests with 1 skipped, and the production `/pt-BR`, `/en`, `/api/health` build. |
| Installer and ports | ✅ passed | `bash -n install/*.sh && install/test.sh` emitted `PASS install-contract`. Rendering with `APP_PORT=43123` published only app target 3000 at `127.0.0.1:43123`; DB and jobs had no host ports. |
| Digests | ✅ passed | Node index `sha256:6f7b03f7c2c8e2e784dcf9295400527b9b1270fd37b7e9a7285cf83b6951452d`; PostgreSQL index `sha256:1961f96e6029a02c3812d7cb329a3b03a3ac2bb067058dec17b0f5596aca9296`; both matched live exact tags and linux/amd64 children. |
| Documentation range | ❌ plan blocker | The exact C8 merge-base is now `d544b45` because PR #4 was integrated into `develop` before human testing. `pnpm db:contract-check` passed, but `pnpm container:contract-check --docs "$BASE..HEAD"` rejected `Dockerfile missing from committed task range`: the post-integration repair range cannot contain the already-integrated original Dockerfile change. |
| PoP artifact | ❌ committed-copy blocker | The worktree's committed verification file has 210 lines, so `python3 pop/scripts/pop_validate.py` fails the 150-line contract. This is not an unavoidable workflow contradiction: superseded rounds can be summarized without losing recoverability from Git history, as done in this task-side copy; the compacted artifact must be synchronized into the branch. |
| Integrity | ✅ passed | `git diff --check d5fa447..6bb51cc` and current worktree whitespace checks were clean. No application code was edited during verification. |

**Automated verdict:** rejected → return to `002_planning`. Implementation blockers from Round 5 are fixed, but the approved C8 committed-range contract no longer matches the post-integration Git topology; planning must define a truthful historical range or equivalent committed-documentation proof, then 004 must synchronize the compacted verification artifact. Because `critical: true`, the human checkbox remains unchecked and is not actionable; do not advance to 006.

## Round 7 — immutable-range final — 2026-07-14

- Exact revised C8 passed from immutable `8ab3f72b5449620c6716578ca6aa955978898e88` through committed `688c5a957e856894e6b0920897001807f8d81637`: `PASS committed-delivery-range`, quality, database/container contracts, live Node/PostgreSQL digest equality, documentation, DOX, PoP validation, and cumulative whitespace.
- Same-HEAD clean-clone build emitted `PASS docker-context-exclusion`; bootstrap, migrate, and app images were `1000:1000`. `bash -n install/*.sh && install/test.sh` emitted `PASS install-contract`.
- Rendering `APP_PORT=43123` published only app target 3000 at `127.0.0.1:43123`; DB/bootstrap/migrate exposed no host port. Task-side and worktree verification artifacts matched byte-for-byte at 110 lines before this round; this synchronized addition remains below 150 lines. Diff checks were clean.

**Automated verdict:** C1, C8, and C9 fix-specific gates pass at current HEAD. Remain in `005_verifying` for the required human critical verification; the existing `- [ ] Done` checkbox remains intentionally unchecked. Do not advance to 006 until the human completes it.
