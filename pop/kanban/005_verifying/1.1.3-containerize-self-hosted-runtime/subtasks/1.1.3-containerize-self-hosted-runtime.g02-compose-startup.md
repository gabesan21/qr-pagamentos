# Group g02 — Compose startup — [[1.1.3-containerize-self-hosted-runtime]]

- **Order:** 2.
- **Depends on:** g01.
- **Description:** Encode external secrets, PostgreSQL persistence, one-shot jobs, and app availability as separate gates.

## Movements

- [x] (agent) Add `.env.compose.example` with original secret paths and `container:prepare-secrets`: require invoking-Docker-user ownership/mode 0600; use a pinned no-network helper in the daemon's namespace to make per-secret UID1000/0400 staged copies; reject failed mapping/read probes and clean copies on exit.
  - Expected: DB gets only original admin; bootstrap gets three staged files, migrate only migrator, app only runtime; original and staged stat/read-as-consumer/non-grant probes pass without output; no Compose uid/gid/mode remap is trusted.
  - If it fails: interpolation produces empty paths or inspect exposes values → use required guards/file sources; never put passwords/URLs in Compose environment.
- [x] (agent) Define pinned official `db` with named volume at `/var/lib/postgresql`, no host port, original admin `_FILE`, internal network, and `pg_isready`; preserve entrypoint identity semantics and keep initialization separate from migrations.
  - Expected: config proves only DB entrypoint mounts the original admin source; preflight proves invoking-user readability; initial remote admin auth proves its one initialization consumption; after health every server PID UID is nonzero with no secret FD; inspect/log scans show no value; no init-directory migration exists.
  - If it fails: legacy mount/no persistence/root server → repair exact image/volume/runtime configuration; existing incompatible major data → abort for explicit operator migration.
- [x] (agent) Define `bootstrap` and `migrate` from db-ops with `restart: "no"`: bootstrap depends on DB healthy, migrate depends on bootstrap completed successfully, both on the database network only.
  - Expected: jobs exit 0 in order; rerun is idempotent/no-op; nonzero remains visible and blocks downstream creation/start.
  - If it fails: Compose wait ambiguity → retain conditions and implement explicit exit inspection per fork; SQL/migration error → stop, never retry-loop it.
- [x] (agent) Define read-only, init-enabled app as UID/GID 1000 on database+edge networks, cache tmpfs, `unless-stopped`, migration-completion dependency, runtime preflight wrapper, Node healthcheck, and loopback-only publication.
  - Expected: app binds only after runtime `SELECT 1`; `/api/health` remains exact and DB-independent afterward; bad runtime auth leaves app absent; restart repeats only preflight, never completed jobs.
  - If it fails: write outside authorized cache/tmp or health needs DB → abort; port collision → set a different loopback `APP_PORT`, not a public bind.

## Execution notes

- 2026-07-14 — No reverse proxy, TLS, provider, Nautt service, or deployment topology is authorized in this task.
- 2026-07-14 — Final committed Compose config passed source/staged permissions, official DB initialization identity, non-root server PID, four ordered gates, loopback-only app publication, and no DB/ops port.
- 2026-07-14 — The requested collision avoidance is already the approved Compose contract: `APP_PORT` selects the loopback host port for app only; PostgreSQL, bootstrap, and migrate publish no host ports. Static and installer contracts preserve this boundary.
