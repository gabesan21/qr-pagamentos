# Group g04 — Quality and closeout — [[1.1.3-containerize-self-hosted-runtime]]

- **Order:** 4.
- **Depends on:** g03.
- **Description:** Run every gate, close documentation/DOX drift, and prepare the critical-human verification handoff.

## Movements

- [x] (agent) Run acceptance criteria 1–8 exactly, twice for the full clean-clone happy/lifecycle path; capture digest/platform, PASS lines, image sizes/users, job exits, health, ports, and orphan-free cleanup.
  - Expected: every listed run and observable pass succeeds twice from fresh test projects; no task test resource remains.
  - If it fails: return to the owning group and rerun the complete table; unplanned behavior/spec change returns to 002.
- [x] (agent) Update README with exact build/up/status/log/stop commands, secret helper/permissions, health meanings, loopback exposure, failure recovery, image rollback, volume-preserving operations, explicit destructive test cleanup, and a 005-only critical verification command block.
  - Expected: `container:contract-check --docs "$(git merge-base HEAD origin/develop)..HEAD"` prints `PASS container-documentation`; it parses committed task-range changes and exact required headings/commands/semantics rather than substring-only snapshots.
  - If it fails: a provider/reverse-proxy/TLS/manual capability is needed → remove the assumption or abort to planning; add no `(user)` execution item.
- [x] (agent) Re-walk every changed path against root/prisma contracts, update affected DOX indexes/rules, and make the docs checker assert committed-range Docker/Compose/wrapper paths, exact commands, boundaries, and child routing before secret/diff/PoP validation.
  - Expected: `PASS dox-contract`; nearest contracts explain all paths and the task-range whitespace check is clean; no stale command, unreviewed child, secret, generated output, or spec divergence remains.
  - If it fails: complete the owning contract within caps; material behavior divergence → stop for 002 rather than rewrite silently.
- [x] (agent) Prepare 005 evidence instructions: human creates fresh disposable secret files, follows README clean startup, confirms app on loopback in both locales plus `/api/health`, observes successful one-shot jobs/non-root users/no DB port, restarts app, then runs the documented test-only cleanup and checks no resources remain.
  - Expected: `.verify.md` can present exact commands/results and an unchecked critical-human `Feito`; human action occurs only in 005.
  - If it fails: verification needs production credentials/provider/public exposure → abort and remove that dependency.
- [x] (agent) Synchronize the compacted task-side `.verify.md` into the task worktree, commit it with the approved criterion-8 amendment, and rerun criterion 8 from immutable pre-task base `8ab3f72b5449620c6716578ca6aa955978898e88` through the then-current committed `HEAD`.
  - Expected: the committed worktree artifact remains below 150 lines; `PASS committed-delivery-range`, `PASS container-documentation`, `PASS dox-contract`, `pop_validate.py`, and cumulative-range whitespace checks all pass without changing any existing documentation/path assertion.
  - If it fails: a non-descendant HEAD, missing original container path, stale current-state contract, or oversized artifact returns to 002; never substitute a moving merge-base or describe historical changes as part of only the latest repair range.

## Execution notes

- 2026-07-14 — In 006, replace the planned spec slice with the delivered slice and keep the broader administrative-foundation status accurate.
- 2026-07-14 — Implementation commits: `bc51cff` (runtime) and `f2e9656` (exact documentation checker); final HEAD `f2e96567d71c39d200036fe3f80bc6435e48ef5a`.
- 2026-07-14 — Criterion 8 passed `pnpm check`, database/container static checks, committed-range `PASS container-documentation` and `PASS dox-contract`, PoP validation, and range whitespace validation. Final Docker inventory and worktree status were empty.
- 2026-07-14 — 005 human instructions: create fresh disposable mode-0600 files; follow README production startup; check `/pt-BR`, `/en`, and `/api/health` on loopback; inspect zero-exit bootstrap/migrate, UID1000 app, non-root PostgreSQL server, and no DB port; restart only app and observe another runtime preflight without job changes; use documented test-only cleanup and confirm no project containers, networks, volumes, images, or staged files remain. Do not mark human `Feito` before performing these steps in 005.
- 2026-07-14 — Returned from 005 for the exact `$BASE..HEAD` criterion-8 failure. Commit `796b541` now resolves both range endpoints with `git rev-parse --verify <endpoint>^{commit}`, requires base ancestry and a positive committed range, and retains every docs/DOX assertion. The unchanged exact criterion-8 command and the complete criteria 1–8 table passed at `796b541f7da0d5096bb47fa13616624951816183`, including happy and lifecycle twice; final Docker/tmp inventories and worktree status were empty.
- 2026-07-14 — Returned from 005 Round 2 only for authorized Node exact-tag drift. After commit `2152c5c`, exact C1 and C8 passed with the refreshed Node index; the complete criteria 1–8 table passed again, including happy and lifecycle twice. A final live check confirmed no second drift. Final HEAD `2152c5ce507f0462caf6149fb797ee7cddc1c5a4`; Docker/tmp inventories and worktree status were empty.
- 2026-07-14 — Installation-failure patch checks passed: `bash -n install/*.sh`, `install/test.sh`, host `pnpm db:generate && pnpm build`, exact-image/static container contract, app-target Docker build, and `git diff --check`. DOX/spec review found no behavior-contract change requiring updates; the patch restores the approved build-context boundary.
- 2026-07-14 — Commit `6bb51cc` repaired package and internal references for the scripts moved to `pop/scripts`, removed the ineffective installer assertion, and passed `pnpm container:contract-check`, `pnpm db:contract-check`, `install/test.sh`, `pnpm check`, the committed clean-clone build scenario, and diff checks. PoP validation now executes at its moved path but reports the pre-existing oversized cumulative 005 verification note; no application/spec/DOX behavior changed.
- 2026-07-14 — Round 6 returned to planning because early integration made `origin/develop` an invalid moving base for original-delivery coverage. The proposed repair keeps all C8 assertions and uses the immutable pre-task base plus current committed HEAD; 004 must also synchronize the already-compacted verification artifact before rerunning the gate.
- 2026-07-14 — Commit `c75dd83` synchronized the current 004 card, amended plan/approval, subtask evidence, and 110-line verification artifact. Exact revised C8 passed from immutable base `8ab3f72` through committed HEAD with `PASS committed-delivery-range`, quality/database/container documentation/DOX gates, valid PoP vault, and cumulative-range whitespace check.
