# Verification — 6.2.2 Create official brand assets

- **Mode:** full, fresh independent yolo 005 review after return 1
- **Task commit:** `9794d5c3d747a20b36d2286f8670cf950c473687`
- **Verdict:** **APPROVE — round 2/2**

## Original-request check

The delivery meets the original request. It supplies one official four-identity
QR Pagamentos family for favicon, login, future role shells, and the current
merchant-logo fallback; integrates the applicable current consumers; and
protects the family with a versioned manifest, deterministic generation,
provenance, usage, accessibility, and visual evidence. Authentication,
authorization, storefront projection, mutation, and future media/shell
capabilities remain unchanged.

Both prior blockers are resolved. Static lockups now embed reviewed IBM Plex
Sans outlines and contain no text, font declaration, or non-local reference.
The outline, pinned font, and license hashes match the repository dependency,
and the declared conversion tool/version is auditable. A separate hardcoded
contract owns the exact four IDs and eight ID/role/variant/output mappings;
negative tests prove unknown, missing, duplicate, and remapped regenerated
inventories fail.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| AC1 — original, safe, auditable family | **PASS** | Original canonical geometry, exact name, reviewed outline source, matching source/license hashes, local-only SVG references, and explicit provenance/tool settings inspected. |
| AC2 — deterministic manifest and derivatives | **PASS** | `brand:check` passed twice for 12 assets; independent contract tests passed all unknown/missing/duplicate/remap mutations; worktree remained clean. |
| AC3 — theme, contrast, and responsive behavior | **PASS** | All final specimen captures and static positive/reversed renders were inspected; one geometry serves six themes, minimum mark is 32px, measured contrast is 13.31:1 or higher, and no changed-surface clipping or S2+ issue remains. |
| AC4 — specs, DESIGN, and DOX | **PASS** | `DESIGN.md`, design-system spec, root index, brand DOX, manifest, and generator agree; standalone PoP validation passes. |
| AC5 — bounded current integrations | **PASS** | Login, specimen, compact composition, and storefront fallback use the shared component; focused tests pass except the independently known storefront locale expectation; no capability surface changed. |
| AC6 — accessible naming and layout | **PASS** | Decorative adjacent marks, exactly one named standalone mark, no SVG title, unchanged login focus order, four viewports, and axe evidence pass. |
| AC7 — evidence and project gates | **PASS with classified baselines** | Current design-system/login evidence verifies and binds the repaired manifest; typecheck, lint, build, scope, diff, and PoP pass. Full tests retain only the pre-existing `server-only` resolution and storefront locale failures. |

## Prior-blocker verification

### B1 — font-independent static lockups: resolved

- `src/brand/wordmark.outlines.svg` is hash-pinned and embeds only local glyph
  paths/uses.
- All six static lockup files reject `<text>`, `font-family`, `@font-face`, and
  non-local references in `brand:check`.
- The installed IBM Plex Sans WOFF2 and OFL license hashes exactly match the
  manifest's pinned values.
- Independent rasterization of the positive and reversed SVGs produced the
  complete, correctly bounded `QR Pagamentos` wordmark with identical metrics.

### B2 — independently closed inventory: resolved

- `scripts/brand-contract.mjs` hardcodes the exact four IDs and eight required
  output/role/variant mappings independently of `geometry.ts` and regeneration.
- `scripts/brand-contract.test.ts` proves that matching source+manifest changes
  still reject an unknown ID, a missing ID, duplicate inventory, and a remapped
  composition.
- The validator also rejects extra/missing SVG counts, duplicate output paths,
  unknown mappings, and regenerated duplicate bytes.

## UI review

- Nielsen consistency/recognition: **PASS** — one recognizable composition
  family and semantic foreground remain consistent across every inspected use.
- WCAG 2.2 AA automated layer: **PASS** for the changed surfaces — no
  serious/critical axe finding; measured contrast, naming, focus, and responsive
  assertions pass.
- Semantic layer: **PASS** — identity naming is context-correct and the login
  keyboard order is unchanged.
- Visual loop: **PASS** — 24 specimen and eight login final captures at
  320/375/768/1440 plus independently rasterized static lockups were inspected;
  no changed-surface severity 2+ issue remains.
- Known limits: screenshot anti-aliasing and sub-12px text are not treated as
  pixel-perfect proof; the review relies on semantic assertions and direct
  inspection.

## Verification evidence

- `pnpm brand:check` twice — **PASS**, 12 assets each run
- focused brand-contract/brand/login/storefront Vitest — **BASELINE**, 15/16;
  only the unchanged storefront locale expectation fails
- full `pnpm test` — **BASELINE**, 95 files pass, one skipped; 758 tests pass,
  six skipped; unchanged `server-only` resolution suite and storefront locale
  expectation fail
- design-system/login evidence verification — **PASS**
- typecheck — **PASS**
- lint — **PASS**, 11 unchanged `order-view` warnings
- production build — **PASS**
- outline/font/license SHA-256 inspection and static rasterization — **PASS**
- scope check — **PASS**, 113 paths within the planned asset/UI/evidence set
- `git diff --check` and clean worktree — **PASS**
- standalone PoP validation — **PASS** with expected 005 worktree warnings

## Findings and decision

No blocking, suggestion, or nit finding remains in the changed surface.

**APPROVED.** The task may advance to 006.

verificado por revisor independente (yolo) — 2026-07-24
