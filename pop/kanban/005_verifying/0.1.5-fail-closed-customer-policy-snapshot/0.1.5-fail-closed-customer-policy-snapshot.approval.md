# Aprovação — [[0.1.5-fail-closed-customer-policy-snapshot]]

- **Etapa:** 003_human_approval · **Responsável:** revisor independente em yolo

## Rodada 1 — 2026-07-21

### Brief para decisão

- **Entrega:** `toPolicySnapshot` (`src/orders/order-view.ts`) passa a fail-closed: valor de `checkoutDataPolicy` fora do enum fechado expõe o snapshot todo-nulo, não a tupla completa.
- **Estratégia:** tornar `NAME_EMAIL_CPF_ADDRESS` um branch explícito e converter o fall-through em snapshot em branco; teste de regressão via cast; uma linha de esclarecimento no slice da Task 4.3.1 da spec.
- **Topologia:** executor único, frente F01 única.
- **Risco principal:** reescrita dos branches existentes alterar a tupla de política conhecida — mitigado pelos testes de tabela das cinco políticas.
- **Critérios principais:** 1–4 (regressão fail-closed, tabela conhecida intacta, `pnpm check` verde, invariante registrado na spec).
- **Plano:** [[0.1.5-fail-closed-customer-policy-snapshot.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(delegada ao crítico yolo — gate 003 em modo yolo)_

- [ ] Feito

### Decisão do agente

`aprovado → 004`

### Resposta do revisor (yolo)

- **Contexto:** strong independente.
- **Devolução:** 0 de 2.
- **Decisão:** aprovado → 004.
- **Motivo/evidência:** código confere com o brief — `order-view.ts:80-87` enumera 4 dos 5 membros de `CHECKOUT_DATA_POLICIES` (`payment-link-order.ts:6`) e o fall-through expõe a tupla completa; tornar o 5º explícito e o default em branco é fail-closed inequívoco. Critérios 1–3 executáveis com `re-run` e resultado observável; critério 4 verificável por leitura. Spec realmente silente sobre valores fora do enum (slice 4.3.1 diz apenas "exact policy-shaped"), logo a linha de invariante é justificada e proporcional. Origem confirmada na memory de 4.3.1 (sugestão do 005). Sem item `(user)`, sem DAG, tamanho S respeitado.

- [x] Feito

_aprovado por revisor independente (yolo) — 2026-07-21_
