# Verification — 7.2.1 Add owner product categories

- **Mode:** full, mandatory for a critical task after a return to 004
- **Round:** 2
- **Task commits:** `a24153cd747d8a992eafbe1d7196df493fd1fa76`, `498f8c5324bc70fe1d7f818508dde12d38e8736d`
- **Verdict:** **APPROVE**

## Original-request check

The delivery satisfies the requested owner-category slice: owner-scoped
bilingual persistence, opaque owner-only create/edit/deactivate behavior,
expected-version CAS, restrictive same-owner product references, safe atomic
replacement, cross-owner rejection, and hard-owner-deletion protection are
implemented without adding ordinary product selection or public category
exposure.

The round-1 blockers are resolved. Runtime category authority is now exactly
`SELECT`, `INSERT`, and `UPDATE`, with `DELETE` revoked by both the migration
and the relation-guarded bootstrap normalization. A disposable PostgreSQL run
executes three independent-client ACL/concurrency scenarios before the known
unrelated product-price fixture stop.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| AC1 — additive canonical least-grant migration | **PASS** | Migration policy and static DB contracts pass. Canonical SQL grants only category `SELECT, INSERT, UPDATE` and explicitly revokes `DELETE`; bootstrap safely preserves the revocation on repeat runs. |
| AC2 — PostgreSQL integrity and V1 preservation | **PASS with classified later baseline** | Fresh replay and idempotence pass. The category ACL/concurrency suite passes before the unchanged malformed product-price fixture fails with `42601` instead of its expected `23514`; inspection and the earlier schema assertions cover nullable no-backfill, composite owner FKs, restrictive deletion, exact uniqueness, and nonnegative versions. |
| AC3 — owner-only validated opaque management | **PASS** | Eleven focused service/store/route tests pass, covering role denial before work, cookie-only ownership, origin-first rejection, bilingual bounds, exact uniqueness, stale/cross-owner/inactive targets, closed actions, and opaque redirects. |
| AC4 — deterministic history-safe reassignment | **PASS** | Two real Prisma clients are held behind an observed PostgreSQL row lock. Edit-versus-deactivate and competing replacements each produce one commit and one `ProductCategoryConflictError`; persisted state/version is coherent, the winning replacement owns every moved product, and no source reference remains. |
| AC5 — specs, DOX, and application baseline | **PASS with classified baselines** | Catalog/storefront specs, root and Prisma DOX, 17-path scope, diff hygiene, PoP validation, typecheck, lint, and production build pass. Full Vitest retains only the unchanged `server-only` resolution and storefront-locale failures (808 passed, 9 skipped). |

## Round-1 blocker resolution

### B1 — resolved: retained categories cannot be physically deleted by runtime

`prisma/migrations/20260724010000_owner_product_categories/migration.safe.json`,
the generated SQL, `prisma/bootstrap.sql`, and both static/runtime ACL checks
now deny category `DELETE`. Fixture cleanup uses the admin connection. The
bootstrap edit extends F01 ownership only as required to prevent its broad
ordinary-table grant from restoring the forbidden privilege on an existing
database.

### B2 — resolved: concurrency proof reaches PostgreSQL and Prisma

`src/auth/product-category.database.test.ts` uses two independent runtime
Prisma clients plus an admin observer/blocker. The successful disposable run
observed both operations waiting on PostgreSQL locks, then proved one opaque
loser and one durable winner for both required races. Serialization/deadlock
codes from the actual Prisma PG adapter are mapped to the same conflict family.
`pop/scripts/db-test.mjs` runs this focused proof immediately after migration
replay/idempotence, so the unrelated later fixture cannot hide it.

## Findings

- No blocking finding, suggestion, or nit remains.
- No unexplained ownership invasion was found. The bootstrap/Prisma-DOX
  expansion is necessary for least privilege; the remaining 17 changed paths
  stay within category persistence, service/route, request-log inventory,
  durable contracts, and verification.
- Product, payment-link, checkout, order, provider, and storefront projection
  implementations remain unchanged.

## Verification evidence

- `pnpm db:migration-policy` — **PASS**
- `pnpm db:contract-check` — **PASS**
- `pnpm db:test` — **CATEGORY PROOF PASS**, then known unrelated product-price
  fixture stop; output includes `PASS product-category-database-concurrency`
  before the stop
- focused category Vitest — **PASS**, 11/11
- `pnpm typecheck` — **PASS**
- `pnpm lint` — **PASS** with 11 unchanged order-view warnings
- `pnpm build` — **PASS** on the authorized rerun; sandbox-only worker-port
  denial was reproduced first
- `pnpm test` — **KNOWN BASELINE**, 101 files passed / 2 failed / 2 skipped;
  808 tests passed / 1 failed / 9 skipped
- `python3 pop/scripts/pop_validate.py --standalone` — **PASS**
- `python3 pop/scripts/pop_check_scope.py ...` — **PASS**, 17 paths
- `git diff --check develop...HEAD` — **PASS**

## Decision

Approve round 2. The original request, critical concurrency/retention contract,
and all acceptance criteria are met; the remaining failures are unchanged,
independently known repository baselines outside this task.

verificado por revisor independente (yolo) — 2026-07-24
