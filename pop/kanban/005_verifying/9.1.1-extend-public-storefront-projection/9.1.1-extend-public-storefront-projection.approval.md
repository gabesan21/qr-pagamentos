# Approval — [[9.1.1-extend-public-storefront-projection]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read: card, plan, the three linked specs, the root `AGENTS.md` `/store/[slug]` contract line, plus reality checks on `src/storefront/public-storefront.ts`, `src/app/store/[slug]/page.tsx`, `src/auth/storefront-settings.ts` (extended columns `storefrontStandalonePaymentsEnabled` / `storefrontDefaultCurrencyCode` exist), `src/auth/product.ts` (`active`/`archivedAt`/`currencyCode`/`imageMediaId` exist), `src/security/public-rate-limit.ts` (closed three-route surface confirmed), `package.json` (`check`, `db:test`, `test` scripts exist), the referenced test files (`public-storefront.test.ts`, `page.test.tsx`, `routes.test.ts` all exist), and the dependency memories (7.1.2, 7.2.2, 8.1.2 all in `pop/memory/`; 006_done is cleaned per workflow). The plan's factual claims check out: the current projection is branding+V1-link-driven products only, the spec's redaction boundary (storefront-and-customization.md line 27) does list "never exposes the default currency code", and line 28 explicitly defers grouped category presentation to 9.1.1 — so the planned amendment is real, scoped, and lands exactly where the spec itself points.

On the two deliberate boundary amendments, which this critical gate exists to judge: (1) exposing the product's canonical UUID as an opaque cart reference is sound and bounded — UUIDs are unguessable/non-enumerable, there is direct precedent in the already-delivered public V2 link resolution exposing provider pair UUIDs "required for quoting", and the plan pins that 9.1.3 must revalidate every reference server-side, so the exposure grants no authority; (2) exposing the resolved display currency code (product `currencyCode` ?? store default) is display metadata only, with provider pair UUIDs explicitly kept out. Both are honestly framed as amendments with a documented abort path (blocked + replan with a minted public identifier) rather than silent drift. The catalog-eligibility broadening (all active non-archived products, not just link-backed ones) is within the human-released scope — the card's What and the roadmap entry both name product/price/availability exposure — so it is a design consequence of the cart model, not an avoidable `(user)` business decision.

#### Criteria checked

1. **Verifiable deliverable — pass.** Each of the 6 criteria names a concrete check with an observable result: updated/new contract tests in `public-storefront.test.ts` (1, 2), existing page/routes tests green with no page edit (3), a file-untouched diff read on the limiter (4), spec/AGENTS.md wording reads (5), `pnpm check` + `pnpm db:test` exit 0 (6). All referenced files and scripts exist.
2. **Sufficient, lean brief — pass.** Objective, observable result, three pinned decisions, affected areas with exact paths, fronts with own/may-read/must-not-edit, risks with abort conditions. No pseudocode or micro-edits demanded.
3. **Safe execution — pass.** Two sequential fronts, single executor; F02 (docs only) genuinely depends on F01's final DTO. Write sets are disjoint (`src/storefront/` vs specs+AGENTS.md) and no false parallelism is claimed.
4. **Proportional specs — pass.** All three linked specs mapped: storefront spec amended (boundary + 9.1.1 entry, resolving its own line-28 deferral), catalog spec gets one delivered-by note, checkout spec explicitly read-only. Root `AGENTS.md` `/store/[slug]` line correctly identified as an affected durable contract.
5. **No avoidable `(user)` item — pass.** No account, credential, or new business decision required; the eligibility broadening is covered by the human-released roadmap/card scope and the two exposure amendments are exactly what this critical gate reviews.
6. **Proportional to risk/size — pass.** Recon is limited to files the front already owns or reads; `db:test` is justified because the Prisma store query itself changes (bounded reads, `take` caps); size L matches a contract-amending cross-layer read evolution.
7. **Economic verification — pass.** No web research; re-run mode on criteria 1, 2, 3, 6 against an existing runtime; evidence-only reads (4, 5) are not redundant with the test runs.

#### Risk assessment

005 runs full mode (critical). It must verify, beyond green tests:

- **Absence-assertion completeness:** the DTO contract tests must actually prove absence of owner id/username/email, credential state, provider pair UUIDs, raw toggle values beyond the single derived `standalonePayments` boolean, versions, timestamps, category UUIDs/internal fields, and product internal fields beyond the one UUID reference and media identifiers — not just assert presence of the new members.
- **Amendment wording fidelity:** the spec and root `AGENTS.md` never-expose lists must lose exactly the two amended items (product UUID reference, resolved display currency code) and nothing more; the eligibility broadening (active non-archived products independent of V1 links) must be written into the boundary explicitly, since 005 treats the amended boundary as the contract.
- **Currency resolution edge:** a stored product/store code whose registry mapping was later deactivated still renders (stored codes survive deactivation per the registry contract) — confirm the spec wording covers this as display metadata and that no provider UUID or inactive-code discovery leaks with it.
- **Bounded reads:** deterministic ordering plus explicit `take` caps on both categories and products must be asserted by tests; note the current products query has no product-level `take`, so this is a real new invariant, not a preserved one.
- **Byte-stability:** `/store/[slug]` page, the V1 `products` member shape, and `/pay/[identifier]` linking unchanged; `public-rate-limit.ts` untouched; no edits under F01's must-not-edit list.
- **Availability semantics:** the `available` flag's exact definition (active ∧ non-archived, plus whatever category/uncategorized grouping rule) must be pinned in tests and match the spec wording.

aprovado por revisor independente (yolo) — 2026-07-26
