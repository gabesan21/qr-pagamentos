# Group g02 — browser evidence — [[1.4.3-redesign-login-experience]]

- **Order:** 2
- **Depends on:** g01
- **Description:** Produce and bind fresh production `/login` visual and accessibility evidence.

## Movements

- [ ] (agent) Adapt the existing run-bound evidence pattern into login-specific Playwright test, package scripts, artifact namespace, and verifier; do not weaken or repurpose the `/design-system` runner.
  - Expect to see: the runner exercises default and invalid-recovery login states, eight light/dark responsive captures, keyboard focus, labels/autofill, target sizes, local font, overflow, console/page errors, and axe.
  - If it fails: a missing browser/evidence dependency or a requirement that needs route behavior → use the plan fork or abort; do not accept static/manual-only evidence.
- [ ] (agent) Run `pnpm login:evidence`, inspect all current captures, write the manifest-bound review with final severity below 2, then run `pnpm login:evidence:verify`.
  - Expect to see: exactly eight fresh hashed PNGs and assertions, with a review naming the matching run ID and manifest hash; both commands exit 0.
  - If it fails: fix a local visual/accessibility defect and recapture; missing Chromium/axe/capture capability aborts the task.

## Execution notes

- YYYY-MM-DD — record run ID, manifest path/hash, review path, verifier output, and any resolved visual finding.
