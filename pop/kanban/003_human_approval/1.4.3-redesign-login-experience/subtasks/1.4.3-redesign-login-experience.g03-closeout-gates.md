# Group g03 — closeout gates — [[1.4.3-redesign-login-experience]]

- **Order:** 3
- **Depends on:** g01, g02
- **Description:** Run the aggregate quality gate and prepare the factual documentation/spec closeout.

## Movements

- [ ] (agent) Update `DESIGN.md` only to record `/login` as an existing-primitive `PIX ledger` consumer and its applicable default, pending, disabled, error/recovery, and focus states; do not alter the token contract or create new visual direction.
  - Expect to see: the persistent design contract describes the shipped screen without conflicting with its primitive inventory.
  - If it fails: a new token/primitive is actually needed → return to 002 rather than silently expand the shared system.
- [ ] (agent) Run `pnpm check` after the evidence runner; capture the full result and review the diff for only intended presentation, test/evidence, and documentation changes.
  - Expect to see: all aggregate gates exit 0 and no POST route, schema, secret, locale-route, or generated-file change is present.
  - If it fails: fix the local regression; a contract or generated-file failure returns to 002.
- [ ] (agent) Record evidence locations and the final actual behavior in the execution notes for 005/006; at 006, synchronize the two listed specs with what was actually delivered, including their implementation slices/statuses.
  - Expect to see: 005 can audit every evidence criterion without inference and 006 has exact spec changes to make.
  - If it fails: evidence is missing or spec reality diverges → treat it as a verification failure or return to 002, respectively.

## Execution notes

- YYYY-MM-DD — record `pnpm check` output, diff-review conclusion, and exact pending 006 spec updates.
