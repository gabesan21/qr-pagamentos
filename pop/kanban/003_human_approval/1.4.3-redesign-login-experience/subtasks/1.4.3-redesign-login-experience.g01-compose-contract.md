# Group g01 — compose contract — [[1.4.3-redesign-login-experience]]

- **Order:** 1
- **Depends on:** none
- **Description:** Characterize the presentation boundary and compose `/login` only from owned primitives.

## Movements

- [ ] (agent) In the task worktree, re-read the listed root and `src/components/ui` contracts; run `pnpm install --frozen-lockfile` before changing code.
  - Expect to see: pinned dependencies install and the allowed primitive/state APIs remain available.
  - If it fails: locked install or CLI failure → environment is not evidence-capable → apply the plan abort condition and record output.
- [ ] (agent) Add `src/app/login/page.test.tsx` and `src/app/login/login-submit.test.tsx` to first characterize the current form action, label/control associations, required/autofill attributes, generic query recovery, both dictionaries, and mocked pending/ready `useFormStatus` semantics.
  - Expect to see: focused test initially fails only for the intended redesigned-state assertions.
  - If it fails differently: existing contract is misunderstood → inspect only the named login/dictionary source, correct the characterization, and do not change the POST route.
- [ ] (agent) (after prior) Add only the needed bilingual copy, a page-local client submit control using `useFormStatus`, and shadcn `Card`, `Field`, `Input`, `Alert`, `Button`, and `Spinner` composition in `src/app/login/`; preserve `/login/submit`, generic error wording, native form fields, and unprefixed routing.
  - Expect to see: default, pending/disabled, invalid/recovery, hover/focus, and autofill states render through the existing source inventory without raw visual values or a legacy adapter.
  - If it fails: missing primitive capability or need for route/session/i18n change → take the matching fork; do not create a page-specific variant.
- [ ] (agent) Run `pnpm test -- src/app/login/login-submit.test.tsx src/app/login/page.test.tsx` and the criterion-1 focused command; record exact outputs in this group. Reserve lint and typecheck for the aggregate `pnpm check` gate.
  - Expect to see: all commands exit 0 and tests demonstrate the intended regression boundary.
  - If it fails: repair only the local composition/test until green; a behavior-contract failure returns to 002.

## Execution notes

- YYYY-MM-DD — record changed paths, focused command output, and any fork decision.
