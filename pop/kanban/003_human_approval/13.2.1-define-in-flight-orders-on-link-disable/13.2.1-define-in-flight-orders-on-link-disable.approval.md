# Aprovação — [[13.2.1-define-in-flight-orders-on-link-disable]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-23

### Brief para decisão

- **Entrega:** pin the undefined money-state transition — what happens to in-flight V2 checkout attempts and their `order_v2` rows when an owner deactivates a link — in [[specs/checkout-and-order-lifecycle|Checkout and order lifecycle]], and implement exactly that rule.
- **Estratégia:** **The pinned contract (decided here; the executor implements it, it does not re-decide it).** Deactivation is the only owner action that touches attempts, and it touches exactly one attempt state. In **one transaction**, `setActive(..., false)` keeps its version CAS on `payment_link_v2` (that `UPDATE` takes the link row lock, serializing with the reservation's `FOR UPDATE` in `src/checkout/public-checkout-v2.ts:165-1…
- **Topologia:** executor único — one contract, one cohesive write set, one skill; the UI and spec edits are two lines each and share the decision. **Perfis:** fixos por papel nativo; `size: M` limita orçamento, não topologia.
- **Risco principal:** deactivate × settlement race — mitigation: the sweep writes only orders still in `CREATED` (never dispatched, never reconcilable), so it can never collide with a settlement CAS; settlement never reads attempt state.
- **Critérios principais:** 1, 2, 3, 4, 5, 6, 7, 8, 9.
- **Plano:** [[13.2.1-define-in-flight-orders-on-link-disable.plan]] — *siga para revisar o brief completo*.
- **Decisão de contrato a confirmar:** desativar um link faz sweep transacional só dos attempts `RESERVED` → `FAILED` (pedido `CREATED → REJECTED`, mesma forma de 15.2.2); `CREATING/PENDING/INDETERMINATE` não são tocados porque o POST pode já ter chegado à Nautt e seguem ao desfecho autoritativo; comprador afetado cai no estado de falha de submit → start over → link inativo → view indisponível; sem migração, sem DTO novo; cópia "existing orders are unaffected" corrigida.
- **Critical:** order money/state transition or destructive data operation — human approval required before 004.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
