# Approval — [[9.1.3-issue-storefront-one-time-links]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read of the card, plan, and the three linked specs, with reality checks against the live tree and both dependency branches:

- `src/auth/payment-link-v2.ts` (develop): `create(actor, input)` takes a `Principal` via `requireUserPrincipal` and validates composition kind, `currencyPairId` UUID, link type, expiry, and lines (1–20 lines, 1–9,999 integer quantity). A sessionless issuance service can therefore reuse it unchanged by synthesizing the actor from the resolved storefront owner row — the plan's "reuse server-side, never the owner POST route, never edit the file" posture is feasible as written.
- `src/auth/supported-exchange-currency.ts` exposes `requireActivePair(code)` resolving an active code to its registry pair server-side, so the single-currency rule (product code ?? store default → one active-mapped pair) has a real delivered seam.
- Branch `task/9.1.2-build-storefront-catalog-and-cart` delivers `src/storefront/cart.ts` with exactly the contract the plan pins: storage key `qr-pagamentos:storefront-cart:v1:<slug>` via `storefrontCartStorageKey`, item kinds `product` / `custom-amount`, quantity clamp 1–9,999. Decisions 3 (custom-amount exclusion) and 6 (clear only that key) match reality.
- Branch `task/9.2.1-implement-standalone-payment-orders` delivers `POST /api/store/[slug]/checkout` (+ `/status`), `src/checkout/standalone-checkout.ts`, a `standalone-checkout-submit` rate-limit class at burst 12 / 12-per-minute refill, and two literal `serverRequestRoutes` entries — the plan's "mirror 9.2.1, same 12/min submit class, one literal log entry, appended after 9.2.1 merges" is accurate, including limiter-before-parse, `force-dynamic`, `no-store`, and no origin guard on the sessionless prefix.
- Both dependency branches are genuinely unmerged (`git log develop..<branch>` non-empty for both), so the hard temporal gate is real, not decorative.
- `package.json` provides `test`, `db:test`, and `check`, so every re-run criterion is executable as written.
- Card skills table was already amended (ui-change in 004, ui-review in 005) per the plan's F02 flag — the UI-skill gap is closed.

The six pinned decisions are internally consistent with the DOX bullets (sessionless revalidation, closed opaque outcomes, additive-only inventories, no origin guard on the sessionless surface) and with the storefront spec's amended redaction boundary (opaque authority-free product-UUID `reference`, revalidated server-side by later cart commands — this task is that later command). No `(user)` items; fronts are sequential with justified dependencies and clean write sets.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result names the route, the exact success payload (`201 { paymentLinkIdentifier }`), the closed 429/400/404 set, and the client clear-and-redirect behavior; each acceptance criterion maps to a concrete command or diff check.
2. **Sufficient lean brief — pass.** Objective, six pinned decisions with rationale, three fronts with owns/may-read/must-not-edit, ordering, risks with abort conditions, and a seven-row acceptance table; no implementation prose beyond what an executor needs.
3. **Safe execution — pass.** Hard temporal gate on both unmerged dependencies (verified), rebase-before-edit, sequential waves (F02/F03 depend on F01's contract — no false parallelism), ownership disjoint, and explicit must-not-edit lists covering `payment-link-v2.ts`, `order-v2.ts`, 9.2.1's files, V1 surfaces, and `prisma/`.
4. **Proportional specs — pass.** Three specs linked with per-spec expected changes; decisions 1–6 explicitly framed as spec amendments reviewed at this critical gate; F03 owns the DOX/spec sync after behavior lands.
5. **No avoidable (user) item — pass.** Zero `(user)` items; dependency merges are the orchestrator's mechanical yolo integration, not human gates.
6. **Proportional to risk/size — pass.** L/critical: sessionless money-adjacent issuance with rate-limit, request-log, redaction, and disposable-Postgres contract coverage matches the risk; no gold-plating (no new persistence, no schema change, no provider call).
7. **Economic verification — pass.** Criteria 1, 2, 3, 4, 7 are `re-run` (`pnpm test`, `pnpm db:test`, `pnpm check`); 5–6 are read-only evidence; no web anywhere.

#### Risk assessment

Residual risks the 005 gate (full verification, mandatory for critical) must actively verify:

- **Actor synthesis:** the issuance service must build the `Principal` from the resolved owner row and confirm that owner is an active merchant `USER` (never ADMIN, never disabled); a sessionless path must not mint authority beyond what the owner's own authenticated creation would have.
- **Response whitelist:** the `201` body carries exactly `paymentLinkIdentifier` — assert absence of pair UUIDs, amounts, titles, owner identifiers, and composition echo; thrown service faults (including the dependency-deactivation race surfacing as `PaymentLinkV2DependencyError`) must land as an opaque outcome with a `failed` request-log record, never a mapped cause-bearing body.
- **Single-currency edge set:** null product code with null store default, mixed codes across lines, and unmapped/inactive code must all be the one opaque `400`; verify the pair is derived from the registry, never from browser fields.
- **Limiter posture:** the new submit surface is decided before JSON parsing, keyed per the closed-surface + hashed-XFF rule, and every outcome is `no-store`.
- **Inventory byte-safety:** diff vs `develop` shows only additive entries in `public-rate-limit.ts` and `server-request-log.ts`, appended after 9.2.1's; 9.2.1 routes/service and all V1 surfaces byte-unchanged; `/pay/[identifier]` untouched.
- **Client scoping:** success clears only `qr-pagamentos:storefront-cart:v1:<slug>` for the exact slug and redirects to `/pay/[identifier]`; failure leaves the cart byte-intact with one opaque localized error; the control is absent/disabled when a `custom-amount` item is present.
- **Interim landing honesty:** until 9.3.1, a V2 identifier on `/pay/[identifier]` renders the generic unavailable view — 005 must confirm no test, evidence, or doc claims V2 page rendering in this task.

aprovado por revisor independente (yolo) — 2026-07-26
