# Approval — [[8.1.3-implement-generalized-order-records]]

## Round 1 — 2026-07-25

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent adversarial read of the plan against the card and the binding V2 contract ([[specs/checkout-and-order-lifecycle]] Commerce V2 sections, [[specs/catalog-and-payment-links]] V2 section), with reality checks on `prisma/schema.prisma` (no V2 models yet — 8.1.2 correctly pending), `package.json` scripts, and both epoch roadmaps.

#### Criteria checked

1. **Verifiable objective — pass.** Observable result names concrete behavior (owner creates ad-hoc order, comments, sets local outcomes; V2 projections expose `state` and local outcome separately; `pnpm check`, `pnpm db:test`, `pnpm db:contract-check` green; no V1 behavior change). All seven acceptance criteria bind to executable commands (`pnpm db:migration-policy`, `db:contract-check`, `check`, `test`, `db:test` — all confirmed present in `package.json`) or direct artifact inspection (criterion 7).
2. **Spec fidelity — pass.** F01's migration inventory matches the spec's fixed inventory table-for-table: `order_v2`, `order_v2_line`, `order_comment_v2`, `order_local_outcome_v2`, `checkout_attempt_v2`, `payment_link_v2_single_use_settlement`, plus exactly one nullable `provider_order.order_v2_id`. Grant least-privilege (no runtime `DELETE` on comment/outcome, no runtime `UPDATE` on outcome/settlement, snapshot lines `SELECT`/`INSERT` only) is restated verbatim. The four service-fenced invariants (24-char identifier grammar, canonical decimal grammar, active-dependency enforcement, source vocabulary with link/state coherence) are kept in service code exactly as the spec mandates, with an explicit abort on any inexpressible inventory item rather than a raw-SQL deviation. `AD_HOC` carries no state, no attempt, no capability, no provider dispatch; snapshots immutable; comments append + author-only CAS edit with the 1–2,000 code-point/NFC/line-break rules; outcomes append-only, latest-is-current, never touching `state`; sessionless standalone explicitly deferred to 9.2.1 with no irreversible encoding (no public/sessionless route, no rate-limit change).
3. **Topology and ownership — pass.** F01 alone (shared Prisma/migration write set, serialized after 8.1.2), wave-2 F02/F03/F05 on disjoint new files under `src/orders/`, F04 after service signatures stabilize, F06 closeout. Ownership, may-read, and must-not-edit lists are explicit per front; parallelism is confined to logically and write-independent fronts.
4. **Dependencies — pass.** 8.1.2 write-set serialization is handled concretely: preflight greps for `model PaymentLinkV2` and the merged migration, F01 declares `Depends on: 8.1.2 merged`, and absence is an explicit abort with evidence. (Non-blocking observation: the card's `depends_on` frontmatter lists only 8.1.1; the plan's preflight/abort covers the 8.1.2 ordering, and the orchestrator may optionally add 8.1.2 to `depends_on` at closeout.)
5. **Risks — pass.** Immutable baseline (must-not-edit, policy-clean criterion, `migration-policy-baseline.json` untouched), V1 freeze (criterion 6 full-suite regression), settlement-map sole authority (outcome APIs expose no state parameter; criterion 4 verifies state untouched), exact-decimal money (never JS `Number`, dedicated criterion-3 suites), and safe-language expressibility (regenerate-first, `RECON NEEDED` on inexpressibility) are all named with mitigations and abort conditions.
6. **Acceptance criteria executable — pass.** Each criterion has a command, an observable pass condition, and a 005 mode; runtime criteria are `re-run` as required.
7. **No hidden indispensable decision — pass.** Route placement (`src/app/(merchant)/orders/` or sibling) is an implementation detail inside the owner-POST contract, not a contract decision; the spec's implementer checklist states no item requires new design.
8. **Economy — pass.** No web research, no avoidable `(user)` item, proportionate to size L / critical; preflights are justified by real dependencies (8.1.2 serialization, safe-language vocabulary).

#### Risk assessment

Primary residual risks are execution-stage, not planning-stage: safe-language expressibility of composite FKs/checks (aborted explicitly if hit), wave-2 file-set discipline under `src/orders/` (three fronts share the directory on disjoint new files — ownership lists make this enforceable), and the local-outcome/state precedence fence, which 005 must verify in full mode (mandatory for `critical: true`) against the spec's precedence clause.

aprovado por revisor independente (yolo) — 2026-07-25
