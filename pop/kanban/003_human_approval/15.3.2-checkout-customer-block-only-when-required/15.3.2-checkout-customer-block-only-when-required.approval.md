# Aprovação — [[15.3.2-checkout-customer-block-only-when-required]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-22

### Brief para decisão

- **Entrega:** with checkout data policy `NONE`, both buyer surfaces render no customer block at all — no customer heading, no `Alert role="status"` notice, no field group holding zero customer fields — and the now-unused dictionary key disappears.
- **Estratégia:** The template is the reference: `needsForm` (`docs/template/app/src/pages/checkout/CheckoutPage.tsx:416,500-551`) gates the entire buyer fieldset, while the submit button and privacy line sit **outside** it and never move. Mirror that shape — do not replace the notice with another placeholder and do not reorder the card.
- **Topologia:** executor único — one cohesive front, small contiguous write set (two adapters, two dictionaries, two test files), both skills applied to the same diff.
- **Risco principal:** deleting `checkoutNoCustomerData` while a live consumer survives — mitigation: repo-wide grep before deleting (C4); any non-V1 consumer stops the deletion.
- **Critérios principais:** C1, C2, C3, C4, C5, C6, C7.
- **Plano:** [[15.3.2-checkout-customer-block-only-when-required.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
