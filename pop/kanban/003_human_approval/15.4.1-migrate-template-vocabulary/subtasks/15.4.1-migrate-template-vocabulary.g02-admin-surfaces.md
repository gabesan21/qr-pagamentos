# Frente F02 — Admin family — [[15.4.1-migrate-template-vocabulary]]

- **Entrega:** admin dashboard, accounts, settings and payment-link surfaces reading only the declared template vocabulary, with no rendering change.
- **Escopo:** apply the plan's closed substitution map to the owned files; nothing else. No size, spacing, state, copy, dictionary, behavior, role-boundary or authorization change; no new component; no file created, renamed or deleted.
- **Responsável:** agent. **Papel:** `pop-executor`.
- **Paths de entrada:** card `15.4.1-migrate-template-vocabulary.md`; seções "Estratégia", "Mapa de substituição (fechado)" e "Critérios de aceite" de [[15.4.1-migrate-template-vocabulary.plan]]; esta fatia; `DESIGN.md`; [[pop/specs/application-frontend-system|application-frontend-system]]; [[pop/specs/administrative-foundation|administrative-foundation]]; `src/app/globals.css`; `docs/template/app/src/pages/admin/**` (a tela equivalente antes de reestilizar).
- **Owns:** `src/app/admin/**/*.tsx`, **exceto** `page.tsx`, `loading.tsx` e `error.tsx`; os `*.test.tsx` colocalizados **apenas** para atualizar asserção que nomeia uma classe migrada por esta frente.
- **May read:** `src/**`, `docs/**`, `scripts/**`, `DESIGN.md`, `pop/specs/**`.
- **Must not edit:** qualquer `page.tsx`/`loading.tsx`/`error.tsx` (fontes de hash da paridade — inclui `src/app/admin/accounts/[id]/page.tsx`, cujo `style` de acento é autorizado por path+padrão exatos em `scripts/check-design-tokens.mjs`); `docs/frontend-template-parity/**`; `src/components/ui/**`; `src/app/design-system/**`; `src/app/(merchant)/**`; `src/app/profile/**`; `src/app-shell/**`; `src/app/store/**`; `src/app/pay/**`; `scripts/**`; specs, DOX e `DESIGN.md`.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] — *use para o inventário anti-drift e a checagem de estados antes de tocar uma superfície*; [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para o contrato antes de editar e a triagem do que não é desta task*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval (não yolo); nenhum delta.
- **Saída:** relatório ≤20 linhas com contagem grep antes/depois por padrão do mapa, lista de arquivos alterados e exceções mantidas com motivo; status `concluída | BLOCKED`.
- **Critérios:** 1, 2, 4, 5, 6, 7 do [[15.4.1-migrate-template-vocabulary.plan|plano]].

## Contrato de execução

- Caminhe o DOX aplicável (raiz e `src/components/ui/AGENTS.md`) antes da primeira edição; os primitivos owned são importados, nunca editados.
- Concentração conhecida do legado: `admin/dashboard.tsx`, `admin/accounts/[id]/{access,preferences,storefront}-section.tsx`, `admin/accounts/{create-account-modal,identity-form}.tsx`, `admin/settings/**` (não-rota), `admin/admin-controls.tsx`, `admin/payment-links/associated-orders-card.tsx`.
- A fronteira de papel continua valendo: nada em `src/app/admin/**` importa componente do grupo merchant, e `SegmentedControl` admin-local não é promovido nem consolidado aqui.
- Ocorrência legada que só existe em arquivo de rota **não é migrada**: registre-a no relatório como resíduo para follow-up (o repair da paridade é de 15.4.2).
- `<input type="hidden">` desta subárvore permanece como está: a isenção do gate é da F03, não há primitivo owned a compor e criar um é proibido.
- Alvos de 44 px, dicionários e plural são de 15.4.2 — não toque, mesmo que o arquivo seja seu.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura para verificar.
- Dependência ou entrada ausente/incompatível → responda `BLOCKED` ao agente principal com evidência; não implemente nem simule dependência.
- Não altere caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <contagens grep e observação relevante>.
