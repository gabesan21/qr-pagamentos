# Approval — 7.2.2-evolve-products-for-catalog-media

## Round 1 (yolo critic, strong, fresh session)

### Resposta do crítico (yolo)

**Verdict: RETURN to 002 (return 1 of 2).**

Grounding verified as consistent with repo reality (keep as-is): currency
bounds-check doctrine matches `supported_exchange_currency_code_bounds`
(`prisma/migrations/20260724120000_supported_exchange_currencies/migration.sql:14`)
and the typed `NoActiveExchangeCurrencyMappingError` exists
(`src/auth/supported-exchange-currency.ts:31,114`); V1 public DTOs are exactly
`{ title, description, price }` (`src/checkout/public-checkout-presentation.ts:47`,
`src/storefront/public-storefront.ts:50-52`); the media lifecycle supports
STAGED→ACTIVE activation and orphan-with-grace (`src/media/media-service.ts:129-141`)
and `PRODUCT_IMAGE` exists (`src/media/types.ts:1`); 7.2.1 reassignment already
moves every referencing product with no state filter
(`src/auth/product-category.ts:223-235`), matching the plan's default rule with a
clear execution-time owner; 7.1.2 is still in 003 and the newest integrated
migration is `20260724120000`, so the lane-preflight rule is coherent; V1 product
delete is DB-restricted (`prisma/schema.prisma:233`, `onDelete: Restrict`).

Blocking reasons:

1. **Archival's "cannot back new payment links" is unenforceable inside the plan's
   own write set, and the only remaining mechanism contradicts the plan's
   "existing links intact" invariant.** New-link eligibility is enforced
   exclusively by the immutable baseline trigger
   `payment_link_require_active_dependencies`
   (`prisma/migrations/20260721020000_payment_links/migration.sql:28-60`,
   BEFORE INSERT, checks only `product.active`) with the error mapping in
   `src/auth/payment-link.ts:62-63` — the trigger is inside the immutable
   19-migration baseline and `payment-link.ts` is on the plan's must-not-edit
   list. The closed safe migration language
   (`pop/scripts/migration-policy.mjs:192-224`: createTable, addColumn,
   createIndex, addConstraint, validateConstraint, grant/revoke only) admits no
   trigger or function replacement, so `archived_at` cannot be added to that
   enforcement path by the new migration either. The plan names no mechanism at
   all; as written the executor cannot deliver the invariant without exceeding
   the declared write set or the safe language.

2. **The two mechanisms left open each falsify a stated plan claim — the plan
   must choose and record the consequence.** (a) If archival sets `active=false`
   (the only in-contract way to engage the baseline trigger), existing links to
   the archived product stop rendering at checkout, because
   `src/checkout/public-checkout-presentation.ts:45` requires
   `product: { is: { active: true } }` for an existing link to resolve (the route
   test already treats `product-unavailable` as an unavailable outcome) — this
   contradicts the plan's "leaves existing links/orders intact" / "Existing
   links and orders are untouched". (b) If archival leaves `active=true` and
   relies on `archived_at` alone, nothing blocks new payment links for archived
   products: the link-creation picker (`src/auth/payment-link.ts:167-168`,
   `where: { ownerId, active: true }`) would still offer them and creation would
   succeed — contradicting "cannot back new payment links". The risk section
   flags the category-reassignment interaction but never this one.

Required 002 revision: pick one explicit archival mechanism and make the plan's
invariants, ACs, and spec/DOX impact consistent with it — e.g., declare archival
as terminal `active=false` + `archived_at`, narrow "links intact" to data-level
(link rows and policy-exact order snapshots preserved; checkout presentation of
existing links becomes unavailable, mirroring V1 deactivation) and record that
in `pop/specs/catalog-and-payment-links.md`; or propose an explicit contract
change (e.g., a governed safe-language extension for trigger replacement) with
its `prisma/AGENTS.md` impact. Either resolution keeps the verified-consistent
currency/image/category fronts unchanged.

devolvido por revisor independente (yolo) — 2026-07-24

## Round 2 (yolo critic, strong, fresh session)

### Resposta do crítico (yolo)

**Verdict: APPROVE.**

Both round-1 blocking findings are resolved with mechanisms verified against the
repo:

1. **Enforcement now lives inside the declared write set.** Archival is one CAS
   mutation in `src/auth/product.ts` (Front A owns it; the existing
   `setActive`/`updateMany` version-CAS pattern at `src/auth/product.ts:150-157`
   is the exact template). New-link ineligibility needs no new surface: the
   immutable baseline trigger `payment_link_require_active_dependencies` is
   BEFORE INSERT and checks only `product.active`
   (`prisma/migrations/20260721020000_payment_links/migration.sql:28-60`), and
   the picker already filters `active: true`
   (`src/auth/payment-link.ts:166-168`). Neither file is edited, and the safe
   migration language is not extended.

2. **The data-level vs checkout-level distinction is stated honestly and
   consistently.** The decision text, the "Archival overclaim" risk, the
   acceptance criteria, and the spec-impact section all scope "links intact" to
   preservation of link rows and policy-exact order snapshots, and state
   head-on that checkout presentation of links to archived products becomes
   unavailable — matching `public-checkout-presentation.ts:45`
   (`product: { is: { active: true } }`) and V1 deactivation semantics
   (`setActive(active=false)` exists today). No AC claims stronger
   preservation.

Additional spot-checks: reassignment is confirmed state-agnostic
(`src/auth/product-category.ts:222-235`, no `active` filter on the product
update), consistent with the plan's explicit resolution; the apparent tension
between "archived products cannot be assigned a category" (product-edit path)
and category-side reassignment is addressed in the plan text and is not a
contradiction. Unchanged sections remain correct:
`NoActiveExchangeCurrencyMappingError` exists
(`src/auth/supported-exchange-currency.ts:31,114`), `PRODUCT_IMAGE` exists
(`src/media/types.ts:1`), V1 public DTOs are unchanged claims hold, the newest
integrated migration is `20260724120000_supported_exchange_currencies`, and
7.1.2 is still in 003 — so the lane-preflight rule is coherent. Single
serialized front, no `(user)` items, re-run criteria present. No new
contradictions found.

aprovado por revisor independente (yolo) — 2026-07-24
