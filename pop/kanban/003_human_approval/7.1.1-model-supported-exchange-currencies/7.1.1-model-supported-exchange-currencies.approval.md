# Approval — [[7.1.1-model-supported-exchange-currencies]]

## Round 1 — 2026-07-24

### Resposta do crítico (yolo)

**Decision: RETURN to 002_planning** (return 1/2).

The plan correctly embodies the human directive (no closed code list, no build-time/authenticated Nautt research, UUIDs as admin-registered runtime data, deterministic BRL-or-explicit-none default with no fallthrough, correct ADMIN/USER role boundary per the admin contract, no public discovery surface, concrete typed gating signal for 7.1.2/7.2.2, serialized fronts and migration lane). Two blocking contradictions remain:

1. **AC2 contradicts the plan's own risk fallback and the closed migration language.** AC2 requires "PostgreSQL enforces ... at most one ACTIVE mapping per code" and its pass condition asserts a second active mapping per code fails under `pnpm db:test`. The closed `migration.safe.json` vocabulary cannot express that: `createIndex` in `pop/scripts/migration-policy.mjs` emits only plain column lists (no partial/WHERE or expression index), and row-local CHECK constraints cannot enforce a cross-row invariant. Yet the Risks section simultaneously authorizes "application-only enforcement with the serializing transaction" as an acceptable fallback. AC2, the F01 delivery text ("database-enforced single-active-per-code"), and the fallback are mutually exclusive. The replan must commit to one mechanism — note that a single-active pointer table (e.g. `code UNIQUE` + `pair_id UNIQUE NOT NULL` FK `ON DELETE RESTRICT`) **is** expressible in the safe language via `createTable` + `foreignKey` — and align AC2, the risk, and the abort conditions with that choice.

2. **The immutable-history registration model collides with an existing constraint the plan never mentions.** `CatalogCurrencyPair` already carries `@@unique([currencyUuid, exchangeCurrencyUuid], map: "catalog_currency_pair_uuids_key")` (`prisma/schema.prisma:148`). The plan's "registration creates a new row; historical rows are never deleted or remapped" makes re-registering an identical UUID pair (restore after mistaken deactivation, or replace then replace-back) violate that unique constraint, and the plan's own abort conditions forbid destructive constraint replacement — leaving no sanctioned path. The replan must define explicit semantics against this constraint: either typed rejection of re-registering a historically seen pair (with register requiring an existence lookup), a defined restore/reactivate path, or a sanctioned non-destructive constraint evolution.

Non-blocking observations for the replan:

3. Roadmap text is now stale: `pop/roadmap/7-merchant-settings-and-catalog.md` still describes 7.1.1 as "Model active BRL, ARS, BOB, and COP" and Phase 7.1 as the "closed currency catalog", contradicting the binding directive. F03's deny list forbids roadmap edits, so no front owns the sync — assign it explicitly (F03 or 006 closeout).
4. F02 leaves its own subtree (`src/auth/**` vs a dedicated `src/currencies/**`) to a 004 decision; acceptable, but the chosen location must be recorded in the root AGENTS.md contract added by F03.

— devolvido por revisor independente (yolo) — 2026-07-24

## Round 2 — 2026-07-24

### Resposta do crítico (yolo)

**Decision: APPROVE.**

Round-1 finding 1 resolved: the pointer table is fully expressible in the closed language — verified against `pop/scripts/migration-policy.mjs`: `createTable` (columns incl. `varchar(3)`), `addConstraint` with `primaryKey`/`unique`/`foreignKey` (`RESTRICT` is in `REFERENTIAL_ACTIONS`) and `check` (`gte`/`lte` + `and` + string literals cover `code >= 'AAA' AND code <= 'ZZZ'`); `createIndex` is plain-columns-only and the plan cites no partial index. PK-on-`code` gives database-enforced single-active-per-code; the application-only fallback is deleted and the abort condition now requires DB enforcement. Round-1 finding 2 resolved: `catalog_currency_pair_uuids_key` (`prisma/schema.prisma:148`) is preserved unchanged, and the in-transaction lookup → typed duplicate-pair conflict / re-point-to-retained-row restore is coherent with immutable pair rows (only the pointer row mutates, which is legitimate configuration DML). Round-1 observations resolved: F03 owns the stale roadmap wording ("closed currency catalog" / "BRL, ARS, BOB, and COP" confirmed at `pop/roadmap/7-merchant-settings-and-catalog.md:18,22`) and the root `AGENTS.md` subtree record for F02's chosen location. Binding conditions hold: dynamic admin-registered UUIDs with no closed code list, DB-state gating with typed errors and discriminated UI-prohibition signal, deterministic BRL-or-explicit-none default with no fallthrough, ADMIN-only mutation with redacted USER reads per the admin contract, serialized fronts with complete owns/deny, preflight migration-lane check, and all AC commands exist in `package.json` (`db:migration-policy`, `db:contract-check`, `db:generate`, `db:test`, `vitest run`, `check`).

Non-blocking correction for 004: the plan's parenthetical "createTable, inline constraints" is imprecise — `createTable` in the safe language accepts only `op`/`table`/`columns`; PK/UNIQUE/FK/CHECK are separate `addConstraint` operations. The mechanism is identical and the generator fails closed, so this changes no outcome; F01 must write the manifest as `createTable` + `addConstraint` operations.

— aprovado por revisor independente (yolo) — 2026-07-24
