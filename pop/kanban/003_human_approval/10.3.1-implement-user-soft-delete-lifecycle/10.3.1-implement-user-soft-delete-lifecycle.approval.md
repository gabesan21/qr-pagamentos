# Approval — [[10.3.1-implement-user-soft-delete-lifecycle]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read with reality checks against the live tree; every load-bearing claim in the plan verified:

- **Decision 1 (marker + `DISABLED`) is the only expressible model.** `user_status_closed CHECK (status IN ('ACTIVE','DISABLED'))` sits in baseline migration `20260714190000_local_identities`, one of the 19 hash-pinned immutable directories (`prisma/AGENTS.md:24`), and the safe migration language admits only additive constructs — no check replacement (`prisma/AGENTS.md:27-28`). A `DELETED` status is indeed permanently inexpressible. Pairing `deleted_at` with `DISABLED` keeps every existing gate byte-identical, verified: sign-in `credential.status !== "ACTIVE"` (`src/auth/session.ts:70`), principal resolution (`src/auth/authorization.ts:29`), admin guard (`src/auth/administration.ts:29`), `countActiveAdmins` filters `status: "ACTIVE"` (`src/auth/administration.ts:104`).
- **Decision 3 (withdrawal via service writes) is safe.** The `payment_link_require_active_dependencies` trigger is `BEFORE INSERT` only (`prisma/migrations/20260721020000_payment_links/migration.sql:59-62`); the only other trigger in history is `deployment_bootstrap_immutable`. Bulk `active=false` UPDATEs cannot trip anything. Public withdrawal is real, not cosmetic: V1 public reads filter `active: true` (`src/checkout/public-checkout-presentation.ts:45`, raw SQL at `src/checkout/public-checkout.ts:132`), V2 public read filters active (`src/auth/public-payment-link-v2.ts:115`), and even in-flight capabilities re-check `paymentLink.active` at status resolution (`src/checkout/public-checkout.ts:71`). Storefront projection already requires `storefrontEnabled: true` (`src/storefront/public-storefront.ts:187`) — zero projection edits needed.
- **Decision 2 (terminality) is coherent.** `mutateAdminSafety` exists with the advisory-lock + final-admin pattern (`src/auth/administration.ts:33-44`); a deleted admin is `DISABLED` and never counted, so final-active-admin safety composes. MFA genuinely does not exist (TOTP is future task 11.1.3, sitting in 001) — the plan honestly resolves the card's "session/reset/MFA revocation" wording instead of pretending to deliver MFA.
- **Decision 5 (append-only audit) mirrors proven precedent.** V2 append-only tables with `SELECT`/`INSERT` runtime grants and relation-guarded bootstrap re-pin exist (`prisma/AGENTS.md:45`); the FK-less actor column is the right call so actor deletion never fences audit history.
- **Route/observability pattern is real.** `/admin/users/[id]/{password,role,status}` subroutes run origin-guard first, opaque redirects, one literal `server-request-log` entry each (`src/app/admin/users/[id]/status/route.ts`, `src/observability/server-request-log.ts:32-36`). The new delete subroute follows it exactly.
- **FKs are `Restrict` across orders/products/links** (`prisma/schema.prisma`) — nothing cascades; attribution survives.
- **The 9.2.1 collision is real and honestly handled.** 9.2.1 is in `004_processing` and also owns `prisma/schema.prisma` plus a new migration (`standalone_checkout_attempt`). F01's "wait for merge, regenerate/re-timestamp against then-current develop" is the correct serialization, not hand-waving.

#### Criteria checked

1. **Verifiable deliverable — pass.** All 7 acceptance criteria have objective execution (named `pnpm` commands) or inspection (diff/reading) per row; pass conditions are explicit ("all exit 0", "frozen files absent from diff").
2. **Sufficient lean brief — pass.** 106 lines: pinned decisions with rationale, fronts with Owns/May-read/Must-not-edit, abort conditions, criteria mapped to fronts. No implementation prose beyond what the decisions require. Appropriate for size L critical.
3. **Safe execution — pass.** Three strictly sequential fronts, single executor, write sets disjoint per front (prisma/ → src auth/admin/observability → docs), hard serialization point (migration) declared with its cross-task dependency. No unsafe parallelism claimed.
4. **Proportional specs linked — pass.** administrative-foundation (lifecycle home), checkout-and-order-lifecycle (attribution preservation), product-scope (confirm-only) — all three card links land in F03 with scoped expected changes.
5. **No avoidable `(user)` item — pass.** None in the plan.
6. **Proportional to risk/size — pass.** L/critical: one migration, one service extension, one route, docs sync. No speculative UI (correctly deferred to 10.3.2), no new public surface.
7. **Economic verification — pass.** No web anywhere; criteria 1–5 and 7 are `re-run` (five distinct command gates plus diff inspection on 4); criterion 6 is read-only evidence, correctly not re-run. No redundant gates spotted — `pnpm container:contract-check` is justified because bootstrap.sql is touched.

#### Risk assessment

Residual risks the 005 gate (full verification, mandatory for critical) must check:

- **R1 — Migration sequencing.** 9.2.1 is still in `004_processing` and owns the same write set. F01 must not execute until 9.2.1 merges into `develop`; 005 must verify the delivered migration timestamp sorts after 9.2.1's and the history verifies with both present.
- **R2 — DTO leak via shared `toUserDto`.** `src/auth/identity.ts:59-69` whitelists DTO fields; adding `deletedAt` changes every consumer's shape. 005 must enumerate all `toUserDto` consumers and prove none feeds a public/merchant/sessionless serialization (absence assertions required by criterion 5).
- **R3 — Fence completeness.** Terminality fences must cover `changeStatus` (both directions), `changeRole`, and `changePassword` — including that `DISABLED`-reversible suspension semantics are unchanged for non-deleted users, and that self-deletion by a non-final admin behaves like existing self-disable (sessions revoked atomically).
- **R4 — In-flight financial state.** Deleting an owner with PENDING provider orders: webhooks must still reconcile (attribution rides provider-order UUID, not sessions) — verify no new error path and no public reachability; capability status reads post-withdrawal must return the existing opaque unavailable (`src/checkout/public-checkout.ts:71` re-checks `active`).
- **R5 — Audit table least privilege.** Grants exactly `SELECT`/`INSERT` with the bootstrap relation-guard re-pin block, mirroring the V2 append-only precedent; actor column carries no FK; one row per deletion inside the same transaction (rollback test required by criterion 2).
- **R6 — Opaque outcome uniformity.** Unknown, already-deleted, and final-admin targets share one redirect cause; 005 verifies no oracle distinguishes them and that the request-log inventory gains exactly one literal entry.

aprovado por revisor independente (yolo) — 2026-07-26
