# Frente F01 — Schema and migration baseline rebase — [[15.1.2-remove-v1-persistence-and-rebase-migrations]]

- **Entrega:** a V2-only declared model and a V2-only migration history, with the baseline inventory, the verifier anchor and the policy fixture re-pinned in the same change.
- **Escopo:** the four V1 models and their back-relations, the `provider_order.payment_link_order_id` column with its two unique maps, the V1 baseline directories, the pinned inventory and the two policy scripts. Nothing else: no new migration, no `db push`, no renaming or renumbering of surviving directories, no new DDL, no replacement trigger.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card and the plan's *Estratégia*, *Riscos* and *Critérios* sections; [[pop/notes/decisions/2026-09-22-v1-removal-and-checkout-decisions|2026-09-22 decisions]] (destructive authorization); [[pop/researches/pix-checkout-review/2026-09-22-review-findings|review findings]] (Persistence item of the V1 footprint); esta fatia.
- **Owns:** `prisma/schema.prisma`, `prisma/migrations/**`, `prisma/migration-policy-baseline.json`, `pop/scripts/migration-policy.mjs`, `pop/scripts/migration-policy-contract.mjs`.
- **May read:** `prisma/bootstrap.sql`, `prisma/AGENTS.md`, `pop/scripts/db-contract-check.mjs`, `pop/scripts/db-test.mjs`, `install/update.sh`, `src/**`, `pop/specs/administrative-foundation.md`.
- **Must not edit:** `pop/scripts/db-contract-check.mjs` and `db-test.mjs`, `prisma/bootstrap.sql`, `prisma/AGENTS.md`, `README.md`, `install/**`, `container/**`, `src/**`, `pop/specs/**`, any other `AGENTS.md` — todos são de F02 ou de outra task.
- **Depends on:** nenhuma · **Entrada esperada:** nenhuma (15.1.1 já fechada é pré-condição da task, não desta frente).
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use antes da primeira edição e a cada decisão de contrato*.
- **Web:** deny · **Gate/delta:** 003_human_approval (task `critical`) · nenhum delta.
- **Saída:** relatório curto ao agente principal com a nova contagem de baseline, o novo digest âncora, os ids de diretório removidos/editados e a lista de arquivos alterados; status `concluída | BLOCKED`.
- **Critérios:** C01, C02, C03, C09 do [[15.1.2-remove-v1-persistence-and-rebase-migrations.plan|plano]].

## Contrato de execução

- Caminhe [`prisma/AGENTS.md`](../../../../../prisma/AGENTS.md) antes da primeira edição; esta frente só o lê, não o reescreve.
- A rebase é cirúrgica: três diretórios inteiramente V1 saem, `20260721030000_owner_isolation_checkout_policy` perde apenas seus statements de `payment_link`, e nenhum diretório posterior ao baseline muda um byte.
- Inventário, âncora do verificador e contagem da fixture andam juntos; recompute o digest por último, a partir dos bytes finais do JSON canônico.
- A linguagem fechada das migrações futuras **não** ganha operação nova; a autorização destrutiva se esgota nesta reescrita histórica.
- Não rode Docker nem nenhum comando do projeto sob container; o gate de teste é a task de verificação da phase.
- Entregar somente o escopo e os critérios desta frente.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Números finais:** baseline count · último id do baseline · digest âncora · diretórios removidos/editados.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <observação relevante>.
