# Frente F03 — Parity gate hygiene — [[15.4.2-fix-target-sizes-and-dictionary-debt]]

- **Entrega:** semantic derivation bound to the git-tracked tree (a present `src/generated/prisma` can no longer reach `obligations.ndjson`), a probe proving it inside `--semantic-mutation-probes`, the two checkout loading routes refreshed by the official operation, and the README stating the boundary.
- **Escopo:** the parity checker and its contract folder. Two defects, one order: fix the discovery boundary first, then run the official refresh — a refresh taken under the old boundary can bake the generated tree in. Diagnose each of the two routes before acting: `PARITY_CURRENT_ROUTE_MISSING` means no record exists (registration), a stale source hash means the record exists and needs refreshing.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `15.4.2-fix-target-sizes-and-dictionary-debt`, seções "Objetivo", "Estratégia" e "Critérios" de [[15.4.2-fix-target-sizes-and-dictionary-debt.plan]], esta fatia, [`docs/frontend-template-parity/README.md`](../../../../../docs/frontend-template-parity/README.md) (seção "Regeneration and validation"), [[pop/memory/2026-09-23/15.1.4-phase-verification.03-out-of-reach-follow-ups|follow-ups 15.1.4]] (F2), [[pop/memory/2026-09-23/15.3.4-phase-verification.02-out-of-reach-follow-ups|follow-ups 15.3.4]] (F5), [[pop/memory/2026-09-22/15.1.3-rewrite-contracts-and-parity-without-v1.02-parity-retirement-unblocked|15.1.3 parity retirement]] e [[pop/memory/2026-09-22/15.1.5-create-missing-parity-current-route-records.01-registration-and-anomaly-decision|15.1.5 registration decision]] para as operações de aposentadoria e registro já existentes no checker.
- **Owns:** `scripts/check-frontend-template-parity-contract.mjs`, `docs/frontend-template-parity/**` (manifest e obligations **somente** como saída das operações oficiais).
- **May read:** `src/**`, `scripts/**`, `docs/**`, `.gitignore`, `pop/specs/**`, `pop/memory/**`.
- **Must not edit:** `src/**`, `DESIGN.md`, specs, DOX, cards, e qualquer arquivo fora de `Owns`.
- **Depends on:** F01 e F02.
- **Entrada esperada:** F01 e F02 integradas na worktree da task — `src/**` (inclusive a rota `src/app/admin/payment-links/page.tsx`) na forma final. Sem isso, o refresh nasce obsoleto: responda `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para contrato antes do código e mudança mínima em script de gate*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval (não-yolo); sem delta nesta rodada.
- **Saída:** diff no checker e no README + manifest/obligations regenerados pelas operações oficiais + seção "Resultado" desta fatia, teto 50 linhas, evidência: comandos executados verbatim com sua saída de sucesso e o antes/depois de contagem de registros; status `concluída | BLOCKED`.
- **Critérios:** C7, C8, C9, C10 do [[15.4.2-fix-target-sizes-and-dictionary-debt.plan|plano]].

## Contrato de execução

- **Fronteira de descoberta.** A derivação semântica hoje varre `filesUnder("src")` (l.260) sem filtro. Limite-a ao conjunto rastreado pelo Git, reaproveitando o mecanismo que o próprio script já usa (`git ls-files`, l.329) em vez de inventar outra convenção; a alternativa aceitável é uma exclusão declarada de `src/generated/**`, e a escolha fica registrada no Resultado com a razão. O filtro vale para a descoberta/derivação — não relaxe nenhuma validação existente nem mude IDs, dispositions, owners ou evidence targets.
- **Sonda obrigatória.** Acrescente a `--semantic-mutation-probes` um caso que prove a fronteira: com um arquivo sob `src/generated/**` presente em disco, o contrato derivado permanece idêntico (mesmos IDs e mesma contagem). A sonda não deixa resíduo no repositório e não altera arquivos versionados.
- **Nunca edite `manifest.json` nem `obligations.ndjson` à mão** — nem para "consertar" um campo. Só `--refresh-semantic-contract` e `--register-missing-current-routes` escrevem lá, cada um no caso que lhe cabe.
- Abort do refresh (`PARITY_REFRESH_*`, `PARITY_REGISTER_*`) é resultado, não obstáculo a contornar: reporte o código literal. Se sanar exigir tocar registro de rota alheia ao delta F5, responda `BLOCKED`.
- README: atualize a seção "Regeneration and validation" para declarar a fronteira de derivação e a sonda nova. Uma frase por fato; não duplique o contrato da spec.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Rodar `pnpm frontend-parity:check` e as sondas é critério **`phase`** (task de verificação da phase); esta frente entrega o diff e as saídas das operações oficiais que precisou executar para regenerar o contrato.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Operações oficiais executadas:** <comando → saída>. **Registros antes/depois:** <n → n>.
- **Fronteira adotada:** <git-tracked | exclusão declarada> — <razão>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <observação relevante>.
