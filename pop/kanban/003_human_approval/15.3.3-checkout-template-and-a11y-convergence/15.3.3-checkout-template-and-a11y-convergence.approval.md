# Aprovação — [[15.3.3-checkout-template-and-a11y-convergence]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-22

### Brief para decisão

- **Entrega:** the public checkout surface obeys the vocabulary and the AA rules already declared in `DESIGN.md` — one polite live region announcing the payment state, the `INDETERMINATE` line on a soft warning surface, a locale-aware `loading`, the declared `max-w-checkout` / `font-display` / `bg-accent` utilities in place of ad-hoc forms, a 44 px privacy trigger, and a scroll to the first invalid field that respects reduced motion.
- **Estratégia:** This is the phase's last task and its **conflict integrator**: it rebases over 15.3.1 (explicit no-retry states) and 15.3.2 (`NONE` customer block) and resolves whatever residue those two leave in the two adapters, without re-opening their decisions. Scope is the **public checkout surface only** (`src/app/pay/[identifier]/**` plus `src/app/store/[slug]/pay/loading.tsx`): finding A9's wider drift (`text-muted-foregrou…
- **Topologia:** executor único — one cohesive front over one contiguous surface, a single skill pair (`ui-change` + `clean-code-change`) on the same diff; splitting the shared vocabulary decision across contexts would invite inconsistency without buying parallelism. Perfis fixos por papel nativo; `size: M` limita orçamento e profundidade apenas.
- **Risco principal:** the accent whitelist in `scripts/check-design-tokens.mjs` is keyed by exact path and expression, so the `bg-primary` → `bg-accent` swap must leave the `--storefront-accent` inline style byte-identical — mitigation: that attribute is out of scope and the script is `must_not_edit`; needing to widen the whitelist aborts.
- **Critérios principais:** C1, C2, C3, C4, C5, C6, C7, C8, C9, C10.
- **Plano:** [[15.3.3-checkout-template-and-a11y-convergence.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
