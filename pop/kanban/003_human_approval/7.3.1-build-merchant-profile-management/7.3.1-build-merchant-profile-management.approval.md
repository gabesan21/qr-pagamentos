# Approval — [[7.3.1-build-merchant-profile-management]]

- **Stage:** 003_human_approval · **Responsible:** independent yolo reviewer

## Round 1 — 2026-07-24

### Resposta do crítico (yolo)

- **Context:** strong independent.
- **Return:** 1 of 2.
- **Decision:** returned → 002.
- **Original request:** the plan directly addresses the requested merchant-only profile area for username, optional contact email, and current-password-proven rotation. It correctly preserves username-only login, role-exclusive panels, administrator and storefront boundaries, complete session revocation, and a secondary profile affordance outside the fixed five-entry merchant navigation.
- **Resolved strengths:** origin denial precedes authorization and parsing; page and POST role outcomes are fail-closed; identity writes use a dedicated nonnegative version and exact CAS; password replacement and session deletion are atomic; sign-in, merchant rotation, and administrator replacement converge on one per-user serialization boundary with deterministic interleaving coverage; the additive migration, runtime grants, bilingual owned-component UI, and sequential F01→F02→F03 topology are proportionate. The current active media-operations worktree does not touch Prisma, migrations, or database fixtures, so the serialized migration lane is presently free.
- **Concrete blockers:**
  1. The current-password side-channel contract is missing. The plan preserves unknown-username timing work for login, but it requires only an opaque response for wrong, malformed, missing/stale, or same-password profile cases. In the current implementation, malformed plaintext or a malformed credential record can return before scrypt, so response equality alone does not prove the requested current-password timing property. State which profile-rotation failure classes must execute the same bounded credential-verification work, require current-password proof before any replacement, and add deterministic instrumentation/interleaving evidence (not a flaky wall-clock threshold) that would fail on an early-return path.
  2. The mutation wire contract is not closed enough to verify. Password success is fixed as `/login?password=changed`, but identity success, identity conflict/failure, and password failure have no exact relative redirect vocabulary. AC1 merely says redirects are “closed,” allowing implementation-chosen outcomes and tests to approve themselves. Define the complete finite redirect set and map every validation, CAS, uniqueness, proof, and unavailable outcome to it without echoing submitted or conflicting data.
  3. AC4, AC6, and AC7 are not executable as written. “Deterministic interleaving tests,” “focused page/component/dictionary tests and pinned shadcn preflight record,” and “focused production profile evidence command and its manifest verifier” name neither commands nor artifact/verifier paths. Replace these placeholders with exact commands (and the run-bound manifest/review paths or naming contract), including both sign-in/rotation and admin-reset/rotation lock orderings, all six themes × two locales × three widths, native POST state assertions, and the verifier that rejects stale or incomplete evidence.

## Round 2 — 2026-07-24

### Resposta do crítico (yolo)

- **Context:** strong independent.
- **Decision:** approved.
- **Original request:** the repaired plan directly and verifiably delivers the separate active-merchant profile area for normalized username, optional contact email, and current-password-proven rotation. It preserves username/password-only login, role-exclusive panels, the fixed five-entry merchant navigation, opaque uniqueness handling, and the intended distinction between identity edits that retain sessions and password changes that revoke every session.
- **Timing/work-factor blocker resolved:** every authorized parsed password attempt now performs exactly one instrumented verification KDF at the fixed scrypt parameters before its application outcome. Correct and wrong valid plaintext use the observed credential; malformed plaintext, malformed record, or an unavailable target fall through one valid dummy credential/plaintext path whose result is forced false; stale/CAS, invalid-new, mismatched-new, same-password, and success paths are named explicitly. The plan requires callback-count assertions and replacement-denial assertions and correctly claims bounded expensive-work shape rather than wall-clock equality. Only proof against the exact observed credential can reach the separate new-password hash and conditional replacement.
- **Wire contract blocker resolved:** the response vocabulary is finite and exact: identity uses only `303 /profile?identity=changed|conflict|failed`, password failure uses only `303 /profile?password=failed`, and successful rotation uses only `303 /login?password=changed`. The table fixes session mutation and `Set-Cookie` behavior for every class; origin, missing-principal, and wrong-role denials remain empty `403`/`401`/`403` with neither redirect nor cookie.
- **Executable-evidence blocker resolved:** AC4 names the concurrency/session/administration test files and both sign-in/rotation and administrator-reset/rotation orderings. AC6 names the pinned `shadcn@4.13.0` preflight command, its `artifacts/profile/shadcn-preflight.json` record, and every focused page/component/route/shell/dictionary test. AC7 names the evidence runner and verifier and binds a 14-digit UTC run to 36 six-theme/locale/viewport base captures plus 14 localized interaction captures, exactly 50 PNGs and 53 run files, a sole current pointer, recomputed hashes, click-and-Enter single-submit assertions, session/cookie/password proofs, axe/runtime checks, and rejection of visual severity 2 or higher.
- **Full-plan judgment:** one per-user serialization boundary covers sign-in, merchant rotation, administrator replacement, session creation, and revocation with deterministic interleavings; the additive profile-version migration has a free serialized Prisma lane; the F01→F02→F03 topology prevents interface and evidence invention; UI states, bilingual copy, native form semantics, 320px reflow, focus, targets, specs, DESIGN, DOX, logging redaction, and AC1–AC8 are proportionate and objectively inspectable. Missing database or browser runtime evidence remains inconclusive at mandatory full 005 rather than being simulated.

aprovado por revisor independente (yolo) — 2026-07-24
