# Approval — [[1.2.2-establish-local-identities]]

- **Stage:** 003_human_approval · **Owner:** yolo critic

## Round 1 — 2026-07-14

### Plan summary

Establish server-only identity/password contracts, add an immutable least-privilege identity migration, and integrate file-secret deployment seed and operator recovery. Verification covers unit, database, container, installer, clean-build, documentation, and DOX contracts. Plaintext leakage, ambiguous recovery, broadened database privileges, immutable-history edits, and unavailable live evidence are explicit abort conditions. Full plan: [[1.2.2-establish-local-identities.plan]].

### Yolo critic response

- **Returned to 002.** The plan is not yet blind-executable or economically verified:
  1. `1.2.2-establish-local-identities.plan.md:25,37` delegates security-critical `scrypt` choices to the executor: no exact versioned record grammar, `N`/`r`/`p`, salt/key lengths, `maxmem`, or parser bounds are fixed, while the fork only says to tune a parameter record within an unspecified memory ceiling. Fix the security contract and observable vectors/limits in the plan so implementation and malformed-record handling require no new cryptographic judgment.
  2. `1.2.2-establish-local-identities.plan.md:29` and `subtasks/1.2.2-establish-local-identities.g03-secret-seed-operator-recovery.md:12-14` require generated and operator-supplied initial-admin email/password files but never define the generated email value/rule or the exact non-secret operator input path. This leaves a product/operational identity decision to execution and makes the seed/recovery route non-reproducible. Specify deterministic email provisioning, file paths/formats, and how recovery continues to identify the originally seeded row if the input file changes; otherwise add an objective abort/fork.
  3. `1.2.2-establish-local-identities.plan.md:52,56` runs `pnpm test` independently and then runs `pnpm check`, whose declared aggregate already includes the same test suite. Remove the redundant full-suite run or replace it with a genuinely focused, non-duplicative identity test command/evidence strategy.

- [x] Done — returned

devolvida pelo crítico (yolo) — 2026-07-14

### Agent decision

- **Accepted all three objections.** The amended plan fixes the complete `scrypt` grammar, parameters, resource/parser bounds, and malformed vectors; requires operator-supplied email at exact ignored/staged paths while generating exact protected passwords; persists an immutable singleton bootstrap marker so recovery never depends on mutable email; and removes the standalone `pnpm test` run in favor of the single aggregate `pnpm check` execution.

## Round 2 — 2026-07-14

### Plan summary

The amended plan pins the complete password-record security contract, requires a deterministic operator-provided initial-admin email, persists a singleton recovery marker, defines exact secret paths and retry-safe password rotation, and removes duplicate unit-suite execution. The four-group scope and executable database/container/installer/application evidence remain unchanged. Full plan: [[1.2.2-establish-local-identities.plan]].

### Yolo critic response

- **Returned to 002.** Round 1 fixed the record grammar, deterministic operator input, and duplicate suite, but three blocking contracts remain:
  1. `1.2.2-establish-local-identities.plan.md:27,31,45,54` makes the initially seeded row permanently undeletable even when another active administrator exists, while the approved spec only forbids removal/demotion of the **final active administrator** (`pop/specs/administrative-foundation.md:28`). It also allows the marked row to be disabled/demoted, so the marker is neither the final-active-admin invariant nor a neutral recovery locator. Resolve this product-contract divergence explicitly; do not silently encode a privileged immortal account to answer Round 1.
  2. `1.2.2-establish-local-identities.plan.md:18,54-57` prepends `$HOME/.local/share/pnpm`, but the pinned binary is in `$HOME/.local/share/pnpm/bin`; the exact advertised commands resolve `/usr/bin/node` v26.4.0, not v24.18.0. The pinned Node 24.18.0 does execute the fixed `scrypt` parameters successfully, but every acceptance run must select it observably.
  3. `1.2.2-establish-local-identities.plan.md:25,29,31` and `subtasks/1.2.2-establish-local-identities.g03-secret-seed-operator-recovery.md:12-17` refer to an “invalid email” and an explicit recovery mount/invocation without defining either an executable email syntax contract or how Compose supplies the recovery-only secret while normal seed startup does not require that absent file. Pin the validator and the recovery service/profile/override plus exact secret mount/order behavior so the executor does not invent security-sensitive topology.

- [x] Done — returned

devolvida pelo crítico (yolo) — 2026-07-14

### Agent decision

- **Accepted all Round 2 objections.** Replaced the immortal user marker with an immutable singleton no-FK locator that preserves normal user disable/demotion/deletion and aborts recovery for a deleted UUID; pinned the exact PATH plus observable versions for every gate; fixed an executable ASCII email grammar/vectors; and isolated the recovery secret/service in `compose.recovery.yaml`, loaded only by the exact explicit `--no-deps` recovery invocation.

## Round 3 — 2026-07-14

### Plan summary

The final amendment uses an immutable no-FK deployment locator without restricting the user lifecycle, selects pinned Node/pnpm observably in every gate, fully specifies canonical email syntax and vectors, and isolates recovery in an opt-in Compose override that normal startup never loads. All prior password, secret, privilege, idempotency, test-economy, and abort contracts remain explicit. Full plan: [[1.2.2-establish-local-identities.plan]].

### Yolo critic response

_(pending final fresh adversarial review; a third return blocks for human intervention)_

- [ ] Done

### Agent decision

_(filled after the critic response)_
