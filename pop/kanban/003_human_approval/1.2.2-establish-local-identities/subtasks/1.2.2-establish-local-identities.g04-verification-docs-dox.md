# Group g04 — Verification, docs, and DOX closeout — [[1.2.2-establish-local-identities]]

- **Order:** 4.
- **Depends on:** g01, g02, g03.
- **Description:** Produce economical independent evidence and leave operator/spec/DOX contracts aligned with the delivered identity slice.

## Movements

- [ ] (agent) **g04.m1** Review the diff for one intention per module, server-only secret handling, no public registration/reset/MFA/session scope, no unrelated refactor, and no generated/secret artifacts.
  - Expected: changed code maps directly to g01–g03; `git diff --check` is clean; ignored secret/generated files are not staged.
  - If it fails: local safe cleanup is needed → fix it and rerun focused tests; broad debt becomes a tracked follow-up, while scope/security drift returns to 002.
- [ ] (agent) **g04.m2** First record exact `node --version`/`pnpm --version`, then run `pnpm db:generate`, `pnpm db:contract-check`, elevated `pnpm db:test`, `pnpm container:contract-check`, both named clean-clone scenarios, and `install/test.sh`; prepend every command with exact `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH"`.
  - Expected: every independent gate exits 0 and notes retain redacted positive/denial/order/idempotency/recovery evidence.
  - If it fails: classify product failure versus Docker isolation → repair product failures within the plan; request scoped elevation for isolation; persistent missing live evidence aborts.
- [ ] (agent) **g04.m3** Run/record `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH" pnpm check` once after specialized gates; do not run full `pnpm test` separately.
  - Expected: typecheck, lint, the sole full unit-suite execution (including exact scrypt/email/DTO vectors), and production build exit 0 on Node 24.18.0/pnpm 11.13.0.
  - If it fails: generated client or test/build ordering is stale → rerun generation and the failing constituent for diagnosis, repair only task-owned behavior, then rerun the aggregate gate.
- [ ] (agent) **g04.m4** Update operator documentation and review/update root and Prisma `AGENTS.md` for changed structure, secret flow, migration/seed ownership, commands, and indexes; prepare the administrative-foundation implemented-slice update for 006.
  - Expected: docs explain initial seed and file-only recovery without secret examples; DOX accurately routes future edits; spec still defers email reset/MFA and remains approved until later slices.
  - If it fails: documentation requires an unsafe manual workaround or contradicts runtime → correct runtime/docs together within approved behavior; material contract divergence returns to 002 and is recorded under spec `Open`.

## Execution notes

- 2026-07-14 — Record exact commands, exit codes, concise redacted evidence, changed documentation/DOX paths, and any debt disposition here for 005 audit.
