# Group g04 — Replacement verification and closeout — [[1.2.2-establish-local-identities]]

- **Order:** 4
- **Depends on:** g01, g02, g03
- **Description:** Re-prove the entire corrected identity slice and align operator, spec, roadmap, task, and DOX contracts.

## Movements

- [ ] (agent) **g04.m1** Review the replacement diff for one username-first intention and run criterion 5's exact bounded no-email-login command plus criterion 6's exact diff/tracked/ignored-artifact commands.
  - Expected: `git diff --check` is silent and all four named PASS lines appear without matches, tracked private artifacts, session/UI scope, or unrelated refactor.
  - If it fails: local cleanup stays in task; broad debt is tracked separately; behavioral drift returns to 002.
- [ ] (agent) **g04.m2** Run exact pinned `db:generate`, database contract/live tests, container contract plus both clean-clone scenarios, and installer tests.
  - Expected: every specialized gate exits 0 with redacted username/nullable-email/UUID recovery/privilege evidence and disposable cleanup.
  - If it fails: repair task-owned behavior, request narrow Docker elevation for isolation, and abort if live evidence remains unavailable.
- [ ] (agent) **g04.m3** Run exact pinned `pnpm check` once after specialized gates and record versions/results; do not duplicate the full unit suite.
  - Expected: typecheck, lint, focused identity tests inside the sole full suite, and production build exit 0 on Node 24.18.0/pnpm 11.13.0.
  - If it fails: diagnose the constituent, repair within scope, then rerun the aggregate once for final evidence.
- [ ] (agent) **g04.m4** Update README, root/Prisma DOX, spec/roadmap implementation notes, and task evidence, then run all four exact criterion 6 documentation-presence commands.
  - Expected: commands print `PASS operator-root-identity-contract`, `PASS prisma-rewrite-exception`, `PASS spec-roadmap-username-login`, and `PASS prior-evidence-superseded` exactly.
  - If it fails: any current executable/docs contract still requires email or permits email login → fix and rerun its owning gate before 005.

## Execution notes

- 2026-07-14 — Record replacement evidence only after all identifier-coupled surfaces have been re-run; prior email-first verification cannot be reused.
