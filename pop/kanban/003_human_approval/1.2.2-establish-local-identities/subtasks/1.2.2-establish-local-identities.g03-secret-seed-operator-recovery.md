# Group g03 — Secret-file seed and operator recovery — [[1.2.2-establish-local-identities]]

- **Order:** 3.
- **Depends on:** g01, g02.
- **Description:** Integrate idempotent first-admin seed and unambiguous server-side recovery into the established deployment/operator boundary.

## Movements

- [ ] (agent) **g03.m1** Add non-shell `seed`/`recover-initial-admin`: seed atomically inserts user/credential then singleton locator with original UUID; rerun validates UUID still resolves with credential and otherwise no-ops; recovery selects only locator UUID, aborting if deleted, then rotates/restores that row without insert/rename.
  - Expected: user role/status may change; changed email cannot retarget; deleted user causes generic abort/no-create; final-active-admin behavior is absent and reserved for 1.2.4.
  - If it fails: exact immutable locator cannot resolve one existing row → stop; never select by email/order or substitute/create.
- [ ] (agent) **g03.m2** Require `INITIAL_ADMIN_EMAIL` in ignored `install/.env`; canonicalize it into mode-0600 `.install-secrets/initial_admin_email`; generate/reuse exact 32-char base64url `.install-secrets/initial_admin_password`; stage both at mode 0400 under `.container-secrets/`; implement retry-safe distinct recovery candidate generation/promotion exactly as the plan states.
  - Expected: tests cover exact source/staged/container paths, one-line email format, missing/blank/multiline/NUL/invalid email, atomic 32-character password generation/reuse, recovery candidate reuse/promotion/removal, redaction, and unchanged data retention.
  - If it fails: a host/container ownership mismatch requires embedding or environment transport → repair mount ownership/readability within existing non-root rules; embedded/environment secrets abort.
- [ ] (agent) **g03.m3** Add base `identity-seed` after migrate with only runtime/email/initial-password secrets; add exact `compose.recovery.yaml` profile/service and installer `--recover-initial-admin` invocation/mount from the plan, using `--no-deps` against the running DB.
  - Expected: base `docker compose config/up` needs no recovery file; only the two-file explicit command parses/mounts `/run/secrets/initial_admin_recovery_password`; no public port/API or secret value argv/env exists.
  - If it fails: Compose cannot express the edge with current wrappers → apply the plan's dependency-graph fork and extend existing contract tooling; never retry a one-shot or duplicate bootstrap/migration logic.
- [ ] (agent) **g03.m4** Extend harnesses for exact email/files, normal config without recovery file, opt-in override invocation/mount, seed/redeploy after email change, UUID-locator recovery, deleted-user abort/no-create, startup gate, and leakage.
  - Expected: scenarios prove one immutable locator, mutable/deletable user lifecycle, isolated retry-safe recovery, and no exposed value.
  - If it fails: Docker isolation alone blocks execution → request exact scoped elevation; if daemon/evidence remains unavailable, abort rather than substituting mocks.

## Execution notes

- 2026-07-14 — Capture redacted installer/static output and clean-clone seed/recovery assertions here; never paste secret material.
