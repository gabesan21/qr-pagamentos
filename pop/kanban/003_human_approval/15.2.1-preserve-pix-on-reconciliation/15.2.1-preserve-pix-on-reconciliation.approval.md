# Aprovação — [[15.2.1-preserve-pix-on-reconciliation]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-22

### Brief para decisão

- **Entrega:** `reconcile` merges the authoritative `GET /orders/{uuid}` into the stored provider order **without** clearing `pixCopyPaste`/`pixQrcodeUrl` when the read omits them — in the Prisma store and in the in-memory double — leaving the versioned compare-and-swap untouched.
- **Estratégia:** `orderData` is shared by `completeCreation` and `reconcile` (`provider-order-store.ts:49-61`) and forces `pixCopyPaste: order.pixCopyPaste ?? null`. Creation writes that explicit `null` onto a fresh row and **stays exactly as it is**; only the reconciliation write changes, by omitting the two PIX keys when the parsed view has no value. Prisma leaves an omitted column untouched and the in-memory spread keeps the curre…
- **Topologia:** executor único — uma frente coesa, uma skill (`clean-code-change`) e um conjunto de escrita indivisível (store + seus testes + a linha de contrato).
- **Risco principal:** the CAS or its guards drift while the payload changes — mitigation: C2 compares the `where` tuple, guards and increment line by line.
- **Critérios principais:** C1, C2, C3, C4, C5, C6, C7, C8.
- **Plano:** [[15.2.1-preserve-pix-on-reconciliation.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
