# Aprovação — [[15.1.6-remove-residual-v1-store-calls]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-22

### Brief para decisão

- **Entrega:** delete the last Prisma calls that still name the dropped V1 models/column, and give the public storefront `products` projection a V2-only rule — no new table, column or migration.
- **Estratégia:** The seven call sites listed in C1 of [[15.1.4-phase-verification.verify]] split into two kinds. **Pure removal**, where the logic existed only for V1: the `deactivatePaymentLinks` member of the administration store (V1 soft-delete withdrawal — `deactivatePaymentLinksV2` already covers the live model), the `db.paymentLink.count` half of `identifierTaken`, and the whole `paymentLinkOrderId` surface (`StoredProviderOrde…
- **Topologia:** single `pop-executor` — one continuous write set (the `nautt` signature change reaches the `checkout` callers) and one skill chain; splitting would cut files the same type crosses.
- **Risco principal:** a name-based sweep deletes live V2 surfaces (`paymentLink` relation in `order-v2-view.ts`, `order-v2-directory.ts`, `public-checkout-v2.ts`) — mitigação: remove only the delegate calls and the `Product.paymentLinks` relation named in C1; every other `paymentLink` occurrence stays.
- **Critérios principais:** C1, C2, C3, C4, C5, C6, C7, C8.
- **Plano:** [[15.1.6-remove-residual-v1-store-calls.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
