# Plan — [[13.1.2-clear-beta-caveats-and-close-reversal-gate]]

- **Stage:** 002_planning · **Role:** `pop-planner` (separate from execution)

## Objective and expected result

- **Objective:** every durable contract, the project brief, the reversal gate and the research entry describe the restored (verified) webhook intake, with no `BETA(M-5.1)` caveat left anywhere normative.
- **Observable result:** a `M-5.1|BETA(` sweep over `pop/specs/`, `src/integrations/nautt/AGENTS.md`, `pop/PROJECT.md` and root `AGENTS.md` returns nothing; the 2026-07-25 open question reads `answered` and carries 13.1.1's commit; `RESEARCHES.md` shows a standing human research whose FAIL opens a modification.

## Strategy

Documentation only, no product code. Every edit **deletes a beta clause written on top of permanent wording that is already present** — restore by removing, never by drafting new contract text (recon §A: the DOX bullets and the spec sentence preceding each caveat already state the post-beta contract). The spec keeps no changelog of the suspension: that history lives in `memory/` and in the 2026-07-25 decision note, which stays untouched as the historical record.

The closed edit list in F01 is the whole task; anything not listed is out of scope. Reversal steps 1–2 of the open question belong to 13.1.1; its step 3 also names root `AGENTS.md`, which has no occurrence (recon §A) — verify, do not edit. The commit sha that closes the gate is read from 13.1.1's memory ledger, never guessed.

## Affected areas

- `pop/specs/nautt-finance-integration.md` — 8 points: header `Updated` (line 7) plus the caveats at 26, 36, 37, 38, 39, 59, 70.
- `src/integrations/nautt/AGENTS.md` — line 42, the whole `BETA(M-5.1) OVERRIDE` bullet under `## Webhook intake`.
- `pop/PROJECT.md` — line 17, the webhook clause of the "Two beta/deferred conditions" paragraph.
- `pop/open_questions/2026-07-25-pre-production-gate-restore-webhook-hmac.md` — frontmatter + closing answer.
- `pop/RESEARCHES.md` — the `nautt-production-webhook-hmac-contract` status/feeds lines and the stale "How to use" step 3.

## Execution fronts

### F01 — Clear the caveats and close the reversal gate

- **Delivery:** the five files above edited exactly as listed below; no other file touched.
- **Role:** `pop-executor` (single cohesive front, one write set).
- **Scope, edit by edit:**
  1. Spec line 7: lead the `Updated` list with a `2026-09-23 — task 13.1.2` entry (contracts describe the restored HMAC verification) and drop the `Earlier: 2026-07-25 — task M-5.1 …` clause; keep every other `Earlier:` entry verbatim.
  2. Spec 26, 36, 37, 38, 39: delete only the trailing `BETA(M-5.1): …` sentence of each bullet; the permanent sentence preceding it stays byte-identical.
  3. Spec 59: delete the trailing `BETA(M-5.1): …` sentence of the task 2.3.1 slice, which already claims strict all-owner HMAC verification.
  4. Spec 70: delete only the parenthetical `(BETA(M-5.1): …)`; the open item itself stays open.
  5. DOX line 42: delete the override bullet; bullets 43–47 stay byte-identical.
  6. `PROJECT.md` line 17: the paragraph keeps one deferred condition (CSP, by decision) and loses the webhook clause; point its tail at [[pop/notes/decisions/2026-09-23-epoch-13-decisions|the 2026-09-23 decisions]] (accepted caveats) instead of `pop/open_questions/`. Touch nothing else in that paragraph nor the `Current state (2026-07-31)` heading — 13.3.1 owns that refresh.
  7. Open question: frontmatter `status: answered`; append a final `**Answered 2026-09-23:**` paragraph (format of [[2026-07-26-v1-settle-wiring-gap]]) stating that the reversal landed in 13.1.1 (commit read from `pop/memory/<date>/13.1.1-restore-webhook-hmac-verification.md`) with the contracts cleared by 13.1.2, that step 3's root `AGENTS.md` had no occurrence, and that step 4's research stays standing per decision 1 of 2026-09-23.
  8. `RESEARCHES.md`: the status becomes a standing human research (no longer `on-hold-by-beta-decision`) — the reversal shipped against the documented 2026-07-17 dispatcher contract; a PASS confirms it, a FAIL/UNKNOWN opens a modification, never a re-suspension. Replace the stale `task 2.3.1 pause condition` feed with the Epoch 13 fork, and rewrite "How to use" step 3 so it no longer unblocks or re-plans 2.3.1.
- **Owns:** the five paths listed in Affected areas.
- **May read:** `pop/**`, `src/integrations/nautt/AGENTS.md`, root `AGENTS.md`.
- **Must not edit:** product code and tests, `pop/notes/**` (the 2026-07-25 decision is history), `pop/memory/**`, the card, the roadmap, any other spec.
- **Depends on:** 13.1.1 — its ledger must exist in `pop/memory/<date>/` before 004; it is also the commit source.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *contract before editing*; [[.agents/skills/sync-specs/SKILL|sync-specs]] — *this restores an existing contract: reference and clean, never create a spec or a changelog*.
- **Web:** deny. **Gate:** 003. **Output:** the five edited files, evidence = the criterion-1 sweep plus the diff, status `concluída | BLOCKED`.
- **Criteria:** 1–9.

## Order and parallelism

1. **Wave 1:** F01 alone, after 13.1.1 is closed (ledger present).
2. **Integration:** the main agent validates ownership (`pop_check_scope.py`) and checks criteria 1–9.

## Risks and abort conditions

- **Risk:** 13.1.1 ships behavior the spec does not describe (e.g. the redacted rejection log) — mitigation: that line belongs to 13.1.1's own closing sync; F01 neither adds nor removes it and reports the observation instead of editing.
- **Risk:** a hidden test asserts the removed prose — none exists (recon §A pins only `webhook-intake*.test.ts`, owned by 13.1.1). If the executor finds one, it records the run in the 13.1.3 phase checklist; this task runs no suite.
- **Abort if:** the 13.1.1 ledger or its commit is missing → `BLOCKED` (criterion 6 is unreachable); or removing a caveat would leave a bullet without its permanent rule → `blocked: true` with the file:line, since writing new contract text is out of scope.

## Acceptance criteria

| # | Criterion | Verifies | Verification | Pass is | 005 mode |
|---|-----------|----------|--------------|---------|----------|
| 1 | No normative `BETA(M-5.1)`/`M-5.1` survives | agent | ripgrep `M-5\.1\|BETA\(` over `pop/specs`, `src/integrations/nautt/AGENTS.md`, `pop/PROJECT.md`, `AGENTS.md` | zero matches (notes/memory mentions stay, outside the sweep) | evidence |
| 2 | Restoration by deletion only | agent | read the spec and DOX diff | every hunk removes a beta clause; no requirement sentence added or rewritten, except the `Updated` entry | evidence |
| 3 | Spec header truthful | agent | read spec line 7 | leads with 2026-09-23 / task 13.1.2, keeps the other `Earlier:` entries, carries no beta history | evidence |
| 4 | DOX intake section intact | agent | read `## Webhook intake` | override bullet gone; the five permanent bullets byte-identical | evidence |
| 5 | Brief has one deferred condition | agent | read `PROJECT.md` line 17 | CSP deferral remains, webhook clause gone, rest of the paragraph unchanged | evidence |
| 6 | Reversal gate closed | agent | read the open question | `status: answered` + dated answer carrying 13.1.1's commit exactly as its ledger records it | evidence |
| 7 | Research standing, not on hold | agent | read `RESEARCHES.md` | no `on-hold`, no 2.3.1 unblock/re-plan; "FAIL → modification" stated | evidence |
| 8 | Touched links resolve | agent | check each wikilink written | every target file exists | evidence |
| 9 | Write set respected | agent | `pop_check_scope.py` over the diff | only the five owned paths changed | evidence |

## Specs and contracts

- [[pop/specs/nautt-finance-integration|nautt-finance-integration]] — *follow for the permanent webhook contract; expected change: the beta caveats leave, nothing is added or rewritten*.
- [`src/integrations/nautt/AGENTS.md`](../../../../src/integrations/nautt/AGENTS.md) — *walk before editing the subtree; expected change: the override bullet leaves*.

## Execution topology

- **Form/justification:** single `pop-executor` — one cohesive documentation front, one write set, no DAG; profile fixed by native role, `size: S` limits only budget.
