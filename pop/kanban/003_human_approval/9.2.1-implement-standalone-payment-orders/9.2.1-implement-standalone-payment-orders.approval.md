# Approval — [[9.2.1-implement-standalone-payment-orders]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read with reality checks against the code, contracts, and linked specs (no web). The plan is a faithful, bounded settlement of the question the spec explicitly deferred to this task, and every behavior-shaping claim I could falsify checked out:

- **Decision 1 (third source value `STANDALONE`).** `pop/specs/checkout-and-order-lifecycle.md` (Order sources / lifecycle / attempt-binding bullets) names 9.2.1 as "the known first candidate" and sanctions either a third source or an AD_HOC variant; `prisma/AGENTS.md` keeps the `order_v2.source` vocabulary service-fenced with no closed-set check, so the extension is additive-safe. Rejecting the AD_HOC variant is correct: the spec pins AD_HOC as "no state, no attempt, no dispatch", and `src/orders/merchant-analytics.ts:403` (locally finalized = `AD_HOC`) depends on that dichotomy.
- **Decision 2 (new table).** Verified forced: `prisma/schema.prisma:406` has `CheckoutAttemptV2.paymentLinkV2Id` non-null with a composite FK, and `prisma/AGENTS.md` restricts future manifests to create-table/add-column/add-constraint/grant — dropping NOT NULL is outside the safe language. New additive table with `(owner_id, retry_key_verifier)` uniqueness and `(owner_id, capability_expires_at)` index mirrors the V2 attempt shape exactly.
- **Decision 3/4 (sessionless trust + one opaque 404).** V1 route matrix confirmed (`src/app/api/payment-links/[identifier]/checkout/route.ts:20-24`: 400/404/503; limiter 429 before parse; 201/202 in tests) and `CAPABILITY_TTL_MS = 24h` in `src/checkout/public-checkout.ts:16`. Slug → owner → `requireActivePair(code)` seam exists (`src/auth/supported-exchange-currency.ts:112`); no-BRL-fallback matches the storefront contract's reads-only survival rule.
- **Decision 7 (V2-only settle wiring).** The recon finding is real: `settle` in `src/orders/order-v2.ts` and `src/orders/payment-link-order.ts` has no production caller (only tests); `reconcileWebhookOrder` (`src/integrations/nautt/owner-pricing-orders.ts:179-190`) only reconciles `provider_order`. The production reconciler lives in `owner-pricing-orders.ts`, which F02 owns — no front-ownership gap. Scoping the V1 gap out as an open question is honest, with an explicit abort condition if V2-only wiring proves impossible.
- **Decision 9 (inventory ownership).** Verified against the in-flight wave: `9.1.2-build-storefront-catalog-and-cart` is in 004 and its plan pins zero edits to `src/security/public-rate-limit.ts`, `src/observability/server-request-log.ts`, and sessionless routes ("9.2.1 owns those"). Spec line "Closed route-inventory impact" also assigns public inventory extension to the owning task. Current inventories match the plan's baseline (two payment-link surfaces; two `/api/payment-links/...` templates).
- **Decision 8 (read-side coherence).** Baselines verified: directory enum is exactly `["AD_HOC", "LINK"]` (`src/orders/order-v2-directory.ts:47`); provider-confirmed sales filter `source: "LINK"` (`merchant-analytics.ts:387`); the funnel reads `checkoutAttemptV2` (line 420). The amendment keeps the never-merge rule (locally finalized stays AD_HOC-only).
- Roadmap coherence: 9.2.2 owns the buyer UI (roadmap line 33); this task's "no UI" scoping is consistent.

#### Criteria checked

1. **Verifiable deliverable — pass.** All 10 acceptance criteria pair an observable behavior with a concrete command or inspection (`pnpm test`, `pnpm db:migration-policy`/`db:contract-check`/`db:test`, diff reads, absence assertions) and an explicit pass condition plus 005 mode.
2. **Sufficient lean brief — pass.** Objective, 10 pinned decisions, fronts with owns/may-read/must-not-edit, risks with abort conditions. Decision rationale is included only where the critical 003 gate must ratify a behavior-shaping amendment; no implementation code or demanded reasoning.
3. **Safe execution — pass.** F01→F02→{F03,F04}→F05 with a single executor; write sets are disjoint (prisma / services / routes+inventories / read-side / docs); F03 and F04 are declared independent and deliberately serialized; cross-task wave with 9.1.2 verified collision-free above.
4. **Proportional specs linked — pass.** All three linked specs carry real amendments (checkout lifecycle is the main one; storefront gains the `standalonePayments` consumer; catalog gains the registry-resolution note). No irrelevant link.
5. **No avoidable `(user)` item — pass.** The card's only user item (release) is already checked; everything else is agent-executable, including the disposable-Postgres contract.
6. **Proportional to risk/size — pass.** L + critical: five fronts, unit + disposable-PG contract tests, replay/trust absence assertions, three explicit abort conditions, honest deferral of the V1 settle gap and poll/recover wiring. No gold-plating (no UI, no public V2 checkout, no V1 fixes).
7. **Economic verification — pass.** No web anywhere; criteria 1, 2, 4, 5, 7, 8, 10 are pure re-run (3 and 6 add an evidence read appropriate to trust/freeze claims); 9 is evidence-only. `pnpm db:test` correctly stays outside the `pnpm check` gate per project convention. No redundant runs.

#### Risk assessment

For the 005 gate (mandatory full mode — critical task), the residual risks to verify against the delivered diff:

- **Sessionless trust posture.** The body must accept only `{ idempotencyKey, amount, customer }`; owner, currency pair, policy, description, and expiry must be derived from locked persisted state (owner `FOR UPDATE`, registry pair `FOR SHARE`); amount validated in the canonical grammar without `Number`; the description must be the pinned server-side bilingual constant pair. Verify absence assertions and re-read the whole diff against the epoch's Pause-if line.
- **Replay fencing.** Identical replay reissues the capability with zero quote/attach/dispatch/recovery; reused key + changed payload → the one opaque 404; expiry/revocation/toggle-off gates hold (the storefront+standalone toggle gate is the analog of V1's link-active gate and must be present).
- **Settle wiring integrity.** The V2 settle invocation must run only after the authoritative owner-bound GET reconciliation has persisted status/version (notification payloads stay non-evidence — acute under the BETA HMAC suspension), must not hold a transaction across provider I/O, must skip the single-use claim for link-less standalone orders, and must leave every V1 module byte-unchanged (diff evidence). The V1 settle-wiring open question must actually be recorded under `pop/open_questions/`.
- **Migration discipline.** One canonical `migration.safe.json` with byte-exact generated SQL; composite owner-bound restrictive FK; unique order binding; `(owner_id, capability_expires_at)` index; runtime grants exactly `SELECT`/`INSERT`/`UPDATE`; `pnpm db:migration-policy` and `pnpm db:contract-check` green. Abort (never hand-write SQL) if the language cannot express it.
- **Inventory closedness.** Exactly two limiter surfaces and two log templates; limiter decides before parsing; origin-guard never applies; root `AGENTS.md` rate-limit/request-log bullets updated at closeout to match.
- **Analytics coherence.** The funnel reading both attempt tables must not double-count (each order is attempt-bound in exactly one table); provider-confirmed (`LINK` ∪ `STANDALONE`) and locally finalized (AD_HOC-only) groups remain separate, never merged, never `Number`, never cross-currency.
- **Spec honesty at closeout.** The spec's "deferred to 9.2.1" wording (Order sources, lifecycle vocabulary, attempt-binding, reconciliation fences bullets) must be resolved to settled wording matching the delivered behavior; the directory filter documentation (`LINK`|`AD_HOC`) must gain `STANDALONE`.

aprovado por revisor independente (yolo) — 2026-07-26
