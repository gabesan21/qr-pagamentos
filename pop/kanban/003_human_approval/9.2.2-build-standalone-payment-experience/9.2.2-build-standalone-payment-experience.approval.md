# Approval — [[9.2.2-build-standalone-payment-experience]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read against live `develop`:

- 9.2.1 is merged (merge commit `5abf910`): `src/checkout/standalone-checkout.ts` confirms the accept-state set `RESERVED|CREATING|PENDING|INDETERMINATE` (lines 27, 155–189), owner-side `checkoutDataPolicy` derivation from the locked owner row (line 143), and capability HMAC issuance; `src/checkout/standalone-payment-status.ts` confirms the capability-only status contract returning the V1 `PaymentLinkOrderState` vocabulary with `pixCopyPaste`/`pixQrCodeUrl` while `PENDING`. The plan's pinned state union matches reality.
- Decision 2's claimed precedent holds: `src/checkout/public-checkout-presentation.ts:35` already exposes `checkoutPolicy` (the owner's `checkoutDataPolicy`) through the sessionless V1 presentation read. The proposed single additive `checkoutDataPolicy` member on `src/storefront/public-storefront.ts` is therefore an alignment with an existing public exposure, not a new class of disclosure — proportionate for a critical gate, provided 005 verifies it adds exactly that one member.
- V1 reuse claim verified: `createPollingController` is exported from `src/app/pay/[identifier]/public-checkout-form.tsx:34`; import-only reuse with zero V1 edits is feasible and the orchestrator diff check is pinned as criterion 4.
- Token mechanism verified: `scripts/check-design-tokens.mjs:9–45` has the exact-declaration `allowedAccentStyles` whitelist; extending it by one route is same-mechanism, no new visual value.
- Evidence-pair pattern verified: ten `*:evidence` / `*:evidence:verify` script pairs exist in `package.json`.
- Temporal gate is honest and load-bearing: `task/9.1.2-build-storefront-catalog-and-cart` exists but is NOT merged into `develop` (verified via `git branch --merged develop`). The DTO currently exposes `standalonePayments` but NOT `standalonePaymentCurrencyCode` — that member is a 9.1.2 deliverable the plan depends on, and the plan's explicit abort condition (replan on contract mismatch) is the correct handling.
- Minor imprecision, non-blocking: F02 cites `src/storefront/cart.ts` as the amount-grammar read, but that file is a 9.1.2 artifact; the canonical `AMOUNT_PATTERN` is already merged in `src/orders/order-v2.ts:103`. The executor must reuse that exact grammar rather than inventing a client-side variant.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result states the full buyer journey end-to-end with concrete green gates (`pnpm check`, evidence pair, `pnpm db:test`).
2. **Sufficient lean brief — pass.** 104 lines; seven pinned decisions with explicit "executor must not revisit" framing; fronts carry owns/may-read/must-not-edit boundaries.
3. **Safe execution — pass.** Four strictly sequential fronts, single executor, real dependency chain (projection → page → evidence → docs); explicit temporal gate on the unmerged 9.1.2; forbidden-path inventory (V1 files, `src/checkout/*`, rate-limit, request-log) pinned and diff-verifiable.
4. **Proportional specs — pass.** Two spec slices (storefront + checkout-lifecycle), root `AGENTS.md` line extension, `DESIGN.md` states; catalog spec correctly read-only.
5. **No avoidable `(user)` item — pass.** None; evidence harness seeds directly and derives the capability HMAC from its own disposable key, with an explicit infeasibility fallback (signal blocked, never stub app code).
6. **Proportional to risk/size — pass.** Size L critical; the one deliberate redaction-boundary amendment is surfaced exactly where it belongs (this gate) and justified by verified V1 precedent; all other choices are reuse-only.
7. **Economic verification — pass.** Criteria 1, 2, 3, 5, 6 are `re-run` (≥1 satisfied); criteria 4 and 7 are evidence reads; no web anywhere.

#### Risk assessment

The 005 gate (mandatory full verification, critical) must confirm:

- **9.1.2 contract fidelity at merge time:** if 9.1.2 merges without `standalonePaymentCurrencyCode` (or with a different route/projection contract), the pinned abort condition must have triggered a replan — reject any delivery that silently worked around a contract mismatch.
- **Decision-2 boundary discipline:** the DTO diff adds exactly `checkoutDataPolicy` and nothing else (no owner identity, raw toggles, pair UUIDs); spec and root `AGENTS.md` amendments match the delivered member word-for-word.
- **Amount grammar identity:** client-side validation uses the canonical grammar of `src/orders/order-v2.ts:103` (or a shared extraction of it), not a divergent copy.
- **State-machine honesty:** `RESERVED`/`CREATING` render the waiting treatment (never an error), the terminal set is unchanged V1 vocabulary, every unavailability is the one opaque 404 view, and the submit body is exactly `{ idempotencyKey, amount, customer }` — no browser-supplied owner/currency/total/status.
- **Zero forbidden diffs:** `git diff --stat` against the task base shows no V1 pay/checkout files, no `src/checkout/*`, no rate-limit/request-log inventory edits, no new API route.
- **Evidence authenticity:** seeded attempt/order/provider rows exercise the real capability HMAC verification path (not a stubbed status), and the manifest covers themes × locales × 375/768/1440 plus every decision-5 state.

aprovado por revisor independente (yolo) — 2026-07-26
