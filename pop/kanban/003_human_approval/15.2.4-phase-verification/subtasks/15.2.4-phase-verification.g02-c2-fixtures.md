# Frente F02 — C2 fixtures and aggregate re-run — [[15.2.4-phase-verification]]

- **Entrega:** the two order-detail page test fixtures carry the `paymentMethod` field 15.2.3 made required on `OrderV2View`, and C2 (`pnpm check`) re-runs green on the integrated tree.
- **Escopo:** fixture literals only — the `OrderV2View` objects built in the two files below; no assertion, mock, product, admin or type source changes. The value is the explicit absent state (`null`) unless an existing assertion in the same file already expects a method.
- **Responsável:** agent.
- **Papel:** `pop-phase-verifier`.
- **Paths de entrada:** the card (section "Resultado de 004 — rodada 1" and Log), the [[15.2.4-phase-verification.plan|plan]] (Estratégia, criteria C2 and C15), this slice, `src/orders/order-v2-view.ts` (the `paymentMethod` field contract).
- **Owns:** `src/app/(merchant)/orders/v2/[id]/page.test.tsx` · `src/app/admin/orders/v2/[id]/page.test.tsx` — the latter is a single-file exception to `src/app/admin/**`, valid for this front only.
- **May read:** `src/**`, `package.json`, the card, the plan.
- **Must not edit:** everything else under `src/app/admin/**`; every F01 path and every other file already integrated at `194e28e1`; the card, approval and telemetry; `pop/specs/**`, any `AGENTS.md`, `prisma/**`, `src/generated/**`, `src/i18n/**`, `docs/frontend-template-parity/**`, `Dockerfile`/`compose*.yaml`/`container/**`/`install/**`.
- **Depends on:** F01 — integrated on `task/15.2.4-phase-verification` at `194e28e1`, worktree `pop/worktrees/15.2.4-phase-verification`.
- **Entrada esperada:** the branch at or after `194e28e1`; if it is absent or diverged, answer `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use when editing the fixture literals*.
- **Web:** deny.
- **Gate/delta:** lacuna rodada 2 (follow-up F7, human option (a)); C1 and C3–C14 already passed in rodada 1 and are not re-run; F01 repairs are not redone.
- **Saída:** one commit on the task branch touching only the two owned files, plus the C2 and C15 results — evidência: command and observed exit/output per criterion (tsc, eslint, vitest, next build), teto 10 lines, status `concluída | BLOCKED`.
- **Critérios:** C2, C15 of the [[15.2.4-phase-verification.plan|plan]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- **Transporte:** same as rodada 1 — if `pnpm` aborts on the Node pin, run the direct binaries (`./node_modules/.bin/{prisma,tsc,eslint,vitest,next}`); the fallback is not an attempt.
- **Saber parar:** no máximo 2 tentativas quando a falha é de ambiente; na segunda, registre `ambiente` e reporte a reclassificação para `verify: user`.
- If C2 shows any failure other than the two fixture errors, record it with `file:line` and answer `BLOCKED`; do not repair it.
- Dependência ou entrada ausente/incompatível → `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
