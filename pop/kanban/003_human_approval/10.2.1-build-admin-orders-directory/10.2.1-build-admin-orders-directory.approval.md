# Approval — [[10.2.1-build-admin-orders-directory]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read with reality checks against the code, not just the plan's claims:

- `queryAdministratorDirectory` and the `ADMIN_GLOBAL` scope purpose are delivered and tested in the foundation (`src/data-directory/server/directory-page.ts:163`, `cursor.ts:20`); the plan needs zero foundation edits, honoring the DOX fence against concrete projections in `src/data-directory/`.
- The admin detail read it reuses exists: `getForAdmin` in `src/orders/order-v2-view.ts:170`, and `toOrderV2Summary` already carries the fail-closed policy-exact `payer: CustomerSnapshotV1` tuple (`order-v2-view.ts:43`).
- The schema supports the additive owner attribution without migration: `OrderV2.owner` relation (`prisma/schema.prisma:350`) and `User.deletedAt` (`schema.prisma:27`) both exist.
- The payer-exposure "spec amendment" is honestly scoped, not a redaction loosening: `checkout-and-order-lifecycle.md` already sanctions administrator customer-data access ("available only to the order owner and administrators through protected interfaces"; snapshots "may be selected only by a future re-authorized owner/administrator projection"), and the V1 admin projection (Task 4.3.1) already exposes `CustomerSnapshotV1` to admins. The amendment codifies the same exposure for the V2 admin directory.
- Admin-only `deletedAt` exposure on order rows is consistent with 10.3.1's fence ("Public and merchant surfaces never expose deletion state"); the spec already grants `deletedAt` on the administrator user DTO.
- The current `/admin/orders` page (`src/app/admin/orders/page.tsx`) is the thin V1 `listForAdmin` composition the plan builds above; the byte-frozen claim targets the V1 ledger section, matching the delivered 8.3.3 `/orders` coexistence precedent.
- Not rendering the V2 comment thread to administrators is a conservative narrowing (the spec permits admin read of comments but the card never asks for it) — acceptable for this slice; the admin mutation absence (no comment/outcome forms) is correct.
- Evidence-pair convention matches reality (`package.json` `*:evidence`/`:verify` pairs, `scripts/run-admin-evidence.mjs` present).

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable results are concrete: `/admin/orders` V2 directory above the frozen V1 ledger, `/admin/orders/v2/[id]` detail, evidence pair exit-0, `pnpm check` + `pnpm db:test`.
2. **Sufficient lean brief — pass.** Objective, six pinned decisions, four fronts with own/must-not-edit lists, abort conditions, and a 7-row acceptance table with per-criterion 005 modes; no implementation dumps.
3. **Safe execution — pass.** Purely additive reads; frozen V1/V2 services, views, triggers, and foundation explicitly excluded per front; no migration (with an abort condition if the delivered schema cannot serve it); disjoint write sets against concurrent 9.1.3/9.3.1/10.1.1 worktrees with append-only spec handling.
4. **Proportional specs — pass.** One real amendment (admin payer/deleted-owner exposure, already sanctioned in spirit by existing spec text) plus delivered-by notes; no speculative spec rewrites.
5. **No avoidable `(user)` item — pass.** Every verification step is agent-runnable (`pnpm test`, evidence pair, `pnpm check`, `pnpm db:test`); the card's only user gate (release) is already checked.
6. **Proportional to risk/size — pass.** L-size, critical, cross-layer read surface: four sequential fronts, explicit risks and two abort conditions match the weight without gold-plating.
7. **Economic verification — pass.** Acceptance table is dominated by `re-run` modes (criteria 1–5, 7); no web dependency anywhere.

#### Risk assessment

Residual risks the 005 gate (full verification — critical task) must actively probe:

- **Redaction diff, not trust:** run the absence assertions and diff the admin row/detail DTO against the amended boundary — no provider data, verifiers, capability material, lifecycle internals, owner email/id; provider order UUIDs must be match-only in `q`, never rendered.
- **Opaque-unavailable preservation on detail:** the additive owner-attribution read must run only after `getForAdmin` returns `found`; cross-owner-naive (admin is global, but malformed/missing IDs), and attribution-read failures must not leak a second outcome shape.
- **`ADMIN_GLOBAL` cursor binding:** prove a `MERCHANT_OWN` cursor (and vice versa) is rejected on the new directory, and that every page re-authorizes the admin.
- **Deleted-owner invariant:** soft-deleted owners' rows present in every filter/page position, localized non-color badge, `/admin/accounts` interim link, and deletion never alters eligibility — including the evidence harness's delete-through-`POST /admin/users/[id]/delete` seed path.
- **Global keyset without an owner-prefixed index:** the plan pins no-migration; 005 should confirm reads stay bounded (`pageSize + 1`, no offset/count) and note the seq-scan/sort cost as accepted scale debt, not silently regress it.
- **V1 freeze byte-check:** `order-view.ts`, `order-views.tsx`, `src/app/admin/orders/[id]/*`, `order-v2-directory.ts`, `order-v2-view.ts`, and anything under `src/data-directory/` must be untouched in the diff.
- **Spec/contract sync:** root `AGENTS.md` `src/app/admin/orders/` bullet and the three spec files updated append-only, with no rewording of 10.1.1-owned sections.

aprovado por revisor independente (yolo) — 2026-07-26
