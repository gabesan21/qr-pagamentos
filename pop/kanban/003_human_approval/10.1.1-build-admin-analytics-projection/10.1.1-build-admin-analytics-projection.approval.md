# Approval — [[10.1.1-build-admin-analytics-projection]]

## Round 1 — 2026-07-26

### Resposta do crítico (yolo)

**Verdict: APPROVED.**

Independent read against the live tree, not the planner's word:

- The merchant precedent the plan reuses is real: `src/orders/merchant-analytics.ts` pins exactly the semantics decision 2/3 claim — `MERCHANT_ANALYTICS_PERIODS = ["today","7d","30d"]`, fixed UTC−03:00 half-open bounds via `resolvePeriodBounds`, two never-merged sales members (`confirmedSales` = LINK∪STANDALONE CONFIRMED with `settledAt` in period; `locallyFinalizedSales` = AD_HOC latest-outcome LOCAL_FINALIZED), per-pair BigInt micro-unit accumulation with registry-redacted labels, and the funnel union over `checkoutAttemptV2` + `standaloneCheckoutAttempt` with four-digit exact-decimal rates, null on zero denominator. The "additively export a pure helper if needed" path is genuine: `resolvePeriodBounds` is already exported, `parseDecimalUnits`/`formatDecimalUnits` are not — an `export` keyword is truly sufficient.
- The admin-authorization posture is implementable without touching `src/auth/`: `Principal` carries `role`, and existing admin-scoped services (`payment-settings`) already deny inline ("Administrator access is required") on a passed principal. The plan's "mirroring `requireUserPrincipal` usage" wording is loose — there is no exported `requireAdminPrincipal` — but the precedent makes the mechanism safe and edits to `src/auth/` unnecessary.
- The 10.3.1 dependency is honestly handled. 10.3.1 sits in `005_verifying` (unmerged); the plan's temporal gate bars 004 until it merges, and its decision 5 (`registeredTotal` includes deleted, `activeNow` = ACTIVE + `deleted_at IS NULL`, top-owner identity `{ username, deleted }`) matches 10.3.1's approved downstream read contract (`deletedAt` on the admin DTO; permanent retention, no anonymization). Note: the plan's reference path `pop/kanban/004_processing/10.3.1-...` is stale — the task has since moved to `005_verifying`; cosmetic, the orchestrator resolves the live location.
- The administrative-foundation spec already anticipates exactly this slice: read-only, re-authorized, redacted global analytics projections are the approved administrator capability (spec lines 60–65, 91), and the merchant-analytics section explicitly defers "administrator/global analytics" to 10.1.1.
- Global-only scope with no per-owner drilldown is the right cut: owner-level exploration is 10.2.x directory work, and bounding per-owner identity to `{ username, deleted }` keeps the redaction fence honest.

#### Criteria checked

1. **Verifiable deliverable — pass.** Observable result is a named module (`src/orders/admin-analytics.ts` + contract tests) consumed by 10.1.2, with concrete commands (`pnpm test`, `pnpm check`, `pnpm db:migration-policy`, `pnpm db:contract-check`, `pnpm db:test`) and per-criterion pass definitions.
2. **Sufficient lean brief — pass.** Objective, seven pinned decisions, three scoped fronts with owns/may-read/must-not-edit, dependencies, contracts, risks with abort conditions, and a 7-row acceptance table. No implementation prose beyond decision level.
3. **Safe execution — pass.** Pure read projection: no route, no mutation, no UI. Write sets are narrow and disjoint from in-flight work (9.3.1 `src/checkout/`, 10.3.1 `src/auth/`); frozen surfaces enumerated per front; the 10.3.1 temporal gate plus orchestrator serialization address the one real collision risk (shared spec/AGENTS edits in F03). Abort conditions name the exact decisions whose rejection forces replan.
4. **Proportional specs — pass.** One new "Administrator analytics definitions" section in the owning spec, one reuse note in checkout-and-order-lifecycle, one root AGENTS bullet, one slice entry. No speculative spec churn; product-scope is read-only context.
5. **No avoidable `(user)` item — pass.** The card's only Release checkbox is already marked by the user; the plan assigns no human execution item.
6. **Proportional to risk/size — pass.** Size L, critical: the 003 gate ran, sequential single-executor fronts are justified by the dependent write set (persistence → service → docs), and leaderboards are hard-capped at 5 with period-bounded aggregates.
7. **Economic verification — pass.** Criteria 1–5 and 7 are `re-run` (or re-run + evidence); only the spec-wording criterion is read-only evidence. No web.

#### Risk assessment

For the 005 gate (always full, critical task) — verify against the delivered diff, not the plan:

- **Temporal gate discipline:** 004 must not have started before 10.3.1 merged into `develop`; the user-count tests must run against the delivered `deleted_at`/`deletedAt` model, not a stub. Confirm `activeNow` semantics match 10.3.1's final delivered lifecycle (deleted ⇒ DISABLED, so the double condition must not miscount).
- **Redaction honesty:** absence assertions must cover payer snapshot members, owner email, owner internal UUIDs, pair UUIDs, and all capability/verifier/provider material; top-owner rows must carry exactly `{ username, deleted }` and nothing more. Read the DTO type against the new spec section word-for-word.
- **Merchant-surface byte compatibility:** confirm the `merchant-analytics.ts` diff is only `export` keywords (plus at most one additive test); the merchant DTO and behavior must be unchanged and existing suites green.
- **F01 evidence:** if a migration landed, it must be one canonical safe-language, plain-column additive index migration (no expression/partial, no baseline edit) with `pnpm db:migration-policy`/`db:contract-check`/`db:test` green; if none landed, the front report must map every query shape to delivered indexes.
- **Funnel double-count:** tests must assert the exactly-one-attempt-table-per-order invariant across both attempt tables, globally (no owner filter).
- **Denial-before-I/O:** the ADMIN role check must run before any store read, and `USER`/unresolved principals must share the denial outcome.
- **Boundedness:** confirm leaderboards are capped at 5 and every aggregate is period-bounded or a cheap full-table count (registered/active/deleted users, product counts).

aprovado por revisor independente (yolo) — 2026-07-26
