# Frente F03 — Delete the merchant and admin V1 surface — [[15.1.1-remove-v1-application-surfaces]]

- **Entrega:** `/orders`, `/admin/orders` and `/links` resolve Commerce V2 only; the V1 order and link services, their legacy tables and sections, the two V1 detail pages and the V1 `/payment-links` routes no longer exist.
- **Escopo:** delete `src/orders/{payment-link-order,order-view}.ts` and their tests, `src/auth/payment-link.ts` and its tests, `src/app/orders/order-views.tsx`, `src/app/(merchant)/orders/legacy-order-table.tsx`, `src/app/admin/orders/legacy-order-table.tsx`, `src/app/(merchant)/links/legacy-links.tsx`, the pages `src/app/(merchant)/orders/[id]/**` and `src/app/admin/orders/[id]/**`, and `src/app/payment-links/**` (the V1 `POST`/`[id]` routes). Remove the V1 sections and the `getPaymentLinkService`/`getOrderViewService` calls from `src/app/(merchant)/orders/page.tsx`, `src/app/admin/orders/page.tsx`, `src/app/(merchant)/links/page.tsx`, `src/app/(merchant)/links/new/page.tsx` and `src/app/(merchant)/links/v2/[id]/edit/page.tsx`, together with the V1 mocks and cases in their tests. The V2 directories, cursors, filters and role fences stay exactly as they are.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card [[15.1.1-remove-v1-application-surfaces]]; seções "Objetivo", "Estratégia" e "Critérios" de [[15.1.1-remove-v1-application-surfaces.plan]]; esta fatia; o relato de F01; [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Owns:** `src/orders/{payment-link-order,order-view}*.ts`; `src/auth/payment-link.ts` e seus testes; `src/app/orders/order-views.tsx`; `src/app/payment-links/**`; `src/app/(merchant)/orders/**`; `src/app/admin/orders/**`; `src/app/(merchant)/links/**`; `src/app/admin/payment-links/**` somente para remover import morto de `order-views`.
- **May read:** `src/**`, `tests/**`, `prisma/schema.prisma`, `pop/specs/catalog-and-payment-links.md`, `pop/specs/administrative-foundation.md`.
- **Must not edit:** `src/checkout/**`, `src/app/pay/**`, `src/app/api/**`, `src/auth/public-payment-link.ts` (F02); `src/app/routes.test.ts`, `src/observability/**`, `src/security/**`, `src/i18n/**`, `tests/**` (F04); `prisma/**` (15.1.2); `pop/specs/**`, qualquer `AGENTS.md`, `docs/**` (15.1.3); o card e o plano.
- **Depends on:** F01.
- **Entrada esperada:** o relato de F01 com os membros relocados e seu novo home — em especial `orderStateLabel`/`OrderStateBadge` e `PAYMENT_LINK_TYPES`, consumidos por telas V2; sem ele, `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para separar a remoção de seção da limpeza local e manter o diff legível*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval já cumprido pela task; nenhum delta de reentrada.
- **Saída:** relato curto no bloco "Resultado" desta fatia, teto 12 linhas, evidência: arquivos apagados, seções removidas e imports V1 restantes (esperado: zero na sua superfície), status `concluída | BLOCKED`.
- **Critérios:** C01, C02, C05, C08, C10 do [[15.1.1-remove-v1-application-surfaces.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- Deletion is subtraction: never relocate a helper here (that was F01), never promote a V1 view into a V2 screen, never add a redirect or placeholder for a removed route.
- A Prisma model reference that blocks a deletion is 15.1.2's, not yours: report it with `arquivo:linha` and stop that deletion.
- Este é o único front que toca sua superfície; F02 roda em paralelo em write set disjunto e a árvore só volta a compilar após a integração das duas.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Nenhum teste roda nesta task — `pnpm check` é critério `phase` da [[15.1.4-phase-verification]].
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <arquivos apagados, seções removidas, varredura de imports V1>.
