# Frente F01 — Shell class hygiene — [[13.3.2-close-admin-source-check-gate]]

- **Entrega:** every `app-shell__*` / `workspace-heading*` class token written as a string literal under `src/app-shell/**` resolves to a selector defined in `src/app-shell/app-shell.css`.
- **Escopo:** the class tokens that resolve to nothing. The planner's read found exactly one — `app-shell__footer-locale` (`src/app-shell/app-shell.tsx:76`), which has no rule anywhere in `app-shell.css` and therefore carries no styling. Confirm the full set yourself before editing; if your own comparison finds more than that one token in `app-shell.tsx`, resolve them the same way; if it finds tokens in a file outside your `Owns`, stop and report.
- **Responsável:** agent. **Papel:** `pop-executor`.
- **Paths de entrada:** card `13.3.2-close-admin-source-check-gate.md`; sections "Objetivo", "Estratégia" and "Critérios de aceite" of [[13.3.2-close-admin-source-check-gate.plan|o plano]]; esta fatia; `src/app-shell/AGENTS.md`; `DESIGN.md` §"BEM retirement (14.7.1)".
- **Owns:** `src/app-shell/app-shell.tsx`.
- **May read:** `src/app-shell/**`, `DESIGN.md`, `scripts/check-admin-ui-inventory.mjs`.
- **Must not edit:** `src/app-shell/app-shell.css`, `scripts/**`, any other file under `src/`, any route file (`page`/`loading`/`error`), specs, DOX, `package.json`.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para the contract-before-coding pass and the no-dead-code judgement*. [[.agents/skills/ui-change/SKILL|ui-change]] — *use para confirm the removal is inert and the anti-drift inventory stays intact*.
- **Web:** deny.
- **Gate/delta:** nenhum (rodada 1).
- **Saída:** diff, teto 1 arquivo, evidência: the two lists you compared (tokens used under `src/app-shell/**` vs. selectors defined in `app-shell.css`), status `concluída | BLOCKED`.
- **Critérios:** C2, C3.

## Contrato de execução

- **Remove the inert token; never add a CSS rule for it.** Inventing a rule is a design change this task has no mandate for. A token with no rule renders nothing today, so deleting it is visually a no-op — that is the whole point, and C8 hands the human the confirmation.
- Do not rename, merge or re-style any resolving class, and do not touch markup structure, props, state, `aria-*`, copy or dictionary keys. A `className` attribute left empty by the removal is deleted along with the attribute, not left as `className=""`.
- Entregar somente o escopo e os critérios desta frente.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente (sandbox, permissão, flakiness); na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- **Não rode teste nem `pnpm admin:source-check`**: os critérios de gate desta task são `verify: phase` e rodam em 13.3.4. Sua evidência é a comparação por leitura.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não implementar, simular ou corrigir dependências por conta própria.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <as duas listas comparadas>.
