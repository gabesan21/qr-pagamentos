# Aprovação — [[13.3.2-close-admin-source-check-gate]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-23

### Brief para decisão

- **Entrega:** make `pnpm admin:source-check` exit 0 across `src/app-shell/**`, `src/app/admin/**`, `src/app/(merchant)/**` and `src/app/language-preference/**`, with no behavior, authorization, copy or dictionary change.
- **Estratégia:** **Read-measured footprint (planner, by grep + AST-rule reading; the gate was not run).** Three of the four counters are already clean: every raw `<input>` in the scanned tree is a literal `type="hidden"` or `className="sr-only"` bridge already exempt (`admin/**` 12 hits, `(merchant)/**` 11 hits, `app-shell/**` and `language-preference/**` zero); no `@/app/ui` import and no `cva` import exist; the single `style=` (`sr…
- **Topologia:** two sequential `pop-executor` calls — disjoint write sets, one predominant skill (`clean-code-change`), F02 logically depends on F01. No recon worker (the footprint was resolvable below the floor). **Perfis:** fixos por papel nativo; `size` altera só orçamento/profundidade.
- **Risco principal:** an exemption wide enough to hollow out the gate — mitigação: rule-backed (`app-shell.css` selector must exist) and `var(…)`-scoped, both strictly narrower than a prefix allowance; C5 pins what must still fail.
- **Critérios principais:** C1, C2, C3, C4, C5, C6, C7, C8.
- **Plano:** [[13.3.2-close-admin-source-check-gate.plan]] — *siga para revisar o brief completo*.
- **Decisões a confirmar:** size rebaixado L → M pelo principal; nenhum primitivo é composto — o vermelho é só `local_variants` (~37 tokens BEM sancionados do shell + 13 `var(--token)` em Tailwind arbitrário); duas isenções tipadas ancoradas em regra (token do shell só se `app-shell.css` define o seletor; `var(--…)` como canal sancionado) e remoção do token inerte `app-shell__footer-locale`; dois `pop-executor` sequenciais em vez de orquestrador.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [ ] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_
