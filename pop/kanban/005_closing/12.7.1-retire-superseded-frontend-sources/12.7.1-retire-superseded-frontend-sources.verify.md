# 12.7.1-retire-superseded-frontend-sources — verification

## What was done

1. **Removed locale-prefixed stubs** — deleted the entire `src/app/[lang]/` tree (9 files: pages, routes, and their tests).
2. **Rewrote route contract test** — `src/app/routes.test.ts` now asserts that the removed stub paths no longer exist on disk, instead of importing the deleted handlers.
3. **Removed orphan components** — deleted `src/app/(merchant)/catalog/dirty-select.tsx`, `src/app/(merchant)/catalog/product-image-field.tsx`, and `src/app/(merchant)/links/share-copy.tsx`.
4. **Updated evidence inventories** — removed those component paths from `tests/catalog.evidence.spec.ts` and `tests/payment-links.evidence.spec.ts` source hash lists.
5. **Cleaned dead CSS** — removed unused custom classes and CSS variables from `src/app/globals.css`; added missing `--space-7` and `--control-height` definitions; removed unused `.app-shell__account-sign-out` from `src/app-shell/app-shell.css`.
6. **Removed legacy template SVGs** — deleted `public/globe.svg`, `public/next.svg`, `public/vercel.svg`, `public/window.svg`.
7. **Removed unreferenced generated assets and updated brand contract** — deleted `public/application-assets/{auth-texture,checkout-success,store-logo-fallback}.svg` and their source files under `docs/template/app/public/`; updated `src/brand/template-asset-sources.ts`, `scripts/generate-brand-assets.mjs`, `scripts/brand-contract.mjs`, and `src/brand/assets.manifest.json` to the closed 14-source / 25-derivative contract; updated related tests.
8. **Updated token projection** — removed dead compatibility aliases and soft feedback color projections from `scripts/generate-design-tokens.mjs`; added `type-label` and `type-compact-heading` to the common projection; regenerated `src/app/globals.css` via `pnpm tokens:generate`.

## Diff summary

```
35 files changed, 49 insertions(+), 817 deletions(-)
```

### Removed
- `src/app/[lang]/*` (all 9 files)
- `src/app/(merchant)/catalog/dirty-select.tsx`
- `src/app/(merchant)/catalog/product-image-field.tsx`
- `src/app/(merchant)/links/share-copy.tsx`
- `public/globe.svg`, `public/next.svg`, `public/vercel.svg`, `public/window.svg`
- `public/application-assets/{auth-texture,checkout-success,store-logo-fallback}.svg`
- `docs/template/app/public/{auth-texture,checkout-success,store-logo-fallback}.svg`

### Modified
- `src/app/routes.test.ts` — rewritten to assert non-existence of removed stubs
- `src/app/globals.css` — regenerated token projection plus manual dead-class cleanup and missing variable aliases
- `src/app-shell/app-shell.css` — removed `.app-shell__account-sign-out`
- `scripts/generate-design-tokens.mjs` — dropped dead aliases/projections, added missing type tokens
- `scripts/brand-contract.mjs` — 14-source / 25-derivative contract counts
- `scripts/generate-brand-assets.mjs` — dropped three application roles
- `src/brand/template-asset-sources.ts` — 14 sources
- `src/brand/assets.manifest.json` — regenerated
- `scripts/brand-contract.test.ts`, `src/brand/brand-identity.test.tsx` — updated counts
- `tests/catalog.evidence.spec.ts`, `tests/payment-links.evidence.spec.ts` — removed deleted component paths from inventories

## Verification results

```bash
$ pnpm check
... (full run)
```

- `pnpm typecheck` — pass
- `pnpm lint` — pass (65 pre-existing warnings, 0 errors)
- `pnpm test` — pass (all relevant tests green; input-otp unhandled `window is not defined` still surfaces as a pre-existing uncaught exception but does not fail the test file)
- `pnpm build` — pass
- `pnpm brand:check` — `Verified 14 sources and 25 deterministic derivatives.`
- `pnpm tokens:check` — pass

No new failures were introduced beyond the baseline `input-otp.test.tsx` `window is not defined` unhandled error.

## Safety confirmation

- No business schemas (`prisma/schema.prisma`, `src/db/`, `src/auth/`, etc.) were modified or removed.
- No active API routes under `src/app/api/` or `src/app/*/route.ts` were removed.
- Active business routes (`/admin`, `/catalog`, `/links`, `/orders`, `/pay/[identifier]`, `/store/[slug]`, `/products`, `/product-categories`, `/checkout-policy`, `/payment-links`, `/payment-links-v2`, `/profile`, `/settings`, `/storefront`, `/media/[identifier]`, `/login`, `/logout`, `/reset-password`, `/language-preference`, `/design-system`, `/nautt-credentials`) remain intact.
- Referenced assets (`avatar-default.svg`, `empty-*.svg`, `logo.svg`, `product-fallback.svg`, `theme-swatch-*.svg`, `unavailable.svg`, `public/brand/*`, `src/app/favicon.ico`, `public/file.svg`) remain intact.
- `git grep` over `src/`, `tests/`, and `scripts/` finds no remaining references to the removed component paths, removed SVG assets, or `[lang]` stubs, except for `src/app/routes.test.ts` which intentionally asserts their absence.

## Gate 005 — Judge Dredd round 1

<!-- pop-verdict round=1 decision=execucao -->

**Finding:** `--text-on-info` was removed from `src/app/globals.css` and from `COMPATIBILITY_ALIASES` in `scripts/generate-design-tokens.mjs`, but is still referenced by:
- `src/components/ui/status-badge.tsx:19`
- `src/components/ui/timeline.tsx:29`

This breaks the text color for the `info` tone in those components.

<!-- pop-delta round=1 paths=src/app/globals.css,src/components/ui/status-badge.tsx,src/components/ui/timeline.tsx,scripts/generate-design-tokens.mjs -->

**Delta:** Reintroduce `--text-on-info` (or update `status-badge.tsx` and `timeline.tsx` to use `--color-feedback-info-foreground` or equivalent class), then rerun `pnpm check` and confirm no other removed variable/class has real usage in `src/` or `tests/`.

---

## Repair — round 1 delta

**Decision:** update the two components to use the canonical `--color-feedback-info-foreground` instead of reintroducing the dead compatibility alias `--text-on-info`. This keeps the token projection clean while preserving the intended info-tone foreground color.

**Changed files:**
- `src/components/ui/status-badge.tsx:19` — `text-[var(--text-on-info)]` → `text-[var(--color-feedback-info-foreground)]`
- `src/components/ui/timeline.tsx:29` — `text-[var(--text-on-info)]` → `text-[var(--color-feedback-info-foreground)]`

**Post-repair sweep for removed variables/classes:**

```bash
$ git grep -n -E -e '--(color-feedback-(success|warning|danger|info)-(soft|soft-foreground)|action-primary-hover|text-on-action|action-primary-active|action-secondary|feedback-warning|text-on-success|text-on-warning|text-on-danger|text-on-info)' -- src/ tests/
No removed CSS variable references found.

$ git grep -n -E '\.(specimen-grid|admin-navigation|admin-catalog-list|admin-catalog-item|admin-skeletons|ds-form|ds-skeletons|brand-specimen|checkout-payment__heading|checkout-skeleton|checkout-skeleton--title|checkout-skeleton--body|storefront-empty|app-shell__account-sign-out)' -- src/ tests/
No removed CSS class references found.
```

**Post-repair `pnpm check`:**
- `pnpm typecheck` — pass
- `pnpm lint` — pass (65 pre-existing warnings, 0 errors)
- `pnpm tokens:check` / `pnpm brand:check` / `pnpm fonts:check` / `check-design-tokens.mjs` — pass
- `pnpm test` — all 228 test files pass (9 skipped); the only uncaught exception is the pre-existing `input-otp.test.tsx` `window is not defined`, which does not fail its test file
- `pnpm build` — pass
- `pnpm check` aggregate exits with code 1 solely because of the baseline `input-otp.test.tsx` unhandled error; no new failures introduced

**Repair date:** 2026-08-05

---

## Gate 005 — Judge Dredd round 2

<!-- pop-verdict round=2 decision=aprovada -->

**Adendo:** O reparo do round 1 resolveu a falha. A substituição por `--color-feedback-info-foreground` mantém a projeção de tokens limpa e preserva a semântica de foreground info. Nenhuma nova regressão identificada por leitura.

**Resultado:** Task aprovada para encerramento.
