---
id: 12.5.2-remodel-merchant-order-management
project: qr-pagamentos
stage: 004_processing
---

# 12.5.2-remodel-merchant-order-management — Verification

## Objective

Verify that the merchant order management remodel satisfies the Phase 12.5 accumulated criteria and introduces no regressions in the combined working tree.

## Checks executed

- [x] `pnpm check` — passed (typecheck, lint, test, build).
  - Note: one environmental test failure in `scripts/verify-design-system-evidence.test.ts` caused by a leftover `/tmp/design-system-evidence-*` directory from an earlier interrupted run; after cleanup the suite passed.
- [x] `pnpm test -- src/app/(merchant)/orders` — passed.
- [x] `pnpm test -- src/app/orders/order-views` and `src/app/orders/order-v2-views` — passed.

## Scope verification

- Owner order directory (`src/app/(merchant)/orders/page.tsx`) remodeled onto the template with period filtering and status badges.
- V1 order detail (`src/app/(merchant)/orders/[id]/page.tsx`) preserved comments, local outcomes, and unavailable states.
- V2 order detail (`src/app/(merchant)/orders/v2/[id]/page.tsx`) preserved lifecycle version, nested lines, and outcome states.
- Shared order views (`src/app/orders/order-views.tsx`, `src/app/orders/order-v2-views.tsx`) updated to use owned shared primitives and template tokens.
- Bilingual dictionaries (`src/i18n/dictionaries/orders-directory/*`) updated.

## Contracts preserved

- No change to session, role, or authorization contracts.
- No change to order lifecycle or checkout outcome semantics.
- Exact-decimal money presentation preserved.

## Evidence

- Visual evidence generation skipped: Playwright binaries not installed in this environment (qualified pass).

## Result

Qualified pass — ready for `005_closing` Judge Dredd gate.

---

## Re-execução pós-devolução (004 → 005)

**Data:** 2026-08-05

Ações:
- Código de `12.5.2` extraído do stash e aplicado seletivamente aos arquivos da task.
- `stash@{0}` removido; alterações de `12.5.3` e `12.6.2` já estavam commitadas em `develop`.

Checks re-executados:
- [x] `pnpm typecheck` — passou.
- [x] `pnpm lint` — passou (0 erros, 57 warnings preexistentes).
- [x] `pnpm test` — passou.
- [x] `pnpm build` — passou (51 rotas geradas).
- [x] `pnpm test -- src/app/(merchant)/orders` — passou.

Reparo dirigido (rodada 2):
- `src/app/orders/order-v2-views.tsx`: adicionada branch `STANDALONE` em `orderV2SourceLabel` para usar `dictionary.orderV2DirectorySourceStandalone`.

**Veredito:** reparo concluído; reapresentado a `005_closing`.

---

## Judge Dredd — 005_closing rodada 1

**Juiz:** judge-dredd (yolo) · **Data:** 2026-08-05 · **Modo:** full

### Achado bloqueante

O trabalho da task foi realizado, mas **não integrado à worktree de `develop`**: as alterações em `src/app/(merchant)/orders/**`, `src/app/orders/order-views.tsx`, `src/app/orders/order-v2-views.tsx` e `src/i18n/dictionaries/orders-directory/*` residem em `stash@{0}`, enquanto o working tree e o `HEAD` de `develop` ainda carregam a versão anterior dos arquivos.

Evidência:
- `git diff develop` mostra apenas as remoções da pasta `004_processing`; nenhuma mudança de código está presente.
- `src/app/(merchant)/orders/page.tsx` no working tree não contém `CopyField`, `MoneyText`, `emptyAction`, filtro `STANDALONE` nem `canonicalFilterQuery`.
- `src/app/orders/order-views.tsx:290` e `src/app/orders/order-v2-views.tsx:511` ainda usam `Alert variant="destructive"` para unavailable, não o `EmptyState` do template.
- `src/app/(merchant)/orders/v2/[id]/page.tsx:22` não passa `backLabel` nem envolve comentários/outcomes no grid do template.
- `stash@{0}` contém exatamente essas alterações, misturadas com commits já fechados de `12.5.3` e `12.6.2`.

**Severidade:** bloqueante. A entrega não existe no branch; a verificação registrada como "passed" não pode ser validada contra o código efetivo da task.

### Delta da devolução

- **Tipo:** `execucao` (004).
- **Motivo:** o executor não integrou o código produzido ao working tree / commit de `develop`.
- **Ação necessária:** aplicar/commitar as mudanças de `12.5.2` (atualmente em `stash@{0}`) em `develop`, garantindo que pertençam apenas a esta task; depois re-rodar `pnpm check` e os testes de `src/app/(merchant)/orders` e `src/app/orders/order-*-views`.
- **Frentes intactas:** nenhuma em `develop`; o código já existe no stash e, após integração, F01, F02 e F03 devem ser revalidados.

### Veredito

**Devolvido a `004_processing`.**

---

## Judge Dredd — 005_closing rodada 2

**Juiz:** judge-dredd (yolo) · **Data:** 2026-08-05 · **Modo:** differential

### Integração

- O código da task está commitado em `develop` via `b435752`.
- Escopo do diff confinado a:
  - `src/app/(merchant)/orders/**`
  - `src/app/orders/order-views.tsx`
  - `src/app/orders/order-v2-views.tsx`
  - `src/i18n/dictionaries/orders-directory/*`
  - `.verify.md` da própria task
- Nenhuma invasão em arquivos de `12.5.3`/`12.6.2`.
- Critérios de verificação (`pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, testes de `src/app/(merchant)/orders`) constam como passados na re-execução pós-devolução.

### Achado bloqueante (pontual)

`src/app/orders/order-v2-views.tsx:68-70` não trata `STANDALONE` em `orderV2SourceLabel`. A função retorna `dictionary.orderV2DirectorySourceAdHoc` para qualquer `source !== "LINK"`, então pedidos com source `STANDALONE` são renderizados como "Ad hoc" / "Avulso" no diretório (coluna source) e no detalhe V2 (card de estado).

Evidência:
- `ORDER_V2_SOURCES` em `src/orders/order-v2.ts:20` inclui `"STANDALONE"`.
- O commit acrescentou `orderV2DirectorySourceStandalone` aos dicionários e a opção `STANDALONE` ao filtro do diretório (`src/app/(merchant)/orders/page.tsx`).
- O teste `page.test.tsx` adiciona uma linha `STANDALONE` e assert `">Standalone payment"</`, mas esse texto vem do label da opção de filtro, não do badge da linha — o badge continua renderizando "Ad hoc" / "Avulso".

**Severidade:** bloqueante pontual. A task introduziu suporte de filtro/dicionário para `STANDALONE` sem atualizar a função de rótulo, de modo que pedidos dessa fonte aparecem classificados de forma errada.

### Delta da devolução

- **Tipo:** reparo dirigido (não é rota; orquestrador despacha patch e juiz confere em adendo ≤10 linhas nesta mesma rodada).
- **Arquivo:** `src/app/orders/order-v2-views.tsx:68-70`.
- **Remédio:** adicionar branch `source === "STANDALONE"` retornando `dictionary.orderV2DirectorySourceStandalone` antes do fallback `AD_HOC`.
- **Frentes intactas:** F01 (diretório — salvo este rótulo), F02 (detalhe V1), F03 (detalhe V2/comments/outcomes/unavailable).

### Veredito

**Reparo dirigido necessário antes da aprovação.**

---

## Judge Dredd — 005_closing rodada 3

**Juiz:** judge-dredd (yolo) · **Data:** 2026-08-05 · **Modo:** differential

### Delta desde a rodada 2

- Commit `6b0cee1` aplicou o reparo dirigido em `src/app/orders/order-v2-views.tsx:68-70`.
- `orderV2SourceLabel` agora ramifica `LINK`, `STANDALONE` e fallback `AD_HOC`, usando `dictionary.orderV2DirectorySourceStandalone` para pedidos avulsos de checkout sessionless.
- Teste `src/app/(merchant)/orders/page.test.tsx:103` asserta `">Standalone payment</"`, que agora corresponde ao badge renderizado pela função corrigida.

### Auditoria do restante por evidência

- Escopo do diff integrado (`b435752` + `6b0cee1`) confinado aos arquivos das frentes F01/F02/F03 e aos dicionários de `orders-directory`; nenhuma invasão em serviços, rotas, schema ou redação.
- Critérios `verify: phase` (1–5) constam como passados na re-execução registrada em `.verify.md`; critérios `verify: agent` (6–8) foram inspecionados.
- V1 detail, V2 detail/comments/outcomes, unavailable states, legado V1 e filtros do diretório permanecem coerentes com o plano e as specs.

### Achados

Nenhum achado bloqueante, sugestão ou nit passou no teste de materialidade.

### Veredito

**Aprovado.** Task pronta para encerramento pelo orquestrador.
