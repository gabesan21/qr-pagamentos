# Julgamento — [[12.6.1-remodel-public-payment-link-checkout]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-08-05

- **Estratégia:** full — primeira passagem no gate 005 de task size L, diff completo e specs/contratos cobertos.
- **Superfície:** `src/app/pay/[identifier]/**`, `src/components/ui/copy-field.tsx`, `src/components/ui/money-text.tsx`, `src/components/ui/inventory.json`, `src/app/globals.css`, `src/i18n/dictionaries/checkout/*`, `tests/checkout.evidence.spec.ts`, `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| 1 | Match V1/V2 buyer flow, policy forms, QR/copy, polling, retry, privacy, paid, unavailable, terminal states | evidência | Diff de `src/app/pay/[identifier]/**` | passou | V1 summary+customer+payment cards (`public-checkout-form.tsx:233-363`), V2 two-column shell (`public-checkout-v2-page.tsx:78-95`), paid terminal (`public-checkout-v2-page.tsx:102-121`), policy field set preservado (`NAME_EMAIL_CPF_ADDRESS` etc.) |
| 2 | `pnpm lint`, `pnpm typecheck`, `pnpm test` passam | phase | Registrado no card; não re-executado | registrado (phase) | Card Telemetria: "`pnpm check` OK" — aguarda phase-verification |
| 3 | `pnpm design-system:evidence:verify` e `pnpm frontend-parity:check` registrados | evidência | Card + manifest diff | passou / qualified pass (ambiente) | `frontend-parity:check` OK no card; trial design-system OK; full design-system falhou em determinístico repeat (flaky) → qualified pass (ambiente) |
| 4 | Checkout evidence 127 screenshots/state matrix | evidência | `tests/checkout.evidence.spec.ts` | passou (spec) / qualified pass (ambiente) | `expect(screenshots).toHaveLength(127)` em `tests/checkout.evidence.spec.ts:568`; state matrix completa; regeneração full é via Docker (user-exclusive) |
| 5 | WCAG 2.2 AA | evidência | `inspectCheckout` no evidence spec | passou (spec) | axe serious/critical vazio, targets ≥44×44, overflow=false, focus visível em toda captura |
| 6 | V1/V2 routes, bodies, responses, polling, retry, redaction inalterados | evidência | Diff + specs | passou | Rotas `POST /api/payment-links/[identifier]/checkout` e `/status` intactas; `createPollingController` reutilizado (`public-checkout-form.tsx:37-86`); 5s poll + backoff exponencial preservado; V2 paid DTO só branding/composition/currency/total |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | `money-text.tsx` altera o estilo `size="large"` globalmente (`text-[length:var(--type-stat)]` → `text-2xl`), afetando outras surfaces que não rastreiam o componente no sourceInventory | `src/components/ui/money-text.tsx:18` | Avaliar se o token `--type-stat` deve ser ajustado em vez de hardcoded `text-2xl`; se mantido, incluir em futuras evidence runs das surfaces afetadas (dashboard, orders, links) |
| nit | Uso inline de `font-[family-name:var(--font-display)]` em vez de classe de utilitário do design system | `public-checkout-form.tsx:241`, `public-checkout-v2-page.tsx:36`, `public-checkout-v2-page.tsx:110` | Consolidar em utilitário de tipografia se houver; não bloqueante |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente.
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [x] Critérios `verify: phase` registrados na checklist da phase.
- [x] Specs, documentação e contratos DOX afetados estão coerentes.

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** (1) revisar se a alteração global de `MoneyText size="large"` deve ser tokenizada; (2) consolidar utilitário de fonte display.
- **Checklist humana:**
  - Regeneração full da checkout evidence via Docker (`scripts/run-checkout-evidence.mjs` + `scripts/verify-checkout-evidence.mjs`) — pass esperado: 127 PNGs, manifest sem severe axe/external requests/console errors.
  - Re-run de `pnpm design-system:evidence:verify` para confirmar que o flaky determinístico não é regressão visual real.
- **Resumo:** O remodelamento do checkout público em `src/app/pay/[identifier]/**` aplica o sistema visual do template preservando todos os contratos de V1/V2: rotas, bodies, polling, retry, redaction, claim-keyed paid view e matriz de 127 estados. Os testes de unidade foram atualizados, os primitivos compartilhados foram estendidos aditivamente e o manifesto de paridade reflete as mudanças.
