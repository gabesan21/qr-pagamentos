# Julgamento — [[14.4.4-phase-verification]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08 (prosa comprimida na rodada 2; veredito e marcadores preservados)

- **Estratégia:** full — task de verificação da phase; superfície `59addba7..099a425c` (52 paths + evidence `artifacts/{login,design-system}/20260908*`); runs próprios `vitest run` (252 files / 2179 passed / 28 skipped / 0 failed) e `tsc --noEmit` exit 0.
- **Passaram:** 1 (18 suites realinhadas com honestidade, nenhum skip novo, `account-mutation-form.test.ts` segue deletado), 7 (ambos os manifests com `gitHead` `b63665ca` ancestral e hashes de fonte batendo), 8 (gate agregado verde, +39 casos sobre a baseline), 9 (`pop_check_scope` vazio; extensão do write set registrada no card), 10 (defeito fora de alcance proposto, não remendado). 11 é `verify: user`.
- **Falharam:** 2 (storefront link "nunca admin"; monograma do rail), 3 (badges/valor do associated-orders nunca renderizados), 4 (§4 confirm + nomes do POST; idioma aplica ao selecionar), 5 (nada rejeitava o `.admin-dashboard*` aposentado), 6 (comentário do `login-form` contradizendo `noValidate`).
- **Decisão:** devolução `execucao` a 004 — cumprimento incompleto, sem defeito de plano; delta em 5 arquivos, frente `F01`, resto aprovado e congelado.
- **Sugestões/nits registrados:** idioma `{await AssociatedOrdersCard(...)}` em `admin/payment-links/v2/[id]/page.tsx:35`; allowlist morta `admin-dashboard*`/`app-shell__*` em `scripts/check-admin-ui-inventory.mjs:11-23`; ponto-e-vírgulas fora do padrão em `totp-challenge-form.test.tsx`; `"{count} products"` sem plural.
- **Devoluções após a rodada 1:** execução 1 de 2 · plano 0 de 2.

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=src/app-shell/app-shell.test.tsx,src/app/admin/payment-links/associated-orders-card.test.tsx,src/app/admin/settings/page.test.tsx,src/app/admin/page.test.tsx,src/app/login/login-form.tsx frentes=F01 intactas=nenhuma -->

## Rodada 2 — 2026-09-08

- **Estratégia:** differential — retorno por execução; superfície **congelada no delta** `099a425c..HEAD` (commit 429d7d51, 6 arquivos). Tudo aprovado na rodada 1 permanece aprovado e não foi rejulgado; o `return_base` do card aponta para um commit de kanban em `develop`, fora da linha da task, e foi ignorado a favor do delta real.
- **Runs próprios** (legítimos: esta *é* a task de verificação da phase, cujo plano declara re-run como critério `agent`): `vitest run` → **253 files / 2185 passed / 28 skipped / 0 failed**; `tsc --noEmit` exit 0; `eslint` nos 6 arquivos do delta exit 0; worktree limpa, delta inteiramente em `src/`.
- **Contagem confere com o delta:** 2179 → 2185 = +6 casos (1 dashboard, 4 associated-orders, 1 jsdom de payment settings); nenhum caso perdido, nenhum `.skip`/`.todo` novo.

### Pedido original (card "O quê / Por quê")

Agora atendido por inteiro: o gate agregado roda verde na árvore integrada, as suites falsificadas seguem realinhadas sem enfraquecimento, a evidence continua ligada às fontes atuais, os reparos ao alcance da phase estão aplicados — e a lacuna que motivou a devolução, "adicionar os testes de regressão que os checklists acumularam", foi fechada nos cinco sites nomeados. A phase 14.4 não fecha mais com superfícies convergidas sem pin.

### Critérios do delta

| # | Item devolvido | Resultado | Evidência |
|---|----------------|-----------|-----------|
| 2 | Storefront "nunca admin" + monograma do rail | passou | `app-shell.test.tsx:92` `expect(admin).not.toContain("storefrontLink=")`, irmão do pin de `profileLink`, sobre a fonte de `src/app/admin/layout.tsx`; grep confirma que só `(merchant)/layout.tsx:53` passa a prop. Monograma em `app-shell.test.tsx:164-166` (`.app-shell__rail-principal > span`, `rounded-full`, texto `"M"`), casando com `app-shell.tsx:47` |
| 3 | Associated-orders com linhas resolvidas | passou | quatro casos novos com fixture `summary()`: badge de provider `CONFIRMED`, `state: null` → `orderV2DirectoryStateNone` neutro, `LOCAL_CANCELLED` → `orderV2DirectoryOutcomeCancelled` danger, valor formatado `1,234.56` e `href="/admin/orders/v2/order-confirmed"`; view-all em `?filter.link=…`. Bate linha a linha com `associated-orders-card.tsx:46-70,105-118` |
| 4 | §4 payment settings + idioma | passou | `settings/page.test.tsx:71-83` fixa os hidden `name="currencies"`/`name="paymentMethods"` — exatamente os campos que `admin/payment-settings/route.ts:21-23` lê — e a seção de idioma como `type="submit"`+`name="locale"` sem Save; o novo `payment-settings-section.test.tsx` (jsdom) percorre toggle-off → `alertdialog` → confirmar → hidden input removido, espelhando `payment-settings-section.tsx:33-49` |
| 5 | Pin de ausência do BEM aposentado | passou | `admin/page.test.tsx:229-236`: `not.toContain("admin-dashboard")` sobre o markup renderizado, mais a presença de `lg:col-span-5/4/3`. `grep -rn "admin-dashboard" src` só encontra imports do domínio i18n — o pin rejeita a reintrodução do markup pré-14.4 |
| 6 | Comentário do `login-form` | passou | `login-form.tsx:39-44` agora afirma o verdadeiro: o POST nativo sobrevive sem JS, mas o texto "obrigatório" é checagem cliente porque o form usa `noValidate` (`:67`); o comportamento servidor descrito confere em `login/submit/route.ts:20-24` (envio em branco → `/login?error=invalid-credentials` genérico) |
| 7 | Evidence não regenerada | passou | correto não regenerar: o delta só toca testes e um comentário. `b63665ca` segue ancestral de HEAD; o manifest de login vincula apenas `src/brand/assets.manifest.json` (intocado) e o de design-system só fontes de estilo, nenhuma no delta |

### Qualidade da implementação (só o delta)

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| nit | O comentário "Turning a row on stays reversible and needs no confirm." precede uma asserção que verifica outra coisa (o hidden input sobrevivendo até o confirm); o caminho de ligar uma linha nunca é exercitado no teste | `src/app/admin/settings/payment-settings-section.test.tsx:41-43` | reposicionar a frase (ou cobrir o toggle-on) na próxima vez que o arquivo for tocado |

### Escopo e integração

- [x] Delta respeita `Owns`/`Must not edit`: 5 arquivos de teste + 1 comentário, nada de produto, spec, rota ou schema.
- [x] Nenhuma frente aprovada na rodada 1 foi refeita, enfraquecida ou tocada.
- [x] Critérios `verify: phase` — não se aplica: esta **é** a task de verificação da phase; o critério 11 vai à checklist humana.
- [x] Specs e contratos DOX coerentes: nenhuma spec tocada, nenhum contrato durável alterado.

## Veredito

- **Decisão:** **aprovada** (terminal). Os cinco itens do delta foram entregues nos sites nomeados, com asserções que realmente falhariam contra o código pré-14.4, e o gate agregado segue verde com +6 casos e zero perdas.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** um nit acima; não bloqueia.
- **Ambiente:** `lint` e `build` não foram re-rodados nesta rodada (evidência do executor da rodada 1 + eslint dirigido aos arquivos do delta); confirmação vai à checklist humana.
- **Checklist humana (critério 11, carregada da rodada 1):** passagem no navegador da Phase 14.4, nos dois idiomas e nos seis temas, a 320px e acima de 1024px — título por rota ao navegar, drawer abaixo de 1024px e rail acima, alvo do storefront link, colar e auto-submit do OTP, estados e sucesso do reset, 404 bilíngue, controle de período sem salto de scroll, os dois drill-downs, modal de criar conta e gerador, restauração da aba por hash no reload, scroll-spy e `aria-current`, confirm de payment settings, idioma aplicando ao selecionar, um toast por ação, ordem de foco e WCAG 2.2 AA. Confirmar também `pnpm lint` e `pnpm build`.

## Follow-ups rastreados (fora do escopo desta task)

- Podar do `scripts/check-admin-ui-inventory.mjs:11-23` as classes `admin-dashboard*`/`app-shell__*` que nenhum `.tsx` renderiza — a allowlist morta ainda passa no guard.
- Reavaliar o idioma `{await AssociatedOrdersCard(...)}` em `admin/payment-links/v2/[id]/page.tsx:35`: agora que o card tem cobertura própria de linhas, a chamada direta pode voltar a `<AssociatedOrdersCard … />`.
- Pluralizar `paymentLinkDirectoryProductsCount` nos dois locales; o pin atual cimenta `"1 products"`.

<!-- pop-verdict round=2 decision=aprovada -->
