# Judgment — [[12.2.2-reconcile-template-identity-and-assets]]

- **Stage:** 005_closing (act 1) · **Owner:** judge

## Original request

- **Fulfilled:** no — the approved identity and all supplied artwork are installed through the deterministic pipeline, but the delivered evidence does not prove that the complete static Sora wordmark reproduces the approved logo target.

## Item-by-item judgment

| # | Objection (axis) | Judgment | Reason |
|---|-------------------|----------|--------|
| 1 | Complete Sora wordmark comparison is absent (execution) | sustained, blocking | C02 requires the Sora lockup to match the target and F03 requires locally settled fonts plus an approved wordmark target mask. Instead, the contact sheet declares `system-ui` (`tests/brand-assets.evidence.spec.ts:187-191`), excludes the logo from every full-raster pair, and compares only its left 32 px (`tests/brand-assets.evidence.spec.ts:308-331`). The current hash/structure assertions therefore cannot detect wrong letter geometry. |
| 2 | Seventeen Playwright contracts still assert IBM Plex Sans (execution) | not sustained | The cited assertions exist, but they are legacy route-evidence expectations, not executable production consumption. C09 removes the transitional package/provenance/runtime contract; the card installs identity/assets, while the plan explicitly defers route migration and forbids opportunistic consumer edits. Updating those suites was not requested by this task. |

## Verdict

- **Decision:** blocking execution defect → 004_processing.
- **Sustained blockers:** C02 lacks full, mutation-sensitive Sora wordmark fidelity evidence.
- **Accepted suggestions/nits:** retire each stale IBM Plex Sans route-evidence assertion with its owning route migration (or final convergence); it does not block this asset task.
- **Returns:** execution 0 of 2 · plan 0 of 2 — a third return on the same route activates the circuit breaker.

## Return delta

- **Type:** execucao.
- **Affected criteria:** C02, C07, C08 — bind the complete target wordmark to pinned Sora 700, compare its full region under a documented derivation tolerance, prove altered letter geometry fails, and generate/verify a fresh run plus aggregate gate.
- **Affected fronts:** `F03` — amend only the independent evidence harness and its run-bound artifacts; if the new proof exposes an actual F01 byte/placement defect, report that evidence rather than silently editing outside ownership.
- **Intact fronts:** `F01`, `F02`, `F04` — canonical sources/identity, deterministic derivatives, and font-transition retirement remain integrated; do not reexecute them absent concrete failure from the new proof.
- **Expected action:** load the pinned local Sora bytes in F03, replace the QR-only mask with complete mutation-sensitive wordmark proof, then rerun the evidence verifier and exact-HEAD gates.
