# Accusation — [[12.1.2-specify-application-frontend-system]]

- **Stage:** 005_closing (act 1) · **Owner:** devil's advocate

## Execution axis — criteria re-run

| # | Plan criterion | Verification executed | Result | Evidence |
|---|---|---|---|---|
| 1 | C01 | `pop_validate.py` + reciprocal lifecycle read | passed | Active/partial successor and superseded predecessor are reciprocal. |
| 2 | C02 | Requirement read against parity palette and WCAG contrast calculation | failed | Exact palette conflicts with the required 4.5:1 normal-text floor. |
| 3 | C03 | `DESIGN.md` read against spec and template | failed | The same unresolved contradiction makes implementation ambiguous. |
| 4 | C04 | Public-route dispositions and design-memory read | passed | Both routes are authorized extrapolations; current routes win. |
| 5 | C05 | `git diff --name-only` + `git diff --check` | passed | Only `DESIGN.md` and the two owned specs changed; diff check passed. |
| 6 | C06 | `pnpm frontend-parity:check && pnpm check` | passed | Parity: 2230/69/454/48; supplied clean HEAD run: 1850 tests and 51 pages. Local rerun independently reached the same parity/test result before sandbox-only Turbopack port denial. |

### Execution objections

| Severity | Objection | Evidence | Remedy |
|---|---|---|---|
| blocking | **E1.** The delivered contract is internally impossible: it requires byte-equivalent template palette values and WCAG 2.2 AA normal-text contrast simultaneously, although the template’s tertiary text is used as normal text and fails 4.5:1. Later Epoch 12 implementers cannot satisfy C02/C03 without violating one side of the canonical contract. | `pop/specs/application-frontend-system.md:31-33,67`; `DESIGN.md:70-81,290-294`; `docs/template/app/src/index.css:9-10,45-46,69-70`; normal-text use at `docs/template/app/src/pages/admin/AdminLinkDetail.tsx:34`. WCAG luminance run observed `pix-paper` tertiary/surface-2 = 2.65:1, `midnight-clearing` = 3.12:1, and `terminal-amber` tertiary/page = 4.38:1. | Define an explicit accessibility precedence and contrast-safe semantic projection/divergence for affected occurrences, then synchronize both the canonical spec and `DESIGN.md` so exact parity and AA are no longer contradictory. |

## Decision axis — attack on the defense

| Severity | Decision attacked | Objection | Evidence | Remedy |
|---|---|---|---|---|
| blocking | Decision 1 — Visual authority | **D1.** Its premise that exact parity and existing contracts remain jointly enforceable is falsified by the observed palette/contrast conflict. | Same measured contrast evidence as E1; the predecessor also requires 4.5:1 at `pop/specs/administrative-design-system.md:31-35`. | Amend the authority decision to specify and evidence accessibility-safe divergence from the template wherever exact colors fail the inherited WCAG contract. |

## Result

- **Execution axis:** 1 objection (1 blocking).
- **Decision axis:** 1 objection (1 blocking).
- **Surface covered:** Original card request; C01–C06; complete `6a69a2b..322a57d` diff; lifecycle reciprocity; parity hashes/counts/dispositions; public extrapolations; decisions 1–6; scope and aggregate evidence.
- **Not examined:** Manual line-by-line reading of all 2,230 NDJSON records; their integrity and semantics were covered by the canonical checker, with targeted manual audit of palette and public-route records.
