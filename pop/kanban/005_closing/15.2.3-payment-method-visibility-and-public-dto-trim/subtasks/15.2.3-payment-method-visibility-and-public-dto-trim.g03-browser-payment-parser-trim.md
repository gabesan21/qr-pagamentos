# Front F03 — Browser payment parser trim — [[15.2.3-payment-method-visibility-and-public-dto-trim]]

- **Delivery:** the browser payment types and response parsers no longer accept, carry or forward `pixQrCodeUrl`.
- **Scope:** exactly the `pixQrCodeUrl` lines of three client modules — the shared checkout payment type, the V2 link form parser and the standalone payment parser. Nothing else in those files changes.
- **Responsible:** agent. · **Role:** `pop-executor`.
- **Input paths:** `pop/kanban/002_planning/15.2.3-payment-method-visibility-and-public-dto-trim/15.2.3-payment-method-visibility-and-public-dto-trim.md` (card), the plan's Strategy, Risks and Acceptance criteria sections, this slice, `pop/researches/pix-checkout-review/2026-09-22-review-findings.md` (finding A8 and the 14.6.1 decision it cites).
- **Owns:** `src/app/pay/[identifier]/checkout-experience.tsx`, `src/app/pay/[identifier]/public-checkout-v2-form.tsx`, `src/app/store/[slug]/pay/standalone-payment-experience.tsx`.
- **May read:** `src/app/pay/**`, `src/app/store/**`, `src/checkout/**`, `pop/specs/checkout-and-order-lifecycle.md`.
- **Must not edit:** `src/app/pay/[identifier]/checkout-payment-views.tsx` (reserved for [[15.3.1-checkout-explicit-states-without-retry]]), any other `src/app/**` path, `src/checkout/**`, dictionaries, specs, DOX, any test file, and every path owned by F01/F02.
- **Depends on:** none. **Expected input:** none — the field is optional on both sides, so this front is valid whether or not F02 has landed.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use for the readability check that no other client behaviour rides along*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval; no re-entry delta.
- **Output:** integrated diff, evidence = diff, status `concluída | BLOCKED`.
- **Criteria:** C6, C8 of the [[15.2.3-payment-method-visibility-and-public-dto-trim.plan|plan]].

## Execution contract

- Remove the member from the shared client payment type, from both response parsers (including their type guards) and from both call sites that forward it to the presentational payment view. `pixCopyPaste`, the closed client state sets, the polling contract and the submit flow stay exactly as they are — the QR is rendered from the copy-and-paste payload, per the 14.6.1 decision, and no provider image is ever a QR source.
- **Do not touch the retry, backoff, poll-failure or waiting surfaces**, even where they sit on the very lines being edited: they belong to 15.3.1, which is the reason this front exists as a narrow slice.
- The presentational `checkout-payment-views.tsx` keeps its now-unreferenced optional prop declaration: it is already dead code and falls with 15.3.1's rewrite of that file. Removing it here is out of scope, not diligence.
- Write and run no test: the assertions belong to [[15.2.4-phase-verification]] (criterion C9), including the stale expectations in `public-checkout-v2-form.test.tsx` and `standalone-payment-experience.test.tsx`.
- **Knowing when to stop:** at most 2 attempts to make an `agent` criterion pass when the failure is environmental; on the second, record `ambiente`, report the reclassification to `verify: user` and move on. Never build new infrastructure just to verify.
- Missing or incompatible expected input → answer `BLOCKED` to the main agent with evidence. Never implement, simulate or repair the dependency, and never change paths outside `Owns`.

## Result

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
