# Verificação — [[11.2.3-rehearse-production-upgrade-and-recovery]]

- **Etapa:** 005_closing · **Responsável:** crítico independente (yolo)
- **Modo:** `full` — task `critical: true`, `size: L`; `pop_yolo.py verify-mode` confirma `full (critical)`.
- **Waiver humano literal (fato central):** o dono do projeto dispensou nesta conversa a execução de `pnpm check`, `install/test.sh` e do próprio `pnpm container:test --clean-clone --scenario production-rehearsal` — "deixe os testes para o usuário final". Registrado no Log do card (2026-07-31, duas entradas) e nos commits `4979abd` e `37fc644`. Comando humano literal vence no alcance nomeado; este gate julga o que é verificável estaticamente e a honestidade do registro, sem exigir a execução dispensada.

## Critérios verificados

| # | Critério do plano | Resultado |
|---|-------------------|-----------|
| 1 | `container:test --scenario production-rehearsal` verde end-to-end | **Dispensado por waiver** — nunca completou run verde; avançou até o redeem do token nas rodadas de depuração. Estado declarado no Log. Revisão estática integral do cenário em substituição. |
| 2 | `install/test.sh` e `pnpm check` sem regressão | **Dispensados por waiver.** Em substituição: `npx vitest run` dos 4 arquivos de teste de password-reset — 31/31 passam; `node --check` do `container-test.mjs` — OK. |
| 3 | Manifest/ledger com revisão exata e sem segredos | Atendido estaticamente — asserts de revisão exata (40-char SHA, labels de imagem, volumes) e `assertRedacted` sobre install/update/backup/restore, manifest e logs. |
| 4 | Inventário pós-restore + `/api/health` | Atendido estaticamente — comparação de inventário mídia/produto/pedido pré-backup vs pós-restore e health `{"status":"ok"}` nos três limites. |
| 5 | `install/AGENTS.md` atual se wording mudou | Atendido — o diff não toca `install/` nem `container/`; nenhum fato operacional mudou, nenhuma atualização devida. |
| 6 | Frente 6 (11.2.2) — asserts bilíngues/tema | Atendido — 11.2.2 fechou antes; `efcaff5` adiciona os asserts, coerentes com o contrato real de `/store/[slug]` (locale da preferência persistida do principal, sessionless = pt-BR, `pix-paper` default, `vault-blue` válido). |

## Evidências

- **Diff líquido da task (fora kanban/artifacts):** apenas `pop/scripts/container-test.mjs` (+492) e `src/auth/password-reset.ts` (+7/−2). A instrumentação de diagnóstico na rota admin (`ADMIN_RESET_DIAG`) foi integralmente revertida — diff líquido zero em `src/app/admin/users/[id]/reset-password/route.ts`. Escopo limpo, sem invasão de ownership.
- **Fix de produção (`5bc64af`) verificado contra o DDL real:** `prisma/migrations/20260727233000_password_reset_token/migration.sql` cria `id UUID NOT NULL` **sem** DEFAULT (a linguagem migration-safe só expressa defaults constantes tipados, não `gen_random_uuid()`); o `@default(dbgenerated(...))` do schema não gera valor client-side. Sem o `id: randomUUID()` explícito o insert quebrava com null-constraint violation no caminho de migração de produção, mascarado nas evidências por tokens semeados via SQL. O fix replica o padrão já usado no insert de auditoria `password_reset_request`. Correto.
- **Testes existentes:** `src/auth/password-reset.test.ts`, `src/auth/admin-password-reset.test.ts`, `src/app/reset-password/submit/route.test.ts`, `src/app/admin/users/[id]/reset-password/route.test.ts` — 31/31 verdes (rodados neste gate; sem Docker).
- **Contratos de rota conferidos um a um contra os asserts do cenário:** `POST /admin/users` → `/admin?success=created`; `POST /admin/users/[id]/reset-password` → `/admin/accounts/[id]?reset=requested`; `POST /reset-password/submit` → `/login?password=changed`; `POST /storefront` → `/?storefront=changed` (campos ausentes = inalterados, confirmado em `src/app/storefront/route.ts` `form.has`); `POST /language-preference` → `/?language=saved`; tema default `pix-paper` via `resolver.modifiers.theme.default`; `POST /api/store/[slug]/cart/checkout` → `201 { paymentLinkIdentifier }`. Todos coerentes.
- **Installer:** `install/install.sh` gera a senha inicial do admin em `.install-secrets/initial_admin_password` via `prepare-identity-secrets.mjs` (nunca a aceita via env) — o read-back do cenário (`24bd406`) está correto.
- **Higiene de segredos:** sentinelas randômicas por run em tmpdir 0700 (removido no `finally`); `assertRedacted` ignora sentinelas <8 chars (falso positivo "587" em portas/SHAs) e nomeia a chave vazada; e-mail capturado checado só contra credenciais (From/URL de reset são conteúdo esperado); token nunca impresso (falha exibe apenas prefixo de digest); probes de falha redigem `smtpUser` e rodam `assertRedacted` nos logs antes de falhar; sink SMTP pinado por digest e rodando uid 1000; QP decode do token correto (soft breaks + hex escapes).

## Achados

- **Bloqueantes:** nenhum.
- **Sugestão (não bloqueante):** `prisma/AGENTS.md` não registra o padrão "id application-supplied" do `password_reset_token` (a linguagem migration-safe não expressa DEFAULT de função); análogo ao caso já documentado de `system_settings.updated_at`. Fora do mandato deste gate (edições documentais limitadas a `install/` e `container/`, onde nada mudou factualmente) — fica registrado para o orquestrador/futura task.
- **Nit:** a fixture de imagem do produto semeada via SQL usa bytes não-WebP (`"rehearsal-product-image"`); é fixture de inventário/persistência que não passa pelo pipeline de mídia — coerente com o objetivo do cenário, sem impacto.
- **Ressalva central (registrada, não bloqueante):** o cenário nunca completou um run verde end-to-end; os fixes posteriores à última falha conhecida (fixture de mídia do produto) não foram re-executados por causa do waiver. Restam para o usuário final: `pnpm container:test --clean-clone --scenario production-rehearsal`, `pnpm check` e `install/test.sh`.

## Decisão

O "O quê / Por quê" do card está coberto pelo cenário entregue e o que é estaticamente verificável está correto — incluindo um bug real de produção encontrado e corrigido no caminho. O waiver está honestamente registrado no Log e em commits, com o estado de execução declarado sem maquiagem. A saída honesta é aprovar com a ressalva registrada: a verificação executiva fica para o usuário final, como o humano decidiu.

verificado por revisor independente (yolo) — 2026-07-31
