# Julgamento — [[14.5.2-converge-merchant-payment-links]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08 (condensada na rodada 2 para caber no teto de 80 linhas)

- **Estratégia:** full — rodada 1, `size: L`, quatro frentes integradas em 28 paths de `git diff develop...HEAD`, mais `tsc --noEmit` e `eslint` como evidência estática.
- **Pedido original:** atendido em quase toda a superfície; faltavam a família de badges de domínio nos drill-downs e o fechamento do gate estático.

### Conformidade com objetivo e specs (rodada 1)

| # | Critério | Modo | Resultado | Evidência |
|---|----------|------|-----------|-----------|
| C01 | Filtros e colunas do diretório | evidência | passou | 6 filtros fixos + `pair` condicional = 7 ≤ 8; coluna `currency`; estado via `LinkLifecycleBadge`; `pageSize`/keyset/digest e aviso de filtros inválidos intactos |
| C02 | Era legacy na mesma tabela | evidência | passou | `era=legacy` em `DataDirectory` com chip + `Modal` read-only; create/revoke V1 restilizados; nenhum import de `@/app/admin/payment-link-management`; nenhum byte de serviço/rota V1 |
| C03 | Projeção do owner aditiva | evidência | passou | `StoredPaymentLinkV2OwnerDetail` só em `findForOwner`; `listWindow`/reuso admin e desfecho opaco intactos; `hasCheckoutAttempt` sobre o seam existente (`payment-link-v2.ts:71`) |
| C04 | Decimais exatos, sem float | evidência | passou | `Number()` só sobre dígitos de data (`legacy-links.tsx:61-63`); totais/subtotal/volume em micro-unidades BigInt |
| C05 | Formulário create/edit | evidência | falhou (rodada 1) | picker, steppers, total exato, clear de expiração, preview sticky e `aria-invalid` presentes; radio card sem foco visível |
| C06 | Locks, banner e recuperação | evidência | passou | kind/tipo/par read-only com nota; lock e banner só sob `prefill.hasCheckoutAttempt`; nomeação dirty-only preservada |
| C07 | Descrições honestas | evidência | passou | par bilíngue sempre renderizado; em `PRODUCT_LINES` fica `disabled`, sem `name` e com caption — nada postado |
| C08 | Ações, guards e resumo | evidência | passou | action row sob os badges; deactivate em `state !== inactive`; activate só `inactive`; single-use liquidado desabilitado com caption; caption de reabertura só `expired`+`REUSABLE`; volume/contagem reais |
| C09 | Drill-downs iguais ao template | evidência | falhou (rodada 1) | breadcrumbs 3/4 níveis, `CopyField`, coluna de pagador e `EmptyState` OK; estado do provedor e desfecho local em `Badge` genérico |
| C10 | Fronteiras congeladas | evidência | passou | `PaymentLinkV2UnavailableCard` byte-idêntico; helpers exportados preservados (prop `actions` e `available?` aditivos); zero paths sob `payment-links*/`, `prisma/`, `auth/payment-link.ts` |
| C11 | Locales e gates estáticos | evidência | falhou (rodada 1) | chaves pareadas nos dois domínios; `eslint` exit 0; `tsc --noEmit` exit 1 com 3 erros |
| C12 | Contratos e checklist da phase | evidência | passou | spec com `Updated:` 2026-09-08 e slice 14.5.2; `DESIGN.md` com era, composição do form e `link-money.ts`; 8 suítes listadas para 14.5.4 |
| C13 | Gate agregado | phase | registrado (phase) | `pnpm check` carregado para `14.5.4-phase-verification` |
| C14 | Conferência renderizada | humano | checklist humana | ver veredito da rodada 2 |

### Qualidade da implementação (rodada 1)

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | `RadioCard` esconde o `input` com `sr-only` e o `label` não tem estilo de foco (WCAG 2.4.7 AA; C05 pede "accessible radio cards") | `link-v2-form.tsx:155-160` | aplicar o padrão de `components/ui/image-uploader.tsx:208` |
| bloqueante | `pnpm typecheck` quebra: os 3 dublês de `createPaymentLinkV2PrefillService` sem `hasCheckoutAttempt` — o ajuste mecânico que o plano autorizou | `tsc` exit 1 → `payment-link-v2-prefill.test.ts:34,42,54` | acrescentar o membro aos dublês, sem caso ou asserção nova |
| bloqueante | Drill-down mantém `Badge` genérico onde C09 exige a família de domínio | `orders/order-v2-views.tsx:64-76` | usar `ProviderStateBadge`/`LocalOutcomeBadge`, precedente em `admin/orders/page.tsx:119-130` |
| sugestão | Toast "already added" inalcançável: `searchResults` já remove os escolhidos; duas chaves de dicionário mortas | `link-lines-editor.tsx:141-150` | resultado desabilitado que dispara o toast, ou remover a cópia |
| sugestão | O DOX foi emendado para legalizar a filtragem client-side do era legacy em vez de filtrar no servidor como pedia o contrato de F01 | `src/data-directory/AGENTS.md:60-67`, `legacy-links.tsx:240-243` | manter só se o humano ratificar |
| nit | `toLocaleString`/`new Date()` no cliente divergem do `formatLinkInstant` (UTC) e podem gerar mismatch de hidratação | `link-v2-form.tsx:696`, `legacy-links.tsx:238` | usar o formatador UTC compartilhado |
| nit | Guarda morta inalcançável no card de ciclo de vida | `link-v2-actions.tsx:47` | remover o ramo |

- **Escopo:** `Owns`/`Must not edit` respeitados (só `dictionaries/domains.ts` fora da lista literal de F02, mesmo ato de registro do domínio); nenhuma dependência implementada oportunisticamente; C13 registrado; contratos coerentes.
- **Veredito da rodada 1:** bloqueante de execução → `004_processing`; três bloqueantes, o terceiro reclassificando a rodada como difusa. Marcadores desativados: `pop-verdict round=1 decision=execucao` · `pop-delta round=1 kind=execucao pontual=false paths=link-v2-form.tsx,payment-link-v2-prefill.test.ts,order-v2-views.tsx frentes=F02,F03 intactas=F01,F04`.

## Rodada 2 — 2026-09-08

- **Estratégia:** differential — superfície congelada no delta da rodada 1; `F01` e `F04` permanecem aprovadas e não foram rejulgadas.
- **Superfície:** `git diff 7414ef19..59b770b1` (3 arquivos, exatamente os três paths do delta), mais `tsc --noEmit` e `eslint`.
- **Devoluções:** execução 1 de 2 · plano 0 de 2.

| # | Critério | Modo | Resultado | Evidência |
|---|----------|------|-----------|-----------|
| C05 | Foco visível no radio card | evidência | passou | `link-v2-form.tsx:157` ganhou `has-[:focus-visible]:ring-2 has-[:focus-visible]:ring-ring has-[:focus-visible]:ring-offset-2`, idêntico a `image-uploader.tsx:208`; demais controles intactos |
| C09 | Família de badges de domínio | evidência | passou | `order-v2-views.tsx:64-91`: `ProviderStateBadge` com os 8 rótulos (`PAYMENT_LINK_ORDER_STATES` cobre exatamente o union `ProviderState`, cast total); `LocalOutcomeBadge`/`StatusBadge` cobrem os dois membros de `ORDER_V2_LOCAL_OUTCOMES`, rótulos preservados; import genérico removido |
| C11 | Gates estáticos | evidência | passou | `tsc --noEmit` exit 0; `eslint` nos três arquivos exit 0; dublês ampliados mecanicamente em `payment-link-v2-prefill.test.ts:34,42,54`, sem caso ou asserção nova |

- **Sanidade:** o delta toca só os três paths nomeados; nenhuma regressão nas frentes congeladas e nenhum path congelado por C10 no diff.
- **Sugestões e nits da rodada 1 seguem abertos e não bloqueiam** (toast morto, exceção de DOX, datas locais no cliente, guarda morta).

## Veredito

- **Decisão:** aprovada — entrega e encerramento. Os três bloqueantes da rodada 1 foram corrigidos exatamente como prescrito, sem ampliação de escopo.
- **Bloqueantes:** nenhum. **Defeito de plano:** nenhum — os critérios cobriam o pedido do card.
- **Checklist humana (C14):** abrir `/links` nas duas eras (filtros era/moeda/data, chip legacy, modal read-only, create/revoke V1), `/links/new`, uma edição, um detalhe e os dois drill-downs, em `pt-BR`/`en` e nos dois temas; conferir steppers, total corrente exato, preview sticky, captions dos guards, breadcrumbs, toasts e o foco visível por teclado nos radio cards, sem overflow nem chip lavado; ratificar (ou recusar) a exceção acrescentada a `src/data-directory/AGENTS.md`.
- **Resumo:** o pedido do card foi atendido — diretório com era/moeda/data e legacy fundido, formulário de composição fiel ao template, detalhe com action row, guards e resumo real, drill-downs com breadcrumbs e badges de domínio — com fronteiras congeladas intactas e gate estático limpo.

<!-- pop-verdict round=2 decision=aprovada -->
