# Julgamento — [[14.5.2-converge-merchant-payment-links]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** full — rodada 1 sem delta anterior, `size: L`, quatro frentes integradas em 28 paths.
- **Superfície:** `git diff develop...HEAD` inteiro (projeção, diretório/era legacy, formulário, detalhe, drill-downs, dicionários, contratos), mais `tsc --noEmit` e `eslint` nos arquivos alterados como evidência estática.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

**Pedido original (card):** atendido em quase toda a superfície — diretório com era/moeda/data e coluna de moeda, legacy fundido na mesma tabela com modal read-only, formulário com radio cards/picker/steppers/total exato/preview sticky, detalhe com action row, guards e resumo real, breadcrumbs nos drill-downs. Falta a família de badges de domínio nos drill-downs (parte explícita do pedido "domain badges" via C09) e o gate estático não fecha.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Filtros e colunas do diretório | evidência | `payment-link-v2-view.ts`, `directory-query.ts`, `page.tsx` | passou | 6 filtros fixos + `pair` condicional = 7 ≤ 8; coluna `currency`; estado via `LinkLifecycleBadge`; `pageSize`/keyset/digest e aviso de filtros inválidos intactos |
| C02 | Era legacy na mesma tabela | evidência | `legacy-links.tsx`, `page.tsx` | passou | `era=legacy` renderiza `DataDirectory` com chip + `Modal` read-only; create/revoke V1 restilizados postando `/payment-links`; nenhum import de `@/app/admin/payment-link-management`; nenhum byte de serviço/rota V1 |
| C03 | Projeção do owner apenas aditiva | evidência | diff de `payment-link-v2-view.ts`, `payment-link-v2-prefill.ts` | passou | `StoredPaymentLinkV2OwnerDetail` só em `findForOwner`; `listWindow`/reuso admin e o desfecho opaco intactos; `hasCheckoutAttempt` sobre o seam existente (`payment-link-v2.ts:71`) |
| C04 | Decimais exatos, sem float | evidência | `grep "Number("` na subárvore + `link-money.ts` | passou | `Number()` só sobre dígitos de data (`legacy-links.tsx:61-63`); totais/subtotal/volume em micro-unidades BigInt |
| C05 | Formulário create/edit | evidência | `link-v2-form.tsx`, `link-lines-editor.tsx`, ambas as páginas | **falhou** | composição/tipo, picker, steppers, total exato, clear de expiração, preview sticky e `aria-invalid` presentes; radio card sem indicador de foco visível (bloqueante 1) e toast "already added" inalcançável (sugestão) |
| C06 | Locks, banner e recuperação | evidência | `v2/[id]/edit/page.tsx`, formulário | passou | kind/tipo/par como fatos read-only com nota; lock e banner só sob `prefill.hasCheckoutAttempt`; banner de conflito com reload/retry sem novo valor de notice; nomeação dirty-only preservada |
| C07 | Descrições honestas nos dois kinds | evidência | `DescriptionFields`, registro F04 | passou | par bilíngue renderiza sempre; em `PRODUCT_LINES` fica `disabled`, sem `name` e com caption — nada é postado |
| C08 | Ações, guards e resumo do detalhe | evidência | `v2/[id]/page.tsx`, `link-v2-actions.tsx`, `link-v2-views.tsx` | passou | action row sob os badges; deactivate em `state !== inactive`; activate só `inactive`; single-use liquidado desabilitado com caption; caption de reabertura só `expired`+`REUSABLE`; `confirmedOrderCount`/`confirmedVolume` reais; badge de linha indisponível |
| C09 | Drill-downs iguais ao template | evidência | `orders/page.tsx`, `[orderId]/page.tsx`, `order-v2-views.tsx` | **falhou** | breadcrumbs 3/4 níveis, `CopyField`, coluna de pagador, fatos ausentes rotulados e `EmptyState` OK; estado do provedor e desfecho local continuam em `Badge` genérico (bloqueante 3) |
| C10 | Fronteiras congeladas | evidência | `git diff`, `diff` do trecho | passou | `PaymentLinkV2UnavailableCard` byte-idêntico; helpers exportados mantêm nome (prop `actions` e linha `available?` são aditivos e opcionais); zero paths sob `payment-links*/`, `prisma/`, `auth/payment-link.ts` |
| C11 | Locales e gates estáticos | evidência | dicionários + `tsc --noEmit`, `eslint` | **falhou** | chaves pareadas em `payment-links-directory` e `payment-links-form`; `eslint` nos 25 arquivos alterados exit 0; **`tsc --noEmit` exit 1** com 3 erros (bloqueante 2) |
| C12 | Contratos e checklist da phase | evidência | spec, `DESIGN.md`, DOX, fatia g04 | passou | spec com `Updated:` 2026-09-08, slice 14.5.2 e bullet da partição/projeções; `DESIGN.md` com era, composição do form e `link-money.ts`; 8 suítes listadas para 14.5.4 |
| C13 | Gate agregado após convergência | phase | registrado na fatia g04 | registrado (phase) | lista de suítes falsificadas + `pnpm check` carregado para `14.5.4-phase-verification` |
| C14 | Conferência renderizada | humano | — | checklist humana | ver veredito |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | `RadioCard` esconde o `input` com `sr-only` e o `label` não tem estilo de foco: teclado seleciona composição/tipo sem indicador visível (WCAG 2.4.7 AA; C05 pede "accessible radio cards") | `src/app/(merchant)/links/link-v2-form.tsx:155-160` | aplicar o padrão já dono da casa em `src/components/ui/image-uploader.tsx:208` (`has-[:focus-visible]:ring-2 has-[:focus-visible]:ring-ring has-[:focus-visible]:ring-offset-2`) no `label` |
| bloqueante | `pnpm typecheck` quebra: os 3 dublês de `createPaymentLinkV2PrefillService` não têm o membro novo `hasCheckoutAttempt`, exatamente o ajuste mecânico que o plano autorizou ("Riscos") | `tsc --noEmit` exit 1 → `src/auth/payment-link-v2-prefill.test.ts:34,42,54` | acrescentar `hasCheckoutAttempt: vi.fn().mockResolvedValue(false)` aos três dublês; nenhuma asserção ou caso novo (as asserções de shape ficam para 14.5.4) |
| bloqueante | Drill-down mantém `Badge` genérico onde C09 exige a família de domínio, já usada pelo admin | `src/app/(merchant)/links/v2/[id]/orders/order-v2-views.tsx:64-76` | trocar por `ProviderStateBadge`/`LocalOutcomeBadge` com labels do dicionário, seguindo `src/app/admin/orders/page.tsx:119-130` |
| sugestão | O toast "already added" é inalcançável: `searchResults` já remove os produtos escolhidos, então `addProduct` nunca vê duplicata — as duas chaves `alreadyAdded` ficam mortas | `src/app/(merchant)/links/link-lines-editor.tsx:141-150` | mostrar o produto já adicionado como resultado desabilitado (que dispara o toast) ou remover a cópia morta |
| sugestão | O DOX foi emendado para legalizar a filtragem client-side do era legacy em vez de a fatia cumprir "aplicadas no servidor" do contrato de F01; a exceção é honesta e escopada, mas inverte a direção do reparo | `src/data-directory/AGENTS.md:60-67`, `src/app/(merchant)/links/legacy-links.tsx:240-243` | manter só se o humano ratificar; alternativa é derivar/filtrar no server component e passar linhas prontas |
| nit | `new Date(expiresAtValue).toLocaleString(locale)` no preview e `new Date()` em `LegacyLinksDirectory` divergem do `formatLinkInstant` (UTC) usado no resto e podem gerar mismatch de hidratação | `link-v2-form.tsx:696`, `legacy-links.tsx:238` | usar o formatador UTC compartilhado |
| nit | Guarda morta: `if (!canDeactivate && !canActivate && !settledSingleUse) return null` é inalcançável (`!canDeactivate` implica `inactive`) | `link-v2-actions.tsx:47` | remover o ramo |

### Escopo e integração

- [x] Alterações respeitam `Owns`/`Must not edit` (só `src/i18n/dictionaries/domains.ts` ficou fora da lista literal de F02, e é o mesmo ato de registro do domínio).
- [x] Nenhuma dependência implementada oportunisticamente por consumidor.
- [x] Critério `verify: phase` (C13) registrado na checklist de 14.5.4.
- [x] Spec, `DESIGN.md` e DOX coerentes com o diff (ver sugestão sobre a exceção de DOX).

## Veredito

- **Decisão:** bloqueante de execução → `004_processing`. Três bloqueantes: dois pontuais e um terceiro que, pela regra da rota, reclassifica a rodada como difusa.
- **Bloqueantes:** foco invisível no `RadioCard` (C05); `tsc --noEmit` exit 1 por dublê não ampliado (C11); badges genéricos no drill-down (C09).
- **Defeito de plano:** nenhum — os critérios cobriam o pedido do card.
- **Sugestões/nits:** toast morto no picker; exceção de DOX no lugar de filtragem no servidor; datas locais no cliente; guarda morta no card de ciclo de vida.
- **Checklist humana:** C14 — abrir `/links` nas duas eras (filtros, chip, modal), `/links/new`, uma edição, um detalhe e os dois drill-downs, em `pt-BR`/`en` e nos dois temas; conferir steppers, total corrente, preview sticky, captions de guard, breadcrumbs e toasts, sem overflow nem chip lavado. Conferir também, com teclado, o foco visível nos radio cards depois do reparo. Ratificar (ou recusar) a exceção adicionada a `src/data-directory/AGENTS.md`.
- **Resumo:** a convergência está substancialmente entregue e as fronteiras congeladas foram respeitadas; falta fechar o gate estático e dois pontos de fidelidade (foco acessível e família de badges) antes de encerrar.

## Delta da devolução

- **Tipo:** execucao.
- **Critérios afetados:** C05 (radio card sem foco visível), C09 (badges de domínio ausentes no drill-down), C11 (`tsc --noEmit` exit 1).
- **Frentes afetadas:** `F02` — foco do `RadioCard` e ampliação mecânica dos dublês de prefill; `F03` — badges de domínio no drill-down.
- **Frentes intactas:** `F01`, `F04` — aprovadas, permanecem integradas; não reexecutar (F04 só ajusta, se quiser, a linha de `payment-link-v2-prefill.test.ts` na lista de suítes, que passa a ser "asserções pendentes" em vez de "dublês pendentes").
- **Ação esperada:** 004 aplica os três reparos nomeados, sem tocar em mais nada, e devolve com `tsc --noEmit` exit 0.

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=src/app/(merchant)/links/link-v2-form.tsx,src/auth/payment-link-v2-prefill.test.ts,src/app/(merchant)/links/v2/[id]/orders/order-v2-views.tsx frentes=F02,F03 intactas=F01,F04 -->
