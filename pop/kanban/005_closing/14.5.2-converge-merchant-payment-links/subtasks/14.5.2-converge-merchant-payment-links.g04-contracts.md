# Frente F04 — Contracts and evidence — [[14.5.2-converge-merchant-payment-links]]

- **Entrega:** the durable record telling the truth about what F01–F03 integrated — the era partition and the extended merchant filter set, the additive owner projection members, the description section locked for product-line links, the adapted lifecycle guards and the exact-decimal money module — plus the phase checklist that 14.5.4 will run.
- **Escopo:** `DESIGN.md`, the catalog spec and the DOX files this diff falsified. No application source, no dictionary, no kanban file other than this slice.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** o card, esta fatia, o [[14.5.2-converge-merchant-payment-links.plan|plano]] inteiro, os `Resultado` das fatias g01–g03 e o diff integrado da task.
- **Owns:** `DESIGN.md`, `pop/specs/catalog-and-payment-links.md`, `src/data-directory/AGENTS.md` e `src/components/ui/AGENTS.md` (somente se o diff integrado os contradisser), e o campo `Resultado` desta fatia.
- **May read:** todo o diff integrado da task, `pop/specs/{application-frontend-system,administrative-foundation,checkout-and-order-lifecycle}.md`, `pop/researches/template-fidelity-convergence/merchant-commerce.md`, `pop/roadmap/14-template-fidelity-convergence.md`, `src/app/(merchant)/links/**`, `src/auth/payment-link-v2*.ts`.
- **Must not edit:** `src/**`, `docs/**`, `prisma/**`, dicionários, o card, o plano, as fatias g01–g03, qualquer arquivo fora de `Owns`.
- **Depends on:** F01, F02, F03. **Entrada esperada:** o diff integrado das três frentes e seus relatórios, incluindo a divergência de descrição registrada por F02 e os membros de projeção reportados por F01.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para decidir o que é contrato durável e o que morre na memory*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** relatório listando cada arquivo de contrato alterado com uma linha de justificativa e a checklist de phase para 14.5.4, status `concluída | BLOCKED`.
- **Critérios:** C12.

## Contrato de execução

- **Spec:** record the 14.5.2 slice in `pop/specs/catalog-and-payment-links.md` — the additive owner-detail projection (confirmed count, exact confirmed volume, per-line product availability) and the owner prefill's checkout-attempt member, both read-only and owner-scoped — and state plainly that the bilingual description remains bound to `FIXED_AMOUNT`, with the merchant form rendering it read-only for `PRODUCT_LINES`. Do not restate presentation detail there and do not weaken any V1 or Commerce V2 invariant; update the `Updated:` line the way earlier slices did.
- **DESIGN.md:** record the era partition as the merchant directory's answer to the template's merged table (with the reason: keyset pagination and the data-directory contract), the composition form's radio-card/picker/preview composition and its lock notes, the lifecycle guard rules as implemented, and the exact-decimal money module as the single owner of money arithmetic in the links subtree. Keep it inside the file's existing sections.
- **DOX:** touch `src/data-directory/AGENTS.md` or `src/components/ui/AGENTS.md` **only** if the integrated diff contradicts a line there; a new consumer of an existing contract is not a contract change. Say so explicitly in your report when you change nothing.
- **Phase checklist for 14.5.4:** list every existing suite this diff falsifies (at minimum the merchant link page, form, detail and drill-down tests, plus any directory-query double touched), each with the file and the one-line reason, and carry criterion C13 forward. Do not write, run or fix a test here.
- Anything real but outside the card's "O quê / Por quê" is a follow-up proposal in your report — never a new criterion or a new front.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `docs(14.5.2): F04 contracts and design memory` on `task/14.5.2-converge-merchant-payment-links--f04`.
- **Arquivos alterados:** `pop/specs/catalog-and-payment-links.md` (116 lines, ≤150), `DESIGN.md`, `src/data-directory/AGENTS.md`. `src/components/ui/AGENTS.md` left untouched — no contradiction found (F02's plain named `Input` deviation from `LocalizedFieldGroup` is a documented usage choice, not a contract change; no new inventory component was added).
- **Desvios:** nenhum de escopo. One DOX carve-out added to `src/data-directory/AGENTS.md`: the delivered legacy-era component (`legacy-links.tsx`) loads the full byte-frozen owner V1 list and filters it client-side by `state`/`type`/`from`/`to`/`q` before handing rows to `DataDirectory` — a literal contradiction of the DOX's "never add ... client-side full-list filtering." Recorded as a scoped exception for byte-frozen, cursorless sources only (never extends to any adapter-backed or cursor-bearing source).
- **Evidência:** spec — new `Updated:` entry (2026-09-08), new "Task 14.5.2" implemented-slice line, new bullet under Commerce V2 recording the era partition, the three additive `findForOwner`/prefill projection members, and the locked-description decision. `DESIGN.md` — new paragraph under "Shared component ownership and anti-drift" (era partition rationale, radio-card/picker/preview composition, `LocalizedFieldGroup` deviation, lifecycle guard rules, `link-money.ts` as sole money arithmetic owner). DOX — one carve-out bullet added to `src/data-directory/AGENTS.md` as above.

### Suites invalidated for 14.5.4

- `src/app/(merchant)/links/directory-query.test.ts` — no coverage of the new `era`/`pair`/`from`/`to` registered filters or `buildLinksDirectoryFilterDefinitions`.
- `src/app/(merchant)/links/page.test.tsx` — directory now era-partitions into `v2`/`legacy`, adds the currency column, and renders `LegacyLinksDirectory` for `era=legacy`; existing assertions predate this shape.
- `src/app/(merchant)/links/new/page.test.tsx` — form replaced with radio cards, searchable picker with steppers, running total, and sticky preview (`link-v2-form.tsx` rewritten, 684-line diff).
- `src/app/(merchant)/links/v2/[id]/edit/page.test.tsx` — lock and new-version banner now gate on the prefill's additive `hasCheckoutAttempt` flag instead of rendering unconditionally.
- `src/app/(merchant)/links/v2/[id]/page.test.tsx` — action row, lifecycle guard captions (settled single-use, expired+reusable reopen), and the real `confirmedOrderCount`/`confirmedVolume` summary are new; per-line `available` badge is new.
- `src/app/(merchant)/links/v2/[id]/orders/page.test.tsx` and `.../orders/[orderId]/page.test.tsx` — breadcrumbs, `CopyField` ids, payer column, and domain badges (`ProviderStateBadge`/`LocalOutcomeBadge`/`LinkLifecycleBadge`) are new render shape.
- `src/auth/payment-link-v2-prefill.test.ts` — store contract widened to require `hasCheckoutAttempt`; existing doubles/assertions predate the member.
- `src/auth/payment-link-v2-view.test.ts` — already mechanically widened by F01 for the `StoredPaymentLinkV2OwnerDetail` shape, but has no assertions yet for the new `era`/`pair`/`from`/`to` filter resolution, the per-line `available` flag, or `confirmedOrderCount`/`confirmedVolume` values — carry forward as incomplete, not just widened.
- Carries forward criterion **C13** (`pnpm check` on `14.5.4-phase-verification` must exit 0 with these suites updated to the converged compositions).
