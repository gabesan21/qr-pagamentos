# Front F03 — Spec amendment — [[15.3.1-checkout-explicit-states-without-retry]]

- **Delivery:** the checkout contract states the buyer no-retry rule, the waiting rule and the explicit states, replacing the pins that still mandate the backoff and a manual retry.
- **Scope:** one spec file. No code, no DOX, no other spec, no roadmap or card edit.
- **Owner:** agent · **Role:** `pop-executor` · **Web:** deny · **Depends on:** none.
- **Input paths:** the card, the plan's *Strategy* and *Acceptance criteria* sections, this slice, `pop/notes/decisions/2026-09-22-v1-removal-and-checkout-decisions.md`, `pop/researches/pix-checkout-review/2026-09-22-review-findings.md`, the current `pop/specs/checkout-and-order-lifecycle.md`.
- **Owns:** `pop/specs/checkout-and-order-lifecycle.md`.
- **May read:** `pop/specs/**`, `src/app/pay/**`, `src/app/store/**` (read only), `pop/roadmap/15-v2-only-pix-integrity-and-explicit-checkout.md`.
- **Must not edit:** every other spec, `src/**`, `pop/specs/INDEX.md` unless the file's own metadata changed, `docs/**`, the card, the plan.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use for the contract-before-text pass*.
- **Gate:** `003_human_approval` is already past when this runs; deliver for the human PR gate in `005_closing`.
- **Output:** the amended spec plus a short report naming each replaced pin, teto 20 lines, evidence: section names and the replaced sentences, status `concluída | BLOCKED`.
- **Criteria:** C08.

## Execution contract

- **Match by rule, not by line number:** Phase 15.1 (task 15.1.3) rewrote this file without V1, so anchors moved. Locate the pins by their content, in whatever sections survive: the client/polling pin that mandates `1s·2^n` backoff and a manual-retry affordance "on the first polling failure"; the standalone "Polling, retry, and return semantics" pin (three consecutive failures, manual retry, `503`/network submit failure keeping the form behind an error that reuses the same idempotency key); and the evidence sentences that bind a "status-error with manual retry" capture.
- **State exactly these contracts (no code, no prose beyond the rule):** the buyer checkout has **no retry**, automatic or manual — no backoff, no "check again", no "try again", no "new payment", no affordance re-executing a failed call; every non-confirmed outcome is an explicit named state whose single action is start-over, which discards the attempt and mints a new idempotency key; the 5 s visibility-aware status read stays as state observation, a failed read stops the loop and surfaces the status-unavailable state; server-side idempotent replay of the same request stays untouched; the opaque `404` treatment is unchanged.
- **Waiting rule:** a waiting treatment is allowed only while the status can still change (`RESERVED`/`CREATING`/`CREATED`); `PENDING` without a payload, `INDETERMINATE` and a rejected QR generation are the explicit PIX-unavailable state — the buyer never sees a placeholder for data that will not arrive.
- **Evidence sentences:** replace the retired manual-retry captures with the named states; keep the run-bound evidence pair's own contract intact and never promise a capture this task did not define.
- Keep the file's existing structure, frontmatter and status vocabulary; amend in place rather than appending a parallel section, and never duplicate plan text into the spec.
- Deliver only this front's scope and criteria; at most 2 attempts on an environment-flaky `agent` criterion, then record `ambiente` and report the reclassification to `verify: user`.
- Missing or incompatible input → answer `BLOCKED` with evidence; never implement, simulate or repair a dependency, and never write outside `Owns`.

## Result

- **Status:** <concluída | BLOCKED>.
- **Commit/artifact:** <reference>.
- **Files changed:** <short list, checked against `Owns`>.
- **Deviations:** <none | deviation and the main agent's authorization>.
- **Evidence:** <replaced pins, by section>.
