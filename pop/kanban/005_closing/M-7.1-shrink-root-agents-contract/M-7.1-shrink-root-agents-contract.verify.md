# M-7.1-shrink-root-agents-contract — Verificação

## Critérios

- [x] Root `AGENTS.md` reduzido de 195 para ~89 linhas.
- [x] Seção genérica `Processo DOX` removida e substituída por link para `[[pop/WORKFLOW]]`.
- [x] `Application contract` condensado em índice de domínios com links para specs/AGENTS.md filhos.
- [x] `prisma/AGENTS.md` adicionado ao DOX index.
- [x] Links quebrados em `pop/PROJECT.md` para `AGENTS.md` corrigidos.
- [x] `pnpm lint` e `pnpm typecheck` passam.
- [x] `python3 pop/scripts/pop_validate.py --scope pop` não apresenta violações relacionadas aos arquivos tocados (única violação remanescente é harness defasado, fora do escopo desta task).

## Notas

A violação de harness defasado (`pop/.included-harness.json`) persiste porque o instalador local não consegue atualizar a si mesmo; deve ser resolvido pelo escopo hospedeiro do harness.
