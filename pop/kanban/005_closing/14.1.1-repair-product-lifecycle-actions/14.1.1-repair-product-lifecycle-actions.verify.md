# Julgamento — [[14.1.1-repair-product-lifecycle-actions]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-07

- **Estratégia:** differential — task não crítica, primeira rodada, frente única já integrada.
- **Superfície:** `git diff 7751cba3..HEAD -- src/` (commit `57c2b0aa`, 8 arquivos, +402/-180); card, plano C1–C13, specs de catálogo e frontend, DOX raiz/`components/ui`/`data-directory`, testes existentes de catálogo lidos como contexto.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

**Pedido do card atendido.** As duas ações de ciclo de vida voltam a submeter (`submitFormById` resolve `product-active-toggle` e `product-archive`, forms que existem no mesmo ramo `!archived`), o produto arquivado permanece inspecionável em formulário read-only não submissível, e a falha de mutação de produto/categoria retorna ao formulário de origem com os valores digitados e o aviso opaco visível.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C1 | Activate/Deactivate confirmado submete `active` | evidência | diff `product-detail-client.tsx` | passou | `onConfirm` → `submitFormById("product-active-toggle")` (:216); refs `useRef` removidas; form existe em :80-88 |
| C2 | Archive confirmado submete `archive`, dois diálogos mantidos | evidência | mesmo diff | passou | `submitFormById("product-archive")` (:228); `ArchiveForm` em :99-107; ambos `ConfirmDialog` intactos |
| C3 | Arquivado visível como formulário read-only | evidência | diff `product-detail-client.tsx` + `product-form.tsx` | passou | `readOnly` retorna `<div id={formId}>` sem `action="/products"`, sem hidden action/id/version, sem `CatalogSubmit`, sem `SegmentedControl` (product-form.tsx:426-428, 372-381, 340-350); contêiner dessaturado em product-detail-client.tsx:164 |
| C4 | Falha de create volta ao form de criação com valores | humano | run manual | qualified pass (ambiente) | wiring lido (rota :27-31, notice em `new/page.tsx`:57, draft `product-create`); sem browser/DB no gate → checklist |
| C5 | Falha/conflito de update volta ao detalhe | humano | run manual | qualified pass (ambiente) | idem, alvo `/catalog/products/<id>` e draft `product-edit-<id>` |
| C6 | Falha de categoria volta ao form de categorias | humano | run manual | qualified pass (ambiente) | rota de categorias já aterrissava em `/catalog/categories`; drafts novos em `categories/page.tsx`:31 e `category-row-actions.tsx`:42 |
| C7 | Wiring de retorno existe nos três formulários | evidência | diff das rotas e componentes | passou | alvo derivado de action/id (route.ts:27-33); draft por chave de form, restaurado só sob notice de falha, limpo caso contrário (`catalog-draft.tsx`:39-88) |
| C8 | Semântica das rotas preservada | evidência | diff das rotas | passou | guards, conjunto de ações, chamadas de serviço, decimais e todos os redirects de sucesso idênticos; só o `catch` mudou; `product-categories/route.ts` intocada |
| C9 | Feedback opaco e localizado | evidência | diff | passou | apenas `conflict\|failed` e chaves de dicionário existentes via `ProductNotice`; nenhuma string de UI hardcoded, nenhuma chave nova |
| C10 | Um único submit de criação em `/products/new` | evidência | diff `new/page.tsx` | passou | botão de página removido (:57-60); submit do form mantido em product-form.tsx:380 |
| C11 | Tipos e lint limpos nos arquivos tocados | evidência | `tsc --noEmit`; `eslint` | qualified pass (ambiente) | `tsc --noEmit` exit 0 na branch (orquestrador); lint não re-executado no gate — os dois disables miram `react-hooks/set-state-in-effect`, regra real do `eslint-config-next` 16, e `pnpm check` recobre lint em 14.1.4 |
| C12 | Suíte de catálogo verde | phase | `pnpm check` em `14.1.4-phase-verification` | registrado (phase) | leitura das asserções existentes não achou nenhuma invalidada: `products/route.test.ts` só afirma redirects de sucesso; `[id]/page.test.tsx` exige `not.toContain('action="/products"')` no arquivado, satisfeito; páginas seguem chamáveis sem `searchParams` |
| C13 | Nenhuma escrita fora do `owns` | evidência | `pop_check_scope.py` | passou | 8 paths, todos em `owns`; `globals.css`, `app-shell`, `admin`, `components/ui`, `i18n`, testes intocados |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | O `id` vindo do formulário é interpolado sem codificação no path do header `Location`; um id malformado (CR/LF) faria o `NextResponse` lançar dentro do próprio `catch`, trocando falha tratada por 500. Não é open redirect (o prefixo `/catalog/products/` impede sair da origem) e exige owner autenticado, mas o plano prometia que "nada do cliente nomeia um path" | `src/app/products/route.ts:27-31` | `encodeURIComponent(id)` ao montar o alvo |
| sugestão | Sob `categories=failed` originado por *qualquer* mutação de categoria, uma linha não relacionada com draft antigo reabre sozinha o form de edição com valores velhos — a mitigação de "limpar em load sem notice" não cobre esse caminho | `src/app/(merchant)/catalog/categories/category-row-actions.tsx:42-57` | ao salvar um draft, remover os demais `qr-catalog-draft:` (um rascunho por vez) |
| nit | A página de detalhe mostra dois controles "Save" idênticos (o do form e o de página) — mesma duplicação que C10 removeu em `/products/new`, porém **pré-existente** e fora do pedido do card | `product-detail-client.tsx:193` + `product-form.tsx:380` | follow-up da epoch 14, não desta task |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` da frente F01.
- [x] Dependências não foram implementadas oportunisticamente por consumidores (nenhuma dependência).
- [x] Critérios `verify: phase` registrados na checklist da phase (C12 → `14.1.4-phase-verification`).
- [x] Specs, documentação e contratos DOX coerentes: nenhuma spec fixa o alvo de redirect de falha, e a leitura read-only do produto arquivado é justamente o que [[pop/specs/catalog-and-payment-links|Catalog and payment links]] já exige; a linha de sync prevista no plano fica com o agente principal.

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** três acima, todas não bloqueantes; a duplicação do "Save" no detalhe vira follow-up da epoch.
- **Checklist humana:**
  - **C4** — em `/catalog/products/new`, preencher o form e forçar a falha do create (ex.: derrubar o banco). Pass: volta a `/catalog/products/new` com os valores digitados e o alerta de erro; não aterrissa em `/catalog`.
  - **C5** — em `/catalog/products/<id>`, submeter com `version` obsoleta. Pass: volta ao detalhe do mesmo produto com valores intactos e o alerta de conflito.
  - **C6** — em `/catalog/categories`, forçar a falha de um create/edit de categoria. Pass: volta a `/catalog/categories` com os nomes digitados e o alerta opaco.
  - **C11 (metade lint)** — `pnpm lint` local. Pass: sem erro; recoberto por `pnpm check` em 14.1.4.
- **Resumo:** o pedido era destravar arquivar/ativar, manter o arquivado legível e parar de descartar o input do lojista; o entregue faz exatamente isso com três movimentos estreitos (resolução do form real por id, contêiner read-only não submissível, alvo de falha derivado de action/id mais draft de sessão), sem tocar regra de negócio, guard, token ou dicionário.

<!-- pop-verdict round=1 decision=aprovada -->
