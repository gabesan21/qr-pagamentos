# Acusação — 12.4.3-remodel-administrator-account-management

- **Etapa:** 005_closing (ato 1) · **Responsável:** advogado do diabo

> Artefato do gate adversarial — configuração A (devils-advocate + adversarial-judge). Contexto fresco, tier strong.

## Eixo de execução — critérios re-rodados

| # | Critério do plano | Verificação executada | Resultado | Evidência |
|---|-------------------|------------------------|-----------|-----------|
| 1 | Tipagem e build | `pnpm typecheck && pnpm build` | passou | zero erros, build Next.js 51 páginas |
| 2 | Lint e formatação | `pnpm lint` | passou | 0 erros, 59 warnings pré-existentes |
| 3 | Testes de accounts | `pnpm test src/app/admin/accounts src/app/admin/users` | passou | 78 tests passaram (9 files) |
| 4 | Testes de rotas de perfil | mesma run do critério 3 | passou | 78 tests passaram (9 files) |
| 5 | Porta de qualidade | `pnpm check` | passou | 1980 passados, 28 skipped |
| 6 | Banco descartável | `pnpm db:test` | passou | 43 passos |
| 7 | Acessibilidade | `pnpm test` (inclui axe) | passou | zero novas violações |
| 8 | Evidence visual | `pnpm admin-users:evidence && :verify` | passou | run `20260804070811`, verified |
| 9 | Contrato de redação | Inspeção manual do diff | passou | nenhum hash/secret/UUID/motivo deleted exposto |
| 10 | Estados obrigatórios | Inspeção manual | falhou | `id="preferences"` duplicado, ver item abaixo |

### Objeções de execução

| Severidade | Objeção | Evidência | Remédio |
|------------|---------|-----------|---------|
| bloqueante | Dois cards (`LocaleCard` e `CheckoutPolicyCard`) compartilham o mesmo `id="preferences"`; o link `#preferences` da navegação local não alcança o formulário de política de checkout. | `src/app/admin/accounts/[id]/page.tsx:317` e `:352` | Renomear a âncora de um dos cards e atualizar `SECTIONS`/`SectionNav` para refletir as subseções distintas, ou fundir os dois formulários em um único card `preferences`. |

## Eixo de decisão — ataque à defesa

| Severidade | Decisão atacada | Objeção | Evidência | Remédio |
|------------|-----------------|---------|-----------|---------|
| bloqueante | 1 — organização do editor em seções numeradas/âncoras com navegação local | A decisão não foi executada corretamente: `LocaleCard` e `CheckoutPolicyCard` usam `anchor="preferences"`, gerando `id` duplicado e ocultando o formulário de checkout da navegação por âncora/teclado — exatamente o falsificador declarado na defesa ("navegação local oculta conteúdo de leitores"). | `src/app/admin/accounts/[id]/page.tsx:317` e `:352`; `.defense.md` decisão 1 | Garantir `id` único por seção e ajustar `SECTIONS` + `SectionNav` para cobrir todas as seções acessíveis. |

## Resultado

- **Eixo de execução:** 1 objeção material (1 bloqueante).
- **Eixo de decisão:** 1 objeção material (1 bloqueante).
- **Superfície coberta:** diff de `src/app/admin/accounts/**`, `src/app/admin/admin-surface.tsx`, `src/i18n/dictionaries/admin-user-profile/*`, `tests/admin-users.evidence.spec.ts`, `scripts/check-design-tokens.mjs`; critérios 1–8 re-executados; critérios 9–10 inspecionados manualmente.
- **Não examinado:** arquivos auxiliares `src/data-directory/ui/data-directory.tsx`, `src/components/ui/native-select.tsx`, `src/app-shell/app-shell.css` não constam no diff integrado deste working tree; arquivos das tasks paralelas 12.4.1/12.4.2 deixados de fora.
