# Julgamento — 12.4.3-remodel-administrator-account-management

- **Etapa:** 005_closing (ato 1) · **Responsável:** juiz

> Artefato do gate adversarial — configuração A.

## Pedido original

- **Atendido:** parcialmente — a remodelagem de `src/app/admin/accounts/**` foi entregue, mas a navegação local do editor de perfil não cobre todas as seções como previsto na defesa.

## Julgamento item a item

| # | Objeção (eixo) | Julgamento | Motivo |
|---|----------------|------------|--------|
| 1 | `LocaleCard` e `CheckoutPolicyCard` compartilham `id="preferences"`; link `#preferences` não alcança o formulário de checkout (execução) | procedente | `src/app/admin/accounts/[id]/page.tsx:317` e `:352` usam `anchor="preferences"`; `SECTIONS`/`SectionNav` só têm uma entrada `preferences`, corroborando duplicação real e quebra de navegação local/teclado. |

## Veredito

- **Decisão:** reparo dirigido.
- **Procedentes bloqueantes:** `id="preferences"` duplicado em `LocaleCard`/`CheckoutPolicyCard`.
- **Sugestões/nits acolhidos:** nenhum.
- **Checklist humana:** nenhum.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

## Delta da devolução

- **Tipo:** execucao.
- **Critérios afetados:** 10 — estados obrigatórios/navegação local.
- **Frentes afetadas:** F7 (Locale/Checkout policy) — reentra em 004 para correção pontual.
- **Frentes intactas:** F1–F6, F8–F10 — aprovadas, permanecem integradas.
- **Ação esperada:** Renomear a âncora de `CheckoutPolicyCard` (ou `LocaleCard`) para valor único, atualizar `SECTIONS`/`SectionNav` para refletir duas subseções distintas, e garantir que cada link de navegação local aponte para um único `id`.

## Adendo — reparo da rodada 1

- **Reparo aplicado:** `LocaleCard` e `CheckoutPolicyCard` foram convertidos em `LocaleForm`/`CheckoutPolicyForm` e agrupados sob `PreferencesCard` com `anchor="preferences"`; `SECTIONS`/`SectionNav` mantêm uma única âncora `preferences`; numeração ajustada (`StorefrontCard` de 6 para 5).
- **Verificação executada:** `pnpm typecheck`; `pnpm test src/app/admin/accounts/[id]/page.test.tsx` (14 pass); `pnpm check` (1980 pass, 28 skipped); `pnpm admin-users:evidence` + `:verify` (run `20260804072519`) — tudo passou.
- **Avaliação:** a objeção de `id="preferences"` duplicado foi eliminada sem introduzir novo dano; a navegação local cobre todas as seções listadas.
- **Veredito final:** aprovada → ato 2. Nenhuma devolução.
