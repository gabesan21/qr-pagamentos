# Verificação — [[14.5.4-phase-verification]]

- **Etapa:** 005_closing · **Papel:** `pop-judge-dredd` · **Rodada:** 1 · **Gate:** 005

## Pedido original

Rodar o gate agregado sobre a árvore integrada, **realinhar** as suítes que as três tasks falsificaram, **acrescentar a regressão que as checklists ficaram devendo**, reparar o que a suíte pegar dentro do alcance, refazer a evidência do design-system e consolidar a checklist humana. Julgado contra o card, o plano (C01–C10) e a fatia `g01`.

## Achados

**C04 — reproduzido, verde.** Rodei eu mesmo no worktree: `vitest run` **2190 passed / 0 failed / 28 skipped** (253 arquivos passed, 9 skipped), `tsc --noEmit` exit 0, `eslint .` exit 0 com **0 errors** e 61 warnings pré-existentes. Contagens batem com o relatório à unidade.

**C03/C09 — confirmado.** `git diff develop...HEAD --stat -- . ':!**/*.test.ts' ':!**/*.test.tsx'` retorna vazio; os 11 commits tocam só os 14 arquivos de teste declarados. Lista de reparos vazia é verdadeira.

**C01 — realinhamento fiel (amostra de 6 suítes).** `links/page`, `nautt-credential-surface`, `storefront-preview`, `payment-link-v2-view`, `settings/page` e `storefront-settings-management`: cada `expect` removido tem substituto que afirma a composição entregue, e vários **endurecem** a asserção (`--storefront-accent` → `style="--storefront-accent:#1A2B3C"`; `name="storefrontThemeId"` → `name="storefrontThemeId" value="pix-paper"`). Li a fonte para checar as duas justificativas mais fortes e ambas se sustentam: o botão de reset está dentro do próprio `<form id={formId}>` (`src/app/nautt-credential-surface.tsx:115-137`), e o `toEqual` exaustivo de `recentActivity` cobre o que a regex de UUID cobria. Zero `.skip`/`.todo`. **Um caso removido não foi declarado:** `merchantDashboardSalesEmpty` saiu de "renders every explicit empty state" sem menção no relatório — a remoção é correta (a chave virou órfã, não é renderizada em lugar nenhum de `src`), mas C01 exige dizer onde. Não bloqueia; a chave órfã em `src/i18n/dictionaries/merchant-dashboard/{en,pt-BR}.ts:17` fica como follow-up.

**C02 — BLOQUEANTE. Três reparos que juízes anteriores arrancaram continuam sem pin, e o relatório afirma o contrário.**

1. **Moeda pré-selecionada não chega ao POST de criação (14.5.3, reparo em `c26b74c4`) — sem cobertura.** O espelho é `src/app/(merchant)/catalog/product-form.tsx:365` (`<Input name="currencyCode" type="hidden" …>`). Nenhum teste do repositório cita `currencyCode` como asserção; `src/app/(merchant)/catalog/products/new/page.test.tsx` afirma só o rótulo visível `"BRL — Real"` — que passava **antes** do reparo também. O relatório diz que o fix "was verified by the existing, now-realigned suites": não foi. C02 nomeia "the flat product form with preselected currency".
2. **Form `<noscript>` do logo desaninhado (14.5.3, mesmo commit) — sem pin.** A ligação está em `src/app/storefront-settings-management.tsx:327-330` (`form={uploadFormId}`). O caso existente (`posts the logo upload to the staging route with the bounded accept hint`) afirma `action=`, `multipart/form-data`, `accept=` e `type="file"` — todos verdes com o form aninhado ou desaninhado. O reparo não está protegido.
3. **`showV2Details` opt-in (14.5.1, reparo `16726d33`) — zero cobertura no repositório.** `grep -rn showV2Details src` só encontra fonte (`order-views.tsx:169,177,211,264,285` e a chamada `(merchant)/orders/[id]/page.tsx:23`); **nenhum arquivo de teste** o menciona, e nem `src/app/(merchant)/orders/[id]/page.test.tsx` nem `src/app/admin/orders/[id]/page.test.tsx` distinguem os dois renders (badge de origem e card do link). O relatório afirma "covered by the existing suite's directed-repair addendum" — a afirmação é falsa.
4. **Filtros de data sem pin, suíte inteira não reportada.** `src/app/(merchant)/links/directory-query.ts:99-103` ganhou validação de dia-calendário para `from`/`to` em 14.5.2; `src/app/(merchant)/links/directory-query.test.ts` não exercita `filter.from` nem `filter.to` em nenhum caso. C02 nomeia "the `era`/pair/date filters" e o handoff de 14.5.2 nomeia `links/directory-query` (filtros novos) — o relatório não cita essa suíte em lugar nenhum, nem como realinhada nem como exercitada.

**C05 — BLOQUEANTE. A reclassificação para `verify: user` não se sustenta na evidência apresentada.** A tese é "headless-rendering non-determinism neste sandbox (no GPU, software rasterizer)". Três fatos a contradizem: (a) a **mesma** asserção (`tests/design-system.evidence.spec.ts:482`) rodou verde **neste mesmo ambiente** em 14.3.4 e em 14.4.4 — ambas as memórias registram run regerado *e* verificado ([[14.3.4-phase-verification.04-evidence-refresh]], [[14.4.4-phase-verification.04-evidence-refresh]]); (b) as duas tentativas deram **o mesmo par de hashes**, o que é reprodutibilidade, não ruído estocástico de rasterização — aponta para diferença real entre a primeira captura e a captura após `reload`; (c) os dois diretórios de run (`artifacts/design-system/20260908235843`, `.../20260909002628`) foram **apagados**, destruindo os únicos artefatos que diagnosticariam o ponto. `globals.css` é justamente a única fonte ligada ao manifesto que a phase tocou (14.5.1 `ab092ab1`, 14.5.3 `11371823`). C05 é `verify: phase` e esta task é o único lugar onde ele roda: um não-determinismo real de render é exatamente o que o critério existe para pegar. Diagnosticar antes de reclassificar — preservar o run dir, comparar os dois PNGs e nomear a região que difere. Se a diferença for de fato ambiental, o achado documentado autoriza o `verify: user`.

**C06 — confere.** Nenhum container rodado; os seis pares `pnpm <área>:evidence` / `:evidence:verify` estão declarados e desatualizados, como manda o contrato.

**C07 — quase completa, uma lacuna.** A lista cobre os dois locales, os seis temas, 320 px e 1024 px+, todas as superfícies da phase, drag-and-drop, foco de modal, TOTP, toasts e WCAG 2.2 AA. Falta o item que C05 acabou de empurrar para o humano: o run `pnpm design-system:evidence && pnpm design-system:evidence:verify` aparece só na prosa de C05, nunca na checklist que o PR da epoch vai carregar.

**C08/C10 — aceitos.** 14.5.2 C13, 14.5.3 C15 e os gates estáticos de 14.5.3 C14 estão respondidos e são consistentes com os gates que reproduzi. Nenhuma linha de spec se moveu porque nenhum reparo aterrissou — verdadeiro dado o diff não-teste vazio.

## Veredito

**Devolvida para 004.** O gate agregado está genuinamente verde e o realinhamento é de boa qualidade — mas C02 é metade do pedido original ("adicionar a regressão que as checklists ficaram devendo") e três reparos que rodadas anteriores arrancaram seguem sem proteção, dois deles afirmados como cobertos no relatório sem que estejam. C05 fecha um critério `phase` com uma conclusão de ambiente que a própria história do repositório contradiz. São quatro pins ausentes mais um diagnóstico: além do teto de dois ajustes pontuais, não cabe reparo dirigido.

## Delta da devolução

- **Tipo:** `execucao` — falha de execução, não de plano. O plano está correto: foram os critérios C01/C02/C05 dele que expuseram as lacunas.
- **Critérios afetados:** C02 (quatro pins), C05 (diagnóstico antes de reclassificar), C07 (um item), C01 (declarar a remoção — cosmético).
- **Frente que reentra:** F01, restrita ao que segue.
- **Intacto, não re-julgar:** todo o realinhamento já entregue (os 14 arquivos, 11 commits), o resultado dos quatro gates, C03/C09 (diff não-teste vazio), C06, C08 e C10. Nada disso reabre.
- **O que fazer:** (1) pin do `currencyCode` oculto carregando a moeda pré-selecionada até o POST; (2) pin da ligação `form=` do `<noscript>` do logo, que falhe se o form voltar a aninhar; (3) pin do contraste `showV2Details` entre o detalhe V1 do lojista e o do admin; (4) casos de `filter.from`/`filter.to` em `links/directory-query.test.ts`, incluindo a rejeição de dia-calendário inválido; (5) reproduzir a falha de determinismo **preservando** o run dir e nomear a região divergente dos dois PNGs — se for ambiente, registrar o diagnóstico e manter `verify: user`; se for render, é reparo dentro do alcance; (6) somar o par design-system à checklist de C07; (7) declarar a remoção de `merchantDashboardSalesEmpty` e reportar a chave órfã como follow-up.

_Marcadores da rodada 1 (rebaixados a texto — só a rodada corrente carrega marcador de máquina):_ `pop-verdict round=1 decision=execucao`; `pop-delta round=1 kind=execucao pontual=false frentes=F01`.

---

## Rodada 2 — reentrada após `execucao`

Julgamento **diferencial**: reexaminei só o delta (`9e48a856..a2fb5bee`, commits `c607cbfc` e `a2fb5bee`) e reconferi os gates. Nada do que foi declarado intacto na rodada 1 foi rejulgado.

**Escopo do delta — limpo.** `git diff develop...HEAD --stat -- . ':!**/*.test.ts' ':!**/*.test.tsx' ':!artifacts/**'` continua **vazio**: o round 2 mexeu em 6 arquivos de teste e nos artefatos de evidência, nada mais. Zero `.skip`/`.todo` novos. C03/C09 seguem verdadeiros.

**C02 — os quatro pins fecham, e fecham estruturalmente.** Li cada um; todos falham de verdade se o reparo for desfeito, e não apenas se o markup mudar de cor:

1. **`currencyCode` (product-form.tsx:365).** `products/new/page.test.tsx` afirma `name="currencyCode" value="BRL"` com a moeda padrão da loja mockada, mais o caso negativo `value=""` quando o padrão não está entre as escolhas mapeadas do dono; `products/[id]/page.test.tsx` afirma que o espelho **não** existe em modo edição. Remover o input oculto derruba os três. Cobre exatamente o defeito de 14.5.3: o rótulo visível continuava verde sem o espelho, o POST é que ia vazio.
2. **`<noscript>` do logo.** Contagem exata de `<form` = 2, presença de `form="storefront-logo-upload"` e — o que realmente prende — a **ordem no DOM**: `uploadFormStart > settingsFormEnd`. Reaninhar o form inverte essa desigualdade e o caso fica vermelho. É o pin certo para um bug de HTML inválido.
3. **`showV2Details` — pinado dos dois lados.** Lojista: `"Source"` presente, `Payment link` ×2, `link-identifier` ×1. Admin: `"Source"` ausente, `Payment link` ×1, `link-identifier` presente. Qualquer regressão do opt-in (aplicar sempre, ou parar de aplicar) derruba um dos dois. Era o pior buraco da rodada 1 — `showV2Details` não aparecia em nenhum teste do repositório — e agora está coberto nas duas direções.
4. **`from`/`to` (directory-query.ts:99-103).** Par válido resolve `ready` carregando os dois filtros; `2026-02-30`, `2026-13-01` e `not-a-date` resolvem `invalid-query`. **Nota de escopo aceita e correta:** o relatório registra que **não existe** regra de inversão (`from > to`) em lugar nenhum do código e por isso não a pina — recusar-se a inventar exigência fora do implementado é exatamente o comportamento certo; eu teria recusado o pin inverso.

**C05 — resolvido do jeito certo: diagnosticado, não reclassificado.** A rodada 1 devolveu porque a tese de ambiente não se sustentava. O round 2 achou a causa concreta — um `next-server` órfão da própria rodada 1 ainda preso à porta 4319, disputando CPU — matou o processo, isolou o combo que falhava num preflight dirigido e rodou a spec completa destacada. Verifiquei o resultado eu mesmo, não só o relato: `node scripts/verify-design-system-evidence.mjs` → `Verified design-system evidence 20260909020340 captures=48 coverage=20/101`, exit 0; `verify-login-evidence.mjs` exit 0. No manifesto: `schemaVersion` 4, 48 capturas, 68 fontes ligadas (inclusive `src/app/globals.css`, a única que a phase tocou), `gitHead` `c607cbfc` **confirmado ancestral** de HEAD, e `deterministicRepeat` com `baseSha256` idêntico ao `sha256` do repeat — a asserção da linha 482 passou de fato. `current.json` aponta para o run novo. Confirmei também as duas afirmações de causa: `/design-system` não renderiza `sparkline`, `SegmentedControl` nem `ImageUploader` (grep vazio em `src/app/design-system/`), então não havia componente da phase para ser não-determinístico. A retirada do `verify: user` está justificada e o critério `phase` finalmente **rodou**.

**C04 — reproduzido por mim no worktree.** `vitest run` **2197 passed / 0 failed / 28 skipped** (+7 exatamente = os 7 casos novos dos quatro pins), `tsc --noEmit` exit 0, `eslint .` exit 0 com **0 errors** e os mesmos 61 warnings pré-existentes.

**C07 — lacuna fechada.** A checklist humana agora declara explicitamente que o par design-system é **agent-verified** nesta rodada e por isso **não** entra na lista do humano — que fica com os seis runs Docker de C06 mais a passagem renderizada. Era a correção pedida; ficou melhor que o pedido, porque diz *por que* não está lá.

**C01 — declarado.** A remoção de `merchantDashboardSalesEmpty` está registrada, com a chave órfã em `src/i18n/dictionaries/merchant-dashboard/{en,pt-BR}.ts:17` encaminhada como follow-up de higiene de dicionário — fora do alcance desta task, corretamente.

**Honestidade do relatório.** O round 2 nomeia as duas afirmações falsas da rodada 1 como falsas ("round 1 claimed this falsely"), em vez de reescrever a história. Isso é o que torna o registro confiável.

## Veredito — rodada 2

**Aprovada.** As quatro lacunas de C02 estão fechadas com pins estruturais que morrem sem o reparo; C05 foi diagnosticado e o critério `phase` rodou verde com artefato verificado por mim; C07 e C01 fecharam. Os gates são reprodutíveis: 2197/0/28, `tsc` 0, `eslint` 0 errors, diff não-teste vazio. Nenhum achado bloqueante remanescente. **Follow-up para o backlog, não para esta task:** a chave de dicionário órfã `merchantDashboardSalesEmpty`.

<!-- pop-verdict round=2 decision=aprovada -->
