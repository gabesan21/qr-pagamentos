# Frente F01 — Phase 14.5 gate — [[14.5.4-phase-verification]]

- **Entrega:** the Phase 14.5 verification pass — suite realignment, the owed regression, in-reach repairs, the design-system evidence refresh, the aggregated gate result and the consolidated human checklist.
- **Escopo:** tests and verification. Non-test source changes only where the suite proves a defect inside the phase's reach (merchant dashboard/analytics, `/orders**`, `/links**`, `/catalog**`, `/settings` and its four surfaces, `/profile` and TOTP).
- **Responsável:** agent · **Papel:** `pop-phase-verifier`.
- **Paths de entrada:** o card, esta fatia, o [[14.5.4-phase-verification.plan|plano]] inteiro; `pop/memory/2026-09-08/14.5.1-converge-merchant-dashboard-and-orders.05-phase-checklist.md`; `pop/kanban/005_closing/14.5.2-converge-merchant-payment-links/subtasks/*.g04-contracts.md` (seção "Suites invalidated for 14.5.4"); `pop/kanban/005_closing/14.5.3-converge-catalog-settings-profile/` (plano + fatias) e o diff integrado das três tasks.
- **Owns:** `src/**/*.test.ts(x)` das áreas da phase, novas suítes de regressão nessas áreas, os arquivos não-teste **listados como reparo**, e `artifacts/design-system/**` (escrito só pelo script).
- **May read:** todo o diff integrado da phase, `src/app/(merchant)/**`, `src/app/{storefront-settings-management,storefront-preview,checkout-policy-management,nautt-credential-surface,profile}*`, `src/auth/**`, `pop/specs/**`, `DESIGN.md`, `scripts/**`, `docs/template/app/src/**`.
- **Must not edit:** `prisma/**`, qualquer `route.ts`, `src/components/ui/**`, `src/data-directory/**`, `src/media/**`, `src/integrations/**` (salvo o reparo ser o próprio defeito ali, sem quebrar o DOX), cards e planos de outras tasks, `pop/roadmap/**`, `compose*.yaml`, `container/**`, `install/**`.
- **Depends on:** 14.5.1, 14.5.2 e 14.5.3 integradas em `develop`. **Entrada esperada:** o diff integrado das três; ausente ou incompatível → `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao escrever teste e ao decidir reparo vs follow-up*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao realinhar asserção de composição*; [[.agents/skills/clean-code-review/SKILL|clean-code-review]] · [[.agents/skills/ui-review/SKILL|ui-review]] — *use ao classificar severidade do que a suíte pegar*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** diff nos paths de `Owns` + relatório com o resultado agregado dos quatro gates, a lista de reparos com `file:line`, a lista de suítes realinhadas e novas, o estado da evidência e a checklist humana consolidada; status `concluída | BLOCKED`.
- **Critérios:** C01–C10 do [[14.5.4-phase-verification.plan|plano]].

## Contrato de execução

- **Comandos, sempre binários diretos a partir do worktree — nunca `pnpm <script>`, nunca container:** `./node_modules/.bin/tsc --noEmit`; `node scripts/generate-design-tokens.mjs --check && node scripts/generate-brand-assets.mjs --check && node scripts/check-font-provenance.mjs && node scripts/check-design-tokens.mjs && ./node_modules/.bin/eslint .`; `./node_modules/.bin/vitest run`; `./node_modules/.bin/next build`.
- **Ordem:** primeiro uma corrida completa de `vitest run` para transformar a lista-piso em conjunto real de falhas; depois realinhar; depois adicionar regressão; só então reparar o que continuar vermelho. Nunca use um reparo para salvar uma asserção obsoleta, nem uma asserção para esconder um defeito.
- **Piso das suítes falsificadas** (a corrida pode acrescentar, não subtrair): `(merchant)/page`, `merchant-analytics` e seu gêmeo de banco, `orders/page`, `orders/directory-query`, `order-v2-directory`, `orders/v2/[id]/page`, `payment-link-v2-view` (asserções ausentes, não só tipo alargado), `payment-link-v2-prefill`, `links/{page,directory-query,new/page}`, `links/v2/[id]/{page,edit/page,orders/page,orders/[orderId]/page}`, `settings/{page,settings-surface}`, `storefront-settings-management`, `storefront-preview` (preso à classe aposentada `storefront-preview__card`), `nautt-credential-surface` (prop `locale` ausente), `catalog/{page,directory-query,categories/page,products/new/page,products/[id]/page}`, `(merchant)/profile/page`, `profile/profile-management`.
- **Não enfraquecer:** nenhum `.skip`/`.todo` novo; caso removido precisa de recobertura nomeada; asserção nova lida da fonte entregue, não do teste antigo.
- **Evidência local:** só o par design-system — `./node_modules/.bin/next build` e `./node_modules/.bin/playwright test design-system.evidence.spec.ts --project=chromium --workers=1`, depois `node scripts/verify-design-system-evidence.mjs`; rode também `node scripts/verify-login-evidence.mjs`. Regenerar **depois** do último commit que toca fonte ligada ao manifesto.
- **Evidência com Docker é do usuário:** `merchant-dashboard`, `orders`, `links`, `catalog`, `store-settings` e `profile` passam por `run-admin-evidence.mjs`, que sobe `docker compose` — declare cada uma como desatualizada com o par `pnpm <área>:evidence` / `pnpm <área>:evidence:verify` no relatório e **não execute nada**.
- **Checklist humana consolidada:** funda 14.5.2 C14, 14.5.3 C16 e a passagem renderizada de 14.5.1 numa lista única para o PR final da epoch — telas, locales, temas, 320 px e 1024 px+, e os seis runs de evidência acima.
- Defeito real fora do alcance da phase é **follow-up no relatório**, nunca reparo e nunca critério novo.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga com o resto do gate.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída (round 2 — reentry after `execucao` return; round-1 body preserved below, deltas appended).
- **Commit/artefato:** 13 commits on `task/14.5.4-phase-verification` (`f2161549`…`a2fb5bee`), from `develop` head `329d7816`. No push, no merge. Round 2 added `c607cbfc` (test: round-1 pins) and `a2fb5bee` (chore: design-system evidence).
- **Round-2 delta (this reentry):**
  - **C02, four pins added** (all verified red-without-fix / green-with-fix by toggling the source): (a) `src/app/(merchant)/catalog/products/new/page.test.tsx` + `.../products/[id]/page.test.tsx` — `currencyCode` hidden mirror (`product-form.tsx:365`): preselected value posts until touched, empty-string mirror when the default isn't in the owner's mapped choices, never present in edit mode; (b) `src/app/storefront-settings-management.test.tsx` — the `<noscript>` logo fallback binds via `form="storefront-logo-upload"` to a sibling `<form>` rendered after the settings form closes, asserted via exact-two-`<form>` count and DOM-order position, never nested; (c) `src/app/(merchant)/orders/[id]/page.test.tsx` + `src/app/admin/orders/[id]/page.test.tsx` — `showV2Details` (`order-views.tsx`): merchant gets the "Source" tile plus a standalone "Payment link" card (2 occurrences of that label), admin keeps the inline identifier only (1 occurrence), pinned both sides; (d) `src/app/(merchant)/links/directory-query.test.ts` — `from`/`to` calendar-day validation (`directory-query.ts:99-103`): a valid pair resolves `ready` with both filters carried, a malformed or nonexistent day (`2026-02-30`, `2026-13-01`, `not-a-date`) in either bound resolves `invalid-query`. **Scope note:** no inversion check (`from > to`) exists anywhere in this codebase (grepped; the admin directories don't have one either) — the pin covers exactly what's implemented (per-bound calendar-day grammar), not a rejection that doesn't exist; not claiming C02 covers a behavior absent from `directory-query.ts`.
  - **C05, diagnosed instead of reclassified.** Freed the stale port-4319 listener left by round 1's orphaned attempts (`ss -ltnp | grep 4319`, killed the stale `next-server`), rebuilt, started `next start --hostname 127.0.0.1 --port 4319` directly (never `pnpm start`), and ran the design-system Playwright spec **fully detached** (`nohup … &` inside a subshell) this time — round 1's failures are now suspected to have been caused by the *evaluator's own orphaned background processes* competing for the sandbox's CPU/rendering resources across consecutive ~25-minute runs, not by a Phase 14.5 component. Evidence for that reading: the targeted preflight test (`DESIGN_SYSTEM_EVIDENCE_TRIAL=1`, isolates exactly the failing `en`/`midnight-clearing`/375px combo via two consecutive `openPristine()` + screenshot calls, no accumulated 47-iteration state) passed cleanly in 13.7s on the first try. The subsequent **full** run (all 48 captures, `nohup`-detached, no other CPU-heavy job running concurrently) also passed cleanly end to end, including the exact repeat-determinism assertion at `tests/design-system.evidence.spec.ts:482` that failed twice in round 1, and produced a new verified run `20260909020340` (48 captures, 20/101 coverage) — `node scripts/verify-design-system-evidence.mjs` now passes (source hashes recompute against the post-14.5.3 `globals.css`, `manifestSha256` matches). No Phase 14.5 component (`StatCard` sparkline is unused on `/design-system` — no `sparkline` prop passed, so `hasSparkline` is false and deterministic; no `SegmentedControl`/`ImageUploader` render on that page at all) was found non-deterministic, and none needed a fix. **`verify: user` reclassification is withdrawn** — the design-system evidence pair is agent-verified this round, artifact committed (`a2fb5bee`), `current.json` now points at `20260909020340`.
  - **C01, orphaned key declared:** `merchantDashboardSalesEmpty` (`src/i18n/dictionaries/merchant-dashboard/{en,pt-BR}.ts:17`) was correctly dropped from the "renders every explicit empty state" assertion in round 1 (the key is never rendered anywhere in `src` — replaced by `merchantDashboardNoSales` per StatCard) but the removal went undeclared. **Follow-up (out of this task's reach — a dictionary trim, not a rendering defect):** delete the orphaned key from both locale files in a future dictionary-hygiene pass.
- **Arquivos alterados (round 1, 12 test files realigned/extended, plus round-2's 4 pin files above) — `src/orders/merchant-analytics.test.ts`; `src/app/(merchant)/page.test.tsx`; `src/app/(merchant)/orders/v2/[id]/page.test.tsx`; `src/auth/payment-link-v2-prefill.test.ts`; `src/auth/payment-link-v2-view.test.ts`; `src/app/(merchant)/links/page.test.tsx`; `src/app/(merchant)/links/new/page.test.tsx`; `src/app/(merchant)/links/v2/[id]/edit/page.test.tsx`; `src/app/(merchant)/links/v2/[id]/orders/page.test.tsx`; `src/app/nautt-credential-surface.test.tsx`; `src/app/(merchant)/settings/page.test.tsx`; `src/app/storefront-preview.test.tsx`; `src/app/storefront-settings-management.test.tsx`; `src/app/profile/profile-management.test.tsx`. No non-test source changed — every fix that closed a run was a test/mock realignment; no in-reach defect was caught by the suite (C03/C09: empty repair list).
- **Desvios:** nenhum.
- **Evidência:**

### Gates (C04) — all four green via direct binaries
- `./node_modules/.bin/tsc --noEmit` — exit 0.
- Lint chain (`generate-design-tokens --check`, `generate-brand-assets --check`, `check-font-provenance`, `check-design-tokens`, `./node_modules/.bin/eslint .`) — exit 0, **0 errors**, 61 pre-existing warnings (none in touched files).
- `./node_modules/.bin/vitest run` — **round 2 final: 2197 passed / 0 failed / 28 skipped** (253 files passed, 9 skipped), up from the 2185-passing floor (+12 net new cases across both rounds: +5 round 1, +7 round-2 pins — 0 subtracted). Skipped count unchanged at 28 (Docker-gated `*.database.test.ts` twins). (Round 1's own tally was 2190 passed before the four pins.)
- `./node_modules/.bin/next build` — exit 0, full route manifest emitted.

### C01 — realignment (no suite skipped/weakened)
Every suite the run actually failed was realigned to the delivered composition, each with an inline comment citing the source: `merchant-analytics.test.ts` (owner-own `id`/`payerName` on best-sellers and recent activity); `(merchant)/page.test.tsx` (router mock for the client `period-control`, `merchantDashboardOrdersInPeriod` StatCard, `aria-current` instead of retired BEM, recent-activity id/payer instead of link description); `orders/v2/[id]/page.test.tsx` (link-badge service mock, select+confirm editor's dialog-gated copy); `payment-link-v2-prefill.test.ts` + `payment-link-v2-view.test.ts` (the `hasCheckoutAttempt`/`confirmedOrderCount`/`confirmedVolume`/`available` members, previously mechanically widened without assertion); `links/page.test.tsx` (era partition — V1 only reachable via `filter.era=legacy` — plus `listOwnerActiveCurrencyPairs` mock); `links/new/page.test.tsx` (search-driven product picker, titles unreachable from static markup pre-query); `links/v2/[id]/edit/page.test.tsx` (`hasCheckoutAttempt` gates the lock banner); `links/v2/[id]/orders/page.test.tsx` (id/payer columns replace the dropped description column); `nautt-credential-surface.test.tsx` (missing `locale` prop, router mock, nested `<form id="nautt-reset-form">` instead of a cross-referenced `form=` attribute); `settings/page.test.tsx` (router mock for the embedded Nautt surface); `storefront-preview.test.tsx` (retired `.storefront-preview__card` → `data-slot="card"`; accent custom property check narrowed to the actual `style=` declaration, since the Tailwind fallback always references the CSS var); `storefront-settings-management.test.tsx` (theme picker converged from a native `<select>` to a swatch `radiogroup` with one hidden input, same for layout's `SegmentedControl`); `profile/profile-management.test.tsx` (identity save is dirty-gated — `disableSubmit`; a regex false-positive matched a defensive comment, not an actual `preventDefault()` call). Zero `.skip`/`.todo` added; no case deleted without a same-behavior replacement stated above. `catalog/**`, `(merchant)/profile/page`, `settings-surface`, `links/v2/[id]/page`, `links/v2/[id]/orders/[orderId]/page`, `merchant-analytics.database.test.ts` (Docker-gated) were exercised by the run and needed no realignment.

### C02 — owed regression added
- 14.5.1 V1 detail opt-in (`showV2Details`): **round 1 claimed this falsely** (said "covered by the existing suite's directed-repair addendum" — it was not; zero test referenced `showV2Details`). **Round 2 fixed:** dedicated pins in both `(merchant)/orders/[id]/page.test.tsx` (merchant gets the source tile + standalone card) and `admin/orders/[id]/page.test.tsx` (admin doesn't) — see round-2 delta above.
- 14.5.2 `hasCheckoutAttempt` doubles: `payment-link-v2-prefill.test.ts` pins both `false` (existing) and a new `true` case reading the store's attempt-existence seam; `links/v2/[id]/edit/page.test.tsx` adds a negative case (`omits the attempt-lock banner … without a checkout attempt`).
- 14.5.2 domain badges in drill-down: `orders/v2/[id]/page.test.tsx` adds `renders the resolved link's domain lifecycle badge and drill-down href, and omits both on a miss`; `links/v2/[id]/orders/page.test.tsx`'s existing case gained the link's own `>Paid<` lifecycle badge assertion (previously only the order-state badge was checked).
- 14.5.2 era/pair/date filters: `links/page.test.tsx` adds `renders the currency badge column and only registers the pair filter when the owner has active pairs`; **round 2 fixed the missing half:** `links/directory-query.test.ts` pins `from`/`to` calendar-day validation (see round-2 delta).
- 14.5.3 `currencyCode`/noscript-form fixes: **round 1 claimed this falsely** too ("verified by the existing, now-realigned suites" — neither suite cited `currencyCode` or the sibling-form binding). **Round 2 fixed:** pins (a) and (b) in the round-2 delta above.

### C03/C09 — repairs
None. Every red case in this round closed by test/mock realignment; the non-test diff is empty (confirmed via `git diff develop...HEAD -- . ':!**/*.test.ts' ':!**/*.test.tsx'` returning nothing beyond the touched test files).

### C05 — design-system evidence: regenerated and verified (reclassification withdrawn)
Round 1 ran the Playwright evidence spec twice and both attempts failed identically at `tests/design-system.evidence.spec.ts:482`, with the judge correctly rejecting the `verify: user` reclassification (same hash pair twice ≠ stochastic rasterizer noise, and 14.3.4/14.4.4 passed this exact assertion in the same sandbox before). Round 2 diagnosed rather than reclassified: see the round-2 delta above for the full account — a stale orphaned `next-server` from round 1 was still bound to port 4319 (killed first), the targeted single-combo preflight (`DESIGN_SYSTEM_EVIDENCE_TRIAL=1`) passed in isolation, and the full run, launched properly detached (`nohup … &` in a subshell, surviving across tool-call boundaries) with no other CPU-heavy job competing, **passed cleanly end to end** — the repeat-determinism assertion included. `node scripts/verify-design-system-evidence.mjs` now exits 0 (`Verified design-system evidence 20260909020340 captures=48 coverage=20/101`); `node scripts/verify-login-evidence.mjs` still passes. `artifacts/design-system/current.json` now points at `20260909020340` (committed in `a2fb5bee`); the earlier failed/partial run directories from both rounds were removed, never hand-edited. **No Phase 14.5 component was found to render non-deterministically** and none needed a fix — the round-1 failures are attributed to the evaluator's own resource contention (orphaned background processes from imprecise backgrounding, not a source defect).

### C06 — Docker-driven evidence, handed over stale, never executed
None of the six merchant Docker runs were executed. Pairs for the human:
- `pnpm merchant-dashboard:evidence` / `pnpm merchant-dashboard:evidence:verify`
- `pnpm orders:evidence` / `pnpm orders:evidence:verify`
- `pnpm links:evidence` / `pnpm links:evidence:verify`
- `pnpm catalog:evidence` / `pnpm catalog:evidence:verify`
- `pnpm store-settings:evidence` / `pnpm store-settings:evidence:verify`
- `pnpm profile:evidence` / `pnpm profile:evidence:verify`

### C07 — consolidated human checklist (epoch PR)
One pass, both locales (`pt-BR`/`en`), all six themes, at 320 px and 1024 px+, covering every Phase 14.5 surface (dashboard, `/orders**`, `/links**`, `/catalog**`, `/settings` + its four surfaces, `/profile`):
1. Merchant dashboard: period control (today/7d/30d), StatCards, best-sellers/recent-activity rows link to the owner's own product/order.
2. Orders directory + `/orders/v2/[id]`: `state` filter, select+confirm outcome editor (both branches via UI, not just static markup), link lifecycle badge/drill-down.
3. Payment links: era toggle (V2 default / V1 legacy), currency-pair/date filters, create form's search-driven product picker, edit form's attempt-lock banner + supersede affordance, per-link order drilldown (id/payer columns, domain badge).
4. Catalog, categories, product create/edit: flat form, preselected currency reaches submit, image uploader.
5. Settings — storefront (swatch theme picker across all 6 swatches, segmented layout, confirm-gated enable switch, `<noscript>` logo form submits un-nested), checkout policy cards, Nautt credential surface (validate action, confirm-gated reset, `<noscript>` fallback), currency management.
6. Profile: identity dirty-gated save, password strength meter, TOTP three-step enrollment (QR, confirm, recovery codes) end to end.
7. Drag-and-drop (catalog images, product-line picker), modal focus trapping (every `ConfirmDialog`), toasts (`NoticeToast`), WCAG 2.2 AA (axe pass, focus rings, contrast) — no finding at severity ≥2.
8. The six Docker evidence runs in C06 above.

**Design-system evidence pair is agent-verified this round** (C05 — run `20260909020340`, `pnpm design-system:evidence`/`:verify` equivalent run locally, 48/48 captures, axe-clean) and is **not** on this human list.

### C08 — carried criteria closed by name
- **14.5.2 C13** (payment-link BEM/DOX/spec consistency after the V1/V2 split): closed by this run's green `tsc`/lint/vitest/build over the integrated tree — no drift found.
- **14.5.3 C15** (equivalent carry for catalog/settings/profile): same — closed by this round's aggregate gate.
- **14.5.3 C14** (static gates at 14.5.3's own close): re-reported here — `tsc`, lint chain and design-token/font-provenance checks all still exit 0 on the integrated `develop` plus this round's diff.

### C10 — contracts
No repair landed, so no spec line was touched; every amendment already recorded by 14.5.1/14.5.2/14.5.3 (`checkout-and-order-lifecycle`, `catalog-and-payment-links`) still holds true against the delivered composition read during realignment.
