# Frente F03 — Shell, public surfaces and gate classification — [[15.4.1-migrate-template-vocabulary]]

- **Entrega:** shell, auth, checkout, storefront, shared order views and the directory shell coherent with the declared vocabulary; the two owned-primitive compositions of F6; and the gate classification repaired so `admin:source-check` measures drift instead of sanctioned constructs.
- **Escopo:** per-family rule below; the closed substitution map and nothing else. No state, copy, dictionary, behavior, polling, capability or money change; no new component; no file created, renamed or deleted.
- **Responsável:** agent. **Papel:** `pop-executor`.
- **Paths de entrada:** card `15.4.1-migrate-template-vocabulary.md`; seções "Estratégia", "Mapa de substituição (fechado)" e "Critérios de aceite" de [[15.4.1-migrate-template-vocabulary.plan]]; esta fatia; `DESIGN.md`; [[pop/specs/application-frontend-system|application-frontend-system]]; [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]]; `src/app/globals.css`; `src/app-shell/app-shell.css`; `scripts/check-admin-ui-inventory.mjs` e `scripts/check-design-tokens.mjs`; `docs/template/app/src/pages/{auth,checkout}/**`.
- **Owns:** `src/app-shell/**/*.tsx`; `src/app/language-preference/**/*.tsx`; `src/app/orders/*.tsx`; `src/app/auth-card.tsx`; `src/app/login/**/*.tsx`, `src/app/reset-password/**/*.tsx`, `src/app/pay/[identifier]/**/*.tsx`, `src/app/store/[slug]/**/*.tsx`, **exceto** `page.tsx`, `loading.tsx` e `error.tsx`; `src/data-directory/ui/*.tsx`; `scripts/check-admin-ui-inventory.mjs`; os `*.test.tsx` colocalizados **apenas** para atualizar asserção que nomeia uma classe migrada por esta frente.
- **May read:** `src/**`, `docs/**`, `scripts/**`, `DESIGN.md`, `pop/specs/**`.
- **Must not edit:** qualquer `page.tsx`/`loading.tsx`/`error.tsx`; `docs/frontend-template-parity/**`; `src/components/ui/**`; `src/app/design-system/**`; `src/app/(merchant)/**`; `src/app/profile/**`; `src/app/admin/**`; `src/app/globals.css` e `src/app-shell/app-shell.css`; qualquer outro script além de `check-admin-ui-inventory.mjs`; specs, DOX e `DESIGN.md`; a expressão `style={branding ? (...) }` de `src/app/pay/[identifier]/checkout-shell.tsx` (autorizada por path+padrão exatos em `check-design-tokens.mjs`).
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] — *use para o inventário anti-drift antes de compor primitivo*; [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para o contrato antes de editar o gate e para a triagem do que não é desta task*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval (não yolo); nenhum delta.
- **Saída:** relatório ≤25 linhas com contagem grep antes/depois, arquivos alterados, as 3 isenções do gate com a razão de cada uma e as 2 composições; status `concluída | BLOCKED`.
- **Critérios:** 1, 2, 3, 4, 5, 6, 7 do [[15.4.1-migrate-template-vocabulary.plan|plano]].

## Contrato de execução

- **Regra por família:** auth (`auth-card`, login, reset-password) e o par de views compartilhadas de pedido (`src/app/orders/*.tsx`, ~15 ocorrências) e o shell de diretório (`data-directory-client.tsx`) migram pelo mapa; `src/app/pay/[identifier]/**` recebe **só** o mapa mecânico — 15.3.3 é dona do comportamento e dos estados explícitos, que não se reabre aqui; `src/app/store/[slug]/**` não tem tela equivalente na template (extrapolação autorizada) e mantém seu vocabulário como exceção legada permitida, salvo `font-[family-name:var(--font-display)]` → `font-display`, que é bypass do utilitário declarado, não decisão de design; o chrome BEM de `src/app-shell/**` é sancionado por `DESIGN.md` e não migra.
- **F6, composição:** o `<select>` cru de `language-preference/language-switcher.tsx` passa a compor o `NativeSelect` owned preservando rótulo acessível, `name`, submit por `onChange` e a aparência compacta; o `<button type="submit">` do account-panel em `app-shell/shell-navigation.tsx` passa a compor o `Button` owned mantendo a classe de chrome, como `shell-navigation.tsx:224` já faz.
- **F6, classificação do gate** em `scripts/check-admin-ui-inventory.mjs`, cada uma comentada com sua razão: (a) acrescentar `app-shell__account-panel`, `app-shell__account-panel-item` e `app-shell__account-panel-signout` à allow list de chrome, ao lado dos 20 irmãos já listados; (b) deixar de classificar como raw control o `<input>` sem presentação — `type="hidden"` literal e o bridge `sr-only` —, porque `DESIGN.md` sanciona os dois (14.4.3 e 14.5.2) e o conjunto `owners` congelado não tem dono a compor; (c) aceitar o `style` de propriedade customizada `--storefront-accent` exatamente onde `check-design-tokens.mjs` já o autoriza por path+padrão. Nenhuma isenção é ampla: cada uma nomeia atributo ou path.
- Se qualquer achado remanescente do gate exigir uma quarta isenção, **pare** e reporte `BLOCKED` — ampliar a regra é mudança de contrato, não reparo.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura para verificar.
- Dependência ou entrada ausente/incompatível → responda `BLOCKED` ao agente principal com evidência; não implemente nem simule dependência.
- Não altere caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <contagens grep, isenções e composições>.
