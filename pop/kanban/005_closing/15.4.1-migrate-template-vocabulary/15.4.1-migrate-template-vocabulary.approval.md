# Aprovação — [[15.4.1-migrate-template-vocabulary]]

- **Etapa:** 003_human_approval · **Responsável:** user

## Rodada 1 — 2026-09-23

### Brief para decisão

- **Entrega:** replace the legacy shadcn vocabulary with the declared template utilities in the screen families the template maps, keep the unmapped families on a recorded exception, and clear the `admin:source-check` raw-control/local-variant findings (F6) by composing owned primitives and by repairing the gate's own classification where the contract already sanctions the construct.
- **Estratégia:** The migration is a **closed, value-identical rename**, never a redesign: each pair in the map resolves to the same variable in all six themes (`src/app/globals.css:106-136,1308-1336`), so contrast, geometry and captures do not move. Anything outside the map stays untouched — the safe way to end a two-vocabulary drift is to move only what provably renders the same. Two boundaries shape the write sets. **Route files** …
- **Topologia:** `pop-execution-orchestrator` sobre três `pop-executor` paralelos — três write sets independentes por família de tela, mesma dupla de skills (`ui-change` + `clean-code-change`), sem DAG; perfis fixos por papel nativo e `size: M` altera só orçamento.
- **Risco principal:** editar arquivo de rota deixa `frontend-parity:check` stale e invade 15.4.2 — mitigação: `page.tsx`/`loading.tsx`/`error.tsx` ficam fora de todo `owns`; resíduo vira follow-up na memory.
- **Critérios principais:** 1, 2, 3, 4, 5, 6, 7, 8.
- **Plano:** [[15.4.1-migrate-template-vocabulary.plan]] — *siga para revisar o brief completo*.
- **Decisões a confirmar:** (a) `bg-primary`/`text-primary-foreground` NÃO migram para `bg-accent`/`text-accent-fg` porque o par accent reprova AA (~2.5:1) em `pix-paper`; (b) arquivos de rota (`page.tsx`/`loading.tsx`/`error.tsx`) ficam fora do diff para não gerar staleness de paridade — o legado residual neles é follow-up declarado; (c) a isenção `inline_styles` no gate `admin:source-check` para o `--storefront-accent` já autorizado por `check-design-tokens.mjs` vai um passo além do card (raw-control/local-variant) e é o único ponto a reverter se você discordar.

### Resposta do humano

Aprovado — comando explícito do usuário 2026-09-23: "execute exclusivamente 15.4.1-migrate-template-vocabulary, gate 003 aprovado pelo coordenador, até PR" (rule 20, AGENTS.md raiz). As três decisões a confirmar do brief (bg-primary/accent, arquivos de rota fora do diff, isenção --storefront-accent) ficam como propostas no plano.

- [x] Feito

### Decisão do agente

`aprovado → 004`
