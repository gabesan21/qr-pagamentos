# Front F02 — Evidence and parity binding — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** fresh, mutation-tested proof for complete coverage, real states and the 6×2×4 matrix at exact task HEAD.
- **Scope:** specimen coverage checker, runtime evidence runner/verifier, generated parity identity refresh and run artifacts.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, design-system evidence/checker scripts and tests, design-system evidence script entries in `package.json`, `artifacts/design-system/**`, canonically refreshed `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`.
- **May read:** F01 output, coverage/shared inventories, all bound owner sources, dictionaries, tokens/fonts/assets, parity checker/README, project Playwright config.
- **Must not edit:** specimen/runtime source, shared owners, `pnpm-lock.yaml`, other evidence families, template snapshot, product routes, specs/DOX.
- **Depends on:** F01.
- **Expected input:** integrated closed specimen and coverage map whose fixture identifiers are stable.
- **Skills:** `clean-code-change`, `ui-change`, `vercel-react-best-practices`.
- **Criteria:** C01-C08.

## Execution contract

- Derive assertions and source inventory from executable authorities; do not duplicate an unverified hand-maintained list in the test.
- Record exactly 48 base captures and bound real interaction/state probes; each manifest key includes locale, theme, viewport, task HEAD, source hashes and result.
- Assert both full/reduced motion, actual keyboard focus, hit targets, responsive modes, complete locale content, WCAG/axe, console/request cleanliness and final-state equivalence.
- Bind the 187 shared obligations and the current-only `/design-system` record without inventing a direct template route or raster golden; parity refresh is canonical and preserves protected fields/counts.
- Verifier mutation tests must reject owner, fixture/state, locale/theme/viewport, artifact, authority or source drift. Missing F01 input → `BLOCKED`; never edit it.

## Result

- **Status:** complete.
- **Commit/artifact:** initial atomic F02 commit `81e51fb47fe26af2ed039a759c92141da79c1903`; final repair commit recorded after this result. Current evidence run `20260803165612`, manifest SHA-256 `ff119aa7273880f7cf2b01cb9ba23bdd435a3c8985a2eb9a4e2b045be2801f06`, captured from source HEAD `81e51fb47fe26af2ed039a759c92141da79c1903`.
- **Files changed:** `tests/design-system.evidence.spec.ts`; `scripts/check-design-system-coverage.mjs`; `scripts/verify-design-system-evidence.{mjs,test.ts}`; the design-system source-check entry in `package.json`; `artifacts/design-system/{current.json,20260803163508/**,20260803165612/**}`; canonical `docs/frontend-template-parity/{manifest.json,obligations.ndjson}`; this result and task telemetry.
- **Deviations:** none in F02 scope. Evidence resumed only after the two approved dependency returns landed: the F04 OTP focus contract and the F01 narrow-viewport containment correction.
- **Evidence:** exactly 48 base captures (`2` locales × `6` themes × `4` viewports), 48 full/reduced-motion interaction probe pairs, 2,160 keyboard-focus measurements, no serious/critical axe findings, no horizontal overflow, and no console, page, or external-request failures. Coverage independently verifies `20` entries, `101` applicable states, `72` explicit N/A states, `2,230` parity records, and `187` shared obligations. Visual review covered final-run narrow, medium, and wide captures with no severity 2–4 finding. Mutation/coverage tests passed `9/9`, including evidence-only descendant acceptance, divergent-history rejection, and bound-source drift rejection. The verifier requires the captured source HEAD to remain an ancestor and all 63 bound sources to retain exact hashes, so later evidence/docs commits remain verifiable while source changes fail closed. Shared inventory, parity, typecheck, lint (0 errors; 58 pre-existing warnings), production build, and the full `pnpm check` gate passed (`229` test files passed, `9` skipped; `1,956` tests passed, `28` skipped).
