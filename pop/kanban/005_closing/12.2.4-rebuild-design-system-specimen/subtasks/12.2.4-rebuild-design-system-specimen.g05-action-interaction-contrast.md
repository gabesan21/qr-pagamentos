# Front F05 — Contrast-safe action interaction roles — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** explicit semantic default/hover/active action roles whose rendered foreground pairs pass WCAG AA in every theme.
- **Scope:** the two shared consumers with opacity-based action states and the minimal DTCG→generated-CSS lane/tests required to replace that blending.
- **Owner:** agent.
- **Owns:** `src/components/ui/button.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/interaction-colors.test.tsx`, `src/design-system/tokens/components.tokens.json`, `src/design-system/tokens/semantic.tokens.json`, `src/design-system/tokens/themes.tokens.json`, `src/design-system/tokens/themes/*.tokens.json`, `scripts/generate-design-tokens.mjs`, `scripts/design-tokens.test.ts`, `src/app/globals.css`.
- **May read:** active frontend spec/DESIGN, color/token helpers and gates, generated Tailwind output, shared inventory/DOX, F02 axe and computed-color evidence, preserved F02 browser probes.
- **Must not edit:** token resolver and non-action token roles/audit palette values; every other shared component; package/lock; specimen/dictionaries; paused F02 scripts/tests/parity files; F03/F04 output, artifacts and specs/DOX.
- **Depends on:** completed F01 plus F02 proof that settlement-sand default Button hover computes `#b06a32` under white text at 4.23:1 because opacity blends `#a85b1e` toward the light surface.
- **Expected input:** exact six-theme accent/foreground pairs, immutable audit primitives, current semantic graph/generator and the default Button/Badge variants.
- **Skills:** `color-expert`, `design-tokens`, `ui-change`, `clean-code-change`, `shadcn`.
- **Criteria:** C11.

## Execution contract

- Preserve template audit accents byte-for-byte and retain each theme's identity. Encode interaction decisions through primitive/reference → semantic → component usage; all references/types resolve in every theme context.
- Derive a gamut-safe, perceptually coherent OKLCH progression from each theme accent/foreground pair while enforcing WCAG ≥4.5:1 for default, hover and active; tests explain direction/constraints instead of pinning unrelated manual swatches.
- Replace opacity-based action-background blending in Button and any affected Badge state with semantic roles. Add no raw component color, theme branch, manual dark override, dependency or alternate visual owner.
- Focused tests cover token resolution, all-six-theme ratios, immutable audit values, monotonic/perceptual interaction intent, generated variables and consumer classes; resumed F02 supplies actual computed hover/active browser proof and axe.
- Change only files needed within `Owns`; generated CSS changes must match the generator exactly. Missing safe projection or broader palette need → `BLOCKED`, never edit another front.

## Result

- **Status:** complete.
- **Commit/artifact:** included in the final F05 commit.
- **Files changed / deviations / evidence:** Added immutable-audit-derived OKLCH action default/hover/active roles for all six themes, projected them through semantic and Button component aliases, and replaced the Button/linked-Badge opacity states. The F02 computed preflight returned one narrow correction: linked Badge now also consumes `primary-active` on mouse-down, with a focused class/markup assertion. `pnpm tokens:generate`, focused token/UI tests, `pnpm typecheck`, `pnpm lint`, `pnpm tokens:check`, and `git diff --check` pass; lint retains only pre-existing repository warnings.
