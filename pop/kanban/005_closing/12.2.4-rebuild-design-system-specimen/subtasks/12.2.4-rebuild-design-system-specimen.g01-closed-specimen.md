# Front F01 — Closed specimen composition — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** inert `/design-system` composition and executable coverage map for every shared owner and applicable state in both locales.
- **Scope:** specimen/demo code only; consume the delivered token, asset, primitive, composition and directory contracts without changing them.
- **Owner:** agent.
- **Owns:** `src/app/design-system/**`, `src/i18n/dictionaries/design-system/**`, focused specimen unit/component tests under the route subtree.
- **May read:** active frontend spec, `DESIGN.md`, parity authority/template sources, 12.2.1–12.2.3 memory, `components.json`, every shared owner and dictionary type.
- **Must not edit:** `src/components/ui/**`, `src/data-directory/**`, `src/brand/**`, `src/design-system/**`, `src/app/globals.css`, other routes/dictionaries, parity/evidence artifacts, package/lock, specs/DOX.
- **Depends on:** task 12.2.3.
- **Expected input:** stable shared inventory with 20 reachable responsibilities, primitive foundation, N/A dispositions, canonical directory and asset family.
- **Skills:** `ui-change`, `clean-code-change`, `shadcn`, `vercel-react-best-practices`.
- **Criteria:** C01-C05.

## Execution contract

- Treat the coverage map as fail-closed input: one production source/public API/applicable state per deterministic fixture or real interaction probe; preserve explicit N/A.
- Use official owned variants/composition rules and local source APIs; add/update no registry component or dependency. Keep static fixture sections server-rendered and client boundaries interaction-specific.
- Fixture data is formatted, localized, redacted and inert. It may not call services, submit real mutations, authorize capability, import a product route, or simulate missing domain behavior.
- Locale uses the existing unprefixed preference contract; theme changes semantic tokens only. Keep `[lang]/design-system` as its existing 404 guard.
- Dependency absent/incompatible → `BLOCKED`; never implement it or write outside `Owns`.

## Result

- **Status:** complete.
- **Commit/artifact:** F01 source commit in task branch history.
- **Final accessibility verification:** the completed mounted suite is `14/14`: in both `en` and `pt-BR`, all six InputOTP fixtures resolve by a localized state-qualified accessible name, and the reference input, native select, and raw Textarea resolve through their localized labels while primitive witness IDs remain on wrappers. A read-only full-page axe scan in real Chromium at 320px reports `0` violations, `0` critical/serious findings, and `52` passing rules for each locale; document geometry remains `320/320`.
- **Files changed / deviations / evidence:** rebuilt the server-first specimen and its narrow client island so the 20-entry executable map resolves every one of its `101` applicable states to a unique mounted DOM target (`52` interactive and `49` static/server) while preserving `72` explicit N/A dispositions. All `23` shared primitives now have mounted bindings. The correction adds concrete Button hover/focus/loading/disabled, Checkbox checked/focus/invalid/disabled, InputOTP active/focus/invalid/disabled, Switch checked/hover/focus/disabled, CopyField ready/pending/copied/failed/focus/disabled, LocalizedFieldGroup populated/selected/invalid/focus/disabled, SimpleTabs selected/hover/focus/disabled, Modal open/focus-loop/confirmation/pending/failed/focus-restored, Toast info/success/warning/error/dismiss/retry, QR preparing/available/waiting/recovery/terminal, StatCard empty/unavailable, Timeline empty/default/success/info/danger, DataDirectory table/filter/pagination states, and StatusBadge tone/archived fixtures. Each binding now carries a DOM-semantic witness via `data-probe`, `data-copy-state`, real component state, or CSS-module persistent hover/focus indicators. Scope remained inside `src/app/design-system/**` and `src/i18n/dictionaries/design-system/**`; no shared owner, data-directory source, brand, token, route, or dictionary was edited. Full evidence run and aggregate gate were skipped at user request after implementation was finalized.
