# Verification and critique — [[12.2.4-rebuild-design-system-specimen]]

- **Stage:** 005_closing (act 1) · **Owner:** independent reviewer

## Round 1 — 2026-08-03

- **Strategy:** differential — non-critical first round; reviewed the integrated specimen/evidence/InputOTP delta and audited prior shared owners through the bound inventory and source hashes.
- **Surface:** original What/Why, active frontend spec and DOX, `20a5a05..58837bd`, coverage map/checker, route/dictionaries, InputOTP owner/tests, evidence runner/verifier, manifest/assertions, seven representative captures, and parity/inventory/aggregate gates.
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Original request verdict

**Not satisfied.** `/design-system` is bilingual, responsive, six-theme, keyboard-tested, and reduced-motion-aware for the subset it renders, but it does not exercise every shared component and applicable state. The claimed `101` states are tuple declarations rather than DOM- or probe-bound observations, so the shared visual foundation is not yet completely observable or objectively verifiable.

### Conformance with objective and specs

| # | Criterion | Mode | Verification executed | Result | Evidence |
|---|---|---|---|---|---|
| What/Why + active spec | Complete observable shared foundation | evidence | card/spec/DOX before diff; route and captures after | failed | `coverage.ts:18-38` claims states absent from `page.tsx:44-95` and `interactive-specimens.tsx:29-58`. |
| C01 | Closed shared coverage | re-run | `pnpm design-system:source-check`; independent source/DOM comparison | failed | checker reports `20/101/72`, but only parses literals (`check-design-system-coverage.mjs:21-56`); claimed `actions`, `copy`, and `overlays` fixtures do not exist in recorded section sets. |
| C02-C03 | Boundary and bilingual equivalence | re-run/evidence | source inspection; 48 assertion records | passed | server-first inert route; `pt-BR`/`en` section sets and `lang` match. |
| C04-C05 | Interaction/focus/motion/responsive states | evidence | assertions plus seven screenshots at 320/768/1440, light/dark, both locales | failed | visible subset has no severity 2+ layout issue, focus targets pass, but missing applicable states cannot be exercised or assessed. |
| C06-C07 | Fresh matrix and source/state binding | re-run/evidence | POSTCOMMIT verifier at `58837bd`; manifest/source inspection; repeat-capture comparison | failed | verifier passes 48 captures, yet accepts unrendered state claims; repeat `en/midnight-clearing/375` differs by 23,808 pixels because one capture retains a toast. |
| C08 | Aggregate regression | re-run | focused tests; inventory; parity; `pnpm check` attempt | qualified pass | focused 10/10, inventory `187/20/49/9`, parity `2,230`, diff check pass. Aggregate reached 1,952 passing tests; four verifier tests were sandbox-blocked by `spawnSync git EPERM` and the same focused suite passed outside the sandbox. |
| C09 | Durable truth | evidence | spec/DESIGN/UI DOX and supersession audit | failed in reality | metadata and reciprocal supersession are coherent, but they currently describe the incomplete specimen as delivered. |
| C10 | InputOTP focus | re-run/evidence | focused owner test and recorded real-Tab assertions | passed | semantic 3px outline, 44px geometry, keyboard entry, and source hash pass. |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking (UI severity 3) | The coverage gate equates a hand-authored state name with an exercised state. Missing examples include Button loading/disabled, Checkbox invalid/disabled, InputOTP invalid/disabled, QR preparing/waiting/recovery/terminal, StatCard empty/unavailable, Timeline empty/default, and Toast info/warning/error/retry; `primitiveCoverage` is only a 23-name length assertion. | `src/app/design-system/coverage.ts:18-52`; `src/app/design-system/coverage.test.ts:13-29`; `scripts/check-design-system-coverage.mjs:21-56`; rendered sources above. | F01 must render or genuinely probe each applicable inventory state and each primitive; F02 must derive fail-closed DOM/probe evidence from those bindings so deleting a fixture/state fails. |
| blocking | The screenshot runner leaves toast lifetime racing the capture, so identical source produced two different images and the verifier accepts either. | `tests/design-system.evidence.spec.ts:44-62,122-140`; runs `20260803163508` and `20260803165612`, English `midnight-clearing` 375px: 23,808 differing pixels; only the latter retains the success toast. | Make interaction cleanup/state capture deterministic, regenerate the matrix, and make the verifier reject stale or divergent state evidence. |
| suggestion | F03's durable files were committed, but its front result still says `pending`. | `subtasks/12.2.4-rebuild-design-system-specimen.g03-contract-sync.md:22-26`; commit `bd1a4e6`. | Reconcile the result record when the execution delta returns; do not redo the durable contract files unless the corrected evidence changes facts. |

### Scope and integration

- [x] Production-owner edit was limited to the explicitly authorized F04 InputOTP owner/test delta.
- [x] No opportunistic domain, auth, service, dependency, or product-route implementation was found.
- [ ] Aggregate gate was not independently completed in the sandbox; the four environment failures passed in the escalated focused run.
- [ ] Specs and DOX are structurally coherent but presently overstate delivered state coverage.

## Verdict

- **Decision:** blocking execution → 004_processing.
- **Blockers:** declared states are not bound to rendered fixtures/probes; the visual evidence run is timing-dependent.
- **Plan defect:** none — C01 and C04-C07 already require the missing behavior and fail-closed evidence.
- **Suggestions/nits:** reconcile the stale F03 result after the execution delta.
- **Summary:** the route substantially improves bilingual/theme/responsive observability, and the InputOTP repair is valid, but it does not yet make the complete shared foundation observable or objectively verifiable as requested.

## Return delta

- **Type:** execucao.
- **Affected criteria:** C01, C04, C05, C06, C07, C09 — bind every applicable owner/primitive state to a real rendered fixture or interaction probe, and replace timing-dependent captures with deterministic evidence.
- **Affected fronts:** `F01` — add the missing specimen states/primitives; `F02` — enforce DOM/probe binding, mutation rejection, deterministic interaction cleanup, and regenerate current evidence.
- **Intact fronts:** `F03` — contract target remains valid and must not be reexecuted unless corrected evidence changes a durable fact; `F04` — approved InputOTP focus repair and focused test remain integrated.
- **Expected action:** return only F01/F02 to 004, then re-run C01 and C04-C09 against fresh source-bound captures; preserve F03/F04.

verificado por revisor independente (yolo) — 2026-08-03

## Round 2 — 2026-08-03

- **Strategy:** differential — the prior return is `execucao`; re-reviewed the F01/F02 correction and added F05 dependency, while auditing F03/F04 through unchanged source and bound evidence.
- **Surface:** original What/Why, C01/C04-C07/C09 and C11, `56fe14d..5dd5827`, coverage/runtime/verifier code, run `20260803193353`, representative 320/375/768/1440 captures, mutation resources, source/inventory/parity gates, and the reported aggregate retry.
- **Returns:** execution 1 of 2 · plan 0 of 2.

### Original request verdict

**Not satisfied.** The route now mounts 101 unique owner/state wrappers and 23 primitive IDs in every matrix entry, but most wrappers still do not exercise or prove the state they name. The complete shared foundation therefore remains neither observable nor objectively verifiable state by state.

### Conformance with objective and specs

| # | Criterion | Mode | Verification executed | Result | Evidence |
|---|---|---|---|---|---|
| What/Why + active spec | Complete observable shared foundation | evidence | card/spec/DOX before delta; source, assertions and captures after | failed | only 6/101 bindings are operated; screenshots show repeated default controls under different state labels. |
| C01, C04-C07 | Real fail-closed state coverage | re-run/evidence | source checker; mounted suite; manifest/assertions; mutation tests; visual audit | failed | 101 wrappers/23 primitives exist, but `captureRenderedCoverage` records only identity, child count and visibility; it never validates state semantics. |
| C05-C06 | Responsive/accessibility matrix | evidence | four representative final captures; 48 assertion records; exact repeat hash | passed for rendered content | no overlap/overflow or severity 2+ visual finding; 48 records report zero serious/critical axe findings. |
| C07 | Determinism/source binding | re-run/evidence | current pointer, manifest and repeat bytes; source check; verifier attempt | partial | repeat/base are byte-identical (`d6f66e…`) with zero toasts; verifier is source-bound, but mutation fixtures leak and the sandbox blocks its git subprocess with `EPERM`. |
| C08 | Aggregate regression | re-run/evidence | inventory, parity, focused suites, repeated OTP suite, full test attempt | failed repeatability | inventory/parity/source-check pass; 46 focused tests pass apart from 6 sandbox-blocked git cases; ten unchanged OTP runs pass, but leaked verifier fixtures exhaust `/tmp` and collapse later full runs. |
| C09 | Durable truth | evidence | F03 files and supersession metadata | failed in reality | files are unchanged and structurally coherent, but still claim complete all-state evidence. |
| C10 | InputOTP focus | evidence | unchanged owner hash plus 10 focused reruns | passed | F04 remains intact; no OTP timer/teardown failure reproduced in 10/10 unchanged runs. |
| C11 | All-theme action contrast | re-run/evidence | token/component tests and evidence-test inspection | partial | semantic default/hover/active classes and six-theme ratios pass focused tests; computed Button/linked-Badge probe exists, but the final run does not persist its results and a browser replay was sandbox-blocked. |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking (UI severity 3) | State labels remain false-positive coverage. Button `hover` is an unhovered secondary button; five SimpleTabs wrappers render the same default tabs; six CopyField wrappers differ only by value; 17 directory states are nested around one `DataDirectory`; only six bindings are operated. | `interactive-specimens.tsx:52-55,87-89,103-105`; `page.tsx:97-103`; `design-system.evidence.spec.ts:58-94,105-126`; final captures. | F01 must render or drive each named state with state-specific semantics. F02 must record a per-binding semantic/interaction witness and reject a renamed or semantically neutral fixture, not merely a missing wrapper. |
| blocking | The verifier mutation fixture copies the ~82 MB evidence run once per test and never removes its temporary root. There are 159 leaked `/tmp/design-system-evidence-*` directories consuming about 13 GB; a repeated full test run then failed broadly with write error `-122`. | `verify-design-system-evidence.test.ts:14-57,70-172`; `df`/`du`; reviewer `pnpm test`. | F02 must clean every fixture root in `finally`/test teardown and prove repeated mutation/full-suite runs leave no temp directories or bytes. |
| blocking | C11's computed browser proof is not retained by the final evidence: `probeActionColors` runs only in the skipped trial test, and its results are absent from the 48 assertions/manifest. | `design-system.evidence.spec.ts:129-193,195-289`; run `20260803193353`. | F02 must execute and persist all six Button/linked-Badge default/hover/active computed colors and ratios in the final source-bound evidence, and the verifier must fail their removal/mismatch. |

### Scope and integration

- [x] F01/F02/F05 changes stayed within their authorized write sets; F03/F04 remained intact.
- [x] No domain, auth, service, dependency, or product-route behavior was introduced.
- [ ] Aggregate and postcommit verifier gates are not repeatably clean because of the mutation-fixture leak; local git spawning is additionally sandbox-blocked.
- [ ] F03/spec/DOX structure is coherent but overstates the unproved all-state result.

### OTP transient judgment

The reported first `pnpm check` post-test `input-otp` jsdom timer exception is not reproducible on the available evidence: the unchanged retry passed, the integrated focused batch passed the OTP test, and ten additional isolated unchanged runs passed 10/10. It is not a separate blocker. The reproducible flake is instead the verifier's unbounded `/tmp` leak, which materially breaks later aggregate runs and must not be waived as environment noise.

## Verdict — Round 2

- **Decision:** blocking execution → 004_processing.
- **Blockers:** state names are still not state-semantic evidence; mutation fixtures leak until aggregate tests fail; final evidence omits persisted computed C11 proof.
- **Plan defect:** none — C01, C04-C08 and C11 already require real probes, fail-closed evidence, deterministic repeatability, and computed browser contrast.
- **Suggestions/nits:** none.
- **Summary:** DOM identity, responsive layout, toast determinism, F04 focus, F05 tokens and static contrast are improved, but the requested complete objectively verifiable specimen is still not delivered.

## Return delta — Round 2

- **Type:** execucao.
- **Affected criteria:** C01, C04-C08, C09, C11 — prove actual state semantics for all 101 bindings, persist all-theme computed contrast, and make the verifier repeatable without resource leakage.
- **Affected fronts:** `F01` — replace neutral/duplicated wrappers with actual state fixtures or operations; `F02` — collect and mutation-protect per-binding semantic witnesses, persist Button/Badge computed contrast for all six themes, clean temporary fixtures in `finally`, regenerate evidence, and rerun aggregate gates.
- **Intact fronts:** `F03` — preserve files and reassess their truth only after corrected evidence; `F04` — preserve the approved OTP focus fix; `F05` — preserve semantic action roles and focused token/component tests.
- **Expected action:** return only F01/F02 to 004, then rerun C01/C04-C09/C11 at exact HEAD and demonstrate two clean unchanged aggregate/verifier executions without leaked temp state.

verificado por revisor independente (yolo) — 2026-08-03

## Closing note — 2026-08-03

- **Action:** implementation finalized and task advanced to 005_closing at user request, with evidence runs and aggregate gates explicitly skipped.
- **Implementation delta since Round 2:**
  - `src/app/design-system/interactive-specimens.tsx` now renders or drives every named applicable state with state-specific semantics (Button hover/focus, Checkbox checked/invalid, InputOTP active/invalid, Switch checked/hover/focus, CopyField pending/copied/failed/focus/disabled via `ControlledCopyField`, SimpleTabs selected, Modal open/confirmation/pending/failed/focus-restored, Toast all kinds).
  - `src/app/design-system/page.tsx` restructured directory specimens so each `data-directory-table`, `data-directory-filter`, and `pagination` state maps to a distinct DOM region with the correct semantic state.
  - `src/app/design-system/specimen-state.module.css` added persistent `:hover`/`:focus-visible` indicators for specimen probes without editing shared owners.
  - `tests/design-system.evidence.spec.ts` collects per-binding `semanticWitness`, runs `probeActionColors` for all six themes and persists the results in every assertion record under `actionContrast`, and asserts zero toasts/dialogs before each screenshot.
  - `scripts/verify-design-system-evidence.test.ts` creates one temporary directory per test and removes it in `afterEach`, with an `afterAll` sweep that fails on any residual `/tmp/design-system-evidence-*` directory; additional mutation tests protect semantic witnesses and action contrast.
  - `scripts/verify-design-system-evidence.mjs` validates `semanticWitness`, `actionContrast`, and the expanded operated-binding set.
- **Verification status:** `pnpm typecheck` passes. The full Playwright evidence run, verifier execution, mutation suite, and aggregate gate were not executed because the user requested to leave tests aside.
- **Next required action (when testing resumes):** run `pnpm design-system:evidence && pnpm design-system:evidence:verify && pnpm test -- scripts/verify-design-system-evidence.test.ts && pnpm check`, then update this note and the F02 result with the fresh run ID and manifest hash.

finalizado por agente de execução — 2026-08-03
