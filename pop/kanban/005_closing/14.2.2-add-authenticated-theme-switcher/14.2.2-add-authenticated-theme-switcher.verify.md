# Julgamento — [[14.2.2-add-authenticated-theme-switcher]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-07

- **Estratégia:** differential — `critical: false`, first round, single front, no prior return; the surface is the 13-path integrated diff of `af906be2`.
- **Superfície:** cookie contract module, root-layout stamping, shell picker + props/labels/CSS, both role layouts, both app-shell dictionaries, DOX and DESIGN.md. Risks covered: cookie trust, SSR/hydration agreement, public-surface leakage, menu a11y, 44px targets, bilingual parity, token purity, ownership.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

**Pedido do card atendido.** Authenticated surfaces now stamp `data-theme` from a validated cookie and the account menu carries an instant six-swatch picker; storefront/checkout `data-theme-preview` is untouched. The six `:root[data-theme=…]` blocks stopped being dead CSS for merchants and administrators.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C1 | Stamping principal-gated and validated | evidência | `src/app/layout.tsx`, `theme-preference.ts` | passou | `layout.tsx:28-30` stamps only for a resolved principal; `theme-preference.ts:18-24` returns `undefined` for anything outside the registry, and React omits the attribute, keeping `globals.css:210` `prefers-color-scheme` fallback (dark block byte-equal to `midnight-clearing`, `globals.css:694`) |
| C2 | One source for ids and cookie name | evidência | diff | passou | ids and guard come from `themes.ts` (`resolver.json`-derived); cookie name declared once at `theme-preference.ts:9`; the label map is `satisfies Record<StorefrontThemeId, string>` (`theme-preference.ts:56`), so it cannot drift from the registry |
| C3 | Picker present for both roles, once | evidência | `src/app-shell/**`, both role layouts | passou | single `AccountMenu` call site (`shell-navigation.tsx:282`) inside the only `TopBarShellControls`; both layouts build `themeOptions` from `STOREFRONT_THEME_IDS`; no drawer copy |
| C4 | Accessible radio semantics | evidência | diff | passou | `role="group"` + `aria-label` and six `role="menuitemradio"` native buttons with `aria-checked` (`shell-theme-picker.tsx:55-73`), `CheckIcon` as non-color marker, `alt=""` swatch, focus ring from the global `button:focus-visible` rule (`globals.css:1177`); `menu > group > menuitemradio` is valid ARIA |
| C5 | Instant switch, cookie persistence, no hydration workaround | evidência | handler + diff | passou | `shell-theme-picker.tsx:45-53` sets the attribute and writes `qr_theme` with `path=/`, `max-age=31536000`, `SameSite=Lax`, `Secure` on https (`theme-preference.ts:33-49`); no route, form or navigation; nothing computes `<html>` during render and no `suppressHydrationWarning` |
| C6 | Bilingual labels without duplication | evidência | `dictionaries/app-shell/{en,pt-BR}.ts` | passou | `shellThemeMenu` added to both, parity pinned by the existing test (`app-shell.test.tsx:20-24`); names reuse `storefrontTheme*` keys, no new per-theme string |
| C7 | Numeric UI laws and token purity | evidência | `app-shell.css` | passou | `min-height`/`min-inline-size: var(--control-min-height)` = 44px (`globals.css:36`); panel is `248px - 32px = 216px`, so a 3-column grid of ~61px columns fits inside 320px with the panel right-anchored; all visual values are `var(--…)`, `--primary` is theme-defined (`globals.css:103`) |
| C8 | Adjacent contracts untouched | evidência | diff + scope check | passou | `globals.css`, `design-system/tokens/**`, `themes.ts`, `admin/settings/**`, storefront/checkout sources and every test file are outside the 13 paths; the preview subtree still wins visually because `.storefront-shell`/`.checkout-shell` repaint the viewport (`globals.css:1279`, `globals.css:1253`) and `dark:` utilities stay banned by test |
| C9 | Contracts documented | evidência | `src/app-shell/AGENTS.md`, `DESIGN.md` | passou | DOX names the second client boundary, the inert theme props and the per-request revalidation; `DESIGN.md:229-235` records the picker and the cookie inside the "Authenticated application" subsection only |
| C10 | Static gates green | evidência | registered run output | passou | `tsc --noEmit` exit 0 (orchestrator); executor: eslint 0 errors (one `<img>` warning matching the repo-wide pattern — `store/[slug]/page.tsx:52` and 8 more), `check-design-tokens` exit 0 |
| C11 | Regression coverage in the phase suite | phase | — | registrado (phase) | [[14.2.5-phase-verification]] line 35 already carries "theme cookie + picker" |
| C12 | Browser behaviour and small-viewport fit | humano | — | checklist humana | goes to the verdict checklist below |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | `DESIGN.md` claims public surfaces "stay untouched", but a signed-in principal browsing `/store/<slug>` or `/pay/<id>` does receive `data-theme` on `<html>` — it is only invisible because the preview shells repaint the whole viewport | `DESIGN.md:232-235` vs `src/app/layout.tsx:28-30` | Reword to "unauthenticated public surfaces", matching the precise comment already in `layout.tsx` |
| sugestão | The active id is discovered by a mount effect, forcing a `set-state-in-effect` suppression and leaving all six options `aria-checked="false"` until hydration | `shell-theme-picker.tsx:38-43` | The role layouts already read cookies server-side; passing the resolved id as a prop would delete the effect and the suppression |
| nit | With the `themeOptions = []` default, the picker renders an empty bordered/padded `role="group"` inside the menu | `shell-navigation.tsx:244`, `app-shell.css:286-292` | Return `null` when the option list is empty |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` da frente F01 (13 paths, nenhum nos deny sets).
- [x] Dependências não foram implementadas oportunisticamente por consumidores — 14.2.1's `themes.ts` untouched, only consumed.
- [x] Critérios `verify: phase` registrados na checklist da phase (C11 em [[14.2.5-phase-verification]]).
- [x] Specs, documentação e contratos DOX afetados estão coerentes (DOX e DESIGN.md amendados; a linha de spec em `application-frontend-system` é sync do agente principal).

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** three above, none blocking; the declared deviation (optional `themeMenu`/`themeOptions` props to keep `app-shell.test.tsx` compiling without editing it) is correct — the envelope forbade test edits.
- **Checklist humana:** C12 — in both roles, open the account menu, pick each of the six themes and confirm the repaint is immediate with no reload; navigate and reload and confirm the choice survives; repeat at 320px and 375px with the menu open, confirming the swatch grid is fully visible, operable and free of overflow or clipped controls; confirm no surface is unreadable in any of the six themes.
- **Resumo:** the card asked for a validated, principal-gated `data-theme` stamp plus an instant shell picker; the diff delivers exactly that in one cohesive front, with the cookie never trusted without revalidation, no token or preview contract disturbed, and both DOX and DESIGN.md amended.

<!-- pop-verdict round=1 decision=aprovada -->
