# Julgamento — [[14.2.4-build-domain-badge-and-inventory-parity]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-07

- **Estratégia:** differential — round 1, `critical: false`; `size: L` buys deeper reading, not a second judge. Surface is the whole integrated diff (13 files, 4 commits), read once against card, plan, template sources and the touched contracts.
- **Superfície:** `status-badge.tsx` tone-map fidelity and caller compatibility · `image-uploader.tsx` (new owner) six states, staging boundary and a11y · the five F03 owners and their preserved call sites · `inventory.json`/validator/test/AGENTS/DESIGN/spec coherence · absence of `src/app/**` changes and of raw visual values.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Pedido original

Card's "O quê/Por quê" is met: the five domain families, the owned uploader, the compact `CopyField`, the 48px `Monogram`, payload-generating `QrDisplay`, the `StatCard` sparkline and the inventory/`localAdditions` record all exist, additive and with no page migrated. One accessibility gap inside the new uploader is the single defect.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | API preservada + 5 famílias sobre um mapa de tom | evidência | leitura contra `docs/template/app/src/components/ui/StatusBadge.tsx` | passou | 17 estados batem 1:1 com o template (created/pending→info, indeterminate/disabled→warning, confirmed/finalized/active→success, rejected/cancelled/expired/deleted→danger, refunded/none/inactive/archived→neutral); `StatusBadge` props intactos (`status-badge.tsx:57`); tsc 0 |
| C02 | Tinta soft + dot, `Archive` em archived, username riscado em deleted | evidência | leitura do markup | passou | `status-badge.tsx:33-46` (dot `bg-current` em `:39`, `ArchiveIcon` em `:35`), `:155-160` (username `line-through` em `:158`); rótulo textual sempre presente |
| C03 | Uploader `"use client"`, staging injetado sem rota, seis estados, teclado, ≥44px, live region | evidência | leitura + grep de rota | **falhou** | grep de rota vazio ✔, `role="status"`/`role="alert"` ✔, `min-h-11` ✔ — mas foco invisível no estado staged e drag-over só por cor (achados 1 e 2) |
| C04 | `CopyField` compacto, `Monogram` xl, `StatCard` sparkline, `MoneyText` pill | evidência | leitura + typecheck | passou | `copy-field.tsx:108-133` replica `aria-label`/`aria-describedby`/anúncio; `monogram.tsx:11,27`; `stat-card.tsx:20-35` SVG `aria-hidden`, sem lib de motion; `money-text.tsx:23`; nenhum import novo |
| C05 | `QrDisplay` gera do `payload` com o `qrcode` pinado, moldura clara, `H` com identidade | evidência | leitura + typecheck + `package.json` fora do diff | passou | `qr-display.tsx:39-84`; `errorCorrectionLevel = identity ? "H" : "M"`; `graphic` ainda vence; `role="img"`+`aria-label` preservados; SSR e cliente renderizam o mesmo placeholder (sem hydration mismatch); `dangerouslySetInnerHTML` recebe só bytes do `qrcode` — payload vira módulos, nunca markup |
| C06 | `localAdditions` com o `ImageUploader`, `owners` em 20 | evidência | leitura do JSON | passou | `inventory.json:54-56`; `owners=20` no output do validador |
| C07 | `check-shared-ui-inventory.mjs` OK e validando `localAdditions`; source-check verde | evidência | execução dos scripts baratos | passou | `SHARED_UI_INVENTORY_OK obligations=187 owners=20 exclusions=49 additions=9`; `DESIGN_SYSTEM_COVERAGE_OK entries=20 …`; `check-design-tokens.mjs` exit 0 |
| C08 | Asserção de conjunto de arquivos contempla o owner local | phase | registrado para 14.2.5 | registrado (phase) | `primitive-inventory.test.tsx:70` soma `localAdditionNames` |
| C09 | AGENTS/DESIGN/spec descrevem F01–F03 | evidência | leitura dos três | passou | `src/components/ui/AGENTS.md:19-38`, `DESIGN.md:295-360`, spec `application-frontend-system.md:72` troca "No upload component is admitted" pela regra de adição local, sem tocar a exclusão nem os números de 12.2.3 |
| C10 | Nenhuma página migrada, nenhum valor cru, typecheck/lint limpos | evidência + phase | `git diff --name-only` + greps | passou (agente) / registrado (phase) | zero arquivos sob `src/app/**`; grep de hex/`style=`/`dark:`/px/rem vazio nos sete owners; tsc 0, eslint 0 |
| C11 | Evidência de `/design-system` regenerada uma vez na phase | phase | registrado para 14.2.5 | registrado (phase) | hashes de fonte obsoletos pela edição dos owners |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | No estado **staged** o alvo "replace" não tem foco visível: o `<label>` estilizado por `buttonVariants` (que carrega o `focus-visible:` ring) nunca recebe foco, e o `<input>` real é **irmão** dele, `sr-only` com `clip-path: inset(50%)` (`globals.css:1301`) — o outline global de `input:focus-visible` fica recortado. No tile idle/failed o mesmo input está *dentro* do label e o `focus-within:ring-2` cobre o caso; no staged não há `focus-within` em nenhum ancestral. Viola "foco visível" do contrato F02 e WCAG 2.4.7 AA | `src/components/ui/image-uploader.tsx:204-208` (label de replace) e `:181-192` (`picker`, irmão do label); tile com `focus-within` em `:83` | Mover `{picker}` para dentro do `<label>` de replace (como no tile) ou dar ao label um anel `focus-within:`/`has-[:focus-visible]:` equivalente |
| bloqueante | **drag-over** difere de **idle** apenas por cor (`border-accent bg-accent-soft text-accent`): mesmo ícone `ImagePlus`, mesmo texto, mesma borda tracejada. C03 exige "seis estados distinguíveis sem cor", e o `DESIGN.md` que F04 acabou de escrever já afirma essa propriedade | `src/components/ui/image-uploader.tsx:222`; `DESIGN.md:352` | Acrescentar um sinal não-cromático ao drag-over (borda sólida/mais grossa, troca de ícone ou de rótulo) |
| nit | `StatusPill` declara `strikethrough`, mas nenhum chamador o define — `archived \|\| strikethrough` é sempre `archived` | `src/components/ui/status-badge.tsx:22,33,44` | Remover o prop morto ou usá-lo em `AccountStateBadge` deleted |
| nit | `DESIGN.md` diz "struck-through label when archived or deleted"; em deleted quem fica riscado é o **username**, não o rótulo do badge | `DESIGN.md:297-299` | Ajustar a frase junto do reparo |
| nit | Validador exige `notApplicable` só por truthiness em `localAdditions` (`[]` passa) enquanto em `owners` exige `.length > 0` | `scripts/check-shared-ui-inventory.mjs:70` | Igualar a asserção |
| sugestão | Se `QRCode.toString` rejeitar (payload acima da capacidade em `H`), `isGenerating` fica `true` para sempre: skeleton perpétuo com `aria-busy`, sem estado de erro. Espelha o precedente de `totp-qr-code.tsx`, mas aqui a decisão é do owner compartilhado que 14.4–14.6 vão adotar | `src/components/ui/qr-display.tsx:57-62,69-70` | Follow-up: estado de falha (ou cair para o bloco `alternativeValue`) quando a geração rejeita |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit`: os 13 arquivos do diff mapeiam exatamente F01–F04; nenhum arquivo fora, nenhum `.test.tsx` além da exceção declarada.
- [x] Dependências não implementadas oportunisticamente: `package.json` intacto, `qrcode@^1.5.4` já pinado, nenhuma página migrada.
- [x] Critérios `verify: phase` registrados: C08, C11 e a parte `test`/`build` de C10 vão para 14.2.5.
- [x] Specs, DOX e documentação coerentes — ressalvado o `DESIGN.md:352`, que ficará verdadeiro quando o achado 2 for reparado.

## Veredito

- **Decisão:** **reparo dirigido** — dois defeitos pontuais, mesmo arquivo, mesma frente, remédio objetivo e sem mudança de estratégia. Não consome contador de devolução.
- **Bloqueantes:** foco invisível no estado staged do `ImageUploader`; drag-over distinguível só por cor.
- **Defeito de plano:** nenhum — o plano cobria o pedido e os critérios; o desvio é de execução dentro de F02.
- **Sugestões/nits:** prop `strikethrough` morto; frase do `DESIGN.md` sobre deleted; assimetria do validador; falha silenciosa de geração do QR (follow-up).
- **Checklist humana:** nenhum critério `verify: user` e nenhum qualified pass de ambiente. Para a phase (14.2.5): `status-badge.test.tsx` (espera `<svg` por tom, invalidado pelo dot) e `store/[slug]/pay/standalone-payment-experience.test.tsx:152` (espera `bg-destructive` nos terminais) precisam ser reescritos, e `design-system:evidence` regenerado.
- **Resumo:** paridade de badges, uploader, identidade/numéricos e inventário entregues como pedidos, aditivos e sem tocar em página alguma; falta apenas fechar duas lacunas de acessibilidade no uploader novo.

## Delta da devolução

- **Tipo:** execucao (reparo dirigido, pontual).
- **Critérios afetados:** C03 — foco visível no estado staged e distinção não-cromática do drag-over.
- **Frentes afetadas:** `F02` — reentra só para o patch nos dois pontos citados.
- **Frentes intactas:** `F01`, `F03`, `F04` — aprovadas e integradas; **não** reexecutar (o ajuste de uma frase do `DESIGN.md` é nit, opcional, e não reabre F04).
- **Ação esperada:** no `image-uploader.tsx`, mover o `picker` para dentro do label de replace (ou dar-lhe anel de foco equivalente) e acrescentar um sinal não-cromático ao drag-over.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/components/ui/image-uploader.tsx frentes=F02 intactas=F01,F03,F04 -->
