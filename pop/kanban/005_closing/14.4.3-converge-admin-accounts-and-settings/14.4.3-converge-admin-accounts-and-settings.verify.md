# Julgamento — [[14.4.3-converge-admin-accounts-and-settings]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — rodada 1 de task `size: L`, não crítica: a superfície é o diff integrado inteiro (31 paths, e2da92b0..c8e07107), lido por frente, sem re-run de suíte.
- **Superfície:** F01 diretório/modal, F02 editor em abas, F03 hub de settings, F04 spec/`DESIGN.md`; rotas congeladas e `src/{auth,components/ui,data-directory}` conferidos como diff vazio.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

O pedido do card foi atendido no essencial: `/admin/accounts` cria por `Modal`, `/admin/accounts/[id]` é editor em cinco abas e `/admin/settings` tem scroll-spy com §4 operável. Falta uma peça pedida pelo plano (C07).

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Modal de criação no header, sem card inline | evidência | `create-account-modal.tsx`, `admin-surface.tsx` | passou | `admin-surface.tsx:24-33` troca o `Card` pelo trigger; `create-account-modal.tsx:107-206` compõe gerador/medidor/`CopyField` |
| C02 | POST inalterado, pouso em `/admin/accounts`, toast + `<noscript>`, sem status inicial | evidência | `users/route.ts`, `admin-surface.tsx` | passou | `users/route.ts:8` só troca o alvo; campos `username/email/password/role` idênticos; `admin-surface.tsx:43-52` toast + `<noscript>`; nenhum campo `status` |
| C03 | Última atividade relativa nos dois locales, props do diretório intactas | evidência | `accounts/page.tsx`, `instant.ts` | passou | `instant.ts:17-22` usa `Intl.RelativeTimeFormat(locale)`; `page.tsx:165-178` mantém href/ações/filtros |
| C04 | Cinco abas com aba ativa restaurada após reload | evidência | `account-editor-tabs.tsx`, `[id]/page.tsx` | passou | `account-editor-tabs.tsx:15-32` restaura do hash e usa `replaceState`; `page.tsx:230-238` lista as cinco |
| C05 | Role/status segmentados com `ConfirmDialog` antes da rota congelada | evidência | `access-section.tsx`, diff de `users/[id]/**` | passou | `access-section.tsx:56-87,172-207`; `git diff` vazio em `src/app/admin/users/[id]/` |
| C06 | Grid de política, swatches de tema, layout segmentado, nomes POST idênticos | evidência | `storefront-section.tsx`, `preferences-section.tsx` | passou | nove campos conferidos contra `users/[id]/storefront/route.ts:20-28`; `policy` contra `checkout-policy/route.ts:15` |
| C07 | Deletado só banner+fatos; `?editor=`/`?reset=` em toast com `<noscript>`; arquivos retirados | evidência | `[id]/page.tsx`, `globals.css`, árvore | **falhou (parcial)** | banner/fatos, remoção de `account-mutation-form*` e de `.admin-confirm-actions` OK; a ponte de notice **não** foi entregue (achado 1) |
| C08 | Scroll-spy com `aria-current`, ids de seção inalterados | evidência | `settings-nav.tsx` | passou | `settings-nav.tsx:17-45`; ids `sec-*` idênticos ao base |
| C09 | §4 operável, confirm ao desligar, pill em §1 | evidência | `payment-settings-section.tsx`, `exchange-currencies-section.tsx` | passou | `disabled` removido; `payment-settings-section.tsx:78-96` confirma só o desligamento e a rota substitui a lista inteira (`payment-settings/route.ts:20-23`) |
| C10 | Feedback por seção, idioma aplica na seleção com fallback sem JS | evidência | `settings-surface.tsx`, `language-section.tsx` | passou com defeito | submit nativo por botão (`language-section.tsx:26-44`); notice duplicado no idioma (achado 2) |
| C11 | Spec e `DESIGN.md` registram composição e as três lacunas | evidência | `administrative-foundation.md`, `DESIGN.md` | passou | lacunas de status inicial, §1 UUID/inativos e slug de loja nomeadas; `DESIGN.md:363-375` registra o controle admin-local e seus estados |
| C12 | Paridade `en`/`pt-BR` e domínios disjuntos | evidência | dicionários tocados | passou | 13 chaves novas em ambos os arquivos de `admin-users-directory`; F02 só em `admin-user-profile`; F03 não criou chave |
| C13 | `typecheck` e `lint` sem erros | evidência | evidência de execução + `check-design-tokens` | passou | `tsc` 0 erros e eslint 0 erros registrados na integração; `node scripts/check-design-tokens.mjs` rodado aqui, exit 0 |
| C14 | Realinhamento da suíte | phase | registro na checklist da phase | registrado (phase) | `accounts/page.test.tsx`, `accounts/[id]/page.test.tsx`, `admin/users/routes.test.ts`, `admin/settings/page.test.tsx` e a remoção de `account-mutation-form.test.ts` vão para [[14.4.4-phase-verification]] |
| C15 | Passe humano de navegador/WCAG | humano | checklist humana | humano | ver veredito |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | Editor mantém o `Alert` sempre visível do base em vez da ponte `NoticeToast` + `<noscript>` exigida por C07 e pelo contrato de F02; o param fica na URL e o banner reaparece a cada reload, única superfície convergida sem a ponte | `src/app/admin/accounts/[id]/page.tsx:55-65,243`; comparar `admin-surface.tsx:43-52` e `settings-section-notice.tsx:21-35` | montar `NoticeToast` com os pares `editor=changed\|conflict\|failed\|totp-disabled` e `reset=requested\|failed`, mantendo o `Alert` dentro de `<noscript>` |
| bloqueante | Toast de idioma duplicado: o layout raiz já registra `language=saved\|error` globalmente e a seção registra os mesmos pares; ambos montam na mesma página e `showToast` não deduplica (sonner sem `id`), então cada troca de idioma em `/admin/settings` empilha dois toasts idênticos | `src/app/admin/settings/language-section.tsx:16-21` versus `src/app/layout.tsx:39-48`; `src/components/ui/toast.tsx:24-38` | passar `toastEntries={[]}` na seção de idioma (o registro global já cobre o toast) e preservar o `<noscript>` |
| sugestão | `aria-describedby` pendurado: o texto de ajuda da senha não tem `id` (defeito herdado do base, reescrito nesta task) | `src/app/admin/accounts/[id]/access-section.tsx:211,218` | dar `id={`password-help-${detail.id}`}` ao parágrafo |
| sugestão | `htmlFor` pendurado: o `SegmentedControl` de layout não renderiza elemento com esse id, o rótulo não ativa nada | `src/app/admin/accounts/[id]/storefront-section.tsx:126-136` | remover o `htmlFor` (o grupo já tem `ariaLabel`) ou expor um id no controle |
| sugestão | Fallback morto no gerador: `randomValues[index] ?? fallback` nunca cai no fallback (0 não é nulo), então sem `crypto.getRandomValues` a senha sairia de caractere único | `src/app/admin/accounts/create-account-modal.tsx:23-33` | testar a disponibilidade do `crypto` uma vez e ramificar, ou assumir o `crypto` e remover o fallback |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — 31 paths, `users/[id]/**`, `src/auth/**`, `src/components/ui/**` e `src/data-directory/**` com diff vazio.
- [x] Dependências não foram implementadas oportunisticamente por consumidores — F02/F03 consomem `admin-controls.tsx` publicado por F01 sem editá-lo.
- [x] Critérios `verify: phase` registrados na checklist da phase (C14).
- [x] Specs, documentação e contratos DOX afetados estão coerentes — a spec não afirma ponte de notice no editor, então nada nela vira falso com o reparo.

## Veredito

- **Decisão:** **reparo dirigido** — dois bloqueantes pontuais, cada um em um arquivo, com remédio objetivo e sem mudança de estratégia.
- **Bloqueantes:** ponte de notice ausente no editor (C07); toast de idioma duplicado (C10).
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** três sugestões acima, não bloqueantes; podem ser absorvidas no mesmo patch se o executor já estiver nesses arquivos. Follow-up rastreável: consolidar o `SegmentedControl` admin-local com o gêmeo mercante (já nomeado em `DESIGN.md`).
- **Checklist humana:** C15 — passe de navegador: foco preso e devolvido no modal de criação; aba ativa sobrevive ao reload e não vaza entre contas; scroll-spy marcando a seção certa; toasts (após o reparo, um por ação); 320 px; dois temas e dois locales; WCAG 2.2 AA. Verificar em especial que o `SegmentedControl` (role/status/locale/layout) é operável só por Tab, sem setas nem roving tabindex — mesmo padrão já aceito no gêmeo mercante — e que o botão do idioma ativo, por ficar `disabled`, sai da ordem de foco.
- **Resumo:** o pedido do card foi entregue — modal com gerador/medidor/cópia, editor em cinco abas com controles segmentados, grids e visão de deletado, hub com scroll-spy e §4 operável, três lacunas registradas na spec — e o que falta são dois pontos de feedback: a ponte de notice que o editor não recebeu e um toast de idioma que dispara duas vezes.

## Delta da devolução

- **Tipo:** execucao (pontual).
- **Critérios afetados:** C07 — a ponte `NoticeToast` + `<noscript>` do editor não foi entregue; C10 — o feedback de idioma duplica com o registro global do layout.
- **Frentes afetadas:** `F02` (editor) e `F03` (hub) — reentram só nesses dois arquivos.
- **Frentes intactas:** `F01`, `F04` — aprovadas, permanecem integradas; **não** reexecutar.
- **Ação esperada:** montar a ponte de notice em `[id]/page.tsx` espelhando `admin-surface.tsx` e zerar as `toastEntries` de idioma em `language-section.tsx`.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/admin/accounts/[id]/page.tsx,src/app/admin/settings/language-section.tsx frentes=F02,F03 intactas=F01,F04 -->
