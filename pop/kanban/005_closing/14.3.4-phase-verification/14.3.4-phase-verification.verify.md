# Julgamento — [[14.3.4-phase-verification]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08 *(prosa compactada na rodada 2; resultados e marcadores preservados na íntegra)*

- **Estratégia:** full — task de verificação de phase em rodada 1: a superfície é o checklist inteiro da phase, não um delta.
- **Superfície:** diff integrado `01b8e1ac..6dcad432` (26 arquivos de código + `artifacts/design-system/**`), os três checklists em `pop/memory/2026-09-08/14.3.{1,2,3}-*phase-checklist`, `src/data-directory/AGENTS.md`, a evidência do gate registrada no card e uma run dirigida dos dois arquivos de token em disputa.
- **Pedido original:** atendido em dois terços — gate verde, asserções de diretório/mutação realinhadas sem apagar teste, reparos baratos aplicados, bug real corrigido (cliques de linha engolidos) e evidência atual; faltaram os pins on-soft, a contagem de 27 utilities e a chave reservada, que só rodam neste estágio.
- **Critérios:** C01 passou (tsc 0 · lint 0 erros/66 warnings pré-existentes · vitest 2116/28 · build 0) · C02 passou (nenhum teste apagado, nenhum skip novo) · C03 falhou parcial (mocks e redirects `?filters=ignored` corretos; os três `directory-query.test.ts` admin intocados e chave reservada sem asserção) · C04 passou (destinos on-page, rotação de sessão, âncora `#settings-connection`) · C05 falhou (nenhum dos três arquivos de token tocado) · C06 falhou parcial (debounce, commit imediato, drop de `cursor`, chips e row href cobertos; strip do par reservado e skeleton não) · C07 passou · C08 falhou parcial (`accent` descoberto, 30 primitivas sem pin) · C09 passou parcial (encode, contraste do uploader e footer aplicados; debounce e `pageSize` default viraram follow-up pela cláusula do próprio critério; `tokens:check` verde) · C10 passou (48 capturas, `gitHead` ancestral, 68 hashes, 0 divergência) · C11–C15 → checklist humana.
- **Achados:** **bloqueante B1** — os pins de token codificavam o contrato pré-14.3.3 e passavam por acidente: `status-badge.test.tsx:11-25` espelhava `text-danger` contra `text-danger-on-soft` renderizado e `toContain` casava por substring; `globals-theme-tokens.test.ts:26-50` listava 22 nomes chamando-se "exatamente 22"; `design-tokens.test.ts:161-177` não fixava hex nem `interpolationStep` das 30 primitivas. **bloqueante B2** — a invariante mais frágil da phase (todo resolver removendo `filters=ignored` **antes** de canonicalizar, para o redirect de reset nunca entrar em laço) não tinha teste algum: `notice.ts:12-15` sem arquivo de teste e oito sites de strip sem caso. **sugestão B3** — o caso do footer não fixa o reparo (`data-directory-client.test.tsx:159-176`: `router.replace` é mock síncrono, `isPending` já é `false`). **sugestão S1** — o manifesto de evidência vincula 68 fontes mas não `data-directory-client.tsx`. **nit N1** — `FormDraftGuard` exclui password/file por construção.
- **Escopo:** `Owns`/`Must not edit` respeitados (specs, prisma, docs e i18n intocados; `artifacts/` só regenerado pelo script); dependências não implementadas oportunisticamente; texto de contrato não tocado, como o plano exigia.
- **Veredito:** bloqueante de execução → 004_processing. Bloqueantes B1 (C05/C08) e B2 (C03/C06); defeito de plano: nenhum — os critérios cobriam o pedido, a entrega é que não os cumpriu.
- **Delta:** tipo `execucao`; frente F01 reentra só com esses itens. **Aprovado e a não refazer:** os nove `page.test.tsx`, `specimen-mounted`, as suítes de mutação, `data-directory-client.test.tsx`, os três reparos de código e a evidência regenerada.

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=scripts/design-tokens.test.ts,src/app/globals-theme-tokens.test.ts,src/components/ui/status-badge.test.tsx,src/data-directory/server/notice.ts,src/app/admin/accounts/directory-query.test.ts,src/app/admin/orders/directory-query.test.ts,src/app/admin/payment-links/directory-query.test.ts frentes=F01 intactas=nenhuma -->

## Rodada 2 — 2026-09-08

- **Estratégia:** `differential` — retorno por `execucao`; a superfície congela no delta e tudo aprovado na rodada 1 permanece aprovado.
- **Superfície:** commit de reentrada `318b8ecc` (`git diff 6dcad432..318b8ecc`), 6 arquivos, todos de teste. Nota: o `return_base` `e1aa7dd0` do card é o commit de kanban da devolução em `develop`, fora da linha da branch da task — `e1aa7dd0..HEAD` reabriria a rodada 1 inteira e não é a superfície desta rodada. Nenhum arquivo de produção tocado; `notice.ts` intocado (nada a mudar nele).
- **Evidência própria** (única task da phase em que teste é `verify: agent`, por [[WORKFLOW|WORKFLOW]] §"Verificação de phase"): `./node_modules/.bin/vitest run` → **242 arquivos, 2131 passed / 28 skipped** (baseline 2116/28: +15, nenhum apagado, nenhum skip novo); os 6 arquivos do delta isolados → 102 passed; `tsc --noEmit` exit 0; `eslint` nos 6 arquivos → 0 erro, 0 warning.
- **Devoluções:** execução 1 de 2 · plano 0 de 2 — 3ª da mesma rota ativa circuit breaker.

| # | Critério (delta) | Modo | Resultado | Evidência |
|---|------------------|------|-----------|-----------|
| C03 | Suítes de diretório realinhadas | evidência | **passou** | os três `directory-query.test.ts` admin agora afirmam a chave reservada removida; a lista de arquivos do próprio C03 está integralmente descarregada |
| C05 | Pins movidos para o contrato on-soft | evidência | **passou** | `status-badge.test.tsx:26-52` compara **tokens** de classe (não substring), exige `text-<tone>-on-soft` e **proíbe** `text-<tone>` em todas as famílias/tonalidades; `globals-theme-tokens.test.ts:26-70` isola o sub-bloco do vocabulário e fixa `toHaveLength(27)` + igualdade de conjunto com as cinco `-on-soft`; `design-tokens.test.ts:180-193` fixa hex + `interpolationStep` das 30 primitivas. Todos falhariam contra o código pré-14.3.3 — B1 sanado |
| C08 | Regressão 14.3.3 coberta | evidência | **passou** | mesmo bloco, 5 papéis × 6 temas incl. `accent`, com `contrastRatio(primitiva, softSurface) ≥ 4.5` medido pela fórmula WCAG da spec — asserção independente da derivação, não tautológica |
| C06 | Regressão 14.3.1 coberta | evidência | **passou parcial** | par reservado fixado nos 3 resolvers admin (equivalência strip-antes-de-canonicalizar, `location` nunca carrega o par, o local de reset resolve `ready` sem novo redirect); os 3 resolvers merchant seguem sem caso — ver R1 |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante (R1) | O strip do par reservado é **duplicado em cada resolver** (6 cópias da mesma linha `if (key === DIRECTORY_INVALID_FILTERS_PARAM) continue;`), e só 3 ficaram fixadas. Nos resolvers merchant a invariante de não-laço continua descoberta: as suítes de página deles afirmam apenas o *destino* do redirect, nunca que resolver `?filters=ignored` devolve `ready`. C06 pede "all eight resolvers" e o card pede "in every resolver" | `src/app/(merchant)/orders/directory-query.ts:58`, `(merchant)/links/directory-query.ts`, `(merchant)/catalog/directory-query.ts` vs. `grep -rl DIRECTORY_INVALID_FILTERS_PARAM src/**/directory-query.test.ts` → só os três admin; `(merchant)/{orders,links,catalog}/page.test.tsx` só afirmam `rejects.toThrow("redirect:/…?filters=ignored")` | **Reparo dirigido (pontual):** espelhar o bloco admin em `src/app/(merchant)/{catalog,links,orders}/directory-query.test.ts` — mesma estratégia, mesmos três casos, só trocando filtro e path canônico |
| sugestão (S2) | `expect(vocabularyStart, …)` roda no corpo do `describe`, em tempo de coleta: uma quebra do âncora `--color-bg:` vira erro de coleta do arquivo, não falha de teste nomeada | `src/app/globals-theme-tokens.test.ts:31` | Follow-up: mover a âncora para dentro de um `it` |
| nit (N2) | `expect(buildLocation).toHaveBeenCalledTimes(1)` sobre um `vi.fn(directoryInvalidFiltersLocation)` chamado pelo próprio teste é tautológico — o que sustenta o caso é a metade seguinte (o local resolve `ready` sem novo redirect) | `src/app/admin/orders/directory-query.test.ts:73-76` (idem accounts/payment-links) | Nenhuma ação: o caso permanece material sem essa linha |

**Rodada 1 congelada:** B3 (pin do footer inerte), S1 (manifesto sem `data-directory-client.tsx`) e o `pageSize` default seguem como follow-ups declarados — eram sugestões, não voltam como bloqueantes. Nenhum arquivo aprovado na rodada 1 foi tocado por `318b8ecc`.

### Escopo e integração

- [x] Delta dentro dos `paths` autorizados na rodada 1; nenhum arquivo de produção, spec, prisma, docs ou i18n alterado.
- [x] Suíte não enfraquecida: +15 testes, 0 apagados, 0 skips novos, gate agregado verde por run próprio.
- [ ] C06 registrado integralmente para a checklist da phase — falta 1 item, endereçado por reparo dirigido nesta mesma rodada.

## Veredito

- **Decisão:** reparo dirigido (`pontual=true`) — B1/B2 sanados e C03/C05/C08 aprovados; resta um único item mecânico de C06.
- **Bloqueantes:** R1 (par reservado sem caso nos 3 resolvers merchant).
- **Defeito de plano:** nenhum — o critério cobria o pedido; a rodada 1 é que enumerou em `paths` menos sites do que a própria prosa exigia.
- **Sugestões/nits:** S2 (assert em tempo de coleta) · N2 (`toHaveBeenCalledTimes` tautológico) · B3/S1 mantidos como follow-up.
- **Checklist humana (carregada da rodada 1):** C11 filtragem ao vivo sem Apply, chips localizados, linha abre o detalhe · C12 query malformada reseta com um toast informativo e sem laço, e sem JS Apply/Reset funcionam sem alerta "filters ignored" · C13 texto digitado sobrevive a falha em comentário, outcome, identidade e formulário de link · C14 troca de senha mantém a sessão em `/profile` · C15 pílulas on-soft legíveis a 12px nos seis temas.
- **Resumo:** os pins de token deixaram de ser verdes e vazios — agora falham contra o contrato antigo — e a invariante do par reservado ganhou casos reais nos resolvers admin, com a suíte inteira verde por verificação própria. Falta apenas replicar o mesmo bloco em três resolvers merchant, o que é reparo dirigido, não replanejamento.

## Delta da devolução

- **Tipo:** `execucao`, **pontual=true** (reparo dirigido, sem mudança de estratégia).
- **Critérios afetados:** C06 apenas.
- **Frentes afetadas:** `F01`, restrita a `src/app/(merchant)/{catalog,links,orders}/directory-query.test.ts`.
- **Frentes intactas:** tudo o mais — as suítes de página e mutação, `data-directory-client.test.tsx`, os arquivos de token da rodada 2, os três `directory-query.test.ts` admin, os reparos de código e a evidência regenerada.
- **Ação esperada:** um bloco `describe` por arquivo, espelhando o admin: (a) par reservado + filtro real ≡ filtro real sozinho, `status: "ready"`, alvo canônico sem o par; (b) requisição com o par que redirecione nunca devolve `location` contendo `filters`; (c) resolver `?filters=ignored` puro devolve `ready` e alvo canônico limpo. Fora do reparo: os sites de página (`links/v2/[id]/orders/page.tsx`, `directory-support.tsx`) ficam como follow-up rastreável.

<!-- pop-delta round=2 kind=execucao pontual=true paths=src/app/(merchant)/catalog/directory-query.test.ts,src/app/(merchant)/links/directory-query.test.ts,src/app/(merchant)/orders/directory-query.test.ts frentes=F01 intactas=F01-restante -->

### Adendo do reparo dirigido — 2026-09-08 *(re-checagem restrita a R1)*

- **Patch:** `dc5f88bd`, exatamente os três `src/app/(merchant)/{catalog,links,orders}/directory-query.test.ts` (+94 linhas, nenhum outro arquivo). A rodada 2 teve **um** reparo dirigido; o veredito interino `reparo-dirigido` fica encerrado por este adendo.
- **R1 sanado:** cada arquivo ganhou o trio contra `/catalog`, `/links` e `/orders` — equivalência strip-antes-de-canonicalizar (`withPair` ≡ `withoutPair`, `status: "ready"`), `location` de redirect que nunca contém `filters`, e replay do local de reset resolvendo `ready` com alvo canônico limpo. Sem a tautologia do `toHaveBeenCalledTimes`; os três quebram se a linha do strip sair do resolver. **C06 → passou.**
- **Evidência própria:** `./node_modules/.bin/vitest run` → 242 arquivos, **2140 passed / 28 skipped** (+9 sobre 2131, nenhum apagado, nenhum skip novo); `tsc --noEmit` exit 0; `eslint` nos três arquivos → 0/0. Nenhuma frente aprovada foi revisitada.
- **Follow-up (não bloqueante):** os dois sites de nível de página (`links/v2/[id]/orders/page.tsx`, `directory-support.tsx`) seguem sem caso de não-laço, junto de B3, S1, S2 e do `pageSize` default.
- **Decisão da rodada 2: aprovada** — C01–C10 descarregados, C11–C15 na checklist humana acima.

<!-- pop-verdict round=2 decision=aprovada -->
