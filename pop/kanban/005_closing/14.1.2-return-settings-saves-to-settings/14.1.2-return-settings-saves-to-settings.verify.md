# Julgamento — [[14.1.2-return-settings-saves-to-settings]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-07

- **Estratégia:** differential — non-critical task, first round, no prior return; surface = the whole integrated diff `7751cba3..HEAD` over `src/` and `scripts/` (13 paths).
- **Superfície:** the four redirect targets and the new return-path module (F01); the settings surface layout/CSS/notices/copy (F02); the design-token allowlist line; open-redirect and host-validation risk; anchor↔section id parity; SSR safety of the scroll-spy; dictionary key parity.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

**Pedido do card atendido.** All four save paths now land on `/settings` at their own section with the outcome notice rendered inside it, `settings-surface__*` finally has real CSS (tokens only), every section shows its title/description once, and the page description is settings-owned copy in both locales. No guard, form action, field name or notice code changed.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C1 | `/storefront` → identity anchor | evidência | `storefront/route.ts` | passou | `changed`/`conflict`/`failed` → `/settings?storefront=<code>#settings-identity`; no `/` target remains |
| C2 | `/checkout-policy` → policy anchor | evidência | `checkout-policy/route.ts:15,17` | passou | `changed`/`failed` → `/settings?checkout-policy=<code>#settings-policy` |
| C3 | `/storefront/logo` → store anchor | evidência | `storefront/logo/route.ts:21,29,33` | passou | `staged&logo=${staged.identifier}` and both `failed` → `…#settings-store`; identifier still the opaque media handle |
| C4 | `/language-preference` → allowlisted page | evidência | route + `settings-return-target.ts` | passou | referer `/settings` → `/settings?language=<code>#settings-language`; `/` → `/?language=<code>`; absent/invalid → `/?language=<code>`, no fragment |
| C5 | No redirect surface opened | evidência | `settings-return-target.ts:10-26` | passou | output is always an allowlist literal; `//host` and relative referers throw in `new URL` → `/`; non-http(s) scheme, foreign host and unlisted path all fall back; host check byte-mirrors `origin-guard.ts:20-23`; query/fragment discarded |
| C6 | Two-column sticky nav | evidência | `globals.css:1424-1442` | passou | one column by default; from `64rem` `grid-template-columns: calc(var(--space-12)*4) 1fr` (192px) with `nav-inner` sticky at `calc(var(--top-bar-height) + var(--space-6))`; nav hidden below |
| C7 | Target size, focus, non-color active | evidência | `globals.css:1430-1433`, `settings-surface.tsx:93` | passou | `min-height: var(--control-min-height)` (44px), `:focus-visible` outline `--focus-width`/`--focus-color`/`--focus-offset`, active = background + inline-start border + `--font-weight-strong` + `aria-current` |
| C8 | Anchor clears the top bar | evidência | `globals.css:1435` | passou | `scroll-margin-block-start: calc(var(--top-bar-height) + var(--space-6))` equals the sticky offset |
| C9 | One title/description per section, notice inside | evidência | the four surface files | passou | every `CardHeader` duplicating its section `h2`/`p` removed (settings-surface, storefront-settings-management ×4, checkout-policy-management); storefront notice in `#settings-identity`, policy in `#settings-policy`, language in `#settings-language`. Nautt duplication is plan-excluded and outside F02's `owns` |
| C10 | Page description stops reusing admin copy | evidência | `page.tsx:39` + dictionaries | passou | `dictionary.settingsPageDescription`; `adminLanguageDescription` survives only in `src/app/admin/settings/settings-surface.tsx:124` |
| C11 | Both locales, identical key sets | evidência | `settings/{en,pt-BR}.ts` | passou | same two keys added in both; pt-BR fully translated, no English leak |
| C12 | Types and lint clean | evidência | orchestrator run | passou | `tsc --noEmit` exit 0; `node scripts/check-design-tokens.mjs` passes; by reading, no unused import survives the `CardHeader` removals and prop/import ordering is unchanged. `eslint .` is not separately recorded — it re-runs inside the `pnpm check` gate of C13 |
| C13 | Suite green with realigned assertions | phase | checklist de 14.1.4 | registrado (phase) | invalidated by this diff: `storefront/route.test.ts:61,69,72,100,111`, `checkout-policy/route.test.ts:19`, `storefront/logo/route.test.ts:80,89,100,103,106,111,120`. `language-preference/route.test.ts:24,31` stay green (no `referer` → `/` fallback); new coverage owed for the allowlist, the fallback and an external-target refusal |
| C14 | No write outside `owns` | evidência | `pop_check_scope.py` | passou | ownership valid over 13 paths; `src/app-shell/**`, `src/app/admin/**`, `src/app/(merchant)/catalog/**`, `src/components/ui/**` and every test file untouched; the single `scripts/` line is the orchestrator-authorized allowlist extension |
| C15 | Visual check of the settings page | humano | checklist | humano | see the human checklist below |

**Design-token allowlist (`scripts/check-design-tokens.mjs:32`):** judged and accepted. `64rem` is exactly `--breakpoint-lg: 1024px` at the default root size, and mapping a media-query literal to its named token is the script's established idiom (`900px`→`--breakpoint-auth`, `48rem`/`23.4375rem`→ the shell breakpoints). Writing the query as `1024px` would have required an allowlist line just the same, so the extension is unavoidable, minimal and faithful to the script's intent.

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | The merchant dashboard still branches on `storefront` and `checkout-policy` notice codes that no route can emit any more — inert dead branches (`payment-links` and `language` remain live) | `src/app/(merchant)/page.tsx:44` | Follow-up: the file is outside both fronts' `owns`, so the executor could not have touched it; retire the two dead branches in a later Phase 14.1 task |
| sugestão | The proxy-host derivation (`x-forwarded-host` first value, else `host`) is now duplicated verbatim in two modules; a change to the proxy convention must land in both or one silently drifts | `src/app/settings-return-target.ts:21-23` vs `src/app/origin-guard.ts:20-22` | Follow-up: extracting a shared `expectedHost()` requires editing `origin-guard.ts`, outside F01's `owns` — the duplication is plan-imposed, not an executor choice |
| nit | The list of test assertions this change invalidates exists only in the executor's return message; 14.1.4 would otherwise rediscover it | card Log, 004→005 line | Carry the C13 row of this file into the Phase 14.1 verification checklist |
| nit | The out-of-`owns` edit to `scripts/check-design-tokens.mjs` is not named in the card Log; only "design-token check ok" appears | card Log, 004→005 line | Record the authorization (commit `f856177b`) explicitly at close-out so the ownership exception stays auditable |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — the only out-of-`owns` path is the orchestrator-authorized `scripts/` line.
- [x] Dependências não foram implementadas oportunisticamente por consumidores — `depends_on: []`; F01 and F02 met at the frozen anchor/notice contract without touching each other.
- [x] Critérios `verify: phase` registrados na checklist da phase — C13, with the invalidated assertions enumerated above.
- [x] Specs, documentação e contratos DOX afetados estão coerentes — no contract contradicted; the two spec lines the plan owes are listed as close-out follow-ups.

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** the four rows above, none blocking. Benign deviation worth knowing: the section stack uses `--space-8` where the slice wrote `--space-6` (template uses `space-y-5`); still token-only and defensible now that each section carries its own `h2`/`p`.
- **Checklist humana:** C15 — open `/settings` at `320`, `768` and `1440`, in `pt-BR` and `en`, and save each section. Expected pass: (a) each save returns to `/settings` scrolled to its own section, clear of the fixed top bar, with the outcome notice inside that section; (b) the language switch from `/` still returns to `/`; (c) from `1024px` the 192px sticky nav tracks the scrolled section and shows no horizontal overflow, below it the nav is absent and the anchors still work; (d) no section shows its title twice — the Nautt connection section is the known plan-excluded exception.
- **Resumo:** the card asked for four saves that come home to their settings section and for a settings page that is actually styled; both arrived, with the security-sensitive half (the `Referer`-derived language return) built as a closed allowlist that can only ever emit `/settings` or `/`, host-checked exactly as the existing CSRF guard checks Origin.

<!-- pop-verdict round=1 decision=aprovada -->
