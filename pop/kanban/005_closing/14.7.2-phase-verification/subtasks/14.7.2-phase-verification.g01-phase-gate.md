# Frente F01 — Epoch 14 gate — [[14.7.2-phase-verification]]

- **Entrega:** the Epoch 14 verification pass — suite and evidence-spec realignment, the single-system regression, in-reach repairs, the two local evidence regenerations, the aggregated gate result, the consolidated human checklist file and the `develop` → `main` PR body draft.
- **Escopo:** verification and handover. Non-test source changes only where the suite proves a defect caused by 14.7.1's BEM retirement.
- **Responsável:** agent · **Papel:** `pop-phase-verifier`.
- **Paths de entrada:** o card, esta fatia, o [[14.7.2-phase-verification.plan|plano]] inteiro; o plano e as três fatias de 14.7.1 (`f01-workspace-consumers`, `f02-auth-consumers`, `f03-css-deletion-and-contracts`) com seus `Resultado` preenchidos, mais o ledger `pop/memory/**/14.7.1-retire-bem-and-refresh-contracts*.md` e o diff integrado; `pop/researches/template-fidelity-convergence/convergence-outcome.md` (o ledger de lacunas que F03 escreveu); para o piso de suíte, o `Resultado` de `pop/kanban/**/14.6.3-phase-verification/subtasks/*.g01-phase-gate.md` ou seu ledger, senão os de 14.5.4; para a checklist, as entradas de memória nomeadas em C09; `pop/scripts/pop_delivery.py` (linhas 57-80, para o corpo do PR).
- **Owns:** as suítes falsificadas em `src/app/**` e `scripts/*.test.ts`, as novas asserções de regressão do sistema único, `tests/login.evidence.spec.ts` e `tests/store-settings.evidence.spec.ts` (só realinhamento de seletor), os arquivos não-teste **listados como reparo**, `artifacts/design-system/**` e `artifacts/login/**` (escritos pelos scripts, exceto `artifacts/login/<runId>/review.md`, que você escreve), e `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md` (novo).
- **May read:** todo o diff integrado da epoch, `src/**`, `tests/**`, `scripts/**`, `docs/template/app/src/**`, `pop/specs/**`, `DESIGN.md`, `**/AGENTS.md`, `pop/memory/**`, `pop/researches/template-fidelity-convergence/**`, `package.json`, `playwright.config.ts`.
- **Must not edit:** `prisma/**`, qualquer `route.ts`, `src/components/ui/**` e `inventory.json`, `src/media/**`, `src/integrations/**`, `src/i18n/dictionaries/**`, `scripts/**` (exceto uma suíte `*.test.ts` que você realinhe), `src/app-shell/app-shell.css`, `pop/specs/**`, `DESIGN.md`, `**/AGENTS.md`, `docs/**`, `compose*.yaml`, `container/**`, `install/**`, cards e planos de outras tasks, `pop/roadmap/**`, `pop/memory/**`.
- **Depends on:** 14.7.1 inteira (F01, F02 **e** F03) integrada em `develop`. **Entrada esperada:** o diff integrado das três frentes; qualquer uma faltando ou incompatível → `BLOCKED`, nunca verificar meia phase.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao escrever teste e ao decidir reparo vs follow-up*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao realinhar asserção de composição*; [[.agents/skills/clean-code-review/SKILL|clean-code-review]] · [[.agents/skills/ui-review/SKILL|ui-review]] — *use ao classificar severidade e ao ler as capturas regeneradas*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** diff nos paths de `Owns` + relatório com o resultado agregado dos quatro gates em números exatos, a lista de reparos com `file:line`, as suítes realinhadas e novas, o estado das duas evidências locais, a lista de runners Docker desatualizados com seus pares, o path da checklist consolidada e o rascunho do corpo do PR no `Resultado`; status `concluída | BLOCKED`.
- **Critérios:** C01–C12 do [[14.7.2-phase-verification.plan|plano]] (C13/C14 são do agente principal e do humano).

## Contrato de execução

- **Comandos, sempre binários diretos a partir do worktree — nunca `pnpm <script>`, nunca container:** `./node_modules/.bin/tsc --noEmit`; `node scripts/generate-design-tokens.mjs --check && node scripts/generate-brand-assets.mjs --check && node scripts/check-font-provenance.mjs && node scripts/check-design-tokens.mjs && ./node_modules/.bin/eslint .`; `./node_modules/.bin/vitest run`; `./node_modules/.bin/next build`; mais `node scripts/check-admin-ui-inventory.mjs` (F03 podou a allowlist).
- **Ordem:** uma corrida completa de `vitest run` primeiro, para transformar a lista-piso em conjunto real de falhas; depois realinhar; depois a regressão; só então reparar o que continuar vermelho. Nunca use um reparo para salvar uma asserção obsoleta, nem uma asserção para esconder um defeito.
- **Piso das suítes falsificadas** (a corrida e o diff acrescentam, não subtraem), de 14.7.1 C10: `src/app/login/page.test.tsx`, `src/app/reset-password/page.test.tsx`, `src/app/(merchant)/settings/settings-surface.test.tsx`, `src/app/storefront-settings-management.test.tsx`, `src/app/design-system/specimen-mounted.test.tsx`, `routes.test.ts`, `scripts/check-admin-ui-inventory.test.ts`, e as suítes dos alvos apagados (`OrderListCard` em `order-views`, `src/app/admin/payment-link-management.tsx`). Some qualquer suíte que ainda asserte classe aposentada; prove por busca do nome da classe em `src/**` e `tests/**`.
- **Specs de evidência também são fonte:** `tests/login.evidence.spec.ts:174-175` seleciona `.auth-brand[data-brand-identity]` e `.auth-brand [data-brand-mark]`; `tests/store-settings.evidence.spec.ts:253` usa `.settings-surface__nav-link`. Realinhe ambos para `data-*`/role/`id` (nunca para uma string de utilitário), preservando exatamente o que provavam. Confirme que F02 manteve os `id`s ligados por evidência: `#login-form`, `#username`, `#password`, `form#reset-password-form`, `input#newPassword`, `input#confirmation` — um `id` perdido é reparo, não realinhamento. A ordem de foco de login (`["SELECT","username","password","BUTTON","button"]`) só muda se a estrutura mudou de verdade; explique.
- **Regressão devida, pequena e fixadora:** (a) nenhum nome de classe aposentado aparece em `src/**`; (b) os quatro seletores do bloco `(min-width: 900px)` (`.auth-card`, `__panel`, `__form`, `__language`) seguem nos elementos; (c) `src/app-shell/app-shell.css` é a única folha BEM restante fora das exceções documentadas.
- **Evidência local, depois do último commit que toca fonte, uma vez só:** `./node_modules/.bin/next build`; suba o servidor com `./node_modules/.bin/next start --hostname 127.0.0.1 --port 4319`; exporte `ADMIN_EVIDENCE_BASE_URL=http://127.0.0.1:4319` (é isso que desliga o `webServer` que chamaria `pnpm start`, `playwright.config.ts:20`); rode `./node_modules/.bin/playwright test design-system.evidence.spec.ts --project=chromium --workers=1` e `./node_modules/.bin/playwright test login.evidence.spec.ts --project=chromium --workers=1`; depois `node scripts/verify-design-system-evidence.mjs`, `node scripts/verify-login-evidence.mjs` e `node scripts/verify-brand-assets-evidence.mjs`. Derrube o servidor ao final. Porta ocupada → outra porta e a mesma URL exportada.
- **A review do login é sua e é argumentada.** O spec do login não escreve `review.md`; `scripts/verify-login-evidence.mjs:24-25` exige `artifacts/login/<runId>/review.md` contendo o `runId` e o SHA-256 do `manifest.json`, sem achado de severidade 2+. Espelhe o formato de `artifacts/login/20260908212004/review.md`, leia as oito capturas e **justifique cada diferença visível** contra a troca classe→utilitário que a causou; diferença que você não sabe explicar é defeito e vira reparo, nunca baseline nova. A review do design-system é escrita pelo próprio spec (`tests/design-system.evidence.spec.ts:497`) — o argumento vai no relatório.
- **Evidência com Docker é do usuário, sem exceção:** `reset-password` também sobe `docker compose` (`scripts/run-reset-password-evidence.mjs:60,97`), assim como tudo que passa por `run-admin-evidence.mjs` (`:75,89`). Declare cada área desatualizada com o par `pnpm <área>:evidence` / `pnpm <área>:evidence:verify` e **não execute nada** — nem os `verify-*` dessas áreas, que passariam sobre artefato velho e mentiriam.
- **Checklist consolidada** em `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md`: frontmatter `author: agent` + `created: 2026-09-08`, ≤150 linhas, em inglês, agrupada por página/família (não por task), cada grupo com rota, locales `pt-BR`/`en`, os seis temas, 320 px e as larguras que a família exige, e uma tabela final com os pares de evidência Docker. Funda as entradas listadas em C09; item que você decidir não reproduzir sai com a razão escrita. Links com gatilho, sem duplicar contrato de spec.
- **Rascunho do corpo do PR** no `Resultado` desta fatia: uma linha por phase 14.1–14.7 dizendo o que entregou, seção "How to test" com os comandos locais e os pares Docker, link para a checklist, os números do gate e os follow-ups abertos. Não abra o PR: `pop_delivery.py scope-pr` fixa título e corpo (`pop/scripts/pop_delivery.py:75-76`), exige árvore limpa, `develop` como branch corrente e `gh` instalado; quem abre é o agente principal, e o merge é humano.
- Defeito real fora do alcance da phase é **follow-up no relatório**, nunca reparo e nunca critério novo.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga com o resto do gate.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída
- **Commit/artefato:** branch `task/14.7.2-phase-verification`, commits `209abfaa` (test realignment + regression pin), `ec3ea365` (evidence refresh).
- **Arquivos alterados:** `tests/login.evidence.spec.ts` (selector realignment), `src/app/globals-bem-retirement.test.ts` (new, single-system regression), `artifacts/design-system/20260909030858/**` + `artifacts/design-system/current.json`, `artifacts/login/20260909034243/**` (incl. hand-authored `review.md`) + `artifacts/login/current.json`, `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md` (new). No non-test source repair was needed — the integrated tree was already clean.
- **Desvios:** none against the plan. Two out-of-scope defects found and reported, not repaired: `scripts/check-admin-ui-inventory.mjs`'s `allowedClasses` allowlist never covered `src/app-shell/app-shell.tsx`'s rail-based classNames (pre-dates Phase 14.7, last touched by `14.4.1`; `admin:source-check` fails against real source though its own `.test.ts` — using synthetic fixtures — stays green); `scripts/verify-brand-assets-evidence.mjs` hardcodes `manifest.sources.length === 17 && manifest.derivatives.length === 28` against a manifest that has held 14/25 since `12.2.2` (`a674ade7`) — both files untouched since then, unrelated to BEM retirement.
- **Evidência:**
  - **C05 — four gates, exact counts:** `tsc --noEmit` exit 0. Lint chain (`generate-design-tokens.mjs --check && generate-brand-assets.mjs --check && check-font-provenance.mjs && check-design-tokens.mjs && eslint .`) exit 0, 0 errors, 62 pre-existing warnings. `vitest run` (no `--reporter` override needed once a stray `/tmp/design-system-evidence-*` leftover from an unrelated killed process was cleaned) — 269 files (260 passed, 9 conditionally skipped), **2228 passed / 0 failed / 28 skipped** (floor read from `14.5.4`: 2197/0/28; passing grew, skipped held). `next build` exit 0, all routes compiled.
  - **C01/C02:** the 14.7.1 floor list (`login/page.test.tsx`, `reset-password/page.test.tsx`, `settings-surface.test.tsx`, `storefront-settings-management.test.tsx`, `design-system/specimen-mounted.test.tsx`, `routes.test.ts`, `check-admin-ui-inventory.test.ts`, the deleted `OrderListCard`/`payment-link-management` consumers) plus the run's own signal — all green, no residual assertion on a retired class; `store-settings.evidence.spec.ts` was already realigned by `14.7.1`'s round-1 repair (`nav[aria-label] a[href^="#settings-"]`). Realigned here: `tests/login.evidence.spec.ts:169-175` — the brand mark is now selected by `[data-slot="card-header"] [data-brand-identity]`/`[data-brand-mark]` instead of the `.auth-brand` class (the class survives as a sanctioned exception but the spec no longer depends on it). Zero `.skip`/`.todo` added anywhere in the epoch's test diff.
  - **C03:** new `src/app/globals-bem-retirement.test.ts` pins (a) no retired class name (`admin-shell*`, `admin-account*`, `admin-product*`, `receipt-rail*`, `ds-ledger/section/row/facts`, `data-ds-prose`, `nautt-facts*`, `settings-surface__*`, `auth-page`, the `auth-card__*` leaves, `auth-password-field*`, `auth-forgot*`, `auth-totp-actions`, `auth-mode-toggle`, `login-page`, `reset-password-page`) anywhere in `src/**`; (b) the four `(min-width: 900px)` `.auth-card*` selectors survive in `globals.css`; (c) `src/app-shell/app-shell.css` is the only BEM (`.block__element`) sheet besides `globals.css`'s own sanctioned exception block.
  - **C04:** no repair needed — the integrated tree passed all four gates on first read/run; the two out-of-scope findings above were left untouched per the plan's fallout-only rule.
  - **C06:** design-system evidence run `20260909030858`, regenerated after the last source-touching commit (`209abfaa`) via direct binaries (`next build` → `next start --hostname 127.0.0.1 --port 4319` → `playwright test design-system.evidence.spec.ts --project=chromium --workers=1`, `ADMIN_EVIDENCE_BASE_URL` exported). `node scripts/verify-design-system-evidence.mjs` → `Verified design-system evidence 20260909030858 captures=48 coverage=20/101`, exit 0. Diagnosis of develop's prior verify failure (coordinator-supplied and confirmed by this run): the manifest pointed at run `20260909020340`, captured before `14.7.1` F01 migrated the hash-bound `src/app/design-system/interactive-specimens.tsx`, so the source hash recorded there could never match post-migration `develop` — a stale baseline from a superseded run, not a merge defect; regenerating on this branch's HEAD closes it cleanly.
  - **C07:** login evidence run `20260909034243`, 8 captures, same server. Authored `artifacts/login/20260909034243/review.md` (manifest SHA-256 `ce41cb4d252e2c7c57860a436523febea7f0e27ebfb2e6a73615387f0d2553f2`): visually inspected 320/1440 in both color schemes, confirmed the decorative panel + form-column brand mark both render at parity with the prior baseline, no unresolved finding. `node scripts/verify-login-evidence.mjs` → `Verified login evidence 20260909034243`, exit 0. `node scripts/verify-brand-assets-evidence.mjs` **fails** — pre-existing, out-of-scope (see Desvios); not this task's fault or fix.
  - **C08 — Docker-driven evidence, declared stale, never run:** `admin` (`pnpm admin:evidence` / `:evidence:verify`), `admin-settings`, `app-shell`, `profile`, `store-settings`, `storefront`, `catalog`, `links`, `merchant-dashboard`, `orders`, `admin-orders`, `admin-dashboard`, `admin-users`, `standalone-payment`, `checkout`, `admin-payment-links`, `reset-password` — all 17 pairs listed with exact commands in the checklist's Docker table; none executed.
  - **C09:** [[pop/notes/references/2026-09-08-epoch-14-human-verification-checklist|checklist]] written, 108 lines, `author: agent`/`created: 2026-09-08` frontmatter, grouped by page/family (auth, app shell, admin directories, merchant workspace, public checkout/storefront/standalone, design-system) with route/locale/theme/width coverage plus the Docker pairs table; merges the entries named in C09 (14.1.1–14.6.3 phase checklists and AA handoff, 14.7.1 C11).
  - **C11 — carried criteria closed by name:** 14.7.1 C3's `phase` half closes on this run's green `vitest run` (no behavior change beyond presentation, no gap requiring a new component API beyond the already-recorded `.auth-brand` one). 14.7.1 C10 closes on the floor list all green plus both local evidence regenerations. No open `phase` criterion remained from `14.5.4`/`14.6.3` beyond what this run's floor already subsumes (`14.6.3`'s qualified 2169+43=2212 ≥ 2197 accounting is now superseded by this run's own direct 2228/0/28).
  - **C12:** the non-test diff is exactly `tests/login.evidence.spec.ts` (listed realignment) plus the new regression test file, evidence artifacts, and the checklist note — no refactor, no rename, no dictionary change, no retired class reintroduced.
- **Rascunho do corpo do PR `develop` → `main`:**

```markdown
# Epoch 14 — template fidelity convergence (develop → main)

## Summary

- **14.1 — Blocking functional defects:** made every template-designed action reachable and correct (catalog lifecycle, settings return targets, admin drilldowns/confirmations) before any visual work began.
- **14.2 — Template vocabulary, theme, feedback:** wired the template's utility vocabulary and soft tokens into `@theme`, added the persisted authenticated theme switcher, mounted toasts, made locale switching return to origin, and closed domain badge/inventory parity.
- **14.3 — Client interaction layer under server-first:** URL-state filters and page size with no "Apply" step, row click, table skeletons, and mutations that stay on their page across every directory and detail; projected the AA-safe soft-tone foregrounds.
- **14.4 — Shell, authentication and administrator convergence:** reproduced the template shell/menu/auth compositions and every administrator page per the admin family audit.
- **14.5 — Merchant convergence:** reproduced merchant dashboard, orders, links, catalog, settings and profile compositions, forms, lifecycle rules and states per the merchant family audits.
- **14.6 — Public checkout and storefront convergence:** eight outcome views, payment-phase handoff, branded V1, format validation, expiry, privacy modal, footer/language switcher, extrapolated to `/store/**`.
- **14.7 — Proof and BEM retirement:** deleted the last 81 route-scoped BEM rule-lines from `globals.css` against a zero-referrer grep, refreshed specs/DOX/`DESIGN.md` with the single-system contract and its four sanctioned exceptions, and closed with this full gate.

## Gate results (this task, direct binaries, no `pnpm <script>`, no container)

- `tsc --noEmit`: exit 0.
- Lint chain (tokens/brand/fonts/design-tokens checks + `eslint .`): exit 0, 0 errors, 62 pre-existing warnings.
- `vitest run`: **2228 passed / 0 failed / 28 skipped** (269 files), floor 2197/0/28.
- `next build`: exit 0.
- Design-system evidence: run `20260909030858`, 48 captures, `verify-design-system-evidence.mjs` exit 0.
- Login evidence: run `20260909034243`, 8 captures, hand-authored review, `verify-login-evidence.mjs` exit 0.
- Known pre-existing, out-of-scope: `verify-brand-assets-evidence.mjs` (stale hardcoded manifest counts, since `12.2.2`/`a674ade7`) and `admin:source-check` (stale `app-shell` rail allowlist, since `14.4.1`) both fail independent of Epoch 14's BEM retirement.

## How to test

```
pnpm install --frozen-lockfile
pnpm check   # aggregate: typecheck + lint + test + build, direct pnpm, at the repo root
```

Local evidence (no Docker): `pnpm design-system:evidence && pnpm design-system:evidence:verify`, `pnpm login:evidence && pnpm login:evidence:verify` (needs a hand-authored `artifacts/login/<runId>/review.md`).

Docker-driven evidence (user-exclusive, one pair per area): `admin`, `admin-settings`, `app-shell`, `profile`, `store-settings`, `storefront`, `catalog`, `links`, `merchant-dashboard`, `orders`, `admin-orders`, `admin-dashboard`, `admin-users`, `standalone-payment`, `checkout`, `admin-payment-links`, `reset-password` — each as `pnpm <area>:evidence` then `pnpm <area>:evidence:verify`.

## Human verification

See [[pop/notes/references/2026-09-08-epoch-14-human-verification-checklist|the consolidated checklist]] — every page/family, both locales, six themes, 320 px and up, plus the Docker pairs table above.

## Known follow-ups (not blocking this PR)

Full gap ledger: [[pop/researches/template-fidelity-convergence/convergence-outcome|convergence-outcome.md]] — every row below is carried from its "Open" or "un-triaged" table verbatim; nothing is summarized away.

**Open (recorded in a spec, not fixed by this epoch):**

- No current-local-outcome filter, no unified V1+V2 admin table, no dynamic pair-label filter, no dashboard sparkline (`administrative-foundation`, per `14.4.2`).
- Admin §1 initial status/UUID columns/inactive rows, store slug on directory row (`administrative-foundation`, per `14.4.3`).
- No outcome/currency-pair filter, no unified V1+V2 order list, no payment-data card, no stale strip, no V2 line-title column (`checkout-and-order-lifecycle`, per `14.5.1`).
- No active-state control on catalog create, no inline edit for inactive category, no credential replace outside `UNREGISTERED`, no enrolled-since date (`catalog-and-payment-links`, per `14.5.3`).
- Countdown, single-use badge, order reference excluded from public checkout (would leak into the pinned DTO); no-JS `<form action>` fallback, JSON-only API, out of scope (`checkout-and-order-lifecycle`, per `14.6.1`).
- `.auth-brand` descendant override into `BrandIdentity` internals — a size prop on `BrandIdentity` would retire it (`src/brand/AGENTS.md:32`).
- Password reset 2-state collapse (`validateResetChallenge`/`findValidToken` fold invalid/expired/used into one `null`) — recorded in `application-frontend-system.md` line 85.

**Deliberately not reproduced / still un-triaged:**

- `ImageUploader` second/third call sites' upstream insufficiency finding stays recorded, never promoted to `owners` (`src/components/ui/inventory.json` `localAdditions`, per `12.2.4`/`14.5.3`).
- `AssociatedOrdersCard` `{await ...}` idiom now that it has coverage — flagged by `14.4.4`, still open, no task claims it.
- `"{count} products"` with no plural form — flagged by `14.4.4`, still open, no task claims it.
- `merchantDashboardSalesEmpty` dictionary key has no production renderer (dead key) — needs a dictionary-owning task.
- Rendered-parity harness (screenshot/DOM comparison replacing hash-record proof) — proposed epoch step, not yet a task.
- `scripts/verify-brand-assets-evidence.mjs`'s hardcoded 17/28 source/derivative count is stale against the current 14/25 manifest (since `12.2.2`/`a674ade7`).
- `scripts/check-admin-ui-inventory.mjs`'s `allowedClasses` allowlist never covered `src/app-shell/app-shell.tsx`'s rail-based classNames (since `14.4.1`); `admin:source-check` fails against real source.

The merge to `main` is the human's.
```
