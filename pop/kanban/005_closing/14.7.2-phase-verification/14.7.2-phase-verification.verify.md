# Judgment — [[14.7.2-phase-verification]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-09

- **Strategy:** full — epoch-closing gate, round 1, no prior approved surface to freeze.
- **Surface:** `develop...HEAD` (`209abfaa`, `ec3ea365`); source-only diff (excluding tests/artifacts/pop) is **empty**, as declared. Re-runs are sanctioned here: this *is* the phase-verification task.
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Objective and spec conformance

The card's request — run the gate, realign what 14.7.1 falsified, consolidate the human checklist, hand the scope to the PR — is met, with one pin that does not pin what C03 demanded.

| # | Criterion | Mode | Verification | Result | Evidence |
|---|---|---|---|---|---|
| C01 | Realignment without weakening | evidence | test diff + `.skip/.todo` sweep | passed | only `tests/login.evidence.spec.ts:169-175` changed; zero `.skip`/`.todo` added; suite green |
| C02 | Evidence specs realigned in source | evidence | diff of `tests/**` | passed | `.auth-brand` selectors replaced by `[data-slot="card-header"] [data-brand-identity]`; `store-settings` already rebound by 14.7.1 |
| C03 | Single-system regression, small and pinning | evidence | read the new assertions | **failed** | see blocking B1 — one family unpinnable, the 900px pin never reads the elements |
| C04 | Repairs confined to the fallout | evidence | non-test diff | passed | no repair claimed, none present |
| C05 | Aggregate gate green, exact counts | phase | four direct binaries, re-run by the judge | passed | `vitest` **2228 passed / 0 failed / 28 skipped** (260+9=269 files); `tsc` 0; `eslint .` **0 errors / 62 warnings**; `next build` per report |
| C06 | Design-system evidence regenerated | phase | `verify-design-system-evidence.mjs` re-run | passed | `Verified design-system evidence 20260909030858 captures=48 coverage=20/101`, exit 0 |
| C07 | Login evidence regenerated and reviewed | phase | `verify-login-evidence.mjs` re-run + review read | passed | exit 0; run `20260909034243`; `measured` blocks are **byte-identical** to baseline `20260908212004` at all 8 captures — the parity claim is verified, not asserted |
| C08 | Docker evidence handed over, never run | evidence | checklist table | passed | 17 pairs listed with exact commands; no container invoked |
| C09 | One consolidated human checklist | evidence | file read + memory cross-check | passed (see S1) | 108 lines, `author: agent`, covers 14.1.4/14.2.5/14.3.4/14.4.4/14.5.4/14.6.2/14.6.3 human items |
| C10 | PR body draft ready | evidence | slice `Resultado` | passed (see S2) | per-phase summary matches the ledgers; counts match my runs; checklist linked |
| C11 | Carried criteria closed by name | evidence | report read | passed | 14.7.1 C3/C10 answered; 14.6.3's qualified 2169+43 superseded by 2228 direct |
| C12 | No silent behaviour change | evidence | `git diff develop...HEAD` filtered | passed | source-only diff empty; nothing outside `Owns` |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking | **B1 — the C03 pin does not pin.** (a) The retired-name regex ends in `(?![\w-])`, so the entry `settings-surface__` can **never** match (a real class is always `settings-surface__nav-link`) and every prefix family (`admin-shell`, `nautt-facts`, `ds-*`, `receipt-rail`, `auth-forgot`…) only catches the bare block name, never its `__element`/`--modifier` leaves — reintroducing `.settings-surface__nav-link` passes all three pins. (b) C03 requires the four 900px selectors to survive **on their elements**; the test only greps `globals.css`, never `src/app/auth-card.tsx:38-52`, and `css.includes(".auth-card {")` matches the base rule at `globals.css:1366`, not the media block at `:1370-1375`, of which only 2 of 4 selectors are checked. | `src/app/globals-bem-retirement.test.ts:74`, `:81-91` | Match retired names as prefixes (drop the trailing lookahead; the `(?<!data-)` guard already protects `data-ds-*`), and assert the four classNames on `auth-card.tsx` plus all four selectors **inside** the 900px block. I ran the prefix-semantics sweep myself: **0 offenders** — the fix stays green and needs no evidence regeneration. |
| suggestion | **S1 — one human item dropped without a named reason.** `14.3.2.06` hands over a browser walkthrough of "toast, **on-page return**, typed input kept on failure, session kept"; the checklist carries toast (`:41`) and the last two (`:54`) but never the on-page return of a mutation — the core 14.3.2 deliverable. C09 says nothing is dropped without a written reason. | `pop/memory/2026-09-08/14.3.2-keep-mutations-on-page.06-phase-checklist.md` vs checklist `:46-59` | One line in the merchant group: a mutation (order/link/profile/settings) returns on its own page with a toast, no redirect. |
| suggestion | **S2 — the PR follow-up list is a subset presented as the whole.** It carries 6 items; `convergence-outcome.md`'s "Open" table has 7 rows (admin §1 columns/store slug, catalog active-state/inline-edit/credential-replace, checkout countdown/single-use/no-JS are absent) plus 4 un-triaged, and the body never links the ledger. | `pop/researches/template-fidelity-convergence/convergence-outcome.md:33-54` | Add one line pointing at `convergence-outcome.md` as the full gap ledger. |
| nit | **N1 — attribution.** `verify-brand-assets-evidence.mjs` was last touched by `a674ade7` (12.2.2), not 12.7.1; `check-admin-ui-inventory.mjs` *was* touched by 14.7.1 F03 (`26caa8bc`) — but I confirmed F03 removed only `admin-*`/`receipt-rail` entries and `app-shell__rail*` was never allowlisted, so the defect does date to `c7d1a746` (14.4.1). The out-of-scope call is **correct**: neither script is in `pnpm check` (`typecheck && lint && test && build`), so C13 is unaffected. | `git log -1` on both scripts | Fix the two ids when convenient; no repair owed. |

### Scope and integration

- [x] Changes respect `Owns`/`Must not edit` — the diff touches only the declared paths; no `route.ts`, `prisma/**`, `src/components/ui/**`, `src/i18n/**`, spec or DOX file.
- [x] No dependency opportunistically implemented; the two out-of-scope findings were reported, not repaired — correct per the fallout-only rule.
- [x] `verify: phase` criteria re-run here by mandate; `verify: user` criteria carried to the checklist.
- [x] Specs, DESIGN.md and DOX untouched and coherent with 14.7.1's single-system contract.

## Verdict

- **Decision:** **directed repair** — one pinpoint delta in one test file, no strategy change, no kanban route and no counter.
- **Blocking findings:** B1 (C03).
- **Plan defect:** none — the plan asked for exactly the right pin; the executor delivered a weaker one.
- **Suggestions/nits:** S1, S2, N1.
- **Human checklist:** C14 — run `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md` (108 lines, 17 Docker pairs), then review and merge the `develop` → `main` PR. C13 (`pnpm check` at the root on integrated `develop`) stays with the main agent; my worktree run is its floor: 2228/0/28, `tsc` 0, `eslint` 0 errors.
- **Summary:** an honest, independently reproducible gate — every number I re-ran matched the report, the login realignment is a strictly better selector proven equivalent by byte-identical measurements, and the evidence refresh is argued rather than rubber-stamped. The one thing this task was asked to leave behind as durable proof, the retirement pin, is the one thing that does not hold.

## Delta of the return

- **Type:** execucao (dispatched as a directed repair — the card does not move; I check the patch in a ≤10-line addendum in this round).
- **Affected criteria:** C03 — the retired-name sweep cannot fail for `__element`/`--modifier` leaves and the 900px pin never reads the elements it claims to protect.
- **Affected fronts:** `F01` — only `src/app/globals-bem-retirement.test.ts` re-enters.
- **Untouched fronts:** everything else in `F01` is approved and stays integrated — do **not** re-run the evidence (`209abfaa`/`ec3ea365` stay ancestors and no manifest-bound source changes), do not touch `tests/login.evidence.spec.ts`, `artifacts/**`, or the PR draft.
- **Expected action:** 004 applies the B1 correction in that single file and re-runs only `vitest run src/app/globals-bem-retirement.test.ts`; optionally S1's one checklist line and S2's one PR-body line in the same pass.

- **Round-1 markers (superseded by the addendum below, kept as text so exactly one machine verdict stands):** `pop-verdict round=1 decision=reparo-dirigido` · `pop-delta round=1 kind=execucao pontual=true paths=src/app/globals-bem-retirement.test.ts,pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md frentes=F01 intactas=nenhuma`.

### Addendum — directed repair conferred (2026-09-09, `c518056e`)

- **B1 closed.** Family-prefix matching (`isRetiredToken`, `:70-73`) now catches `-`/`__`/`--` leaves, scoped to `className`/CSS-selector tokens so `dictionary.adminNotProvided` cannot false-positive; `findMediaBlock` (`:128-140`) parses balanced braces and asserts all four selectors **inside** the 900px block, plus the four classNames on `auth-card.tsx` (`:162-174`).
- **Probes verified by me, not taken on trust.** I replicated the matcher against the probe tokens: `admin-foo` → family `admin` and `settings-surface__nav-link` → family `settings-surface` both resolve to offenders (test 1 red); the survivors `auth-card`, `auth-card__{panel,form,language}`, `app-shell__rail`, `brand-identity__mark` all resolve clean. The two probes would indeed fail it.
- **Re-run:** `vitest run src/app/globals-bem-retirement.test.ts` → **4 passed**; `git status` clean (no probe residue).
- **S1/S2 taken in the same pass:** the checklist gained the 14.3.2 on-page-return line (110 lines, still ≤150); the PR draft now carries all 7 "Open" rows and 5 un-triaged rows verbatim, links `convergence-outcome.md`, and re-attributes the brand-assets counts to `12.2.2`/`a674ade7` (N1 closed).
- **Evidence untouched, correctly:** no manifest-bound source changed and `209abfaa`/`ec3ea365` remain ancestors, so the 48+8 captures stand.
- **Decision:** approved. C03 now pins what it claims; every other criterion held from round 1.

<!-- pop-verdict round=1 decision=aprovada -->
