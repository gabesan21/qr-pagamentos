# Front F03 — Route wiring and opaque response — [[11.1.2-add-admin-password-reset-action]]

- **Deliverable:** `POST /admin/users/[id]/reset-password` passes the resolved admin actor into the service and preserves origin-guard, re-authorization, request-log, and the closed `requested`/`failed` redirect set.
- **Scope:** one-line service call change plus route test update; no page or UI change.
- **Responsible:** agent.
- **Owns:** `src/app/admin/users/[id]/reset-password/route.ts`, `src/app/admin/users/[id]/reset-password/route.test.ts`.
- **May read:** `src/app/admin/guard.ts`, `src/app/origin-guard.ts`, `src/observability/server-request-log.ts`.
- **Must not edit:** `/admin/accounts/[id]/page.tsx`, other admin subroutes.
- **Depends on:** F02.
- **Skills:** [[pop/skills/clean-code-change|clean-code-change]].
- **Criteria:** 3, 6.

## Execution contract

- Capture the principal returned by `requireAdminFromCookie()` and pass it as the actor argument to `getAdminPasswordResetService().sendResetEmail(id, actor)`.
- Keep origin-guard first, then auth, then service work; preserve the `withServerRequestLog` wrapper and the `adminUserResetPassword` route literal.
- Keep `requested`/`failed` redirects unchanged; the route must still return empty `401`/`403` via `protectedMutationResponse`.
- Update the route test to assert that the resolved actor is forwarded into `sendResetEmail`.

## Result

- **Status:** done
- **Commit/artifact:** —
- **Changed files:** `src/app/admin/users/[id]/reset-password/route.ts`, `src/app/admin/users/[id]/reset-password/route.test.ts`
- **Deviations:** none.
- **Evidence:** `pnpm test src/app/admin/users/[id]/reset-password/route.test.ts` PASS (4 tests).
