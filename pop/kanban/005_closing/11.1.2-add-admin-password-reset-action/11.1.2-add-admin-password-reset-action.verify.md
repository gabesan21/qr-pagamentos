# 11.1.2-add-admin-password-reset-action — Yolo critic verification

## Decision

**Approved.** The task meets the card objective, the approved plan criteria, and the project gates.

verificado por revisor independente (yolo) — 2026-07-28

## Verification mode

- **Mode:** `full` — task is `critical: true` and this is the first 005 closing review.
- **Surface audited:** diff integrated in `develop` (commit `7a9a871`), plus re-run of all plan criteria.

## Criteria and evidence

| # | Criterion | Evidence | Result |
|---|-----------|----------|--------|
| 1 | Audit table is append-only with restrictive target FK and no actor FK | `prisma/schema.prisma` model `PasswordResetRequest` has FK only on `userId` → `User` (`onDelete: Restrict`); `actorId` is a plain UUID column. Migration `20260728210000_password_reset_request/migration.sql` creates the table, primary key, target FK, and grants only `SELECT, INSERT`. `pnpm db:migration-policy` PASS (baseline=19 future=14). `pnpm db:contract-check` PASS. | PASS |
| 2 | Runtime role holds only `SELECT`/`INSERT` on the audit table | Migration grants `SELECT, INSERT` to `qr_runtime`; `prisma/bootstrap.sql` re-pins the same restricted grant in a relation-guarded block. `pnpm db:test` PASS (includes `runtime-denials` and `runtime-crud`). | PASS |
| 3 | Success, no-email, disabled/deleted, rate-limit, and delivery-failure each write exactly one audit row | `src/auth/admin-password-reset.test.ts` has dedicated tests for `SENT`, `NO_EMAIL`, `UNAVAILABLE` (inactive/deleted/non-merchant), `UNAVAILABLE` (missing), `RATE_LIMITED`, and `DELIVERY_FAILED`. All 7 tests pass. | PASS |
| 4 | Audit row records actor, target, timestamp, and outcome class but never challenge digest, email, or SMTP detail | Test assertions verify `userId`, `actorId`, `outcome`, and `createdAt`; a dedicated test serializes the audit row and asserts it does not contain the token, email, or SMTP error message. | PASS |
| 5 | Service still throws `AdminPasswordResetUnavailableError`/`MailSenderError` and route maps every failure to `?reset=failed` | Service throws `AdminPasswordResetUnavailableError` for all unavailable/rate-limit paths and re-throws `MailSenderError` for delivery failures. Route tests assert `?reset=failed` for both error types. | PASS |
| 6 | Route remains origin-guarded, admin-only, request-logged, and returns empty `401`/`403` | `src/app/admin/users/[id]/reset-password/route.ts` runs `rejectCrossOrigin` first, then `requireAdminFromCookie`, then `withServerRequestLog`; route tests verify empty 403 (cross-origin), empty 401/403, and the literal `adminUserResetPassword` log route. | PASS |
| 7 | `admin-users` evidence still captures reset button and notices | `pnpm admin-users:evidence` PASS (run `20260728210031`); `pnpm admin-users:evidence:verify` PASS. Review notes the password-reset request card and accessible notices with no internal identity. | PASS |
| 8 | Aggregate gates pass | `pnpm check` PASS (typecheck + lint warnings only + test + build). `pnpm db:test` PASS. | PASS |

## Findings

- **bloqueante:** none.
- **sugestão:** none.
- **nit:** The service does not record an audit row for an unexpected non-`PasswordResetRateLimitError`/non-`MailSenderError` thrown by `getPasswordResetService().requestReset`. The five documented outcomes are fully covered, so this is only a future-resilience note; no action required.

## Notes

- The plan noted a deviation in `pop/scripts/db-contract-check.mjs`: the changes only aligned the script with the existing `.env.example` SMTP additions and relaxed a whitespace-sensitive schema assertion. These are contract-drift corrections, not behavior changes, and the contract-check gate passes.
- `pop/specs/administrative-foundation.md` was correctly left unchanged: that spec explicitly excludes password-reset contracts (line 104), which are owned by Epoch 11.
