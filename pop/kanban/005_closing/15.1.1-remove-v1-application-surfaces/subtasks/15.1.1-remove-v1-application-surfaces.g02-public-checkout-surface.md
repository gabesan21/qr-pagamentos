# Frente F02 — Delete the public checkout V1 surface — [[15.1.1-remove-v1-application-surfaces]]

- **Entrega:** the public checkout is V2-only: the V1 checkout, presentation and status services are gone, `/pay/[identifier]` resolves a single V2 path, and the public API routes carry no V1-first fallback.
- **Escopo:** delete `src/checkout/{public-checkout,public-checkout-presentation,payment-status}.ts` and their tests; delete `src/app/pay/[identifier]/public-checkout-form.tsx` and `public-checkout-form.test.tsx`; collapse the V1-first branch in `src/app/pay/[identifier]/page.tsx`, in `src/app/api/payment-links/[identifier]/checkout/route.ts` and in `.../checkout/status/route.ts`; sweep the same V1-first resolution in `src/app/api/payment-links/[identifier]/route.ts` and delete `src/auth/public-payment-link.ts` if the sweep confirms it is V1-only; remove the V1 cases from `page.test.tsx`, `page.identity-wiring.test.tsx`, `checkout-shell.test.tsx` and `checkout-footer.test.tsx`. The V2 request/response shape, status codes, rate-limit buckets and redaction fences stay exactly as they are.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card [[15.1.1-remove-v1-application-surfaces]]; seções "Objetivo", "Estratégia" e "Critérios" de [[15.1.1-remove-v1-application-surfaces.plan]]; esta fatia; o relato de F01; [[.agents/skills/clean-code-change/SKILL|clean-code-change]]; [`src/checkout/AGENTS.md`](../../../../../src/checkout/AGENTS.md) (leitura, caminhe antes da primeira edição).
- **Owns:** `src/checkout/public-checkout*.{ts,tsx}` e `src/checkout/payment-status.ts` mais seus testes (exceto os arquivos `*-v2*`, que só perdem imports mortos); `src/app/pay/[identifier]/**`; `src/app/api/payment-links/[identifier]/route.ts`, `.../route.test.ts`, `.../checkout/**`; `src/auth/public-payment-link.ts` e seu teste.
- **May read:** `src/**`, `tests/**`, `prisma/schema.prisma`, `pop/specs/checkout-and-order-lifecycle.md`.
- **Must not edit:** `src/orders/**`, `src/auth/payment-link.ts`, `src/app/orders/**`, `src/app/(merchant)/**`, `src/app/admin/**`, `src/app/payment-links/**` (F03); `src/app/routes.test.ts`, `src/observability/**`, `src/security/**`, `src/i18n/**`, `tests/**` (F04); `prisma/**` (15.1.2); `pop/specs/**`, qualquer `AGENTS.md`, `docs/**` (15.1.3); o card e o plano.
- **Depends on:** F01.
- **Entrada esperada:** o relato de F01 com os membros relocados e seu novo home; sem ele, qualquer import V1 remanescente em módulo V2 é `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para isolar o menor ponto de mudança e manter o diff com uma intenção*.
- **Web:** deny.
- **Gate/delta:** 003_human_approval já cumprido pela task; nenhum delta de reentrada.
- **Saída:** relato curto no bloco "Resultado" desta fatia, teto 12 linhas, evidência: arquivos apagados, ramos colapsados e imports V1 restantes (esperado: zero na sua superfície), status `concluída | BLOCKED`.
- **Critérios:** C01, C02, C03, C04, C08, C10 do [[15.1.1-remove-v1-application-surfaces.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- Deletion is subtraction: never relocate a helper here (that was F01), never rewrite a V2 assertion into new behavior, never add a compatibility shim or redirect.
- A module the recon did not name but the import graph proves V1-only is in scope; a module that serves any V2 path is not — report it instead of deleting.
- Este é o único front que toca sua superfície; F03 roda em paralelo em write set disjunto e a árvore só volta a compilar após a integração das duas.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Nenhum teste roda nesta task — `pnpm check` é critério `phase` da [[15.1.4-phase-verification]].
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída (com 1 desvio reportado abaixo).
- **Commit/artefato:** `0b3bc4a8` on `task/15.1.1-remove-v1-application-surfaces--f02`.
- **Arquivos alterados:** deletados `src/checkout/{public-checkout,public-checkout-presentation,payment-status}.ts`+testes, `src/auth/public-payment-link.ts`+teste; editados `page.tsx`, `checkout-shell.tsx`(+test), `api/payment-links/[identifier]/route.ts`(+test), `.../checkout/route.ts`(+test), `.../checkout/status/route.ts`(+test), `page.test.tsx`, `page.identity-wiring.test.tsx`.
- **Desvios:** `public-checkout-form.tsx`/`.test.tsx` NOT deleted (escopo pedia). `createPollingController` nesse arquivo é contrato congelado 14.6.1 ("never move, rename or reshape"), consumido por `src/app/store/[slug]/pay/standalone-payment-experience.tsx` (fora do meu Owns) — deletar quebraria um módulo V2 que não possuo. Arquivo ficou intacto, com o componente V1 `PublicCheckoutForm` agora sem consumidor (dead export). Reportado ao coordenador para decisão (mover o helper, ou fechar o gap na 14.6.1).
- **Evidência:** varredura final em `src`/`tests` — zero imports remanescentes de `public-checkout`/`public-checkout-presentation`/`payment-status`(não-v2)/`auth/public-payment-link`, exceto: `src/app/routes.test.ts` (fora do Owns, F04) que assevera a ordem V1-first e agora quebra — esperado, sweep de outro front; comentários de prosa em `src/storefront/public-storefront.ts` e `src/checkout/AGENTS.md` ainda citam o nome do arquivo deletado (fora do Owns).
