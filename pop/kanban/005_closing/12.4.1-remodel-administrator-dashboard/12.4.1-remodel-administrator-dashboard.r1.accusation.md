# Acusação — [[12.4.1-remodel-administrator-dashboard]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** advogado do diabo

## Eixo de execução — critérios re-rodados

| # | Critério do plano | Verificação executada | Resultado | Evidência |
|---|-------------------|------------------------|-----------|-----------|
| 1 | `pnpm test src/app/admin/dashboard.test.tsx src/app/admin/page.test.tsx` | testes focados | passou | 15/15 passaram |
| 2 | `pnpm test` | suite completa | passou | 1980 passed / 28 skipped |
| 3 | `pnpm lint` | lint gate | passou | 0 errors, apenas warnings em arquivos não tocados |
| 4 | `pnpm typecheck` | TypeScript | passou | sem erros |
| 5 | `pnpm check` | aggregate gate | passou | 231 arquivos de teste passaram |
| 6 | `node scripts/check-shared-ui-inventory.mjs` | inventário compartilhado | passou | sem drift reportado |
| 7 | `pnpm frontend-parity:check` | validação de parity | passou | sem quebra de obrigação/inventário |

### Objeções de execução

| Severidade | Objeção | Evidência | Remédio |
|------------|---------|-----------|---------|
| bloqueante | O estado de carregamento de `/admin` não foi remodelado: `src/app/admin/loading.tsx` continua renderizando o cabeçalho antigo (`receipt-rail`) e um único card com `Skeleton`, enquanto o `AdminDashboardSkeleton` novo exportado em `dashboard.tsx` não é consumido em nenhum lugar. O plano previa "loading skeletons" com `CardSkeleton`/`StatGridSkeleton`. | `src/app/admin/loading.tsx:6-15`; `src/app/admin/dashboard.tsx:498-518`; `grep AdminDashboardSkeleton` retorna apenas a definição no arquivo | Fazer `src/app/admin/loading.tsx` renderizar o `AdminDashboardSkeleton` (ou equivalente que use `CardSkeleton`/`StatGridSkeleton`), preservando `aria-busy="true"` e os textos localizados. |

## Eixo de decisão — ataque à defesa

| Severidade | Decisão atacada | Objeção | Evidência | Remédio |
|------------|-----------------|---------|-----------|---------|
| — | — | nenhuma objeção material | — | — |

## Resultado

- **Eixo de execução:** 1 objeção material (1 bloqueante).
- **Eixo de decisão:** nenhuma objeção material.
- **Superfície coberta:** diff de `src/app/admin/page.tsx`, `src/app/admin/dashboard.tsx`, `src/app/admin/page.test.tsx`, `src/app/globals.css`, dicionários `admin-dashboard/en.ts` e `pt-BR.ts`, e `tests/admin-dashboard.evidence.spec.ts`; critérios de verificação do plano auditados pelos runs fornecidos; decisões da defesa revisadas.
- **Não examinado:** variação visual pixel-a-pixel contra o template (a evidência do run cobre acessibilidade, overflow, alvos, foco e fatos, mas não diferença estética subjetiva não coberta por `frontend-parity:check`); estados de erro com retry não implementados, pois a omissão do `loading.tsx` já devolve a task.
