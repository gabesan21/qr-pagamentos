# Julgamento — [[14.4.2-converge-admin-dashboard-and-directories]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — rodada 1, `size: L`, `critical: false`; leitura profunda do diff integrado, sem re-run de suíte.
- **Superfície:** `bc68b9ce..3674fe0e` (27 paths, F01–F04): dashboard e period control, contrato de query e página de orders, contrato e páginas de payment-links, views compartilhadas tocadas aditivamente, dicionários, spec e `DESIGN.md`.
- **Devoluções:** execução 0 de 2 · plano 0 de 2 — reparo dirigido não consome contador.

### Pedido original (card "O quê / Por quê")

Atendido em quase toda a superfície: período segmentado, grid 5/4/3, drill-downs vivos, filtros e colunas do template, tabela V1 restilizada, URL pública e estado indisponível ilustrado. **Uma exceção material:** o "Resultado observável" exige *nenhum chip de enum cru* nessas páginas, e o card de pedidos associados do detalhe do link renderiza o enum de estado do provedor sem tradução.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Period control client URL state | evidência | `period-control.tsx` | passou | `router.replace` em `useTransition`, set fechado, `7d` default resolvido no servidor (`page.tsx:14-22`), fallback `<noscript>` |
| C02 | Grid 5/4/3, delta chip, BEM aposentada | evidência | `dashboard.tsx`, `globals.css` | passou | `lg:col-span-{5,4,3}` (`dashboard.tsx:534-543`); `trend` só de `view.users.activeNow` (`:207`); `.admin-dashboard*` some do CSS e só resta em `*.test.*` |
| C03 | Skeleton por card e erro inline | evidência | `loading.tsx`, `error.tsx` | passou | skeleton 3 / 5-4-3 / 4 / 2 (`dashboard.tsx:499-522`); tira `role="alert"` com retry `reset()` |
| C04 | Leaderboard drilla em orders | evidência | `dashboard.tsx:432` | passou | `/admin/orders?filter.merchant=${encodeURIComponent(username)}`, substitui `/admin/accounts` |
| C05 | Contrato de query de orders estendido | evidência | `order-v2-admin-directory.ts:51-70,180-187` | passou | 7 filtros; `state` = 8 estados + `STATELESS`→`{ state: null }`; `merchant` igualdade `insensitive`; keyset/page sizes/`q` intactos |
| C06 | Página de orders no padrão do template | evidência | `admin/orders/page.tsx`, `legacy-order-table.tsx` | passou | colunas do template com badges de domínio, `STANDALONE` de volta no select, ledger V1 read-only na mesma linguagem, nunca no keyset |
| C07 | Colunas e filtros de links | evidência | `admin/payment-links/page.tsx:121-136,189-224` | passou | identifier `CopyField`, amount-ou-"N products", created+expiry, filtros `merchant`/`money` localizados |
| C08 | Composição do detalhe do link | evidência | `associated-orders-card.tsx:53-56` | **falhou** | URL pública, empty, view-all e `EmptyState` ok; as linhas mostram `order.state` cru (`CONFIRMED`) e `order.amount` cru, não os badges de provedor/outcome nem o valor formatado |
| C09 | Detalhe do pedido volta ao link | evidência | `admin/orders/v2/[id]/page.tsx:27-46`, `order-v2-views.tsx:180-195` | passou | prop `link` opcional, badge de ciclo de vida, skeleton e um único heading — mas ver achado 2 (merchant não ficou idêntico) |
| C10 | Fronteira read-only e redação | evidência | diff dos dois serviços e das rotas admin | passou | nenhuma mutação nova; `requireAdministrator`/`requireAdminFromCookie` em cada leitura; `merchant` só por username, `money` via ponteiro de câmbio sem expor UUID; DTO de linha inalterado |
| C11 | Dois locales e gates estáticos | evidência | dicionários + relatos de execução | passou | mesmas chaves em `en`/`pt-BR` nos três domínios; `tsc` sem erro fora de fixture de teste invalidado; `check-design-tokens` exit 0 (rodado aqui) |
| C12 | Contratos e checklist da phase | evidência | `administrative-foundation.md`, `DESIGN.md` | passou | filtros, período client-side, BEM aposentada e as 4 lacunas escritas onde o contrato mora; suítes falsificadas listadas para 14.4.4 (a frase "provider/outcome badges" só fica verdadeira após o reparo de C08) |
| C13 | `pnpm check` agregado | phase | 14.4.4 | registrado (phase) | não se julga aqui |
| C14 | Conferência renderizada | humano | — | checklist humana | ver veredito |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | Card de pedidos associados publica enum cru e valor não formatado: `StatusBadge label={order.state ?? "—"}` mostra `CONFIRMED`/`PENDING` em qualquer idioma, o outcome vira o rótulo genérico "Local outcome" e `MoneyText value={order.amount}` imprime o decimal armazenado | `src/app/admin/payment-links/associated-orders-card.tsx:53-56` | usar `ProviderStateBadge` (+ `StatusBadge` neutro para `state === null`) e `LocalOutcomeBadge`/tom danger para `LOCAL_CANCELLED` com rótulos do dicionário, e formatar o valor com o mesmo `formatCatalogPrice(..., locale)` da diretoria (passar `locale` da página) |
| sugestão | Mudança compartilhada não fica atrás de prop: o hint "não exigido pela política" entra em todo `OrderV2DetailCard`, então o detalhe do merchant deixa de renderizar igual — o que C09 e o contrato de F02 pediam explicitamente | `src/app/orders/order-v2-views.tsx:308-311,119-129` | condicionar o hint a uma prop opcional (como `link`) ou registrar a mudança de superfície do merchant na spec |
| sugestão | `aria-label` do controle de período fica na raiz `Tabs` (div sem role) e não na `TabsList`, então o tablist perde o nome acessível que a `<nav>` anterior tinha | `src/app/admin/period-control.tsx:54` · `src/components/ui/tabs.tsx:9-24` | mover o `aria-label` para `TabsList` |
| sugestão | `try/catch` engole qualquer erro do diretório, inclusive o `redirect()` do guard, exibindo card vazio no lugar do erro | `src/app/admin/payment-links/associated-orders-card.tsx:30-34` | tratar só o resultado `status !== "ready"` e deixar o erro subir |
| nit | Delta chip sempre `direction: "up"` com tom success, mesmo com `activeNow` 0 — repete o valor em vez de variar (decisão fixada no plano/F01, não desvio do executor) | `src/app/admin/dashboard.tsx:207` | follow-up: chip neutro ou lacuna registrada como já foi feito para o sparkline |
| nit | `paymentLinkDirectoryFilterMerchant` usa "Vendedor" enquanto o resto do admin usa "Lojista" | `src/i18n/dictionaries/payment-links-directory/pt-BR.ts:116` | uniformizar o termo |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — 27 paths, nenhum `src/components/ui/**`, nenhum `*.test.*`, V1 (`order-v2-directory.ts`, `order-view.ts`) intocado.
- [x] Dependências não foram implementadas oportunisticamente: F02 consome `getForAdminByIdentifier` de F03, sem ler o serviço de link por conta própria.
- [x] Critérios `verify: phase` registrados para 14.4.4 (C13 + suítes admin falsificadas).
- [x] Specs, `DESIGN.md` e DOX coerentes com o entregue (com a ressalva de C08/C12 acima).

## Veredito

- **Decisão:** **reparo dirigido** (bloqueante pontual, 1 arquivo + o call site que passa `locale`).
- **Bloqueantes:** C08 — enum cru e valor não formatado nas linhas do card de pedidos associados.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** hint do payer vazando para o merchant; `aria-label` no tablist; `catch` abrangente; delta chip sempre "up"; "Vendedor" vs "Lojista".
- **Checklist humana:** C14 — abrir `/admin`, `/admin/orders`, `/admin/payment-links` e um detalhe de link nos dois locales e nos dois temas; conferir troca de período sem navegação cheia, filtros `state`/`merchant`/`money`, chips traduzidos, drill-downs e skeletons, sem pílula lavada nem overflow.
- **Resumo:** as quatro frentes entregam a convergência pedida; falta apenas fechar o único ponto onde a página ainda mostra dado bruto ao administrador.

## Delta da devolução

- **Tipo:** execucao (pontual — reparo dirigido, sem trocar de estratégia).
- **Critérios afetados:** C08 — as linhas do card de pedidos associados não usam os badges de domínio nem o valor formatado.
- **Frentes afetadas:** `F03` — só o card e a passagem de `locale` no detalhe.
- **Frentes intactas:** `F01`, `F02`, `F04` — aprovadas, permanecem integradas; não reexecutar.
- **Ação esperada:** trocar os dois `StatusBadge` por `ProviderStateBadge`/`LocalOutcomeBadge` com rótulos do dicionário (neutro para `state === null`, danger para `LOCAL_CANCELLED`) e formatar o valor com `formatCatalogPrice(order.amount, null, locale)`; opcionalmente mover o `aria-label` para `TabsList` na mesma passada.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/admin/payment-links/associated-orders-card.tsx,src/app/admin/payment-links/v2/[id]/page.tsx frentes=F03 intactas=F01,F02,F04 -->
