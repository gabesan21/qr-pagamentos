# Frente F02 — Settings workspace — [[14.5.3-converge-catalog-settings-profile]]

- **Entrega:** `/settings` with the template's store controls (theme swatch grid, segmented layout, color picker paired with hex, logo `ImageUploader`), a confirm-gated store switch in the identity section with the enable guard, a default-currency Clear, five checkout-policy radio cards, and a Nautt section with badge, Validate, confirm-gated reset and one notice per outcome.
- **Escopo:** presentation and client behaviour inside the settings surface over the unchanged `/storefront`, `/storefront/logo`, `/checkout-policy`, `/language-preference` and `/nautt-credentials*` mutations.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.5.3-converge-catalog-settings-profile.md`; seções Objetivo, Estratégia e Critérios de [[14.5.3-converge-catalog-settings-profile.plan|plano]]; esta fatia; [[researches/template-fidelity-convergence/merchant-catalog-settings-profile|audit]]; [[pop/specs/storefront-and-customization|storefront-and-customization]]; [[pop/specs/nautt-finance-integration|nautt-finance-integration]]; [[pop/specs/media-storage|media-storage]].
- **Owns:** `src/app/(merchant)/settings/{page.tsx,settings-surface.tsx}`, `src/app/{storefront-settings-management,storefront-preview,checkout-policy-management,nautt-credential-surface}.tsx`, `src/app/storefront-form-preparation.ts`, `src/i18n/dictionaries/{settings,storefront,checkout-policy,nautt}/{en,pt-BR}.ts`.
- **May read:** `docs/template/app/src/pages/settings/SettingsPage.tsx`, `docs/template/app/src/pages/catalog/fields.tsx`, `src/app/(merchant)/merchant-controls.tsx`, `src/components/ui/**`, `src/components/ui/AGENTS.md`, `src/media/AGENTS.md`, `src/auth/{storefront-settings,checkout-policy,supported-exchange-currency}.ts`, `src/integrations/nautt/**`, `src/app/{storefront,checkout-policy,nautt-credentials}/**`, `src/design-system/themes.ts`, `DESIGN.md`.
- **Must not edit:** qualquer `route.ts`, `src/auth/**`, `src/integrations/**`, `src/media/**`, `src/data-directory/**`, `src/components/ui/**`, `src/design-system/**`, `src/app/(merchant)/{catalog,profile}/**`, `src/app/(merchant)/merchant-controls.tsx`, `src/app/profile/**`, `src/app/globals.css`, qualquer `*.test.ts(x)`, dicionário de outra frente, `pop/specs/**`, `DESIGN.md`.
- **Depends on:** F01.
- **Entrada esperada:** `src/app/(merchant)/merchant-controls.tsx` exportando o `SegmentedControl` merchant-local; ausente ou incompatível → `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao alterar código*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao compor qualquer tela*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff integrado nos paths de `Owns` + relato ≤10 linhas no bloco Resultado, evidência por leitura de arquivo, status `concluída | BLOCKED`.
- **Critérios:** C05, C06, C07, C08, C11, C13 do [[14.5.3-converge-catalog-settings-profile.plan|plano]].

## Contrato de execução

- Read the template `SettingsPage.tsx` first; reproduce the section composition and controls, never the mock persistence, framer-motion or the per-section save model.
- Keep **one** `/storefront` form with **one** Save and the existing section ids/anchors, the `omitUnchangedExtendedFields` payload rule and every POST field name; the scroll-spy nav and `.settings-surface*` styling delivered by 14.1.2/14.3.2 stay as they are.
- Store section: theme swatch grid over the six supported ids, layout `SegmentedControl` imported from `merchant-controls`, `type="color"` paired with the hex text input (both writing the same `storefrontAccentColor` value), and the logo `ImageUploader` whose injected `stage` posts the unchanged `logo` multipart field to `/storefront/logo` and reads `logo=<identifier>` from the followed 303 target, resolving `/media/<identifier>`; a failed or unreadable outcome rejects into the uploader's failed state, and the current native upload form stays under `<noscript>`. The live preview keeps reflecting theme, layout, accent and logo.
- Store-enabled becomes a `Switch` in the identity section behind `ConfirmDialog`; before submitting, a client guard blocks enabling without a valid slug or without both display names and explains what is missing. Standalone payments keeps its own control and hidden mirror.
- Currency section gains Clear, which resets the select to the empty value and submits the same form; keep the no-mapping warning state.
- Checkout policy becomes five radio cards (`NONE`, `NAME_EMAIL`, `EMAIL`, `NAME_EMAIL_CPF`, `NAME_EMAIL_CPF_ADDRESS`) listing the fields each one collects, posting the unchanged `checkoutDataPolicy` value.
- Nautt: keep the register form where a credential is absent; show status badge plus the projected `credential.updatedAt` labelled as a credential update — never a fabricated validation instant. **Validate** re-reads `readStatus` by returning to `/settings` and renders valid/unavailable from `balance`/`balanceUnavailable`; no new call, route or adapter. Offer **reset behind `ConfirmDialog` only** in `REGISTERING`/`INDETERMINATE`, and do not render a replace form where the contract blocks replacement outside `UNREGISTERED` — state the situation instead and leave the finding for F04. Remove the duplicated Nautt notice so each outcome renders once.
- Every new string lands in this front's own domains with `en`/`pt-BR` parity; do not touch `shared` or another front's domain.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <observação relevante>.
