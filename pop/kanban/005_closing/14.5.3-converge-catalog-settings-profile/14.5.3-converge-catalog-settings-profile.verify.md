# Julgamento — [[14.5.3-converge-catalog-settings-profile]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** full — primeira rodada de task `size: L` com quatro frentes e 37 paths; nenhuma superfície previamente aprovada.
- **Superfície:** `git diff develop...HEAD` completo em `pop/worktrees/14.5.3-converge-catalog-settings-profile` (HEAD `11371823`), quatro specs, `DESIGN.md`, `globals.css`, dicionários; `tsc --noEmit`, `check-design-tokens.mjs` e `eslint` nos arquivos do diff.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

O pedido do card ("O quê / Por quê") está atendido em quase toda a superfície: forma plana, uploader com staging, preview sticky, categorias, controles de loja, cards de política, ordem do enrollment TOTP. Duas quebras pontuais impedem o fecho (C02 e C06).

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Form plano, um submit, erros inline, draft mantido | evidência | `product-form.tsx` | passou | pilha única sem `SectionCard` aninhado; `CatalogSubmit` só no create (`product-form.tsx:487`), `FieldError` tem `role="alert"` (`field.tsx:202`); `FormDraftGuard`/`saveFormDraft` intactos |
| C02 | Preselect da moeda padrão, sem controle morto de estado, gap registrado | evidência | formulário + `src/app/products/**` | **falhou** | preselect visível mas não submetido — ver bloqueante B1; sem controle de estado no create e gap (a) registrado na spec |
| C03 | `ImageUploader` com `stage`, `ImageField` removido | evidência | `product-form.tsx`, `catalog-fields.tsx` | passou | `stageProductImage` posta `image` em `/products/images` (`product-form.tsx:52`); `grep ImageField` em `src/` sem ocorrências |
| C04 | Preview com par de moeda e `product-fallback`, badges neutras | evidência | `catalog/page.tsx`, `product-form.tsx`, `product-detail-client.tsx` | passou | `sticky top-20` + `MoneyText pairLabel`; `EntityStateBadge` mapeia `archived → neutral` (`status-badge.tsx:89`); fallback nas três superfícies |
| C05 | Categorias com primitivas, rótulos de diálogo, contagem sem arquivados, linha inativa explicada | evidência | `categories/page.tsx`, `category-row-actions.tsx` | passou | `Field`/`Input`; `closeLabel={dictionary.close}` substitui `dataDirectoryResetFilters`; `if (product.archivedAt !== null) continue`; `catalogCategoryInactiveNoEdit` |
| C06 | Swatches, `SegmentedControl`, color+hex, logo uploader, fallback `<noscript>` | evidência | `storefront-settings-management.tsx` | **falhou** | quatro controles presentes; o fallback `<noscript>` está aninhado dentro do próprio `<form>` — ver bloqueante B2 |
| C07 | Switch confirmado + guard + Clear no form único | evidência | `storefront-settings-management.tsx`, `settings-surface.tsx` | passou | `requestStorefrontEnabledChange` nomeia slug/nomes faltantes; `ConfirmDialog` gate; Clear zera o select do mesmo form; âncoras de seção preservadas |
| C08 | Cinco radio cards com o mesmo POST | evidência | `checkout-policy-management.tsx` | passou | `POLICY_ORDER` com `EMAIL`; `<input name="checkoutDataPolicy" value={selected}>` inalterado |
| C09 | Badge + `updatedAt` como atualização, Validate por re-leitura, reset confinado, sem replace morto, um aviso | evidência | `nautt-credential-surface.tsx` | passou | `router.refresh()` sem nova chamada de provider; `ResetAction` só em `REGISTERING`/`INDETERMINATE`; `nauttLastUpdated` rotulado; aviso único via `Notice` |
| C10 | Stack `max-w-3xl`, save por dirty, medidor contínuo | evidência | `profile-management.tsx`, `password-fields.tsx` | passou | `flex max-w-3xl flex-col gap-6`; `disableSubmit={!identityDirty}`; medidor de 13 passos por caractere com erros inline |
| C11 | Ordem QR → confirm → códigos, download, confirm no regenerate, sem persistência | evidência | `totp-section.tsx`, `recovery-codes.tsx` | passou | `enrollStep`/`handleConfirmSubmit` ramificam pelo param `totp`; `Blob` + object URL; `ConfirmDialog` antes de `handleRegenerate`; nenhum `sessionStorage`/`localStorage` em `src/app/profile/` |
| C12 | Paridade de chaves, domínio próprio, fronteiras intactas | evidência | dicionários + diff integrado | passou | paridade `en`/`pt-BR` conferida por diff (5/6/10/15/14 chaves); nenhum `*.test.*`, `route.ts`, `src/auth/**`, `src/integrations/**`, `src/data-directory/**` ou `src/components/ui/**` no diff |
| C13 | Specs/`DESIGN.md` atualizados, BEM exata removida, resto intacto | evidência | quatro specs, `DESIGN.md`, `globals.css` | passou | blocos nomeados removidos; único consumidor restante é `storefront-preview.test.tsx:57` (teste falsificado); `.settings-surface*`, `.storefront-logo` e `.storefront-*` públicas preservadas |
| C14 | `typecheck` e `lint` sem erro | evidência | `tsc --noEmit`, `check-design-tokens.mjs`, `eslint` | qualified pass | tokens ok; eslint 0 erros (3 warnings, 1 pré-existente); `tsc` acusa **1** erro, e só em `nautt-credential-surface.test.tsx:13` (prop `locale` nova) — teste falsificado que o plano roteou para 14.5.4 |
| C15 | Realinhamento de suíte na verificação da phase | phase | registro para [[14.5.4-phase-verification]] | registrado (phase) | falsificados: `storefront-preview`, `nautt-credential-surface`, `storefront-settings-management`, `settings/page`, `settings-surface`, `catalog/page`, `catalog/categories/page`, `catalog/products/[id]/page`, `catalog/products/new/page`, `profile/profile-management` |
| C16 | Passe de navegador (drag-and-drop, foco, TOTP, 320 px, temas, locales, AA) | humano | checklist humana | humano | ver Checklist humana |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | **B1 — a moeda pré-selecionada não é enviada no create.** `CurrencyField` usa `DirtyNativeSelect`, que só ganha `name` depois de um `change`; sem interação, `currencyCode` não vai no POST, `productValues` o omite e o serviço grava `null`. O merchant vê BRL selecionado e o produto nasce sem moeda — e a spec já registra o preselect como entregue | `catalog-fields.tsx:98`; `product-form.tsx:355-379`; `src/app/products/route.ts:19`; `src/auth/product.ts:219` | no create, submeter o valor exibido (espelho oculto de `selectedCurrency` ou `name` sempre presente quando `creating`), mantendo a omissão dirty no edit |
| bloqueante | **B2 — o fallback `<noscript>` do logo está aninhado no form de settings.** `<form action="/storefront/logo">` nasce dentro de `<form action="/storefront">`; com script desligado (único caso em que o conteúdo do `noscript` é parseado como markup) o parser descarta o `<form>` interno e o `input[name=logo]` + submit passam a pertencer ao form externo, urlencoded. O caminho sem JS que C06 exige deixa de postar em `/storefront/logo` — o `develop` fazia certo com form irmão vazio + atributo `form=` | `storefront-settings-management.tsx:181` e `:325-330`; padrão anterior em `develop:src/app/storefront-settings-management.tsx:286` | tirar o form do `<noscript>` de dentro do form de settings: form irmão (fragmento) com `id`, e input/botão associados por `form={uploadFormId}` |
| sugestão | `URL.revokeObjectURL` roda no `finally` imediatamente após `link.click()`; em alguns navegadores isso aborta o download dos códigos | `recovery-codes.tsx:45-55` | revogar após um tick (`setTimeout`) ou no unmount |
| nit | `aria-describedby` do campo de nova senha aponta para um `<span>` dentro de container `aria-hidden` | `password-fields.tsx:143-150` | mover o `id` do texto do medidor para fora do bloco `aria-hidden` |

### Escopo e integração

- [x] Alterações respeitam `Owns`/`Must not edit` de cada frente; os dois `loading.tsx` fora do `Owns` original de F04 têm autorização registrada do agente principal.
- [x] Dependências não foram implementadas oportunisticamente (rotas, serviços, `components/ui` e `data-directory` intactos).
- [x] Critérios `verify: phase` registrados para [[14.5.4-phase-verification]].
- [x] Specs e `DESIGN.md` coerentes com o diff — com a ressalva de B1, que a spec de catálogo já descreve como entregue.

## Veredito

- **Decisão:** **reparo dirigido** — dois bloqueantes pontuais, `arquivo:linha` e remédio objetivo, sem mudança de estratégia (teto da rodada: 2).
- **Bloqueantes:** B1 (moeda pré-selecionada não persistida no create) · B2 (fallback `<noscript>` do logo aninhado, inoperante).
- **Defeito de plano:** nenhum — os critérios cobrem o pedido do card.
- **Sugestões/nits:** revogação imediata do object URL; `aria-describedby` para nó `aria-hidden`. Não bloqueiam.
- **Checklist humana:** C16 — drag-and-drop de imagem no produto e no logo, foco preso/restaurado nos modais, fluxo TOTP em três passos com código errado, toasts, 320 px, ambos os temas e locales, WCAG 2.2 AA; qualified pass de C14 — confirmar `pnpm check` verde em 14.5.4 após o realinhamento das suítes.
- **Resumo:** a convergência pedida foi entregue nas três superfícies; restam duas quebras pontuais — uma promessa de formulário que não chega ao POST e um fallback sem JS que o HTML não permite aninhar.

## Delta da devolução

- **Tipo:** execucao (reparo dirigido, não consome contador).
- **Critérios afetados:** C02 — o preselect não é submetido no create; C06 — o fallback `<noscript>` do logo não posta em `/storefront/logo`.
- **Frentes afetadas:** `F01` (B1, `product-form.tsx`) e `F02` (B2, `storefront-settings-management.tsx`) — apenas os dois trechos nomeados.
- **Frentes intactas:** `F03`, `F04` — aprovadas, permanecem integradas; não reexecutar.
- **Ação esperada:** dois patches pontuais; nada mais do diff é reaberto e nenhum teste é escrito ou rodado aqui.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/(merchant)/catalog/product-form.tsx,src/app/storefront-settings-management.tsx frentes=F01,F02 intactas=F03,F04 -->
