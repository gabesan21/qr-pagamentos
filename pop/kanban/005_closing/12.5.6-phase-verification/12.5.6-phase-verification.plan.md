# 12.5.6-phase-verification — Plano

## Objetivo
Executar a verificação acumulada da Phase 12.5 via `pnpm check` (direct pnpm) e reparar falhas dentro do escopo da fase, garantindo que as entregas 12.5.1–12.5.5 permaneçam saudáveis e integradas.

## Escopo
- Task: `12.5.6-phase-verification` · size: M · yolo: true
- Dependências: `12.5.1` a `12.5.5` — todas fechadas (ausentes do kanban).
- Rota yolo não-crítica: 002 → 004 → 005_closing (pulo de 003).

## Critérios verify: phase acumulados

### 12.5.1 — Merchant dashboard
- `src/app/(merchant)/page.tsx` e `dashboard.tsx` usam template tokens/components.
- Preserva analytics, período, moeda e link da loja.
- Loading skeletons espelham o layout.
- Dicionários `merchant-dashboard` cobrem `pt-BR`/`en`.

### 12.5.2 — Merchant order management
- `/orders`, `/orders/[id]`, `/orders/v2/[id]` usam `DataDirectory`, `CopyField`, `MoneyText`, `StatusBadge`.
- V1 byte-frozen; V2 source/outcome vocabulary preservado.
- Filtro/label `STANDALONE` presente.

### 12.5.3 — Merchant payment links
- `/links` directory remodelado com filtros, busca, paginação e legado V1.
- Detalhe, criação, edição, lifecycle e share seguem template.
- Drill-down de pedidos vinculado ao identificador do link.
- Sem alteração de serviço, rota, schema ou fronteira de redação.

### 12.5.4 — Merchant catalog
- `/catalog`, `/catalog/products/new`, `/catalog/products/[id]`, `/catalog/categories` no template.
- Preço exact-decimal, dirty-field omission, image staging, currency gating e archival preservados.

### 12.5.5 — Merchant settings and profile
- `/settings` e `/profile` remodelados; 7 anchors de navegação ancorada em storefront settings.
- TOTP modal respeita evidence spec (Escape no step 2).
- POST routes, autorização e redação preservados.

## Estratégia
1. Rodar `pnpm check` (typecheck → lint → test → build) em direct pnpm.
2. Falhas dentro do escopo Phase 12.5 → reparar no working tree.
3. Falhas fora do escopo → reportar como bloqueio, não implementar.
4. Autorizar `.verify.md` com resultados, comandos e commits.
5. Submeter ao Judge Dredd em 005_closing.

## Riscos
- Testes de evidence podem depender de build/Playwright; `pnpm check` não os roda, mas `pnpm test` inclui spec unitários.
- Warnings pré-existentes de lint/fontes não devem bloquear se já existiam.

## Frentes
- F01 — `pnpm check` e reparos pontuais no escopo Phase 12.5.

## Dependências externas
- Nenhuma; nada a implementar além da verificação/reparo.
