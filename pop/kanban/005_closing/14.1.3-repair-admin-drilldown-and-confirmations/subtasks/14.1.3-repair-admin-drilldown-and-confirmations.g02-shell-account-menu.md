# Frente F02 — Shell account menu — [[14.1.3-repair-admin-drilldown-and-confirmations]]

- **Entrega:** the top-bar account menu offers Sign out for merchants and administrators alike, offers Profile only where the role has one, and never renders an empty panel.
- **Escopo:** the account panel inside `src/app-shell/` and its styling. The rail footer and mobile drawer sign-out controls stay as delivered; navigation labels, breakpoints and the top-bar title belong to later 14.2 fidelity work.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this front file, the plan's "Objetivo", "Estratégia" and "Critérios de aceite" sections, `pop/researches/template-fidelity-convergence/foundation-shell-auth.md` (row "User menu: Profile + Sign out"), the specs and DOX listed in the plan.
- **Owns:** `src/app-shell/shell-navigation.tsx`, `src/app-shell/app-shell.tsx`, `src/app-shell/shell-types.ts`, `src/app-shell/app-shell.css`, `src/app/admin/layout.tsx`, `src/app/(merchant)/layout.tsx`.
- **May read:** `src/app-shell/AGENTS.md`, `src/app/logout/route.ts`, `src/i18n/dictionaries/app-shell/{en,pt-BR}.ts`, `src/i18n/dictionaries/shared/{en,pt-BR}.ts`, `src/components/ui/button.tsx`, `docs/template/app/src/components/{Navbar,Layout}.tsx`, `DESIGN.md`.
- **Must not edit:** `src/app/admin/**` except `layout.tsx`, `src/app/(merchant)/**` except `layout.tsx`, `src/app/globals.css`, `src/app/logout/**`, `src/components/ui/**`, `src/auth/**`, specs, roadmap, the card.
- **Depends on:** nenhuma.
- **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use antes de escrever, para fixar o contrato do componente*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use para o inventário anti-drift, os estados de foco/hover e as leis numéricas de alvo*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff plus this file's `Resultado` section, cap 20 lines, evidence `pnpm exec tsc --noEmit` and `pnpm exec eslint <touched files>`, status `concluída | BLOCKED`.
- **Critérios:** 6, 7, 9, 11, 12 do [[14.1.3-repair-admin-drilldown-and-confirmations.plan|plano]].

## Contrato de execução

- Sign out is a `<form action="/logout" method="post">` inside the panel, exactly the delivered route and semantics used by the rail and drawer; the route file is read-only here.
- `signOut` and `shellProfile` labels already exist and are already passed to the top bar — reach them through the existing `ShellLabels` contract rather than inventing dictionary keys; add a key only if the panel genuinely needs new copy, and then in both `en` and `pt-BR`.
- Profile stays conditional on the caller passing a profile link; the administrator layout still passes none. The panel must never render with zero items in either role.
- Styling stays in `src/app-shell/app-shell.css` (the `app-shell__account-panel*` rules) or Tailwind utilities; `src/app/globals.css` is reserved to a sibling task.
- Keep keyboard and focus behaviour intact: the trigger keeps its `aria-*` contract, panel items stay focusable with a visible outline, and hit targets stay at the shell's existing size floor.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <saída de `tsc`/`eslint` e observação relevante>.
