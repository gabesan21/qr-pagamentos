# Julgamento — [[14.1.3-repair-admin-drilldown-and-confirmations]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-07

- **Estratégia:** differential — non-critical task, first round: the surface is this task's whole diff; untouched code is not re-audited.
- **Superfície:** `git diff 7751cba3..HEAD -- src/` (10 files, +147/−19) — drill-down query, the new client confirmation boundary and the two account pages, the shell account panel and its CSS, four dictionaries.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

**Pedido original ("O quê / Por quê"): atendido.** The link detail emits the accepted `filter.link` grammar, the three destructive administrator actions are dialog-gated (typed username for both deletions) ahead of the same native POST, and the account menu always carries Sign out for both roles.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| 1 | Orders control emits `/admin/orders?filter.link=<encoded id>` | evidência | leitura de `page.tsx` | passou | `src/app/admin/payment-links/v2/[id]/page.tsx:37` — `?link=` gone, identifier through `encodeURIComponent` |
| 2 | Target resolves to the filtered directory, never `invalid-query` | phase | registro na checklist da phase | registrado (phase) | grammar matches `src/orders/order-v2-admin-directory.ts:55,189-190` (`link` text filter → `paymentLink.identifier`); runtime proof is 14.1.4 |
| 3 | Row delete requires the typed row username before POST | evidência | leitura de `page.tsx` e do módulo cliente | passou | `src/app/admin/accounts/page.tsx:120-133` — sole submit path is `DestructiveActionForm` with `confirmation.expectedValue = row.username` |
| 4 | Soft delete typed-confirmed and TOTP disable destructive-confirmed | evidência | leitura de `[id]/page.tsx` | passou | `src/app/admin/accounts/[id]/page.tsx:306` (TOTP) and `:502` (delete, typed username); no bare submit remains |
| 5 | Every confirmation submits a real `<form>` with the ref attached in the same component | evidência | leitura do diff | passou | `src/app/admin/accounts/destructive-confirm.tsx:41,53` — `ref={formRef}` on the rendered `<form>`, `requestSubmit()` on that element |
| 6 | Menu shows Sign out for both roles, Profile only when a link exists, never empty | evidência | leitura de `shell-navigation.tsx` | passou | `src/app-shell/shell-navigation.tsx:110-124` — sign-out form unconditional, `profileLink` still conditional |
| 7 | Routes, authorization, safeguard, POST semantics and redaction unchanged | evidência | leitura do diff | passou | actions byte-identical (`/admin/users/<id>/{delete,totp-disable}`, `/logout`, all `method="post"`); no path under `src/app/admin/users/**`, `src/auth/**`, `src/orders/**`, `src/data-directory/**`, `src/app/logout/**` in the diff |
| 8 | New copy in `en` and `pt-BR`, no literal user-facing string | evidência | leitura dos dicionários e do diff | passou | 4 keys per locale in `admin-users-directory`, 5 per locale in `admin-user-profile`; `destructive-confirm.tsx` holds no literal copy |
| 9 | Files reserved to sibling tasks untouched | evidência | leitura do diff | passou | `--stat` lists no `src/app/globals.css`, `src/app/(merchant)/catalog/**`, settings or `src/components/ui/**` |
| 10 | Dialogs and menu keyboard-operable, visible focus, ≥44px targets | humano | inspeção manual | checklist humana | reading supports it: `button:focus-visible` outline at `src/app/globals.css:1177`, `min-height: var(--control-min-height)` (44px) on the panel item; focus trap/Escape are Radix runtime behaviour |
| 11 | `tsc --noEmit` and `eslint <touched files>` clean | evidência | execução direta | qualified pass (evidência) | `tsc --noEmit` exit 0 verified mechanically on the integrated branch; eslint output was never registered — both front `Resultado` sections stayed as template placeholders |
| 12 | `pnpm check` green with regression coverage | phase | `pnpm check` em 14.1.4 | registrado (phase) | known invalidated assert `src/app/admin/payment-links/v2/[id]/page.test.tsx:83` (expects `?link=`) handed to 14.1.4; account-page asserts on `action="/admin/users/<id>/delete"` still hold, the module renders that same form |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | The spec still documents the old drill-down query, now falsified by the fix | `pop/specs/administrative-foundation.md:58` — "drill-down to `/admin/orders?link=<identifier>`" | Orchestrator spec sync (ato 2): rewrite the query as `filter.link=<identifier>` |
| sugestão | DOX says the username block may expose only the `/profile` link; the panel now also carries a Sign out form | `src/app-shell/AGENTS.md:11-12` | Restate the rule to admit the role-neutral sign-out affordance alongside the conditional `/profile` link |
| sugestão | A `<form>` sits between `role="menu"` and `role="menuitem"`, breaking the menu's owned-children chain | `src/app-shell/shell-navigation.tsx:117-122` | Add `role="none"` to the form, or hoist it and bind the button with the `form` attribute |
| nit | Front `Resultado` sections left unfilled, so criterion 11's eslint evidence is unrecorded | `subtasks/…g01-admin-drilldown-confirmations.md` and `…g02-shell-account-menu.md`, "Resultado" | Record the executed evidence when a front closes; here `pnpm lint` at 14.1.4 absorbs it |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — F01 touched only its pages, the new module and its dictionaries; F02 only `shell-navigation.tsx` and `app-shell.css`.
- [x] Dependências não foram implementadas oportunisticamente por consumidores — `ConfirmDialog` consumed unchanged; no filter, route or service added.
- [x] Critérios `verify: phase` registrados na checklist da phase — 2 and 12, plus the invalidated assert, go to `14.1.4-phase-verification`.
- [ ] Specs, documentação e contratos DOX afetados estão coerentes — pending the two documentation syncs above (`administrative-foundation.md:58`, `src/app-shell/AGENTS.md:11-12`); neither path was inside any front's `Owns`.

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum — the plan's "mudança esperada: nenhuma" for `administrative-foundation` proved slightly optimistic, but the doc line is a stage-005 sync, not a criterion gap.
- **Sugestões/nits:** the four rows above; the first two are follow-ups for the orchestrator's spec/DOX sync, the third a follow-up for the shell menu's ARIA chain.
- **Checklist humana:**
  - Criterion 10 — open the account menu and each destructive dialog with the keyboard: Tab reaches the trigger and every panel item with a visible outline, Enter/Space opens, the dialog traps focus, Escape cancels without posting, and controls measure ≥44px. Pass: no unreachable control, no focus escape, no invisible focus ring.
  - Criterion 11 (qualified) — run `pnpm exec eslint src/app-shell/shell-navigation.tsx src/app/admin/accounts src/app/admin/payment-links/v2/\[id\]/page.tsx src/i18n/dictionaries`. Pass: exit 0, no new diagnostic (also absorbed by `pnpm lint` in 14.1.4).
- **Resumo:** the three defects named by the card are repaired inside their declared surface — the query string alone for the drill-down, one shared client boundary for the confirmations, one unconditional form for sign-out — with delivered routes, authorization and redaction untouched and bilingual copy at parity. What remains is documentation sync, which belongs to the orchestrator's act 2.

<!-- pop-verdict round=1 decision=aprovada -->
