# Judgment — [[14.7.1-retire-bem-and-refresh-contracts]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-08

- **Strategy:** full — first round, three fronts, whole-diff surface (25 paths, `develop...HEAD` = `26caa8bc`).
- **Surface:** every deleted `globals.css` rule re-grepped on HEAD (`src`, `tests`, `scripts`, `docs/template`); token equivalence of each replacement utility checked against the token block; allowlist prune re-derived; gap ledger checked against `pop/memory/2026-09-08/`; static gates re-run.
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Objective and spec conformance

| # | Criterion | Mode | Verification | Result | Evidence |
|---|---|---|---|---|---|
| C1 | Workspace/Nautt/`/design-system` free of BEM, `lg:` two columns | evidence | own grep + diff | **failed** | classes gone (0 hits), but `/design-system` `<h1>` lost `--type-display` — see B1 |
| C2 | Auth free of BEM, only the four 900px selectors survive | evidence | own grep | passed | `auth-*`/`login-*`/`reset-*` class strings 0; survivors = `.auth-card{,__panel,__form,__language}` + documented `.auth-brand` |
| C3 | No behaviour change, geometry preserved | evidence | diff × tokens | **failed** | all colour/space/radius swaps exact (`--muted-foreground`≡`--text-secondary`, `--color-action-accent`≡`--color-action-default` in all 6 themes, `min-h-11`=44px, `pe-10`=40px, `rounded-sm`≡`--radius-tight`); one geometry loss — B1 |
| C4 | Dead exports removed only with zero importers | evidence | grep `src`+`tests` | passed | `OrderListCard` and `admin/payment-link-management` have 0 referrers on HEAD; `legacy-links.tsx` comment realigned |
| C5 | Every deleted rule had zero referrers; nothing live removed | evidence | 27-family grep on HEAD | passed | only `.sr-only`, `.brand-identity*`, `.auth-brand*`, `.auth-card*` + base/`@theme` remain below the tokens; remaining `ds-section`/`login-form`/`settings-surface` hits are `data-*`, `id=`, filenames or type imports |
| C6 | Static gates green | evidence | own run | passed | `check-design-tokens` exit 0; `tsc --noEmit` only the 2 known 14.5.4 test errors; `eslint` on 13 changed files 0 errors / 4 pre-existing `no-img-element` warnings |
| C7 | DESIGN.md + spec + DOX describe the single system | evidence | reading | passed | spec 132 lines (<150), 4 DOX lines added, "BEM retirement (14.7.1)" states each exception's reason; no contract cites a deleted class |
| C8 | Gap ledger: every audit gap closed or open, named | evidence | ledger × memory | passed | 11 closed / 7 open / 6 not-reproduced; spot-checked `14.3.3`, `14.4.2.04`, `14.6.2.03`, `14.5.2.04` — each memory file exists and says what the row claims |
| C9 | Allowlist holds only live classes, script passes | evidence | own run + reasoning | qualified pass | all 22 pruned entries have 0 referrers; only `__`/`--` literals can fail, so the prune cannot add a failure — `(merchant)/settings/settings-surface.tsx` went 14→8 offending literals. Script still exit 1 (95 pre-existing `raw_controls`/`local_variants`, incl. `var(--…)` false positives); `admin:source-check` is **not** in `pnpm check` |
| C10 | Suites/evidence handed to 14.7.2 | phase | handoff list × own grep | **failed** | `tests/store-settings.evidence.spec.ts` falsified and unlisted — see B2 |
| C11 | Rendered pass, 2 locales × 6 themes × 4 widths | human | — | human checklist | — |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking | **B1** `/design-system` page title collapses 24px→14px. `.receipt-rail h1 { font-size: var(--type-display) }` was deleted and the new header gives the `<h1>` no size; Tailwind preflight resets `h1` to `font-size: inherit` (`node_modules/tailwindcss/preflight.css:78-86`), and no base rule sizes `h1` (`globals.css:1351` sets family/tracking only). Same loss the app-shell avoids with `.workspace-heading h1` | `src/app/design-system/page.tsx:229`; `git diff develop...HEAD -- src/app/design-system/page.tsx` | Give the `<h1>` the display size on the projected vocabulary (e.g. `text-[length:var(--type-display)]`, mirroring `app-shell.css:479`), no literal |
| blocking | **B2** `tests/store-settings.evidence.spec.ts:253` still locates `.settings-surface__nav-link` and asserts 7 nav links; that class no longer exists in CSS or markup, so the store-settings evidence runner now fails. F03's report states no further class-string assertion exists in tests and C10's handoff omits this spec | `tests/store-settings.evidence.spec.ts:253-255`; own grep of the 27 deleted families over `tests/**` | Rebind the locator to the surviving contract (`nav[aria-label] a[href^="#settings-"]`) and add the spec to the 14.7.2 handoff |
| nit | Gap ledger links `[[pop/memory/2026-09-08/14.7.1-retire-bem-and-refresh-contracts.f02-auth-consumers]]`, a memory entry that will never exist (the subtask report dies with the card) | `pop/researches/template-fidelity-convergence/convergence-outcome.md:42` | Point at the memory entry that will exist or at the `src/brand/AGENTS.md` line |
| nit | Merchant settings nav-link lost `:focus-visible { outline: var(--focus-width) … }`; UA focus ring still applies (no global outline reset) and the change mirrors the approved admin hub, which has the same absence — not a 2.4.7 failure, but both hubs now rely on the UA ring | `src/app/(merchant)/settings/settings-surface.tsx:92`, `src/app/admin/settings/settings-nav.tsx:40` | Follow-up for a token focus ring on both nav hubs (not this task) |
| nit | Pre-existing, unrelated to this diff: `storefront-settings-management.test.tsx:72` (`value="cashier-daylight"` — the theme picker became a radiogroup in `14.5.3`) and 2 `useRouter` invariants in `(merchant)/settings/page.test.tsx` fail on HEAD | own `vitest run` of the touched colocated suites (3 failed / 83 passed) | Hand to 14.7.2 as inherited red, not caused here |

### Scope and integration

- [x] Every front stayed inside its `Owns`; the two colocated test edits (`login/page.test.tsx:45`, `reset-password/page.test.tsx:43`) are inside F02's `Owns` and are realignments, not weakenings — `type="button"` keeps the paired `aria-label` assertion and `id="reset-password-form"` keeps the form identity; `.auth-card__panel`/`__form` assertions correctly untouched.
- [x] `.auth-card__panel-brand` (migrated outside the leaf list) is byte-identical to `BrandIdentity`'s defaults (`--space-12`/`--type-title` = `globals.css:1357-1358`); dropping it changes nothing.
- [x] No consumer implemented a dependency opportunistically; `.auth-brand` correctly refused and recorded as an open gap.
- [x] Specs, `DESIGN.md` and the four DOX files are coherent with the surviving CSS.

## Verdict

- **Decision:** **directed repair** — two pinpoint defects, one line each, in an otherwise correct and unusually well-evidenced delivery.
- **Blocking findings:** B1 (`/design-system` h1 size), B2 (falsified store-settings evidence locator + missing handoff).
- **Plan defect:** none — the plan's criteria covered the card's request; C1/C3/C10 caught these.
- **Suggestions/nits:** ledger link to a non-existent memory entry; focus-ring follow-up for both settings navs; inherited red suites for 14.7.2.
- **Human checklist:** C11 (rendered pass on `/login`, `/reset-password`, `/settings`, `/admin/settings`, `/design-system` — check the design-system title size after the repair) and the Docker-dependent evidence runners; regenerate `/design-system` hash-bound evidence and the login/reset pixel baselines (C10, 14.7.2).
- **Summary:** the parallel CSS system is genuinely retired — 81 rule-lines removed against a verified zero-referrer grep, every replacement bound to tokens, contracts and gap ledger honest and complete. Two pinpoint misses keep it from approval.

## Delta of the return

- **Type:** execucao.
- **Affected criteria:** C1/C3 (h1 geometry) and C10 (falsified evidence spec not handed off).
- **Affected fronts:** `F01` (design-system h1), `F03` (handoff/ledger record); the test-locator fix rides with the same patch.
- **Untouched fronts:** `F02` — approved, stays integrated; do not re-execute.
- **Expected action:** size the `/design-system` `<h1>` with the projected display token; rebind the `.settings-surface__nav-link` locator in `tests/store-settings.evidence.spec.ts` and name that spec in the 14.7.2 handoff (ledger/report); optionally fix the ledger's dead memory link.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/design-system/page.tsx,tests/store-settings.evidence.spec.ts,pop/researches/template-fidelity-convergence/convergence-outcome.md frentes=F01,F03 intactas=F02 -->
