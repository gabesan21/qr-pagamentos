# Frente F03 — CSS deletion, contracts, DOX and the gap ledger — [[14.7.1-retire-bem-and-refresh-contracts]]

- **Entrega:** every `globals.css` rule whose last referrer is gone is deleted against a grep proof; the surviving exceptions are documented once, where they belong; `DESIGN.md`, the frontend spec, the four DOX files and the admin inventory allowlist describe the single system; the audit's gaps exist as one consolidated ledger for 14.7.2's report.
- **Escopo:** CSS deletion and durable contracts. No `.tsx` file changes here, no behaviour change, no new spec.
- **Responsável:** agent. · **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.7.1-retire-bem-and-refresh-contracts.md`; this slice; the whole [[14.7.1-retire-bem-and-refresh-contracts.plan|plan]], especially "Inventário de partida"; the integrated reports of F01 and F02 (which class names they dropped); `src/app/globals.css` and `src/app-shell/app-shell.css`; `scripts/check-design-tokens.mjs` (read-only — it is what sanctions the two literals); the six files of [[researches/template-fidelity-convergence/template-fidelity-audit|the audit]]; the memory entries `14.4.2.04-contracts-and-gaps`, `14.4.3.04-contracts-and-gaps`, `14.5.1.04-contracts-and-gaps`, `14.5.2.04-contracts-and-gate`, `14.5.3.04-contracts-gaps-and-bem`, `14.6.1.04-contracts`, `14.2.5.05-aa-finding-handoff`, `14.4.4.06-gate-result-and-human-checklist`, plus the 14.6.2 memory when it lands; the specs that record gaps (`administrative-foundation`, `catalog-and-payment-links`, `checkout-and-order-lifecycle`, `storefront-and-customization`, `identity-security`); [[notes/decisions/2026-09-07-template-convergence-decisions|the 2026-09-07 decisions]].
- **Owns:** `src/app/globals.css`, `DESIGN.md`, `pop/specs/application-frontend-system.md`, `src/app-shell/AGENTS.md`, `src/components/ui/AGENTS.md`, `src/data-directory/AGENTS.md`, `src/brand/AGENTS.md`, `scripts/check-admin-ui-inventory.mjs` (**only** the `allowedClasses` set), `pop/researches/template-fidelity-convergence/convergence-outcome.md` (new file, ≤150 lines).
- **May read:** every path under "Paths de entrada"; any file in `src/**` and `tests/**` for grep evidence.
- **Must not edit:** every path owned by F01 and F02, any other `.tsx`, `src/app-shell/app-shell.css`, `scripts/check-design-tokens.mjs`, any other script or any other line of `check-admin-ui-inventory.mjs`, `scripts/check-admin-ui-inventory.test.ts`, other specs, `src/components/ui/inventory.json`, the root `AGENTS.md`, `docs/**`, `artifacts/**`, `pop/memory/**`, `pop/roadmap/**`.
- **Depends on:** F01 and F02, both integrated.
- **Entrada esperada:** the two fronts' reports of dropped class names. Missing → re-derive by grep yourself. A rule whose grep still returns a referrer is **not** deleted: keep it and record why.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep one intention per diff*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the token contract and for `DESIGN.md` as design memory*.
- **Web:** deny. · **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (rules deleted with the grep proof, exceptions documented and where, spec lines compacted, allowlist entries pruned, gaps closed vs open counted), evidence from the three static commands plus `node scripts/check-admin-ui-inventory.mjs`, status `concluída | BLOCKED`.
- **Critérios:** C3 a C9 do [[14.7.1-retire-bem-and-refresh-contracts.plan|plano]].

## Contrato de execução

- **Delete only against a zero grep.** For each class name, grep `src/**` (tests included) before removing its rule; paste the proof shape in the report. Expected deletions: `admin-shell{,__intro}`, `admin-account*`, `admin-product*`, `receipt-rail*` (if 14.6.2 left it), `ds-*` + `[data-ds-prose]`, `nautt-facts*`, `settings-surface*`, and the auth leaves F02 freed. Expected survivors, all inside `globals.css`: the token blocks, `@custom-variant`, `@theme inline`, `@layer base`, the element base rules including `.font-mono, .font-money, code, kbd`, `.sr-only`, `.brand-identity*` with `[data-brand-identity]`, and the `(min-width: 900px)` block with `.auth-card`, `.auth-card__panel`, `.auth-card__form`, `.auth-card__language`.
- **Document each survivor once, where it belongs.** `DESIGN.md` gets the design-memory paragraph: the BEM era is retired, `src/app-shell/app-shell.css` is the single remaining route-neutral BEM file (shell chrome), the auth split-card block stays because `check-design-tokens.mjs` sanctions `(min-width: 900px)` only in `globals.css` and a `min-[900px]:` utility would be a raw-value violation, `.brand-identity*` is component-owned identity geometry, `.sr-only` is a global a11y utility. Do not repeat the same paragraph in the spec.
- **Spec compaction is mandatory, not optional.** `pop/specs/application-frontend-system.md` is at 132 lines against a 150 cap; add the single-system contract line to "Visual language and numeric composition" or "Component and page states" and compact elsewhere by merging redundant sentences — never by dropping a contract clause, a table row or an evidence rule. Report the before/after line count.
- **DOX, one line each, only where it is true:** `src/app-shell/AGENTS.md` — this subtree owns the only remaining BEM stylesheet and no other subtree may add one; `src/components/ui/AGENTS.md` — owned primitives style through the projected utilities, never a page-scoped class in `globals.css`; `src/data-directory/AGENTS.md` — the directory shell and its states are utility-composed; `src/brand/AGENTS.md` — `brand-identity*` is the sanctioned component-owned exception. Do not restate what already lives in the spec.
- **Allowlist prune** in `check-admin-ui-inventory.mjs`: remove from `allowedClasses` every entry whose class no longer exists anywhere (this closes 14.4.4's recorded follow-up about the dead `admin-dashboard*` allowlist). Touch nothing else in the script and do not edit its test; run `node scripts/check-admin-ui-inventory.mjs` and report the exit.
- **The gap ledger** `convergence-outcome.md` is a synthesis alongside the audit, in English, with frontmatter and triggered wikilinks. One row per gap from the six audit files: **closed** with the memory ledger that closed it, **open** with the spec paragraph that records it as a fact, or **deliberately not reproduced** with the reason. Include the gaps this task itself creates (any migration refused for a component API change) and the AA/`ImageUploader`/plural follow-ups already recorded. Nothing from the audit may leave the ledger without a named destination.
- Behaviour stays frozen: no `.tsx` edit, no dictionary change, no token value change, no edit inside `/* generated-theme-tokens:start|end */`.
- Evidence commands, direct binaries only, never `pnpm <script>` and never a container: `./node_modules/.bin/tsc --noEmit`, `node scripts/check-design-tokens.mjs`, `./node_modules/.bin/eslint <changed files>`, `node scripts/check-admin-ui-inventory.mjs`.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída
- **Commit/artefato:** `26caa8bc` on `task/14.7.1-retire-bem-and-refresh-contracts--f03` (worktree, uncommitted to develop/main; not pushed/merged).
- **Arquivos alterados:** `src/app/globals.css` (81 lines deleted, 0 added), `DESIGN.md` (+39/-6), `pop/specs/application-frontend-system.md` (+1), `src/app-shell/AGENTS.md`, `src/components/ui/AGENTS.md`, `src/data-directory/AGENTS.md`, `src/brand/AGENTS.md` (one DOX line each), `scripts/check-admin-ui-inventory.mjs` (allowlist pruned from 47 to 25 entries), `pop/researches/template-fidelity-convergence/convergence-outcome.md` (new, 58 lines).
- **Desvios:** none from the brief's class list. `.receipt-rail*` was already 0-referrer at run time (F01 had migrated `/design-system` off it, ahead of the plan's "14.6.2 may still leave it alive" risk), so it was deleted here alongside the rest instead of surviving as an exception — no divergence to report against F01/F02, matched their reports exactly.
- **Evidência:**
  - Deletions, all pre-verified 0-referrer via `grep -rn "<class>" src --include='*.tsx' --include='*.ts' | grep -v globals.css` before removal: `admin-shell{,__intro}` (2 rules), `admin-account*` (6 rules incl. nested dl/dt/dd), `admin-product*` (9 rules), `receipt-rail*` (5 rules), `ds-{ledger,section,section__heading,row,facts}` + `[data-ds-prose]` (6 rules), `nautt-facts*` (4 rules), `settings-surface*` (15 rules incl. the 64rem media block), `auth-page`, `auth-card__{panel-brand×2,tagline,caption,strip,swatch,form--tight,footer}` (8 rules), `auth-password-field*` (3 rules), `auth-forgot*` (3 rules), `auth-totp-actions`, `auth-mode-toggle*` (3 rules), `login-page`, `login-form`, `reset-password-page`, `reset-password-form` — 81 rule-lines total.
  - Resulting sanctioned inventory below the token block: `.sr-only`; `.brand-identity{,__mark,__name}` + `[data-brand-identity="…"]` descendant selectors; `.auth-brand` + its two `.brand-identity` descendant overrides; `.auth-card{,__panel,__form,__language}` plus the `(min-width: 900px)` block; the `.font-mono`/element base rules; `@theme inline`, `@layer base`, the two `prefers-*` media blocks. Confirmed via `grep -n "^\." src/app/globals.css` after edit — no other selector remains.
  - `DESIGN.md`: new "BEM retirement (14.7.1)" paragraph names all four sanctioned survivors and why each cannot become a utility; the `.receipt-rail*`/`storefront-*` line from `14.6.2`'s entry corrected to reflect the actual retirement.
  - `pop/specs/application-frontend-system.md`: one line added to "Visual language and numeric composition" naming the single system and its four exceptions with a `DESIGN.md` pointer; 131 → 132 lines (well under the 150 cap; no destructive merge attempted since every existing sentence maps to a distinct contract clause).
  - DOX: `src/app-shell/AGENTS.md` (app-shell.css is the only remaining BEM stylesheet), `src/components/ui/AGENTS.md` (owned primitives never styled by a page-scoped BEM class), `src/data-directory/AGENTS.md` (directory shell is utility-composed), `src/brand/AGENTS.md` (`brand-identity*`/`.auth-brand` sanctioned, size-prop gap linked).
  - Allowlist: removed 22 dead entries (`admin-account*`, `admin-catalog*`, `admin-confirm-actions`, `admin-navigation`, `admin-shell*`, `admin-skeletons`, `admin-dashboard*`, `admin-product*`, `receipt-rail*`) — confirmed 0 non-globals.css referrers each; kept `h-11 w-full` (still literally used) and all `app-shell__*`/`workspace-heading*` entries (still live). `node scripts/check-admin-ui-inventory.mjs` fails identically before and after this change (95 pre-existing `raw_controls`/`inline_styles`/`local_variants` failures unrelated to the allowlist prune, confirmed via `git stash` A/B) — pre-existing environment/inventory-drift issue outside F03's write set, not introduced here.
  - Gap ledger `convergence-outcome.md`: 11 closed rows (each with a memory-ledger link), 7 open rows (each with a spec pointer), 6 not-reproduced/follow-up rows (incl. the newly discovered orphaned `merchantDashboardSalesEmpty` dictionary key), 0 gaps created by this task.
  - Suites at risk for 14.7.2: only the ones already named in C10 (`login/page.test.tsx`, `reset-password/page.test.tsx`, `(merchant)/settings/settings-surface.test.tsx`, `storefront-settings-management.test.tsx`, `design-system/specimen-mounted.test.tsx`, `routes.test.ts`, `scripts/check-admin-ui-inventory.test.ts`, `tests/store-settings.evidence.spec.ts` — `.settings-surface__nav-link` locator, rebound in directed repair) — grep of every deleted class name against `*.test.*`/`tests/**` found no additional class-string assertion (`login-form`/`reset-password-form` hits in tests are `id=`/filename references, unaffected by CSS deletion).
  - Static checks: `./node_modules/.bin/tsc --noEmit` 0 non-test errors (2 pre-existing 14.5.4-owned test errors ignored, `nautt-credential-surface.test.tsx` + `payment-link-v2-view.test.ts`); `node scripts/check-design-tokens.mjs` exit 0; `./node_modules/.bin/eslint src/app/globals.css scripts/check-admin-ui-inventory.mjs` 0 errors (globals.css warning "ignored, no config" is expected — not a JS/TS file); `node scripts/check-admin-ui-inventory.mjs` exit 1, pre-existing (see above); `./node_modules/.bin/next build` succeeded, all routes compiled including `/login`, `/reset-password`, `/settings`, `/design-system`.
