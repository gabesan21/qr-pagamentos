# Front F01 — Fail-closed creation path — [[15.2.2-fail-closed-on-documented-creation-errors]]

- **Delivery:** the documented refusal of `POST /orders/onramp` becomes a deterministic, terminal local outcome across adapter, owner service, provider-order store and both V2 checkout services, with unit tests written for the phase suite.
- **Scope:** creation-path classification and the failed marking only. In scope: the closed code allowlist (`order.quote_not_found`, `order.quote_expired`, `order.exchange_not_configured`, `order.payment_method_not_available`, `orders.deposit_bank_account_not_found`, `order.deposit_fields_required`, `order.deposit_fields_validation_failed`) on `400`/`422` with a parseable body; the attempt `FAILED` + order `REJECTED` + capability revocation write; the discard that keeps the quote claim. Out of scope: quote-phase failures, reconciliation, webhook, logging of the provider code (15.2.3), buyer-facing copy and states (15.3.1), any `prisma/**` or route change.
- **Responsible:** agent.
- **Role:** `pop-executor`.
- **Input paths:** card `15.2.2-fail-closed-on-documented-creation-errors.md`; sections "Objective", "Strategy", "Affected areas", "Risks", "Acceptance criteria" of [[15.2.2-fail-closed-on-documented-creation-errors.plan|the plan]]; this slice; [[pop/specs/nautt-finance-integration|nautt-finance-integration]]; [[pop/specs/checkout-and-order-lifecycle|checkout-and-order-lifecycle]]; [`src/integrations/nautt/AGENTS.md`](../../../../../src/integrations/nautt/AGENTS.md); [`src/checkout/AGENTS.md`](../../../../../src/checkout/AGENTS.md); [`prisma/AGENTS.md`](../../../../../prisma/AGENTS.md); [[pop/researches/nautt-finance/raw/orders/create-onramp-order|create-onramp-order]] (Errors section).
- **Owns:** `src/integrations/nautt/pricing-orders-client.ts`, `src/integrations/nautt/owner-pricing-orders.ts`, `src/integrations/nautt/provider-order-store.ts`, `src/checkout/public-checkout-v2.ts`, `src/checkout/standalone-checkout.ts` and the sibling `*.test.ts` of exactly these five modules.
- **May read:** `src/integrations/nautt/**`, `src/checkout/**`, `src/orders/**`, `src/app/api/payment-links/[identifier]/checkout/route.ts`, `src/app/api/store/[slug]/checkout/route.ts`, `prisma/schema.prisma`, `prisma/migrations/**`.
- **Must not edit:** `prisma/**`, `src/app/**`, `pop/specs/**`, any `AGENTS.md`, `src/checkout/payment-status-v2.ts`, `src/checkout/standalone-payment-status.ts`, every `*.database.test.ts`, and any file outside `Owns`.
- **Depends on:** none (task-level `depends_on: 15.1.4-phase-verification`).
- **Expected input:** none.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to fix the contract before coding and to keep the change readable and reversible*.
- **Web:** deny.
- **Gate/delta:** 003 approved plan; no re-entry delta.
- **Output:** implemented change plus written (never executed) unit tests, cap: the five modules and their sibling tests, evidence: diff and the new test names, status `concluída | BLOCKED`.
- **Criteria:** C1, C2, C3, C4, C5, C6, C8, C9.

## Execution contract

- Deliver only this front's scope and criteria.
- Classification is read-only over the response already received: never issue a second `POST /orders/onramp`, never add a retry, backoff or fallback.
- The refusal branch is neither the validation release nor the ambiguity marking: the quote claim stays, no provider-order row survives, and `INDETERMINATE` semantics are untouched for every other outcome.
- The failed marking is one conditional write per checkout service, from `RESERVED`/`CREATING` only, and never widens the public payment view or the route outcome matrix.
- No provider body, message or code may reach an HTTP response; the refusal answers the existing redacted pre-dispatch outcome on first submit and on idempotent replay.
- **Know when to stop:** at most 2 attempts to make an `agent` criterion pass when the failure is environmental (sandbox, permission, flakiness); on the second, record `ambiente`, report the reclassification to `verify: user` and move on. Never build new infrastructure just to verify.
- Run no tests and no Docker: every behavioral criterion belongs to the 15.2.4 phase suite.
- Missing or incompatible dependency/input → answer `BLOCKED` to the main agent with evidence.
- Do not implement, simulate or fix dependencies on your own.
- Do not change paths outside `Owns`; a new need returns to the main agent.

## Result

- **Status:** concluída | BLOCKED.
- **Commit/artifact:** <reference>.
- **Changed files:** <short list, checked against `Owns`>.
- **Deviations:** none | <deviation and the main agent's authorization>.
- **Evidence:** <relevant gate or observation>.
