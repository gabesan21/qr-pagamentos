# Front F02 — Contracts and DOX — [[15.2.2-fail-closed-on-documented-creation-errors]]

- **Delivery:** the durable contract of the documented refusal recorded in the two owning specs and the two subtree `AGENTS.md`, faithful to what F01 landed.
- **Scope:** contract text only. In scope: the refusal class (closed code allowlist on `400`/`422` with a parseable body) as a deterministic terminal outcome; the attempt `FAILED` state paired with the reused order `REJECTED`; the preserved quote claim with no surviving provider-order row; the revoked capability; the unchanged redacted public outcome. Out of scope: any code change, the buyer-facing state copy (15.3.1), the provider-code logging and DTO trim (15.2.3), parity records, root `AGENTS.md`, and every spec not listed in `Owns`.
- **Responsible:** agent.
- **Role:** `pop-executor`.
- **Input paths:** card `15.2.2-fail-closed-on-documented-creation-errors.md`; sections "Objective", "Strategy", "Acceptance criteria", "Specs and contracts" of [[15.2.2-fail-closed-on-documented-creation-errors.plan|the plan]]; this slice; the F01 diff; [[pop/notes/decisions/2026-09-22-v1-removal-and-checkout-decisions|2026-09-22 decisions]] (item 5).
- **Owns:** `pop/specs/nautt-finance-integration.md`, `pop/specs/checkout-and-order-lifecycle.md`, `src/integrations/nautt/AGENTS.md`, `src/checkout/AGENTS.md`.
- **May read:** `src/integrations/nautt/**`, `src/checkout/**`, `pop/specs/**`, `prisma/AGENTS.md`.
- **Must not edit:** all source under `src/**` except the two listed `AGENTS.md`, `prisma/**`, `AGENTS.md` at the repository root, other specs, `docs/**`, and any file outside `Owns`.
- **Depends on:** F01.
- **Expected input:** the landed F01 change (typed refusal, failed marking, discard semantics) — describe what exists, never what was planned.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use for the contract-before-text discipline when the wording fixes an invariant*.
- **Web:** deny.
- **Gate/delta:** none.
- **Output:** amended specs and DOX, cap: the four files, evidence: diff showing the refusal class in each, status `concluída | BLOCKED`.
- **Criteria:** C7, C9.

## Execution contract

- Deliver only this front's scope and criteria.
- Keep "exactly one `POST /orders/onramp`, never retry an ambiguous result" intact: the amendment narrows what counts as ambiguous, it never authorizes a second dispatch.
- State the closed vocabulary as it now is — the order keeps the eight-member state set and the attempt gains `FAILED` — without describing code or duplicating the allowlist in more than one owning spec.
- Record the reason the order state is reused (the migration-safe language cannot widen an existing CHECK) where the lifecycle vocabulary lives; never contradict [`prisma/AGENTS.md`](../../../../../prisma/AGENTS.md).
- Keep each file within its own contract boundary and the project's note size limits; do not restate V1 surfaces removed in Phase 15.1.
- **Know when to stop:** at most 2 attempts to make an `agent` criterion pass when the failure is environmental; on the second, record `ambiente`, report the reclassification to `verify: user` and move on.
- Run no tests and no Docker.
- Missing or incompatible dependency/input → answer `BLOCKED` to the main agent with evidence.
- Do not implement, simulate or fix dependencies on your own.
- Do not change paths outside `Owns`; a new need returns to the main agent.

## Result

- **Status:** concluída | BLOCKED.
- **Commit/artifact:** <reference>.
- **Changed files:** <short list, checked against `Owns`>.
- **Deviations:** none | <deviation and the main agent's authorization>.
- **Evidence:** <relevant gate or observation>.
