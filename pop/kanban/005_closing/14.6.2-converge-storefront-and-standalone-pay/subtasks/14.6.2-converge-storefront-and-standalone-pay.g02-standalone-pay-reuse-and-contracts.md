# Frente F02 — Standalone pay reuse, CSS retirement and contracts — [[14.6.2-converge-storefront-and-standalone-pay]]

- **Entrega:** `/store/[slug]/pay` (page, experience, loading, error) rebuilt as a consumer of 14.6.1's payment/outcome views and `checkout-experience` controller under the same converged shell as F01; the public `storefront-*`/`receipt-rail*` rules and the seven `.checkout-*` rules 14.6.1 kept alive for this route deleted from `globals.css`; the `DESIGN.md` and storefront-spec lines that record the converged public surface.
- **Escopo:** presentation of the standalone payment route, the CSS retirement and the durable contract lines. The sessionless trust boundary is untouched: the submit body stays exactly `{ idempotencyKey, amount, customer }`, required fields stay derived from `checkoutDataPolicy`, polling stays on `POST /api/store/[slug]/checkout/status`, every unavailable cause stays the one opaque view, and the amount keeps the canonical grammar with exact money.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.6.2-converge-storefront-and-standalone-pay.md`; this slice; sections "Objetivo", "Estratégia", "Riscos", "Critérios" of [[14.6.2-converge-storefront-and-standalone-pay.plan|the plan]]; [[researches/template-fidelity-convergence/public-checkout-and-inventory|the audit]] (Part A `/store/[slug]/pay` row, "Key element gaps", the tone-drift lines); [[pop/specs/checkout-and-order-lifecycle|checkout spec]] "Standalone payment buyer experience"; [[pop/specs/storefront-and-customization|storefront spec]]; [[pop/specs/application-frontend-system|frontend spec]]; `DESIGN.md`; [[14.6.1-converge-public-checkout.plan|the 14.6.1 plan]], its slices and memory ledger, plus the delivered `src/app/pay/[identifier]/**` (shell, payment/outcome module, `checkout-experience` controller) — read before the first edit; F01's integrated diff; [`src/checkout/AGENTS.md`](../../../../../src/checkout/AGENTS.md); [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md); `src/components/ui/status-badge.tsx`; `src/i18n/dictionaries/{checkout,storefront}/**` (read-only); `scripts/check-design-tokens.mjs` (read-only).
- **Owns:** `src/app/store/[slug]/pay/page.tsx`, `src/app/store/[slug]/pay/standalone-payment-experience.tsx`, `src/app/store/[slug]/pay/loading.tsx`, `src/app/store/[slug]/pay/error.tsx`, `src/app/store/[slug]/pay/page.test.tsx` and `src/app/store/[slug]/pay/standalone-payment-experience.test.tsx` (only to retire assertions on removed classes/props — never new coverage, never a run), `src/app/globals.css`, `DESIGN.md`, `pop/specs/storefront-and-customization.md`.
- **May read:** every path listed under "Paths de entrada"; `src/app/store/[slug]/*.tsx` as F01 left them; `src/storefront/cart.ts`; the merchant settings surfaces that still consume `storefront-workspace|logo-*|preview*`.
- **Must not edit:** `src/app/store/[slug]/*.tsx`, `src/i18n/dictionaries/**` (a missing key goes back to the agente principal), `src/components/ui/**`, `src/components/ui/inventory.json`, `src/checkout/**`, `src/app/pay/**`, `src/app/api/**`, `scripts/**`, every other spec and DOX file.
- **Depends on:** F01.
- **Entrada esperada:** F01 integrated (it releases the last `storefront-*` referrer) plus 14.6.1's payment/outcome views and controller. Consumption is graded: the views and the shell/footer are consumed as delivered; the controller is consumed when its submit/poll seam accepts the standalone endpoints, and when it is welded to the link routes the local controller survives but obeys the same phase rule, with the deviation recorded. Duplicating an outcome view, a rail or a tone map is `BLOCKED`, not a workaround.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to delete the local fork instead of adapting it*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the state matrix, tone contract and anti-drift inventory*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (what was consumed from 14.6.1, which rules were deleted with the grep proving no referrer, spec/DESIGN lines written, deviations), evidence `pnpm typecheck` + `pnpm lint` output, status `concluída | BLOCKED`.
- **Critérios:** C1 a C8 do [[14.6.2-converge-storefront-and-standalone-pay.plan|plano]].

## Contrato de execução

- The local `paymentTone`/`paymentCopy` map goes away: provider states render through `ProviderStateBadge` and the owned tone map (`refunded` neutral), never a page-local tone.
- The unavailable view stops being `Card` + destructive `Alert` and adopts the same `EmptyState` vocabulary as the storefront and the checkout, keeping the return-to-store affordance and the single opaque cause.
- Delete from `globals.css` only rules whose last referrer is gone, proving each with a search over `src/**` before removal: `.receipt-rail*`, the public `.storefront-*` set (shell, rail, heading, introduction, products, card, price, group, product-*, stepper, custom-amount, cart, error, skeleton, logo, pay-footer and the cart `[data-slot="empty"]` override) and the seven `.checkout-*` rules 14.6.1 preserved (`card`, `form`, `payment`, `qr`, `copy`, `privacy`, `checkout-v2__total`). `.storefront-workspace*`, `.storefront-logo-block|current|actions|staged` and `.storefront-preview*` belong to merchant settings and stay for 14.5.3/14.7.1.
- Keep the page's `data-theme-preview` + `--storefront-accent` mechanism: its own allowlisted declaration verbatim, or delegation to 14.6.1's shell. `scripts/check-design-tokens.mjs` is path-bound and this front may not edit it.
- Do not reintroduce what 14.6.1 excluded by DTO contract (expiry countdown, single-use badge, order reference); creating a new shared component or touching `inventory.json` is out of scope.
- Spec and `DESIGN.md` receive lines, not narration: one `Task 14.6.2` entry in the storefront spec's "Implemented slices", the minimal amendment to any requirement bullet the diff falsified, and `DESIGN.md` "Public payment and storefront surfaces" updated to the converged shell and states.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `8b30e753` on branch `task/14.6.2-converge-storefront-and-standalone-pay--f02` — "feat(14.6.2): F02 standalone pay reuse, BEM retirement and contracts".
- **Arquivos alterados:** `src/app/store/[slug]/pay/page.tsx`, `standalone-payment-experience.tsx`, `loading.tsx`, `error.tsx`, `page.test.tsx`, `standalone-payment-experience.test.tsx`, `src/app/globals.css`, `DESIGN.md`, `pop/specs/storefront-and-customization.md`.
- **Desvios:** the local phase in `StandalonePaymentExperience` stays a hand-rolled state machine (14.6.1's `useCheckoutExperience` is welded to `/api/payment-links/[identifier]/**` and a customer-only body; the standalone amount-bearing attempt against `/api/store/[slug]/checkout` cannot fit its seam) — it now obeys the same rule the shared controller enforces: field/amount edits only clear their own validation flag, never `attempt`/`payment`/`capability`, fixing a real bug where editing after submit used to silently discard an issued payment. `.receipt-rail*` was NOT deleted (plan expected it retired): `src/app/design-system/page.tsx` still consumes `.receipt-rail`/`.receipt-rail__label`/`.receipt-rail__facts` for an unrelated specimen header, out of this task's scope — kept, reported per contract.
- **Evidência:** consumed from 14.6.1 as delivered — `CheckoutShell` (via `page.tsx`/`loading.tsx`/`error.tsx`, same as F01), `CheckoutPaymentView` (payment/outcome phases in `standalone-payment-experience.tsx`, `CheckoutPaymentViewState` identical to `StandalonePaymentState` so no adapter), `createPollingController` (frozen import, unchanged). Rules removed with grep proof: seven `.checkout-*` rules (`card`, `form`, `payment`, `copy`, `qr`, `privacy`, `v2__total`+span) — `grep -rn 'className="[^"]*\bcheckout-(card|form|payment|copy|qr|privacy|v2__total)\b' src --include=*.tsx` (excl. tests) → 0; the public `.storefront-*` set (shell, rail, heading, introduction, products(+`__heading`/`__list`), card, product-description, price(+span), skeleton(+4 variants), logo, experience, group(+`__heading`), product-name, product-image, stepper(+`__input`), custom-amount(+3 members), pay-footer, cart(+9 members incl. the orphaned `[data-slot="empty"]` override), error(+`__retry`)) — grep for each `className="..."` pattern across `src/**/*.tsx` (excl. tests) → 0 referrers repo-wide, since F01 already dropped the storefront-page markup and this front replaced the last consumer (`/store/[slug]/pay`'s own local header/BEM). Kept with referrer: `.receipt-rail`, `.receipt-rail h1`, `.receipt-rail__label`, `.receipt-rail__facts(+ span)` — `grep -rn "receipt-rail" src --include=*.tsx` (excl. tests) → `src/app/design-system/page.tsx:227-230`; `.settings-surface*` and the storefront-preview family were already retired by 14.5.3 and are absent from `globals.css` (nothing to keep/remove there). Spec/DESIGN lines: one `Task 14.6.2` line in the storefront spec's "Implemented slices" plus header "Updated"; `DESIGN.md` "Public payment and storefront surfaces" bullet rewritten with the converged composition, the deviation, and the `.receipt-rail*` exception (spec 64 lines total). Checks: `./node_modules/.bin/tsc --noEmit` 0 non-test errors (2 pre-existing unrelated test errors ignored per contract); `./node_modules/.bin/eslint` on the 6 changed TS/TSX files — 0 errors; `node scripts/check-design-tokens.mjs` exit 0; `./node_modules/.bin/vitest run "src/app/store/[slug]/pay/"` — 2 files, 40 tests passed (assertions realigned for the Monogram header precedence, `CheckoutPaymentView`'s copy/tone vocabulary, and `EmptyState` unavailable markup — none removed net, several rewritten to match the new composition, none weakened).
