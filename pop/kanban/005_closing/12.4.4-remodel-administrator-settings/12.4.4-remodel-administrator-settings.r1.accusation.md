# Acusação — [[12.4.4-remodel-administrator-settings]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** advogado do diabo

## Eixo de execução — critérios re-rodados

| # | Critério do plano | Verificação executada | Resultado | Evidência |
|---|-------------------|------------------------|-----------|-----------|
| 1 | `pnpm check` passa | reportado pelo executor; não re-rodado | passou | - |
| 2 | evidence 40 capturas + verify passa | reportado; NÃO re-rodado por instrução | passou | run `20260804121936` |
| 3 | `frontend-parity:check` sem PARITY_*_MISSING/INVALID | reportado pelo executor; não re-rodado | passou | - |
| 4 | testes unitários `page.test.tsx` | `pnpm test src/app/admin/settings/page.test.tsx` | passou | 3 tests passed |
| 5 | nenhuma rota/serviço muda contrato | auditoria das rotas reutilizadas | passou | rotas inalteradas |

### Objeções de execução

| Severidade | Objeção | Evidência | Remédio |
|------------|---------|-----------|---------|
| bloqueante | A remodelagem removeu da UI a operação `replace` de moedas de troca. A rota `POST /admin/exchange-currencies` ainda aceita `intent=replace` (`route.ts:27`), mas `ExchangeCurrenciesSection` só expõe registro (`intent="register"`, `exchange-currencies-section.tsx:135`) e rejeita códigos existentes. Administradores não podem mais trocar os UUIDs Nautt de um código existente nem restaurar um código desativado — capacidade presente na UI anterior (`adminExchangeCurrencyReplace`). | `src/app/admin/exchange-currencies/route.ts:24-29`; `src/app/admin/settings/exchange-currencies-section.tsx:102-138`; diff da UI anterior; `route.test.ts:79` | Adicionar à seção `sec-currencies` um fluxo de substituição que poste `intent=replace` para códigos já registrados, mantendo validação de UUIDs. |

## Eixo de decisão — ataque à defesa

| Severidade | Decisão atacada | Objeção | Evidência | Remédio |
|------------|-----------------|---------|-----------|---------|

## Resultado

- **Eixo de execução:** 1 objeção material (1 bloqueante).
- **Eixo de decisão:** nenhuma objeção material.
- **Superfície coberta:** diff de `src/app/admin/settings/**`, `admin-submit.tsx`, `loading.tsx`, `app-shell.css`, dicionários `administration`/`shared`, evidence spec, manifesto de parity; rotas reutilizadas auditadas; testes unitários re-rodados.
- **Não examinado:** execução completa de `pnpm check`, `frontend-parity:check` e `admin-settings:evidence` (já reportados passados; evidence proibido de re-rodar); análise visual screenshot-a-screenshot.
