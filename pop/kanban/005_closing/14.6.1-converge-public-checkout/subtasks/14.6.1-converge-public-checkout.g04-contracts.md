# Frente F04 — Contracts and phase checklist — [[14.6.1-converge-public-checkout]]

- **Entrega:** the durable record of what F01–F03 integrated — the additive V1 `branding` member, the three template affordances excluded by the pinned public boundary, the 560px single column superseding the 1280px sanction, and the DOX lines this diff falsified — plus the checklist `14.6.3-phase-verification` will run.
- **Escopo:** specs, `DESIGN.md` and DOX. No application source, no dictionary, no CSS, no test, no kanban file other than this slice.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** o card, esta fatia, o [[14.6.1-converge-public-checkout.plan|plano]] inteiro, os `Resultado` das fatias g01–g03 e o diff integrado da task.
- **Owns:** `pop/specs/checkout-and-order-lifecycle.md`, `DESIGN.md`, `src/checkout/AGENTS.md`, e `src/components/ui/AGENTS.md` / `pop/specs/application-frontend-system.md` **somente se** o diff integrado os contradisser; e o campo `Resultado` desta fatia.
- **May read:** todo o diff integrado da task, `pop/specs/{storefront-and-customization,media-storage,application-frontend-system}.md`, `pop/researches/template-fidelity-convergence/public-checkout-and-inventory.md`, `pop/roadmap/14-template-fidelity-convergence.md`, `pop/notes/decisions/2026-09-07-template-convergence-decisions.md`, `src/app/pay/[identifier]/**`, `src/checkout/**`, `src/brand/AGENTS.md`.
- **Must not edit:** `src/**` exceto os dois `AGENTS.md` acima; `docs/**`, `prisma/**`, `public/**`, `scripts/**`, dicionários, `globals.css`, qualquer `*.test.*`, `tests/**`; o card, o plano e as fatias g01–g03.
- **Depends on:** F01, F02, F03. **Entrada esperada:** o diff integrado das três frentes e seus relatórios, incluindo as chaves publicadas por F01, as regras CSS mantidas para 14.6.2 e a lista de invariantes de rede conferida por F03.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para separar contrato durável do que morre na memory*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** relatório listando cada arquivo de contrato alterado com uma linha de justificativa, as três exclusões registradas e a checklist de phase para 14.6.3, status `concluída | BLOCKED`.
- **Critérios:** C12.

## Contrato de execução

- **Spec:** amend `pop/specs/checkout-and-order-lifecycle.md` where the V1 presentation projection is pinned (the 4.2.2 statement), recording that the projection additionally carries the owner's persisted branding — localized display name, validated accent, resolved theme id with the design-system default, logo media identifier — resolved from the owner record independent of `storefront_enabled`, exactly as the V2 branch already does, and that nothing else was added. Update the `Updated:` line the way earlier slices did. Weaken no fence: the checkout/status bodies, the closed state vocabulary, the opaque outcomes, the replay/capability rules and the exact-money rules are restated only if the diff changed them, and it must not have.
- **Recorded exclusions:** state plainly, in the same spec, that the template's expiry countdown, single-use badge and order reference are **not** rendered by this convergence because the pinned public boundary excludes link timestamps, link type and every identifier from the public projection and from the status body — and that obtaining them requires a separate approved contract change, not an opportunistic field. Name them as a follow-up proposal in your report; do not open one here.
- **DESIGN.md:** replace the sanction that lets the branded V2 checkout widen to the application cap with the delivered rule — both eras compose one column at the 560px checkout cap — keeping the decision of 2026-09-07 as the authority; record the public checkout furniture (merchant header, footer with powered-by, privacy link and language switcher, privacy modal) and the outcome-view set inside the file's existing sections. Do not restate implementation detail that belongs to the code.
- **DOX:** update `src/checkout/AGENTS.md` with one line scoping the additive V1 branding member under the existing redaction fences. Touch `src/components/ui/AGENTS.md` or `pop/specs/application-frontend-system.md` **only** if the integrated diff contradicts a line there; a new consumer of an existing contract is not a contract change — say so explicitly in your report when you change nothing.
- **Phase checklist for 14.6.3:** list every existing suite this diff falsifies (at minimum `src/app/pay/[identifier]/{page,public-checkout-form,public-checkout-v2-form}.test.tsx` and any route/markup assertion touching retired classes), each with the file and a one-line reason, carry criterion C13 forward, and note the `.checkout-*` rules deliberately kept alive for 14.6.2. Do not write, run or fix a test here.
- Anything real but outside the card's "O quê / Por quê" is a follow-up proposal in your report — never a new criterion or a new front.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Commit/artefato:** `bf705640` on `task/14.6.1-converge-public-checkout--f04`.
- **Arquivos alterados:** `pop/specs/checkout-and-order-lifecycle.md`, `DESIGN.md`, `src/checkout/AGENTS.md`; plus the granted extra-ownership file `src/app/pay/[identifier]/public-checkout-v2-page.tsx` (V2 total/currency wiring). `src/components/ui/AGENTS.md` and `pop/specs/application-frontend-system.md` were read and left unchanged (see below).
- **Desvios:** nenhum.
- **Evidência:**
  - **Spec (`checkout-and-order-lifecycle.md`, 150 lines, ceiling held):** line 7 (`Updated:`) prepends the 14.6.1 entry; line 29 (task 4.2.2 statement) amends the V1 DTO with the additive `branding` member; line 99 (`Page.`) supersedes the two-column/shell-cap description with the 560px single column and records the three exclusions (expiry countdown, single-use badge, order reference — each would add a link timestamp/type/identifier the pinned DTO/status boundary forbids) plus the no-JS `<form action>` gap (checkout API is JSON-only, `src/app/api/**` out of scope); line 100 (`Client.`) moves the retry affordance to the first polling failure and records start-over as the only reset.
  - **DESIGN.md:** "Public payment and storefront surfaces" section supersedes the V2 two-column widen-to-app-cap rule with the shared 560px `CheckoutShell` (merchant header, footer, privacy modal) and the closed eight-view outcome set.
  - **DOX:** `src/checkout/AGENTS.md` gains one line scoping the additive V1 `branding` member under the existing redaction fences. `src/components/ui/AGENTS.md` and `application-frontend-system.md` were read; the diff introduced no contradiction (checkout cap was already 560px there; `QrDisplay`'s pre-existing `identity`/`payload` props and `CopyField`'s compact variant were not touched by this diff) — left unchanged.
  - **V2 wiring (extra ownership):** `public-checkout-v2-page.tsx` now forwards `currencyLabel={presentation.currencyCode ?? undefined}` and `total={... composition.total or .amount}` into `PublicCheckoutV2Form`, closing the C04 gap F03 flagged. `merchantIdentity` was **not** wired: `PublicCheckoutV2Form`'s prop type has no `merchantIdentity` member at all (only `currencyLabel`/`total` are optional today), so plumbing it would require editing the form file, which is outside this front's `owns` — reported as unresolved, not implemented.
  - **Checks:** `./node_modules/.bin/tsc --noEmit` 0 non-test errors (2 known pre-existing unrelated test errors excluded); `./node_modules/.bin/eslint` 0 errors on the changed page file; `node scripts/check-design-tokens.mjs` exit 0; `wc -l` on the spec = 150.
  - **Checklist for 14.6.3 (phase-verification):**
    - Suites falsified, need execution: `src/app/pay/[identifier]/public-checkout-form.test.tsx`, `src/app/pay/[identifier]/public-checkout-v2-form.test.tsx` (payment-phase markup moved from inline `StatusBadge`/`QrDisplay` to `CheckoutPaymentView`; static reading suggests compatibility but is unverified by execution — F03 flag, carried forward).
    - Grep every test for retired `.checkout-v2__*`/`.checkout-shell`/`.checkout-main` class assertions before running; F01 confirmed no other file references them, but 14.6.3 should re-grep against `tests/**` and any `.test.*` after the full merge.
    - `.checkout-*` rules deliberately kept alive (still consumed by `/store/[slug]/pay`): `.checkout-description`, `.checkout-card`, `.checkout-form`, `.checkout-payment`, `.checkout-copy` (+nested), `.checkout-qr`, `.checkout-privacy`, `.checkout-v2__total` (+ `span`) — do not remove these in 14.6.3.
    - C13 (aggregate gate) carried forward: `pnpm check` must exit 0 with the checkout suites updated to the converged composition.
    - Human browser checklist (C14, `verify: user`): open a V1 and a V2 link, both locales, both themes, at 375/768/1440 and 320px — submit malformed data (masks/format errors/focus), pay (submit label names the total), force a polling failure (retry appears on the first failure), open the privacy modal (focus trap/return), confirm QR scannability with the identity centre-cut, confirm no overflow at 320px; note there is no native no-JS fallback (JS-required, by contract per the spec exclusion above).
  - **Follow-up proposals (not implemented here, outside this card):** (1) a separate approved contract change to expose link expiry/single-use/order-reference data for the excluded template affordances, if ever wanted; (2) wiring `merchantIdentity` into `PublicCheckoutV2Form`/`PublicCheckoutForm` (both currently omit it) as a small follow-up task, since F02's `CheckoutPaymentView` already accepts the prop.
