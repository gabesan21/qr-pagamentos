# Judgment — [[14.6.1-converge-public-checkout]]

- **Stage:** 005_closing (act 1) · **Owner:** `pop-judge-dredd` (single judge)

## Round 1 — 2026-09-08

- **Strategy:** full — first round, `size: L`, four fronts merged into one head; the whole 20-path diff `develop...bf705640` was read.
- **Surface:** `src/app/pay/[identifier]/**` (12 files), `src/checkout/public-checkout-presentation.ts`, both checkout dictionaries, `src/app/globals.css`, `scripts/check-design-tokens.mjs`, spec/`DESIGN.md`/`src/checkout/AGENTS.md`. Guarded paths (`src/app/api`, `src/app/store`, `src/components/ui`, `src/brand`, `prisma`, `*.test.*`) confirmed empty in `git diff --stat`.
- **Verification commands (run by me, static only):** `tsc --noEmit` → only the 2 known pre-existing test errors (`nautt-credential-surface.test.tsx`, `payment-link-v2-view.test.ts`, being realigned by 14.5.4); `eslint` on the 15 changed source files → 0; `node scripts/check-design-tokens.mjs` → exit 0. No suite run (14.6.3 owns it).
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Objective and spec conformance

| # | Criterion | Mode | Verification | Result | Evidence |
|---|---|---|---|---|---|
| C01 | Merchant identity and theme on both eras | evidence | shell, `page.tsx`, `public-checkout-v2-page.tsx`, presentation | passed | `checkout-shell.tsx:14-32,47-52` renders logo/`Monogram`/fallback + trust line and scopes `data-theme-preview`+`--storefront-accent`; `public-checkout-presentation.ts:31-44,75-90` mirrors the V2 member with `DEFAULT_STOREFRONT_THEME_ID`, no `storefront_enabled` gate |
| C02 | One 560px column | evidence | shell + `globals.css` | passed | `--checkout-max: 560px` (globals.css:47) is the only cap; `.checkout-v2` (1280/`--shell-max`) and the whole `__columns` block deleted; no `lg:grid-cols-2` left under `src/app/pay` |
| C03 | Form yields, attempt never destroyed | evidence | `checkout-experience.tsx` + adapters | passed | `updateField` (157-166) touches only `values`/`errors`; `startOver` (210-221) is the only reset and re-keys via `attempt=null`; `submit` (179) reuses the live key |
| C04 | Policy-exact validation, masks, amount on submit | evidence | `checkout-form-validation.ts`, controller | passed | server-identical CPF check digits (36-45), CEP/e-mail patterns, `maskCpf`/`maskPostalCode`, `inputMode="numeric"`, `focusFirstInvalid` (66-73), `submitLabel` by `.replace("{total}", …)` (225-227), both eras wired |
| C05 | Eight distinct outcome views | evidence | `checkout-payment-views.tsx` | passed | one live view for RESERVED/CREATING/CREATED/PENDING/INDETERMINATE with distinct badge copy + the INDETERMINATE line (151); CONFIRMED/REFUNDED/REJECTED/CANCELLED/EXPIRED each own a composition; start-over only on the three failures; no new asset |
| C06 | QR and copy per the template | evidence | payment module + call sites | **failed** | `QrDisplay` `payload` + white frame + compact `CopyField` are right (152-165), but `identity` is fed by `props.merchantIdentity`, and **no call site anywhere passes it** — the identity centre-cut the card asks for never renders |
| C07 | Polling contract preserved, retry immediate | evidence | controller vs. previous forms | passed | 5s cadence, `1_000 * 2 ** failures`, visibility pause, opaque 404 all unchanged (128-155); `setPollFailed(true)` on the first catch; `createPollingController` unmoved at `public-checkout-form.tsx:34` with the same signature |
| C08 | Privacy modal and public footer | evidence | notice, footer, dictionaries | passed | `checkout-privacy-notice.tsx` opens `Modal` from `checkoutPrivacyModalTitle/Body`; footer carries powered-by, the link and `LanguageSwitcher` with the anonymous cookie path untouched |
| C09 | No new public exposure | evidence | `src/checkout/**` diff, api diff | passed | only the additive V1 `branding`; `src/app/api` diff empty; status/checkout bodies, states and the opaque outcomes unchanged; countdown/badge/order-reference absent by contract |
| C10 | Scope and CSS retirement | evidence | `rg "className=\"[^\"]*checkout-" src/app/pay` | **failed** | `/pay/**` still emits `checkout-card|checkout-form|checkout-payment|checkout-description` in 10 places (`public-checkout-form.tsx:134,135,154,158,161,171,176`, `public-checkout-v2-form.tsx:78,96,101`); `.checkout-description` survives in `globals.css:1440` although its only referrer in the repo is that `/pay` file — no `/store` referrer |
| C11 | Both locales and static gates | evidence | dictionaries + the three commands | passed | key sets of `checkout/en.ts` and `checkout/pt-BR.ts` are identical (diff empty); tsc/eslint/token-check results above; exactly one accent whitelist entry, now on `checkout-shell.tsx` |
| C12 | Contracts and phase checklist | evidence | spec, `DESIGN.md`, DOX, F04 slice | passed | spec 150 lines, `Updated:` + lines 29/99/100 amended; the three exclusions and the no-JS gap recorded with the boundary that forbids them; `DESIGN.md` supersedes 1280px with the 560px shell; DOX line added; falsified suites listed for 14.6.3 |
| C13 | Aggregate gate after convergence | phase | `pnpm check` in 14.6.3 | recorded | carried to 14.6.3 with the two falsified suites and the retired-class grep; not judged here |
| C14 | Rendered buyer check | human | browser | to the checklist | see Verdict |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking | The identity centre-cut is dead plumbing: `CheckoutPaymentView` accepts `merchantIdentity` and neither era supplies it, so the QR never carries the merchant mark — and `DESIGN.md` now records "the merchant mark as the identity centre-cut" as delivered, so the contract is falsified by its own diff | `checkout-payment-views.tsx:155` vs. `public-checkout-form.tsx:136-146`, `public-checkout-v2-form.tsx:79-90`; `DESIGN.md` "Public payment and storefront surfaces" | pass `branding` (already on both pages) down to both forms and render the logo/`Monogram` node into `merchantIdentity`; `QrDisplay` already raises the error-correction level when `identity` is present |
| blocking | The opaque unavailable treatment became unreachable during the payment phase: a poll `404` sets `unavailable`, but both adapters early-return the payment view, whose tree has no unavailable alert — the buyer is left with a frozen QR and no message when a capability expires or is revoked | `checkout-experience.tsx:132` sets it; `public-checkout-form.tsx:132-150` and `public-checkout-v2-form.tsx:76-93` never read it; spec `checkout-and-order-lifecycle.md` › **Client.**: "`404` (submit or poll) to the one opaque unavailable treatment" | surface `unavailable` inside the payment phase in both adapters (banner above `CheckoutPaymentView` or a prop on it) so the pinned 404 treatment stays reachable after an attempt exists |
| blocking | C10's emission clause was skipped: the converged `/pay` files keep four legacy global BEM classes, and `.checkout-description` is kept alive in CSS with no `/store` referrer, while F04's 14.6.3 checklist records those classes as "still consumed by `/store/[slug]/pay`" — an inaccurate handover that will block 14.6.2's retirement | the 10 `className` sites listed under C10; `globals.css:1440`; `subtasks/…g04-contracts.md` checklist bullet 3 | replace the four class names with the token/utility composition used elsewhere in this diff, delete `.checkout-description`, and correct the 14.6.3 checklist to list only the classes `/store` really consumes |
| suggestion | `merchantName` is a required prop that is never read, which forced V2 to invent a value (`dictionary.storefrontFallbackName`) for a slot that renders nothing | `checkout-payment-views.tsx:74`, `public-checkout-v2-form.tsx:82` | drop it, or use it (it is the natural label next to the identity centre-cut) |
| nit | `phase`/`CheckoutExperiencePhase` is computed and exported but no adapter consumes it (both branch on `payment`) | `checkout-experience.tsx:17,224` | remove or consume |
| nit | Stale comment claims the V2 page "does not forward them yet"; F04 wired it in the same head | `public-checkout-v2-form.tsx:55-58` vs. `public-checkout-v2-page.tsx:64,68` | update the comment when touching the file |

### Scope and integration

- [x] `Owns`/`Must not edit` respected; F04's extra ownership over `public-checkout-v2-page.tsx` is recorded and its edit is confined to the total/currency wiring. F02 reading `alert.tsx`/`button.tsx` outside `may_read` is a process note only — no content from those files entered the delivery.
- [x] No consumer front implemented a dependency of another; `createPollingController` frozen and unchanged.
- [x] Static commands re-run by me in the task worktree; results recorded above.
- [ ] Contracts coherent — `DESIGN.md` asserts a centre-cut the code does not render (blocking 1) and the 14.6.3 checklist misstates the surviving classes (blocking 3).

## Verdict

- **Decision:** **execution blocker → 004_processing**. Three punctual blockers exceed the two-per-round directed-repair budget, so the delta goes back as a diffuse execution return.
- **Blocking findings:** identity centre-cut never wired (C06 + card "O quê"); poll-`404` opaque unavailable treatment unreachable in the payment phase (spec › Client.); `/pay/**` still emits the four retired `checkout-*` classes and keeps `.checkout-description` alive (C10).
- **Plan defect:** none — the criteria covered the request; the executors did not deliver C06/C10 and dropped a spec-pinned client behavior while implementing C03.
- **Suggestions/nits:** dead `merchantName` prop, unused `phase`, stale V2 comment — fix opportunistically in the same pass, none is blocking.
- **Human checklist (C14, `verify: user`):** open a V1 and a V2 link in both locales, both themes, at 320/375/768/1440 — merchant header and theme render; malformed e-mail/CPF/CEP show the format message and the first invalid field is scrolled and focused; CPF/CEP masks and numeric keypad behave; the submit names the exact total; the form gives way to the QR and typing never wipes it; the QR scans **with the merchant mark centred** (after the repair); the poll-failure banner and "check again" appear on the first failure; the privacy modal traps and returns focus; each terminal view is distinct and the three failures offer start-over; 44px targets and no overflow at 320px. Note: there is no no-JS fallback — JS is required, recorded as a spec exclusion.
- **Summary:** the convergence is substantially delivered — one 560px branded shell for both eras, the shared controller with a real state machine and format validation, eight distinct outcomes, first-failure retry, footer and privacy modal, contracts recorded honestly with their exclusions. It falls short on three concrete points: the QR identity centre-cut is plumbed but never fed, the pinned opaque-404 treatment lost its only render site when the form started yielding, and the legacy BEM classes the criterion ordered retired are still emitted by `/pay`.

## Delta of the return

- **Type:** execucao.
- **Affected criteria:** C06 — `merchantIdentity` never passed, no centre-cut; C10 — four `checkout-*` classes still emitted and `.checkout-description` still defined; spec › **Client.** — poll-`404` unavailable treatment unreachable once a payment exists.
- **Affected fronts:** `F01` (both pages must forward the identity node; `globals.css` rule to delete) and `F03` (both adapters: identity prop, unavailable surface, class removal); `F04` corrects only the 14.6.3 checklist bullet about the surviving classes.
- **Untouched fronts:** `F02` — `checkout-payment-views.tsx` is approved as it stands (it already accepts `identity`); the spec, `DESIGN.md` and `src/checkout/AGENTS.md` amendments stay integrated, with `DESIGN.md` becoming true once the centre-cut is wired. Do not re-execute them.
- **Expected action:** in 004, wire `branding` → `merchantIdentity` into `CheckoutPaymentView` on both eras, render the opaque unavailable treatment inside the payment phase, swap the four legacy class names for the utility composition already used in this diff, delete `.checkout-description` from `globals.css`, and correct the F04 checklist line.

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=src/app/pay/[identifier]/public-checkout-form.tsx,src/app/pay/[identifier]/public-checkout-v2-form.tsx,src/app/pay/[identifier]/page.tsx,src/app/pay/[identifier]/public-checkout-v2-page.tsx,src/app/globals.css frentes=F01,F03,F04 intactas=F02 -->
