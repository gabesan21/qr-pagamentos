# Julgamento — [[14.2.5-phase-verification]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** full — phase-verification task: the surface is the whole phase and its plan declares the re-run as an `agent` criterion.
- **Superfície:** diff `6321bdfc..3d789d71` (33 files), the generated `src/app/globals.css`, `status-badge.tsx`, plan C1–C17, the 14.2.1–14.2.4 checklists, and my own `vitest run` executions (branch ×2 completed + 1 hung, base ×1).
- **Devoluções:** execução 0 de 2 · plano 0 de 2 — this round is a directed repair: no route, no counter.

### Conformidade com objetivo e specs

**Original request:** met almost everywhere — the gate ran, 17 assertions were realigned without deleting a test, 9 coverage files were added, and the six cheap repairs are in the diff. Two pieces of the "O quê" are missing: the badge families are pinned by no test at all, and the suite does not exit 0.

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C1 | Aggregate gate green | evidência | `vitest run` (3 executions) | falhou | tests are green (2080 passed / 28 skipped / 0 failed) but the process exits 1: `ReferenceError: window is not defined` in `input-otp/dist/index.mjs` via `Timeout._onTimeout`, in 2/2 completed branch runs; a third run hung and leaked a worker fork. The base (`develop`) run never emits it |
| C2 | No coverage lost | evidência | same runs + diff | passou | 2080 ≥ 2010, 28 skipped; no new `it.skip/only/todo`; the 4 removed `it(` are renames with a replacement in the same file |
| C3 | Badge tone realigned and families covered | evidência | reading the tests | falhou (partial) | realignment is done (`bg-current`; `bg-danger-soft text-danger` plus `not.toContain("bg-destructive")`); but **no test in the repository names `ProviderStateBadge`/`LocalOutcomeBadge`/`LinkLifecycleBadge`/`AccountStateBadge`/`EntityStateBadge` or the 17-state map** — `status-badge.test.tsx` holds 2 cases, both on `StatusBadge` |
| C4 | Navigation mocks completed | evidência | the four `page.test.tsx` | passou | all four mocks declare `useSearchParams: () => new URLSearchParams()` |
| C5 | Return target and locale route | evidência | reading the tests | passou | `//evil`, `\\evil`, `https://evil.example`, absent/unparseable Referer all fall back to `/`; query kept, fragment stripped; anonymous POST asserts `303` + `qr_locale` |
| C6 | Template utilities and soft projections | evidência | `globals-theme-tokens.test.ts` | passou | the 22 `@theme inline` entries resolve to a generated variable, never a literal; 4 soft roles × 6 themes |
| C7 | Theme stamping, picker and boundary | evidência | theme-preference/layout/picker/app-shell | passou | 4 stamping cases; six `menuitemradio` with a non-color marker; empty list renders `null`; the boundary assertion names `shell-theme-picker.tsx` |
| C8 | Notice-toast bridge | evidência | `notice-toast.test.tsx` + 3 `*-notices` | passou | text comes only from the registry, `replaceState` strips only its own params, each site keeps its `<noscript>` alert |
| C9 | Auth switcher rendered | evidência | login and reset-password | passou | labelled PT/EN control ≥44px posting to `/language-preference`, no principal read added |
| C10 | ImageUploader states | evidência | `image-uploader.test.tsx` | passou | six states; the open target is the native input inside its label; nothing relies on color alone |
| C11 | Numeric, identity and inventory | evidência | qr-display/stat-card/inventory | passou | M/H/`graphic` precedence; `aria-hidden` sparkline plus the constant-series edge; `owners: 20` and six `localAdditions` rejections |
| C12 | In-phase repairs applied | evidência | reading the diff | passou | all six present: `position="top-right"`, duplicate viewport removed, `hasFailed` clears `aria-busy`, empty picker returns `null`, `strikethrough` dropped, DESIGN.md says "unauthenticated public surfaces" |
| C13 | Design-system evidence regenerated | evidência | tokens + `status-badge.tsx` + report | falhou (above the phase's reach) | a real product defect, not environment: soft pills render `text-<tone>` over `bg-<tone>-soft` at 12px and four pairs land below 4.5:1 — pix-paper danger 4.21 / info 4.40 and cashier-daylight success 4.41 / warning 4.41. With no manifest, `design-system:evidence:verify` never ran |
| C14 | Theme picker in both roles | humano | — | human checklist |
| C15 | Toasts on real pages | humano | — | human checklist |
| C16 | Language switch from deep pages | humano | — | human checklist |
| C17 | Not-found accent and destructive terminals | humano | — | human checklist |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | The 17-state map and the five families delivered by 14.2.4 are pinned by no test: reverting them breaks nothing, which contradicts the plan's observable result | `src/components/ui/status-badge.test.tsx` (2 cases) against `src/components/ui/status-badge.tsx:87-165` | Cover the five families with caller-supplied `labels` and the map's 17 states in the already owned test file |
| bloqueante | `vitest run` exits 1 on the branch (2/2) and hung a third time leaking a worker; the base does not reproduce it — the very suite this phase exists to make green does not close | run output: `ReferenceError: window is not defined`, originated in `src/components/ui/input-otp.test.tsx` | Stop the `input-otp` timer from outliving the file's teardown (fake timers/unmount) — the file is inside F01's `owns` |
| bloqueante (outside `owns`) | Four soft pairs breach the AA gate the spec declares to take precedence over byte-identical rendering, and the badge is not the only consumer | `src/app/globals.css` (pix-paper and cashier-daylight blocks) · [[pop/specs/application-frontend-system\|spec]] §Accessibility | New task/modification: `k`-step projected on-soft foregrounds, generator, `@theme` and consumers — forbidden to this front by `must not edit` |
| nit | The docstring still promises "struck-through label when archived **or explicitly requested**" after `strikethrough` was removed | `src/components/ui/status-badge.tsx:29` | Drop the clause |
| nit | `designSystemToastRegion` was orphaned in both dictionaries once the duplicate viewport went away | `src/i18n/dictionaries/design-system/{en,pt-BR}.ts:123` | Follow-up: this front could not touch `src/i18n/**` |

### Escopo e integração

- [x] Changes respect F01's `Owns`/`Must not edit` — the 33 files fall inside `src/**/*.test.*`, `scripts/*.test.ts`, the five authorized sources and `DESIGN.md`.
- [x] No dependency was implemented opportunistically by a consumer.
- [x] The `verify: phase` criteria of 14.2.1–14.2.4 are consumed here (this is the checklist's task).
- [ ] Specs/DOX coherent: `DESIGN.md` is aligned, but the AA gate of [[pop/specs/application-frontend-system|application-frontend-system]] is red in the delivered code and the bound evidence stays stale — recorded as a follow-up, with no remedy available inside this task.

## Veredito

- **Decisão:** **reparo dirigido** (2 pontual patches, the round's maximum) — no kanban route and no counter.
- **Bloqueantes:** C3 (families/17 states uncovered) and C1 (exit 1 from an `input-otp` timer surviving teardown). Both name a file, have an objective remedy and sit inside F01's `owns`.
- **Defeito de plano:** none — the plan foresaw the durable-contract failure (Riscos) and routed it to a follow-up, exactly as [[WORKFLOW|WORKFLOW]] › Verificação de phase requires. C13 neither returns nor becomes a new front: the gate does not expand scope.
- **Sugestões/nits:** the `StatusPill` docstring and the orphaned i18n key. Neither blocks.
- **Checklist humana:** C14, C15, C16 and C17 as planned; **plus** the AA finding — in pix-paper and cashier-daylight the soft `danger`/`info` and `success`/`warning` pills sit between 4.21 and 4.41:1 (floor 4.5), and `/design-system` still carries no fresh 48-capture manifest since 14.2.4. Expected pass only after the on-soft foreground task lands.
- **Resumo:** the request is met in 11 of the 13 `agent` criteria; what is missing is pinning the badge families and closing the suite's exit code. The AA defect inherited from tokens kept byte-equal to the template is real, wider than reported (4 pairs, 2 themes) and belongs to a task of its own.

## Delta da devolução

- **Tipo:** `execucao`, **pontual** — directed patch, not a stage re-entry.
- **Critérios afetados:** C3 — families and the 17-state map have no test; C1 — `vitest run` exits 1 on an unhandled teardown error.
- **Frentes afetadas:** `F01`, two files only: `src/components/ui/status-badge.test.tsx` and `src/components/ui/input-otp.test.tsx`.
- **Frentes intactas:** no other front exists; **all the rest of F01 is approved** (C2, C4–C12) and must not be re-executed or re-integrated.
- **Ação esperada:** (1) cover the five families with caller-supplied `labels` and the map's 17 states; (2) neutralize the `input-otp` timer at teardown and prove `vitest run` exit 0 twice in a row. No tokens, generator, `globals.css` or `src/i18n/**`.

<!-- pop-delta round=1 kind=execucao pontual=true paths=src/components/ui/status-badge.test.tsx,src/components/ui/input-otp.test.tsx frentes=F01 intactas=none -->

## Adendo — reparo dirigido conferido (2026-09-08)

Round 1 issued `reparo-dirigido` (`pontual=true`, delta marker above); patch `77c82da5` touched only the two delta files, nothing else (`git show --stat`).
- **C3 closed:** `status-badge.test.tsx` now exercises the five families with caller `labels`, asserting the rendered tone class per state; the union covers all 17 map entries plus the archived/deleted markers, so reverting 14.2.4's map fails a test.
- **C1 closed:** `input-otp.test.tsx` flushes the uncancelled `setTimeout(0/10/50)` in `afterEach` after `cleanup()`; my own re-run gives `vitest run` exit 0, 241 files, 2104 passed / 28 skipped, no unhandled error.
- **Unchanged:** F01's already-approved surface, C13 and the human checklist — the AA finding is handed to [[14.3.3-project-aa-soft-tone-foregrounds]], and C14–C17 plus the AA item stay with the human.
- **Decisão final:** aprovada — gate encerrado.

<!-- pop-verdict round=1 decision=aprovada -->
