# Julgamento — [[14.3.3-project-aa-soft-tone-foregrounds]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — yolo não crítica, rodada 1, frente única (F01) integrada em `a8d48d8e`; superfície = os 15 paths do diff.
- **Superfície:** tokens DTCG (8 JSON), `generate-design-tokens.mjs`, região `@theme inline` de `globals.css`, dois consumidores, spec/`DESIGN.md`/DOX de `src/components/ui`. Riscos cobertos: derivação errada, primitivo de auditoria tocado, consumidor esquecido, hex cru.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

Pedido do card ("O quê/Por quê"): atendido — os quatro pares medidos em 4.21/4.40/4.41/4.41 e o tile accent (2.170 em pix-paper) passam a usar foreground projetado; o rerun da evidência é `verify: phase`.

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C1 | Camada de tokens completa | evidência | leitura dos 8 JSON | passou | 4 `feedback.<tone>.soft-foreground` deixam de aliasar `text-primary` (`semantic.tokens.json:69,84,99,114`), `action.soft-foreground` criado (`:53`); 6 temas × 5 aliases; 30 primitivos com hex==components e `interpolationStep` |
| C2 | Derivação pelo método `k` | evidência | re-derivei os 30 com `deriveAccessibleProjection(audit tone, text.primary, [soft], 4.5)` | passou | 0 divergências de hex e de `k`; razões 4.505–9.045, mínimo `vault-blue/action` 4.505; nenhum valor colapsa em `text.primary` |
| C3 | Gerador projeta, não calcula | evidência | `generate-design-tokens.mjs --check` e `check-design-tokens.mjs` (rodados por mim) | passou | ambos exit 0; 5 entradas novas em `THEME_PROJECTION` (`scripts/generate-design-tokens.mjs:105,109,111,113,115`), nenhuma matemática de cor |
| C4 | Utilities ligadas a variáveis | evidência | leitura de `globals.css:1265+` | passou | `--color-{success,warning,danger,info}-on-soft` e `--color-accent-on-soft` em `globals.css:1324-1328`, todas `var(...)`, fora da região gerada (fim em `:1255`) |
| C5 | Todo consumidor text-on-soft trocado | evidência | leitura + grep `-soft` em `src/**/*.tsx` | passou | `status-badge.tsx:10-14` e `image-uploader.tsx:226`; restantes `bg-accent-soft` (`monogram.tsx:27`, `field.tsx:106`, `button.tsx:13-15`) usam texto primário/secundário (5.95–12.8:1), nenhum `text-<tone>` sobre `bg-<tone>-soft` |
| C6 | Primitivos de auditoria byte-equal | evidência | comparação da subárvore `color.primitive.audit` contra o merge-base | passou | subárvore idêntica; diff do arquivo tem 150 inserções e 0 remoções; linha SHA `762edf36…` intacta (`pop/specs/application-frontend-system.md:34`) |
| C7 | Contratos duráveis atualizados | evidência | leitura de spec, `DESIGN.md`, `src/components/ui/AGENTS.md` | passou | tabela 6×5 da spec (`:55-88`) confere hex/`k`/razão com minha re-derivação nos 30; `DESIGN.md:134-148` espelha e lista as 5 utilities; `src/components/ui/AGENTS.md:18-21` proíbe `text-<tone>` sobre `bg-<tone>-soft` |
| C8 | Fonte limpa | evidência | registro do 004 (`tsc --noEmit` 0, `eslint` 0 erros) | passou | não re-executado aqui (critério comum já evidenciado); `check-design-tokens` exit 0 comprova ausência de hex cru em `src/components/ui` e na região manual de `globals.css` |
| C9 | Derivação fixada por teste | phase | `scripts/design-tokens.test.ts` em `14.3.4` | registrado (phase) | invalidado por desenho: precisa pinar os 30 primitivos |
| C10 | Inventário de utilities e classes do badge | phase | `globals-theme-tokens.test.ts:51` (22→27) e `status-badge.test.tsx:16,19` | registrado (phase) | mapa local do teste ainda diz `text-<tone>`; nenhum arquivo de teste foi tocado nesta task |
| C11 | Evidência renderizada regerada | phase | `design-system:evidence(+:verify)` em `14.3.4` | registrado (phase) | evidência ligada segue stale desde 14.2.5 |
| C12 | Os quatro pares reportados leem bem | humano | checklist humana | humano | ver checklist |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | A spec passou de 120 para 154 linhas e estoura o teto de 150 do `pop_validate` (`MAX_NOTE_LINES`) | `pop/specs/application-frontend-system.md:1` | compactar a tabela de 30 linhas na forma de 6 linhas por tema já usada em `DESIGN.md:143-148` (manutenção de harness, direta, sem card) |
| nit | 6 chaves novas indentadas com 20 espaços em vez de 10; nenhum formatter cobre JSON no repo | `src/design-system/tokens/themes.tokens.json:1841,1920,1999,2078,2157,2236` | normalizar a indentação junto do próximo toque no arquivo |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de F01 — nenhum `*.test.*`, `tests/**`, `artifacts/**`, `src/data-directory/**`, rota de 14.3.2 ou `color.primitive.audit.**` tocado.
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [x] Critérios `verify: phase` (C9–C11) registrados aqui para a checklist de `14.3.4-phase-verification`.
- [x] Specs, documentação e contratos DOX afetados estão coerentes (a tabela da spec bate valor a valor com a re-derivação).

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** teto de linhas da spec; indentação dos 6 blocos JSON. Follow-up fora do pedido: em `image-uploader.tsx:226-227`, `failed` + `dragOver` compõe `bg-accent-soft` com `text-danger` (tw-merge mantém `text-danger`) — 4.420 em `midnight-clearing` e 4.359 em `vault-blue`; condição pré-existente ao diff, não regressão, candidata a task própria.
- **Checklist humana:** C12 — abrir `/design-system` e conferir, a 12px, que as pills `danger`/`info` em `pix-paper` e `success`/`warning` em `cashier-daylight` estão legíveis e ainda distinguíveis entre si e do texto primário; conferir também o tile de drag-over do `ImageUploader` em `pix-paper` (accent-soft agora com `text-accent-on-soft`). Pass esperado: rótulo tonal legível, sem colapso para texto primário.
- **Resumo:** o pedido era dar a toda superfície soft um foreground AA derivado pelo método `k`; o entregue projeta 30 primitivos (5 papéis × 6 temas), repointa os semânticos, adiciona 5 projeções ao gerador e 5 utilities `text-*-on-soft`, troca os dois consumidores tonais e transcreve o método e a tabela na spec, no `DESIGN.md` e no DOX — sem tocar um único primitivo de auditoria. Re-derivei os 30 valores de forma independente: batem todos.

<!-- pop-verdict round=1 decision=aprovada -->
