# Front F03 — Runtime visual evidence — [[12.2.1-rebuild-template-tokens-and-typography]]

- **Delivery:** fresh, verifier-bound browser proof for the installed theme and typography foundation.
- **Scope:** enhance and run existing `/design-system` evidence without changing the specimen/page or shared components.
- **Owner:** agent.
- **Owns:** `tests/design-system.evidence.spec.ts`, `scripts/verify-design-system-evidence.mjs`, `artifacts/design-system/**` (new run and current pointer only).
- **May read:** F01/F02 output, existing specimen, Playwright config, prior evidence format, `DESIGN.md`, application frontend spec.
- **Must not edit:** application/CSS/token/font source, package/lock, other evidence families, prior immutable evidence runs, specs and task files.
- **Depends on:** F02 initially; F04 and F05 before the current retry.
- **Expected input:** current generated CSS, passing focused token/font gates and locally available production fonts.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]].
- **Criteria:** C06, C07.

## Execution contract

- Assert computed Inter body, Sora heading/tracking and IBM Plex Mono numeric role after `document.fonts.ready`, with exact admitted weights and no fallback result.
- Bind all six themes at 320/375/768/1440 to computed semantic color, tertiary, radius, focus, elevation and reduced-motion values; keep independent overflow, target, keyboard, axe, console and external-request assertions.
- Produce exactly 24 fresh PNGs plus hashed assertions/manifest/current pointer, inspect them, and bind a review with no unresolved severity 2+.
- Do not rebuild the component specimen or claim bilingual/state completeness owned by 12.2.4.
- Missing runtime/font or failed objective/visual assertion is `BLOCKED`; do not weaken the verifier.
- Deliver only this scope; report run ID/files/evidence without staging or committing.

## Reentry contract

- Retry only after F04 and the canonical F05 parity sync are complete. Keep this front's original evidence-only ownership and verify the real F04 ID/money consumers; do not add test-only DOM, page/CSS/parity edits, or specimen structure.
- The Result below records the first attempt and remains historical evidence; replace it only with the retry outcome after executing the unchanged C06/C07 gate plus C08 computed-style assertions.

## Result

- **Status:** COMPLETED — retry closed the unchanged C06/C07/C08 evidence gate with a fresh 6-theme × 4-viewport run after F04 numeric consumers and F05 parity sync.
- **Commit/artifact:** task commit `3b410d7`; `artifacts/design-system/current.json` points atomically to `20260803010240` (`beaed702d5891a707c4a63246bc3672f0b5154c8a6d6aa28db1b17c06a0d4c99`).
- **Files changed:** F03-owned run `artifacts/design-system/20260803010240/` (24 PNGs, assertions, manifest, review) and current pointer. No application, CSS, token, font, component, parity, package, spec, prior-run, or task-branch workflow file changed.
- **Evidence:** `pnpm design-system:evidence:verify`, focused ESLint, scope and `git diff --check` passed. Production build passed (51/51 static pages) against an isolated disposable PostgreSQL using the repository's pinned image, bootstrap and 35 migrations. The run proves computed Inter 14/20, Sora heading/-0.02em, all admitted local faces, and four real PIX/BRL IBM Plex Mono tabular-number consumers; every cell has no overflow, serious/critical axe finding, console/page error, or external request, with full/reduced-motion, keyboard-focus, target, token, fallback, and contrast assertions closed.
- **Visual review:** all six themes were inspected at 320px and 1440px; the full 24-capture matrix is manifest-bound. Compact and desktop compositions remain consistent and legible, with no unresolved severity 2–4 finding.
- **Attempt history:** first attempt exposed absent real numeric consumers; the second exposed destructive-text contrast; the third lacked runtime DB configuration. F04, the F02 contrast delta, F05 and isolated official test infrastructure resolved those prerequisites; this is the first valid published run.

## Reentry r1 result

- **Status:** completed at task commit `5d2efe0`; current run `20260803015559` is bound by manifest `1a2fe2409352a2ac74138427ca8991127a76a585506b52fcc6babc0c918185ca`.
- **Delivery:** the browser now composites the computed semantic focus ring against page, raised and secondary surfaces, requires every ratio at `>=3:1`, and proves each focused control's actual outline or ring color equals that semantic token. The run contains exactly 24 PNGs plus assertions, manifest and review; no unresolved severity 2–4 finding remains.
- **Evidence:** production build 51/51, Playwright 2/2, evidence verifier, focused ESLint, diff-check and the 30-path F03 allowlist passed. The minimum runtime ratio is `3.006:1`; all six themes were visually inspected at 320px and 1440px. F01/F04/F05, token/CSS sources and prior immutable runs remained intact.
