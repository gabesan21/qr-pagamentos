# Verificação — [[14.6.3-phase-verification]]

- **Etapa:** 005_closing · **Papel:** `pop-judge-dredd` · **Rodada:** 1 · **Modo:** full (rodada inicial)
- **Julgado:** `task/14.6.3-phase-verification` @ `1be49893` (`ed871d4e`, `d3500138`, `1be49893` sobre `develop`), card, plano, fatia `g01`.

## Contagens confirmadas (rodadas por mim, binários diretos, sem Docker, sem servidor)

- `vitest run`: **2161 passed | 43 failed | 28 skipped** (13 files failed | 245 passed | 9 skipped), exit 1.
- Os 13 arquivos vermelhos: `(merchant)/links/{new,page,v2/[id]/edit,v2/[id]/orders}`, `(merchant)/{page,settings}`, `(merchant)/orders/v2/[id]`, `nautt-credential-surface`, `profile/profile-management`, `storefront-preview`, `storefront-settings-management`, `auth/payment-link-v2-prefill`, `orders/merchant-analytics`. **Nenhum** sob `src/app/pay/**`, `src/app/store/**`, `src/checkout/**` ou `tests/**`.
- `tsc --noEmit`: exit 2, exatamente os dois erros conhecidos (`nautt-credential-surface.test.tsx:13`, `payment-link-v2-view.test.ts:61`), ambos arquivos de teste.
- `eslint .`: **0 errors / 62 warnings**, exit 0. Diff não-teste contra `develop`: **vazio** (confirmado).
- `verify-design-system-evidence.mjs`: exit 0 — `20260909010850 captures=48 coverage=20/101`; `current.json` aponta para ele; `gitHead 8d35a376` é ancestral de HEAD. `pop_validate.py`: 32 violações, **todas** legadas de Epoch 12/`M-7.1`, nenhuma desta task.

## C01 — realinhamento sem enfraquecer · **passa**

`public-checkout-presentation.test.ts` continua em `toEqual` (forma fechada) e ganhou o membro `branding` mais um caso novo de fallback de tema, ambos lidos de `public-checkout-presentation.ts:36-43,62-66`. `page.test.tsx` só alimentou fixtures e **acrescentou** asserção. Zero `.skip`/`.todo` no delta. Nenhum caso removido.

## C02 — regressão devida · **falha parcial (bloqueante)**

Cobertos e bons: máscaras/e-mail/CPF (dígitos verificadores)/CEP, oito composições de desfecho com refunded neutro, banner de poll só em `pollFailed`, shell 560px com precedência logo→`Monogram`→fallback, rodapé com `LanguageSwitcher`, total exato no submit. **Não cobertos, embora nomeados no plano e na fatia:**

1. **`startOver` nunca é exercido.** Nenhum teste clica `checkoutStartOver` nem compara duas `idempotencyKey`. O caso `checkout-experience.test.tsx:63` **promete no nome** ("resets to the form and re-keys the idempotency key only on explicit start-over") e afirma apenas que um `404` de `/checkout` preserva o valor do campo — nome que excede a asserção é exatamente o que `clean-code-review` proíbe. `checkout-experience.tsx:210-221` (o único reset, com re-chaveamento implícito por `setAttempt(null)`) fica sem pino.
2. **`inputMode="numeric"`** (`public-checkout-form.tsx:108,112`) não tem asserção alguma.
3. **Foco no primeiro campo inválido** (`checkout-experience.tsx:66-72,173`) não tem asserção alguma.

## C03 — pinos dos defeitos julgados em 14.6.1 · **falha (bloqueante)**

Previsão teste×código feita lendo as fontes; dois dos três pinos **não ficam vermelhos sem o reparo**:

- **(a) centre-cut alimentado pelas duas páginas `/pay` — não pinado.** `checkout-payment-views.test.tsx:64` passa `merchantIdentity` como prop explícita: pina só `checkout-payment-views.tsx:155`, não a fiação. As asserções `/media/logo-…` de `page.test.tsx:41,99,168` são satisfeitas **sozinhas** pelo header do shell (`checkout-shell.tsx:17-23`, provado por `checkout-shell.test.tsx:33` com a mesma fixture), e `renderToStaticMarkup` de um server component nunca alcança a fase de pagamento. Apagar `merchantIdentity={merchantIdentityMark(...)}` de `page.tsx:57` **e** de `public-checkout-v2-page.tsx:85` deixa a suíte inteira verde — o defeito reabre em silêncio, e nada toca a página V2.
- **(b) sítio de renderização do indisponível — pinado, válido.** `checkout-experience.test.tsx:78-92` dirige `/checkout`→CREATED+capability e poll→`404` e exige `checkoutUnavailableHeading`; sem o ramo `unavailable ? <Alert…>` que `508e21d8` inseriu em `public-checkout-form.tsx:137-141`, fica vermelho. Cobre só a era V1 (nit).
- **(c) nenhuma classe `checkout-*` sob `/pay/**` — não pinado.** As classes aposentadas (`checkout-card`, `checkout-form`, `checkout-payment`, `checkout-description`) eram emitidas pelos **dois formulários** (`508e21d8`), nunca por `CheckoutPaymentView` — que é o único componente que o pino renderiza. Reverter os `className` dos forms mantém o pino verde. O grep de C01 é uma corrida única, não regressão durável. (Nota para quem consertar: o regex `class="[^"]*\bcheckout-[a-z-]+` casa `max-w-[var(--checkout-max)]` do shell — estenda por lista de classes aposentadas, não pelo prefixo.)

## C04/C09 — reparo confinado · **passa**

Diff não-teste vazio; `artifacts/design-system/**` escrito só pelo script. Nada reintroduzido do que o DTO excluiu.

## C05 — gate agregado e contabilidade da corrida paralela · **qualified pass, com ressalva**

**Aceito** que a phase feche com as 43 falhas presentes: verifiquei por histórico que elas nascem de fontes tocadas por 14.5.3 (`storefront-preview.tsx` e `storefront-settings-management.tsx` em `ef837284`/`c26b74c4`; o resto merchant/admin), e Phase 14.6 só tocou `src/app/pay/**`, `src/app/store/[slug]/**`, `public-checkout-presentation.ts`, dicionários de checkout e `globals.css` — nenhum consumido pelos 13 arquivos vermelhos. 14.5.4 está de volta em 004 realinhando exatamente esses arquivos, e 14.7.2 C05/C13 refaz o gate agregado no `develop` integrado. Exigir aqui violaria o `must not edit` desta fatia. **Mas o registro precisa ser honesto:** C05 **não fecha como escrita** (exits 1 e 2; 2161 < piso 2185) — fecha como "verde dentro do alcance da Phase 14.6", com o piso numérico deferido a 14.7.2. E a aritmética da fatia ("2161 + the fixed 4 = above floor") está **errada**: 2165 < 2185; o número que sustenta o argumento é 2161+43 = 2204 não-skipped.

## C06/C07/C08/C10 — **passam**

Evidência local regenerada após o último commit que toca fonte ligada e verificada por mim offline; três pares Docker declarados desatualizados e não executados; critérios carregados de 14.6.1/14.6.2 respondidos com a corrida que os fechou; `.checkout-*`/`.storefront-*` sumiram de `globals.css` e `.receipt-rail*` sobrevive com consumidor vivo (`design-system/page.tsx:227`), como 14.6.2 registrou e 14.7.1 herda. Nenhuma spec precisou mover.

## C11 — checklist humana · **incompleta (não bloqueante isolada)**

Cobre integralmente o handoff `06-checklist` de 14.6.1. De C10 de 14.6.2 falta um item: **foco visível** nas rotas públicas ("visible focus", entrada `05-checklist`) — "foco no primeiro campo inválido" e "foco de modal" são outra coisa.

## Veredito

Devolvida a **004**. O que a task existe para entregar — a regressão devida e os pinos anti-reabertura — está parcialmente ausente, e dois dos três pinos afirmam em memória uma proteção que o código não tem. São mais de dois ajustes pontuais em três arquivos, logo não cabe reparo dirigido nesta rodada. Fora isso a entrega é sólida: realinhamento fiel, zero mudança silenciosa de comportamento, evidência regenerada e verificada, contabilidade paralela defensável.

**Delta a reparar (só isto; o resto da superfície fica intacto e não será re-revisado):**

1. Pinar a fiação `merchantIdentity` das **duas** páginas `/pay` até o slot de identidade (C03.a) — page/V2-page → form → `CheckoutPaymentView`.
2. Estender o pino de classe aposentada para as composições que as emitiam (`public-checkout-form.tsx`, `public-checkout-v2-form.tsx`), por lista de classes, não por prefixo (C03.c).
3. Cobrir `startOver` (reset + re-chaveamento da idempotency key) e alinhar o nome do caso `checkout-experience.test.tsx:63` à asserção; cobrir `inputMode` numérico e foco no primeiro campo inválido (C02).
4. Corrigir a aritmética de C05 na fatia e declarar C05 como qualified pass com o piso deferido a 14.7.2.
5. Acrescentar "foco visível" à checklist consolidada (C11).

<!-- pop-verdict round=1 decision=execucao -->
<!-- pop-delta round=1 kind=execucao pontual=false paths=src/app/pay/[identifier]/checkout-payment-views.test.tsx,src/app/pay/[identifier]/checkout-experience.test.tsx,src/app/pay/[identifier]/page.test.tsx,pop/kanban/005_closing/14.6.3-phase-verification/subtasks/14.6.3-phase-verification.g01-phase-gate.md frentes=F01 intactas= -->
