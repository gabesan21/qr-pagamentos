# Verificação — [[14.6.3-phase-verification]]

- **Etapa:** 005_closing · **Papel:** `pop-judge-dredd` · **Rodadas:** 1 (full) e 2 (differential).

# Rodada 1 — modo full · devolvida a 004

**Julgado:** `1be49893` (`ed871d4e`, `d3500138`, `1be49893` sobre `develop`), card, plano, fatia `g01`.

**Contagens que rodei (binários diretos, sem Docker, sem servidor):** `vitest run` **2161 passed | 43 failed | 28 skipped** (13 files failed | 245 passed | 9 skipped), exit 1; os 13 vermelhos são merchant/admin — `(merchant)/links/{new,page,v2/[id]/edit,v2/[id]/orders}`, `(merchant)/{page,settings}`, `(merchant)/orders/v2/[id]`, `nautt-credential-surface`, `profile/profile-management`, `storefront-preview`, `storefront-settings-management`, `auth/payment-link-v2-prefill`, `orders/merchant-analytics` — **nenhum** sob `src/app/pay/**`, `src/app/store/**`, `src/checkout/**` ou `tests/**`. `tsc --noEmit` exit 2, exatamente os dois erros conhecidos (`nautt-credential-surface.test.tsx:13`, `payment-link-v2-view.test.ts:61`), ambos arquivos de teste. `eslint .` **0 errors / 62 warnings**. Diff não-teste contra `develop`: **vazio**. `verify-design-system-evidence.mjs` exit 0 — `20260909010850 captures=48 coverage=20/101`, `current.json` apontando para ele, `gitHead 8d35a376` ancestral de HEAD. `pop_validate.py`: 32 violações, todas legadas de Epoch 12/`M-7.1`, nenhuma desta task.

**C01 passa.** `public-checkout-presentation.test.ts` segue em `toEqual` (forma fechada), ganhou o membro `branding` e um caso novo de fallback de tema, ambos lidos de `public-checkout-presentation.ts:36-43,62-66`; `page.test.tsx` só alimentou fixtures e **acrescentou** asserção. Zero `.skip`/`.todo`, nenhum caso removido.

**C02 falhou (bloqueante).** Bons: máscaras/e-mail/CPF/CEP, oito desfechos com refunded neutro, banner de poll só em `pollFailed`, shell 560px com precedência logo→`Monogram`→fallback, rodapé com `LanguageSwitcher`, total exato no submit. Ausentes, embora nomeados: (1) `startOver` nunca exercido — nenhum clique, nenhuma comparação de `idempotencyKey`, e `checkout-experience.test.tsx:63` **prometia no nome** o re-chaveamento afirmando só que um `404` preserva o campo; (2) `inputMode="numeric"` (`public-checkout-form.tsx:108,112`) sem asserção; (3) foco no primeiro campo inválido (`checkout-experience.tsx:66-72,173`) sem asserção.

**C03 falhou (bloqueante).** (a) **não pinado** — `checkout-payment-views.test.tsx:64` passa `merchantIdentity` como prop explícita, pinando só `checkout-payment-views.tsx:155`; as asserções `/media/logo-…` de `page.test.tsx:41,99,168` são satisfeitas sozinhas pelo header do shell (`checkout-shell.tsx:17-23`, provado por `checkout-shell.test.tsx:33`) e `renderToStaticMarkup` de server component nunca alcança a fase de pagamento — apagar a fiação em `page.tsx:57` e `public-checkout-v2-page.tsx:85` deixava tudo verde. (b) **válido** — `checkout-experience.test.tsx:78-92` fica vermelho sem o ramo `unavailable` que `508e21d8` inseriu em `public-checkout-form.tsx:137-141`. (c) **não pinado** — as classes aposentadas eram emitidas pelos **dois formulários**, nunca por `CheckoutPaymentView`, único componente que o pino renderizava; e o regex por prefixo casaria `max-w-[var(--checkout-max)]`.

**C04/C09 passam:** diff não-teste vazio, `artifacts/**` escrito só pelo script, nada reintroduzido do que o DTO excluiu. **C06/C07/C08/C10 passam:** evidência local regenerada após o último commit que toca fonte ligada e verificada offline; três pares Docker declarados desatualizados e não executados; critérios carregados de 14.6.1/14.6.2 respondidos; `.checkout-*`/`.storefront-*` sumiram de `globals.css` e `.receipt-rail*` sobrevive com consumidor vivo (`design-system/page.tsx:227`), como 14.7.1 herda; nenhuma spec precisou mover. **C05 aceito com ressalva** (ver rodada 2). **C11 incompleta:** faltava "foco visível" do C10 de 14.6.2.

**Delta prescrito:** (1) pinar a fiação `merchantIdentity` das duas páginas `/pay`; (2) mover o pino de classe aposentada para os emissores, por lista e não por prefixo; (3) cobrir `startOver` (reset + re-chaveamento), renomear o caso que prometia demais, cobrir `inputMode` e foco no primeiro inválido; (4) corrigir a aritmética de C05 e declarar o qualified pass; (5) acrescentar "foco visível" à checklist.

Marcadores da rodada 1, rebaixados a texto pela rodada 2: `pop-verdict round=1 decision=execucao`; `pop-delta round=1 kind=execucao pontual=false paths=src/app/pay/[identifier]/checkout-payment-views.test.tsx,src/app/pay/[identifier]/checkout-experience.test.tsx,src/app/pay/[identifier]/page.test.tsx,pop/kanban/005_closing/14.6.3-phase-verification/subtasks/14.6.3-phase-verification.g01-phase-gate.md frentes=F01 intactas=`.

# Rodada 2 — modo differential (só o delta `713a4fcd`)

**Escopo do delta:** 4 arquivos, **todos de teste**, todos em `src/app/pay/[identifier]/` — `page.identity-wiring.test.tsx` (novo), `checkout-experience.test.tsx`, `public-checkout-form.test.tsx`, `public-checkout-v2-form.test.tsx`. Diff não-teste contra `develop` segue **vazio**; **nenhum arquivo de 14.5.4 tocado**. A superfície aprovada na rodada 1 fica preservada e não foi re-revisada.

**Contagens que rodei nesta rodada:** `vitest run` **2169 passed | 43 failed | 28 skipped** (246 files passed | 13 failed | 9 skipped) — os 13 vermelhos são **exatamente a mesma lista**, todos 14.5.4-owned, nenhum no alcance da phase. Os 8 casos novos batem com a aritmética (2161 + 8 = 2169). `tsc` e `eslint` inalterados.

**C03.a reparado.** `page.identity-wiring.test.tsx` mocka `./public-checkout-form` e `./public-checkout-v2-form`, captura a prop `merchantIdentity`, exige `not.toBeUndefined()` e renderiza o nó capturado até `/media/logo-…`. O header do shell **não pode** satisfazer isso — a asserção lê a prop, não o markup. Apagar `page.tsx:57` ou `public-checkout-v2-page.tsx:85` deixa `captured*.current` `undefined` → vermelho, cada era no seu caso.

**C03.c reparado.** O pino saiu de `CheckoutPaymentView` e foi para os emissores reais: os dois formulários na fase de formulário e `checkout-experience.test.tsx` na fase de pagamento, com lista exata `checkout-(card|form|payment|description)` em vez de prefixo — o falso-positivo do token `--checkout-max` fica impossível, e reverter qualquer `className` de `508e21d8` fica vermelho.

**C02 reparado.** `startOver` é exercido de verdade: submit → poll `REJECTED` → clique em `checkoutStartOver` → volta ao rótulo de total, campo limpo, e o terceiro fetch carrega `idempotencyKey` distinta da primeira (sem `setAttempt(null)` em `checkout-experience.tsx:214` a chave se repetiria). Caso renomeado para o que afirma. `inputMode="numeric"` pinado em CPF/CEP com controle negativo em `name`; foco no primeiro inválido pinado por ordem de campo via `document.activeElement`, com `scrollIntoView` stubado e a limitação do jsdom declarada no teste.

**C05 qualified pass, agora registrado com honestidade.** A fatia declara que C05 não fecha como escrita (exits 1 e 2) e fecha como "verde dentro do alcance da Phase 14.6"; a aritmética virou 2169 + 43 = **2212 ≥ 2185**, com o erro da rodada 1 nomeado em vez de apagado, e o piso numérico deferido a 14.7.2 C05/C13. Contabilidade paralela **aceita**: verifiquei por histórico que a vermelhidão nasce de fontes de 14.5.3 (`ef837284`, `c26b74c4`) e que Phase 14.6 tocou só `src/app/pay/**`, `src/app/store/[slug]/**`, `public-checkout-presentation.ts`, dicionários de checkout e `globals.css` — nada que os 13 arquivos consomem. 14.5.4 está em 004 realinhando exatamente esses arquivos; exigir o conserto aqui violaria o `must not edit` da fatia.

**C11 reparado.** "Visible focus" entrou creditado ao C10 de 14.6.2; a lista funde os dois handoffs por inteiro.

**Veredito: aprovada.** O delta corrige exatamente o que foi devolvido, sem tocar fonte, sem enfraquecer nada e sem invadir a frente de 14.5.4. Os três pinos julgados agora ficam vermelhos sem o reparo, a regressão devida está completa, a evidência local segue verificada e o gate fecha com ressalva nomeada em vez de número maquiado. Nenhum achado de severidade ≥2 em aberto.

<!-- pop-verdict round=2 decision=aprovada -->
