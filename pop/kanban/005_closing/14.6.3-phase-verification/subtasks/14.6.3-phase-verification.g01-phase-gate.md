# Frente F01 — Phase 14.6 gate — [[14.6.3-phase-verification]]

- **Entrega:** the Phase 14.6 verification pass — suite realignment, the owed regression (including the three defects judged in 14.6.1), in-reach repairs, the design-system evidence refresh, the aggregated gate result and the consolidated human checklist.
- **Escopo:** tests and verification. Non-test source changes only where the suite proves a defect inside the phase's reach (`src/app/pay/[identifier]/**`, `src/app/store/[slug]/*`, `src/app/store/[slug]/pay/**`, `src/checkout/public-checkout*presentation*`).
- **Responsável:** agent · **Papel:** `pop-phase-verifier`.
- **Paths de entrada:** o card, esta fatia, o [[14.6.3-phase-verification.plan|plano]] inteiro; `pop/memory/2026-09-08/14.6.1-converge-public-checkout.md` e suas seis entradas (`06-checklist` = lista de suítes falsificadas e checklist humana; `05-repair` = os três bloqueios julgados; `04-contracts` = as exclusões por DTO); `pop/kanban/**/14.6.2-converge-storefront-and-standalone-pay/` (plano + as duas fatias, com o `Resultado` de `g01` preenchido e o de `g02` a conferir no momento da corrida); o diff integrado das duas tasks; para o piso de suíte, o `Resultado` de `pop/kanban/**/14.5.4-phase-verification/subtasks/*.g01-phase-gate.md` ou o ledger `pop/memory/**/14.5.4-phase-verification.md`.
- **Owns:** `src/app/pay/[identifier]/**/*.test.ts(x)`, `src/app/store/[slug]/**/*.test.ts(x)`, `src/checkout/public-checkout*presentation*.test.ts`, as novas suítes de regressão nessas áreas, os arquivos não-teste **listados como reparo**, e `artifacts/design-system/**` (escrito só pelo script).
- **May read:** todo o diff integrado da phase, `src/app/pay/**`, `src/app/store/**`, `src/checkout/**`, `src/storefront/**`, `src/components/ui/**`, `src/i18n/dictionaries/{checkout,storefront}/**`, `src/app/globals.css`, `pop/specs/**`, `DESIGN.md`, `scripts/**`, `docs/template/app/src/**`, `researches/template-fidelity-convergence/**`.
- **Must not edit:** `prisma/**`, qualquer `route.ts`, `src/components/ui/**` e `inventory.json`, `src/data-directory/**`, `src/media/**`, `src/integrations/**`, `scripts/**`, `src/i18n/dictionaries/**`, superfícies merchant/admin (salvo o reparo ser o próprio defeito ali, sem quebrar o DOX), cards e planos de outras tasks, `pop/roadmap/**`, `compose*.yaml`, `container/**`, `install/**`.
- **Depends on:** 14.6.1 e 14.6.2 (F01 **e** F02) integradas em `develop`. **Entrada esperada:** o diff integrado das duas; se `g02` não estiver integrada, ou a entrada for incompatível → `BLOCKED`, nunca verificar meia phase.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao escrever teste e ao decidir reparo vs follow-up*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao realinhar asserção de composição*; [[.agents/skills/clean-code-review/SKILL|clean-code-review]] · [[.agents/skills/ui-review/SKILL|ui-review]] — *use ao classificar severidade do que a suíte pegar*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** diff nos paths de `Owns` + relatório com o resultado agregado dos quatro gates, a lista de reparos com `file:line`, a lista de suítes realinhadas e novas, o estado da evidência (local regenerada, três runs Docker declaradas desatualizadas) e a checklist humana consolidada; status `concluída | BLOCKED`.
- **Critérios:** C01–C11 do [[14.6.3-phase-verification.plan|plano]].

## Contrato de execução

- **Comandos, sempre binários diretos a partir do worktree — nunca `pnpm <script>`, nunca container:** `./node_modules/.bin/tsc --noEmit`; `node scripts/generate-design-tokens.mjs --check && node scripts/generate-brand-assets.mjs --check && node scripts/check-font-provenance.mjs && node scripts/check-design-tokens.mjs && ./node_modules/.bin/eslint .`; `./node_modules/.bin/vitest run`; `./node_modules/.bin/next build`.
- **Ordem:** primeiro uma corrida completa de `vitest run` para transformar a lista-piso em conjunto real de falhas; depois realinhar; depois adicionar regressão; só então reparar o que continuar vermelho. Nunca use um reparo para salvar uma asserção obsoleta, nem uma asserção para esconder um defeito.
- **Piso das suítes falsificadas** (a corrida e o diff podem acrescentar, não subtrair): `pay/[identifier]/public-checkout-form`, `pay/[identifier]/public-checkout-v2-form` (markup migrou para `CheckoutPaymentView`/`CheckoutShell`), `pay/[identifier]/page`, `src/checkout/public-checkout-presentation` (membro aditivo `branding` na V1) e seu gêmeo V2 se a corrida o falsificar, `store/[slug]/page` e `store/[slug]/storefront-experience` (F01 recompôs a experiência e só realinhou `page.test.tsx`), `store/[slug]/pay/page` e `store/[slug]/pay/standalone-payment-experience` (F02, cujo `Resultado` pode estar vazio — descubra pelo diff, não presuma). Some a isso qualquer suíte que ainda asserte classe `storefront-*`/`receipt-rail*`/`.checkout-*` aposentada ou a forma antiga de `public-checkout-presentation`; prove com busca por nome de classe em `src/**`.
- **Regressão devida, sem tela nova:** máquina de estado compartilhada (`checkout-experience.tsx`: edição de campo não descarta tentativa/pagamento/capability; só o start-over explícito reseta e re-chaveia a idempotency key), validação e máscaras (`checkout-form-validation.ts`: e-mail, dígitos verificadores de CPF idênticos ao servidor, CEP, `inputMode` numérico, foco no primeiro campo inválido), total exato nomeado no submit, fase de pagamento que o formulário cede e as oito views de desfecho (`checkout-payment-views.tsx`, refunded neutro), shell 560px com precedência logo→`Monogram`→fallback e rodapé com `LanguageSwitcher` (`checkout-shell.tsx`/`checkout-footer.tsx`), vocabulário `EmptyState` no storefront e reuso das views pelo avulso com body de submit e desfecho opaco inalterados.
- **Regressão dos defeitos julgados em 14.6.1** (entrada `05-repair`): (a) ambas as páginas `/pay` alimentam `merchantIdentity`, então o centre-cut renderiza; (b) a view de indisponível tem sítio de renderização alcançável depois do `404` do polling; (c) nenhuma composição sob `/pay/**` emite nome de classe `checkout-*`.
- **Não enfraquecer:** nenhum `.skip`/`.todo` novo; caso removido precisa de recobertura nomeada; asserção nova lida da fonte entregue, não do teste antigo. Não reintroduza o que 14.6.1 excluiu por contrato de DTO (countdown de expiração, selo de uso único, referência de pedido).
- **Piso numérico:** leia o `Resultado` da fatia de 14.5.4 ou seu ledger de memória no início da corrida e use os números reportados como piso; se nenhum dos dois estiver preenchido, use 2185 passando / 28 skipped e lint 0. Passando só cresce; skipped não.
- **Evidência local:** só o par design-system, **depois** do último commit que toca fonte ligada ao manifesto (`src/app/globals.css` mudou em 14.6.1 e 14.6.2). Rode `./node_modules/.bin/next build`, suba o servidor com `./node_modules/.bin/next start --hostname 127.0.0.1 --port 4319`, exporte `ADMIN_EVIDENCE_BASE_URL=http://127.0.0.1:4319` para o Playwright não disparar o `webServer` que chama `pnpm start`, e rode `./node_modules/.bin/playwright test design-system.evidence.spec.ts --project=chromium --workers=1`; depois `node scripts/verify-design-system-evidence.mjs` e `node scripts/verify-login-evidence.mjs`. Derrube o servidor ao final.
- **Evidência com Docker é do usuário:** `checkout`, `storefront` e `standalone-payment` passam por `run-admin-evidence.mjs`, que sobe `docker compose` (`scripts/run-admin-evidence.mjs:75,89`) — declare cada uma desatualizada com o par `pnpm <área>:evidence` / `pnpm <área>:evidence:verify` no relatório e **não execute nada**.
- **Checklist humana consolidada:** funda o handoff de 14.6.1 (`06-checklist`) e o C10 de 14.6.2 numa lista única para o PR final da epoch — as duas eras de `/pay` e as duas rotas públicas, ambos os locales, seis temas, 320/375/768/1440, máscaras e foco de erro, total no submit, leitura do QR com a marca centrada, retry na primeira falha de poll, foco de modal, `404` forçado, troca de idioma — mais as três evidências desatualizadas acima.
- Defeito real fora do alcance da phase é **follow-up no relatório**, nunca reparo e nunca critério novo.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga com o resto do gate.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída.
- **Round 2 (delta after judge round 1 `execucao`):** commit `713a4fcd` — pins C03.a for real via `page.identity-wiring.test.tsx` (mocks the two form modules to capture `merchantIdentity`, so deleting either `merchantIdentityMark(...)` wiring line in `page.tsx:57`/`public-checkout-v2-page.tsx:85` reddens; verified locally by removing each line and seeing red, then restoring — no source diff survives); extends the C03.c class pin to `public-checkout-form.test.tsx`/`public-checkout-v2-form.test.tsx` (form phase) and `checkout-experience.test.tsx` (payment phase), which are the components that actually emitted the retired classes, replacing the shell-only C01 grep as the durable regression; exercises `startOver` for real (click → form phase → cleared payment/attempt → distinct idempotency key on the next submit) and renames the misleadingly-named prior case; adds `inputMode="numeric"` and focus-to-first-invalid-field pins. No non-test source touched.
- **Commit/artefato:** `ed871d4e` (test: V1 branding realignment + validation regression), `d3500138` (test: owed regression — state machine, payment views, shell, footer), `1be49893` (chore: design-system evidence refresh), `713a4fcd` (test: round-1 pins and coverage), on `task/14.6.3-phase-verification` (uncommitted-to-develop, awaiting the 005 gate).
- **Arquivos alterados:** `src/checkout/public-checkout-presentation.test.ts` (realigned to the additive `branding` projection); `src/app/pay/[identifier]/page.test.tsx` (three fixtures fed `branding` so `merchantIdentityMark` no longer throws); new `checkout-form-validation.test.ts`, `checkout-payment-views.test.tsx`, `checkout-shell.test.tsx`, `checkout-experience.test.tsx`, `checkout-footer.test.tsx` under `src/app/pay/[identifier]/`; `artifacts/design-system/20260909010850/**` + `current.json` (script-written only). No non-test source changed — every red case discovered by the run-1 `vitest run` was a stale test fixture, not a defect, so C04/C09 close with an empty repair list.
- **Desvios:** nenhum.
- **Evidência:**
  - **C01 realignment:** `public-checkout-presentation.test.ts` now asserts the additive `branding` member (populated + default-theme-fallback cases), matching the delivered `projectBranding`/`DEFAULT_STOREFRONT_THEME_ID` source; `page.test.tsx`'s three affected fixtures gained a `branding` object each (no assertion weakened, no case deleted). `pay/[identifier]/public-checkout-form.test.tsx`, `public-checkout-v2-form.test.tsx`, `store/[slug]/{page,storefront-experience}.test.tsx`, `store/[slug]/pay/{page,standalone-payment-experience}.test.tsx` were already realigned by 14.6.1/14.6.2's own `g01`/`g02` runs (0 failures in run 1) — verified by re-running the full suite plus a grep for retired `checkout-*`/`storefront-*`/`receipt-rail*` class literals in `src/app/pay/**`, `src/app/store/**`, `src/checkout/**` tests (zero hits outside module/identifier names). Zero `.skip`/`.todo` added (`rg` clean).
  - **C02 owed regression:** new `checkout-form-validation.test.ts` (masks, email, CPF check digits, CEP, both grammars); `checkout-payment-views.test.tsx` (all 10 states → 8 distinct compositions since RESERVED/CREATING/CREATED share one live shell, confirmed total, refunded neutral badge/no start-over, poll-error banner only on `pollFailed`); `checkout-shell.test.tsx` (560px cap, footer always present, logo→Monogram→fallback precedence, theme/accent binding); `checkout-experience.test.tsx` (field edits never touch attempt/payment/capability; one idempotency key survives a submit; start-over is the only reset path); `checkout-footer.test.tsx` (powered-by copy, `LanguageSwitcher` bound to the persisted locale, privacy trigger). Storefront `EmptyState` vocabulary and standalone reuse were already covered by 14.6.2's suites (unchanged, still green).
  - **C03 judged-defect pins:** (a) `checkout-payment-views.test.tsx` "feeds the merchant identity into the QR display's identity slot" + `page.test.tsx`'s first fixture now asserting `/media/logo-media-identifier-…` renders through the form → payment view; (b) `checkout-experience.test.tsx` "renders the unavailable warning inside the payment phase, reachable after the status poll returns 404"; (c) `checkout-payment-views.test.tsx` "never emits a retired checkout-* class name in any state", plus the phase-wide grep in C01.
  - **C04/C09 repairs:** none. Every failure the run-1 `vitest run` surfaced in Phase 14.6's reach (`public-checkout-presentation.test.ts`, `page.test.tsx` ×3) traced to a stale fixture missing the `branding` member the source already returns — test realignment, not a source defect. Non-test diff is empty; `artifacts/design-system/**` is the only non-test change and is script-written.
  - **C05 aggregate gate (direct binaries only) — qualified pass, floor deferred to 14.7.2:** `tsc --noEmit` exits 2 and `vitest run` exits 1, so C05 does **not** close as written ("all four gates exit 0"); it closes as "green within Phase 14.6's reach." `tsc --noEmit` — the only 2 errors are `nautt-credential-surface.test.tsx:13` and `payment-link-v2-view.test.ts:61`, both 14.5.4-owned test files, unrelated to this phase, unchanged since round 1. Lint chain — exit 0, 0 errors / 62 warnings (none in touched files), unchanged. `vitest run` (full tree, round 2) — **246 files passed | 13 failed, 2169 tests passed | 43 failed | 28 skipped**; **corrected arithmetic:** 2169 passed + 43 non-skipped-but-failed = **2212 total non-skipped ≥ the 2185 floor** (round 1's "2161 + the fixed 4 = above floor" was wrong — 2165 < 2185; the number that actually sustains the floor is passed+failed together, not passed alone). All 13 failing files are 14.5.4-owned (same list as round 1, unchanged): `src/app/(merchant)/links/{new,page,v2/[id]/edit,v2/[id]/orders}.test.tsx`, `src/app/(merchant)/{page,settings}.test.tsx`, `src/app/(merchant)/orders/v2/[id]/page.test.tsx`, `src/app/nautt-credential-surface.test.tsx`, `src/app/profile/profile-management.test.tsx`, `src/app/storefront-preview.test.tsx`, `src/app/storefront-settings-management.test.tsx` (merchant-side preview widget, not `/store/[slug]`), `src/auth/payment-link-v2-prefill.test.ts`, `src/orders/merchant-analytics.test.ts` — none under `src/app/pay/**`, `src/app/store/**`, or `src/checkout/public-checkout*presentation*`, and none of round 2's 4 new test files touch any 14.5.4-owned source or test. Zero failures remain in Phase 14.6's own suites. 14.5.4 is back in 004 realigning exactly those 13 files on its own branch; 14.7.2 C05/C13 redoes the aggregate gate on integrated `develop`, where the numeric floor is enforced as written. `next build` — exit 0, `/pay/[identifier]`, `/store/[slug]`, `/store/[slug]/pay` all compiled as dynamic routes. No `pnpm <script>`, no container used anywhere in this run.
  - **C06 design-system evidence:** `next build` → server on `127.0.0.1:4320` (`ADMIN_EVIDENCE_BASE_URL` pinned) → `playwright test design-system.evidence.spec.ts --project=chromium --workers=1` → 1 passed / 1 skipped, run id `20260909010850` (superseding the stale `20260908212142`, an ancestor of pre-14.6.2 `globals.css`). `node scripts/verify-design-system-evidence.mjs` → `Verified design-system evidence 20260909010850 captures=48 coverage=20/101` (exit 0). `node scripts/verify-login-evidence.mjs` → `Verified login evidence 20260908212004` (exit 0, unaffected by this phase). Server killed after the run.
  - **C07 Docker-driven evidence, declared stale, not run:** `checkout` — `pnpm checkout:evidence` / `pnpm checkout:evidence:verify`; `storefront` — `pnpm storefront:evidence` / `pnpm storefront:evidence:verify`; `standalone-payment` — `pnpm standalone-payment:evidence` / `pnpm standalone-payment:evidence:verify`. None executed; all three go through `scripts/run-admin-evidence.mjs`'s `docker compose up -d --build`.
  - **C08 carried criteria closed by name:** 14.6.2 C4 (storefront shell convergence) and C6 (BEM retirement) — closed by the green `store/[slug]/*` suites plus the C01 class-name grep finding zero survivors; C9 (contracts written down) — closed, the storefront spec's Task 14.6.2 line already documents the convergence, retirement and controller deviation with no drift found. 14.6.1's suite handoff (`public-checkout-form.test.tsx`, `public-checkout-v2-form.test.tsx`, the retired-class grep) — closed: both suites were already green in run 1 (14.6.1's own gate realigned them), and the grep in C01 found no survivor.
  - **C10 contracts still true:** no repair contradicted any spec line — `pop/specs/checkout-and-order-lifecycle.md` (lines documenting 14.6.1's additive `branding`, one-column shell, first-failure retry) and `pop/specs/storefront-and-customization.md` (Task 14.6.2 line: shared shell, `EmptyState`, `CheckoutPaymentView` reuse, BEM retirement) both already state the delivered shape; no spec edit was needed.
  - **C11 consolidated human checklist (for the epoch PR):** verify across **both eras of `/pay`**, **both public routes** (`/pay/[identifier]`, `/store/[slug]`, `/store/[slug]/pay`), **both locales** (pt-BR, en), **six themes** (pix-paper, cashier-daylight, settlement-sand, midnight-clearing, vault-blue, terminal-amber), **320/375/768/1440** viewports: masks (CPF, CEP) and focus-to-first-invalid-field on submit; **visible focus** on every interactive element across both public routes (14.6.2 C10 item, missing from round 1's checklist); the exact total named on the submit button; QR scan with the merchant mark centre-cut; retry affordance appears on the **first** poll failure (not the third); modal focus for the privacy notice; a forced `404` on checkout/status renders the one opaque unavailable view; language switch via the footer `LanguageSwitcher` on every route; no overflow in the storefront's `boxed`/`table` layouts; identity and accent tones equal between `/pay` and `/store/[slug]`. Plus the three stale Docker-driven evidence pairs from C07 (checkout, storefront, standalone-payment) — none exercised by this task.
