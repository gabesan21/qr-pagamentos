# Julgamento — [[14.4.1-converge-shell-and-auth-pages]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — first round of a non-critical `size: L` task; the surface is the whole integrated diff (31 paths, `c7d1a746`..`46ba5384`), read once against the card, specs and template.
- **Superfície:** shell chrome and both role layouts, merchant shell context, auth card + login/TOTP, reset states + 404, dictionaries (both locales), contracts, token-check allowlist.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

**Pedido original primeiro:** the card's "O quê" is met. Route-derived title, `lg` drawer switch, template rail footer, wired storefront link, template nav labels, leading brand panel with tagline/strip/six swatches, forgot-password, show/hide, inline required copy, six-cell TOTP with paste/auto-submit/Back, reset with distinct unusable-link states, hint/meter/mismatch, on-page success, bilingual 404 — all present. The three-way invalid/expired/used split is not delivered because the service cannot prove it; the limit is recorded in spec and `DESIGN.md`, which honours the "Por quê" instead of faking a state.

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Route-derived top-bar title | evidência | shell + both layouts | passou | `shell-navigation.tsx:261-262` matches with `isActiveRoute`; merchant registry adds `/profile` (`(merchant)/layout.tsx:21-24`), fallback `shellDashboard` both roles |
| C02 | Drawer <1024px, one nav copy | evidência | `app-shell.css` | passou | rail `display:none` at `max-width: 63.9375rem` (`:500-503`); mobile trigger/nav `display:none` by default (`:386-388`), shown only inside the same query |
| C03 | Rail footer + template labels | evidência | shell + dictionaries | passou | `app-shell.tsx:46-53` monogram/username/role pill/caption; sign-out stays in the account menu (`shell-navigation.tsx:124,274`); `Payment Links`/`Catalog` and pt-BR pairs present |
| C04 | Storefront link only when enabled+slug | evidência | merchant layout + context | passou | `shell-context.ts:15-18` targets `/store/<slug>`, `undefined` otherwise; admin layout passes no `storefrontLink` |
| C05 | Leading brand panel | evidência | `auth-card.tsx`, `.auth-*` | passou | panel is the first grid child and `grid-template-columns: var(--auth-panel-width) 1fr` at ≥900px (`globals.css:1419-1420`); `aria-hidden`; swatches from the closed registry assets |
| C06 | Forgot-password, show/hide, inline required | evidência | login page/form | passou (ver sugestão 1) | `login-form.tsx:95-114` toggle on a `var(--target-min-size)` 44px target with localized names; per-field `aria-invalid` + `data-invalid`; alert copy stays generic |
| C07 | Six OTP cells, paste, auto-submit, Back | evidência | challenge form | passou (ver sugestão 4) | `totp-challenge-form.tsx:80-99` six `InputOTPSlot`, `onComplete` → `requestSubmit`, Back anchor, recovery toggle still posts `code` to the unchanged route |
| C08 | Two unusable-link states + back-to-login | evidência | reset page | passou | `reset-password/page.tsx:60-88` missing-token and rejected-token branches, each with illustration and `/login` action |
| C09 | 12–128 hint, meter, mismatch, own success | evidência | reset page/form/route | passou | `reset-password-form.tsx:96-150` `maxLength=128`, hint, live meter, client mismatch; `submit/route.ts:29` redirects to `/reset-password?status=changed`, rendered by `page.tsx:57-69` |
| C10 | Bilingual 404 | evidência | `not-found.tsx` + dictionary | passou | cookie-resolved dictionary, `notFound` domain registered in `domains.ts`/`en.ts`/`pt-BR.ts` with both keys in both locales |
| C11 | Presentation-only, a11y laws kept | evidência | full diff | passou | `src/auth/**` untouched; the only route edit is the reset success target; opaque outcomes preserved (generic invalid credentials, no token cause revealed); one nav copy, 44px toggles, tokens only |
| C12 | Commands clean, durable rules recorded | evidência | tsc report + token check + contracts | passou (qualified: `pnpm lint` sem evidência) | `tsc` clean outside the five invalidated test files; `node scripts/check-design-tokens.mjs` exit 0 (re-run here); `DESIGN.md`, spec and shell DOX state title, breakpoint, rail footer, storefront target, auth composition and the reset limit |
| C13 | Suites green after convergence | phase | registered for `14.4.4-phase-verification` | registrado (phase) | five invalidated files named in the human checklist; `14.4.4` card already claims the realignment |
| C14 | Browser walkthrough | humano | manual pass | checklist humana | both locales, six themes, 320px width |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| sugestão | The doc comment claims native `required` still works with JavaScript disabled, but the form carries `noValidate`, so nothing enforces it without JS (behaviour is safe — the server answers with the generic error — the comment is not) | `src/app/login/login-form.tsx:41-45,79` | correct the comment (or enforce via `onInvalid` instead of `noValidate`) |
| sugestão | The strength meter is a verbatim copy of the profile one; the 12-char rule now lives in two places and will drift | `src/app/reset-password/reset-password-form.tsx:113-137` vs `src/app/profile/password-fields.tsx:73-95` | follow-up: extract one owned meter (outside F03's write set) |
| sugestão | Every merchant page now issues a second `user.findUnique` on the row the locale preference already read | `src/app/(merchant)/shell-context.ts:13-15` | follow-up: fold both reads into one context query. No failure risk: `getForOwner`'s `ForbiddenError` cannot fire for a principal `requireOwnerFromCookie` already validated |
| sugestão | `InputOTP` ships without `pattern`, so the cells accept letters and auto-submit them; the replaced input enforced `[0-9]{6}` client-side | `src/app/login/totp-challenge-form.tsx:80-92` | restore a digits-only pattern |
| nit | Dead leftovers: `.auth-card__form--tight` has no consumer; the `48rem` allowlist entry now maps nothing; `/login?password=changed` has no producer left (profile redirects to `/profile?password=changed`) | `globals.css:1416`, `scripts/check-design-tokens.mjs:37`, `login/page.tsx:20` | sweep when the area is next touched |
| nit | `disabled={pending}` on a `Button asChild` wrapping `<a>` renders an inert attribute; the Back link is never actually disabled | `src/app/login/totp-challenge-form.tsx:96` | use `aria-disabled` or drop the prop |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — 31 paths, `src/auth/**`, `src/components/ui/**` and every `*.test.*` untouched; the `scripts/check-design-tokens.mjs` line is the orchestrator-authorized extension recorded in the card Log, and the `63.9375rem` → `--shell-mobile-breakpoint` mapping is the same masking technique already used for `48rem`, matching the 1024px law (media queries cannot read `var()`).
- [x] Dependências não foram implementadas oportunisticamente por consumidores — F03 consumed `AuthCard` as delivered, without editing it.
- [x] Critérios `verify: phase` registrados na checklist da phase — C13 → `14.4.4-phase-verification`.
- [x] Specs, documentação e contratos DOX afetados estão coerentes — `application-frontend-system`, `DESIGN.md` and `src/app-shell/AGENTS.md` describe exactly what shipped, including the reset-state limit; `identity-security` and `administrative-foundation` needed no change and got none.

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** four suggestions and two nits above; none blocks — the meter duplication and the double owner read are the two worth a follow-up.
- **Checklist humana:** (1) C14 browser walkthrough on a running instance, both locales and six themes: top-bar title on each of the five entries per role plus `/profile`; rail↔drawer at 1024px; rail footer; storefront link present only for an enabled+slugged merchant and absent for admin; login panel at ≥900px and hidden below; show/hide, inline required, forgot-password note; six-cell TOTP with paste, auto-submit and Back; the two reset unusable-link states, meter, mismatch and the `?status=changed` success; 404 in both locales; no horizontal overflow at 320px. (2) `pnpm lint` was not evidenced in this round — confirm it with `pnpm check` in `14.4.4`. (3) `14.4.4` must realign `src/app-shell/shell-account-menu.test.tsx`, `src/app/(merchant)/shell-context.test.ts` (needs a storefront-settings mock), `src/app/login/page.test.tsx`, `src/app/reset-password/page.test.tsx`, `src/app/reset-password/submit/route.test.ts`, and confirm the build still renders the now-dynamic `/_not-found`.
- **Resumo:** the shell and every shared access page reproduce the template journeys the audit found diverging, with presentation-only changes: no authentication, authorization or session logic moved, the single route edit is the reset success target, opaque outcomes survive intact, and the one state the service cannot prove was recorded as a contract limit instead of being faked.

<!-- pop-verdict round=1 decision=aprovada -->
