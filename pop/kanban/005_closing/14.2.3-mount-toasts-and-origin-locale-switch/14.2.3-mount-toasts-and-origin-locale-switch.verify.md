# Judgment — [[14.2.3-mount-toasts-and-origin-locale-switch]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-07

- **Strategy:** differential — `size: M`, `critical: false`, first round, no prior delta.
- **Surface:** the 18 paths of the integrated diff (F01 toast/bridge, F02 locale/auth), plus every consumer of the `language` query param and the second `ToastViewport` mount the new global mount now reaches; specs `administrative-foundation`, `application-frontend-system`, `identity-security`, and `DESIGN.md`.
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Conformance with objective and specs

The card's request is substantially delivered: `ToastViewport` now exists in production, the five mechanical notices raise toasts with a `<noscript>` fallback, the locale POST returns to its origin page, and login/reset gained the compact switcher. What fails is the **boundary** between that global toast and the `language` banners the plan left out of scope — plus one false statement this very diff wrote into `DESIGN.md`.

| # | Criterion | Mode | Verification | Result | Evidence |
|---|-----------|------|--------------|--------|----------|
| C1 | `ToastViewport` mounted in the root layout with a localized region label | evidence | `layout.tsx`, grep for mounts | passed | mount at `src/app/layout.tsx:47` using `dictionary.toastRegionLabel`; the only other mount is `/design-system`, which the criterion permits |
| C2 | Closed bridge, one toast, param strip, owner durations, zero new motion | evidence | the bridge and its two mount sites | passed | `src/app/notice-toast.tsx:20-45`: single `find`, owner `showToast` (5s / Infinity at `toast.tsx:35`), `history.replaceState` deleting the owned params, no client dictionary import, no new CSS |
| C3 | The five mechanical sites raise toasts and keep the same `Alert` in `<noscript>` | evidence | diff of the notice components and `/admin` | passed | `catalog-notices.tsx:31-40,53-62`, `links-notices.tsx:27-36`, `orders-notices.tsx:29-38`, `admin/page.tsx:57-64` — `role` and `variant` preserved |
| C4 | Same-origin return with query, fragment dropped, hostile values fall back to `/` | evidence + phase | `settings-return-target.ts`; unit test runs in 14.2.5 | passed (agent) · registered (phase) | `src/app/settings-return-target.ts:20-27`: http/https scheme, host matched against `X-Forwarded-Host`/`Host`, `pathname+search`, `//` rejected. WHATWG parsing turns `\`, `/\`, and `//` into a `//…` pathname, so all fall back to `/`; `%2f%2f` stays a same-origin path, and `Location` is always relative (`relative-redirect.ts:4`) |
| C5 | Anonymous caller gets `qr_locale` instead of `401`, session untouched, three public pages read the cookie | evidence + phase | `route.ts` and the three pages | passed (agent) · registered (phase) | `language-preference/route.ts:26-40`: `isSupportedLocale` guard, attributes identical to the `profile/password/route.ts:30-36` precedent, no write to `qr_session`; `pay/[identifier]`, `store/[slug]`, `store/[slug]/pay` now use `localeFromPreferenceCookie` |
| C6 | Compact switcher on login and reset, ≥44px, labelled, Tailwind only, pages stay opaque | evidence | both pages and the switcher | passed | `language-switcher.tsx:16-33` (`h-11`, `min-w-11`, `aria-label={label}`, `onChange→requestSubmit` matching the shipped precedent `app-shell/shell-navigation.tsx:69`); `login/page.tsx:34,53` and `reset-password/page.tsx:34,71` add no principal read |
| C7 | No edit to `globals.css`, `components/ui/**`, `app-shell/**`, `inventory.json`; no raw visual value | evidence | `git diff --stat`, grep | passed | 18 paths, none in the deny sets; no raw hex or raw visual value in the diff |
| C8 | `pnpm check` green with the new tests | phase | `14.2.5-phase-verification` | registered (phase) | orchestrator override: `settings-return-target.test.ts`, `language-preference/route.test.ts`, and the four `page.test.tsx` missing a `useSearchParams` mock are carried to 14.2.5 |
| C9 | Rendered walkthrough across the migrated surfaces in both locales | user | browser | human checklist | see Verdict — the position the criterion states is wrong (finding 1) |
| C10 | Spec records the widened origin return and the anonymous capability | evidence | spec diff | passed | `pop/specs/administrative-foundation.md:69,80` replace the `{ /settings, / }` allowlist and the "self-only preference" row; no other contract weakened |

### Implementation quality

| Severity | Finding | Evidence | Required fix |
|----------|---------|----------|--------------|
| blocking | `DESIGN.md` states the toast mounts "top-right". `ToastViewport` renders `<Toaster>` with no `position`, and sonner's default is `bottom-right` — so the statement this diff just wrote into the design memory is false, and it is what the C9 checklist and every later frontend task will read | `DESIGN.md:376` against `src/components/ui/toast.tsx:43`; `node_modules/sonner/dist/index.mjs` (`position = 'bottom-right'`) | change "top-right" to "bottom-right" in `DESIGN.md`. The divergence from the template (`docs/template/app/src/components/ui/Toast.tsx:55`, `fixed right-4 top-4`) belongs to the primitive's owner, not this task — it becomes a follow-up |
| blocking | The root layout's global `language` registry fires a toast on **every** route, but the two visible `?language=saved\|error` Alerts survive on the merchant dashboard and on `/settings` — precisely where the switcher returns. The user now sees the same message twice and two live regions announce it, contradicting the paragraph this same diff wrote ("sonner's own live region is the single announcer"). This is the risk the plan itself registered; the declared `<noscript>` mitigation never reached these two sites because they sat outside F01's `Owns` | `src/app/layout.tsx:41-48` with `src/app/(merchant)/page.tsx:76-87` and `src/app/(merchant)/settings/settings-surface.tsx:141-151` | wrap those four `language` Alerts in `<noscript>`, exactly the C3 pattern — same `role` and `variant`, no other change to those files |
| suggestion | With the global mount, `/design-system` now carries two `<Toaster>` instances, so every specimen toast renders and announces twice on that route | `src/app/design-system/interactive-specimens.tsx:289` with `src/app/layout.tsx:47` | follow-up for the specimen page's owner: drop the now-redundant local mount. C1 explicitly permitted this second mount and the file is outside this task's ownership |
| nit | `NoticeToastEntry.retry` has no producer: none of the six call sites passes `retry`, so the `window.location.assign` path is born dead | `src/app/notice-toast.tsx:17,27-29` | keep (C2 declares retry optional) or drop it when 14.4.x defines the first retry site |

### Scope and integration

- [x] Changes respect each front's `Owns` and `Must not edit` — 18 paths, deny sets intact.
- [x] No dependency was implemented opportunistically by a consumer — F01 and F02 do not touch each other.
- [x] `verify: phase` criteria registered for the phase checklist (C4, C5, C8, plus the invalidated assertions handed to 14.2.5).
- [ ] Specs, documentation, and DOX contracts coherent — `administrative-foundation` is correct; `DESIGN.md:376` asserts two things the code does not do (findings 1 and 2).

## Verdict

- **Decision:** **directed repair** (2 pinpoint repairs, at the round's cap; consumes no return counter). I confirm both in an addendum ≤10 lines in this same round.
- **Blocking:** (1) `DESIGN.md:376` claims "top-right" against sonner's actual `bottom-right`; (2) duplicated `language` feedback and announcement on the dashboard and `/settings`.
- **Plan defect:** none that warrants 002 — the plan covered the request; the boundary of the two surviving `language` banners is amendable in place, without replanning.
- **Suggestions/nits:** double `Toaster` on `/design-system` (follow-up); `retry` without a producer in the bridge.
- **Human checklist:** C9 walkthrough, with the expectation corrected: (a) after a mutation on catalog, categories, links, orders, and `/admin`, the toast appears **bottom-right** (not top-right) and a refresh does not re-toast; (b) switching language on a deep page with a query stays there with the query preserved, in both locales; (c) after the repair, `/` and `/settings` show exactly **one** language notice; (d) login, reset, and a public checkout page switch language while signed out; (e) if the walkthrough runs under `pnpm dev`, StrictMode can double-fire the toast — confirm against a production build before reporting a duplicate.
- **Summary:** the request landed — global toast, five migrated notices with a `<noscript>` fallback, an origin return that preserves the query without opening a redirect, an anonymous cookie with exact attribute parity, and a compact switcher on the auth pages. Two pinpoint corrections remain: a false position statement in `DESIGN.md`, and the two surviving `language` banners that now compete with the global toast.

## Return delta

- **Kind:** execucao (pinpoint — directed repair, the folder does not move).
- **Affected criteria:** C2 and C9 — the global `language` registry reaches routes that still announce the same message through an `Alert`; the visual transcription in `DESIGN.md` states a position the owner does not implement.
- **Affected fronts:** `F01` — receives the directed patch on the three files below (two of them new to its write set; ownership validation must be reissued).
- **Intact fronts:** `F02` — fully approved (C4, C5, C6, C10); do **not** re-execute.
- **Expected action:** in `DESIGN.md:376` change "top-right" to "bottom-right"; in `src/app/(merchant)/page.tsx:76-87` and `src/app/(merchant)/settings/settings-surface.tsx:141-151` wrap the four `language` Alerts in `<noscript>`, preserving `role` and `variant`. No other change.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=DESIGN.md,src/app/(merchant)/page.tsx,src/app/(merchant)/settings/settings-surface.tsx frentes=F01 intactas=F02 -->
