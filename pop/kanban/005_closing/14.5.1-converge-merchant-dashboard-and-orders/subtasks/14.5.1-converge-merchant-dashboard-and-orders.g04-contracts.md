# Frente F04 — Contracts and evidence — [[14.5.1-converge-merchant-dashboard-and-orders]]

- **Entrega:** the durable record of what the integrated diff actually changed — extended analytics definitions, the six-filter owner directory set, the retired dashboard BEM, the additive `Timeline` slot — plus the recorded gaps and the phase checklist for 14.5.4.
- **Escopo:** specs, `DESIGN.md` and the UI DOX only. No application code, no dictionary, no test.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this slice, the plan's "Estratégia" and "Critérios de aceite", the integrated diff of F01/F02/F03 and their `Resultado` sections.
- **Owns:** `pop/specs/{checkout-and-order-lifecycle,administrative-foundation}.md`, `DESIGN.md`, `src/components/ui/AGENTS.md`.
- **May read:** every path the three implementation slices own, `pop/specs/application-frontend-system.md`, `src/data-directory/AGENTS.md`, `pop/researches/template-fidelity-convergence/merchant-commerce.md`.
- **Must not edit:** all application source, dictionaries, `**/*.test.*`, `src/data-directory/AGENTS.md` and every other `AGENTS.md`, the kanban card and plan.
- **Depends on:** F01, F02, F03. **Entrada esperada:** the three fronts integrated, with their `Resultado` naming the projection fields, the registered filters and every gap recorded. Absent or incompatible → `BLOCKED`; never infer a contract from the plan instead of the diff.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep each contract statement at its smallest true form*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** execution report naming each contract file touched, each gap recorded and the phase checklist entries, status `concluída | BLOCKED`.
- **Critérios:** C13.

## Contrato de execução

- **Spec — checkout-and-order-lifecycle:** extend the merchant analytics definitions with exactly the fields the diff added (orders in period, by-provider-state and by-source counts, owner-own order and product identifiers, link total, active/archived product counts, the derived first-run signal), stating that they are counts and owner-own identifiers only. Record that the merchant local-outcome editor is not gated on the provider state, with the reason already pinned by the contract: `AD_HOC` orders are stateless and a new append is allowed after reconciliation.
- **Spec — administrative-foundation:** record the owner order directory's six registered filters with `state` mapped to the eight provider states plus one stateless value, and state that page sizes, keyset, cursor inputs and the `filter.<name>` grammar are unchanged.
- **Gaps:** record, where each contract lives, the six declared gaps — no local-outcome filter, no currency-pair filter, no unified V1+V2 list, no payment-data card, no stale strip, the V2 line-title column follow-up — each with its one-line reason. A gap is a recorded fact, never a promise of a future task in this plan.
- **DESIGN.md:** update the inventory and state record with the additive `Timeline` `action` slot, the merchant period control as a route-local client wrapper over the owned `Tabs` pill composition (with the rule that a third consumer promotes it to a single route-neutral owner), and the dashboard's retired BEM block.
- **`src/components/ui/AGENTS.md`:** add the `Timeline` additive slot to the sentence that already enumerates the additive family props; change nothing else in that file.
- **Phase checklist:** list in the `Resultado` every merchant suite this diff falsifies (dashboard BEM assertions, the two-`<details>` outcome forms, oldest-first comments, directory filter set) plus criterion C14, so `14.5.4-phase-verification` inherits them.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <contract files touched, gaps recorded, phase checklist entries>.
