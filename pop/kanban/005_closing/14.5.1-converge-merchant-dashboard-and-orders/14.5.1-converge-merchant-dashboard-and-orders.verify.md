# Julgamento — [[14.5.1-converge-merchant-dashboard-and-orders]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — rodada 1, `size: L`, não crítica, sem retorno anterior; leitura profunda do diff integrado inteiro (27 paths, `bf09ce40..9ec214bd`), sem re-run de suíte.
- **Superfície:** F01 projeção owner + dashboard, F02 filtro `state` + colunas, F03 editor de outcome/thread/link card, F04 specs/DESIGN/DOX. Riscos auditados: isolamento owner das leituras novas, exposição de `payerName`, decimais exatos, superfícies compartilhadas com o admin, paridade de locales, a11y das composições novas.
- **Devoluções:** execução 0 de 2 · plano 0 de 2 (reparo dirigido não consome contador).

**Pedido original (O quê / Por quê):** atendido. As quatro rotas passam a reproduzir o modelo do template — período segmentado sem navegação cheia, tabs por estado/origem, quatro cards de inventário, linhas clicáveis com id, banner de loja desativada, first-run, `error.tsx`, filtro de estado registrado, editor de outcome com select + nota + confirmação, composer com `Monogram` e thread newest-first, link card com badge de ciclo de vida. As seis decisões fixadas no plano (sem gate `confirmed`, vocabulário local, `state` registrado, sem filtro de outcome/par, sem lista V1+V2 unificada, sem card de dados de pagamento nem faixa stale) foram respeitadas e registradas como gaps, não como silêncio.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Period control é estado de URL no cliente | evidência | `period-control.tsx`, `page.tsx` | passou | `Tabs`/`TabsList` pill, `router.replace` em `startTransition`, set fechado `today/7d/30d`, default `7d` em `page.tsx:17`; pills `<Link>` só dentro de `<noscript>` |
| C02 | Projeção estendida aditivamente | evidência | `src/orders/merchant-analytics.ts` | passou | `ordersInPeriod`/`byProviderState`/`byOrigin`/`products`/`totalCount`/`isFirstRun` opcionais; `countOrdersBySourceAndState` e `countProducts` filtram `ownerId` (`:552-566`); ausência de capability vira zero, não erro; nenhum `Number(`/`parseFloat` nos arquivos tocados |
| C03 | Linhas e cards do dashboard | evidência | `dashboard.tsx` | passou | stat 1 = pedidos no período com caption LINK/STANDALONE/AD_HOC; card de gráfico com tabs estado/origem; quatro cards de inventário; linhas recentes `#id` + pagador → `/orders/v2/<id>`; best sellers → `/catalog/products/<id>` |
| C04 | Estados do dashboard completos | evidência | `page.tsx`, `loading.tsx`, `error.tsx` | passou | banner → `/settings#storefront-enabled`; `FirstRunState` com CTA de criar link; `error.tsx` faixa inline com `reset()`; geometria do skeleton bate com 4 / 7-5 / 4 / 7-5 |
| C05 | BEM do dashboard aposentada | evidência | `rg "merchant-dashboard__"` + diff de `globals.css` | passou | 25 regras removidas; única ocorrência restante é `(merchant)/page.test.tsx:126`, já listada para 14.5.4 |
| C06 | Contrato de query do diretório owner | evidência | `src/orders/order-v2-directory.ts` | passou | `state` = 8 estados + `STATELESS` → coluna nula (`:167-170`), simétrico ao admin (10.2.1/14.4.2); seis filtros, page sizes, keyset, cursor, `q` e gramática intactos; `directory-query.ts` sequer precisou mudar |
| C07 | Página de pedidos igual ao template | evidência | `orders/page.tsx`, `legacy-order-table.tsx` | passou | oito colunas com badges de domínio, `CopyField variant="compact"`, chips localizados via `orders-directory`; V1 read-only em tabela/cards local, nunca unido à página keyset |
| C08 | Editor de outcome como ação única guardada | evidência | `order-outcome-editor.tsx`, `order-v2-views.tsx` | passou | `NativeSelect` só com `LOCAL_FINALIZED`/`LOCAL_CANCELLED` + nota + `ConfirmDialog` que faz `requestSubmit()` do POST nativo `set-outcome` com `version`; sem gate `confirmed`; caption de precedência; `FormDraftGuard` e o aviso de 14.3.2 preservados |
| C09 | Thread e composer de comentários | evidência | `order-v2-views.tsx`, `timeline.tsx` | passou | `Monogram` + textarea; thread newest-first; edição CAS author-only migrada para o slot opcional `action`, ausente por default em todos os outros callers (ver sugestão S1) |
| C10 | Link card, indisponível e skeletons | evidência | ambas as rotas de detalhe, `payment-link-v2-view.ts` | passou | `getForOwnerByIdentifier` re-autoriza, valida `^[A-Za-z0-9_-]{24}$` e busca `{ identifier, ownerId }`, falhando fechado; `EmptyState` mantido; dois `loading.tsx` com o literal já usado pelas rotas admin equivalentes |
| C11 | Fronteiras e superfícies compartilhadas intactas | evidência | diff da projeção, do serviço e das views/primitivos compartilhados | **falhou** | isolamento owner e ausência de UUID de provider/par/linha: ok; `Timeline.action` e `OrderV2DetailCard.link` são opcionais: ok. **Mas** `src/app/orders/order-views.tsx:210-213` e `:270-276` mudam o `OrderDetailCard` incondicionalmente e `/admin/orders/[id]` deixa de renderizar como entregue (ver B1) |
| C12 | Locales e gates estáticos | evidência | dicionários tocados + `tsc`/eslint da integração | passou | 19 chaves novas em `merchant-dashboard` e 3 em `orders`, mesmas chaves nos dois locales; nenhuma literal de UI nova em componente; `tsc` 0 erros fora dos testes invalidados, eslint 0 erros; `check-design-tokens` re-rodado aqui: exit 0 |
| C13 | Contratos e checklist da phase | evidência | specs, `DESIGN.md`, `src/components/ui/AGENTS.md` | passou (ressalva) | campos de 8.4.1, exceção estreita de `payerName`, filtro `state` em 8.3.1, slot aditivo e os 6 gaps escritos onde o contrato mora; specs em 150/149 linhas. Ressalva: os `Resultado` das quatro fatias seguem em placeholder (ver S2) — a lista de suítes falsificadas fica registrada abaixo e na memory |
| C14 | Gate agregado após realinhamento | phase | `pnpm check` em `14.5.4-phase-verification` | registrado (phase) | suítes falsificadas por este diff: `(merchant)/page.test.tsx`, `merchant-analytics.test.ts`, `merchant-analytics.database.test.ts`, `orders/page.test.tsx`, `orders/directory-query.test.ts`, `order-v2-directory.test.ts`, `payment-link-v2-view.test.ts`, `orders/v2/[id]/page.test.tsx` |
| C15 | Conferência renderizada pelo dono | humano | abrir `/`, `/orders` e um pedido V2 nos dois locales e temas | checklist humana | ver veredito |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | **B1** — a view compartilhada do detalhe V1 muda para os dois papéis: o tile "Origem: pagamento por link" e a promoção do `CopyField` do link a card próprio são incondicionais, então `/admin/orders/[id]` (fora do escopo declarado da fatia F03 e da posse "additive only") passa a renderizar diferente do entregue por 14.4.2/14.4.4, sem registro em spec ou DESIGN | `src/app/orders/order-views.tsx:210-213` e `:270-276`; call site admin em `src/app/admin/orders/[id]/page.tsx:14` | tornar as duas mudanças opt-in por prop opcional ausente por default (o mesmo padrão de `owner?`), com a rota merchant `src/app/(merchant)/orders/[id]/page.tsx:23` optando por elas; nada mais |
| sugestão | **S1** — o corpo do comentário virou `title` do `Timeline`, renderizado em `<p className="font-medium">` sem `whitespace-pre-line`: quebras de linha internas, que o modelo de comentários preserva, somem na leitura e um corpo de até 2.000 caracteres sai em peso de título | `src/app/orders/order-v2-views.tsx:457`; `src/components/ui/timeline.tsx:50` | passar o corpo como texto normal com `whitespace-pre-line` (ou dar ao slot um wrapper próprio), preservando as quebras |
| sugestão | **S2** — as quatro seções `## Resultado` das fatias seguem em placeholder, então nem os arquivos alterados nem a lista de suítes falsificadas ficam em artefato durável do card | `subtasks/*.g0{1,2,3,4}-*.md` | preencher os `Resultado` na integração (a lista de C14 acima já cobre a herança para 14.5.4) |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente (`pop_check_scope.py` ok; `src/app/admin/**`, `prisma/**`, `order-view.ts` V1 e `**/*.test.*` intocados) — mas ver B1: dentro do path próprio, a mudança extrapola o papel merchant.
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [x] Critérios `verify: phase` registrados na checklist da phase (C14, com as oito suítes nomeadas acima).
- [x] Specs, documentação e contratos DOX afetados estão coerentes com o entregue.

## Veredito

- **Decisão:** **reparo dirigido** — um bloqueante pontual, com remédio objetivo e sem mudança de estratégia; o orquestrador despacha o patch e eu confiro em adendo ≤10 linhas nesta mesma rodada.
- **Bloqueantes:** B1 (superfície compartilhada do detalhe V1 alterando o admin sem prop opcional).
- **Defeito de plano:** nenhum — os critérios cobriam o pedido; C11 nomeia exatamente a falha.
- **Sugestões/nits:** S1 (quebras de linha do comentário na `Timeline`), S2 (`Resultado` das fatias vazios). Não bloqueiam.
- **Checklist humana:** C15 — abrir `/` (troca de período sem navegação cheia, tabs estado/origem, linhas clicáveis, banner de loja desativada), `/orders` (chips dos seis filtros, tabela e cards do V1 legado) e um pedido V2 (confirmação do outcome, thread newest-first, link card) nos dois locales e nos dois temas; pass = comportamento como descrito, sem pill lavada e sem overflow.
- **Resumo:** o objetivo foi atingido em toda a superfície merchant e os contratos contam a verdade, inclusive sobre os gaps; o que sobra é um vazamento de escopo de três linhas numa view compartilhada, que devolve o admin ao estado entregue sem tocar em nada do merchant.

## Delta da devolução

- **Tipo:** execucao (pontual — reparo dirigido, sem mover a pasta).
- **Critérios afetados:** C11 — a view compartilhada do detalhe V1 mudou para o call site admin sem prop opcional.
- **Frentes afetadas:** `F03` — apenas as duas hunks de `src/app/orders/order-views.tsx` e o opt-in da rota merchant V1.
- **Frentes intactas:** `F01`, `F02`, `F04` — aprovadas, permanecem integradas; **não** reexecutar.
- **Ação esperada:** tornar o tile de origem e o card de link do detalhe V1 opt-in por prop opcional ausente por default, com `src/app/(merchant)/orders/[id]/page.tsx` optando por elas; nenhuma outra mudança.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/orders/order-views.tsx,src/app/(merchant)/orders/[id]/page.tsx frentes=F03 intactas=F01,F02,F04 -->
