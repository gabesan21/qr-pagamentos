# Plan — [[12.6.4-phase-verification]]

- **Stage:** 002_planning · **Owner:** planning agent

## Objective and expected outcome

- **Objective:** Run the accumulated `verify: phase` checklist for Phase 12.6 and repair any direct-pnpm failures.
- **Observable result:** A `12.6.4-phase-verification.verify.md` report showing `pnpm check` and non-Docker parity/source checks passing, with every failure fixed or triaged.

## Strategy

Accumulate the phase-level criteria registered by 12.6.1, 12.6.2, and 12.6.3; execute them with direct pnpm only. Visual evidence generation is Docker-dependent (`run-admin-evidence.mjs` orchestrates `compose.yaml`), so it stays in the human verification checklist, matching the 12.3.3 disposition.

## Affected areas

- `pop/kanban/005_closing/12.6.*/12.6.*.verify.md` — source of accumulated `verify: phase` criteria.
- `src/app/pay/[identifier]/**`, `src/app/store/[slug]/**`, `src/app/store/[slug]/pay/**` — surfaces whose unit tests and static checks must pass.
- `docs/frontend-template-parity/*` and design-system inventory — parity/source checks that must stay valid.
- `pop/specs/*` and affected `AGENTS.md` — only if evidence selectors or contracts need updating.

## Gaps and preflight

- **RECON NEEDED:** 12.6.1 kanban artifacts are absent from the current worktree; recovered from commit `dce0c72` for criteria collection.
- **Preflight:** `pnpm check` baseline started as background task `bash-xasl3buj`. `typecheck` passed; `lint` failed at `tokens:check` due to a raw visual value in `src/app/profile/password-fields.tsx` (task 12.5.5 surface), blocking the rest of the gate.

## Execution front

### F01 — Phase verification harness

- **Deliverable:** Accumulated checklist executed, failures repaired, report authored.
- **Scope:** Direct-pnpm checks only; no Docker-dependent evidence generation.
- **Owns:** `12.6.4-phase-verification.verify.md` and source repairs inside Phase 12.6 surfaces.
- **Must not edit:** Backend services, routes, request/response contracts, or files owned by tasks outside Phase 12.6.
- **Depends on:** none (dependencies are closed; do not re-implement them).
- **Skills:** [[pop/skills/clean-code-change|clean-code-change]], [[pop/skills/clean-code-review|clean-code-review]], [[pop/skills/sync-specs|sync-specs]].

## Order and parallelism

1. Collect `phase` criteria from 12.6.1 (`dce0c72`), 12.6.2, and 12.6.3 verify files.
2. Run direct-pnpm checks: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` (aggregate `pnpm check`), `pnpm frontend-parity:check`, `pnpm design-system:source-check`.
3. Repair failures inside Phase 12.6 surfaces; triage out-of-scope failures with evidence.
4. Update evidence specs/contracts only if selectors changed.
5. Author `verify.md` with command outputs and disposition.

## Risks and abort conditions

- **Risk:** Concurrent task failures outside Phase 12.6 break `pnpm check`. — Mitigation: triage; do not repair unrelated owned surfaces.
- **Risk:** Playwright/Docker evidence cannot be regenerated in this environment. — Mitigation: record qualified pass and rely on the human checklist, as in 12.3.3.
- **Abort if:** a failure requires changing a backend contract, API route, or dependency surface — signal `blocked: true`.

## Acceptance criteria

| # | Criterion | Verifies | Verification | Pass is | Mode 005 |
|---|-----------|----------|--------------|---------|----------|
| 1 | `pnpm check` passes | agent | `pnpm check` direct | exit 0, no errors | evidence |
| 2 | Frontend template parity holds | agent | `pnpm frontend-parity:check` | exit 0 | evidence |
| 3 | Design system source coverage holds | agent | `pnpm design-system:source-check` | exit 0 | evidence |
| 4 | Phase 12.6 failures are repaired or triaged | agent | `verify.md` report | every direct-pnpm error has a fix or documented exception | evidence |
| 5 | Evidence runs are documented | agent/user | Verify artifacts if present; note Docker dependency otherwise | no stale failures / qualified pass documented | human |
| 6 | Specs/AGENTS stay coherent | agent | diff review | no stale contract references | evidence |

## Specs and contracts

- [[pop/specs/checkout-and-order-lifecycle|Checkout and order lifecycle]] — no behavioral change expected.
- [[pop/specs/storefront-and-customization|Storefront and customization]] — no contract change expected.
- [`src/checkout/AGENTS.md`](../../src/checkout/AGENTS.md) — read before touching checkout surfaces.
- [`src/media/AGENTS.md`](../../src/media/AGENTS.md) — read before touching media reads.

## Execution topology

- **Shape:** single executor.
- **Rationale:** Phase verification is a sequential checklist with one owner; repairs are local and validated by re-running the same commands.
