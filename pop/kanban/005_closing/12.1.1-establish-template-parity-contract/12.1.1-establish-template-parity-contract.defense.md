# Plan defense — [[12.1.1-establish-template-parity-contract]]

- **Stage:** 002_planning · **Owner:** agent planner

## Contestable decisions

| # | Decision | Adopted choice | Rejected alternative | Why | What would falsify it |
|---|----------|----------------|----------------------|-----|-----------------------|
| 1 | Contract form | Source-bound machine manifest plus human rules and validator. | Prose-only checklist. | Exhaustiveness and later drift must be objectively testable. | The validator cannot detect a seeded missing route/import/class/asset/current route. |
| 2 | Template component boundary | Transitively reachable graph from `App.tsx`, with unused generated UI explicitly excluded. | Treat every generated shadcn file as a parity obligation. | Only consumed source defines the supplied product experience. | Runtime/static resolution shows an excluded module is reachable from a routed surface. |
| 3 | Authority conflict | Template owns presentation; current specs own business/security/runtime behavior. | Copy Vite mock/session semantics into production obligations. | The requested redesign preserves the current application contract and stack. | A current approved spec explicitly delegates a named behavior to the template. |
| 4 | Missing public designs | Record `/store/[slug]` and `/store/[slug]/pay` as authorized extrapolations. | Block them or claim direct template parity. | The user explicitly authorized faithful extrapolation for missing template surfaces. | Dedicated approved template sources for either route are found. |
| 5 | Visual comparison | Pinned Chromium, four fixed viewports, deterministic rendering, 0.1 per-pixel threshold and 0.001 ratio ceiling with semantic failures independent. | Subjective review or zero raster tolerance. | It bounds anti-aliasing noise without waiving meaningful visual differences. | Repeated identical captures exceed the ceiling, or a seeded meaningful drift passes. |
| 6 | Spec impact | Reference current specs without changing them; task 12.1.2 owns the superseding application frontend spec. | Amend the old admin-only visual spec here. | This task inventories parity; the next task defines the new durable system contract. | Execution introduces a durable behavior/invariant not already assigned to 12.1.2. |
| 7 | Supplied-source provenance | Version both root prompt and complete template tree byte-for-byte before contract authoring. | Read mutable untracked workspace files or deduplicate the identical prompt copy. | F01/F02 need reproducible inputs; the root prompt records the request while `docs/template/info.md` is part of the delivered bundle. | A committed upstream source proves one path is accidental, or pre/post path-and-hash inventories differ. |
| 8 | Reference/toolchain boundary | Exclude only `docs/template/**` from root TypeScript and ESLint inventories while the parity checker remains its validation owner. | Normalize the immutable Vite source for Next.js, install its toolchain into production, or weaken the aggregate gate. | The source is reference-only and byte-frozen; production checks should validate production while the dedicated gate checks reference completeness and hashes. | A production import depends on the reference, root tooling still traverses it, or the parity checker stops detecting its drift. |
