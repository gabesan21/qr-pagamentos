# Front F01 — Author the parity contract — [[12.1.1-establish-template-parity-contract]]

- **Delivery:** Versioned exhaustive manifest plus concise rules and evidence matrix.
- **Scope:** Supplied reachable template graph, current frontend route surface, mapping dispositions, later ownership, and visual evidence targets.
- **Owner:** agent.
- **Owns:** `docs/frontend-template-parity/**`.
- **May read:** versioned `docs/template/**`, `FRONTEND_TEMPLATE_PROMPT.md`, `src/app/**`, `src/app-shell/**`, `src/components/ui/**`, `src/data-directory/**`, `src/brand/**`, `src/design-system/**`, `src/i18n/**`, `tests/*.evidence.spec.ts`, linked research/specs, `DESIGN.md`, `package.json`, `playwright.config.ts`.
- **Must not edit:** `docs/template/**`, `src/**`, `tests/**`, `scripts/**`, `package.json`, lockfile, specs, `DESIGN.md`, and F02-owned paths.
- **Depends on:** F03.
- **Expected input:** F03's complete versioned reference and the current repository inventory.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] for complete state/evidence obligations; [[.agents/skills/sync-specs/SKILL|sync-specs]] to preserve current durable contract authority.
- **Criteria:** C1-C4 in the [[12.1.1-establish-template-parity-contract.plan|plan]].

## Execution contract

- Inventory only modules transitively reachable from `App.tsx`; record generated-but-unused UI as excluded, never silently omit it.
- Give every mapping a stable ID, source/hash, target, disposition, reason where required, later task owner, and evidence target.
- Keep mock data/session mechanics non-authoritative and preserve the exact current business contracts.
- Missing dependency or unreadable graph edge → report `BLOCKED`; never invent its contents.
- Alter only `Owns` paths.

## Result

- **Status:** completed.
- **Commit/artifact:** uncommitted F01 worktree artifact — `docs/frontend-template-parity/{README.md,manifest.json,obligations.ndjson}`.
- **Files changed:** the concise authority/evidence rules, hash-bound manifest metadata, and 2,233 newline-delimited obligations with concrete downstream task ownership.
- **Deviations:** none; the machine manifest is intentionally split into metadata and NDJSON obligations so the checker can stream it while preserving the required count and SHA-256 binding. Only terminal `excluded-unreachable-generated-ui` records use a null owner.
- **Evidence:** deterministic parser inventory: 69 reachable files / 454 import edges / 29 template routes / 48 current page-loading-error surfaces / 1,444 class occurrences / 17 assets / 2 locales / 6 themes / 260 states / 309 interactions / 49 explicit unreachable generated-UI exclusions. IDs and source hashes remained stable; 2,233 records and the updated SHA-256 were verified locally. Storefront/cart is owned by 12.6.2; standalone pay by 12.6.3; source-derived admin/account/link/order detail and merchant-order records follow their concrete task-family owners, admin shared/unavailable are 12.7.2, and Logo records are 12.2.2.
