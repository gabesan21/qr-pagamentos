# Front F02 — Enforce contract completeness — [[12.1.1-establish-template-parity-contract]]

- **Delivery:** Deterministic manifest validation exposed as `pnpm frontend-parity:check`.
- **Scope:** Validate schema, source hashes, reachable graph, current routes, mapping uniqueness/completeness, dispositions, and evidence dimensions.
- **Owner:** agent.
- **Owns:** `scripts/check-frontend-template-parity-contract.mjs`, `package.json`.
- **May read:** F01 artifacts, versioned `docs/template/**`, `FRONTEND_TEMPLATE_PROMPT.md`, current `src/app/**`, existing checker/evidence scripts, `playwright.config.ts`, `tsconfig.json`.
- **Must not edit:** `docs/**`, `src/**`, `tests/**`, lockfile, specs, `DESIGN.md`, and all non-owned scripts.
- **Depends on:** F03 and F01.
- **Expected input:** F03's complete hash-stable reference plus F01's stable schema and manifest.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] for executable UI-contract validation.
- **Criteria:** C1, C2, C5-C7 in the [[12.1.1-establish-template-parity-contract.plan|plan]].

## Execution contract

- Use repository dependencies and deterministic local parsing; add no package or network dependency.
- Fail closed with stable diagnostics naming the missing/stale obligation.
- Prove route/import/class/asset/current-route mutation sensitivity on a disposable copy; never modify canonical sources for the test.
- Dependency absent or schema incompatible → report `BLOCKED`; never rewrite F01-owned artifacts.
- Alter only `Owns` paths.

## Result

- **Status:** complete.
- **Commit/artifact:** `scripts/check-frontend-template-parity-contract.mjs`; `pnpm frontend-parity:check`.
- **Files changed:** `scripts/check-frontend-template-parity-contract.mjs`, `package.json`.
- **Deviations:** `pnpm` could not run in this worktree because its automatic install hit the local SQLite store error; the identical Node entrypoint passed directly.
- **Evidence:** Direct checker pass: 2,233 obligations, 69 reachable files, 454 edges, and 48 current page/loading/error surfaces. It independently re-extracts reachable components, class occurrences (line/expression hash), `useState`, supported interaction attributes, public assets, exact locales/themes, routes, imports, and excluded UI, with source-derived expected inventories (`*_MISSING` on additions; `*_STALE` on removals). It derives the exact downstream owner from each source/route and rejects tampering with `PARITY_OWNER_INVALID`. Disposable `/tmp` mutations emitted `PARITY_TEMPLATE_ROUTE_MISSING`, `PARITY_IMPORT_EDGE_MISSING`, `PARITY_AUTHORED_CLASS_MISSING`, `PARITY_ASSET_MISSING`, `PARITY_CURRENT_ROUTE_MISSING`, owner invalid, and an asset removal emitted `PARITY_ASSET_STALE`; canonical files remained unchanged.
