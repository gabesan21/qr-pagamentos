# Front F03 — Ingest supplied reference — [[12.1.1-establish-template-parity-contract]]

- **Delivery:** Complete byte-identical, versioned copy of the user-supplied frontend reference.
- **Scope:** `docs/template/**` and root `FRONTEND_TEMPLATE_PROMPT.md`, including dotfiles and ignored files present in the supplied tree.
- **Owner:** agent.
- **Owns:** `docs/template/**`, `FRONTEND_TEMPLATE_PROMPT.md`.
- **May read:** the same untracked paths in the main workspace and task worktree metadata.
- **Must not edit:** file contents, `docs/frontend-template-parity/**`, `src/**`, `scripts/**`, `package.json`, lockfiles outside the supplied tree, specs, and `DESIGN.md`.
- **Depends on:** none.
- **Expected input:** The intact user-supplied files in the main workspace before task-worktree creation.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] to preserve the visual authority; [[.agents/skills/sync-specs/SKILL|sync-specs]] to avoid changing durable contracts.
- **Criteria:** C6-C7 in the [[12.1.1-establish-template-parity-contract.plan|plan]].

## Execution contract

- Capture a sorted relative-path/SHA-256 inventory of both source roots before copying; copy without normalization or content changes.
- Recompute and compare the inventory in the task worktree, then ensure every entry is versioned even when `.gitignore` would normally exclude it.
- Treat both identical `FRONTEND_TEMPLATE_PROMPT.md` and `docs/template/info.md` as inputs: the former preserves the root request record; the latter preserves the delivered bundle.
- Record the equality of those two files as a provenance invariant; do not deduplicate either path.
- Any missing path, byte mismatch, copy-time source change, or untracked copied entry → report `BLOCKED`.
- Alter only `Owns` paths.

## Result

- **Status:** complete; copied byte-for-byte and left unstaged for the root orchestrator's forced-add handoff.
- **Commit/artifact:** `/tmp/12.1.1-f03-source-before.sha256`, `/tmp/12.1.1-f03-source-after.sha256`, `/tmp/12.1.1-f03-target.sha256`, and their matching `.dirs` inventories; no commit created by this front.
- **Files changed:** `FRONTEND_TEMPLATE_PROMPT.md` and the complete `docs/template/**` tree in the task worktree (157 files across 22 directories, including dotfiles and ignored files).
- **Deviations:** none.
- **Evidence:** Source-before, source-after, and target sorted path/SHA-256 inventories compare exactly; the inventory digest is `68831683881e8d643911698adf43574524a20b3879b7dad0704c4e659bbafdf3`. Source and target directory inventories also compare exactly. Both prompt copies equal `docs/template/info.md` byte-for-byte with SHA-256 `99ccfb73123df773a59ea15cbc3c525a9a11a4b66117a6bcf903fc89f3b49a00`. File-mode inventories match (`606e41134061ef5f0c5b936721018d45f66c54e93d20b7a179160beeece634d8`). The ignored `docs/template/app/node_modules/.tmp/*` entries remain present for the orchestrator to add with force; this front did not stage or commit.
