# Front F04 — Isolate reference tooling — [[12.1.1-establish-template-parity-contract]]

- **Delivery:** Production TypeScript, ESLint, and build inventories exclude the immutable Vite reference while its dedicated parity gate still validates it.
- **Scope:** Exact root inventory boundaries for `docs/template/**`; no source normalization or dependency changes.
- **Owner:** agent.
- **Owns:** `tsconfig.json`, `eslint.config.mjs`.
- **May read:** `docs/template/**`, `package.json`, `next.config.ts`, `scripts/check-frontend-template-parity-contract.mjs`, completed F01/F02 artifacts, and aggregate-gate diagnostics.
- **Must not edit:** `docs/template/**`, `FRONTEND_TEMPLATE_PROMPT.md`, `docs/frontend-template-parity/**`, `scripts/**`, `package.json`, lockfiles, `next.config.ts`, `src/**`, tests, specs, and all F01/F02/F03-owned paths.
- **Depends on:** completed F03, F01, and F02 commits.
- **Expected input:** Byte-frozen versioned reference, dedicated checker, and the aggregate failure showing root TypeScript/ESLint traversal.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] for the smallest explicit inventory boundary; [[.agents/skills/sync-specs/SKILL|sync-specs]] to preserve the no-contract-change conclusion.
- **Criteria:** C5, C6, C8 in the [[12.1.1-establish-template-parity-contract.plan|plan]].

## Execution contract

- Add only the exact reference-root exclusions locally evidenced as necessary; do not broaden ignores or change production source coverage.
- Keep the immutable source byte-identical and add no template dependency, alias, build path, or normalization.
- Prove typecheck and lint omit the reference, production build still passes, and `pnpm frontend-parity:check` still inventories it.
- If any production module imports `docs/template/**`, or another root config must change, report `BLOCKED` with the path/evidence; do not expand ownership.
- Alter only `Owns` paths.

## Result

- **Status:** completed.
- **Change:** `tsconfig.json` excludes `docs/template/**`; `eslint.config.mjs` globally ignores the same immutable reference root.
- **Evidence:** no production import reaches `docs/template/**`; TypeScript's file inventory contains no template path; ESLint reports zero template results; `pnpm typecheck`, `pnpm lint` (existing warnings only), `pnpm build`, and `pnpm frontend-parity:check` passed (`records=2233`, `reachable_files=69`). `pnpm check` reached typecheck, lint, and Vitest before the runner ceased reporting completion, so no aggregate-pass claim is made.
- **Specs:** no durable behavior or contract changed; no spec update is required in 004.
