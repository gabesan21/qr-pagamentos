# 12.5.1-remodel-merchant-dashboard — Verification summary

## Status

**done** — implementation complete; typecheck, targeted tests, lint, and build pass.

## What changed

- `src/app/(merchant)/page.tsx` — template-style header with title/greeting, conditional view-store button, and preserved server-rendered period tablist/notices.
- `src/app/(merchant)/dashboard.tsx` — rebuilt on shared UI inventory:
  - key-stats grid (`StatCard`) using `MoneyText`;
  - sales-breakdown card with `SimpleTabs`;
  - funnel card with CSS-only bucketed progress bars;
  - inventory stat grid (active links + links with activity);
  - leading-products and recent-activity cards using `EmptyState`, `Badge`, `MoneyText`, and a "View all" link.
- `src/app/(merchant)/loading.tsx` — `StatGridSkeleton` + `CardSkeleton` matching dashboard row order.
- `src/i18n/dictionaries/merchant-dashboard/{en,pt-BR}.ts` — new keys for greeting, view-all, no-sales, checkout attempts, and links-with-activity.
- `src/app/(merchant)/page.test.tsx` — updated assertions for new markup, empty states, and view-all link.
- `src/app/globals.css` — added merchant stat-grid/progress/funnel classes and removed obsolete merchant header/facts/empty rules.

## Preserved contracts

- Period negotiation: `DEFAULT_PERIOD = "7d"`, invalid/absent → fallback to `7d`.
- Exact-decimal currency formatting via `formatCatalogPrice`; unlabeled currency still appends the dictionary label.
- Storefront-link gating: `storefrontEnabled && storefrontSlug !== null`.
- Analytics service, authorization, and `formatDashboardRate` unchanged.

## Checks run

- `pnpm exec eslint` on touched files: passed (no errors).
- `pnpm typecheck`: passed.
- `pnpm test -- src/app/(merchant)/page.test.tsx`: 9/9 passed.
- `pnpm lint`: passed with 60 pre-existing warnings (0 errors).
- `pnpm check`: started; `typecheck` and `lint` passed, full `vitest run` completed all visible suites without failures but hung during teardown before `build`. The hang appears unrelated to this change; `pnpm build` was run independently and passed.
- `pnpm build`: passed.

## Notes / follow-up

- The full `pnpm check` test teardown hang is not caused by the merchant dashboard changes (targeted tests pass and no failures were reported in the full run). It may be an existing open-handle issue in the test suite.
- No analytics service, authorization, or backend contracts were modified.

## Judge Dredd verdict — 005_closing

- **Verdict:** `aprovado`
- **Mode:** `differential`
- **Reason for differential mode:** the worktree contains uncommitted changes belonging to the in-flight task `12.6.1-remodel-public-payment-link-checkout` (checkout pages, checkout-related `globals.css` rules, `copy-field.tsx`, `inventory.json`, checkout dictionaries, and `tests/checkout.evidence.spec.ts`). This verdict covers only the deliverables of `12.5.1`; the unrelated work must not be merged with this task.

### Findings

1. **Worktree contamination (process note, not a task defect).** Files outside `12.5.1` scope are modified in the same worktree:
   - `src/app/pay/[identifier]/*`
   - `src/app/globals.css` (`.checkout-*`/`.checkout-v2*` rules)
   - `src/components/ui/copy-field.tsx`
   - `src/components/ui/inventory.json`
   - `src/components/ui/money-text.tsx`
   - `src/i18n/dictionaries/checkout/*`
   - `tests/checkout.evidence.spec.ts`
   - **Severity:** process risk — ensure the integration commit for `12.5.1` includes only the task-owned files.

2. **`MoneyText` `size="large"` token change.** The shared component changed from `text-[length:var(--type-stat)]` to `text-2xl`. If this change ships with `12.5.1`, it is a shared-component edit outside the strict task scope, but it does not break any `12.5.1` contract.
   - **Severity:** suggestion / low; verify with the owner of `12.6.1` during its own closing gate.

3. **`InventoryStatGrid` uses a 4-column grid for two stats.** The grid class `merchant-dashboard__stat-grid--4` is applied to only two `StatCard`s. Functionally correct, but may leave empty space on large viewports.
   - **Severity:** nit; visual only.

### Decision

Task `12.5.1-remodel-merchant-dashboard` satisfies its acceptance criteria: the merchant dashboard matches the template composition, preserves period fallback (`7d`), exact-decimal currency formatting, unlabeled-currency fallback, storefront-link gating, analytics service behavior, authorization, and notice handling. Tests pass. Approve and integrate only the task-owned files listed in **What changed**.
