# Front F02 — Server-safe compositions — [[12.2.3-rebuild-shared-visual-components]]

- **Delivery:** production equivalents for reachable static empty, exact-display, identity, QR-frame, skeleton, statistic, status and timeline presentation.
- **Scope:** generic presentational compositions and focused tests; no interactivity, business mapping or route consumption.
- **Owner:** agent.
- **Owns:** `src/components/ui/{empty-state,money-text,monogram,qr-display,skeletons,stat-card,status-badge,timeline}.tsx` and same-basename focused tests.
- **May read:** F01 primitive APIs; parity records/template counterparts; frontend spec, DESIGN, token/asset contracts and current route examples.
- **Must not edit:** F01/F03 files, package/lock, `src/data-directory/**`, routes, shell, specimen, dictionaries, specs/DOX/evidence, template, token/brand/media/domain code.
- **Depends on:** F01.
- **Expected input:** stable primitive exports and asset paths plus obligation IDs for the eight owned responsibilities.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]], [[.agents/skills/clean-code-change/SKILL|clean-code-change]], [[.agents/skills/react-best-practices/SKILL|vercel-react-best-practices]].
- **Criteria:** C01, C03-C06.

## Execution contract

- Accept localized/formatted/redacted strings, inert nodes and exact payload display only; never import i18n providers, mock/domain DTOs, QR/payment generation, or money arithmetic.
- Keep every owned file server-renderable and avoid barrel exports; hoist immutable maps and static asset registries.
- Status tone always carries text plus icon/shape; numeric facts use the semantic mono/tabular role without parsing amounts.
- QR composition owns only accessible frame/caption/alternative presentation; generating or trusting payment payloads stays with existing domain owners.
- Loading preserves final geometry; empty/error/recovery states are present only where applicable, with explicit N/A entries otherwise.
- Do not recreate primitive styles with page-local classes or introduce framer-motion/chart dependencies.

## Result

- **Status:** complete.
- **Commit/artifact:** atomic F02 branch commit on `task/12.2.3-f02-server-compositions`.
- **Files changed:** the eight owned server compositions and their eight same-basename focused tests.
- **Deviations:** none. Static owners remain server-renderable; QR generation, localization, business mappings and global contracts remain outside this front.
- **Evidence:** 17 focused assertions pass across 8 test files; Prisma generation followed by `tsc --noEmit` passes; focused ESLint, design-token checker, `git diff --check`, and ownership check against `5bdf523` pass. The local official shadcn metadata retry was DNS-blocked (`EAI_AGAIN`) without writes; this front consumes the F01-reviewed installed primitives at its pinned base.
