# Plan defense — [[12.2.3-rebuild-shared-visual-components]]

- **Stage:** 002_planning · **Owner:** agent planner

## Contestable decisions

| # | Decision | Adopted choice | Rejected alternative | Why | What would falsify it |
|---|----------|----------------|----------------------|-----|-----------------------|
| 1 | Inventory authority | Treat the 20 reachable parity sources and 187 obligations as a closed production ownership graph. | Copy the generated template/registry inventory wholesale. | Reachability preserves the supplied design while preventing unused generated sources and duplicate owners from entering production. | The inventory checker finds a reachable obligation without exactly one capable production owner, or an excluded source is required by a real consumer. |
| 2 | Directory ownership | Converge table, native-GET filtering, and cursor pagination in the existing `DataDirectory`. | Add parallel `DataTable`, `FilterBar`, or page-number owners modeled on the template. | One owner preserves the server-authoritative keyset, bounded-query, six-state, and responsive accessibility contracts. | Directory or consumer tests show the reachable presentation cannot be expressed without breaking those contracts or creating a distinct responsibility. |
| 3 | Interactive foundation | Compose official Radix/nova and Sonner primitives, adapted to the project's semantic tokens and six-theme resolver. | Reproduce the template's hand-rolled interactions or accept registry visual defaults and `next-themes`. | Official behavior supplies focus/keyboard/live-region semantics while local tokens remain the sole visual and theme authority. | Focus, keyboard, dismissal, live-feedback, theme, or dependency tests fail under the adapted primitives and pass only with another owner. |
| 4 | Rendering boundary | Keep static compositions server-renderable and isolate only interaction-specific behavior behind minimal serializable client props. | Clientify the shared vocabulary or pass domain/store/provider objects into it. | This retains the established server-first runtime and prevents presentation owners from acquiring business authority. | A reachable responsibility cannot be implemented or tested from formatted/redacted props without clientifying static content or importing a domain owner. |
| 5 | Evidence boundary | Prove focused component behavior plus current-consumer regression here; leave the complete component/state/locale specimen to 12.2.4. | Expand 12.2.3 into route/specimen construction. | The roadmap assigns specimen completeness separately, while this task can objectively prove ownership, behavior, and regression safety without widening its write set. | Any delivered owner lacks executable focused coverage or cannot be exercised sufficiently by current-consumer evidence to satisfy C01-C09. |
