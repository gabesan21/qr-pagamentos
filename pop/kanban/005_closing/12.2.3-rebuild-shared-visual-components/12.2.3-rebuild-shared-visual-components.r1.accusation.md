# Accusation — [[12.2.3-rebuild-shared-visual-components]]

- **Stage:** 005_closing (act 1) · **Owner:** devil's advocate

## Execution axis — criteria rerun

| # | Plan criterion | Verification executed | Result | Evidence |
|---|----------------|-----------------------|--------|----------|
| 1 | C01 Closed anti-drift ownership | `node scripts/check-shared-ui-inventory.mjs`; focused inventory test | passed | `187` obligations, `20` owners, `49` exclusions, `9` additions; mutation tests passed. |
| 2 | C02 Faithful primitive foundation | focused component suite; source/API audit | passed | 17 focused files / 52 tests passed; target/focus compilation and dependency pins passed. |
| 3 | C03 Complete reachable vocabulary | inventory map; focused static/client suite; parity checker | passed | 20 owners mapped once and `FRONTEND_PARITY_OK records=2230`; state dispositions present. |
| 4 | C04 Server-first exact inputs | `pnpm typecheck`; source-boundary tests and diff audit | passed | TypeScript and boundaries passed; static F02 owners have no client directive or domain imports. |
| 5 | C05 Accessible interaction and motion | focused suite plus test-body inspection | failed | `modal.test.tsx:4-24`, `simple-tabs.test.tsx:1-26`, and `copy-field.test.tsx:7-37` inspect source/SSR but do not execute focus, keyboard, dismissal, async feedback, or restoration. |
| 6 | C06 Directory contract preserved | directory focused test plus full consumer suite | passed | Directory assertions passed; aggregate `pnpm check` passed 1,944 tests and built 51 pages. |
| 7 | C07 Dependency and scope discipline | lock/name-only diff audit | passed | Only `input-otp@1.4.2` and `sonner@2.0.7` entered; no route, shell, specimen, server, domain, token, or excluded source edit. |
| 8 | C08 Aggregate regression gates | `pnpm frontend-parity:check`; `pnpm check`; `git diff --check`; `pop_validate.py` | passed | 2,230 parity records, 1,944 tests, lint 0 errors, build 51 pages, clean diff, valid vault. |
| 9 | C09 Fresh visual evidence | `pnpm design-system:evidence:verify`; manifest/source audit; four representative captures | failed | Verifier passes, but its source set at `scripts/verify-design-system-evidence.mjs:45-52` omits eight changed primitives. |

### Execution objections

| Severity | Objection | Evidence | Remedy |
|----------|-----------|----------|--------|
| blocking | C05 claims executable interaction coverage, but the focused tests only search source strings or server markup; the new custom modal/confirmation, copy, and tabs behavior can regress while the gate stays green. | `src/components/ui/modal.test.tsx:4-24`; `src/components/ui/simple-tabs.test.tsx:1-26`; `src/components/ui/copy-field.test.tsx:7-37`; no owner appears in the current `/design-system` evidence surface. | Add tests that mount and operate the owners, proving keyboard selection, focus trap/restoration, allowed/blocked dismissal, pending deduplication, failure retention/retry, and live feedback. |
| blocking | `StatCard` renders a direct numeric value in the display font instead of the required semantic mono/tabular role. | `src/components/ui/stat-card.tsx:20` uses `font-heading`; F02 requires mono/tabular numeric facts at `subtasks/12.2.3-rebuild-shared-visual-components.g02-server-compositions.md:18` and `DESIGN.md:299`. | Render the statistic value with the numeric font and tabular numerals (or require a numeric-typography child contract) and add a regression assertion. |
| blocking | C09's hash boundary does not bind all changed current primitives, so the verifier cannot prove that the captures correspond to the delivered `badge`, `card`, `field`, `input`, `native-select`, `spinner`, `table`, and `textarea` sources. | `scripts/verify-design-system-evidence.mjs:45-52,125-131`; the manifest audit reported all eight paths `UNBOUND` while `pnpm design-system:evidence:verify` still passed. | Include every changed current primitive in the evidence source inventory, recapture the matrix, and verify the regenerated manifest. |

## Decision axis — attack on the defense

| Severity | Decision attacked | Objection | Evidence | Remedy |
|----------|-------------------|-----------|----------|--------|
| blocking | 5 — Evidence boundary | Its declared falsifier is observed: delivered interactive owners lack executable focused behavior coverage, while current-consumer evidence does not exercise them. Deferring the complete specimen does not defer C05 behavior proof. | Defense line 13; `modal.test.tsx:4-24`, `simple-tabs.test.tsx:1-26`, and `copy-field.test.tsx:7-37`; no matching owner import in `src/app/design-system/page.tsx` or `tests/design-system.evidence.spec.ts`. | Keep the route/specimen boundary, but add focused executable interaction coverage inside 12.2.3 and rerun C05/C09 evidence checks. |

## Result

- **Execution axis:** 3 objections (3 blocking).
- **Decision axis:** 1 objection (1 blocking).
- **Surface covered:** original What/Why; C01-C09; `8b665b3..02777f1` production, tests, contracts, generated parity/evidence changes; 24-capture verifier and four representative light/dark narrow/wide captures; all five defense decisions in order.
- **Not examined:** `pop/specs/gate-adversarial.md` was absent; the other 20 PNGs were verifier-covered but not individually inspected.
