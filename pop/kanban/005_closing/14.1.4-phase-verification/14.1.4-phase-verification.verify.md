# Judgment — [[14.1.4-phase-verification]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-07

- **Strategy:** full — this is the phase-verification task: its product *is* the evidence, and the only honest way to judge a suite is to re-run it and read every realigned assertion against the contract that falsified it.
- **Surface:** the 17 files of the diff (14 test files, 3 source repairs), the three ledgers of 14.1.1/14.1.2/14.1.3 as the phase checklist, and the whole aggregate gate re-run by me (`tsc`, the full `lint` chain including tokens/brand/fonts, `vitest run`, `next build`).
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Conformance with the objective and the specs

| # | Criterion | Mode | Verification | Result | Evidence |
|---|-----------|------|--------------|--------|----------|
| C1 | Aggregate gate green | evidence | I ran the four stages of `pnpm check` directly | passed | `tsc --noEmit` exit 0; `eslint .` 0 errors / 64 pre-existing warnings; tokens/brand/fonts/design-tokens exit 0; `next build` exit 0 |
| C2 | No coverage lost | evidence | `vitest run` plus a diff sweep for removed `it(` and added `.skip/.todo` | passed | 2010 passed / 28 skipped / 0 failed (baseline 1981/28); the single removed `it(` is the rename at `(merchant)/page.test.tsx:188`, the case itself preserved |
| C3 | Drill-down assertion realigned | evidence | `admin/payment-links/v2/[id]/page.test.tsx:83` | passed | asserts `href="/admin/orders?filter.link=abcdefghijklmnopqrstuvwx"`; no `?link=` left in the file |
| C4 | Settings return targets asserted | evidence | storefront/checkout-policy/logo `route.test.ts` | passed | 8 assertions now require `/settings?<code>#settings-{identity,policy,store}`; no `/` target survives |
| C5 | Referer allowlist covered | evidence | `settings-return-target.test.ts` (10 cases) plus 3 route cases | passed | covers `/settings`, `/`, X-Forwarded-Host precedence, foreign host, `//evil`, unlisted path, non-http scheme, unparseable, absent, and query/fragment discarded |
| C6 | Lifecycle forms reachable by id | evidence | `catalog/products/[id]/page.test.tsx:71-72` | passed | asserts `id="product-active-toggle"` and `id="product-archive"` in the rendered markup |
| C7 | Archived stays read-only | evidence | same file, :86-92 | passed | denies `action="/products"`, `name="action"`, `name="id"`, `name="version"`, `type="submit"` and both ids |
| C8 | Failure return derived from action/id | evidence | `products/route.test.ts:60-92` | passed | create→`/catalog/products/new?products=failed`; update/active/archive→percent-encoded id with `conflict\|failed`; unknown action→`/catalog?products=failed` |
| C9 | Destructive confirm renders the real form | evidence | `admin/accounts/page.test.tsx:100`, `[id]/page.test.tsx:116,127` | passed (with suggestion S1) | the three call sites emit `action=<unchanged> method="post"`; I read `accounts/page.tsx:120` and `[id]/page.tsx:306,502` — all three go through `DestructiveActionForm` |
| C10 | Account menu always offers Sign out | evidence | `shell-account-menu.test.tsx` | passed | queried through the accessibility tree: Sign out `menuitem` for both roles, `/logout` form `method="post"` with `role="none"`; Profile only when provided |
| C11 | Settings surface renders once, with nav | evidence | `settings-surface.test.tsx` | passed | 7 sections × 2 locales: exactly one `id="settings-<x>"` and one heading each (Nautt the known exception); a nav link for every id |
| C12 | In-phase repairs applied | evidence | diff reading | passed | `encodeURIComponent(id)` at `products/route.ts:30`; `role="none"` at `shell-navigation.tsx:117`; `storefront`/`checkout-policy` dropped from the type and from `ownerNotice` at `(merchant)/page.tsx:31,42` |
| C13 | Catalog failure returns keep the typed values | human | force the failures in the browser | human checklist | — |
| C14 | Settings page visual pass | human | 320/768/1440 × pt-BR/en | human checklist | — |
| C15 | Dialogs and menu keyboard-operable | human | Tab/Enter/Escape | human checklist | — |

### Implementation quality

| Severity | Finding | Evidence | Required fix |
|----------|---------|----------|--------------|
| suggestion | S1 — the C9 assertions do not separate a gated form from a bare one: `DestructiveActionForm` renders `type="button"` and no submit, so reverting the gate to a direct submit keeps `action=… method="post"` green. The behavior 14.1.3 repaired is not pinned by a test. | `src/app/admin/accounts/page.test.tsx:100`; `src/app/admin/accounts/destructive-confirm.tsx:40-43` | assert the trigger is `type="button"` (or that no `type="submit"` exists in that form) alongside the current assertion |
| nit | N1 — C11 asks that "every nav link href matches a section id"; the test proves the inverse over a fixed list of 7, so an extra link to a nonexistent anchor would still pass. | `src/app/(merchant)/settings/settings-surface.test.tsx:57-62` | extract the `href="#settings-…"` values from the markup and check each against the ids actually rendered |

### Scope and integration

- [x] Changes respect F01's `Owns` and `Must not edit` — 17 paths: tests beside the sources they cover plus the three repair files named in the plan; nothing under `pop/specs/**`, `prisma/**`, `container/**`, `install/**`, `src/i18n/**`.
- [x] Dependencies were not implemented opportunistically — 14.1.1–14.1.3 were already delivered; this diff only realigns, covers, and applies the three repairs their judgments recorded.
- [x] The phase's `verify: phase` criteria are consumed here: C12 from 14.1.1, C13 from 14.1.2 and the `?link=` handoff from 14.1.3 are all asserted above (C3, C4, C6–C8).
- [x] Affected specs, documentation and DOX contracts: no durable contract moved — only a dead branch removed and a percent-encoding added. Residual spec sync stays with the orchestrator (see follow-ups).

## Verdict

- **Decision:** approved → delivery and closing.
- **Blockers:** none.
- **Plan defect:** none.
- **Suggestions/nits:** S1 (strengthen the destructive-gate assertion) and N1 (direction of the nav check) — traceable follow-ups, neither blocks the phase.
- **Human checklist:** **C13** — force an invalid create, an update with a stale `version`, and a category failure: each returns to its own form with the typed values and the opaque alert, never landing on `/catalog`. **C14** — open `/settings` at 320/768/1440 px in both locales, saving each section: the return arrives scrolled to its section clear of the top bar with the notice inside it; the language switcher from `/` still returns to `/`; from 1024px the sticky nav tracks the section with no horizontal overflow; no section titled twice (Nautt is the known exception). **C15** — Tab/Enter/Escape through the account menu and each destructive dialog: every control reachable with a visible focus ring, focus trapped in the dialog, Escape cancels without posting, targets ≥44px.
- **Summary:** the card asked to run the phase's aggregate gate, realign the assertions the three repairs falsified, cover what the checklist owed, and repair whatever the suite caught inside the phase's reach. That is exactly what landed: 8 assertions realigned to the new settings targets and to `filter.link`, +29 new tests concentrated on the repaired behaviors (Referer allowlist, catalog failure return, archived read-only, Sign out, settings surface, draft), three pinpoint repairs, and the whole gate green on my own run. No defect above the phase's reach surfaced.

<!-- pop-verdict round=1 decision=aprovada -->
