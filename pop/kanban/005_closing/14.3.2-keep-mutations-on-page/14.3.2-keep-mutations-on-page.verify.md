# Julgamento — [[14.3.2-keep-mutations-on-page]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-09-08

- **Estratégia:** differential — primeira rodada, sem retorno anterior; `size: L` elevou a profundidade de leitura (rotas, páginas, primitivo de draft, specs e template).
- **Superfície:** diff integrado 9473b43b..12cbb26d (37 paths, `:!pop/kanban`), specs/DESIGN tocados, primitivo `form-draft`, template `docs/template/app/src/pages/links|profile`.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Pedido original

O "O quê/Por quê" do card é atendido em três das quatro áreas: pedidos, links e profile/settings voltam à própria página com toast pelo bridge, e a troca de senha deixa de deslogar. A promessa "preserva o input digitado na falha" **não** se realiza nos formulários de link (ver bloqueante).

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| C01 | Comentário/outcome voltam a `/orders/v2/<id>` | evidência | `src/app/orders-v2/[id]/route.ts` | passou | as três saídas usam `/orders/v2/${id}` (:27,:29,:31); `id` içado antes do `try` (:11) |
| C02 | Create cai no detalhe do link criado; falha em `/links/new` | evidência | `src/app/payment-links-v2/route.ts` | passou | `created.id` (:34) é o UUID que `payment-link-v2-view.ts:168` exige; falha via `createFailureTarget` (:8-10) com `from` codificado |
| C03 | Edit/activate/deactivate voltam a `/links/v2/<id>` | evidência | `src/app/payment-links-v2/[id]/route.ts` | passou | :40,:42 detalhe; catch escolhe `/links/v2/<id>/edit` só para `action=edit` (:46-47) |
| C04 | Nenhuma mutação cai em `/` | evidência | `src/app/payment-links/**`, `(merchant)/page.tsx` | passou | V1 → `/links` (route.ts:15,17; [id]/route.ts:14,16); Alert morto e searchParam `payment-links` removidos do dashboard (diff de `(merchant)/page.tsx`) |
| C05 | Identity/TOTP em `/profile`; Nautt em `/settings#settings-connection` | evidência | rotas Nautt e profile | passou | âncora em todas as saídas (nautt-credentials/route.ts:28-35, register:21-25, reset:16-21); seção `id="settings-connection"` existe (settings-surface.tsx:102) |
| C06 | Senha rotaciona a sessão sem deslogar | evidência | `profile/password/route.ts`, `auth/profile.ts` | passou | serviço intacto (`replaceCredentialAndRevokeSessions`, profile.ts:157) e só depois `create`/`createMfaVerified` (route:34-38); cookie idêntico ao login (route:9-15 vs `login/submit/route.ts:11`); sem `qr_locale` — logado resolve pela preferência (`layout.tsx:24`) |
| C07 | Destino nunca vem de path do cliente | evidência | diff das rotas | passou | destinos vêm de route param, `action` ou do registro criado; `from` só reentra em `/links/new` codificado; nenhum `fetch`/JSON novo (ver sugestão sobre codificação) |
| C08 | Um notice site e um draft guard por formulário; input preservado | evidência | páginas, componentes e guards | **falhou** | guards de link não alcançam os campos — ver bloqueante; orders/profile ok (campos nomeados e não controlados) |
| C09 | Primitivo em `src/app/form-draft.tsx` | evidência | diff | passou | `catalog-draft` removido, zero referências restantes; importadores migrados (product-form.tsx:6, category-row-actions.tsx:5, categories/page.tsx:6) |
| C10 | Documentos de contrato refletem os destinos | evidência | `pop/specs/*`, `DESIGN.md` | passou | administrative-foundation.md:80,82; catalog-and-payment-links.md:76; checkout-and-order-lifecycle.md:64; DESIGN.md:410-412 — batem com as rotas integradas |
| C11 | `pnpm lint` e `pnpm typecheck` | evidência | evidência registrada pelo orquestrador | passou | `tsc` exit 0; eslint 0 erros no branch integrado |
| C12 | Suíte cobre destinos, bridges e rotação | phase | task de verificação da phase | registrado (phase) | asserções invalidadas listadas e entregues à phase-verification |
| C13 | Walkthrough manual das quatro áreas | humano | checklist | humano | ver checklist |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | O `FormDraftGuard` dos formulários de link é inerte: no edit todos os campos declarados nascem sem `name` até ficarem dirty, e no create os campos de valor fixo nem estão no DOM (o `kind` volta ao default `PRODUCT_LINES` após a falha), então `form.elements.namedItem(...)` devolve `null` e o guard limpa o rascunho em vez de restaurá-lo | `src/app/(merchant)/links/v2/[id]/edit/page.tsx:70-76` e `new/page.tsx:88-94` contra `link-v2-form.tsx:70,86,220,249` e `dirty-input.tsx:21` | Restaurar a partir do estado do próprio formulário client (precedente `ProductForm`, `src/app/(merchant)/catalog/product-form.tsx:187-205`): repor `kind`/valores e marcar dirty para que o restaurado também poste |
| sugestão | `draftKey="payment-link-v2-edit"` não é escopado pelo id do link, ao contrário do padrão de F02 (`order-v2-comment-edit-${orderId}-${comment.id}`): um rascunho do link A pode reaparecer no formulário do link B | `src/app/(merchant)/links/v2/[id]/edit/page.tsx:71` | Escopar a chave pelo id ao aplicar o reparo acima |
| sugestão | Route param interpolado cru no `Location`, enquanto o precedente citado no plano e as rotas de admin usam `encodeURIComponent` | `src/app/orders-v2/[id]/route.ts:27,29,31`, `payment-links-v2/[id]/route.ts:40,42,46` vs `src/app/products/route.ts:30`, `admin/users/[id]/identity/route.ts:15` | Codificar o id na interpolação (mesma linha); não há open redirect — o destino continua ancorado no path fixo |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente (37 paths; conjuntos negados intocados).
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [x] Critérios `verify: phase` registrados para a checklist da phase.
- [x] Specs, documentação e contratos DOX afetados estão coerentes.

## Veredito

- **Decisão:** **reparo dirigido** (delta pontual em F03; não consome contador de devolução).
- **Bloqueantes:** restauração de rascunho inerte nos formulários de link (C08).
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** escopar `draftKey` do edit por id; codificar o route param nos redirects.
- **Checklist humana:** C13 — nas quatro áreas, confirmar toast e retorno à própria página; confirmar que a falha de comentário/outcome, de identity e de link mantém o texto digitado (o bridge apaga o param da URL no mount, então o caso do link é o teste decisivo); confirmar que a troca de senha mantém a sessão e que com TOTP ativo nenhuma re-verificação é pedida.
- **Resumo:** os destinos, os notices e a rotação de sessão foram entregues conforme o pedido e as specs; falta apenas a preservação real do input nos formulários de link, corrigível sem mudar a estratégia.

## Delta da devolução

- **Tipo:** execucao (pontual).
- **Critérios afetados:** C08 — o rascunho dos formulários de link nunca é restaurado após falha.
- **Frentes afetadas:** `F03` — apenas os dois formulários de link.
- **Frentes intactas:** `F01`, `F02`, `F04` — aprovadas, permanecem integradas; não reexecutar.
- **Ação esperada:** mover a restauração para dentro de `LinkV2Form` (estado `kind`/dirty), escopando a chave do edit pelo id; nenhuma rota, spec ou contrato muda.

### Adendo — conferência do reparo dirigido 1 (`e2e221f6`)

C08 no delta: o restore passou para dentro do `LinkV2Form` (:191-216), com estado controlado, `kind` reposto no create, `fixedDirty`/`expiresAtDirty` forçados — o valor restaurado volta a ser nomeado e é postado; chave escopada por link (:189). Em carga sem notice a omissão por dirty segue intacta. **Não passa:** o patch introduziu dois defeitos próprios.

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| bloqueante | Client component passou a importar um módulo `server-only` — o build do Next falha ("'server-only' cannot be imported from a Client Component module"); `tsc`/eslint não veem isso | `link-v2-form.tsx:1,14` → `directory-query.ts:1`; regra em `next/dist/build/webpack-config.js:1146-1157` | Declarar a chave no próprio client, como `product-form.tsx:30` e `order-v2-views.tsx:38` |
| bloqueante | `handleSubmit` grava os três membros financeiros mesmo intocados (:218-226); no restore isso força `fixedDirty` (:208) e o retry passa a postá-los, disparando a trava de tentativa que a spec promete não tocar | `payment-links-v2/[id]/route.ts:7-13` + `auth/payment-link-v2.ts:261-266`; `catalog-and-payment-links.md:76` | Persistir/marcar dirty só o que ficou dirty (`!editing || fixedDirty`; idem `expiresAtDirty`) |

Segundo reparo dirigido da rodada (teto 2): um terceiro achado reclassifica o delta como difuso → 004.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=src/app/(merchant)/links/link-v2-form.tsx frentes=F03 intactas=F01,F02,F04 -->
