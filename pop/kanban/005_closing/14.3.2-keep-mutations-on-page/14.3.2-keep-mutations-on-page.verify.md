# Judgment — [[14.3.2-keep-mutations-on-page]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-08

- **Strategy:** differential — first round, no prior return; `size: L` raised reading depth (routes, pages, draft primitive, specs, template).
- **Surface:** integrated diff 9473b43b..12cbb26d (37 paths, `:!pop/kanban`) plus the two directed repairs `e2e221f6` and `1990ddb2`; specs/DESIGN touched; template `docs/template/app/src/pages/{links,profile}`.
- **Returns:** execution 0 of 2 · plan 0 of 2 (directed repair is not a kanban route and consumes no counter).

### Original request

The card's What/Why is met: order comments and outcomes, link create/edit/lifecycle, and profile/settings mutations all return to the page that submitted them with a bridged toast, and the password change no longer signs the merchant out. Input preservation on failure required two directed repairs (addenda below) before it held for the link forms.

### Conformance with objective and specs

| # | Criterion | Mode | Verification | Result | Evidence |
|---|-----------|------|--------------|--------|----------|
| C01 | Comment/outcome return to `/orders/v2/<id>` | evidence | `src/app/orders-v2/[id]/route.ts` | passed | all three exits use `/orders/v2/${id}` (:27,:29,:31); `id` hoisted above the `try` (:11) |
| C02 | Create lands on the created link's detail; failure on `/links/new` | evidence | `src/app/payment-links-v2/route.ts` | passed | `created.id` (:34) is the UUID `payment-link-v2-view.ts:168` requires; failure via `createFailureTarget` (:8-10) with `from` encoded |
| C03 | Edit/activate/deactivate return to `/links/v2/<id>` | evidence | `src/app/payment-links-v2/[id]/route.ts` | passed | :40,:42 detail; the catch picks `/links/v2/<id>/edit` only for `action=edit` (:46-47) |
| C04 | No mutation lands on `/` | evidence | `src/app/payment-links/**`, `(merchant)/page.tsx` | passed | V1 → `/links` (route.ts:15,17; [id]/route.ts:14,16); dead Alert and `payment-links` searchParam removed from the dashboard |
| C05 | Identity/TOTP on `/profile`; Nautt on `/settings#settings-connection` | evidence | Nautt and profile routes | passed | anchor on every exit (nautt-credentials/route.ts:28-35, register:21-25, reset:16-21); section `id="settings-connection"` exists (settings-surface.tsx:102) |
| C06 | Password change rotates the session instead of signing out | evidence | `profile/password/route.ts`, `auth/profile.ts` | passed | service untouched (`replaceCredentialAndRevokeSessions`, profile.ts:157), new session only afterwards (route:34-38); cookie identical to login (route:9-15 vs `login/submit/route.ts:11`); no `qr_locale` write — a signed-in principal resolves locale from the stored preference (`layout.tsx:24`) |
| C07 | No destination derived from a client-supplied path | evidence | route diff | passed | destinations come from the route param, `action`, or the created record; `from` only re-enters the fixed `/links/new`, encoded; no `fetch`/JSON mutation added |
| C08 | One notice site and one draft guard per form; typed input restored | evidence | pages, components, draft guards | passed after directed repair | orders/profile fields are plain and named; the link forms required repairs 1 and 2 — see addenda |
| C09 | Primitive lives in `src/app/form-draft.tsx` | evidence | diff | passed | `catalog-draft` removed with zero remaining references; importers migrated (product-form.tsx:6, category-row-actions.tsx:5, categories/page.tsx:6) |
| C10 | Contract documents state the delivered destinations | evidence | `pop/specs/*`, `DESIGN.md` | passed | administrative-foundation.md:80,82; catalog-and-payment-links.md:76; checkout-and-order-lifecycle.md:64; DESIGN.md:410-412 match the integrated routes |
| C11 | `pnpm lint` and `pnpm typecheck` | evidence | evidence recorded by the orchestrator | passed | `tsc` exit 0, eslint 0 errors; the second repair also recorded one successful `next build` |
| C12 | Suite covers destinations, bridges, rotation | phase | phase-verification task | recorded (phase) | invalidated assertions listed and handed over |
| C13 | Manual walkthrough of the four areas | human | checklist | human | see checklist |

### Implementation quality

| Severity | Finding | Evidence | Required fix |
|----------|---------|----------|--------------|
| blocking (fixed in repair 1) | The link forms' `FormDraftGuard` was inert: on edit every declared field is unnamed until dirty, and on create the fixed-amount fields are not even mounted (kind resets to `PRODUCT_LINES`), so the lookup returned `null` and the guard cleared the draft instead of restoring it | `edit/page.tsx:70-76`, `new/page.tsx:88-94` against `link-v2-form.tsx:70,86,220,249` and `dirty-input.tsx:21` | Restore from the form's own state (precedent `catalog/product-form.tsx:187-205`) |
| blocking (fixed in repair 2) | Repair 1 made a client component import a `server-only` module, which fails the Next build | `link-v2-form.tsx:1,14` → `directory-query.ts:1`; rule at `next/dist/build/webpack-config.js:1146-1157` | Keep the notice key local, as `product-form.tsx:30` and `order-v2-views.tsx:38` do |
| blocking (fixed in repair 2) | Repair 1 persisted the three financial members even when untouched and forced `fixedDirty` on restore, so a retry after a failed edit posted them and tripped the checkout-attempt lock | `link-v2-form.tsx:218-226` against `payment-links-v2/[id]/route.ts:7-13`, `auth/payment-link-v2.ts:261-266`, `catalog-and-payment-links.md:76` | Persist and mark dirty only what was actually dirtied |
| suggestion | Route params are interpolated raw into `Location`, while the precedent the plan cites and the admin routes encode them; no open redirect, since the path prefix is fixed | `orders-v2/[id]/route.ts:27,29,31`, `payment-links-v2/[id]/route.ts:40,42,46` vs `products/route.ts:30`, `admin/users/[id]/identity/route.ts:15` | Encode the id in place |

### Scope and integration

- [x] Changes respect each front's `Owns` and `Must not edit` (37 paths; denied sets untouched).
- [x] Dependencies were not implemented opportunistically by consumers.
- [x] `verify: phase` criteria recorded for the phase checklist.
- [x] Affected specs, documentation, and DOX contracts are coherent.

## Verdict

- **Decision:** **approved** — two directed repairs conferred in the addenda below (pinpoint route, no return counter consumed).
- **Blocking:** none at round end; the three raised (inert draft, `server-only` import, untouched member named) were fixed by repairs 1 and 2.
- **Plan defect:** none.
- **Suggestions/nits:** encode the interpolated route params in the two V2 mutation routes.
- **Human checklist:** C13 — in each of the four areas confirm the toast and the on-page return; confirm a failed comment/outcome, identity, and link submit keeps the typed text (the bridge strips its own param on mount at `notice-toast.tsx:37-39`, so the browser is where this is settled); confirm the merchant stays signed in after a password change and, with TOTP active, is not asked to re-verify.
- **Summary:** destinations, notices, and session rotation match the request, the template, and the specs; the only real gap — input preservation on the link forms — was closed inside this round.

### Addendum 1 — directed repair 1 (`e2e221f6`)

Restore moved into `LinkV2Form` (:191-216) over controlled state, `kind` restored on create, `fixedDirty`/`expiresAtDirty` forced, key scoped per link (:189): restored values are named again and therefore posted, and on a load without the notice dirty-omission still holds. Did not pass: the patch introduced the `server-only` import and the untouched-member regression recorded above.

### Addendum 2 — directed repair 2 (`1990ddb2`)

Both delta items are closed and no third material finding appears. The key is local again (`link-v2-form.tsx:21`, identical to `directory-query.ts:23`) and no client component reaches the `server-only` graph — `next build` completed in the recorded evidence. The draft now persists the financial trio only under `!editing || fixedDirty` and expiry only under `expiresAtDirty` (:220-232), with restore raising each flag only from its own marker (:212-214), so a failed edit never renames an untouched member and the attempt lock (`auth/payment-link-v2.ts:261-266`) behaves exactly as the spec promises. Round closed: **approved**.

<!-- pop-verdict round=1 decision=aprovada -->
