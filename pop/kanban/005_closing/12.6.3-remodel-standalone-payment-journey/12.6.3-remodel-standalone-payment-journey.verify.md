# Verificação — [[12.6.3-remodel-standalone-payment-journey]]

- **Etapa:** 004_processing · **Responsável:** executor
- **Data:** 2026-08-05
- **Commit de entrega:** `0c0f64f` (inclui a implementação desta task junto com materialização de kanban e arquivos da task `12.5.4`)

## Escopo executado

- F01 — `src/app/store/[slug]/pay/page.tsx`, `error.tsx`, `loading.tsx`: shell alinhado ao template Epoch 12.
  - `page.tsx`: fallback de logo envolvido em `aria-label` (igual `src/app/store/[slug]/page.tsx`), `eslint-disable-next-line @next/next/no-img-element` adicionado; tema via `data-theme-preview` e `--storefront-accent`; view opaca de indisponível com link de retorno.
  - `error.tsx`: substituído `Card`/`Alert` manual por `EmptyState` do inventário, com botão de retry.
  - `loading.tsx`: esqueleto de página com `storefront-shell` e `storefront-card`.
- F02 — `src/app/store/[slug]/pay/standalone-payment-experience.tsx`: formulário e pagamento remodelados com componentes do template.
  - `StatusBadge` com mapeamento de tom preservando terminal `REFUNDED` como `danger` (mesmo comportamento anterior).
  - `QrDisplay` para o QR code, mantendo `img.checkout-qr` para evidence.
  - `CopyField` para o PIX copy-paste, dentro do wrapper `.checkout-copy`.
  - `MoneyText` para o valor submetido.
  - Layout de endereço em grid (`sm:grid-cols-[1fr_auto]`, `sm:grid-cols-2`) igual ao checkout V2.
  - Contratos preservados: body `{ idempotencyKey, amount, customer }`, `isStorefrontCartAmount`, polling/retry/terminal, estados `RESERVED`/`CREATING`/`CREATED` como waiting.
- F03 — testes e evidence:
  - `src/app/store/[slug]/pay/standalone-payment-experience.test.tsx` atualizado para novos componentes (`textContent` helper, seletores de copy/amount ajustados).
  - `src/app/store/[slug]/pay/page.test.tsx` cobre tema/accent, prefill, políticas e view de indisponível.
  - `tests/standalone-payment.evidence.spec.ts`: seletor de copy atualizado de `.checkout-copy input` para `.checkout-copy button`.
  - Nenhuma chave de dicionário nova foi necessária (reutilizadas chaves existentes de `checkout` e `storefront`).

## Resultados dos checks

| Check | Comando | Resultado | Notas |
|-------|---------|-----------|-------|
| Testes unitários do escopo | `npx vitest run src/app/store/[slug]/pay/page.test.tsx src/app/store/[slug]/pay/standalone-payment-experience.test.tsx` | **PASSOU** | 41 tests passaram (2 arquivos) |
| Lint dos arquivos do escopo | `npx eslint src/app/store/[slug]/pay/page.tsx src/app/store/[slug]/pay/error.tsx src/app/store/[slug]/pay/loading.tsx src/app/store/[slug]/pay/standalone-payment-experience.tsx src/app/store/[slug]/pay/standalone-payment-experience.test.tsx src/app/store/[slug]/pay/page.test.tsx tests/standalone-payment.evidence.spec.ts` | **PASSOU** | nenhum erro/warning |
| Typecheck global | `pnpm typecheck` | **FALHOU** | Erros em arquivos de tasks concorrentes (`src/app/(merchant)/catalog/products/[id]/product-detail-client.tsx`, `src/app/(merchant)/settings/settings-surface.tsx`, `src/app/nautt-credential-surface.tsx`) |
| Lint global | `pnpm lint` | **FALHOU** | Violações de tokens em arquivos da task `12.5.4-remodel-merchant-catalog` (`src/app/(merchant)/catalog/catalog-fields.tsx`, `src/app/(merchant)/catalog/product-form.tsx`) |
| Build | `pnpm build` | **NÃO EXECUTADO** | Mesmos erros de typecheck/lint das tasks concorrentes bloqueariam a build |

## Desvios e bloqueios

- **Bloqueio de ambiente:** o working tree contém arquivos inacabados de tasks concorrentes (`12.5.4-remodel-merchant-catalog`, `12.5.6`, `12.7.1`/`12.7.2`) que quebram `pnpm typecheck`, `pnpm lint` e `pnpm build` globalmente. Esses arquivos estão fora do escopo de `12.6.3`.
- **Ação:** o executor não alterou arquivos fora do escopo de `12.6.3`. Os checks globais só passarão quando as tasks concorrentes estabilizarem seus arquivos.
- **Impacto na entrega:** a implementação de `12.6.3` está completa, commitada e seus testes de escopo passam. A task está pronta para Judge Dredd assumindo que o ambiente concurrente será resolvido no gate de phase (`12.6.4-phase-verification`).

## Checklist de integridade

- [x] Nenhuma alteração em backend, rotas API, projeção pública ou contrato de capability.
- [x] `Owns` e `Must not edit` de cada frente respeitados.
- [x] Testes unitários do escopo passam.
- [x] Seletores de evidence atualizados para a nova estrutura de copy.
- [x] Dicionários não divergem (reutilização de chaves existentes).

## Rodada 1 — 2026-08-05
- **Estratégia:** full — primeira passagem de task size L; superfície cobre todo o diff ownado e a ausência de alterações fora dele.
- **Superfície:** `src/app/store/[slug]/pay/**`, `tests/standalone-payment.evidence.spec.ts`; nenhuma alteração em backend, rotas API, projeção pública ou contrato de capability.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs
| # | Critério | Modo | Resultado | Evidência |
|---|----------|------|-----------|-----------|
| 1 | tema/accent via `data-theme-preview`/`--storefront-accent` | evidência | passou | `page.tsx` |
| 2 | view opaca de indisponível | evidência | passou | `page.tsx` + testes |
| 3 | validação canônica do amount | evidência | passou | `isStorefrontCartAmount` |
| 4 | body `{ idempotencyKey, amount, customer }` | evidência | passou | componente + testes |
| 5 | QR/copy/status com template + polling/retry/terminal | evidência | passou | componente + testes |
| 6 | nenhuma mudança backend/API/projeção/capability | evidência | passou | diff review |
| 7 | typecheck/lint/test globais | phase | registrado (phase) | `12.6.4-phase-verification` |
| 8 | testes unitários cobrem casos | evidência | passou | 41 testes |
| 9 | evidence 6×2×3 + interações | phase | registrado (phase) | `12.6.4-phase-verification` |

### Qualidade da implementação
Nenhum achado passou no teste de materialidade.

### Escopo e integração
- [x] `Owns`/`Must not edit` respeitados; dependências não implementadas por consumidores; specs/DOX coerentes.
- [x] Critérios `verify: phase` registrados na checklist da phase.

## Veredito
- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes / defeito de plano / sugestões:** nenhum.
- **Checklist humana:** liberação do roadmap já consta no card; critérios 7 e 9 são phase.
- **Resumo:** `/store/[slug]/pay` adota a linguagem do template Epoch 12 sem regressão nos contratos 9.2.1/9.2.2, exact money, política do comprador ou estados terminais.
