# Verificação — 12.5.3-remodel-merchant-payment-links

- **Etapa:** 005_closing · **Responsável:** Judge Dredd
- **Tipo de gate:** differential · **Tamanho:** L · **Critical:** false
- **Veredito:** devolvido a 004_processing (`execucao`)

## Critérios de aceite avaliados

| # | Critério | Resultado | Evidência |
|---|----------|-----------|-----------|
| 1 | Diretório `/links` segue template e mantém estados derivados V2, filtros, busca e legado V1 | ❌ Não atendido | `src/app/(merchant)/links/page.tsx` não foi remodelado no working tree; permanece na versão pré-12.5 (coluna `orders`, layout antigo, sem `CopyField` no identificador). |
| 2 | Detalhe, sumário, badges, timeline e indisponíveis seguem template e preservam redação | ⚠️ Parcial | `link-v2-views.tsx` e páginas de detalhe foram remodeladas, mas não podem ser aprovadas isoladamente enquanto F01 e a invasão de ownership não forem resolvidas. |
| 3 | Criação `/links/new` segue template e preserva kind/type/pair e editor de linhas | ✅ Em linha | `new/page.tsx` e `link-v2-form.tsx` foram remodelados; contratos de criação mantidos. |
| 4 | Edição segue template, preserva CAS, dirty-field omission e fork de nova versão | ⚠️ Parcial | `edit/page.tsx` remodelado, mas o teste `page.test.tsx` foi ajustado para esperar o identificador na tela, sinalizando que a página pode estar expondo mais do que o template prevê; requer revisão. |
| 5 | Lifecycle e compartilhamento seguem template e preservam confirmação/redação | ✅ Em linha | `link-v2-actions.tsx` usa `ConfirmDialog`, mantém POST para `/payment-links-v2/[id]` com CAS. |
| 6 | Drill-down de pedidos segue template e mantém vínculo forçado ao identificador do link | ✅ Em linha | Páginas e componentes de `v2/[id]/orders/**` foram remodelados; vínculo ao link pai preservado. |
| 7 | Nenhum serviço, rota, schema ou fronteira de redação alterada | ❌ Bloqueante | Arquivos de outras tasks foram alterados no mesmo working tree (`src/app/store/[slug]/**`, `src/app/(merchant)/orders/**`, `src/app/orders/**`, `src/i18n/dictionaries/orders-directory/**`). |
| 8 | Componentes compartilhados são usados por todas as páginas merchant de links | ✅ Em linha | `EmptyState`, `CopyField`, `MoneyText`, `ConfirmDialog` adotados nas páginas de links. |

## Achados principais

### 1. Invasão de ownership — storefront (task 12.6.2)

O diff integrado altera `src/app/store/[slug]/page.tsx`, `error.tsx`, `loading.tsx`, `storefront-experience.tsx`, `page.test.tsx`, `storefront-experience.test.tsx` e `src/app/globals.css`. Esses arquivos estão fora do `owns` de 12.5.3 e são escopo exclusivo de `12.6.2-remodel-public-storefront-and-cart` (em `004_processing`).

- Exemplo: `src/app/store/[slug]/page.tsx` substitui `Card`/`Alert` por `EmptyState`, adiciona `isEmpty`, usa `Link` para ação de retry, etc.
- Não há dependência funcional legítima entre a remodelação de links merchant e a página pública de vitrine.

**Severidade:** bloqueante — viola o princípio de write sets disjuntos e o plano da task (dependência com 12.6.2 apenas como contexto, nunca como sobreposição de arquivos).

### 2. Invasão de ownership — merchant orders (task 12.5.2)

O diff altera `src/app/(merchant)/orders/page.tsx`, `[id]/page.tsx`, `v2/[id]/page.tsx`, `src/app/orders/order-v2-views.tsx`, `src/app/orders/order-views.tsx` e `src/i18n/dictionaries/orders-directory/*`. Esses arquivos são escopo de `12.5.2-remodel-merchant-order-management` (em `004_processing`).

- `src/app/(merchant)/orders/page.tsx` adiciona filtro `STANDALONE`, `emptyAction` apontando para `/links/new`, `CopyField` no identificador do link, `MoneyText` na coluna de valor e `canonicalFilterQuery`.
- `src/i18n/dictionaries/orders-directory/*` adiciona `orderV2DirectorySourceStandalone`.

Mesmo que o uso de `EmptyState`/`MoneyText`/`CopyField` em views compartilhadas seja explicável por componentes comuns, as mudanças comportamentais em `orders/page.tsx` e nos dicionários de orders são claramente fora do escopo.

**Severidade:** bloqueante — o plano de 12.5.3 afirma explicitamente "write sets disjuntos; nenhuma sobreposição em `src/app/(merchant)/orders/**` ou `src/orders/**`".

### 3. F01 — Diretório de links não entregue no working tree

`src/app/(merchant)/links/page.tsx` não aparece no diff do working tree contra `HEAD`. O arquivo permanece sem as mudanças de template previstas na frente F01 (coluna `orders` ainda presente, identificador não copiável, layout de heading/botão não remodelado).

Há indícios de que F01 foi trabalhada em outro contexto (commit `186441a` contém alterações em `links/page.tsx`), mas essas alterações não estão no working tree atual. O teste `links/page.test.tsx` teve expectativas removidas (`>7</td>`, `Orders`), o que reforça que o executor esperava uma versão remodelada que não está presente.

**Severidade:** bloqueante — critério 1 não é atendível sem reexecutar F01.

### 4. V1 e fronteiras de redação preservadas

- Nenhuma alteração em `prisma/schema.prisma`, rotas API (`src/app/api/**`), serviços de link (`src/auth/payment-link*`) ou contratos de lifecycle.
- V1 `OwnerPaymentLinkManagement` e trigger permanecem intactos.
- Nenhum `id` interno de link é exposto em lugar de `sharePath`; o diff em `link-v2-views.tsx` inclusive substitui `value={link.id}` por `value={link.identifier}` no `CopyField` do detalhe.

**Severidade:** não bloqueante — ponto positivo, mas não compensa os achados acima.

## Delta da devolução

- **Tipo:** `execucao`
- **Critérios afetados:** 1, 7 (diretamente); 2, 4, 8 (indiretamente, por contaminação do diff e necessidade de rebase visual).
- **Frentes que reentram:**
  - **F01 — Diretório de links:** reexecutar do zero; garantir que `src/app/(merchant)/links/page.tsx` esteja no diff do working tree e alinhado ao template.
  - **F02 — Visões compartilhadas:** revisar após F01; validar que `link-v2-views.tsx` exporta o mínimo necessário sem expor internos.
  - **F03 — Criação/edição:** revisar o teste ajustado em `edit/page.test.tsx` que passa a esperar o identificador na tela.
  - **F04 — Detalhe/lifecycle:** revisar após F01/F02; validar layout final com componentes compartilhados.
  - **F05 — Drill-down de pedidos:** revisar após F02; garantir que não arraste alterações em `src/app/orders/**` ou `src/app/(merchant)/orders/**`.
- **Frentes intactas:** nenhuma — a F01 está ausente e as invasões tocam componentes compartilhados, então todo o escopo deve ser revalidado.

## Recomendação ao executor

1. Descartar do working tree de 12.5.3 todas as alterações que pertencem a `12.5.2` e `12.6.2`.
2. Reexecutar F01 em `src/app/(merchant)/links/page.tsx` e `page.test.tsx`.
3. Garantir que o diff final contenha apenas arquivos dentro de `src/app/(merchant)/links/**`, componentes declarados no plano (`link-v2-views.tsx`, `link-v2-form.tsx`, `link-lines-editor.tsx`, `link-v2-actions.tsx`, `share-copy.tsx`, `dirty-input.tsx`) e `src/i18n/dictionaries/payment-links-directory/**`.
4. Se componentes compartilhados (`EmptyState`, `MoneyText`, `CopyField`, `ConfirmDialog`) forem necessários e ainda não existirem na base, adicioná-los em local neutro (ex.: `src/components/ui/`) sem alterar consumers de outras tasks.

## Rodada 2 — reavaliação pós-execução

- **Data:** 2026-08-05
- **Tipo de gate:** differential (reentrada de `execucao`)
- **Veredito:** aprovado para integração

### Critérios de aceite reavaliados

| # | Critério | Resultado | Evidência |
|---|----------|-----------|-----------|
| 1 | Diretório `/links` segue template e mantém estados derivados V2, filtros, busca e legado V1 | ✅ Atendido | `src/app/(merchant)/links/page.tsx` remodelado no diff: coluna `identifier` com `CopyField`, layout de heading/botão alinhado ao template, filtros `state`/`type`/`kind`, busca, page size, paginação e seção V1 preservada. |
| 2 | Detalhe, sumário, badges, timeline e indisponíveis seguem template e preservam redação | ✅ Atendido | `link-v2-views.tsx` refatorado em cards (`SummaryCard`, `CompositionCard`, `TimelineCard`, etc.), `StatusBadge`/`Timeline`/`CopyField`/`MoneyText`, redação via dicionário, view indisponível opaca. |
| 3 | Criação `/links/new` segue template e preserva kind/type/pair e editor de linhas | ✅ Atendido | `new/page.tsx` e `link-v2-form.tsx` mantêm selects de `compositionKind`, `currencyPairId`, `linkType` no create; `LinkLinesEditor` serializa `lines` JSON; aviso de nova versão para `?from=`. |
| 4 | Edição segue template, preserva CAS, dirty-field omission e fork de nova versão | ✅ Atendido | `edit/page.tsx` e `link-v2-form.tsx` postam `action=edit` + `version`; campos financeiros usam `omitUntilDirty`/`DirtyInput`/`LinkLinesEditor` com gate de sujeira; breadcrumb oferece `/links/new?from=<id>`. |
| 5 | Lifecycle e compartilhamento seguem template e preservam confirmação/redação | ✅ Atendido | `link-v2-actions.tsx` usa `ConfirmDialog`, posta para `/payment-links-v2/[id]` com `action`/`version`; `v2/[id]/page.tsx` adiciona share/abrir público. |
| 6 | Drill-down de pedidos segue template e mantém vínculo forçado ao identificador do link | ✅ Atendido | `v2/[id]/orders/page.tsx` sobrescreve `filter.link` pelo `identifier` do link pai; `[orderId]/page.tsx` só renderiza se `paymentLinkV2Identifier === link.identifier`. |
| 7 | Nenhum serviço, rota, schema ou fronteira de redação alterada | ✅ Atendido | Diff restrito a `src/app/(merchant)/links/**`, componentes declarados no plano e `src/i18n/dictionaries/payment-links-directory/**`. Nenhuma alteração em `src/app/api/**`, `prisma/schema.prisma`, serviços de link ou rotas. |
| 8 | Componentes compartilhados são usados por todas as páginas merchant de links | ✅ Atendido | `CopyField`, `MoneyText`, `StatusBadge`, `ConfirmDialog`, `WorkspaceHeading`, `DataDirectory`, `Timeline`, `Card` adotados nas páginas de links. |

### Foco da devolução anterior — resolução

| Achado anterior | Estado | Evidência |
|-----------------|--------|-----------|
| F01 ausente no working tree | ✅ Resolvido | `src/app/(merchant)/links/page.tsx` aparece no diff com +56 linhas; testes ajustados. |
| Invasão de ownership em `src/app/(merchant)/orders/**`, `src/app/orders/**`, `src/i18n/dictionaries/orders-directory/**` | ✅ Resolvido | `git status` desses paths retorna vazio; nenhuma alteração no diff integrado. |
| Invasão de ownership em `src/app/store/[slug]/**` e `src/app/globals.css` | ✅ Resolvido | `git status` desses paths retorna vazio; nenhuma alteração no diff integrado. |
| Teste `edit/page.test.tsx` esperando identificador na tela | ✅ Em linha | O breadcrumb exibe `#{link.identifier}` (público, não o id interno); o teste valida redação segura. |

### Checks reportados pelo executor

- `pnpm lint`: 0 errors, 57 warnings (warnings pré-existentes, nenhum novo no escopo).
- `pnpm typecheck`: pass.
- `pnpm test -- --run`: 1980 passed / 28 skipped; processo sai com exit code 1 por **unhandled error pré-existente** em `src/components/ui/input-otp.test.tsx`.
- `pnpm build`: pass.

O arquivo `src/components/ui/input-otp.test.tsx` não consta no diff integrado; seu último commit tocante é `40af2d4` (`12.4.1 remodel administrator dashboard`), anterior a esta task. O erro é pré-existente e fora do escopo.

### Achados desta rodada

- **Nenhum achado bloqueante.** O delta da devolução foi integralmente endereçado.
- **Sugestão não bloqueante:** o `OrdersSummaryCard` em `v2/[id]/page.tsx` recebe valores estáticos (`confirmed: 0`, `volume: "0.00"`). Isso atende ao escopo atual (F04 é detalhe/lifecycle/compartilhamento, não analytics completo), mas um follow-up pode enriquecer o resumo com dados reais de pedidos quando houver task de analytics de links.

### Decisão

Aprovo a task para integração. O escopo está restrito, os contratos estão preservados, os critérios de aceite estão atendidos e os checks reportados são consistentes com a base do projeto.

## Notas de julgamento

- Gate realizado por leitura do diff integrado (`git diff`) e worktree, conforme instrução.
- Nenhum teste ou check foi reexecutado.
- O teste de materialidade foi aplicado: na rodada 1, as invasões de ownership e a ausência de F01 eram materiais o suficiente para impedir aprovação; na rodada 2, todos os itens foram sanados.
