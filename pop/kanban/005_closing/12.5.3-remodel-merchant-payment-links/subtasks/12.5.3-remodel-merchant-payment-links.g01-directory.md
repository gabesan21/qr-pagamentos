# Frente F01 — Diretório de links — [[12.5.3-remodel-merchant-payment-links]]

- **Entrega:** `/links` remodelada com layout/filtros/tabela/paginação do template.
- **Escopo:** Apresentação da página de diretório V2 e legado V1.
- **Responsável:** agent.
- **Owns:** `src/app/(merchant)/links/page.tsx`, `loading.tsx`, `directory-copy.ts`, `links-notices.tsx`.
- **May read:** `src/data-directory/**`, `src/auth/payment-link-v2-view.ts`, template `LinksDirectory.tsx`.
- **Must not edit:** `src/auth/payment-link-v2-view.ts`, `src/data-directory/**`, serviços, rotas, modelos.
- **Depends on:** nenhuma.
- **Skills:** ui-change, clean-code-change
- **Critérios:** 1, 7

## Contrato de execução

- Manter `DataDirectory` como dono das responsabilidades de `DataTable`/`FilterBar`.
- Preservar estados derivados V2 (`active`/`inactive`/`expired`/`paid`), filtros `state`/`type`/`kind`, busca `q`, page sizes e seção legado V1.
- Aplicar layout template: heading, ações primárias, tabela/diretório, paginação canonical e estados empty/error/invalid-query.
- Dependência ou entrada ausente → `BLOCKED`.

## Resultado

- **Status:** concluída | BLOCKED.
- **Arquivos alterados:** <lista contra Owns>.
