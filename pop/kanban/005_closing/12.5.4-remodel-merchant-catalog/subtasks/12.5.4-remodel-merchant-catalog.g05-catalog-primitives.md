# Frente F05 — Primitivas locais do catálogo — [[12.5.4-remodel-merchant-catalog]]

- **Entrega:** módulo `src/app/(merchant)/catalog/catalog-fields.tsx` com `Banner`, `Breadcrumb`, `SegmentedControl`, `ImageField`, `SectionCard` e wrapper `NativeSelect` usados por F02/F03.
- **Escopo:** primitivas visuais locais ao catálogo; sem alterar componentes globais.
- **Responsável:** agent.
- **Owns:** `src/app/(merchant)/catalog/catalog-fields.tsx`.
- **May read:** `src/components/ui/*`, `src/design-system/tokens/*`.
- **Must not edit:** `src/components/ui/*` (salvo caso localizado e justificado).
- **Depends on:** nenhuma.
- **Skills:** [[pop/skills/ui-change|ui-change]]; [[pop/skills/clean-code-change|clean-code-change]].
- **Critérios:** 6.

## Contrato de execução

- Entregar somente o escopo e critérios desta frente.
- Saber parar em até 2 tentativas para falhas de ambiente; reportar `ambiente` e reclassificar para `verify: user`.
- Dependência ausente → `BLOCKED` ao orquestrador.
- Não alterar caminhos fora de `Owns`.

## Resultado

- **Status:**
- **Commit/artefato:**
- **Arquivos alterados:**
- **Desvios:** nenhum
- **Evidência:**
