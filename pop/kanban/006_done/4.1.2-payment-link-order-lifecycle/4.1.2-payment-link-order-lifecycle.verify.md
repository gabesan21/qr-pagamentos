---
id: 4.1.2-payment-link-order-lifecycle
stage: 005_verifying
verified_at: 2026-07-21
verified_by: critic_412_005_r2
decision: approved
critical: true
---

# 4.1.2-payment-link-order-lifecycle — Verification

## Evidence

| Criterion | Result | Evidence |
|---|---|---|
| AC1 | pass | `pnpm db:generate` and `pnpm db:contract-check` passed. The new immutable migration has the fixed snapshot columns/checks, owner/link/product composite FKs, nullable same-owner one-to-one provider relation, and single-use claim constraints. |
| AC2 | pass | `src/orders/payment-link-order.test.ts` exercises all five policy tuples, normalization/rejections, opaque malformed-link handling, and the Prisma creation query derives policy, price, pair and expiry from locked persisted rows. |
| AC3 | pass | Focused test and diff review show a server-only `src/orders/` seam only; no changed route, UI, public resolver, provider client, credential read, URL, log, or projection. |
| AC4 | pass | Focused concurrent fake-store tests passed. Source review confirms status/local-version/current-state predicates, attached same-owner provider UUID fence, claim-before-confirmation, reusable bypass, and irreversible claim across refund. |
| AC5 | resolved in round 2 | The lifecycle spec now records the yolo 003 round-2 approval without changing a contract requirement or implementation. The implementation and diff scope remain sound; the aggregate gate and diff check were re-run. |

## Runs

- `pnpm test -- src/orders/payment-link-order.test.ts` — pass (72 files / 617 tests in the configured suite; 1 file and 6 tests skipped by suite configuration).
- `pnpm db:generate` — pass.
- `pnpm db:contract-check` — pass.
- `pnpm check` — pass (typecheck, lint, test, production build). The sandboxed build could not bind Next's local CSS-worker port; the required rerun outside the sandbox passed.
- `git diff --check develop...HEAD` — pass; changed paths are limited to the planned migration/schema/DB contract check and `src/orders/` seam/tests.
- Deferred by authorization: Docker/container checks, `pnpm db:test`, and browser/runtime verification.

## Findings — round 1 (historical)

- **blocking — truthful lifecycle spec / AC5:** [`checkout-and-order-lifecycle.md`](../../../specs/checkout-and-order-lifecycle.md) lines 5 and 7 still say the task 4.1.2 revision is “awaiting renewed yolo 003 approval” and “not approved or implemented yet.” The card's approved round-2 response and Log record that yolo 003 approval on 2026-07-21. This makes the linked durable contract false and violates AC5's explicit “Specs and DOX remain truthful” requirement. Update the spec's status/update fact to reflect the approved contract and current verification state, then return to 005; no code change is indicated by this finding.

## Round 2 — AC5 correction

- **AC5 blocker resolved:** [`checkout-and-order-lifecycle.md`](../../../specs/checkout-and-order-lifecycle.md) now says `approved — task 4.1.2 contract revision approved by yolo 003, round 2` and accurately records the limited status correction. Its requirements, scope, and open item are unchanged; no application code, migration, or provider/recovery surface changed.
- **Independent re-review:** the candidate `task/4.1.2-payment-link-order-lifecycle` diff remains limited to the planned schema, new immutable migration, DB contract assertions, and server-only `src/orders/` seam/tests. The owner/composite-FK, fixed-snapshot, authoritative status/version/current-state fence, and single-use claim-before-confirmation contracts remain intact. No new blocking, suggestion, or nit finding.
- **Re-run:** `pnpm test -- src/orders/payment-link-order.test.ts` — pass (72 files / 617 tests, 1 file / 6 tests skipped by suite configuration); `pnpm db:generate` — pass; `pnpm db:contract-check` — pass; `pnpm check` — pass outside the sandbox (the build worker requires a local port); `git diff --check develop...HEAD` — pass. Docker/container checks, `pnpm db:test`, and browser/runtime verification remain deferred by explicit user authorization.

## Decision

Approved. The previous AC5 blocker is fixed with no adverse contract or implementation effect. This critical task's persistence/lifecycle seam, migration, focused regression coverage, and permitted automated gates pass independent review.

Returned by independent reviewer (yolo) — 2026-07-21

Verified by independent reviewer (yolo) — 2026-07-21
