# Verification — [[9.3.2-complete-public-link-terminal-states]]

- **Stage:** 005_verifying · **Responsible:** independent yolo critic (strong gate)
- **Mode:** `full` — mandatory (`critical: true`). Surface: the whole integrated diff `8de44ef..HEAD` (5 commits) plus re-run gates.
- **Worktree:** `pop/worktrees/9.3.2-complete-public-link-terminal-states` (branch `task/9.3.2-complete-public-link-terminal-states`, HEAD `aa6561a`)

## 1. Original request first (card What/Why)

**Met.** The card asks: an explicit already-paid view for consumed one-time links, the same opaque view for inactive/expired/malformed/otherwise-unavailable links, no lifecycle disclosure — because shareable URLs need deterministic safe terminal behavior.

Delivered and proven:

- Consumed `SINGLE_USE` V2 link renders a branded, localized, server-rendered paid view (`src/app/pay/[identifier]/public-checkout-v2-page.tsx` `PublicCheckoutV2PaidPage`, discriminated outcome from `src/checkout/public-checkout-v2-presentation.ts:133-144`). No form, no polling client, no mutation affordance (page tests assert zero `field-group`, evidence asserts `form === false`).
- Every other non-payable state keeps the one opaque unavailable view: unit tests (`missing link`, missing members, malformed identifier) plus runtime evidence `unavailable-unknown` / `unavailable-inactive` / `unavailable-expired` opaque assertions in run `20260727032258`.
- No lifecycle disclosure: paid DTO is type-narrowed to `{ composition, currencyCode, branding }`; claim-keying is proven at runtime by flipping every consumed order to `REFUNDED` before capture and by one consumed link with past expiry — the paid view persists (`paid-consumed-single-use.refundedOrder === true`, `paid-consumed-expired-link.claimKeyed === true`).
- `REUSABLE` links unchanged (consumption requires `linkType === "SINGLE_USE"` + claim; grid checkout captures prove the normal view). V1 surfaces byte-stable (below).

## 2. Diff audit (`8de44ef..HEAD`)

### Claim-first precedence and redaction (highest risk — pass)

- `src/checkout/public-checkout-v2-presentation.ts:191-196`: the claim read is `singleUseSettlement: { select: { paymentLinkV2Id: true } }` — **existence only**. `claimedAt`, `orderV2Id`, and `ownerId` are never selected, so there is nothing to smuggle into the record; the record gains exactly one `consumed: boolean`.
- Claimed links bypass the active/expiry/line-product gates (`if (!consumed && ...)` guards) — the sanctioned design: deactivation, expiry, or line-product deactivation after payment never flips the paid view (spec 9.3.2 "Claim-keyed consumption").
- Unclaimed links keep every 9.3.1 gate: the query-level `active: true` / `expiresAt > now` filters moved to application-level checks with **identical semantics for unclaimed links** (`!link.active || (expiresAt !== null && expiresAt <= now)` → null; line-product-active gate unchanged). Verified line-by-line against `git show 8de44ef`.
- Redaction absence assertions cover the paid DTO: the adversarial unit test smuggles `orderState: "REFUNDED"`, `claimedAt`, `orderV2Id` into the record and asserts none survive serialization, plus a 20-member forbidden-key list (`checkoutPolicy`, `consumed`, `singleUseSettlement`, `settledAt`, `capability`, `attempt`, `verifier`, `retry`, `provider`, pair UUIDs, `version`, `expiresAt`, `linkType`, `identifier`, …). Page tests assert the rendered markup carries no UUID, no ISO timestamp, no `REFUNDED`, no `checkoutPolicy`.
- "Never paid-by-inference": `never maps an unclaimed record to the paid branch` proves an unclaimed record resolves to `checkout`.

### V1 byte-stability (pass)

- `page.tsx`: the V1 branch line (`if (presentation) return <main className="checkout-shell">…<PublicCheckoutForm …/>`) is an untouched context line; only the V2 fall-through changed (additive `kind === "paid"` branch).
- `git diff --name-only 8de44ef..HEAD -- src/` contains only: `src/app/pay/[identifier]/{page.tsx,page.test.tsx,public-checkout-v2-page.tsx}`, `src/checkout/{public-checkout-v2-presentation.ts,.test.ts,AGENTS.md}`, `src/i18n/dictionaries/checkout/{en,pt-BR}.ts`. **Zero diffs** in routes, rate-limit/request-log inventories, `src/orders/*`, `src/auth/*`, `prisma/`, `scripts/check-design-tokens.mjs`, `package.json`, and every V1 file — criterion 3 holds.

### Deviation 1 — paid component inside `public-checkout-v2-page.tsx` via `CheckoutV2Shell` (accepted)

- Compared `git show 8de44ef:…/public-checkout-v2-page.tsx` against the new shell + `CheckoutV2CompositionFacts` + `CheckoutV2Total`: the extraction is **verbatim** — same elements, classes, order, and expressions; `displayName` and `total` computations moved unchanged. The checkout view markup is structurally byte-identical; the pre-existing page-test markup assertions were not relaxed (only the mocked service shape changed).
- The single whitelisted `style={{ "--storefront-accent": presentation.branding.accentColor }}` declaration lives once in the shared shell; the paid view adds no inline style. `node scripts/check-design-tokens.mjs` → **exit 0** (re-run by me); the whitelist file itself is untouched.

### Deviation 2 — `scripts/verify-checkout-evidence.mjs` edited (accepted)

- The contract stays **closed**: hard-coded counts `36 base / 72 paid / 19 state / 127 total / 130 files`, a 127-name expected capture set, manifest SHA-256 binding, assertions SHA-256 binding, and per-source hash re-verification — all equality assertions, no loosening.
- The flipped assertion (`unavailable-consumed-single-use.opaque` → `paid-consumed-single-use` with `composition/total/currency/logo/form/refundedOrder`) is exactly the delivered behavior — consumed `SINGLE_USE` now renders the paid view; the opaque regression coverage was *strengthened* with new `unavailable-inactive` and `unavailable-expired` assertions.
- Run harness `scripts/run-checkout-evidence.mjs` untouched (absent from diff); `tests/checkout.evidence.spec.ts` changes are additive scenarios plus the REFUNDED-flip seeding and the timeout raise.

## 3. Gates re-run by this critic (explicit exit codes, worktree HEAD `aa6561a`)

| Gate | Result |
|------|--------|
| `pnpm typecheck` | **exit 0** |
| `pnpm test` | **exit 0** — 172 files passed, 1473 tests passed, 26 skipped (8 db files skipped by design) |
| `pnpm checkout:evidence:verify` (run `20260727032258`) | **exit 0** — "Verified public checkout evidence 20260727032258" |
| `node scripts/check-design-tokens.mjs` | **exit 0** |
| `pnpm lint` | **exit 0** — 0 errors, 44 warnings all in files untouched by this task (e.g. `tests/merchant-dashboard.evidence.spec.ts`) |
| `pnpm check` / `pnpm db:test` / docker evidence run | accepted on 004 report + artifact spot-check: the verify script re-hashes the manifest, assertions, and every source file against the current tree and passed; two paid captures visually inspected (below) |

## 4. i18n parity and dictionary wording (pass)

- Exactly +3 keys per locale (`checkoutPaidBadge`, `checkoutPaidHeading`, `checkoutPaidDescription`) in `src/i18n/dictionaries/checkout/{en,pt-BR}.ts`; parity enforced by the generic `dictionaries.test.ts` key-set tests (green in the re-run).
- Wording carries no timestamps and no lifecycle vocabulary: "This payment link is already paid" / "Este link de pagamento já foi pago", "one-time link was already used" — the sanctioned consumed bit only.

## 5. UI review of the paid view (pass — Nielsen/WCAG spot read)

Inspected `paid-lines-cashier-daylight-pt-BR-1440.png` and `state-pt-BR-paid-fixed-unbranded-1440.png` from run `20260727032258`:

- **Non-color paid marker:** `Badge variant="secondary"` with `CheckCircle2Icon` plus text ("Pago"/"Paid") — status is never color-only (WCAG 1.4.1).
- **Composition:** branded rail (logo via `/media/[identifier]` or the official merchant-fallback lockup), heading, one-time-use explanation, ruled composition summary, tabular exact total with display code (`Total 34.9 BRL`) / explicit unlabeled treatment; no form or mutation affordance; system feedback is immediate and unambiguous (Nielsen #1, #2).
- **Coverage:** grid binds 2 composition kinds × 6 themes × 2 locales × 375/768/1440 with axe/overflow/target/focus gates on all 127 captures (verify asserts `severeAxe` empty and no measured overflow).

## 6. Closeout consistency (pass)

- `pop/specs/checkout-and-order-lifecycle.md`: new "Public link terminal states (9.3.2 — delivered)" section pins the content boundary, claim-keying, and evidence extension; the 9.3.1 "Page" paragraph correctly re-points the consumed case to 9.3.2. `pop/specs/catalog-and-payment-links.md`: delivered-by note, no model/DTO/route wording changed.
- Root `AGENTS.md` V2 `/pay/[identifier]` contract line, `src/checkout/AGENTS.md` delivered-line, and `DESIGN.md` (paid-view composition + the 127-capture contract) all match the delivered behavior.
- **No conflict markers** anywhere in `pop/specs/`, `src/`, or the touched docs (the 003-flagged header markers in the checkout-lifecycle spec were an in-flight integration artifact; the 9.3.2 amendment lands on a clean file — grep for `<<<<<<<`/`>>>>>>>` returns nothing).

## Findings

1. **(nit)** Plan criterion 4 named "320px fit" for the paid view; the paid grid captures 375/768/1440 only — the 320px reflow capture exists solely for the checkout view (`state-pt-BR-checkout-320`). Mitigating: every paid capture carries a measured no-overflow assertion, and the paid view is strictly narrower than the checkout view (single summary column, no form) inside the same shell proven at 320px. No action required; noted for future evidence pairs.

No blocking findings. No suggestions that gate this delivery.

## Verdict

**APPROVE.** The card objective is met with disciplined redaction, exact claim-keying, V1 byte-stability, both declared deviations verified sound, and all re-run gates green.

verificado por revisor independente (yolo) — 2026-07-27
