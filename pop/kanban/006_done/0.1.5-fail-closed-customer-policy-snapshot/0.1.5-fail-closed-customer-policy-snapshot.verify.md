# Verificação e crítica — [[0.1.5-fail-closed-customer-policy-snapshot]]

- **Etapa:** 005_verifying · **Responsável:** revisor independente

## Rodada 1 — 2026-07-21

- **Estratégia:** full — `critical: true` (fronteira de exposição de dados do cliente) exige verificação full com re-run de toda a superfície.
- **Superfície:** diff `develop...HEAD` na worktree (commit `18d7b79`): `src/orders/order-view.ts` (branch default fail-closed), `src/orders/order-view.test.ts` (1 teste de regressão), `pop/specs/checkout-and-order-lifecycle.md` (1 linha no slice 4.3.1). Re-run: suite `src/orders/order-view.test.ts`, `pnpm lint`, `pnpm typecheck`, `pnpm test` completo, `pnpm build`. Auditoria de caminhos de exposição: grep de `toPolicySnapshot`/`customer:` em `src/`.
- **Devolução yolo:** 0 de 2.

### Conformidade com objetivo e specs

| # | Critério | Modo | Verificação executada | Resultado | Evidência |
|---|----------|------|------------------------|-----------|-----------|
| 1 | Unknown/corrupted policy exposes no customer data | re-run | `pnpm vitest run src/orders/order-view.test.ts` | passou | 13/13 testes verdes; novo teste `order-view.test.ts:87-90` passa `"NAME_EMAIL_CPF_ADDRESS_TAX_ID" as CheckoutDataPolicy` com `stored()` totalmente populado e exige `{ name: null, email: null, cpf: null, address: null }`. Reverter o default para a tupla cheia faria o teste falhar — o teste protege. |
| 2 | Known-policy behavior unchanged | re-run | mesma run + leitura do diff | passou | Tabela das cinco políticas (`order-view.test.ts:80`) e testes de stray members (`:84-85`) intactos no diff — apenas adição, nenhuma modificação. Branches das quatro políticas explícitas pré-existentes inalterados em `order-view.ts:83-86`; `NAME_EMAIL_CPF_ADDRESS` virou branch explícito (`:87`) com a mesma tupla de antes (nome, email, cpf, `storedAddress(stored)`). Enum confirmado em `payment-link-order.ts:6`: cinco membros, `NAME_EMAIL_CPF_ADDRESS` é o único que expõe endereço. |
| 3 | Aggregate gate green | re-run | `pnpm lint` + `pnpm typecheck` + `pnpm test` + `pnpm build` | passou com ressalva documentada | lint: 0 erros (11 warnings pré-existentes de unused vars, incluindo a desestruturação prévia de `order-view.ts:92`, fora do diff). typecheck: limpo. build: ok. `pnpm test`: 676 passed, 1 failed — única falha em `src/app/pay/[identifier]/page.test.tsx:42` (asserção de locale: espera "This payment link is unavailable", página renderiza pt-BR). Provada independente deste diff: `/pay` e `src/checkout/` importam apenas de `payment-link-order.ts` (não tocado), nunca de `order-view.ts`; falha pré-existente em develop corrigida pela task irmã 0.1.4, conforme registro do card. Não bloqueia. |
| 4 | Fail-closed invariant recorded | evidência | leitura do diff da spec | passou | Uma única frase adicionada ao slice 4.3.1 de `pop/specs/checkout-and-order-lifecycle.md:39`: "A persisted `checkoutDataPolicy` outside the closed enum exposes no customer data: the projection fails closed to the all-null `CustomerSnapshotV1`." — casa exatamente com o comportamento entregue (fall-through retorna `blank`). Nenhum outro texto da spec alterado. |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| — | Nenhum achado acionável. Correção: o fall-through `return blank` (`order-view.ts:88`) é inalcançável pelo tipo (união fechada) mas alcançável em runtime via string persistida/cast — exatamente o vetor que o teste espelha e que o comentário novo (`:80`) documenta. Complexidade inalterada; nomes e idioma consistentes; sem mudança de assinatura, caller, store ou rota. | `order-view.ts:79-89` | — |

### Escopo e integração

- [x] Alterações respeitam `Owns` e `Must not edit` de cada frente — exatamente os 3 arquivos de F01; `payment-link-order.ts`, `src/app/orders/**`, `src/app/admin/orders/**`, prisma e dicionários intocados.
- [x] Dependências não foram implementadas oportunisticamente por consumidores — `depends_on: []`, nada além do escopo.
- [x] Gate agregado passou após a integração — lint/typecheck/build verdes; única falha de teste é a pré-existente de `/pay` (fora do grafo de imports do diff), corrigida por 0.1.4.
- [x] Specs, documentação e contratos DOX afetados estão coerentes — spec atualizada com o invariante; contrato raiz (`policy-exact CustomerSnapshotV1`) permanece correto sem edição, como previsto no plano; não há `AGENTS.md` filho em `src/orders/`.

## Veredito

- **Decisão:** aprovada → 006_done.
- **Bloqueantes:** nenhum.
- **Sugestões/nits:** nenhum.
- **Resumo:** o objetivo — default fail-closed em `toPolicySnapshot` — foi entregue com mudança de um branch, um teste de regressão que falharia sob regressão, e uma linha de spec que registra o invariante. As cinco políticas reais mantêm comportamento idêntico e `NAME_EMAIL_CPF_ADDRESS` segue como única política que expõe endereço; nenhum outro caminho de exposição de dados do cliente existe fora de `toPolicySnapshot` (grep em `src/`). Escopo, spec e contratos DOX coerentes.

verificado por revisor independente (yolo) — 2026-07-21
