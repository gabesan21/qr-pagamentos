# Verification — [[6.1.1-define-panel-architecture-and-permissions]]

- **Stage:** 005_verifying
- **Reviewer:** independent yolo critic
- **Round:** 3
- **Mode:** full — mandatory for a critical task after returns; reviewed the original objective first, all three delivery commits, both prior blockers, linked durable specs, current route/authentication inventory, DOX, scope, and repository gates.
- **Delivery:** `291b4bd` repaired by `8cf135c` and `b31b244`

## Original request

**Met.** The durable contract now defines mutually exclusive `ADMIN` and merchant `USER` capabilities, role landings, current and planned route-family ownership, denial outcomes, public/sessionless and locale compatibility, and the shared-design versus role-specific-shell boundary needed by the panel rebuild.

## Prior blockers and full-surface evidence

- **Resolved — administrator projections:** `pop/specs/administrative-foundation.md:53-63` permits only separate, re-authorized, redacted, read-only administrator projections for orders, payment links, and analytics; it does not retain merchant-route/service access or owner mutations.
- **Resolved — current-route compatibility:** `pop/specs/administrative-foundation.md:64-65` retains `/admin-access` as the sole unprefixed administrator probe and makes `/admin/users/[id]/nautt-credentials` denial-only with no parsing or service/provider work.
- **Resolved — disabled-session semantics:** `pop/specs/administrative-foundation.md:64` and `AGENTS.md:88` now specify empty `401` whenever no active principal resolves, including disabled sessions, and empty `403` only for an active `USER`. This matches `src/auth/authorization.ts:18-32`, `src/app/admin-access/route.ts:7-13`, and the disabled-principal assertion in `src/auth/authorization.test.ts:9-21`.
- The route matrix covers the current authenticated merchant/admin/authentication inventory and planned dashboard, catalog, payment-link, order, profile/security, analytics, user-management, storefront, and system-settings families. `/design-system` remains governed as the non-business specimen by the linked design-system spec; public/sessionless identifiers, rate limits, protocol outcomes, unprefixed i18n, and `[lang]/*` `404` stubs remain outside both panels.
- The complete branch diff is limited to the planned owners: `AGENTS.md`, `pop/specs/administrative-foundation.md`, and `pop/specs/administrative-design-system.md`. No runtime, schema, dependency, route, generated artifact, or unrelated durable contract changed; root DOX and both specs agree.

## Findings

- No blocking, suggestion, or nit finding.

## Checks

- `python3 pop/scripts/pop_validate.py`; acceptance `rg` probes; `git diff --check`; complete diff/scope inspection — pass.
- Focused authorization and legacy-route tests — 3 files, 5 tests passed.
- `pnpm lint` — pass with 11 pre-existing warnings in unchanged `src/orders/order-view.ts`; `pnpm build` — pass outside the restricted sandbox after the sandboxed run could not bind the internal Turbopack worker port.
- `pnpm check` / `pnpm typecheck` retain the unchanged baseline failure at `src/app/storefront/route.test.ts:29`; `pnpm test` retains the unchanged `server-only` resolution failure and storefront locale expectation failure (723 passed, 6 skipped). None of those files or their inputs changed in this documentation-only delivery.

## Decision

**Approved → 006.** The original request and AC1–AC3 are met; AC4 is baseline-classified with all changed-surface and independently runnable gates clean. This approval ends round 3 without activating the circuit breaker.

verificado por revisor independente (yolo) — 2026-07-23
