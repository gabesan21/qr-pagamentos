# Verify — M-4.1-repair-test-baseline-determinism

- **Data:** 2026-07-24
- **Estratégia:** differential (task não crítica, size S, primeira rodada; superfície alterada é só teste/config — reexecutados os gates agregados na worktree e auditado o diff integral dos 4 arquivos)
- **Commit:** 3a870ee (branch `task/M-4.1-repair-test-baseline-determinism`)

## Pedido original

Reparar as três falhas pré-existentes do baseline de testes da develop e tornar `pnpm test` determinístico no checkout principal com worktrees de task presentes, sem mudança de comportamento de produção — **atendido**. As três falhas foram corrigidas na raiz e a causa da não-determinismo (vitest coletando testes duplicados de `pop/worktrees/**` porque o `exclude` customizado substituía os defaults) foi eliminada.

## Diff auditado

Apenas 4 arquivos, todos teste/config — nenhuma fonte de produção alterada:

1. `src/auth/sign-in.test.ts` — `vi.mock("server-only", () => ({}))`: correto. O teste importa `../app/login/submit/route`, cuja cadeia transitiva inclui `import "server-only"` (padrão já usado em `src/auth/public-payment-link.test.ts:3` e `src/auth/public-product.test.ts:4`). Mínimo.
2. `src/app/store/[slug]/page.test.tsx:59` — expectativa corrigida para `"Esta vitrine está indisponível"`, que confere byte a byte com `src/i18n/dictionaries/storefront/pt-BR.ts:19`; a página resolve `defaultLocale = "pt-BR"` (`src/i18n/locales.ts:9`) sem principal, e o teste agora também pinha `read("unknown", "pt-BR")`, reforçando (não mascarando) o comportamento de produto pretendido. A string en segue intacta em `en.ts:19`.
3. `pop/scripts/db-test.mjs:504` — correto: o INSERT lista 7 colunas mas fornecia 6 valores (→ 42601 "INSERT has more target columns than expressions", mascarando a violação 23514 pretendida); `'${otherUserId}'` está definido antes e segue o padrão das 10+ fixtures vizinhas idênticas. Lado de correção é fixture, não SQL de produção.
4. `vitest.config.ts` — `[...configDefaults.exclude, "tests/**", "pop/worktrees/**"]`: `tests/**` contém apenas specs Playwright de evidência (package.json roda-os via `playwright test`; o `exclude` antigo já os excluía do vitest, então nada legítimo foi escondido — confirmado: os mesmos 119 arquivos/880 testes rodam). `configDefaults` restaura `**/node_modules/**`, `**/.git/**`, `dist` etc., que o exclude customizado anterior havia derrubado. `pop/worktrees/**` contém só checkouts de task.

## Reexecução (worktree da task)

- `CI=true pnpm test` — **119 arquivos passaram | 2 skipped; 880 testes passaram | 7 skipped** (db-test e product-category.database são o contrato descartável coberto por `pnpm db:test`, fora do gate por contrato).
- `CI=true pnpm check` — **exit 0** (lint + typecheck + test + `next build`, 36 páginas geradas).
- `python3 pop/scripts/pop_validate.py --standalone` (checkout principal) — **válido, nenhuma violação** (2 avisos preexistentes/esperados de worktree sem task em 004, incluindo a desta task por já estar em 005).

## Achados

Nenhum bloqueante, sugestão ou nit.

## Decisão

**APPROVE** — verificado por revisor independente (yolo) — 2026-07-24
