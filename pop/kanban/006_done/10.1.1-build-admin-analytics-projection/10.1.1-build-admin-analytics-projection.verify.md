# Verification — [[10.1.1-build-admin-analytics-projection]]

- **Stage:** 005_verifying · **Mode:** full (critical) · **Reviewer:** independent yolo critic · **Date:** 2026-07-26

## Original request

Card What: define reporting timezone and periods, then implement indexed administrator-only aggregates for registered/active users, finalized/abandoned orders, links, products, exact volume/conversion, and top owners/products without payer-data leakage. Verified against the delivered diff (`git diff d57bb72..de85a02` in the worktree; merge-base is the 10.3.1 close, so the temporal gate held):

- Reporting timezone/periods: the closed `today`/`7d`/`30d` set with `America/Sao_Paulo` half-open UTC bounds is reused from 8.4.1 via `resolvePeriodBounds` (`src/orders/admin-analytics.ts:34-35,140`); the bounds test asserts the exact 03:00Z boundaries.
- Indexed administrator-only aggregates: `requireAdminPrincipal` (exact `ADMIN` + `ACTIVE` denial, one `ForbiddenError`) runs before any store call (`admin-analytics.ts:118-120,134`); user counts, in-period order counts by source/state, two never-merged sales members, funnel over both attempt tables, V2 link counts, product active/archived counts, and top-5 owner/product leaderboards are all delivered in one global DTO.
- No payer-data leakage: the DTO carries no customer snapshot member, owner email, internal/pair UUID, verifier, capability, or provider field; enforced by absence assertions (test line 272-285) plus an exact-keys assertion on the top-owner identity (`["deleted","username"]`, line 201).
- No route, no mutation, no per-owner drilldown: `admin-analytics` is referenced only by its own test; 10.1.2 owns rendering.

## Surface and tests

Diff scope (10 files, all inside the plan's write sets): `src/orders/admin-analytics.ts` (new, 419 lines), `src/orders/admin-analytics.test.ts` (new, 13 tests), `src/orders/merchant-analytics.ts` (**export-keywords-only** — verified hunk by hunk: `export` added to `CurrencyPairRef`, `parseDecimalUnits`, `formatDecimalUnits`, `formatRate`, `pairKey`, `AccumulatedGroup`, `accumulate`, `labelFor`, `toAmounts`, `toSalesGroups`; zero behavior or DTO change; merchant test file untouched), one canonical migration `20260726120000_admin_analytics_indexes` (5 additive plain-column non-unique indexes via `migration.safe.json`), `prisma/schema.prisma` (+5 `@@index`), `prisma/AGENTS.md` (+1 index entry), root `AGENTS.md` (+1 contract bullet), `pop/specs/administrative-foundation.md` (new "Administrator analytics definitions (10.1.1)" section + slice entry + header), `pop/specs/checkout-and-order-lifecycle.md` (reuse note in the 8.4.1 section + header). No V1 file, route, dictionary, `src/app/`, `src/auth/`, `src/checkout/`, `src/data-directory/`, or baseline migration appears in the diff.

Independent re-runs in the worktree (pipefail, true exit codes): `pnpm check` = 0 (lint + typecheck + `admin-analytics.test.ts (13 tests)` green inside 164 files / 1320 passed, 18 skipped + build), `pnpm db:test` = 0 (25/25 PASS), `pnpm db:migration-policy` = 0 (`baseline=19 future=11`, language, fixtures), `pnpm db:contract-check` = 0 (incl. documentation-contract). Node engine warning is pre-existing environment noise.

## Criteria

1. **Index decision evidenced — pass.** One canonical safe-language migration; generated SQL byte-exact; baseline 19 untouched; timestamp `20260726120000` is the latest and collides with nothing on `develop` since the merge-base. Necessity verified independently: every pre-existing index on `order_v2`, `order_local_outcome_v2`, `checkout_attempt_v2`, and `standalone_checkout_attempt` is owner- or link-prefixed, so the five new plain-column indexes map 1:1 to the delivered global query shapes (state+settledAt, createdAt, outcome+createdAt, createdAt ×2).
2. **Exact definitions — pass.** Tests prove the reporting-zone bounds, per-pair BigInt micro-unit sums ("0.1"+"0.2"="0.3"), never-merged members (`not.toHaveProperty("totalSales")`), registry-redacted labels with fail-closed null code, the funnel union over both attempt tables, four-digit rates (`"0.5000"`), and null on zero denominator.
3. **User/deletion semantics — pass.** `countUsers` implements `registeredTotal` (all rows), `activeNow` (`status = ACTIVE` AND `deletedAt IS NULL` — the double condition matches 10.3.1's deleted⇒DISABLED+marker model, so no miscount), `deletedTotal` separate; a dedicated test keeps a deleted owner inside the global aggregate while marking it on the leaderboard.
4. **Redaction boundary — pass.** Absence assertions cover UUID grammar, `@`, verifier/nonce/credential/provider/cpf; top-owner identity is exactly `{ username, deleted }`; pair UUIDs group internally via `pairKey` and never serialize; the DTO type read word-for-word against the new spec section matches.
5. **Frozen surfaces — pass.** See Surface above; existing suites green.
6. **Specs and DOX match — pass.** The spec section's wording (global scope, periods, users, orders, two-member sales, funnel, links/products, leaderboards, deletion semantics, redaction fences) matches the delivered DTO; the 8.4.1 reuse note keeps the merchant projection owner-only; root and prisma AGENTS entries are accurate.
7. **Aggregate gates — pass** (re-run, above).

## 003 flagged items

- **Temporal-gate discipline — held.** Merge-base `d57bb72` is the 10.3.1 close commit, so 004 branched after 10.3.1 merged; tests and the store run against the real `deleted_at`/`deletedAt` model, and the `activeNow` double condition is consistent with deleted⇒`DISABLED`.
- **Redaction honesty — held.** Assertions are real (serialized-view regex + exact owner-keys), not tautological; identity resolution selects only `id, username, deletedAt` and fail-closed unresolved owners drop off the leaderboard without touching aggregates (test line 238).
- **Merchant byte-compatibility — held.** Diff is export-keywords-only; merchant DTO/service behavior unchanged; merchant suite untouched and green.
- **F01 migration evidence — held with a nit.** The migration is canonical and the need is verifiable from the schema (see criterion 1), but the commit body is empty — the query-shape→index mapping lives implicitly in the code and the prisma AGENTS entry rather than in an explicit front report.
- **Funnel double-count invariant — held.** The test unions both tables globally (3 attempts across both, correct classification, expiry boundary `<=` counts abandoned); the exactly-one-table-per-order invariant itself is service-fenced at write time (LINK→`checkout_attempt_v2`, STANDALONE→`standalone_checkout_attempt`, each table unique on `(orderV2Id, ownerId)`) — same posture as the pinned 8.4.1 semantics, which the spec section restates.
- **Boundedness — held.** Leaderboards hard-slice at `ADMIN_ANALYTICS_LEADERBOARD_LIMIT = 5` (test feeds 6 owners, asserts 5); every aggregate is period-windowed except the plan-sanctioned cheap full-table counts (users, links, products).

## Findings

- **nit** — `957830c` (F01) has an empty commit body; the index-necessity mapping is implicit. The evidence is independently reproducible from `prisma/schema.prisma`, so no rework required — future index fronts should record the shape→index map in the commit or front report.
- **nit** — the admin store's `listAttempts` selects `paymentLinkV2Id` (needed by the shared `StoredAnalyticsAttempt` type for merchant per-link metrics) but the admin service never reads it; harmless, never reaches the DTO, and keeps the shared store-type reuse intact.

No `bloqueante`, no `sugestão` requiring changes.

## Decision

APPROVED

verificado por revisor independente (yolo) — 2026-07-26
