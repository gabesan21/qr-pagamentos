# Group g03 — Converge and evidence — [[1.4.1-repair-login-submit-reliability]]

- **Order:** 3
- **Depends on:** g02
- **Description:** Correct the singleton ACL durably, refresh only the verified stale index pin for the existing exact Node tag, then prove login, least privilege, supply-chain pinning, helper, and production contracts.

## Movements

- [x] (agent) Commit the candidate branch, verify the worktree is clean, then run `pnpm container:test --clean-clone --scenario login`.
  - Expected: exit 0 with all four named login PASS markers, automatic resource cleanup, and no printed secret/database URL.
  - If it fails: valid POST still returns 500 or assertions fail → collect sanitized logs, return to the owning g01 fork, and amend only after the focused regression explains the failure.
- [x] (agent) (after previous) Retain one new migration after `20260716180000_global_payment_settings` that revokes the inherited ACL on `app.global_payment_settings` and re-grants only table `SELECT` plus column `UPDATE` for `currencies`, `payment_methods`, and `updated_at`. At the end of `prisma/bootstrap.sql`, replace unconditional singleton SQL with the same normalization behind an exact relation-existence guard; preserve broad current/default privileges for every ordinary table and never edit schema, generated code, or prior migrations.
  - Expected: bootstrap succeeds before the singleton exists, deployed databases receive the forward repair, and every bootstrap replay re-applies the minimal singleton exception after ordinary grants without changing table ownership.
  - If it fails: absent relation causes bootstrap failure → correct the guard without catching unrelated errors; Prisma needs another column → prove it from the emitted update and grant only that column; another grant source survives → revoke only that demonstrated source.
- [x] (agent) (after previous) Update `pop/scripts/db-test.mjs` and static database/container contracts for the sixth immutable migration and durable ACL: exercise bootstrap before migration, migration deploy, repeated post-migration bootstrap, effective privilege checks, and the existing canonical wrapper/startup routing.
  - Expected: ordinary tables retain intended `SELECT/INSERT/UPDATE/DELETE`; the singleton allows runtime read and allowed-column update only; runtime `INSERT`, `DELETE`, table-wide `UPDATE`, and `id` update return `42501`; the singleton constraint is exercised through admin; static checks require both the forward migration and guarded bootstrap normalization.
  - If it fails: test order or transaction state masks the ACL → isolate each denial with `expectDenied`, retain the constraint probe separately, and capture privileges after each bootstrap replay; container contract drift → update only assertions owned by the canonical SQL/wrapper boundary, never duplicate grant logic in JavaScript or Compose.
- [x] (agent) (after previous) Observe the stale pin with `docker buildx imagetools inspect node:26.4.0-bookworm-slim`: capture the complete published `Digest:` and complete committed Dockerfile index, and record their expected inequality before changing any consumer.
  - Expected: the exact versioned tag returns one complete `sha256:` index that differs from the complete stale committed index; no child digest is claimed at this step.
  - If it fails: registry cannot return a complete index → abort without guessing; equality is already restored → record it and skip replacement, but still add/run the local three-consumer assertion before the default checker.
- [x] (agent) (after previous) Extend `pop/scripts/container-contract-check.mjs` with a registry-free `--local-pins` path that extracts the exact Node tag/index from `Dockerfile`, `install/install.sh`, and `pop/scripts/container-prepare-secrets.mjs`, requires one complete identical index, and prints `PASS node-pin-consistency`. Atomically replace only the observed stale index in those three consumers, then run `pnpm container:contract-check --local-pins`.
  - Expected: local mode exits 0 without invoking Docker/registry and names three synchronized exact-tag consumers; no version, variant, Docker stage, installer, or helper behavior changes.
  - If it fails: extraction or one consumer differs → fix only the assertion/parser or missed atomic replacement; any broader image/helper change or child-manifest pin requires return to 002.
- [x] (agent) (after previous) Run the default `pnpm container:contract-check` only after local pin consistency passes, and capture its complete index plus resolved `linux/<host-arch>` child digest.
  - Expected: unchanged default online logic prints `PASS tag-digest-equality` for the refreshed multi-platform index and a concrete host child digest, followed by existing image/bootstrap contracts.
  - If it fails: tag drifted again → repeat one display lookup, atomic three-consumer refresh, and local assertion before retrying once; child resolution unavailable → abort rather than using the child as the committed pin.
- [x] (agent) (after previous) Run `pnpm db:contract-check`, `pnpm container:contract-check`, `pnpm db:test`, the focused login tests, `pnpm check`, and a newly committed `pnpm container:test --clean-clone --scenario login`.
  - Expected: every command exits 0; role separation and runtime denials pass, the full Node index equals the live tag and resolves a host child, while all four production login PASS markers and the advisory-lock regression remain green.
  - If it fails: ACL-related failure → repair only the new migration/test; login outcome regresses → restore the proven g02 behavior; unrelated failure → record exact evidence and abort rather than suppressing it.
- [x] (agent) (after previous) Run `install/test.sh` and confirm the installer/helper keep using the same refreshed exact-tag index without changing secret staging or privilege-free installation behavior.
  - Expected: exit 0 with `PASS install-contract`; a repeated `pnpm container:contract-check --local-pins` prints `PASS node-pin-consistency` and no stale index remains in the three tracked consumers.
  - If it fails: pin mismatch → update only the missed consumer; behavioral installer failure → revert non-pin drift and return to 002 if fixing it requires any broader installer change.
- [x] (agent) (after previous) Finalize the diagnosis with both root causes and pre/post-fix evidence, verify it contains no generated credential, cookie value, connection string, or client-visible exception detail, and review root plus Prisma DOX contracts.
  - Expected: artifact names the login repair and ACL migration with final commit/test commands; contracts remain accurate or are updated with the code; `python3 pop/scripts/pop_validate.py` exits 0.
  - If it fails: redaction or DOX drift → remove the unsafe evidence/update the owning contract and rerun validation before 005.

## Execution notes

- 2026-07-16 — Planned; clean-clone execution requires committed HEAD by harness contract.
- 2026-07-16 — Candidate `fc5d85d` passed the clean-clone login scenario and `pnpm check`; `pnpm db:test` failed twice at `Runtime has excess privileges` after `PASS global-payment-settings-schema`. Execution identified inherited default table DML on the singleton, and the task returned to 002 instead of suppressing the gate.
- 2026-07-16 — Replanned g03 to authorize exactly one immutable ACL-normalization migration, an objective disposable regression, and full preservation gates for the already proven login outcome.
- 2026-07-16 — Round 2 execution proved bootstrap replay undoes a migration-only ACL repair; Round 3 authorizes a guarded post-grant bootstrap normalization while preserving ordinary-table defaults, plus replay/effective-privilege/static-container evidence.
- 2026-07-16 — Round 3 execution reached the existing registry gate after ACL work and recorded verified Node index drift (`sha256:ec82d…` live versus `sha256:70a832…` committed). Round 4 authorizes only a complete index refresh across the three existing exact-tag consumers; the existing checker remains responsible for live equality and platform-child evidence.
- 2026-07-16 — Round 4 critic returned the plan because the stale checker aborts before child resolution and reads only the Dockerfile pin. Round 5 sequences complete mismatch observation → atomic three-consumer refresh → registry-free equality assertion → successful default equality/child lookup, with installer PASS evidence.
- 2026-07-16 — Durable ACL replay, six-migration contract, local three-consumer Node index consistency, online index/host-child resolution, and installer behavior pass; final aggregate and committed clean-clone gates remain.
- 2026-07-16 — Expanded candidate `4fba364` passed `pnpm check` and the clean-clone production login scenario with all four required login markers; diagnosis and root/Prisma DOX closeout completed without retaining secrets or usable URLs.
