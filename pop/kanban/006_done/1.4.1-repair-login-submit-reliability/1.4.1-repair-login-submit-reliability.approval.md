# Approval — [[1.4.1-repair-login-submit-reliability]]

## Round 1 — 2026-07-16

The plan diagnoses the real Compose/PostgreSQL failure before repair, preserves the authentication contract, adds focused regressions, and requires observable runtime, database, aggregate, and DOX gates.

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Approved. Every acceptance criterion has an executable run and an observable pass condition; the real Compose/PostgreSQL surface is covered by `re-run` evidence, while the diagnosis criterion requires a pre-fix failure and post-fix focused regression. The movements include expected observations, counter-moves, objective forks, and explicit abort conditions. Environment preflight, the binding implemented spec, red-team analysis, redaction constraints, DOX closeout, and aggregate/economical verification are present, with no avoidable human-owned action.

approved by agent critic (yolo) — 2026-07-16

## Round 2 — 2026-07-16

Returned from 004 after the disposable database gate proved that `qr_runtime` inherits excess `INSERT`/`DELETE` privilege on the global-settings singleton. The revised plan preserves the completed login repair and adds the smallest immutable ACL migration, an effective-privilege regression, and database/contract/runtime convergence gates.

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Approved. The revision is executable without widening the product contract: it preserves the already evidenced login repair and confines the newly confirmed privilege defect to one forward-only migration. The acceptance table objectively proves both allowed and denied singleton operations through disposable replay, retains the production login and aggregate gates, and includes static migration/container contracts plus DOX closeout. The route, forks, abort conditions, environment preflight, red-team countermeasures, approved spec, and group movements are proportionate to size M and contain no avoidable human-owned action.

approved by agent critic (yolo) — 2026-07-16

## Round 3 — 2026-07-16

Returned from 004 after replay proved that the forward migration is not durable by itself: the idempotent bootstrap restores blanket DML. The revised plan keeps the forward migration for existing databases and adds a relation-guarded singleton normalization after bootstrap's ordinary grants, with replay, effective-privilege, and static container-contract evidence.

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Approved. The revision closes the demonstrated replay defect without weakening ordinary-table behavior: the forward migration repairs deployed databases, while the exact relation guard re-normalizes only `app.global_payment_settings` after bootstrap's broad current/default grants. The disposable gate makes pre-migration bootstrap safety, migration deploy, repeated post-migration bootstrap, ordinary-table DML, allowed singleton operations, and denied `INSERT`, `DELETE`, table-wide `UPDATE`, and `id` update observable; static database/container checks guard ordering and unchanged startup routing. The route, counter-moves, abort conditions, red-team record, preflight, aggregate/runtime gates, and DOX closeout remain executable and contain no avoidable human-owned action.

approved by agent critic (yolo) — 2026-07-16

## Round 4 — 2026-07-16

Returned from 004 after the existing container contract proved that exact tag `node:26.4.0-bookworm-slim` has a new published multi-platform index. The revised plan authorizes only an atomic complete-index refresh across the Dockerfile and its two existing helper consumers, while the unchanged live equality/child-resolution mechanism and all prior login, ACL, database, installer, and production gates remain mandatory.

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Returned. Two plan claims are not executable as written:

1. `1.4.1-repair-login-submit-reliability.plan.md:37` requires the unchanged checker to capture both the live index and host child before replacing the stale pin, but `pop/scripts/container-contract-check.mjs:25` aborts on index inequality before the raw manifest/child lookup at lines 26–30. Split this into an observable stale-index lookup, atomic refresh, then a successful child-resolution rerun; do not treat the expected first mismatch as the plan's abort condition.
2. Acceptance criterion 6 says the checker proves equality across every committed consumer, but `pop/scripts/container-contract-check.mjs:14-18` reads the Node pin only from `Dockerfile`; neither `install/install.sh` nor `pop/scripts/container-prepare-secrets.mjs` participates. Authorize and require a deterministic three-consumer equality assertion (plus `install/test.sh`) in the acceptance run, while preserving the exact tag/version/variant and using the multi-platform index—not the child digest—as the pin.

The prior login, ACL, database, production, aggregate, and DOX gates remain adequately preserved; this return is limited to making the digest refresh and three-consumer consistency actually observable.

returned by agent critic (yolo) — 2026-07-16

## Round 5 — 2026-07-16

The digest route now follows the executable order required by the return: capture the complete stale/live mismatch, atomically refresh all three exact-tag consumers, prove their equality through a registry-free mode in the existing checker, and only then rerun its unchanged online equality/host-child lookup. `install/test.sh` is an independent observable acceptance gate; all prior login, ACL, database, production, aggregate, and DOX gates remain mandatory.

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Approved. The revised route makes the stale mismatch an explicit observable first step, atomically refreshes only the complete multi-platform index in all three exact-tag consumers, requires a registry-free `PASS node-pin-consistency`, and runs the unchanged online equality/host-child lookup only after local consistency passes. Acceptance criterion 6 deterministically covers the three consumers and criterion 7 independently executes `install/test.sh`; the forks and aborts preserve the exact Node version, Debian variant, index-vs-child distinction, and default live comparison. All prior login, ACL, database, production, aggregate, and DOX gates remain executable and mandatory.

approved by agent critic (yolo) — 2026-07-16

## Merge round — 2026-07-16

### Human response

- [ ] Done

### Yolo critic response

- [x] Done

Approved for local integration into `develop`. The signed critical verification covers every acceptance criterion, the task branch contains the complete verified candidate, and local `develop` already contains the declared PR branch plus the task branch base. No conflicting synchronization or merge condition is present.

approved by agent critic (yolo) — 2026-07-16
