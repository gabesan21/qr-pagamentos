# Verification — [[1.4.1-repair-login-submit-reliability]]

- **Stage:** 005_verifying · **Owner:** agent + yolo critic because `critical: true`

## Round 1 — 2026-07-16

| # | Criterion | Mode | Verification run / evidence audited | Result | Evidence |
|---|-----------|------|-------------------------------------|--------|----------|
| 1 | Valid-login failure has a sanitized diagnosis and root-cause regression. | evidence | Audited the diagnosis and g01 notes; ran `pnpm exec vitest run src/auth/session.test.ts src/auth/sign-in.test.ts`. | ✅ passed | The artifact identifies Prisma `P2010` while deserializing PostgreSQL `void`, retains no credential/cookie/URL, records the pre-fix failure, and the candidate-focused run passed 8 tests. The regression requires the advisory lock to use `$executeRaw`. |
| 2 | Seeded administrator signs in through production Compose. | re-run | `pnpm container:test --clean-clone --scenario login` at committed `db018fa` from a clean detached worktree. | ✅ passed | Exit 0 with `PASS login-invalid-opaque`, `PASS login-valid`, `PASS login-cookie-contract`, and `PASS login-locale-preference`; observed output contained no secret or database URL. |
| 3 | Invalid credentials remain generic and create no authenticated cookie/session. | re-run | `pnpm exec vitest run src/auth/session.test.ts src/auth/sign-in.test.ts`, plus the production scenario above. | ✅ passed | Unit assertions require 303 to the generic error, no validation/locale call, and no cookie; the production probe independently passed the opaque-invalid marker before valid login. |
| 4 | Infrastructure failure is not mislabeled as invalid credentials. | re-run | `pnpm exec vitest run src/auth/session.test.ts src/auth/sign-in.test.ts`. | ✅ passed | Injected sign-in rejection propagates and performs neither validation nor locale resolution; focused suite exited 0. |
| 5 | Migration/bootstrap replay leaves only required singleton privileges. | re-run | `pnpm db:test`. | ✅ passed | Exit 0 after six-migration replay; allowed schema/runtime checks passed together with `global-payment-settings-runtime-denials`, role separation, and runtime denials. |
| 6 | Database, bootstrap, and image-pin contracts converge. | re-run | `pnpm db:contract-check`; `pnpm container:contract-check --local-pins`; `pnpm container:contract-check`. | ✅ passed | All exited 0. Three Node consumers share index `sha256:ec82d…`; the live registry matched it and resolved the `linux/amd64` child. PostgreSQL live-index and image/bootstrap contracts also passed. |
| 7 | Installer remains valid with the synchronized helper pin. | re-run | `install/test.sh`. | ✅ passed | Exit 0 with `PASS install-contract`; diff changes only the exact helper index and the three-consumer check proves synchronization. |
| 8 | Complete database-free gate passes. | re-run | `pnpm check`. | ✅ passed | Exit 0: generate, typecheck, lint with 135 unchanged vendored-skill warnings and zero errors, 137 tests passing with one skipped, and production build successful. |
| 9 | DOX closeout matches changed paths. | re-run | `python3 pop/scripts/pop_validate.py`; `git diff --check develop...HEAD`; root and Prisma contracts reviewed against the diff. | ✅ passed | Vault validation and whitespace check exited 0. Root rules remain accurate; `prisma/AGENTS.md` documents the new guarded post-grant normalization and forward-migration responsibility. |

### Clean-code review

- Behavior follows the observed runtime boundary: the transaction and advisory-lock semantics remain unchanged while only the result-reading Prisma API becomes the execution API.
- Focused tests would fail if the production adapter returned to `$queryRaw`, and route tests cover valid, invalid, and infrastructure-failure paths without weakening opaque authentication.
- The ACL and image-pin expansion was independently approved after executable gates exposed those defects; implementation remains localized, forward-only, and contract-tested.
- No blocking behavior, test, complexity, naming, documentation, or DOX finding remains in the reviewed diff.

**Verdict:** passed — ready for the independent yolo critical gate; this verifier does not sign that gate.

## Yolo critical approval

All nine acceptance criteria passed independent adversarial review. The critic re-ran the focused authentication regressions, the clean-clone production Compose login scenario, six-migration PostgreSQL replay, database and container contracts (including live registry index/child resolution), installer contract, aggregate application gate, PoP validation, and diff hygiene. The sanitized diagnosis is consistent with the pre-fix `$queryRaw` boundary and the focused regression prevents reintroducing result deserialization for PostgreSQL `void`; no blocking clean-code or contract finding remains.

verified by agent critic (yolo) — 2026-07-16
