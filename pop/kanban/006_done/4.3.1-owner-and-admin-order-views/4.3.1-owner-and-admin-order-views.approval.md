# Aprovação — [[4.3.1-owner-and-admin-order-views]]

- **Etapa:** 003_human_approval · **Responsável:** revisor independente em yolo

## Rodada 1 — 2026-07-21

### Brief para decisão

- **Entrega:** projeção server-only de pedidos + views de lista/detalhe owner (escopadas) e admin (globais), somente leitura, com snapshot de cliente exato por política exposto apenas ao principal autorizado.
- **Estratégia:** uma única projeção fechada em `src/orders/` como seam de autorização; ambas as superfícies de UI a consomem; cross-owner indistinguível de inexistente; sem mutações, sem mudança de schema.
- **Topologia:** executor único, 3 frentes sequenciais (F01 projeção → F02 views owner → F03 views admin; F02/F03 serializadas por colisão de escrita nos dicionários).
- **Risco principal:** vazamento de customer snapshot via log/erro/URL/resposta — mitigado por DTO fechado, teste de contrato/estático e 005 full (critical).
- **Critérios principais:** 1–7 (suites focadas `src/orders/`, guards, paridade i18n, `pnpm check`, ui-review; critérios 1, 2, 4, 5, 6 em modo re-run).
- **Plano:** [[4.3.1-owner-and-admin-order-views.plan]] — *siga para revisar o brief completo*.

### Resposta do humano

_(escreva aqui: aprovado, ou o que mudar)_

- [x] Feito

### Decisão do agente

_(após o Feito: `aprovado → 004` ou `mudanças pedidas → 002: <resumo>`)_

### Resposta do crítico (yolo)

- **Contexto:** strong independente.
- **Devolução:** 0 de 2.
- **Decisão:** aprovado → 004.
- **Motivo/evidência:** objetivo verificável com resultado observável; critérios 1–7 executáveis (suites focadas, grep de campos proibidos, `pnpm check`, ui-review) com ≥1 re-run; estratégia coerente com as specs (a spec checkout-and-order-lifecycle reserva exatamente esta "future re-authorized owner/administrator projection"); premissas confirmadas no código (`PaymentLinkOrder` persiste owner/policy/colunas de cliente em `prisma/schema.prisma:231-266`, `CustomerSnapshotV1` em `src/orders/payment-link-order.ts:21-26`, `owner-guard.ts`, `admin/guard.ts`, dicionários en/pt-BR + `dictionaries.test.ts` presentes); frentes com owns/may-read/must-not-edit e serialização explícita F02→F03 por escrita compartilhada nos dicionários; dependências 4.1.1/4.1.2/4.2.1 explícitas e entregues; riscos materiais da boundary de autorização cobertos (opacidade cross-owner, snapshot fechado sem verifiers/nonce/key material, admin read-only sem POST, 401/403 vazios, paridade i18n) com condições de aborto objetivas; sem item `(user)`; nenhuma decisão indispensável escondida (lista paginada fora de escopo, ordenação determinística e janela limitada declaradas).

aprovado por revisor independente (yolo) — 2026-07-21
