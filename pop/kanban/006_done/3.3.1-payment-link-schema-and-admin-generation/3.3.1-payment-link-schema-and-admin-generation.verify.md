# Verification and critique — 3.3.1-payment-link-schema-and-admin-generation

- **Stage:** 005_verifying
- **Reviewer:** independent reviewer (yolo)
- **Reviewed:** 2026-07-20

## Round 1 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Verification performed | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| AC1 | Payment-link persistence, constraints, trigger, and grants | re-run, partial | Reviewed `prisma/schema.prisma` and `20260721020000_payment_links/migration.sql`; reran `pnpm db:generate` through `pnpm check` and `pnpm db:contract-check`. | partial | The model/migration define UUID identity, a 24-character URL-safe unique identifier, closed link types, nullable expiry, restrictive FKs, indexes, runtime DML, and the `Product` then `CatalogCurrencyPair` `FOR SHARE` trigger order. Docker-backed `pnpm db:test`, including the two-client race proof, was expressly not run. |
| AC2 | Active-admin service, validation, identifier, and revocation | re-run | `pnpm exec vitest run src/auth/payment-link.test.ts` (within the focused suite); source review. | failed | The suite validates active-admin checks, UUID/type/expiry input, 24-character CSPRNG output, bounded collisions, exact price preservation, and one-way revocation. It does not model the Prisma error emitted by the trigger; B1 prevents the required named dependency mapping. |
| AC3 | Commit-boundary active dependency guarantee | static review, partial | Reviewed trigger and `pop/scripts/db-test.mjs`. | partial | The trigger locks product before pair and ordinary deactivation updates conflict with the held share locks. The database test structurally uses independent runtime clients and `pg_stat_activity` lock waits in both commit orders. Its runtime proof was not executed by explicit authorization, so this review makes no race-runtime claim. |
| AC4 | Protected create/revoke routes | re-run | Focused route tests and source review. | passed | Cookie-principal reauthorization precedes form parsing or target resolution. Protected errors are empty 401/403 responses; every other create/revoke result uses the same opaque relative redirect. |
| AC5 | Localized, owned administrator UI | re-run, static/semantic review | Focused management/page/dictionary tests; `pnpm admin:source-check`; review against `DESIGN.md` and UI DOX. | passed, within stated runtime limitation | Both dictionaries are in parity; existing primitives and semantic CSS tokens are used; empty, populated, pending/disabled, status, expiry, active/inactive, and revoke-confirmation states are present. The source check reports zero raw controls, adapters, inline styles, or local variants. No visual-runtime or axe claim is made. |
| AC6 | No premature public/checkout/order/provider scope | review | `git diff --check develop...HEAD`; complete changed-path/source review. | passed | The diff adds only migration/schema, protected admin routes/service/UI, dictionaries, and static database/UI checks. It contains no sessionless resolver, public href, checkout/order/consumption mutation, expiry job, provider request, or hosted link. |
| AC7 | Aggregate quality, parity, and closeout readiness | re-run, review | `pnpm check`, `pnpm db:contract-check`, `pnpm admin:source-check`, `git diff --check develop...HEAD`; specs/DOX review. | failed | Prisma generation, typecheck, lint, 592 tests (4 skipped), and production build passed. Database and UI static gates passed and the diff is clean. The blocking service mapping and the unowned UI-check edit prevent closeout. The catalog-spec implemented-slice update remains a 006 responsibility after correction. |

### UI and implementation review

- Nielsen quick pass by source inspection: native forms expose pending feedback, revocation is explicitly one-way, opaque recovery is localized, and the existing PIX-ledger component hierarchy is retained. No severity 3–4 UI issue was found.
- WCAG semantic pass by source inspection: native labels bind to every new control; inherited focus styling covers `summary`; notices use live status/alert semantics; and the existing owned controls meet the source-inventory contract. Automated axe, browser interaction, and the 375/768/1440 screenshot loop were deliberately not run.
- The trigger’s `FOR SHARE` order is consistent with the approved protocol. The implemented raw-SQL test observes actual lock waits rather than relying only on timing, but it remains unexecuted runtime evidence under the stated authorization.

### Implementation quality and findings

| Severity | Finding | Evidence | Required correction |
| --- | --- | --- | --- |
| blocker | B1 — `isInactiveDependency` cannot map the named `23514` raised by the PostgreSQL trigger through the actual Prisma `paymentLink.create` boundary. Prisma reports a database check failure as `P2004` with `meta.database_error`; the implementation accepts only a direct driver-style `constraint`/`23514` object (or unrelated `P2003`). Thus a raced inactive product/pair reaches `PaymentLinkConflictError`, not the required `PaymentLinkDependencyError`. The existing unit test fakes the raw PostgreSQL error and cannot detect this mismatch. | `src/auth/payment-link.ts:103-108`; `src/auth/payment-link.test.ts:103-105`; the trigger raises `23514` with named constraints at `prisma/migrations/20260721020000_payment_links/migration.sql:40-45,55-60`. | Recognize exactly `P2004` errors whose `meta.database_error` names `payment_link_product_active` or `payment_link_currency_pair_active` (retain only any deliberately supported adapter shape). Add service tests with the real Prisma error shape for both names and a non-matching `P2004` negative case. |
| blocker | B2 — The UI source-inventory contract was edited outside every declared front’s `Owns` set. F03 owns the new component, minimal stylesheet rules, dictionaries, and its tests, but not `scripts/check-admin-ui-inventory.mjs`; neither the plan nor the task log records an ownership expansion. Yolo 005 must block an unjustified ownership invasion even though the one-line whitelist addition is logically related. | `3.3.1-payment-link-schema-and-admin-generation.plan.md`, F03 `Owns`; `scripts/check-admin-ui-inventory.mjs:12-15`; `git diff develop...HEAD --name-status`. | Return through planning/approval as required by the workflow to declare the inventory checker’s ownership (or move the change to an already owned contract fixture with an approved rationale), then resume implementation/review. |

### Scope and integration

- [ ] All changed paths respect declared front ownership: B2 is an unapproved ownership expansion.
- [x] No consuming front implemented a missing dependency or public/order/provider behavior opportunistically.
- [x] The authorized aggregate gate passed after integration.
- [ ] Specs and DOX are otherwise coherent; 006 closeout remains pending and cannot start while B1/B2 are open.

## Round 1 verdict

- **Decision:** rejected → 004_processing.
- **Blockers:** B1 — exact Prisma trigger-error mapping is absent; B2 — the UI inventory script is outside declared ownership.
- **Suggestions/nits:** none.
- **Runtime limitation:** Per explicit user authorization, this review did not run Docker, `pnpm db:test`, or any Playwright/admin evidence/screenshot loop. The migration lock/race proof is therefore reviewed statically only and remains unexecuted runtime evidence.
- **Summary:** The schema, shared lock protocol, authorization order, opaque redirects, identifier/expiry validation, localized UI, and no-scope-creep boundary are substantively aligned with the task. The production Prisma error shape is not mapped to the named dependency outcome, and the plan’s ownership contract was exceeded; both must be resolved before 006.

Verified by independent reviewer (yolo) — 2026-07-20

## Round 3 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Result | Evidence |
| --- | --- | --- | --- | --- |
| AC1 | Persistence, constraints, trigger, and grants | static, partial | partial | Schema/migration retain UUID identity, bounded URL-safe identifier, closed types, restrictive FKs, runtime DML, and the product-then-pair `FOR SHARE` trigger. The Docker migration/race run was intentionally skipped. |
| AC2 | Service validation, identifier, and revocation | re-run | passed | The service restricts list/create/revocation to active administrators; validates UUID/type/expiry; creates server-side 24-character CSPRNG identifiers; preserves price strings; bounds collisions; and keeps revocation one-way. |
| AC3 | Commit-boundary active dependencies | static, partial | partial | Trigger lock order and the two-client `pg_stat_activity` proof structure match the approved protocol. The runtime race proof was intentionally skipped. |
| AC4 | Protected mutations | re-run | passed | Reauthorization precedes parsing/target work; protected failures remain empty `401`/`403`, and all other failures use the same opaque relative redirect. |
| AC5 | Localized administrator UI | re-run, static/semantic | passed, within runtime limitation | Paired dictionaries, owned primitives, semantic tokens, and empty/populated/pending/status/confirmation states are present. The inventory check reports zero raw controls, adapters, inline styles, and local variants. |
| AC6 | No premature public/checkout/provider scope | review | passed | All 17 paths are within F01–F03 ownership. No public resolver or `href`, checkout/order/consumption behavior, expiry job, provider request, or hosted-link use was added. |
| AC7 | Quality and closeout readiness | re-run, review | passed, within runtime limitation | Generation, typecheck, lint, 601 passing tests (4 skipped), build, database contract, inventory, focused tests, and diff check pass. Specs/DOX remain coherent; 006 owns the implemented-slice closeout. |

### B1 exact-whitelist review

- The `P2004` mapping accepts only a string `meta.database_error` exactly equal to one of the two expected named-trigger diagnostics.
- Focused tests prove both accepted values map to `PaymentLinkDependencyError` and reject a prefixed product diagnostic, suffixed pair diagnostic, embedded constraint name, unrelated diagnostic, wrong Prisma code, numeric metadata, and absent metadata. They would fail if the prior substring matcher returned.
- B1 is resolved. The earlier B2 ownership issue remains resolved; the inventory-script edit is absent from the task diff.

### Verification commands

- `pnpm check` — generated client, typecheck, lint, 601 passed / 4 skipped tests. Its build subprocess was sandbox-blocked only by local port binding.
- `pnpm build` — passed when rerun with the required local process permission; the nested-worktree multiple-lockfile warning is non-failing.
- `pnpm db:contract-check` — passed.
- `pnpm admin:source-check` — passed (`raw_controls=0`, `adapter_imports=0`, `inline_styles=0`, `local_variants=0`).
- Five focused suites — 33 passed.
- `git diff --check develop...task/3.3.1-payment-link-schema-and-admin-generation` — clean.

### UI and implementation review

- Nielsen quick pass: native pending feedback, explicit one-way revocation, and opaque localized recovery are present; no severity 3–4 issue found.
- WCAG semantic pass: native labels, status/live semantics, keyboard-reachable confirmation, and inherited owned-control focus treatment are intact. No automated axe or screenshot claim is made.

## Round 3 verdict

- **Decision:** approved → ready for 006.
- **Blockers:** none.
- **Runtime limitation:** Per explicit user authorization, Docker, `pnpm db:test`, `pnpm admin:evidence`, `pnpm admin:evidence:verify`, Playwright, axe, and the screenshot loop were not run. Database-concurrency and visual-runtime evidence remain static-only.
- **Summary:** The strict two-value B1 whitelist and adversarial near-match evidence resolve the final blocker. The task meets its approved scope and static/unit verification contract; this reviewer performed no integration or stage move.

Verified by independent reviewer (yolo) — 2026-07-20

## Round 2 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Verification performed | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| AC1 | Payment-link persistence, constraints, trigger, and grants | static, partial | Reviewed the immutable migration/schema and reran `pnpm db:contract-check`. | partial | UUID identity, the bounded URL-safe identifier, closed types, restrictive FKs, least-privilege runtime DML, and the `Product` then `CatalogCurrencyPair` `FOR SHARE` trigger remain aligned. The Docker-backed migration/race proof was not run under the explicit runtime limitation. |
| AC2 | Active-admin service, validation, identifier, and revocation | re-run | Reviewed the corrected P2004 mapping and reran `src/auth/payment-link.test.ts`. | failed | The tests now cover both named P2004 values and one unrelated value, but the production predicate uses unbounded substring matching and therefore accepts non-exact P2004 metadata containing either name. |
| AC3 | Commit-boundary active dependency guarantee | static, partial | Rechecked the trigger, lock order, and database test structure. | partial | The shared lock protocol remains structurally correct. Its two-client runtime proof is intentionally unexecuted. |
| AC4 | Protected create/revoke routes | re-run | Reran both focused route suites and inspected routes. | passed | Cookie-principal authorization occurs before form parsing or route-param use; protected results remain empty `401`/`403`, and all other outcomes use opaque relative redirects. |
| AC5 | Localized, owned administrator UI | re-run, static | Reran focused UI/page tests and `pnpm admin:source-check`; checked dictionaries and component contract. | passed, within runtime limitation | The inventory checker now reports no raw controls, adapters, inline styles, or local variants; the removed checker change means no out-of-ownership script/style change remains. Native labels, pending state, localized empty/populated/revocation states, and semantic token/primitives remain in scope. |
| AC6 | No premature public/checkout/order/provider scope | review | Rechecked complete task diff, changed paths, and `git diff --check develop...HEAD`. | passed | No public resolver or `href`, checkout/order/consumption behavior, expiry job, provider call, or hosted-link behavior is introduced. |
| AC7 | Aggregate quality, parity, and closeout readiness | re-run, review | Reran `pnpm check`, `pnpm db:contract-check`, `pnpm admin:source-check`, five focused suites, and diff check. | failed | All rerun static/unit gates pass, but B1 leaves the required opaque dependency classification broader than its specified named trigger-error contract. |

### Implementation quality and findings

| Severity | Finding | Evidence | Required correction |
| --- | --- | --- | --- |
| blocker | B1 (remaining) — `isInactiveDependency` accepts any `P2004` whose `meta.database_error` merely *contains* `payment_link_product_active` or `payment_link_currency_pair_active`. It consequently classifies unrelated diagnostics such as `"ERROR: unrelated context payment_link_product_active"` as the active-dependency outcome. That is not an exact named trigger-error shape and fails the requirement to reject unrelated database errors. | `src/auth/payment-link.ts:103-109`; `src/auth/payment-link.test.ts:109-127`. The negative test omits both names, so it does not exercise either near-match accepted by `includes`. | Whitelist only the exact P2004 `meta.database_error` diagnostic shapes emitted for the two trigger constraints (with a full anchored match or exact values), and add near-match negative tests containing each constraint name plus an unrelated prefix/suffix. Preserve rejection for every other code, metadata type, and diagnostic. |

### Scope and integration

- [x] B2 resolved: `scripts/check-admin-ui-inventory.mjs` and `src/app/globals.css` are absent from the final task diff; the UI reuses the already-owned product layout classes.
- [x] Changed paths now respect F01–F03 ownership, and no missing/public/order/provider dependency was implemented opportunistically.
- [x] Static/unit quality gates passed: `pnpm check` (599 passing, 4 skipped), `pnpm db:contract-check`, `pnpm admin:source-check`, and the five focused suites (27 passing).
- [ ] Exact active-dependency error mapping is still not satisfied (B1).

## Round 2 verdict

- **Decision:** rejected → 004_processing.
- **Blocker:** B1 — the P2004 mapping is not exact and can treat unrelated metadata as a named active-dependency trigger error.
- **B2:** resolved; no ownership violation remains.
- **Runtime limitation:** Per explicit user authorization, this review did not run Docker, `pnpm db:test`, `pnpm admin:evidence`, `pnpm admin:evidence:verify`, Playwright, axe, or the screenshot loop. Database concurrency and visual-runtime evidence therefore remain static-only.

Verified by independent reviewer (yolo) — 2026-07-20
