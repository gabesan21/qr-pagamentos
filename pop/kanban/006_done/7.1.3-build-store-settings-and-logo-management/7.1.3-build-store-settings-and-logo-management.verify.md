# 7.1.3-build-store-settings-and-logo-management — Verification (005, yolo critic, strong, fresh session)

- **Date:** 2026-07-24 · **Mode:** FULL (critical: true) — full surface re-run, not differential.
- **Delivery:** worktree `pop/worktrees/7.1.3-build-store-settings-and-logo-management`, branch `task/7.1.3-build-store-settings-and-logo-management`, commits `05c290a`…`8b5f09c` over merge-base `25dbf33`.
- **Binding inputs:** card What/Why + both 003 rounds (dirty-field omission contract; disabled state from empty `listActiveChoices`).

## 1. Original request first — MET

Bilingual (en/pt-BR) store-settings workspace with live theme/layout/accent/logo previews, enablement constraints, slug/display-name validation UI, default-currency selection, logo upload/replace/remove with the official `BrandIdentity` `merchant-fallback` lockup (never a page-local mark), all over 7.1.2's untouched persistence, and the currency control disabled + bilingually explained when `listActiveChoices` is empty. Evidence proves every element end-to-end; the 003 contracts are honored exactly.

## 2. Scrutiny results

1. **Dirty-field omission — proven.** `src/app/storefront-form-preparation.ts` deletes only extended fields equal to prefill; changed/cleared/logo-empty always submit (unit tests `storefront-form-preparation.test.ts`). The run artifact `artifacts/store-settings/20260724211133/assertions.json` proves it in-browser: `en-save-pending` omits the unchanged stored `BRL`; the final save after BRL deactivation returns `?storefront=changed` with all four extended fields absent and legacy fields present (enabled unchanged `storefrontThemeId` select absent = the `formdata`-event deletion genuinely fired in Chromium). Changed currency (`pt-BR` first assignment) submits and gates.
2. **`POST /storefront/logo` — clean.** Origin guard runs before auth/parsing (route.test.ts spies prove `requireOwnerFromCookie`/`formData` never called on guard denial); re-auth before `formData()`; only empty `401`/`403` or opaque `/settings?storefront-logo=staged&logo=<43>|failed`; exactly one literal `serverRequestRoutes.storefrontLogo` entry; `create(actor, "STOREFRONT_LOGO", bytes)` staging only; abandoned STAGED objects ride media reconciliation (documented accepted posture in spec + root AGENTS.md). Mirrors `src/app/storefront/route.ts` structure.
3. **Theme preview tokens — verified.** All six `[data-theme-preview="<id>"]` blocks' color declarations are byte-identical to their `:root[data-theme]` counterparts (recomputed programmatically over `globals.css`); the re-declared alias layer matches the authored `:root` semantic aliases + `@theme inline` color map exactly (38 entries, pinned by the new contract test); `pnpm tokens:check` green; the accent lint exception is exactly the planned `style={{ "--storefront-accent": accentColor } as CSSProperties}` in `src/app/storefront-preview.tsx` only, with negative tests.
4. **UI quality (ui-review frame) — pass.** Six mandatory states present (populated/prefill, `loading.tsx` skeleton, empty = official fallback + explained disabled currency, opaque error alerts, success status + staged status line, hover/focus/disabled via owned primitives). Dictionary parity: 38 new keys in both locales, key-identical. axe serious/critical = 0 across 36 grid states; all targets ≥44px; focus ≥2px/box-shadow; keyboard order asserted; 320/375px no overflow; zero external requests/console/page errors. No new primitives; `src/components/ui` and `src/media` AGENTS.md untouched.
5. **Write-set guards — clean.** Zero diffs to `src/auth/storefront-settings.ts`, `src/app/storefront/route.ts`, `src/media/*`, `prisma/*`, admin, checkout, `/store`, `pop/kanban` (name-only diff audited). Request-log inventory +1 entry only. `DESIGN.md`, root `AGENTS.md` storefront bullet (upload route + request-log entry + omission rule), and the spec slice are synced and accurate.
6. **Deviations — within conventions.** The evidence spec seeds/deactivates BRL only through the existing `POST /admin/exchange-currencies` mutation via browser-native `fetch` (session cookie + automatic Origin; documented in-spec comment); `scripts/run-admin-evidence.mjs --store-settings` mirrors the established `--profile` runner pattern with its own password env.

## 3. Evidence re-run (all in the worktree, 2026-07-24)

- `CI=true pnpm check` — green: lint 0 errors (13 warnings), typecheck, **967 passed / 7 skipped (128 files)**, build.
- `pnpm tokens:check` — green; token generator/lint contract tests pass (focused vitest across the six task test files + observability/i18n: 62 passed; full suite subsumes).
- `pnpm store-settings:evidence:verify` — green: run `20260724211133`, 51 hash-bound captures, manifest/review binding valid.
- Visual loop (own vision over final captures): 1440px pt-BR grid, 375px en dark-theme, staged-logo, save-pending — composition, hierarchy, fallback lockup, disabled/busy states all correct; no overlap/overflow; severity <2.
- `python3 pop/scripts/pop_validate.py --standalone` (main checkout) — valid, zero violations (1 benign warning: worktree task is in 005, not 004).

## 4. Findings

1. **nit (severity 1)** — two live `@next/next/no-img-element` lint warnings (`src/app/storefront-preview.tsx:58`, `src/app/storefront-settings-management.tsx:190`); codebase precedent (`public-checkout-form.tsx:249`) carries an explicit eslint-disable with justification. Gate stays green; cosmetic.
2. **nit (severity 1)** — `DESIGN.md` keeps the stale sentence "The storefront card composes labelled `Input` text controls and one horizontal `Checkbox` enablement toggle…" (the removed 5.1.1 card) directly after the new workspace paragraph. Minor doc drift; the new paragraph fully describes reality.

## 5. Decision

**APPROVE.** The original request is met with runnable, hash-bound evidence; both 003 contracts are honored; write-set guards hold; all re-run gates pass. The two severity-1 nits do not impede approval and may be fixed opportunistically later.

verificado por revisor independente (yolo) — 2026-07-24
