# M-4.2-exclude-worktrees-from-tooling — Verification (005)

- **Date:** 2026-07-25
- **Strategy:** `differential` — non-critical, size S, first 005 round. Re-executed the changed surface (tsconfig exclusion, epoch1 guard) with adversarial probes; audited the integrated diff and worktree state directly.
- **Delivery under review:** branch `task/M-4.2-exclude-worktrees-from-tooling`, commit `34269ee`, worktree `pop/worktrees/M-4.2-exclude-worktrees-from-tooling`.

## Original request first

The card asks to stop task worktrees under `pop/worktrees/` from leaking into main-checkout tooling (observed: `pnpm typecheck` scanning a leftover worktree; M-4.1 already covered Vitest). **Met.** `tsconfig.json` now excludes `pop/worktrees`, and the exclusion is pinned by a contract assertion in `scripts/check-epoch1-source.mjs` mirroring the pre-existing ESLint guard.

## Criteria and evidence

1. **Diff scope** — `git show 34269ee` touches exactly `tsconfig.json` (+`"pop/worktrees"` in `exclude`) and `scripts/check-epoch1-source.mjs` (+tsconfig read, +`requireSource` guard, +summary token). No ownership invasion.
2. **Not over-broad** — `git ls-files pop/worktrees` in both main checkout and worktree returns only `pop/worktrees/.gitkeep`; no legitimate tracked source is hidden by the exclusion. The `include` globs (`**/*.ts(x)`, `**/*.mts`) would otherwise match worktree sources, so the exclusion is precisely the leak fix.
3. **Guard is real (mutation test)** — with a temporary stub `src/app/page.tsx` (deleted afterwards), `pnpm epoch1:source-check` passes end-to-end (`EPOCH1_SOURCE_OK ... tsconfig_worktrees=excluded ...`), proving the new assertion at line 34 is reached. Reverting the tsconfig line makes the script fail with exactly `tsconfig.json does not exclude generated PoP worktrees.` tsconfig restored byte-identical afterwards (`git checkout --`).
4. **Positioning vs. pre-existing ENOENT** — `src/app/page.tsx` is absent on develop and in the worktree (owned by sibling M-4.3), so the script currently dies in the top-level `Promise.all` before any assertion. The stub run above proves that once M-4.3 restores the file, the only checks before line 34 are the ESLint guards, which pass — the tsconfig assertion will run. Not a defect of M-4.2.
5. **Exclusion proven both directions** — dropped a deliberately broken `pop/worktrees/probe/broken.ts` (TS2322) in the worktree: `pnpm typecheck` green with the fix; the same file fails under a probe tsconfig without the exclusion. Probe files removed; worktree `git status` clean.
6. **Aggregate gate** — `CI=true pnpm check` in the worktree: exit 0 (typecheck, lint, 994 tests passed / 7 skipped, `next build` succeeded).

## Findings

- **nit** — the guard regex pins the exact `exclude` array order (`["node_modules", "pop/worktrees"]`); a harmless reorder would trip it. Mirrors the existing literal-style guards in the same script, so accepted as-is.
- **note (not a finding against M-4.2)** — `pnpm epoch1:source-check` remains red on develop until M-4.3 restores `src/app/page.tsx`; pre-existing, correctly routed.

## Decision

**APPROVED.** Original request met; diff minimal and exactly as claimed; guard verified by mutation; both-direction exclusion proven; aggregate gate green.

verificado por revisor independente (yolo) — 2026-07-25

## Telemetry

- stage: 005_verifying · context: strong (fresh session) · returns: 0/2 · strategy: differential (diff audit + mutation probes + re-run of changed surface and aggregate gate) · result: approved
