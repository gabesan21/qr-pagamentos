# 7.1.2-extend-merchant-store-settings — Verification

- **Task:** [[7.1.2-extend-merchant-store-settings]] · **Critical:** yes · **Mode:** full (mandatory — `critical: true`)
- **Verified:** 2026-07-24 · **Branch:** `task/7.1.2-extend-merchant-store-settings` @ `e5b7b76` (merge-base `62de4a3`)

## Original request first

The card's What/Why is met. The persisted owner store contract now durably records, in one re-authorized save: theme id (closed six-theme set), logo media reference, boxed/table layout, standalone-payments toggle alongside the existing storefront toggle, slug, bilingual display names, accent color, and one active default currency — with deterministic service-level fallbacks (theme → `pix-paper`, layout → `boxed`, currency → registry default → explicit unavailable signal), canonical media lifecycle for the logo, and registry-gated currency assignment where a missing/inactive mapping is a normal opaque-failure state, not an error leak. The independent-toggles contract (disabled storefront keeps `/store/[slug]` unavailable while payment links stay reachable) is persisted and documented, with checkout consumption explicitly deferred. No UI was added (7.1.3's scope).

## Scope audit (diff vs merge-base, 21 files, +830/−45)

- Only files inside the plan's front write sets: migration dir + `prisma/schema.prisma` + `pop/scripts/db-test.mjs`; `src/media/media-service.ts(.test)` + `src/media/AGENTS.md`; `src/auth/storefront-settings.ts(.test)`, `src/app/storefront/route.ts(.test)`, new `src/design-system/themes.ts(.test)`; `src/storefront/public-storefront.ts(.test)`; contracts (root `AGENTS.md`, `prisma/AGENTS.md`, three specs); plus a type-driven fixture update in `src/app/storefront-settings-management.test.tsx` (test-only, no UI change).
- Zero `pop/kanban` edits, zero edits to any existing migration directory, zero UI/checkout/admin/sessionless-route changes.

## Scrutiny points

1. **Migration** — `20260724210000_store_settings_extension` sorts after `20260724120000`; five additive columns only; closed-set `IN` checks for theme/layout; `AAA`–`ZZZ` bounds check via `and`/`gte`/`lte` (no regex); `BOOLEAN NOT NULL DEFAULT true` typed constant; no FK on the 43-char logo identifier; `migration-policy verify` confirms byte-exact generated SQL and untouched 19-dir baseline (`PASS migration-policy baseline=19 future=5`). Runtime table-level grants proven sufficient by `db:test` runtime-user assertions.
2. **Media ops** — `activateOwned`/`orphanOwned` resolve the identifier internally, re-check owner+purpose, and claim through the same revision-fenced `store.transition`; the drift test proves a concurrent revision bump fails closed as unavailable. Replace sequence is activate→save→orphan with best-effort compensating orphan on save failure: the ordering test pins `["activate","save","orphan"]`, the compensation tests prove the current logo is never lost and failure never leaves a broken reference (worst case: quota-counted unreferenced `ACTIVE`). Quotas, reconciliation, and the read path are untouched.
3. **Merge semantics** — route forwards extended fields only when `form.has(field)`; service merges absent=unchanged, explicit blank clears the nullable fields. The regression test "preserves every extended field on a legacy-only save" seeds all five extended values, submits a legacy-only input, and asserts full preservation with zero media/registry side effects; the route test pins the legacy form's forwarded shape.
4. **Currency** — new non-null assignments gate on `requireActivePair`; `NoActiveExchangeCurrencyMappingError` propagates to the route's opaque `?storefront=failed`. Read-after-deactivation returns the stored code and a save that does not carry the field never re-gates (both tested). Format `[A-Z]{3}` enforced at the service boundary; reads return the stored code as-is.
5. **Projection** — `/store/[slug]` selects and exposes only resolved `themeId`, resolved `layout`, and the opaque `logoMediaIdentifier`; the currency code and toggles are not even selected. A key-set assertion pins the exposed shape to exactly six keys.
6. **Six-theme single source** — `src/design-system/themes.ts` derives the frozen id set and default from `tokens/resolver.json`; the contract test asserts equality with the resolver keys and pins the closed six ids + `pix-paper` default.
7. **Spec/DOX sync** — root `AGENTS.md` storefront and media bullets, `prisma/AGENTS.md` invariant (no FK, no regex tightening, no backfill), `src/media/AGENTS.md` (new operations admitted, all other negative constraints intact), and the three specs (storefront-and-customization requirements + implemented slice, media-storage attachment pattern, catalog consumer note) are accurate against the delivered code.

## Evidence re-run (worktree, all green)

- `CI=true pnpm db:generate` — Prisma Client regenerated clean.
- `CI=true pnpm db:contract-check` — `PASS migration-policy baseline=19 future=5`, `PASS migration-policy-language`, `PASS migration-policy-fixtures`, `PASS documentation-contract`.
- `CI=true pnpm db:test` — 30/30 PASS including `storefront-settings-schema` with the new column/check/default and bounds-check SQLSTATE assertions.
- Focused vitest — 40/40 passed across 6 files (service 18, media 8, route 4, projection 4, themes 3, management fixture 3).
- `CI=true pnpm check` — typecheck + lint + full test suite + build, exit 0.
- `CI=true pnpm media:decoder-preflight` — PASS (sharp 0.34.5, vips 8.17.3).
- `python3 pop/scripts/pop_validate.py --standalone` (main checkout) — only the two known orchestrator-owned plan-length violations (7.1.2 plan 180>150, 7.1.3 plan 237>150); not delivery findings.

## Findings

None blocking. Nit (non-blocking): re-submitting the currently stored currency code re-gates on `requireActivePair`, so a 7.1.3 form that always posts the field would fail the whole save after a later deactivation; this matches the approved plan's "non-null code on save is gated" wording and harms no current consumer — 7.1.3 should omit unchanged fields.

## Decision

**APPROVE.** The original request is fully met, all six scrutiny points verified against code and tests, and every gate re-ran green.

— verificado por revisor independente (yolo) — 2026-07-24
