# Verification — [[10.2.2-build-admin-payment-links-directory]]

- **Stage:** 005_verifying · **Gate:** yolo-critic (strong, independent) · **Mode:** FULL (critical: true — mandatory)
- **Date:** 2026-07-27 · **Branch:** `task/10.2.2-build-admin-payment-links-directory` (6 commits over merge-base `6642cde`, HEAD `9e971c7`)

## 1. Original request first (card "What/Why")

The card asks for an administrator-only read projection and paginated table/detail UI for all links with owner/profile navigation, status, type, product lines or fixed value, expiry, generated-order counts, and drill-down, without cross-owner mutations. **Met.**

- `/admin/payment-links` is the delivered `admin-payment-links-v2` registration over `queryAdministratorDirectory` (`ADMIN_GLOBAL`): page sizes 10/20/50/100 with default 50, `(createdAt desc, id desc)` keyset with `pageSize + 1` bounded reads (unit test asserts `take === 51` at default 50), filters `state`/`type`/`kind`/`from`/`to` plus `q` (exact 24-char identifier match, otherwise bilingual description + owner-username containment), canonical `307`s before I/O, zero-I/O invalid-query.
- `/admin/payment-links/v2/[id]` is the read-only detail: `requireAdminShellContext` → `getForAdmin` re-authorizes exact role/status, one bounded global read, one opaque unavailable outcome for malformed/missing identities, owner attribution card, and the `/admin/orders?link=<identifier>` drill-down. No mutation surface anywhere (no route handlers added; page tests assert no POST form, no edit/lifecycle affordance; evidence asserts zero mutation forms at runtime).
- Owner navigation targets the interim `/admin/accounts`; soft-deleted owners keep every row with the localized non-color deleted badge (proven live: the evidence run soft-deletes `admin.links.gone` through the delivered `POST /admin/users/[id]/delete` route and the rows persist, derived `inactive`).
- The V2-only scope boundary is honestly recorded in the spec and code comments, matching the 10.2.1 precedent — a declared scope decision, not a gap.

## 2. Diff audit (develop..HEAD)

21 files, +2180/−84. Frozen fences hold: `git diff develop...HEAD -- src/auth/payment-link.ts src/auth/payment-link-v2.ts src/auth/payment-link-v2-view.ts src/data-directory src/orders prisma` is empty — V1 link files/trigger, the owner V2 view, the foundation, and order modules are byte-untouched. No conflict markers anywhere (only pre-existing LICENSE `=======` decorations). Shared files (`package.json`, `scripts/run-admin-evidence.mjs`, three spec notes, `AGENTS.md`) kept both sides through the two rebases; the harness change is a strictly additive `--admin-payment-links` mode block alongside 10.2.1's `--admin-orders`.

## 3. Contract invariants

- **Re-authorization before I/O, one shared forbidden outcome:** `queryAdministratorDirectory` throws `DirectoryAuthorizationError` for any non-`ADMIN`/non-`ACTIVE` principal before adapter I/O; `getForAdmin` throws `ForbiddenError` first. Tests prove `USER`, disabled-admin, and anonymous paths fail before any store call, and the pages redirect to `/`/`/login` with zero directory I/O.
- **Derived lifecycle, filter↔derivation agreement:** the local Prisma `stateCondition`/`confirmedPaymentCondition` mirror is byte-identical to the owner view's private mirror (`payment-link-v2-admin-directory.ts:151-169` vs `payment-link-v2-view.ts:250-268`). The 25-row fail-closed matrix (`payment-link-v2-admin-directory.test.ts:264-309`: 2 types × 2 active × 3 expiry × 2 paid + 1 pending-order row) evaluates every emitted where-fragment through an evaluator that throws on any unknown key and asserts exact agreement with the imported `derivePaymentLinkV2State` across all four states — drift fails loudly, including the `paid` = settlement-claim ∪ CONFIRMED-LINK-order semantics (a PENDING LINK order never counts).
- **Derive-don't-recompute:** rows are built through the imported `toPaymentLinkV2DirectoryRow` (which calls `derivePaymentLinkV2State`); the tuple validator is the imported `validatePaymentLinkV2DirectoryTuple`. No reimplementation.
- **Redaction:** `adminViewSelect` is a whitelist of exactly the owner-projection fields plus `owner: { username, deletedAt }`; `currencyPair` selects only `label`. No pair UUIDs, owner email/id, provider data, verifiers, capability material, or lifecycle CAS fields (`version` is never selected). A dedicated test asserts the exact row key set and the exact `{username, deletedAt}` owner shape. The internal link UUID on the detail is sanctioned (the detail route is keyed by it; the plan's redaction list does not cover it).
- **Bounded keyset:** forward/backward seeks verified with exact expected where-fragments; no offset, no total count, `pageSize + 1` takes.
- **Cursor authority:** cursors bind `ADMIN_GLOBAL`; foreign-directory and foreign-purpose cursors are rejected with zero I/O (test); stale page-size cursors reset canonically.
- **Soft-delete attribution:** eligibility and ordering never change on deletion; badge fact travels only on this administrator surface.

## 4. Spec compaction (administrative-foundation.md 169→139)

Word-level diff of removed vs added lines: the only words lost are `areas`, `call`, `scaffolds` — the two stale scaffold sentences (page-map row "payment-link scaffold" → "global payment-link directory"; "dashboard and global payment-link areas are honest scaffolds" → "dashboard area is an honest scaffold"). Everything else is pure unwrap; every contract and invariant (role model, bounded directory contract, cursor rules, 10.2.1 order directory, payer exposure, deleted-owner semantics) survives verbatim. New 10.2.2 paragraph and implemented-slice entry are append-only. `wc -l` = 139 ≤ 150. **No binding content lost.**

## 5. Gates re-run by this reviewer (own exit codes, worktree)

| Gate | Result |
|---|---|
| `pnpm typecheck` | exit 0 |
| `pnpm test` | exit 0 — 179 files, 1534 passed, 26 skipped (incl. new service 12 tests, directory-query 5, page 11, detail 5) |
| `pnpm admin-payment-links:evidence:verify` | exit 0 — "Verified administrator global payment-links directory evidence 20260727035740" (48 captures hash-bound, source inventory hash-bound, zero severe axe/overflow/target/focus failures) |
| `pnpm admin:source-check` | exit 1 by design; failure list extracted and diffed against main repo: exactly 120 lines, byte-identical to the baseline — zero new violations |
| `pnpm admin:contract-check` | exit 0 |
| `pnpm lint` | exit 0 (49 warnings, 0 errors; 47 pre-existing, 2 new nits below) |
| `pnpm check` / `pnpm db:test` / docker evidence run | accepted on the executor's report (all exit 0) + artifact spot-check of run 20260727035740 (manifest, review.md, captures inspected) |

## 6. ui-review (directory + detail)

Inspected `directory-pix-paper-en-1440`, `state-pt-BR-deleted-owner-badge-1440`, `state-en-link-detail-deleted-owner-1440`, `directory-midnight-clearing-pt-BR-375` from run 20260727035740. All six directory states are implemented and proven (ready, empty, filtered-empty, invalid-query without input echo, error with retry, loading via the foundation); bilingual parity holds (dictionary files are key-identical, 10 new keys each, both locales captured); the owner cell's ghost button carries `data-ds-hit-target` and the runtime assertions prove every target ≥ 44×44; the deleted badge is the non-color outline variant in both locales; mobile 375/320 reflow stacks without horizontal overflow; focus visibility and axe serious/critical are clean on all 48 captures. Read-only detail renders composition facts, derived state, expiry, order count, owner attribution, and drill-down only — no owner mutation affordance.

## 7. Merge/rebase quality

Two rebases onto develop preserved both sides of shared files. No conflict markers. Two rebase artifacts found (non-binding, see findings 1–2).

## Findings

1. **sugestão — stale duplicate DOX bullet (rebase artifact).** `AGENTS.md:100` re-adds the pre-10.2.1 short `src/app/admin/orders/` bullet ("…hold no mutation surface.") directly under the delivered 10.2.1 long bullet at `AGENTS.md:99`, which is a strict superset. Redundant, no contradiction; remove the short line at closeout.
2. **nit — duplicated changelog in spec frontmatter (rebase artifact).** `pop/specs/administrative-foundation.md:8` — the `Updated` line now carries the 10.1.1/10.3.1/8.3.1 entries twice (the 10.2.2 entry was prepended to a stale chain, then the full develop chain follows). Non-binding metadata; cosmetic.
3. **nit — two lint warnings in the new test.** `src/auth/payment-link-v2-admin-directory.test.ts:54,55` — `_input`/`_id` mock parameters flagged by `no-unused-vars` (the config does not whitelist the underscore prefix). Gate stays exit 0.
4. **nit — filter definitions re-declared rather than spread.** `src/auth/payment-link-v2-admin-directory.ts:55-61` re-declares the state/type/kind filter entries with literal type/kind values instead of spreading `PAYMENT_LINK_V2_DIRECTORY_FILTER_DEFINITIONS` and appending `from`/`to`. Values are pinned by contract tests, so drift risk is covered; deviation from plan decision 3's letter, not its intent (the brief is strategy, and the delivered behavior meets the objective).

No blocking findings.

## Decision

**APPROVE.** The card objective is fully met with contract invariants intact, frozen fences untouched, spec compaction lossless, every gate re-run green by this reviewer, the source-check failure list byte-identical to the 120 baseline, and the run-bound production evidence verified against run 20260727035740. Findings 1–4 are non-blocking hygiene items for closeout.

verificado por revisor independente (yolo) — 2026-07-27
