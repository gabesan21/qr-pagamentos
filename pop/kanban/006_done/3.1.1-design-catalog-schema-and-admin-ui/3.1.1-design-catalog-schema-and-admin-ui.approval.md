### Resposta do revisor (yolo)

- [x] Feito

aprovado por revisor independente (yolo) — 2026-07-20

**Rationale:** O plano atende aos critérios do gate 003 para uma task yolo `size: M`. O objetivo, arquivos afetados, estratégia sequencial, frentes, dependências, riscos e critérios de aceite estão claros e verificáveis. O plano segue a spec `pop/specs/catalog-and-payment-links.md` (catálogo admin-only de UUIDs da Nautt, migrações versionadas, validação server-side de UUID, flags ativo/inativo, UI bilíngue com dicionário, respostas protegidas 401/403 opacas) e respeita as regras do `AGENTS.md` raiz (rotas não prefixadas por locale, disciplina de migração do `prisma/AGENTS.md`, reutilização do design system do `src/components/ui/AGENTS.md`, `pnpm db:test` separado do `pnpm check`).

**Riscos principais:**
- A spec permite exclusão, mas o plano cobre apenas create/update/toggle (desativação). Válido para a primeira fatia, mas registre no fechamento se o delete será adiado ou incluído.
- `src/auth/nautt-catalog.ts` deve permanecer claramente separado do `src/auth/payment-settings.ts` existente (allowlist singleton BRL/PIX) para evitar confundir os dois catálogos.
- Confirme se os primitivos de tabela/lista em `DESIGN.md` cobrem a listagem do catálogo; caso contrário, atualize `DESIGN.md` ao introduzir novo estado/primitivo.
