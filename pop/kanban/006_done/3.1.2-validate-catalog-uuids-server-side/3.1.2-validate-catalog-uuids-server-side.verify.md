# 3.1.2 — Verificação (005)

**Task:** [[3.1.2-validate-catalog-uuids-server-side]]  
**Revisor:** yolo-critic  
**Data:** 2026-07-20

## Veredito

Aprovado. O diff fecha o gap de testes identificado no plano: as rotas de catálogo agora exercitam o serviço real com a validação de UUID, autorização e redirects opacos. Todos os gates de verificação passaram.

## Critérios verificados

- [x] Chamadores não autenticados/não-admin recebem `401`/`403` vazios nas 4 rotas POST de catálogo.
- [x] Criação com UUID malformado, vazio ou ausente é rejeitada com redirect opaco (`?error=catalog-create-failed`).
- [x] Atualização/toggle com `[id]` inválido ou corpo malformado é rejeitado com redirect opaco (`?error=catalog-change-failed`).
- [x] UUIDs canônicos válidos são aceitos; UUIDs uppercase são normalizados para lowercase.
- [x] `pnpm lint`, `pnpm typecheck` e `pnpm test` passam.
- [x] `pnpm db:test` e `pnpm db:contract-check` passam.

## Evidências

```
$ pnpm lint
$ node scripts/check-design-tokens.mjs && eslint .
# OK

$ pnpm typecheck
$ tsc --noEmit
# OK

$ pnpm test
Test Files  60 passed | 1 skipped (61)
     Tests  552 passed | 4 skipped (556)
# OK

$ pnpm db:test
PASS migrator-connect
PASS migration-replay
PASS migration-idempotence
... (23 checks)
# OK

$ pnpm db:contract-check
PASS documentation-contract
# OK
```

## Achados

### `sugestão` — Local do helper de teste

- **Arquivo:** `src/auth/nautt-catalog-test-store.ts`
- **Impacto:** O helper só é importado por arquivos de teste, mas reside na árvore de produção `src/auth/`. Isso não quebra o build/lint, mas mistura código de teste com código de domínio.
- **Razão:** Mover o helper para um diretório de testes (ex.: `src/auth/__tests__/fixtures.ts` ou `tests/helpers/`) deixa a separação de responsabilidades mais clara e reduz o risco de importação acidental em produção.

### `nit` — Metadado de card já concluído

- **Arquivo:** `pop/kanban/006_done/3.1.1-design-catalog-schema-and-admin-ui/3.1.1-design-catalog-schema-and-admin-ui.md`
- **Impacto:** O diff limpa `claimed_by` e `claimed_at` do card 3.1.1 (já em `006_done`). Não afeta comportamento, mas é uma alteração fora do escopo da task 3.1.2.
- **Razão:** Provável limpeza mecânica do orquestrador; manter registro no verify para rastreabilidade.

## Assinatura

verificado por revisor independente (yolo) — 2026-07-20
