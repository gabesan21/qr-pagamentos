# Verification — [[10.3.2-build-admin-user-directory]]

- **Stage:** 005_verifying · **Mode:** FULL (first return count 0, but stale-base merge risk + cross-layer admin surface warranted full depth) · **Date:** 2026-07-27
- **Surface:** merge-base `05dd378`..`82eaa1d` (7 commits) in `pop/worktrees/10.3.2-build-admin-user-directory`
- **Tests re-run by this reviewer:** `pnpm typecheck`, `pnpm test`, `pnpm admin-users:evidence:verify`, `pnpm lint`, `pnpm admin:contract-check`, `pnpm admin:source-check` (baseline comparison)

## Original request first

Card "What": *a paginated searchable bilingual user table with role, active/disabled/deleted status, store state, created/last-activity evidence, and explicit edit and soft-delete actions* — **met.** `/admin/accounts` is now the `admin-users` registration over the delivered `queryAdministratorDirectory` foundation (10/20/50/100, default 50, `(createdAt,id)` keyset, role/state/from/to/`q` filters), rows carry the sanctioned admin DTO plus derived `deleted`>`disabled`>`active` state, derived store state, and the `lastActivityAt` max-session-sighting aggregate; edit navigates to the delivered read-only `/admin/accounts/[id]` detail (10.3.3's pinned route) and delete posts the byte-frozen `POST /admin/users/[id]/delete`. Bilingual dictionaries are key-for-key parity (32/32). Card "Why" (account operations + profile navigation) is served by the directory plus the detail route contract.

## Fences (all hold)

- `git diff --name-only 05dd378..HEAD` shows **zero** changes under `src/app/admin/users/`, `src/auth/administration.ts`, `src/auth/identity.ts`, `src/auth/session.ts`, `src/data-directory/`, `src/orders/`, `src/observability/`, `src/security/` — mutation routes, the foundation, and the request-log/rate-limit inventories are byte-frozen.
- No admin user mutation added; 10.3.3's profile-editor surface not implemented (detail is read-only; page tests assert no `/role`, `/status`, `password` forms render).
- Legacy inline forms retired only from the page: `AdminAccountsSurface` drops the `Accounts` list and keeps the notice strip + unchanged `CreateAccount`; the frozen routes remain live and covered by the untouched `administration.test.ts` / `account-mutation-form.test.ts` suites (passing). The interim window is recorded in the spec amendment; 10.3.3 is the next dependent task.

## Contract invariants (all verified)

- **Authorization:** `queryAdminUserDirectory` resolves the cookie principal via `requireAdminFromCookie` before canonicalization; the detail read re-checks exact `ADMIN`+`ACTIVE` (`requireAdministrator`) and throws `ForbiddenError` otherwise; tests prove merchant/disabled-admin denial with zero store I/O and `/login`/`/` page redirects.
- **Bounded keyset:** adapter takes `pageSize + 1` (test asserts `take: 51`), forward/backward seek tuples asserted exactly; no offset.
- **Redaction:** row key set asserted closed (`id, username, email, role, status, createdAt, deletedAt, state, storeState, lastActivityAt` — no `storefrontSlug` on rows); detail adds only `storefrontSlug`. Session select is bounded `take: 1` on `lastSeenAt` only — never id/digest/count. `q` containment covers only the two sanctioned admin DTO text fields, matching the amended spec boundary (email is inside the administrator user DTO contract; the "never owner email" fences in the 10.2.x directories are about attribution on order/link rows, not the user directory's own DTO).
- **Derived-state honesty:** `deleted` > `disabled` > `active` computed at read time from `deletedAt`/`status`, never stored; filter↔derivation mapping asserted per value; deleted users are never filtered out of pages and render no actions (row + detail), confirmed in unit tests and in the production evidence (page-2 capture shows the deleted row last with badge and no action cell).
- **Cursor binding:** foreign-directory and `MERCHANT_OWN`-purpose cursors rejected with zero I/O; `ADMIN_GLOBAL` cursors are purpose-bound and shared across admins, per the foundation contract.

## Gates re-run by this reviewer (explicit exit codes)

| Gate | Exit | Evidence |
|------|------|----------|
| `pnpm typecheck` | 0 | clean |
| `pnpm test` | 0 | 179 files passed / 8 skipped; 1539 tests passed |
| `pnpm admin-users:evidence:verify` | 0 | `Verified administrator user directory evidence 20260727044703` — 48 captures hash-bound, source inventory hash-bound to HEAD `82eaa1d` |
| `pnpm lint` | 0 | 49 warnings, 0 errors |
| `pnpm admin:contract-check` | 0 | `ADMIN_CONTRACT_SYNC_OK` |
| `pnpm admin:source-check` | 1 | **pre-existing baseline**: failure set (120 findings, all in `src/app-shell/` + merchant catalog) is byte-identical to develop — diffed both outputs, `IDENTICAL BASELINE`; not introduced by this branch |
| `pnpm check` / `pnpm db:test` | 0 (executor report) | accepted on report + the production evidence run itself builds and exercises the docker image |

## Spec note (stale base — as briefed)

The branch compacted `administrative-foundation.md` against the stale 195-line base (develop is at 139 after the 10.2.2/10.1.2 merges); line count **not** flagged. The append content itself was verified correct and minimal for re-application: one user-directory amendment paragraph (derived state/store/activity facts, sanctioned `q` fields, delete-action surfacing, opaque detail outcome, legacy-form interim window, 10.3.3 route contract, create-account/notice-strip preservation), one implemented-slice entry, the `Updated` header line, and one-line notes in `product-scope.md` and `checkout-and-order-lifecycle.md`. The root `AGENTS.md` bullet sentence matches the delivered behavior.

## ui-review (evidence run 20260727044703, 48 captures + assertions.json)

- Six directory states honest: ready/empty/filtered-empty/invalid-query (no input echo, proven)/error (unit)/loading (foundation); deleted badge is `Badge variant="outline"` (non-color) with text label in both locales; deleted rows/details render zero actions.
- All 48 captures pass axe (0 serious/critical), 44×44 hit-target, no-horizontal-overflow, and visible-focus assertions, re-verified by the verify script.
- Bilingual parity confirmed in dictionaries and captures (pt-BR `Excluída`/`Desativado` badges, localized detail).

## Findings (none blocking)

1. **suggestion — merge-time spec wording** (`pop/specs/administrative-foundation.md` append, branch line 60): the paragraph calls 10.3.2 "the **second** approved `ADMIN_GLOBAL` directory"; on develop, 10.2.2's payment-link directory already exists, so post-merge it is the third. The branch's stale base cannot see 10.2.2, so the fix belongs to the orchestrator's re-application: reword to "an approved `ADMIN_GLOBAL` directory" (base-independent). One-word change at merge; no 004 return.
2. **suggestion — destructive action without confirmation** (`src/app/admin/accounts/page.tsx:80-83`, `[id]/page.tsx:56-60`): the irreversible soft delete is a single-click native POST from both the row and the detail. The route and redirect contract are frozen (10.3.1) and the plan pinned this surfacing, so no change here — but 10.3.3's editor should add a confirmation affordance.
3. **nit — generic search placeholder** (`src/app/admin/accounts/directory-copy.ts:11`): the toolbar reuses the foundation's "Reference or label" placeholder, which reads oddly for a user directory; a directory-specific placeholder ("Username or email") would be clearer. Foundation copy is shared, so this is a copy-contract decision, not a defect.
4. **nit — duplicated seek-tuple validator** (`src/app/admin/accounts/directory-query.ts:37-46` vs `src/auth/admin-user-directory.ts:91-100`): intentional (the service keeps it private; comment says so); acceptable duplication of a 9-line pure predicate.
5. **nit — "Edit" label lands on a read-only page until 10.3.3** (`page.tsx:77`): the destination honestly discloses "editing arrives with the profile editor" in both locales, and the route contract is pinned; label semantics resolve when 10.3.3 lands.

## Merge notes for the orchestrator (006)

- Branch base `05dd378` predates the 10.2.2 and 10.1.2 merges into develop. Re-apply (not just merge) onto develop: the spec append (with finding 1's reword), the root `AGENTS.md` bullet sentence, the `scripts/run-admin-evidence.mjs` additive mode block (10.2.2 appended its own block to the same file), and the dictionary registrations (distinct namespaces, trivial).
- `develop..HEAD` diff shows deletions of 10.1.2/10.2.2 files — these are merge-base artifacts, **not** branch deletions; the true task diff is `05dd378..HEAD` (audited above).

## Decision

**APPROVE.** The card objective is met, all fences hold, contract invariants are proven by tests and run-bound production evidence, and every re-runnable gate passes (the one red gate is a byte-identical pre-existing baseline). Five non-blocking findings recorded; finding 1 is a mechanical merge-time reword for the orchestrator.

verificado por revisor independente (yolo) — 2026-07-27
