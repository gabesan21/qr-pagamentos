# Verification — [[8.3.3-build-owner-orders-management-ui]]

- **Stage:** 005_verifying · **Responsible:** independent yolo critic (strong, fresh context)
- **Mode:** `differential` — non-critical, first pass, no prior return. Surface re-run: the changed UI/projection/evidence surface (`pnpm check` full aggregate + `pnpm orders:evidence:verify`); delivered contract surfaces audited by diff (zero-diff proof) instead of re-run, plus their unedited tests inside the aggregate gate.
- **Worktree:** `pop/worktrees/8.3.3-build-owner-orders-management-ui` · `git diff develop...HEAD` = 83 files, +9049/−91, tip `90bd5cf`.

## 1. Original request first

Card "What": bilingual responsive order table/detail UI with payer and source/link facts, filters, page-size localStorage preference, comment editing, guarded actions, and all loading/empty/error states. **Met.**

- `/orders` composes the V2 directory over the delivered 8.3.1 query (payer facts, source, link identifier, separate state/local-outcome badges, tabular exact-decimal amount, created date; `source`/`money`/`from`/`to`/`link` filters + `q`; keyset prev/next; sizes 10/20/50/100 default 20) above the untouched V1 ledger section; `/orders/[id]` V1 detail untouched (zero diff on `src/app/orders/order-views.tsx` and `src/app/(merchant)/orders/[id]/`).
- `/orders/v2/[id]` renders full facts, policy-exact customer snapshot, ordered lines, the comment thread (append + author edit posting hidden `commentId`/`commentVersion`), and the guarded `LOCAL_FINALIZED`/`LOCAL_CANCELLED` forms posting the projection-supplied `lifecycleVersion` behind `details` + warning-`Alert` confirmations — the exact `/links` guard precedent.
- Page-size preference is one observation-only client boundary (`page-size-preference.tsx`): stores on toolbar change, navigates once only when no explicit `pageSize` and no notice; explicit URL wins and is re-stored — no loop, no canonicalization violation, native GET never intercepted.
- All six directory states, four closed notices, unavailable detail — bilingual (dictionary key parity en/pt-BR verified), responsive 320–1440 (evidence grid + 320px capture sampled).

## 2. Criteria and evidence

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Aggregate gate | PASS | `pnpm check` re-run in worktree: 154 test files / 1180 tests passed (15 skipped), lint/typecheck/build green |
| 2 | Directory over delivered query; V1 byte-frozen | PASS | page tests (redirects, canonical target, pagination URLs); zero diff on all V1 paths |
| 3 | Notice strip before canonicalization, closed set | PASS | `directory-query.ts` strips/validates the closed set pre-canonicalization (catalog `noticeKey` precedent, incl. invalid value → zero-I/O invalid-query); page test asserts all four notices in both locales + forged notice rejected before read |
| 4 | Row facts, no UUID leak | PASS | page test + sampled captures: payer tuple, badges, link identifier, amounts; no currency-pair/product UUIDs |
| 5 | Detail facts, opaque unavailable | PASS | `v2/[id]/page.test.tsx`: facts/lines/thread asserted; one opaque view for cross-owner/malformed/missing |
| 6 | Guarded actions, CAS hidden fields | PASS | tests assert form grammar (`append-comment`/`edit-comment`/`set-outcome`, `commentVersion`, `version=lifecycleVersion`); evidence drives real UI incl. stale-CAS opaque failure |
| 7 | Additive exposures only | PASS | `order-v2-view.ts` diff = `payer` on summary + `lifecycleVersion` on detail, nothing else; zero diff on `order-v2-directory.ts`, `order-engagement-v2.ts`, `order-v2.ts`, `order-view.ts`, `merchant-analytics.ts`, both routes, `data-directory/server/`, `prisma/`, `src/app-shell/`, `src/app/admin/`, `src/app/api/` |
| 8 | Page-size preference | PASS | evidence asserts stored-50 applies to bare URL exactly once, change observed→stored, explicit `?pageSize=10` wins over stored 50 and is re-stored |
| 9 | Bilingual/responsive/states | PASS | key parity check; 48 captures (6 themes × 2 locales × 3 widths + 12 states); DESIGN.md orders paragraph rewritten |
| 10 | Evidence pair | PASS | `pnpm orders:evidence:verify` re-run: "Verified merchant orders management evidence 20260725233055"; manifest-hash-bound, 0 console/page/external errors |

## 3. Fence audit (self-verified diff)

- Zero diff on every delivered service contract, both V2 routes, V1 views/section, `data-directory/server/**`, `prisma/**`, shell, admin, API. No migration.
- `data-directory.tsx` extension is optional-props-only (`pageSizes`, `textFilters`); existing data-directory/links/catalog tests pass unedited.
- `AGENTS.md`, `src/data-directory/AGENTS.md`, `DESIGN.md`, and `pop/specs/checkout-and-order-lifecycle.md` record the new workspace contract, the two additive exposures, and the 8.3.3 attribution.

## 4. Deviations judged

1. **Payer fixture fields** (`merchant-analytics.test.ts`, `order-v2-directory.test.ts`, +1 line each): required typecheck fallout of the sanctioned non-optional `payer` field. Acceptable.
2. **`run-admin-evidence.mjs --orders` mode stales the links source-hash binding:** confirmed — only the links manifest binds that script, so `pnpm links:evidence:verify` fails post-merge until `pnpm links:evidence` re-runs. Mechanism fully understood: the links spec auto-regenerates manifest + `review.md` + `current.json` self-consistently (review.md is spec-written, `tests/payment-links.evidence.spec.ts:481`), so the re-run is purely mechanical. Not documented in card/plan — recorded below as a 006 follow-up. Acceptable, non-blocking (verify scripts are outside the `pnpm check` gate).
3. **`data-directory.tsx` `min-w-0` overflow fix:** two additive classes on the section/table region; role-neutral; fixes real wide-table overflow for every directory. Acceptable.
4. **Upstream enum "All" posts empty `filter.x=` → invalid-query:** confirmed pre-existing in `develop` (`data-directory.tsx` allLabel option `value=""`), affects `/links` identically, documented in the evidence spec comment. Out of scope. Acceptable.

## 5. Findings

- *(nit, severity 1)* `page.tsx` invalid-query branch renders `DataDirectory` without `pageSizes`/filters, so that edge state's toolbar shows the foundation defaults 25/50/100 (25 unregistered for orders; choosing it re-lands on invalid-query, Reset escapes) — same shape as the `/links` precedent. Optional polish: pass `pageSizes` in the invalid-query branch.
- *(nit, severity 1)* `order-v2-views.tsx` detail card: when the order has no description, `CardTitle` falls back to `order.id` and `CardDescription` repeats the same UUID.
- *(suggestion)* 006/orchestrator: after merging to `develop`, re-run `pnpm links:evidence` to re-bind the links manifest to the changed `run-admin-evidence.mjs` (mechanical, self-consistent; see deviation 2).

No blocking findings. UI sampling (directory 1440 pt-BR/en, 375px reflow, 320px ready, detail-with-comments, invalid-query, page-size-preference): no severity ≥2 finding; automated axe/overflow/target/focus gates in the evidence manifest are clean.

## Decision

**APPROVED** — the original request is met, all ten criteria pass on re-run/audit, every fence holds, and the remaining findings are nit/suggestion level.

verificado por revisor independente (yolo) — 2026-07-25
