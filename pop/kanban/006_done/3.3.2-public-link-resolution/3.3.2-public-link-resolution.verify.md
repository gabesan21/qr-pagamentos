# Verification and critique — 3.3.2-public-link-resolution

- **Stage:** 005_verifying
- **Reviewer:** independent reviewer (yolo)
- **Reviewed:** 2026-07-20

## Round 1 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Verification performed | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| AC1 | Exact identifier boundary and active/current resolution | re-run + review | `pnpm exec vitest run src/auth/public-payment-link.test.ts src/app/api/payment-links/[identifier]/route.test.ts src/app/routes.test.ts`; inspected `src/auth/public-payment-link.ts` | passed | The resolver rejects every non-24-character URL-safe spelling before any store call and preserves a supplied valid token verbatim. The Prisma predicate requires `active: true`, an active product, and either no expiry or `expiresAt > now`; equality at the injected read time is therefore unavailable. |
| AC2 | Exact minimal localized DTO and canonical price | re-run + review | Focused resolver suite; explicit Prisma-select and DTO review | passed | The public result has exactly `product.title`, `product.description`, `product.price`, `currencyPair.currencyUuid`, and `currencyPair.exchangeCurrencyUuid`. The projection has no `Number` conversion and selects no link/product IDs, metadata, status, pair label, admin fields, or provider fields beyond the approved UUID pair. Both locale projections and maximum/fractional canonical prices are covered. |
| AC3 | Sessionless dynamic unlocalized HTTP contract | re-run + review | Focused route and route-boundary suites; inspected `src/app/api/payment-links/[identifier]/route.ts` | passed | The route is `force-dynamic`, consumes only the route identifier plus `Accept-Language`, and delegates the latter to closed locale negotiation. It imports neither session, authorization, cookie, dictionary, checkout, nor provider behavior. A successful result is the exact JSON DTO with `200`/`Cache-Control: no-store`; each unavailable result is the same empty `404` with `no-store`. |
| AC4 | Read-only scope and pair-state policy | re-run + review | Complete task diff, `git diff --check 3c303e2..HEAD`, source/schema review | passed | The five changed paths are within F01/F02 ownership. The only database operation is the resolver's `findFirst`; no write, expiry inactivation, `SINGLE_USE` consumption, checkout/order, UI, public href, or provider request was added. The resolver deliberately has no `currencyPair.active` predicate, preserving the approved rule that deactivating a configured pair does not break existing links; the restrictive required relation prevents a dangling pair. |
| AC5 | Aggregate quality and contract coherence | re-run + review | `pnpm check`; card/plan/approved catalog-spec/DOX review | passed | Prisma generation, typecheck, lint, 615 passing tests (4 skipped), and the production build completed successfully. The non-failing nested-worktree lockfile warning did not affect the build. The root DOX contract remains sufficient for these small `src/auth` and `src/app/api` additions. |

### Implementation quality and findings

No blockers, suggestions, or nits.

- The injected store and clock make identifier, unavailable-state, and expiry-boundary tests deterministic without coupling the route to persistence.
- The explicit relation predicate and select establish the active-link/product boundary before projection, while the deliberately omitted pair-active predicate matches the durable catalog contract.
- The minimal route has one mapping from resolver result to either the exact DTO or the uniform empty unavailable response; it does not widen the public surface.

### Scope and runtime limitation

- [x] Changed paths conform to the declared F01/F02 ownership, and `git diff --check 3c303e2..HEAD` is clean.
- [x] The approved catalog-spec public-resolution contract is present in the kanban workspace and matches the delivered behavior.
- [x] Focused contract tests and the aggregate project gate were rerun successfully.
- [ ] Per explicit user authorization, no live-server/browser checkout exercise or disposable PostgreSQL runtime test was run. This review makes no runtime claim beyond the static, unit, and aggregate-build evidence above.

## Final verdict

- **Decision:** approved → ready for 006.
- **Blockers:** none.
- **Summary:** The delivery provides the approved sessionless public read: exact opaque-token handling, strict read-time expiry, an active product boundary, preserved existing-link pair behavior, a minimal redacted DTO, and uniform no-store unavailable responses. It adds no state mutation, administrative/provider data, or premature checkout surface.

Verified by independent reviewer (yolo) — 2026-07-20
