# Verification — 6.2.3 Build role-specific app shells

- **Mode:** full, fresh independent strong yolo 005 review after two returns
- **Task commit:** `2ec5db3cfc4c70b7f72cc50a1af26c6a1370137c`
- **Verdict:** **APPROVE**

## Original-request check

The delivery satisfies the original request. It replaces the authenticated
monoliths with separate bilingual administrator and merchant shells, fixed
five-area navigation, exact-role server authorization, persistent desktop and
accessible mobile navigation, segment-safe active states, the official
administrator identity, the official merchant fallback, and bounded route
workspaces. Existing reads, controls, POST endpoints, protected outcomes, and
public/sessionless routes remain within their prior capability boundaries.

The two prior blockers are resolved. The skip link is visually and
browser-proven above fixed chrome. Every Nautt onboarding, registration, and
reset result now returns to the owning `/settings` workspace; unavailable
balance retry reloads that workspace and performs a fresh re-authorized status
read. The dashboard neither accepts nor renders the Settings-owned Nautt
feedback channel.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| AC1 — exact role shells and routes | **PASS** | Source inspection and 130 focused changed-surface tests prove separate exact-role adapters, the fixed page map, approved redirects, nested order inheritance, and segment-safe active state. |
| AC2 — preserved behavior | **PASS** | All focused tests pass. The three Nautt POST handlers preserve origin-first handling and empty `401`/`403` outcomes while mapping every success/error/recovery result to `/settings?nautt=…`. New bilingual page tests exercise `configured`, `invalid`, `changed`, `recovery`, `reset`, `unavailable`, and balance retry after a fresh owner status read without detail leakage. An additional origin/Nautt run passes 25/25 tests. |
| AC3 — bilingual navigation and identity | **PASS** | Dictionary, shell, and brand tests pass. Both locales expose only the five role-permitted entries, correct official identity, `aria-current`, a non-color marker, one responsive navigation copy, and the skip target. |
| AC4 — owned source, tokens, brand, and RSC health | **PASS** | Admin source inventory, token projection, deterministic brand generation, lint, typecheck, and production build pass. Lint has only 11 unchanged order-projection warnings. The focused client boundary imports no authorization or business service. |
| AC5 — responsive six-theme evidence | **PASS** | Run `20260724053533` verifies 48 base captures plus two mobile-open captures for both roles, all six themes, and 320/375/768/1440 widths. All 50 records have zero serious/critical axe findings, overflow, undersized targets, skip obstruction, or focus-outline failure. Direct representative inspection found no clipping, overlap, identity loss, hierarchy defect, or visual severity 2+. |
| AC6 — contracts and aggregate | **PASS with classified baselines** | Root/shell DOX, DESIGN, both linked specs, 169-path ownership diff, `git diff --check`, standalone PoP validation, and an independent production build pass. `pnpm check` reaches 105 passing files and 822 passing tests, retaining only the unchanged `server-only` resolution suite failure and storefront locale expectation failure already reproduced before this task. |

## Prior blocker resolution

### B1 — skip link obscured by fixed chrome: resolved

- `src/design-system/tokens/themes.tokens.json` defines ordered semantic chrome
  and bypass layers, consumed by `src/app-shell/app-shell.css`.
- Every one of the 48 base browser records proves the first-tab link is visible,
  has a two-pixel outline, wins center-point hit testing, and transfers focus to
  `#app-shell-content` after activation.
- The evidence verifier passes against the run-bound manifest and hashes.

### B2 — relocated Nautt feedback and retry: resolved

- `src/app/nautt-credentials/route.ts`,
  `src/app/nautt-credentials/register/route.ts`, and
  `src/app/nautt-credentials/reset/route.ts` return only the documented opaque
  `/settings?nautt=…` results while leaving guard/auth/service ordering intact.
- `src/app/(merchant)/settings/page.tsx` re-authorizes the merchant, performs a
  fresh status read, and renders the localized result. Its tests cover every
  result in both locales and reject provider/credential detail leakage.
- `src/app/nautt-credential-surface.tsx` retries balance at `/settings`.
- `src/app/(merchant)/page.tsx` has no `nautt` parameter or feedback renderer;
  its regression test proves the dashboard does not claim that channel.

## Code and UI review

- **Blocking findings:** none.
- **Suggestions/nits:** none requiring follow-up.
- Nielsen visibility, match, consistency, recognition, error recovery, and
  user control: **PASS**. Mutation feedback remains beside the owning controls,
  retry returns to the same workspace, navigation state is explicit, and empty
  scaffolds fabricate no data.
- WCAG automated layer: **PASS**. Evidence reports no serious/critical axe
  violation, overflow, undersized visible target, raw-value drift, or focus
  obstruction.
- WCAG semantic layer: **PASS** for landmarks, keyboard order, skip activation,
  visible focus, disclosure semantics, active cues, labels, and one exposed
  navigation inventory.
- Visual loop: direct inspection of both roles at all required responsive
  bands, themes, and open-mobile states found no unresolved severity 2+ issue.
- Known limits: screenshots do not prove touch hardware, motion/jank, cultural
  or RTL behavior, or sub-12px microtext; anti-aliasing differences were not
  treated as defects.

## Verification evidence

- Changed-surface Vitest: **PASS**, 34 files / 130 tests
- Origin guard plus Nautt routes: **PASS**, 4 files / 25 tests
- Admin source, tokens, brand, lint, typecheck: **PASS**
- Production build: **PASS** outside the restricted sandbox; the sandbox-only
  attempt failed because Turbopack could not bind its internal local port
- App-shell evidence verifier: **PASS**, run `20260724053533`
- 50-record assertion audit and representative visual montage: **PASS**
- Ownership scope, diff check, standalone PoP validation: **PASS**
- Aggregate: **BASELINE**, 105 files / 822 tests pass; two unchanged failures

## Decision

**APPROVED.** No blocking finding remains. Because this decisive third review
approves the delivery, the 005 circuit breaker is not activated.

verificado por revisor independente (yolo) — 2026-07-24
