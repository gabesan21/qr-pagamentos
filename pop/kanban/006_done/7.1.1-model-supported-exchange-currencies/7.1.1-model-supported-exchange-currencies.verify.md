# Verification — [[7.1.1-model-supported-exchange-currencies]]

- **Stage:** 005_verifying · **Responsible:** strong independent yolo critic (fresh session)
- **Mode:** FULL (critical: true; first 005 pass, 0 prior returns)
- **Delivery:** worktree `pop/worktrees/7.1.1-model-supported-exchange-currencies`, branch `task/7.1.1-model-supported-exchange-currencies`, commits `81dfc6a` (F01), `e61648c` (F02), `45d8417` (F03); merge-base with `develop` = `9d3c7e4`.

## 1. Original request first (binding human directive)

The redefined request — Nautt currency UUIDs as administrator-registered runtime data, never inferred at build time; no closed four-code catalog; database-state gating with typed backend errors and a UI-prohibition signal; dynamic growth — is **met**:

- No closed code list anywhere: identity is any uppercase `[A-Z]{3}` validated at the service boundary (`src/auth/supported-exchange-currency.ts:46`), with a database `AAA`–`ZZZ` bounds check as defense-in-depth. UUIDs enter only through the re-authorized admin mutation; no env/header/browser sourcing, no build-time seeding.
- Gating on database state: `requireActivePair`/`requireDefaultPair` throw the typed `NoActiveExchangeCurrencyMappingError`; `resolveDefaultChoice` returns the discriminated `{status:"available"|"unavailable"}` read — the UI-prohibition signal 7.1.2/7.2.2 consume (spec records this boundary).
- Dynamic growth: register/replace/deactivate over the pointer table; history retained in immutable `catalog_currency_pair` rows with restrictive FKs.
- Deterministic default: active `BRL` or explicit unavailable, never another code/order/inactive row (tested).
- Role boundary: only active `ADMIN` mutates; owner reads are active-only redacted `{code,label}`; no public/sessionless discovery surface (grep confirms only 3 non-test source files touch the feature).

## 2. Evidence (all re-run in the worktree)

- **AC1** — `CI=true pnpm db:generate` ✔; `pnpm db:contract-check` → `PASS migration-policy baseline=19 future=4`, `PASS migration-policy-language`, `PASS migration-policy-fixtures`, `PASS documentation-contract`. Baseline 19 immutable migrations byte-untouched (diff adds only `20260724120000_supported_exchange_currencies/`). Generated `migration.sql` is canonical `createTable` + `addConstraint` output with PK, `pair_id` UNIQUE, FK `ON DELETE RESTRICT`, bounds CHECK, `GRANT SELECT, INSERT, UPDATE, DELETE` on the pointer table, and `REVOKE DELETE` on `catalog_currency_pair`; contract-check pins no excess grants.
- **AC2** — `CI=true pnpm db:test` (disposable PostgreSQL) fully green, including `PASS supported-exchange-currency-schema` (duplicate code → 23505 on `supported_exchange_currency_pkey`; duplicate `pair_id` → 23505 on `_pair_id_key`; missing pair → 23503 on `_pair_fkey`; `'AA'`/`'A1A'` → 23514 on `_code_bounds`; admin delete of pointed pair → 23001 RESTRICT; runtime `DELETE` on `catalog_currency_pair` denied), plus `PASS role-separation`, `PASS runtime-denials`, `PASS migration-policy-postgresql-preservation`. Existing link/order fixtures and `catalog_currency_pair_uuids_key` untouched.
- **AC3** — `CI=true pnpm vitest run` on the three new test files: **24/24 pass** (10 service + 9 store + 5 route). Coverage matches the pass bar: non-admin denied before persistence on every mutation; ADMIN/disabled-owner denied on owner reads; exact code/UUID/label validation; redacted `{code,label}` reads (key-set asserted); typed duplicate-pair conflict with no second row; typed code-active conflict; re-point restore after deactivation; serialized single-active under concurrent register/replace; BRL-or-explicit-none with no fallthrough; typed `NoActiveExchangeCurrencyMappingError`.
- **AC4** — No new public/sessionless route or DTO field; route is origin-guard-first with empty 401/403 and one opaque success/failure redirect pair (route tests). No UI pages/dictionaries added (Epoch 10 owns the admin UI). Full suite: 900 passed, **2 failed — both confirmed pre-existing on `develop`** (reproduced in the main checkout): `src/auth/sign-in.test.ts` `server-only` resolution and `src/app/store/[slug]` locale assertion, owned by parallel task M-4.1 (carved out by the verification input). `CI=true pnpm build` green separately.
- **AC5** — Specs (`catalog-and-payment-links`, `storefront-and-customization`, `nautt-finance-integration`), roadmap Phase 7.1/7.1.1 wording, root `AGENTS.md`, `prisma/AGENTS.md` all synchronized; `RESEARCHES.md` currency-mappings entry removed, webhook HMAC entry intact. `python3 pop/scripts/pop_validate.py --standalone` from the main checkout: **válido, nenhuma violação** (2 benign worktree warnings).

## 3. Executor-flagged deviations — judged

1. **bootstrap.sql edited against the plan's must-not-edit — ACCEPTED (justified deviation).** The plan's own AC1/AC2 demand least-grant (no runtime `DELETE` on `catalog_currency_pair`), and `prisma/AGENTS.md` establishes relation-guarded bootstrap ACL normalization as the durable doctrine (identical `product_category` precedent). The new block mirrors that precedent exactly, is relation-guarded, contains no credentials, and is pinned by `prisma/AGENTS.md`, `db-contract-check.mjs`, and `db-test.mjs`. The plan's deny-list entry conflicted with the project's own ACL doctrine; the executor resolved it toward the durable contract and documented it.
2. **Two db-test.mjs fixes — ACCEPTED (genuinely pre-existing, corrections right).** Both defects exist verbatim at merge-base `9d3c7e4`: the invalid-price INSERT listed 7 target columns with 6 values (would raise 42601, not the asserted 23514) — fixed with `'${otherUserId}'`, which is in scope (defined at db-test.mjs:283, used at :519); and 23503 was asserted where every relevant FK is `onDelete: Restrict` (schema.prisma:201) — PostgreSQL raises `restrict_violation` 23001 for RESTRICT, so 23001 is correct. Now proven by the green lane.
3. **Service at `src/auth/supported-exchange-currency.ts` without a new subtree AGENTS.md — ACCEPTED.** Plan F02 explicitly allowed `src/auth/**`; F03's obligation was to record the chosen location in root `AGENTS.md`, which carries a complete ownership bullet (registry, pointer-table authority, gating, role boundary, sole mutation route, negative constraints). A child contract was required only for a dedicated new subtree.
4. **Stale fixed-code wording at `pop/researches/panel-rebuild-roadmap/panel-rebuild-roadmap.md:44` — ACCEPTED as historical recon artifact (nit).** Research deposits are point-in-time records; the live contracts (roadmap, RESEARCHES.md index, specs) were all synced. Not in-scope drift.

## 4. Findings

- **Nit (non-blocking):** in `register`, any `P2002` — including a pointer-PK race on `code` — maps to `pair-exists`/`ExchangeCurrencyDuplicatePairError` rather than the code conflict; a race-edge error-type imprecision only, with no leaked constraint error and no second row (the contract's actual requirement).
- **Nit (non-blocking):** stale 7.1.1 wording remains in the historical research deposit (deviation 4 above).

No blocking findings. Diff audit found no ownership invasion: all changes are inside F01–F03 owns plus the two justified deviations above; `src/observability/server-request-log.ts` gained only a literal route template, consistent with its closed-inventory contract.

## Decision: APPROVE

— verificado por revisor independente (yolo) — 2026-07-24
