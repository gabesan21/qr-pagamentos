# M-6.1 verify — Fix admin authorization advisory lock raw void deserialization

- **Task:** [[M-6.1-fix-admin-authorization-lock-raw-void]] · yolo, non-critical · mode: **full** (plan-declared; touches the delivered administrator lifecycle)
- **Diff:** `develop..HEAD` on `task/M-6.1-fix-admin-authorization-lock-raw-void`, 3 commits (`b9aaa6b` fix+unit test, `3af002c` db test+harness, `fff3657` card/telemetry)
- **Date:** 2026-07-26

## 1. Original request first (O quê / Por quê)

**Met.** The card asked to replace the broken `$queryRaw` with `$executeRaw` for the `pg_advisory_xact_lock` call in `withAuthorizationLock` (`src/auth/administration.ts`), with a regression test mirroring the 1.4.1 session-lock fix, so `deleteUser`/`changeStatus`/`changeRole` stop failing at runtime with Prisma P2010.

Evidence:

- `src/auth/administration.ts:39-41` — new `acquireAuthorizationLock` issues `` $executeRaw`SELECT pg_advisory_xact_lock(hashtext('qr:authorization:active-admin'))` ``; `grep queryRaw src/auth/administration.ts` → no match.
- `src/auth/administration.ts:164-169` — `withAuthorizationLock` still wraps `work(...)` in the same `db.$transaction`, lock acquired first; nothing else in the service changed (diff is exactly the helper + call-site swap), so opaque outcomes (`change-failed`, `deleted` redirects) and route surfaces stay byte-identical.
- Runtime proof against real PostgreSQL: `pnpm db:test` prints `PASS administration-database-locked-mutations` — `deleteUser`, `changeStatus`, `changeRole` now succeed through the lock (they rejected with P2010 before, per 10.2.1's blocker report).

## 2. Diff audit (full)

- **Fix shape vs precedent:** `AuthorizationLockTransaction` interface + exported helper is byte-consistent with the 1.4.1 idiom in `src/auth/session.ts:29-34` (`UserSessionLockTransaction` / `acquireUserSessionLock`). Exported-helper shape is justified by testability and matches the project's own pattern — no new idiom invented.
- **Lock semantics:** key literal `qr:authorization:active-admin` preserved; transactional scope (`pg_advisory_xact_lock` inside `db.$transaction`) preserved; lock-before-work ordering preserved.
- **Invariants (root AGENTS.md, administration subtree):** advisory-locked single-transaction semantics intact; one `user_deletion` audit row asserted by the db test; `src/auth/session.ts` (sign-in/principal resolution) untouched — no other `$queryRaw` advisory-lock call sites exist (`grep hashtext src/`).
- **Unit regression** (`src/auth/administration.test.ts:92-108`): mirrors `session.test.ts:52`; asserts exact lexeme `SELECT pg_advisory_xact_lock(hashtext('qr:authorization:active-admin'))` with zero bound values and `$queryRaw` never called (a throwing `$queryRaw` double-guards). Fails against the old code — a real regression test.
- **DB regression** (`src/auth/administration.database.test.ts`, new): env-gated `describe.skipIf` matching the harness pattern; exercises the real `getAdministrationService()` happy paths — deleteUser persists `status='DISABLED'` + `deleted_at` + exactly one `user_deletion` row with the correct `actor_id`; changeStatus/changeRole persist. Fixture `INSERT` validated against `prisma/schema.prisma` (all other `user` columns nullable/defaulted); cleanup deletes `user_deletion` before users, respecting `onDelete: Restrict`.
- **Harness** (`pop/scripts/db-test.mjs`): exactly one additive block, identical in shape to the neighboring registrations (env prefix, `MIGRATION_DATABASE_URL` removal, `assert("2 passed")`, PASS line); inserted before the final runtime-schema assertions, fixtures self-cleaning.
- **Scope discipline:** the 3 commits touch only `src/auth/administration.ts`, `src/auth/administration.test.ts`, `src/auth/administration.database.test.ts` (new), `pop/scripts/db-test.mjs` (one block), and card files. The extra kanban paths in the raw `develop..HEAD` stat are base drift (develop advanced after the branch point), not task commits — per-commit `--stat` confirms.
- **DOX/specs:** repair to already-specified behavior; no spec or AGENTS.md change required — plan's closeout assessment is correct.

## 3. Gates re-run by this critic (worktree)

| Gate | Command | Result |
|------|---------|--------|
| Aggregate | `pnpm check` (exit code captured via `CHECK_EXIT=$?`) | **exit 0** — lint, typecheck, 166 test files / 1358 tests passed (incl. `administration.test.ts` 12 tests; db test correctly skipped outside harness), build green |
| Disposable PostgreSQL | `pnpm db:test` | **exit 0** — 41 PASS lines, 0 FAIL, including `PASS administration-database-locked-mutations` |

## 4. Findings

None blocking. One nit (non-blocking): the db test does not assert `user_deletion.created_at` equals the persisted `deleted_at` marker, which the lifecycle contract implies (same instant written in one transaction); current assertions already cover the P2010 regression and the audit row's existence/actor, so this is coverage polish only.

## 5. Decision

**APPROVE.** Original request met, plan acceptance criteria all satisfied (`$executeRaw` in place, no `$queryRaw` in the file, regression tests prove lexeme + non-call + happy locked paths, both gates exit 0, scope clean). Plan deviation note: 004 added the service-level PostgreSQL test beyond the plan's minimum unit test — this is exactly the plan's own risk fallback ("se não exercitar, adiciona cobertura mínima no nível que já existir") and strengthens the gate; not a finding.

verificado por revisor independente (yolo) — 2026-07-26
