# Verification — [[9.2.2-build-standalone-payment-experience]]

- **Stage:** 005_verifying · **Mode:** full (mandatory — `critical: true`) · **Date:** 2026-07-26
- **Surface audited:** full integrated diff `develop...HEAD` in `pop/worktrees/9.2.2-build-standalone-payment-experience` (96 files, +3286/−16), all new `src/app/store/[slug]/pay/` sources and tests, the DTO amendment, evidence harness/scripts/artifacts, and every closeout document.

## 1. Original request first

The card asks for the bilingual amount-entry, customer-policy, QR, copy, polling, retry, terminal, and return experience for direct store payments. **Met.** From the storefront custom-amount item a buyer reaches `/store/[slug]/pay` (one pay action, draft amount as prefill-only query), enters an amount validated against the canonical grammar and the policy-exact customer fields (`NONE`→`NAME_EMAIL_CPF_ADDRESS` all render exactly), submits to 9.2.1's route with body exactly `{ idempotencyKey, amount, customer }`, receives QR/copy-paste, polls status through V1's unmodified polling controller with retry, reaches every terminal/failure view, and always has the localized return-to-store link — in `pt-BR`/`en`, themed like the storefront. The delivery matches the plan's pinned decisions with no divergence.

## 2. Gates re-run by this reviewer (worktree)

- `pnpm test` — **168 files / 1400 tests passed**, 0 failures (6 db-contract files skipped, as designed outside docker).
- `pnpm lint` — exit 0, 0 errors (44 warnings, all pre-existing on `develop`; the new page's single `no-img-element` warning matches the established `/media/[identifier]` logo pattern on the storefront page).
- `pnpm typecheck` — exit 0.
- `pnpm standalone-payment:evidence:verify` — exit 0 (`Verified standalone payment evidence 20260726225831`): manifest/assertions/capture hashes valid, 67 captures, axe/target/overflow/focus assertions green, review bound to the manifest hash, no severity 2+ finding.
- `node scripts/check-design-tokens.mjs` — exit 0; whitelist extended by exactly the one new route.
- Not re-run (docker-heavy, per gate economics): full `pnpm check` build leg, `pnpm db:test`, and the evidence run itself — the executor reported all green and the `:verify` side plus artifact spot-checks corroborate run `20260726225831` (manifest `gitHead` `762682d`, real seeded capability-HMAC polling path, submit interception documented and consistent with decision 7).

## 3. Contract invariants

- **Forbidden paths clean:** zero diffs under `src/checkout/`, `src/security/`, `src/observability/`, `src/app/pay/`, `src/app/api/`; no new API route; rate-limit and request-log inventories untouched.
- **Decision-2 boundary discipline:** `src/storefront/public-storefront.ts` adds exactly the additive `checkoutDataPolicy` member (V1 `checkoutPolicy` precedent verified at `src/checkout/public-checkout-presentation.ts`); the projection test's closed key set and forbidden-member serialization assertions are extended, and `page.test.tsx` proves no owner/policy/provider strings leak into markup.
- **Amount grammar identity:** the client reuses `isStorefrontCartAmount` from `src/storefront/cart.ts`, byte-identical to the canonical `AMOUNT_PATTERN` of `src/orders/order-v2.ts:103` (no new variant invented).
- **State-machine honesty:** the client state set is exactly the pinned union; `RESERVED`/`CREATING`/`CREATED` render the waiting treatment with a secondary badge, the terminal set is the unchanged V1 vocabulary with destructive failure badges; every 404 (page, submit, poll, expired capability) collapses to the one opaque unscoped unavailable view; unit tests cover each state.
- **Polling reuse:** `createPollingController` imported unchanged from the frozen V1 form (5s, visibility pause, `1s·2^n` backoff, 3 failures → manual retry) — V1 file hash untouched.
- **ui-review lens:** spot-checked captures (QR, terminal-confirmed, waiting, policy variants) — honest localized states, return link present on waiting/failure/terminal views, non-color status markers (labelled badges), no slop, no raw visual values.
- **Closeout:** specs `storefront-and-customization.md` and `checkout-and-order-lifecycle.md` gained accurate 9.2.2 slices; root `AGENTS.md` `/store/[slug]` line extension matches delivered behavior; the pre-existing conflict marker in `checkout-and-order-lifecycle.md` was resolved as a sane union of both Updated headers (10.1.1/10.3.1 + 9.1.3 entries all preserved); no `<<<<<<<`/`>>>>>>>` markers remain anywhere in `pop/specs/` or `src/`.

## 4. Findings

1. **bloqueante — `DESIGN.md:469` (worktree): corrupted catalog-evidence paragraph.** The task's closeout edit deleted the lead sentence `` `pnpm catalog:evidence` and `pnpm catalog:evidence:verify` bind 49 merchant `` instead of inserting the standalone paragraph before it. The text now runs the new standalone-payment paragraph directly into the dangling fragment "catalog captures: the products directory across six themes…" with no subject or paragraph break — a pre-existing DESIGN.md fact mutilated by this delivery. Fix: restore the deleted sentence as the start of the catalog paragraph (one-line repair); acceptance criterion 7 requires DESIGN.md to remain accurate and intact.
2. **nit — `standalone-payment-experience.tsx` copy feedback persists into terminal views.** The `copyState` notice ("Código PIX copiado.") is not cleared when polling reaches a terminal state, so it still renders under the CONFIRMED badge (visible in `interaction-pt-BR-payment-terminal-confirmed.png`). Cosmetic; harmless.
3. **nit — `scripts/run-admin-evidence.mjs` edited by F03** despite the plan's "must not edit other evidence pairs". The edit is the shared compose harness's established mode-flag mechanism (same pattern as `--catalog`/`--links`/`--merchant-dashboard`/`--orders`), purely additive, and bound into the evidence source-hash inventory — justified reuse, noted for the record, not a violation in substance.

## 5. Decision

**RETURN to 004** (return 1/2) — solely for finding 1. The implementation, tests, evidence, contracts, and spec closeout are otherwise verified green end to end; the fix is the one-sentence DESIGN.md restoration above.

## Round 2 — 2026-07-26 (re-verification after return 1/2)

Full mode remains mandatory after a return; since round 1 re-ran every gate green and the fix is docs-only, this round scoped to the fix itself plus integrity checks.

- **Fix verified.** Commit `d974880` is the only new commit since round 1 and touches only `DESIGN.md` (+2 lines). The restored catalog-evidence paragraph is **byte-identical to develop's original** (programmatic paragraph comparison `HEAD:DESIGN.md` vs `develop:DESIGN.md`: equal), and the standalone-payment paragraph added by this task is intact directly above it.
- **Worktree clean** (`git status --porcelain` empty); no other regressions introduced.
- **No conflict markers** in `DESIGN.md`; its claims (67-capture evidence contract, token-whitelist extension, page states) still match the delivered behavior verified in round 1. No code changed since the green gates, so no gate re-run was warranted.

### Decision (round 2)

**APPROVE.** The single blocking finding is resolved exactly as prescribed; the two round-1 nits stand as recorded and do not gate. The delivery satisfies the original card objective, every plan acceptance criterion, and all contract invariants.

verificado por revisor independente (yolo) — 2026-07-26
