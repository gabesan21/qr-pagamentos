# Group g01 — Baseline — [[1.2.1-review-clean-code-baseline]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Establish a reproducible, behavior-characterized baseline before remediation.

## Movements

- [x] (agent) In the task worktree, read root `AGENTS.md`; confirm branch `task/1.2.1-review-clean-code-baseline`, all three `pop/memory/1.1.*.md` records, and `git status --short`. Verify `git rev-parse develop` equals `git rev-parse origin/develop`; set `TASK_BASE=$(git rev-parse develop)`, prove `git merge-base --is-ancestor "$TASK_BASE" HEAD`, and write the 40-hex value after `TASK_BASE=` in Notes.
  - Expected: synchronized refs, complete dependencies, an ancestor base recorded below, and only task-owned harness changes before source edits.
  - If it fails: stale/wrong base, missing memory, or foreign changes → incomplete branch range/integrity risk → stop under the matching abort; the orchestrator must synchronize/recreate the branch rather than the executor rebasing ad hoc.
- [x] (agent) Prove exact pins. If direct `node --version` / `pnpm --version` do not print `v24.18.0` / `11.13.0`, run: `TOOLCHAIN_DIR=/tmp/qr-pagamentos-1.2.1-toolchain; rm -rf -- "$TOOLCHAIN_DIR"; docker build --pull=false --target toolchain --tag qr-pagamentos-toolchain:1.2.1 .; mkdir -p "$TOOLCHAIN_DIR/bin" "$TOOLCHAIN_DIR/lib/node_modules"; CID=$(docker create qr-pagamentos-toolchain:1.2.1); docker cp "$CID:/usr/local/bin/node" "$TOOLCHAIN_DIR/bin/node"; docker cp "$CID:/usr/local/lib/node_modules/pnpm" "$TOOLCHAIN_DIR/lib/node_modules/pnpm"; docker rm "$CID"; ln -s ../lib/node_modules/pnpm/bin/pnpm.mjs "$TOOLCHAIN_DIR/bin/pnpm"; export PATH="$TOOLCHAIN_DIR/bin:$PATH"`; then rerun both version checks. With exact pins active, always run `pnpm install --frozen-lockfile` and then `git diff --exit-code -- package.json pnpm-lock.yaml`.
  - Expected: recovery if needed exits 0; exact pins print from host PATH; frozen install exits 0; the manifest/lockfile diff command produces no output and exits 0.
  - If it fails: cache/extraction/pin or lock drift → unreproducible baseline → remove any created container, set `blocked: true` with captured output, and stop; do not pull, download, or guess another activation route.
- [x] (agent) Run and capture `pnpm check`, `pnpm db:contract-check`, `pnpm container:contract-check`, and `install/test.sh` as the cheap pre-change baseline.
  - Expected: every command exits 0 with its documented PASS/test/build output.
  - If it fails: environment-only or existing defect → preserve exact output, attribute it to an owning surface, and use the baseline-failure fork; unattributable failure aborts.
- [x] (agent) Confirm the closed inventories already declared in the separate g02 and g03 ledger tables; do not write either ledger from this group.
  - Expected: g02 owns five application/database rows and g03 owns four deployment/installer rows, with no duplicate surface and no shared writer.
  - If it fails: ownership/scope overlaps or is incomplete → parallel evidence-loss risk → correct the plan in 002; do not begin parallel review.

## Execution notes

- 2026-07-14 — Planning preflight found Node `v26.4.0` and a failing system pnpm; this group must not mark the toolchain movement complete until the exact project pins work.
- 2026-07-14 — The cached Dockerfile toolchain build and host extraction were executed during round-1 replanning and printed `v24.18.0` and `11.13.0`.
- 2026-07-14 — Branch/base: `task/1.2.1-review-clean-code-baseline`; local `develop`, `origin/develop`, and initial `HEAD` all resolved to the recorded base; all three dependency memories exist; initial status contained only the task-owned roadmap/card harness.
- 2026-07-14 — Toolchain: direct host checks printed Node `v26.4.0` and an unusable pnpm; cached Docker extraction recovered Node `v24.18.0` and pnpm `11.13.0`; frozen install passed with no manifest/lockfile drift.
- 2026-07-14 — Pre-change gates: `pnpm check` passed (4 tests, 1 skipped, production build); `pnpm db:contract-check` printed `PASS documentation-contract`; `pnpm container:contract-check` printed both digest checks and `PASS image-contract`; `install/test.sh` printed `PASS install-contract`. The first sandboxed container-contract attempt failed on restricted Docker/network access and passed unchanged with Docker access, so it was attributed to environment only.
- 2026-07-14 — Inventory ownership is disjoint: g02 owns five application/database rows and g03 owns four deployment/installer rows.
- TASK_BASE=d791b49cedeb8f33d94fff61131fd0274b907379
