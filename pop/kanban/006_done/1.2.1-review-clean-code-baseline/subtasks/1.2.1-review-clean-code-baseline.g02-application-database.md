# Group g02 — Application and database — [[1.2.1-review-clean-code-baseline]]

- **Order:** 2 (may run in parallel with g03).
- **Depends on:** g01.
- **Description:** Review application/database code and remediate only evidence-backed local issues.

## Movements

- [x] (agent) Review `src/app/`, `src/i18n/`, and `src/db/` with their tests for clear inputs/outputs, locale and health invariants, error paths, names, cohesion, and needless coupling; update only this group's ledger below.
  - Expected: every subtree has evidence and a disposition; behavior is traceable to the mounted spec and existing tests.
  - If it fails: behavior cannot be explained from contract/tests → missing characterization → add the smallest test before any structural edit, or abort if a product decision is required.
- [x] (agent) Walk `AGENTS.md` → `prisma/AGENTS.md`, then review `prisma/schema.prisma`, `prisma/bootstrap.sql`, immutable migration SQL, and `pop/scripts/db-{contract-check,test}.mjs` for boundary clarity, duplication, secret safety, role separation, and maintainability.
  - Expected: database surfaces have evidence/dispositions and no proposal mutates the applied foundation migration.
  - If it fails: a fix appears to require migration-history edits or weaker privileges → contract violation → abort; a real forward schema fix uses the plan's new-migration fork.
- [x] (agent) For each `local remediation` finding, state the hypothesis and preserved risk in Notes, add/identify its protecting test, and apply one cohesive transformation at a time.
  - Expected: each diff maps to one ledger finding and tests demonstrate unchanged externally observable behavior.
  - If it fails: the diff grows across unrelated concerns or lacks protection → scope/refactoring risk → revert the task-owned partial transformation and classify larger work as tracked debt.
- [x] (agent) After each transformation, run the narrow owning test/type/lint/database contract and review that transformation's diff before starting another.
  - Expected: each narrow check exits 0 and the local diff contains only the stated intent.
  - If it fails: regression or wrong ownership → use the failing assertion/type/contract to correct the same transformation; an unplanned behavior decision aborts.
- [x] (agent) Mark every application/database ledger row `no issue`, `local remediation`, or `tracked debt`, with tracked debt including context, impact, and next step.
  - Expected: no unresolved candidate remains and no stable code was rewritten without demonstrated value.
  - If it fails: candidate cannot be classified from available evidence → unresolved uncertainty → abort rather than guess.

## Execution notes

- 2026-07-14 — Preserve `/pt-BR`, `/en`, unlocalized `/api/health`, runtime-only `DATABASE_URL`, migrator-only `MIGRATION_DATABASE_URL`, and all least-privilege denials.
- 2026-07-14 — No local remediation was confirmed on this front, so the transformation-specific movements completed vacuously and no stable application/database code was churned.

### Application/database ledger (owned by g02)

| Surface | Evidence | Disposition |
|---------|----------|-------------|
| `src/app/` | Locale pages derive from the closed set and reject unsupported values before dictionary lookup; `/api/health` has an exact status/body/cache characterization test. | no issue |
| `src/i18n/` | The exact `pt-BR`/`en` union drives static typing; dictionaries require locale/key parity and the runtime contract test independently checks both. | no issue |
| `src/db/` | The lazy client fails clearly without `DATABASE_URL`; the static database contract proves it never consumes `MIGRATION_DATABASE_URL`. | no issue |
| `prisma/` | Fixture-only schema, immutable named constraints, schema ownership, explicit DML/sequence grants, and migration-metadata denials match the child DOX contract; no migration edit is justified. | no issue |
| `pop/scripts/db-*` | The uniquely named loopback fixture proves replay/idempotence, CRUD, SQLSTATE constraints, ownership and least-privilege denials, then removes its container; the static contract passed. | no issue |
