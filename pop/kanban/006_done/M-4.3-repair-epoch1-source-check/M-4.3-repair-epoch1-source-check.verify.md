# Verification — M-4.3-repair-epoch1-source-check

- **Date:** 2026-07-25
- **Strategy:** `differential` — single-file tooling diff (size S, non-critical, no prior returns); re-ran the changed surface (epoch1:source-check) plus the aggregate gate, and ran fresh mutation probes.
- **Surface:** `scripts/check-epoch1-source.mjs` (only file in commit `2845012`).

## Original request first

The card asks to repair `pnpm epoch1:source-check`, which failed on develop with ENOENT on `src/app/page.tsx` after the Epoch 6 move to `src/app/(merchant)/page.tsx`, pinning the current intended layout without deleting any assertion whose invariant still matters. Met: the script now reads the merchant home at its real path, exits 0 on develop-derived source, and the re-authorization invariant the old home-page assertion carried was relocated, not dropped.

## Evidence

1. **Diff scope:** `git show 2845012 --stat` — only `scripts/check-epoch1-source.mjs` (+10/-3). The M-4.2 guards (ESLint `pop/worktrees/**` ignore, tsconfig exclude, lint-control visibility) are untouched, byte-identical in the diff hunks.
2. **Pinned chain is the real authorization path (read directly in the worktree):**
   - `src/app/(merchant)/page.tsx:17` calls `requireMerchantShellContext(`.
   - `src/app/(merchant)/shell-context.ts:11` calls `requireOwnerFromCookie(` and redirects Unauthenticated/Forbidden.
   - `src/app/owner-guard.ts:6` calls `getAuthorizationService().requireUser(` and contains no `getSessionService().validate(`.
   - `src/app/page.tsx` does not exist; the old expectation was the stale side — correct call was to repair the script.
3. **No weakening:** the old assertion required `getAuthorizationService().resolve(` on the home page plus a `validate(` ban. The new assertion pins a transitive chain terminating in `requireUser(` (strictly stronger than `resolve` — it throws on inactive/forbidden principals rather than returning a nullable resolution) and keeps the `validate(` bypass ban on the guard where the session decision now lives. Breaking any link fails the check.
4. **Mutation probes (run by this critic in a scratch copy `/tmp/m43-probe`, symlinked `node_modules`):**
   - Removed `requireOwnerFromCookie(` from `shell-context.ts` → exit 1, "merchant shell context no longer routes through the owner guard." ✓ detected
   - Injected `getSessionService().validate(` into `owner-guard.ts` → exit 1, "owner guard bypasses status-aware authorization." ✓ detected
   - Restored copy → `EPOCH1_SOURCE_OK`, exit 0. Scratch dir deleted.
5. **Gates (worktree):** `pnpm epoch1:source-check` → `EPOCH1_SOURCE_OK`; `CI=true pnpm check` → exit 0 (lint 0 errors/17 pre-existing warnings, typecheck clean, 994 passed / 7 skipped, `next build` succeeded).
6. **Main checkout:** `git status --porcelain` clean on `develop`; the executor's admitted accidental edit left no residue.

## Findings

- None blocking. Nit: the `validate(` ban now covers only `owner-guard.ts`, not `shell-context.ts`, but the chain pins make a bypass additive rather than substitutive — a `validate(` call elsewhere cannot replace the required `requireUser(` link, so the invariant holds.

## Decision

**Approved.** verificado por revisor independente (yolo) — 2026-07-25
