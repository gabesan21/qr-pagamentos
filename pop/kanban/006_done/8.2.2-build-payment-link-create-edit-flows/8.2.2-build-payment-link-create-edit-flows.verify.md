# Verification — [[8.2.2-build-payment-link-create-edit-flows]]

- **Stage:** 005_verifying · **Responsible:** independent strong critic (yolo) · **Mode:** full (critical: true)
- **Surface re-run:** aggregate gate, evidence verify, frozen-path diffs, route/component/page tests audit, screenshot vision sampling.

## 1. Original request first

Card "What": owner-safe commands and bilingual flows for creating links from products/quantities or fixed value/description, editing allowed fields, activation/deactivation, expiry, preview, and sharing. **Met.** `/links/new` creates both composition kinds through validated bilingual forms posting exactly the delivered 8.1.2 field grammar; `/links/v2/[id]/edit` edits expiry anytime and financial members under expected-version CAS with dirty-field omission; the detail page activates/deactivates behind confirmation; expiry is set/cleared through the same CAS; preview/sharing is the 8.2.1 detail + share URL, preserved and re-captured; every outcome lands on `/links?payment-links-v2=<outcome>` with closed bilingual notices. "Why" (preserve order snapshots) holds: the immutable-version fork UX is honest — the edit form explains the attempt lock bilingually and `/links/new?from=<id>` prefills a new link with a fresh server-generated identifier; no order snapshot is touched (zero diff on `src/orders/**`).

## 2. Frozen proof (re-verified by the critic)

- `git diff develop...HEAD -- src/auth/payment-link-v2.ts src/auth/payment-link-v2-view.ts src/data-directory prisma` → **empty**.
- Zero diff on `src/auth/payment-link.ts`, `src/app/payment-links/**`, `src/app/admin/**`, `src/app/api/**`, origin-guard, rate-limit, observability. V1 surfaces byte-frozen; legacy section on `/links/page.tsx` untouched (only additive notice + create affordance above it).

## 3. Contract fidelity

- Redirects repointed `/` → `/links?payment-links-v2=…` in both routes, targets only; empty `401`/`403` via `ownerProtectedMutationResponse` preserved (route tests).
- `directory-query.ts` strips/validates the closed notice set before `canonicalizeDirectoryRequest`; forged, repeated, or empty values → zero-I/O `invalid-query`; canonical URLs never carry the notice (tests).
- Additive seam `src/auth/payment-link-v2-prefill.ts`: server-only, `requireUserPrincipal` before any read, canonical-UUID validation, one opaque `null`, exposes exactly `{version, lineProductIds}` — no provider/verifier fields (module tests).
- CAS `version` hidden field posted on edit/activate/deactivate from prefill; cross-owner/malformed/missing share the one opaque unavailable UI on detail, edit, and `?from=` (page tests).
- Pickers ride the V1 `listForOwner` read-only; the frozen V1 module is untouched.
- Dictionaries: 35 new keys symmetric in `en` and `pt-BR` (dictionary contract tests green inside `pnpm test`).

## 4. Acceptance criteria

| # | Result | Evidence |
|---|--------|----------|
| 1 | PASS | `pnpm check` re-run in worktree: exit 0 (lint + typecheck + 1149 tests + build) |
| 2 | PASS | `new/page.test.tsx` field-grammar assertions; evidence `create-product-lines`/`create-fixed-amount` outcomes `created` |
| 3 | PASS | edit page test asserts all financial members + expiry unnamed until dirty; evidence `edit-expiry-only-under-attempt` (edited under a seeded attempt) and `edit-blank-clear` (`cleared: true`) |
| 4 | PASS | version CAS hidden fields asserted; opaque unavailable UI asserted in en + pt-BR on all three pages |
| 5 | PASS | route tests assert all five outcomes on `/links`; directory-query tests assert extraction, forged/repeated/empty rejection, canonical drop; V1 redirects untouched |
| 6 | PASS | prefill module tests green; `git diff` on the two delivered files empty (critic re-run) |
| 7 | PASS | lock Alert + `/links/new?from=<id>` asserted; `?from=` prefill asserted with no identifier input anywhere |
| 8 | PASS | no hardcoded copy found in the new components; empty-picker, invalid-query, unavailable states captured/tested; DESIGN.md state matrix updated |
| 9 | PASS | `pnpm links:evidence:verify` re-run: "Verified merchant payment-link directory evidence 20260725211551" (53 captures, manifest-hash-bound, zero axe/overflow/target/focus findings) |
| 10 | PASS | zero diff over every V1 path; V1 route tests green in the aggregate gate |

## 5. UI review (ui-review)

- **Nielsen:** PASS — feedback (status/destructive notices, observation-only `CatalogSubmit` pending), escape hatches (back links on every page, `details` confirmation before lifecycle mutations), consistency (catalog workspace patterns, owned primitives only), error prevention (chosen products disabled in other lines, `required`/`min`/`max`/`maxLength`, empty-picker warnings disabling submit), opaque failure with recovery guidance. No finding ≥ severity 2.
- **WCAG 2.2 AA:** (a) automated layer green in evidence (axe zero severe, ≥44px targets, focus visible, no overflow); (b) semantic layer inspected: `htmlFor` labels, `aria-describedby` help text, `role="status"`/`role="alert"` on notices, 375px and 320px reflow captures clean, no keyboard trap in the `details` confirmation.
- **Visual loop:** critic sampled `state-pt-BR-link-new-1440`, `state-pt-BR-link-new-375`, `state-pt-BR-link-edit-1440`, `state-pt-BR-link-detail-1440`, `state-en-link-failed-notice-1440`: hierarchy, alignment, tokens, and bilingual copy correct; immutable kind/type/pair shown as read-only facts on edit; no overlap or truncation. Manifest `review.md` records zero unresolved findings.

## 6. Findings

- **nit** — `src/app/(merchant)/links/link-v2-actions.tsx:36-39`: the `<summary>` and the `AlertTitle` repeat the same confirm text; harmless redundancy (severity 1).
- **nit** — `src/app/(merchant)/links/link-v2-form.tsx`: `descriptionPtBr` lacks the `aria-describedby` help its `descriptionEn` sibling carries (severity 1; the generic help is non-essential).
- **nit** — `links-notices.tsx` reuses `adminErrorHeading`/`adminSuccessHeading` dictionary keys cross-domain; works and matches existing reuse, but a dedicated key would read cleaner (severity 1).

No blocking findings. Ownership respected: every changed file is inside F01 `owns` or is an approved contract/doc line (AGENTS.md redirect-target line, spec 8.2.2 slice, DESIGN.md matrix).

## Decision

**APPROVED** — full mode, first pass. verificado por revisor independente (yolo) — 2026-07-25
