# Approval — [[8.2.2-build-payment-link-create-edit-flows]]

### Resposta do crítico (yolo)

**Verdict: APPROVED** (round 1 of 2).

Verified against the delivered code, adversarially:

1. **Objective verifiable:** all 10 criteria carry objective execution or inspection (`pnpm check`, focused tests, `git diff` emptiness, evidence run/verify); at least one `re-run` criterion. ✓
2. **Faithful to the card:** create (product-cart `PRODUCT_LINES` and `FIXED_AMOUNT`), edit allowed fields, activate/deactivate, expiry, opaque bilingual notices, share preserved from 8.2.1 — all covered by the observable result and criteria. ✓
3. **Prefill seam legitimate and minimal:** confirmed `createPaymentLinkV2Store(...).findOwned` (`src/auth/payment-link-v2.ts:64,350`) returns `version` and `lines[].productId` (`OwnerPaymentLinkV2`, `src/auth/payment-link-v2.ts:17-33`), while the redacted 8.2.1 view exposes neither (`src/auth/payment-link-v2-view.ts:208` carries only title/price). One additive server-only owner read with opaque `null`; the plan explicitly forbids touching the mutation service or the view, and criterion 6 enforces it via empty `git diff`. Not contract redesign. ✓
4. **Redirect repointing sound:** routes currently emit `/?payment-links-v2=…` (`src/app/payment-links-v2/route.ts:24-26`, `src/app/payment-links-v2/[id]/route.ts:36-40`); targets-only change matches the 7.2.3 catalog precedent. The `noticeKey` extraction-before-canonicalization pattern exists (`src/app/(merchant)/catalog/directory-query.ts:27,40`) and is required because `src/app/(merchant)/links/directory-query.ts:34` canonicalizes strictly. ✓
5. **V1 byte-frozen verifiable:** `Must not edit` lists V1 module/routes/surfaces; criterion 10 checks zero diff plus V1 tests green. ✓
6. **Picker reuse sound:** V1 `getPaymentLinkService().listForOwner` (`src/auth/payment-link.ts:110`) is read-only owner data; reused without editing the frozen V1 module. ✓
7. **Risks covered:** UTC `YYYY-MM-DDTHH:mm` expiry grammar, opaque attempt-lock failure with the honest new-version fork UX, and the notice/canonicalization collision all have concrete mitigations; abort conditions are explicit. ✓
8. **Proportionality and gates:** single cohesive front, no `(user)` items, specs/DOX expected changes linked, skills table filled. Size L justifies the plan length. ✓

aprovado por revisor independente (yolo) — 2026-07-25
