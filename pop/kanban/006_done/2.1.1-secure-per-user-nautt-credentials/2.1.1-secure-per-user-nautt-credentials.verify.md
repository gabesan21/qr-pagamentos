# 2.1.1-secure-per-user-nautt-credentials — Verification

## Summary

All acceptance criteria passed. The aggregate gate `pnpm check` is green.

## Criterion results

| Critério | Resultado | Comando / evidência |
|----------|-----------|---------------------|
| Encrypted storage schema deploys and runtime role has only DML rights on `app.nautt_credential` | ✅ PASS | `pnpm db:test` — all 16 disposable probes passed, including `PASS nautt-credential-schema` and `PASS role-separation` / `PASS runtime-denials`. The runtime user has `SELECT, INSERT, UPDATE, DELETE` on `app.nautt_credential` and no DDL/privilege escalation. |
| AES-256-GCM round-trip succeeds and wrong key / tampered ciphertext fail closed | ✅ PASS | `pnpm test -- src/lib/nautt-crypto.test.ts` — 9/9 tests passed, including round-trip, IV non-reuse, wrong-key failure, tampered ciphertext, tampered auth tag, malformed composite, and key-length rejection. No key material printed in output. |
| Service enforces owner-or-admin authorization, rejects disabled actors, and redacts the key in DTOs | ✅ PASS | `pnpm test -- src/auth/nautt-credential.test.ts` — 9/9 tests passed, covering owner self-save, admin cross-user save, non-owner rejection, disabled-actor rejection, empty-key validation, and redacted DTO leakage checks. |
| Safe replacement: saving a second key overwrites the first without exposing the old plaintext | ✅ PASS | Covered by the same service test run (`src/auth/nautt-credential.test.ts`); replacement preserves `createdAt`, advances `updatedAt`, and stores a new encrypted value. Old plaintext is never returned or logged. |
| Admin route returns empty `401`/`403` and opaque `303` redirects without target/key disclosure | ✅ PASS | `pnpm test -- src/app/admin/users/[id]/nautt-credentials/route.test.ts` — 3/3 tests passed. Protected responses are empty and carry no `location` header; success/failure redirects omit `secret-key` and `target`. |
| No plaintext or encrypted Nautt key appears in logs, error messages, or DTOs | ✅ PASS | Manual grep across `src/`, `container/`, `install/`, `compose.yaml`, `.env.example` found no `console.log`/`error`/`warn`/`info` emitting `apiKey`, `encryptedApiKey`, or Nautt key patterns. Non-test source uses these only as parameter/field identifiers; the redacted DTO contains only `hasCredential` and `updatedAt`. Generated Prisma types in `src/generated/prisma/` contain the field name but are gitignored generated code, not runtime output. `pnpm lint && pnpm test` passed as part of `pnpm check`. |
| `NAUTT_ENCRYPTION_KEY` is wired through install, compose, and runtime as a file-backed secret | ✅ PASS | `install/test.sh` — `PASS install-contract` (covers key generation/validation paths, `.install-secrets` staging, and dry-run absence of the key value). `pnpm container:contract-check` — `PASS image-contract`. Additionally, manual review confirmed: `compose.yaml` mounts `nautt_encryption_key` only on the `app` service; `container/runtime.mjs` reads `/run/secrets/nautt_encryption_key` and injects `NAUTT_ENCRYPTION_KEY`; `install/install.sh` validates/generates the key and stages it with mode `0400` / uid `1000`. |
| Aggregate quality gate remains green | ✅ PASS | `pnpm check` — `pnpm typecheck && pnpm lint && pnpm test && pnpm build` all passed. `193 passed | 1 skipped` tests, build completed with static page generation. Only output was a Next.js workspace-root inference warning, non-fatal. |

## Clean-code-review observations

- **Behavior & bordas:** cryptography fails closed; service re-authorizes on every call; route uses `protectedMutationResponse` for empty `401`/`403` and opaque redirects for other errors.
- **Tests:** new behavior is covered by dedicated unit tests for crypto, service, and route.
- **Complexidade:** service uses a small `CryptoAdapter` interface, keeping the route body thin.
- **Nomes & comentários:** variables clearly express intent; no explanatory comments are needed beyond the install `.env.example` warning.
- **Consistência:** migration follows existing naming/ACL conventions; schema relation mirrors `PasswordCredential`; spec status updated to `in progress` with the delivered slice documented.

## Outcome

Task **approved** for 005 → 006 transition. No blockers.

### Resposta do crítico (yolo)

- [x] Verificado

verificado por agente crítico (yolo) — 2026-07-17

**Nota:** Re-executei todos os critérios `re-run` na worktree (`pnpm db:test`, testes de crypto/serviço/rota, `install/test.sh`, `pnpm container:contract-check` e `pnpm check`) e todos passaram. Re-fiz o grep por vazamento de chave em `src/`, `container/` e `install/` e não encontrei saídas de `apiKey`/`encryptedApiKey`/`NAUTT_ENCRYPTION_KEY`. A migração concede apenas DML ao papel runtime, o roteamento admin usa `protectedMutationResponse` para respostas vazias e redirecionamentos opacos, e o helper `getDecryptedApiKey` permanece server-only sem exposição HTTP, conforme o escopo aprovado.
