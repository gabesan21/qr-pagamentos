# Verification — 7.2.2-evolve-products-for-catalog-media

- **Mode:** full (critical: true) — fresh strong session, complete diff audit + full evidence re-run.
- **Delivery:** worktree `pop/worktrees/7.2.2-evolve-products-for-catalog-media`, branch `task/7.2.2-evolve-products-for-catalog-media`, commits `7fd8765` + `975cf2a`, merge-base vs `develop` = `52c84aa`.

## Original request first

The card asks for category, currency, image, archival, and immutable historical-reference contracts on products, with currency mappings as admin-registered runtime data and a missing active mapping as a normal gated state with typed errors. All five are delivered and behave as specified; the binding 003 round-2 archival contract (existing baseline trigger + picker filter, data-level-only "links intact", V1-deactivation checkout semantics, state-agnostic reassignment) is implemented exactly as approved. **The original request is met.**

## Scope audit (diff vs merge-base)

12 files: `src/auth/product.ts`, `src/auth/product.test.ts`, `src/auth/product-test-store.ts`, `src/app/products/route.ts`, `src/app/products/route.test.ts`, `prisma/schema.prisma` (Product only), new `prisma/migrations/20260724220000_product_catalog_media/`, `pop/scripts/db-test.mjs`, `pop/specs/catalog-and-payment-links.md`, `AGENTS.md`, `prisma/AGENTS.md`. Zero edits to must-not-edit paths: `src/media/**`, `src/auth/supported-exchange-currency.ts`, `src/auth/product-category.ts`, `src/auth/payment-link.ts`, all payment-link/order/checkout/storefront projections, the 19-migration baseline, existing migration dirs, `src/data-directory/**`, all UI pages. No `pop/kanban` edits. `pop/scripts/db-test.mjs` is outside Front A's "owns" list but is the db-contract harness the plan's own AC requires to assert the new columns/trigger behavior (7.1.x precedent); justified evidence wiring, not an ownership violation.

## Numbered judgments

1. **Identifier-storage deviation — APPROVED.** `src/media/AGENTS.md:16` makes `activateOwned`/`orphanOwned` identifier-only and forbids exposing the internal id/revision/storage key to callers; persisting an internal UUID in `app.product` would force exactly that exposure. The opaque 43-char identifier mirrors the accepted `storefront_logo_media_identifier` precedent. No invariant weakened: owner/purpose fencing runs inside `activateOwned(actor, identifier, "PRODUCT_IMAGE")`; STAGED→ACTIVE happens before the save, orphan-on-replace/remove after it, best-effort compensating orphan on save failure (tests assert call order `activate → save → orphan` and the compensation path); grace period stays inside the media subtree. The no-FK rationale is recorded in `prisma/AGENTS.md`.
2. **Currency gating — correct.** `src/auth/product.ts` gates via `requireActivePair` only when a patch defines a non-null code different from the stored one; unchanged re-submit and clearing never gate (test "re-gates only a changed code…" proves all three with the registry mocked to fail). Stored codes survive later deactivation (list reads carry no registry dependency). Malformed codes rejected pre-registry. Typed `NoActiveExchangeCurrencyMappingError` reaches the route, which collapses it to the one opaque `/?products=failed`. No leak into V1 DTOs/links (payment-link binding untouched).
3. **Archival — correct.** One-way CAS (`where: { id, ownerId, version, archivedAt: null }`, sets `active=false` + `archived_at`, version increment); update/setActive/archive all exclude archived rows at store level, with a service-level `findOwned` pre-check. db-test proves new-link rejection through the untouched baseline trigger (`payment_link_product_active`, SQLSTATE 23514) after archival. No link/order rewrite anywhere in the diff (data-level preservation). `src/auth/payment-link.ts` and all triggers byte-untouched.
4. **Migration — compliant.** `20260724220000_product_catalog_media`: three nullable `addColumn` + one check `addConstraint`, all closed safe language (`migration-policy verify` PASS, baseline=19). `AAA`–`ZZZ` bounds mirror the pointer-table doctrine; no FK on `image_media_id`; sorts after 7.1.2's integrated migration (lane preflight honored).
5. **V1 DTO byte-stability — claim sufficient.** Every V1 projection file is byte-identical to the merge-base, and all read paths use explicit Prisma `select` clauses (`public-checkout-presentation.ts:47`, `order-view.ts:128-129`), so new nullable columns cannot leak by construction; the unmodified payment-link/order/checkout/storefront suites pass inside `pnpm check`. A dedicated assertion would only re-test untouched code; not required.
6. **No UI, spec/DOX sync, no kanban edits — confirmed.** Only form-field plumbing on the existing route (new `archive` action rides `/products`, no new route/limiter/log inventory). Spec bullets match implemented semantics verbatim (gating, lifecycle order, data-level preservation, reassignment scope, 7.2.2 history entry). Root `AGENTS.md` product bullet and `prisma/AGENTS.md` invariant added; `media-storage.md`/`storefront-and-customization.md` untouched per the plan's conditional.

## Evidence re-run (worktree, all green)

- `CI=true pnpm db:generate` — Prisma Client regenerated clean.
- `CI=true pnpm db:contract-check` — `migration-policy verify` (baseline=19 future=6), language, fixtures, documentation-contract: PASS.
- `CI=true pnpm db:test` — 34/34 PASS incl. `product-schema` (new columns, bounds SQLSTATEs, archive CAS, re-archive/edit-lock denials) and `payment-link-schema-and-locking` (baseline-trigger rejection of links to archived product).
- Focused vitest `src/auth/product.test.ts` + `src/app/products/route.test.ts` — 34/34 PASS.
- `CI=true pnpm check` — typecheck, lint (0 errors, 11 pre-existing warnings), 942 passed / 7 skipped, build: all green.
- `python3 pop/scripts/pop_validate.py --standalone` (main checkout) — valid, no violations (1 benign worktree-stage warning).
- Worktree clean; HEAD = `975cf2a`.

## Findings

- No blocking findings.
- Nit (non-blocking): if the post-save orphan of a replaced image throws, the route reports `failed` although the save committed; state stays consistent (old object ACTIVE and quota-counted), matching the documented worst case. No change required.

## Decision

**APPROVE.**

verificado por revisor independente (yolo) — 2026-07-24
