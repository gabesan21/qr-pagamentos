# Verification — [[10.4.1-build-admin-settings-hub]]

- **Stage:** 005_verifying · **Gate:** yolo 005 (single quality gate) · **Mode:** FULL (mandatory — `critical: true`)
- **Critic:** independent reviewer, fresh context · **Date:** 2026-07-27
- **Diff audited:** `05dd378..91fddf8` (8 commits) in `pop/worktrees/10.4.1-build-admin-settings-hub`

## 1. Original request first (card "What / Why")

**Met.** The card asks for the dedicated bilingual Settings area covering the closed exchange-currency catalog, payment methods, the default theme for new users, language, and all existing administrator-only configuration with safe dependency behavior. Delivered and proven:

- `/admin/settings` is a six-section anchored hub (exchange currencies → currency pairs → payment methods → global payment settings → appearance → language) with a 6-item in-page anchor nav, per-section empty states, and safe-dependency copy per section (`settings-surface.tsx`, page test pins all six ids/anchors bilingually).
- The default theme for new users persists in the `system_settings` singleton, is administrator-editable through `POST /admin/settings/default-theme`, falls back to `DEFAULT_STOREFRONT_THEME_ID` on the empty start, and is stamped USER-only at creation time (service unit tests + disposable-DB tests + evidence-run DB probes).
- Every composed mutation redirects back to `/admin/settings?…` with the closed bilingual notice set; wrong-role/unauthenticated outcomes stay the documented empty `401`/`403` (route tests).

## 2. Gate re-runs by this critic (explicit exit codes)

| Gate | Result |
|------|--------|
| `pnpm typecheck` | exit 0 |
| `pnpm test` | exit 0 — 178 files passed / 9 skipped (database suites skip without disposable env), 1514 tests passed |
| `pnpm admin-settings:evidence:verify` (run 20260727053634) | exit 0 — "Verified administrator settings hub evidence 20260727053634" |
| `pnpm lint` | exit 0 (47 warnings, 0 errors) |
| `pnpm db:migration-policy` | exit 0 — baseline=19 future=12, language and fixtures PASS |
| `pnpm admin:contract-check` | exit 0 |
| `pnpm admin:source-check` | exit 1 — **baseline-identical**: the same 120 failures reproduce byte-identically on `develop` (15c1d3f) in the main working tree; zero failures in any file this task touches (see Finding 2) |
| `pnpm check` (build), `pnpm db:test`, `pnpm db:contract-check`, `pnpm container:contract-check` | accepted on the 004 report (all exit 0) + artifact spot-check: bootstrap ACL block, db-test probes, and migration files inspected directly |

Evidence-run provenance verified: `manifest.json` gitHead = `0956e35` (post-fix commit), 40 captures hashed and present, `externalRequests`/`consoleErrors`/`pageErrors` all empty, 13 source hashes still valid at HEAD (verify exit 0 proves no post-run source drift); `91fddf8` only binds the artifacts.

## 3. Acceptance criteria (full mode)

1. **Six sections bilingually + anchor nav + empty states + functional theme form — PASS.** `page.test.tsx` pins all six anchors, forms, notice set, and empty states in both locales; evidence assertions (`hub-structure`, 36-capture grid + 4 state captures) prove it in production. Screenshots visually inspected (`state-pt-BR-hub-empty-375`, `state-en-exchange-currency-registered-1440`): clean composition, no overflow at 375, nav wraps gracefully.
2. **Persistence + fallback — PASS.** `system-settings.test.ts` (4 unit) + `system-settings.database.test.ts` (disposable DB: empty start → `pix-paper`, save → persisted row, replace keeps singleton). Invalid ids → `SystemSettingsValidationError` → opaque `?error=theme-default-failed`.
3. **Creation-time stamping — PASS.** `administration.ts:82-86` stamps only for role `USER`; ADMIN rows NULL; no backfill anywhere in the diff; legacy NULL read resolution in `public-storefront.ts:177` / `public-checkout-v2-presentation.ts:123` byte-untouched (files not in diff). Evidence run probes the live DB (`creation-stamping`: merchant `vault-blue`, admin NULL).
4. **Canonical additive migration — PASS.** `20260727050000_system_settings` sorts after the 19-dir baseline and all later migrations; `migration.sql` carries only create-table + PK + two CHECKs + `GRANT SELECT, INSERT, UPDATE` — no DML/seed/backfill/raw SQL; generated header present; `migration-policy` verify exit 0. `bootstrap.sql` gains one relation-guarded re-pin block (REVOKE ALL → GRANT SELECT/INSERT/UPDATE, never DELETE), matching the `user_deletion` precedent; db-test probes pin singleton/closed-set checks and runtime DELETE denial.
5. **Exactly one new route + one literal — PASS.** `default-theme/route.ts`: request-log wrapper, origin-guard first, `requireAdminFromCookie`, opaque redirects, `protectedMutationResponse` empty 401/403. `server-request-log.ts` diff is exactly one additive literal. `src/security/public-rate-limit.ts` untouched. The six composed routes' diffs are redirect-target lines only (logic byte-identical); their route tests updated accordingly.
6. **`listMappings` — PASS.** Admin-only (`requireAdmin`), returns `store.listActive()` (active pointers, redacted `{code,label}`, ordered by code); test pins redaction shape, ordering, and USER/disabled-ADMIN denial. Owner read (`listActiveChoices`) and gating primitives untouched.
7. **Aggregate/static gates — PASS with one baseline note** (Finding 2): typecheck/lint/test/migration-policy/contract-check exit 0; source-check red but identical to `develop`.
8. **Run-bound evidence — PASS.** Dedicated spec + verify + `--admin-settings` wrapper; verify exit 0 (re-run by this critic).
9. **Role isolation — PASS.** Page re-authorizes via `requireAdminShellContext`; route test pins empty 401/403 with no service work; origin-guard denial precedes authentication.
10. **Specs/DOX — PASS (re-apply note for the orchestrator, below).**

## 4. Spec stale-base extraction (per 005 brief)

The branch compacted `pop/specs/administrative-foundation.md` against a stale 195-line base (126 lines in-branch vs develop's 144). Per instructions, the missing 10.2.2/10.1.2/10.3.2 content is **not** flagged. The four 10.4.1 additions themselves were extracted and verified correct, minimal, and cleanly re-appliable onto develop's 144-line version:

1. **"What it covers" bullet** (default-theme-for-new-users contract: singleton, fallback, admin-only save, USER-only creation-time stamping, no backfill, legacy NULL resolution) — additive, standalone, accurate to delivery.
2. **Route-table row amendment** — replaces the `…and planned administrator dashboard, global catalog/link directory, analytics, and system settings` tail with the delivered `/admin/settings` hub wording; the anchor row exists verbatim on develop, so the edit applies cleanly.
3. **Mutation-endpoint inventory** — adds `/admin/exchange-currencies` and `/admin/settings/default-theme` plus the "redirect back to `/admin/settings?…`" sentence; develop's paragraph has the same sentence stem (only its scaffold-vs-delivered tail differs, which is stale-base content, not the addition).
4. **Implemented-slice entry + Updated header line** — additive, accurate.

`pop/specs/administrative-design-system.md` (151→150) verified **lossless** against develop: the two sentences of the lockup bullet are joined onto one line with identical text — whitespace-only unwrap. `pop/specs/catalog-and-payment-links.md` registry-UI note is accurate and minimal.

## 5. Declared deviations — assessment

- **(a) Dedicated evidence spec/verify/wrapper with additive `--admin-settings` mode — ACCEPTED.** Follows the established per-surface precedent (`admin-orders`, `merchant-dashboard`, `storefront`, `standalone-payment`); the shared runner edit is purely additive.
- **(b) Two in-run evidence fixes — ACCEPTED.** `724bfce` is a test-only navigation-robustness fix; `0956e35` adds `role="listitem"` inside the `role="list"` catalog lists, correctly resolving a latent axe `aria-required-children` violation inherited from the removed legacy surface (the new surface is the only remaining `role="list"` user). The evidence run was re-executed after both fixes (manifest gitHead = `0956e35`) and verifies green.
- **(c) Unused legacy dictionary keys/classes left in place — ACCEPTED as declared** (e.g. `adminCatalogDescription`, the retired `AdminSurface`-era classes). Lint/typecheck green; recorded as a cleanup nit (Finding 3).

## 6. Findings

1. **[nit] Telemetry/frontmatter drift** — card frontmatter says `yolo_003_returns: 2`; the approval file shows one 003 return (round 1) followed by approval (round 2), and the Log records "devolução 1/2". Cosmetic only.
2. **[nit — repo-level observation, not this task]** `pnpm admin:source-check` exits 1 with 120 `local_variants`/`raw_controls` entries, **byte-identical on `develop` (15c1d3f)** and with zero entries in any file touched by this task. The gate is red independent of 10.4.1 (likely owned by an in-flight lane); plan criterion 7's literal "exit 0" is unmet repo-wide, but blocking this task for a baseline failure it did not introduce would be wrong. The orchestrator should track the red gate separately.
3. **[nit] Dead legacy artifacts** — `adminCatalogDescription` dictionary key and the retired `AdminSurface` CSS classes (`admin-shell`, `receipt-rail*`) are now unreferenced; declared deviation (c), safe to clean in a later pass.

No blocking findings. No ownership invasion: every edited file is inside the plan's Affected areas; fences hold (rate-limit untouched, registry semantics unchanged, frozen files untouched beyond the pinned `administration.ts` stamping amendment, no grants beyond the documented `SELECT`/`INSERT`/`UPDATE`, exactly one request-log literal).

## 7. Decision

**APPROVE.** The card objective is met; all re-runnable gates pass (typecheck 0, test 0, evidence verify 0, migration-policy 0, contract-check 0); the one red gate is baseline-identical on develop. Integration note for 006: re-apply the four extracted 10.4.1 spec additions (Section 4) onto develop's 144-line `administrative-foundation.md` instead of taking the branch's stale-base compaction, and keep the branch's whitespace-only unwrap of `administrative-design-system.md` optional.

verificado por revisor independente (yolo) — 2026-07-27
