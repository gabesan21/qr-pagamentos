# M-5.1-beta-unverified-webhook-intake — Approval

## Round 1 — 2026-07-25 (003, critical yolo)

### Resposta do crítico (yolo)

**Verdict: APPROVE.**

Adversarial read of the plan against the code, schema, spec, and DOX, per the five scrutiny axes:

1. **Seam sound.** `providerOrderUuid` is `String? @unique @db.Uuid` (`prisma/schema.prisma:276`) — globally unique with a unique index, and each `provider_order` row has exactly one `ownerId`, so `resolveOwner(providerOrderUuid)` cannot cross owner boundaries. Downstream nothing depends on door-time HMAC attribution: dedup keys on the global `delivery_uuid` PK, the 16s lease is delivery-scoped, and `reconcileWebhookOrder(ownerId, providerOrderUuid)` re-proves ownership through the versioned CAS (owner + provider UUID + version). Beta attribution is, if anything, more accurate than secret-match attribution. `webhook_delivery.owner_id` is NOT NULL (schema:383), so the no-evidence `204`/`400` fallbacks for unresolvable owners are a real constraint, correctly documented.
2. **Protocol honesty holds.** Narrowing to `204/400/503` keeps every response empty and no-store; `route.ts`'s `empty()` union is a superset and needs no change. The public-rate-limit bullet covers only the three `/api/payment-links/*` routes — zero interaction with this route, and the Deny list forbids touching it. Removing the `401` denial is exactly the human decision ("accept the body without verifying", read literally). The `204`-for-unknown-order deviation is documented (§6 + Risks) and safe: no state change, replay costs one indexed local lookup.
3. **Reversibility is real.** `webhook-signature.ts`, `webhook-secret-candidates.ts`, and their tests stay intact and green as the reversal target; `webhook-runtime.ts` keeps wiring `loadActiveWebhookSecrets`. The `webhook-intake-beta.test.ts` source assertion (marker present, `verifyWebhookOwner(` absent) is cheap, rides `pnpm test`, and fails on either marker loss or silent verification regain — i.e., it catches partial reversal in both directions. The pre-production deploy barrier is the open_question gate plus spec/DOX markers, which is the right layer for it.
4. **Registration trail complete and accurate.** The `nautt-production-webhook-hmac-contract` entry exists in `pop/RESEARCHES.md` with status `pending` (verified) and moves to `on-hold-by-beta-decision`; decision note, pre-production open question, spec beta-suspension note, and both DOX overrides are all listed. The cited spec locations match reality (lines 26, 35–36, 57, 64 make exactly the HMAC/401/orphan claims the plan annotates), and the plan adds beta caveats instead of rewriting the durable post-beta contract wording — the permanent contract survives intact.
5. **Security honesty adequate.** The plan states plainly that verification is skipped entirely (missing, malformed, and wrong signatures all accepted) and that any internet caller who learns a provider order UUID can trigger one owner-bound provider GET, with closed-beta exposure as the accepted mitigation. It neither hides nor downplays the unauthenticated window. (For the record: forged bodies cannot inject false order state, because notification status never reaches the mutation boundary — reconciliation takes state only from the authoritative owner-bound GET — so the plan's risk wording is precise, not generous.)

Gate 003 checklist: acceptance criteria are objectively verifiable (pnpm check, enumerated vitest assertions, marker guard, pop_validate); the brief is sufficient and lean; single front/one executor is safe for size S; spec impact is proportional; no avoidable `(user)` item (the business decision is already made and recorded on the card); verification rides the aggregate gate with re-runnable test criteria.

Non-blocking nits for 004: (a) the executor should sweep the whole spec webhook block (lines 35–38), not only 35–36 — line 38's "unknown orders perform zero provider reads" and "authenticated attempt" phrasing also need the beta caveat; (b) `WebhookIntakeDependencies.loadCandidates` is currently required — the beta seam must make it/`verifyOwner` optional or unused without breaking the runtime wiring, and the updated `webhook-intake.test.ts` must keep pinning the zero-call assertions.

aprovado por revisor independente (yolo) — 2026-07-25
