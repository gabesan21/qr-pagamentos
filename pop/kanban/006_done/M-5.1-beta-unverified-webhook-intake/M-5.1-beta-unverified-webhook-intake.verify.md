# M-5.1-beta-unverified-webhook-intake — Verification (005, critical yolo)

- **Mode:** FULL (critical task; full re-verification mandatory). Surface: the complete 10-file diff vs develop merge-base in worktree `pop/worktrees/M-5.1-beta-unverified-webhook-intake` @ `b682c7c`, plus re-run of the focused nautt suite, the aggregate `pnpm check` gate, and `pop_validate --standalone` from the main checkout.
- **Original request first (the explicit human beta decision, 2026-07-25):** Nautt webhook HMAC verification ON HOLD for closed beta; the app ACCEPTS webhook bodies without verifying; clearly marked, trivially reversible, resolved before production at human command; all other intake contracts preserved. **Verdict on the request: MET** — see findings below; no blocking deviations.

## Criteria and evidence

1. **The seam really skips verification.** `webhook-intake.ts` no longer imports `verifyWebhookOwner` or `parseWebhookSignature`; the signature gate, candidate loading, and owner verification block are gone. Missing (`null`), malformed (`sha256=bad`), and present-but-wrong (`sha256=00…0`) signatures are all accepted — pinned by the parametrized beta-acceptance test. The perf test was converted to a beta pin: 1,000-secret scenario asserts `decryptions === 0`, `comparisons === 0`, `loadCandidates`/`verifyOwner` never called, and elapsed drops to 10,600 ms (the 2,000 ms of secret work removed). The `BETA(M-5.1)` marker is prominent (five marked blocks including the result-type line with reversal instructions), and `webhook-intake-beta.test.ts` pins both the marker's presence and the absence of `verifyWebhookOwner(` in the intake module — it fails on marker loss or silent verification regain, in both directions.
2. **Owner attribution is sound.** `resolveOwner(providerOrderUuid)` is wired in `webhook-runtime.ts` as a direct Prisma `findFirst({ where: { providerOrderUuid }, select: { ownerId: true } })` over the globally `@unique` column (`prisma/schema.prisma:276`) — one row, one `ownerId`, no cross-owner attribution possible. Unknown order → `204` returned before any claim/evidence/reconcile (zero provider GETs, pinned). Malformed + resolvable owner → REJECTED evidence exactly as before (finalize called once with `decision: "REJECTED"`); malformed + unresolvable → `400` with no claim row (the `webhook_delivery.owner_id` NOT NULL constraint makes evidence impossible, correctly handled). `resolveOwner` failure → `503` with no state change. Diff vs merge-base confirms claim/lease/bindOrder/finalize/reconcile hunks are untouched.
3. **Reversal readiness.** `webhook-signature.ts`, `webhook-secret-candidates.ts`, and their tests are byte-intact (absent from the diff; `webhook-signature.test.ts` 8/8 still green unchanged). `webhook-runtime.ts` keeps wiring `loadActiveWebhookSecrets`. The 003 nit (b) was handled cleanly: `loadCandidates` is now optional and `verifyOwner` is an optional inline-typed double, so the runtime wiring and tests compile unchanged while nothing calls them. Reversal is one small marked block plus deleting the guard test.
4. **Registration trail landed and is accurate.** `pop/RESEARCHES.md` status → `on-hold-by-beta-decision (M-5.1…)` with the pre-production-command gate. `pop/notes/decisions/2026-07-25-beta-unverified-webhook-intake.md` records the verbatim decision, seam shape, accepted deviations, and reversal instructions, and states the risk plainly: "beta webhook intake is UNAUTHENTICATED. Any internet caller who learns a provider order UUID can trigger one owner-bound provider GET; forged bodies cannot inject false order state…". `pop/open_questions/2026-07-25-pre-production-gate-restore-webhook-hmac.md` (status `aberta`) declares "Production release is BLOCKED until the human explicitly commands the M-5.1 reversal and it lands" with a 4-step reversal checklist. Spec `pop/specs/nautt-finance-integration.md` carries BETA caveats at exactly the claimed spots (orphan-inert line ~26, the four webhook requirement bullets ~35–38, capability bullet ~57, Open-section orphan line ~64) — each caveat appended, never rewriting the permanent post-beta contract wording. Root `AGENTS.md` `src/app/` bullet and `src/integrations/nautt/AGENTS.md` webhook-intake section both carry the override pointing at the gate, with the permanent contract explicitly preserved below.
5. **Protocol preserved.** `route.ts` untouched (not in diff); its `empty()` union `204|400|401|413|503` is a superset, all responses empty with `Cache-Control: no-store`. `WebhookIntakeResult` narrowed to `204 | 400 | 503` — `401` is structurally un-emittable from intake. No changes under `src/security/` (rate limiter untouched), `src/observability/`, or `src/app/api/`.
6. **Scope clean.** Diff vs develop merge-base is exactly the claimed 10 files; zero `pop/kanban/` edits; worktree tree clean.

## Evidence re-run (this session, worktree)

- `CI=true pnpm vitest run src/integrations/nautt` → **16 files, 314/314 passed** (includes beta guard 2/2 and the rewritten intake suite 15/15). Key beta-acceptance tests read: missing/malformed/wrong signature all `204` with zero secret work; unknown order `204` no evidence zero GETs; malformed+unresolvable `400` no evidence; contradictory+resolvable durably REJECTED; perf pin 0 decrypts/0 compares.
- `CI=true pnpm check` → **exit 0** (re-run unpiped after a piped first run to remove pipe-masking doubt): lint 0 errors (17 warnings, all in catalog/storefront/order-view files untouched by this task — pre-existing), typecheck pass, 999 passed / 7 skipped across 135 files, Next build succeeds.
- `python3 pop/scripts/pop_validate.py --standalone` (main checkout) → **valid, no violations** (the single aviso about the worktree lacking a 004 task is expected — the card sits in 005).

## Findings

- **Blocking:** none.
- **Suggestions:** none.
- **Nits:** none. Both 003 non-blocking nits were discharged: (a) the whole spec webhook block including the "unknown orders perform zero provider reads" / "authenticated attempt" bullet received the beta caveat; (b) the optional-dependency wiring keeps the reversal target compiling with zero calls.

## Decision

**APPROVE.** The human's beta decision is implemented exactly as commanded — unverified intake behind one loudly marked, trivially reversible seam; every other intake contract (256 KiB bound, dedup, lease, reconciliation CAS, empty no-store statuses) preserved; the pre-production gate is impossible to miss across the decision note, open question, spec, and both DOX contracts.

verificado por revisor independente (yolo) — 2026-07-25
