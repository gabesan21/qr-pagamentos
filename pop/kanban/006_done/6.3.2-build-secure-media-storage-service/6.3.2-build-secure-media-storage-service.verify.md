# Verification — 6.3.2 Build secure media storage service

- **Mode:** full, mandatory critical-task round-2 independent yolo 005 review after return
- **Task commits:** `b3872a33dbc49bf45bf63a3d66766cacf222897f`, correction `99f68ec813cb2e4b088c41e50d601f620cb30fa2`
- **Verdict:** **APPROVE**

## Original-request check

The delivery meets the card's What/Why: it establishes one hardened server-only
boundary for opaque owner/purpose image metadata, bounded canonical validation,
atomic non-executable storage, revision-fenced lifecycle operations, physical
quota accounting, public/protected descriptor-authenticated reads, and orphan
reconciliation. It adds neither upload UI nor the production provisioning owned
by task 6.3.3.

The round-1 blocker is corrected. Runtime initialization now exercises the
failure semantics on which publication depends and refuses the root if the
filesystem violates them.

## Acceptance results

| Criterion | Result | Evidence |
|---|---|---|
| AC1 — additive least-grant metadata | **PASS** | Fresh migration-policy, language/fixture, documentation, generated SQL, index/check/FK, and static database contract checks pass. Runtime receives only table `SELECT/INSERT/UPDATE/DELETE`; the immutable 19-migration baseline is unchanged. |
| AC2 — PostgreSQL replay | **BASELINE BLOCKED** | The committed fixture covers invalid rows, revision CAS, quota admission, and runtime privilege denial. The prior disposable replay reached the unchanged malformed product-price fixture before media assertions; no media-specific failure was observed. |
| AC3 — pinned decoder | **PASS** | Fresh preflight reports Sharp `0.34.5` and libvips `8.17.3`; focused validation tests pass with format/page/dimension/pixel/byte/timeout/cache/worker limits. Container execution remains registry-DNS blocked. |
| AC4 — durable publication/probe | **PASS** | Fresh storage tests prove exact `EEXIST` for an existing `O_EXCL` source and hard-link target, exact `ELOOP` for `O_NOFOLLOW`, unchanged collision inode/content, regular same-inode hard link, residue-free cleanup, both cleanup directory-sync attempts, and refusal on unexpected success, wrong errno, target mutation, unlink residue/failure, or sync failure. Publication order, crash residue, no-clobber, corruption, and symlink-swap cases pass. |
| AC5 — lifecycle and quota | **PASS** | Owner/purpose/state/revision CAS, ABA rejection, every-state accounting, owner serialization, fail-closed inventory, and public revocation tests pass. |
| AC6 — reconciliation | **PASS** | Exact focused reconciliation suite passes: only stale writing/due orphan/retryable deleting rows are claimed, failed durable deletion remains counted, incomplete inventory blocks, and repeated reconciliation is idempotent. |
| AC7 — opaque reads | **PASS** | Bounded same-descriptor length/SHA-256 checks, final state/revision recheck, owner-role isolation, no-store successes/failures, and identical empty `404` outcomes pass. |
| AC8 — contracts/baselines | **PASS with classified baseline/environment exceptions** | Media spec plus root/subtree DOX are synchronized; PoP validation, diff check, design-token check, and changed-code lint pass. Typecheck reaches only the unchanged storefront `HeadersInit` baseline error. Container contract reaches its pinned-node proof then fails on Docker Hub DNS. |

## Round-1 correction proof

`src/media/media-storage.ts` now:

1. creates and syncs a private source, then requires exact `EEXIST` from a
   second `O_CREAT|O_EXCL|O_NOFOLLOW` open;
2. proves an ordinary hard link is the same regular inode and bytes;
3. creates and syncs an existing collision target, requires exact `EEXIST`
   from the second link, then reopens it and verifies unchanged device, inode,
   and content;
4. creates a symlink and requires exact `ELOOP` from `O_NOFOLLOW`;
5. closes/removes every fixture, verifies every path is absent, attempts both
   object and staging directory syncs even after an earlier cleanup failure,
   and accepts the root only if the complete cleanup succeeds.

The injectable tests fail initialization for semantic success where failure was
required, wrong errno, collision replacement/content mutation, cleanup refusal,
false-success residue, and cleanup sync failure. The normal fixture proves the
host supports the complete sequence. No blocking finding remains.

## Verification evidence

- `node scripts/media-decoder-preflight.mjs` — **PASS**, Sharp `0.34.5`, libvips `8.17.3`
- focused media and route Vitest suites — **PASS**, 5 files / 47 tests
- storage plus route suites alone — **PASS**, 2 files / 29 tests
- `node pop/scripts/migration-policy.mjs verify` — **PASS**
- `node pop/scripts/migration-policy-contract.mjs` — **PASS**
- `node pop/scripts/db-contract-check.mjs` — **PASS**
- `python3 pop/scripts/pop_validate.py --standalone` — **PASS**
- `git diff --check b3872a3^..HEAD` — **PASS**
- changed-code ESLint and design-token check — **PASS**, no errors
- generated-client `tsc --noEmit` — **BASELINE**, only
  `src/app/storefront/route.test.ts:29` `HeadersInit`
- broader application Vitest sampling — 394 files / 4029 tests pass; known
  `server-only` resolution and storefront locale expectation remain, while an
  existing `.next` tree caused unrelated vendored tests to be discovered
- `node pop/scripts/container-contract-check.mjs` — pinned-node proof passes,
  then **ENVIRONMENT BLOCKED** by Docker Hub DNS

## Findings and decision

- **Blocking findings:** none.
- **Suggestions/nits:** none required for closeout.
- **Return count:** one prior 005 return; this round approves without circuit breaker.

verificado por revisor independente (yolo) — 2026-07-24
