# Verification — [[6.1.2-enforce-role-exclusive-authentication]]

- **Stage:** 005_verifying
- **Reviewer:** independent yolo critic
- **Round:** 1
- **Mode:** full — mandatory for a critical task; reviewed the original objective first, dependency memory and linked specs, the complete `6b8c769..c85856a` diff, all principal-taking services and protected route/page inventory, DOX, tests, and project gates.
- **Delivery:** `c85856a`

## Original request

**Met.** Successful login still dispatches through `/`; the root resolves role before merchant or locale work and redirects an active `ADMIN` to `/admin`, while an active `USER` retains the merchant home. A single merchant-only primitive now guards cookie routes and every owner-service seam, and administrators cannot reach merchant reads, parsing, persistence, cryptography, or provider work.

## Full-surface evidence

- `src/auth/authorization.ts:16-20,32-44` preserves no-active-principal as unauthenticated and makes an active wrong-role principal forbidden; `src/app/owner-guard.ts:5-12` maps the primitive to exact empty `401`/`403` mutation outcomes.
- `src/app/page.tsx:24-35` and both owner-order pages redirect `ADMIN` before locale, params, or owner reads. The login submit route remains unchanged and redirects successful authentication through `/`.
- The complete `Principal` inventory separates existing administrator-only services from owner seams. Product, payment-link, checkout-policy, storefront, Nautt credential/onboarding, and owner-order methods all call `requireUserPrincipal` before input validation, store access, randomness, cryptography, registration, or provider work. The changed Nautt DOX records the same boundary.
- Every merchant POST keeps same-origin rejection first, then merchant authorization before form or route-parameter parsing and service work. `src/app/admin/users/[id]/nautt-credentials/route.ts:7-18` is denial-only: no active principal gets empty `401`; either active role gets empty `403`; it parses no body/target and performs no service work.
- `listForAdmin`/`getForAdmin` and `/admin/orders*` are unchanged, separately re-authorized, read-only, and policy-redacted. Public/sessionless handlers, identifiers, rate limiting, origin guard, request-log inventory/wrapping, locale stubs, schema, and provider protocols are outside the diff; their focused contract suites remain green except the exact storefront baseline failure below.
- The 39-file diff stays within planned ownership. Root DOX and the dependency specs already state the delivered runtime contract; the planned spec implemented-slice synchronization remains correctly deferred to 006.

## Findings

- No blocking, suggestion, or nit finding.

## Checks

- Acceptance AC1–AC5 union — 25 files, 130 tests passed.
- Admin-order/public/sessionless/origin/request-log preservation selection — 11 files and 62 tests passed; the one storefront locale expectation failure is reproduced unchanged at the exact pre-delivery commit `6b8c769`.
- `pnpm test` — 93 files and 743 tests passed, 1 file/6 tests skipped; only the pre-existing `server-only` resolution suite failure and storefront locale expectation failure remain. Both failing files reproduce at `6b8c769`.
- `pnpm lint` — pass with 11 pre-existing warnings on unchanged destructuring in `src/orders/order-view.ts`; `pnpm typecheck` — pass; `pnpm build` — pass outside the restricted sandbox after the sandboxed Turbopack worker could not bind an internal port.
- `git diff --check`; complete diff/scope and route/service inventory inspection — pass.

## Decision

**Approved → 006.** The original request and AC1–AC5 are met. AC6 is baseline-classified: all independently runnable components are clean, and both full-suite failures are proven present at the exact parent commit. No blocker warrants a return to 004.

verificado por revisor independente (yolo) — 2026-07-23
