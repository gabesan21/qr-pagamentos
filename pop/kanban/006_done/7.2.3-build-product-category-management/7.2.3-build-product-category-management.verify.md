---
id: 7.2.3-build-product-category-management
stage: 005_verifying
created: 2026-07-25
---

# Verify — 7.2.3 Build product and category management

- **Decision:** APPROVE
- **Mode:** differential — non-critical, first round (yolo_005_returns: 0). The changed
  surface was re-executed in full (aggregate gate + focused tests + evidence verify)
  and every new/changed source file was read line-by-line; unchanged foundations
  (`src/data-directory`, `src/media`, `src/auth`, `prisma`, `src/components/ui`) were
  audited by zero-diff against the merge-base plus their existing contract tests
  inside `pnpm check`. The delivery is large (90 files) but 49 of them are evidence
  PNG/JSON artifacts, so the auditable surface is proportionate to differential depth.
- **Delivery:** worktree `pop/worktrees/7.2.3-build-product-category-management`,
  branch `task/7.2.3-build-product-category-management`, 12 commits, tip `9449b54`,
  merge-base `4a5dd32`.

## Original request first

The card asks for dedicated bilingual product/category list, create, and edit pages
with uploads, placeholders, and size guidance over the integrated 7.2.1/7.2.2
contracts. Delivered: `/catalog` products directory, `/catalog/products/new`,
`/catalog/products/[id]`, `/catalog/categories`, the `POST /products/images` staging
route, dirty-field omission so an unchanged stored currency never re-gates, a
disabled+explained currency select when no active mapping exists
(`currency-unmapped` evidence capture confirms `disabled: true`), a terminal
read-only archived view with zero mutation controls, and STAGED previews riding the
owner-fenced `GET /media/[identifier]`. The request is met.

## Criteria and evidence

1. **`CI=true pnpm check` (re-run):** green — 0 lint errors (15 pre-existing
   warnings), 969 tests passed / 7 skipped across 129 files, production build OK.
2. **Focused vitest (re-run):** `products/images/route.test.ts`,
   repointed `products`/`product-categories` route tests, and all four catalog page
   test files — 8 files, 36 tests, all green.
3. **`pnpm catalog:evidence:verify` (re-run):** "Verified merchant catalog evidence
   20260725044158". Manifest binds 49 captures (36 base = 6 themes × 2 locales ×
   375/768/1440 + 13 state captures), `severeAxe` empty in every assertion, targets
   ≥44px, overflow false, visible 2px focus. Run captured at code tip `7f24e72`;
   `9449b54` only commits the artifacts — no code drift between run and branch tip.
4. **`python3 pop/scripts/pop_validate.py --standalone` (main checkout):** valid, no
   violations (one benign warning: worktree has no task in 004_processing — expected,
   the card sits in 005).
5. **Scope vs merge-base:** zero diffs in `src/auth/`, `src/media/`, `prisma/`,
   `src/data-directory/`, `src/components/ui/`, V1 payment-link/checkout/order
   surfaces, `pop/kanban`. `src/app/admin/product-management.tsx` keeps only
   `formatProductPrice` (still consumed by orders/payment-link surfaces; the removed
   V1 scaffold was superseded by the workspace, as planned).

## Scrutiny points

1. **`POST /products/images`:** origin-guard runs first inside the log wrapper,
   before auth/parse; `requireOwnerFromCookie`; Content-Length integer bound
   (5 MiB + 64 KiB envelope) rejected pre-decode; missing/oversized part and media
   failures share one opaque empty `422`; success is only `200 {identifier}` with
   `no-store`; exactly one literal `productImages: "/products/images"` entry in
   `serverRequestRoutes`. Route tests cover ordering, opacity, and no-store.
2. **Single-page directory:** page-local `directory-query.ts` reuses the foundation's
   `parseDirectoryQuery` (read-only import; zero edits under `src/data-directory/`);
   any cursor value → invalid-query; non-canonical queries → 307 reset (unit-tested);
   pageSize caps rows with an explicit truncation note, and the composition's own
   25/50/100 page-size control stays available — products are not silently hidden.
3. **Archived page:** read-only `dl` facts + warning alert, no edit/active/delete/
   archive control; copy states permanence and data-level preservation only (no
   claim that checkout keeps serving). Category deactivation requires a distinct
   active replacement when referenced; referenced-without-replacement renders a
   destructive explanation alert instead of a submit; inactive rows render
   `getRowActions → null` (edit-locked).
4. **DirtySelect / ProductImageField:** select is `name`-less until `onChange`
   (absent = unchanged at the service boundary, wired in `products/route.ts` via
   `form.has(...)`); hidden `imageMediaId` only for a newly staged identifier,
   explicit blank on remove, initial image omitted; preview via `/media/[id]`;
   client hints (JPEG/PNG/WebP, ≤5 MiB) mirror server limits; placeholders are
   `BrandIdentity` `mark-only` / `merchant-fallback` — no page-local assets.
5. **Bilingual parity:** en/pt-BR key sets identical (73 `catalog*` keys each,
   mirrored; dictionaries contract test inside `pnpm check`). axe serious/critical
   = 0 across all 49 captures; run/verify pair (`catalog:evidence` /
   `catalog:evidence:verify`) reproducible and hash-bound (`current.json`,
   manifest SHA-256 in `review.md`).
6. **Evidence-caught defect:** `f98d14f` correctly nests the archive form inside
   `<details>`, matching the category-deactivation pattern — confirmed in the final
   source. Spec-side fixes are legitimate: `12.5`/`9.9` match the canonical
   `PRICE_PATTERN` (trailing fractional zero rejected); the admin mapping is now
   registered via a browser-context fetch carrying the session cookie and
   same-origin `Origin`; fail-fast assertions harden the evidence spec only.
7. **Docs sync:** DESIGN.md (workspace section + evidence pair), root AGENTS.md
   catalog bullet, and the catalog spec's slice/image-boundary updates all match
   the delivered behavior.

## Findings

1. **Sugestão (UI severidade 2)** — `src/app/(merchant)/catalog/product-form.tsx:128`:
   a product whose stored category is inactive renders the category select as
   "None" (inactive options are absent per plan), while the directory still shows
   the inactive category name — an inconsistent read of the same fact. The stored
   value is safely retained via absent=unchanged (workaround exists), and the
   currency field already demonstrates the factual-disabled-row pattern that could
   be mirrored. Follow-up material, not blocking.
2. **Nit (severidade 1)** — the directories' `error` (loadFailed) state has no
   catalog-side unit test or evidence capture; it is a thin wiring over the
   foundation's `DataDirectory`, whose own `error` state is unit-tested
   (`src/data-directory/ui/data-directory.test.tsx:73`).

verificado por revisor independente (yolo) — 2026-07-25
