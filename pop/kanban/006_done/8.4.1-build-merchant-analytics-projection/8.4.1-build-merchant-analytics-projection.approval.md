# Aprovação — [[8.4.1-build-merchant-analytics-projection]]

## Rodada 1 — 2026-07-25 (gate 003, yolo critical)

### Resposta do crítico (yolo)

**APROVADO.**

Verificação adversarial do plano contra card, roadmap 8.4, specs V2 e código real:

1. **Entregável verificável:** critérios 2–4 executáveis e re-run (`pnpm check`, `pnpm db:test`); 1, 5 e 6 inspecionáveis com resultado observável (seção de spec, DTO, `git diff --stat`). ✔
2. **Métricas fiéis ao roadmap:** o conjunto (sales, conversion, abandonment, product, link, best-seller) cobre exatamente a descrição de 8.4.1 na epoch 8; "recent activity" consta no What do card e entra como reúso da projeção `order-v2-view`, sem invadir a UI de 8.4.2. ✔
3. **Premissas do recon confirmadas no código:** `order_v2` (owner/source/`state` nullable/amount TEXT/par de moedas/`settled_at` carimbado apenas em `CONFIRMED` — `src/orders/order-v2.ts:437`), `order_v2_line`, `checkout_attempt_v2` (campos de capability persistidos, `state`), `order_local_outcome_v2` (`LOCAL_FINALIZED`/`LOCAL_CANCELLED`, latest-is-current) existem conforme `prisma/schema.prisma:309-427`; os índices citados ((owner,created,id), (owner,state,created,id), (owner,source,created,id), attempt (owner,capability_expires_at)) batem com o schema (`@@index` linhas 346-349, 424). ✔
4. **Decisões pinadas sãs e explícitas (sem decisão escondida):** timezone fixo `America/Sao_Paulo` (sem DST desde 2019, não configurável); períodos fechados com bounds UTC half-open server-side; somas BigInt por grupo `(currency_uuid, exchange_currency_uuid)` sem agregação cross-currency (espelha a regra de exatidão decimal já vigente em `order-v2.ts`); vendas confirmadas vs localmente finalizadas como membros separados nunca fundidos — respeita a cerca de precedência da spec (local outcome nunca mascara `state`); funil derivável só de campos persistidos (`created_at`, `capability_expires_at`, `state` do pedido), com preflight obrigatório para confirmar o vocabulário real de `checkout_attempt_v2.state` antes de fixar a redação; DTO aggregates-only com identifiers de 24 chars; V1 excluída por definição. ✔
5. **Fronteira no-Route coerente:** serviço server-only em `src/orders/merchant-analytics.ts` no padrão `order-v2-view.ts`, consumo direto pelo server component de 8.4.2, sem rota, rate-limit ou request-log — consistente com o contrato de `src/data-directory/` (que exclui projeções analíticas concretas) e com os padrões de páginas atuais. ✔
6. **Riscos cobertos:** suficiência de índice (preflight + apenas migration aditiva de índice via `migration.safe.json`, além disso aborta); precisão monetária (BigInt, nunca `Number`); isolamento de owner (db:test multi-owner prova); divergência de semântica de abandono/conversão (funil só sobre campos persistidos + verificação full no 005 critical). Condições de aborto objetivas. ✔
7. **Execução segura:** F01→F02 sequencial, write sets disjuntos (spec/DOX vs módulo novo/testes), F02 trata definição ausente/incompatível como `BLOCKED` sem improviso. Sem item `(user)`. Critério de re-run presente (2, 3, 4). ✔
8. **Specs proporcionais:** seção normativa nova em `checkout-and-order-lifecycle.md` (contrato durável afetado), uma linha de contrato no `AGENTS.md` raiz, ponteiro de uma linha em `catalog-and-payment-links.md`. Aprova esta rodada aprova esses rascunhos.

Sem motivos bloqueantes.

aprovado por revisor independente (yolo) — 2026-07-25
