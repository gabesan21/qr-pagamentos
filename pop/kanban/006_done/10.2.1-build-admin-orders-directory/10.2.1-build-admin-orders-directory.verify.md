# Verification — [[10.2.1-build-admin-orders-directory]]

- **Stage:** 005_verifying · **Mode:** `full` (mandatory — `critical: true`) · **Reviewer:** independent yolo critic, fresh context
- **Surface audited:** `develop...HEAD` of `task/10.2.1-build-admin-orders-directory` in `pop/worktrees/10.2.1-build-admin-orders-directory` (13 commits incl. the merge of develop picking up M-6.1 + 9.2.2)

## Verdict: APPROVE

## 1. Original request first (card What/Why)

The card asks for an administrator-only global order query and read-only table/detail UI with stable 10/20/50/100 pagination (default 50), user/profile links, payer and provider/local ID search, exact USD/FIAT and date filters, and opaque failures, replacing the operational gap of the fixed recent-50 list. **Met:**

- `src/orders/order-v2-admin-directory.ts` registers `admin-order-v2` on the delivered foundation (`queryAdministratorDirectory`, `ADMIN_GLOBAL` scope) with page sizes 10/20/50/100 and **default 50** (`ADMIN_ORDER_V2_DIRECTORY_PAGE_SIZE_POLICY`), the `(createdAt desc, id desc)` keyset, closed `source`/`money` enums, exact-calendar-day `from`/`to` (zero-I/O invalid), exact `link` identifier, and `q` (local/provider order-UUID equality, otherwise payer name/email/cpf containment).
- `/admin/orders` composes the V2 global directory above the byte-frozen V1 ledger; owner cells link to `/admin/accounts` (interim, spec-recorded) with a localized non-color `deletedAt` badge; `/admin/orders/v2/[id]` is a read-only detail (`getForAdmin` first, then one bounded owner-attribution read) with the one opaque unavailable outcome and no comment/outcome surface.
- Opaque failures verified: canonical `307` resets, zero-I/O invalid-query, and the single unavailable detail outcome (missing, malformed, and attribution-miss all collapse).

## 2. Ownership / diff audit

`git diff --stat develop...HEAD` touches only sanctioned paths: the two new `src/orders/order-v2-admin-directory.*` files, `src/app/admin/orders/**` (page, directory-query/copy, `v2/[id]`, tests), the two orders-directory dictionaries, the three evidence scripts + `tests/admin-orders.evidence.spec.ts`, the `package.json` pair, `artifacts/admin-orders/**`, root `AGENTS.md` (one bullet amended), the three spec files, and the card/telemetry. **No frozen file moved:** `order-view.ts`, `order-views.tsx`, `order-v2-directory.ts`, `order-v2-view.ts`, `order-v2.ts`, `order-engagement-v2.ts`, `admin-analytics.ts`, `src/data-directory/**`, and `src/app/admin/orders/[id]/*` are all absent from the diff. No conflict markers anywhere (`git grep '^(<<<<<<<|=======|>>>>>>>)'` clean).

## 3. Contract invariants (root AGENTS.md + administrative-foundation spec)

- **Exact role/status re-authorization before any I/O:** `queryAdminOrderV2Directory` runs `requireAdminFromCookie()` first (`order-v2-admin-directory.ts:335`); the foundation re-checks `role === "ADMIN" && status === "ACTIVE"` before cursor validation and reads (`directory-page.ts:167`); `readOwnerAttribution` re-checks the same before its single read (`order-v2-admin-directory.ts:282-285`). Tests prove denial to `USER` and `DISABLED` admins with zero store calls (`order-v2-admin-directory.test.ts:322-332`).
- **Read-only:** no mutation route, form, or POST anywhere in the diff; the detail test asserts no `<form` and no comment body render.
- **Redaction:** rows are the delivered `toOrderV2Summary` plus exactly `{ username, deletedAt }` (whitelist `select` at `order-v2-admin-directory.ts:305-308`); the key-set assertion (`order-v2-admin-directory.test.ts:119-144`) and the detail markup absence assertions (no pair/product UUIDs, no `lifecycleVersion`) pin it. Provider order UUIDs are match-only inside `q` (`order-v2-admin-directory.ts:194-198`). The payer tuple is the policy-exact `CustomerSnapshotV1` already on the summary — the sanctioned administrator exposure, spec-amended (append-only) in `administrative-foundation.md`.
- **Cursor binding:** `ADMIN_GLOBAL`-purpose rejection tests for a `MERCHANT_OWN` cursor and a foreign-directory cursor, both without I/O (`order-v2-admin-directory.test.ts:278-320`).
- **Bounded keyset reads:** `take = pageSize + 1` asserted (51 at default 50), keyset seek in both directions, no offset/count (`order-v2-admin-directory.test.ts:101-184`). Global payer-text `contains` without a new index is the plan's pinned accepted scale debt (decision 6) — noted, accepted.
- **Soft-deleted owners:** never filtered (no `deletedAt` predicate anywhere in the where builder); evidence drives the real `POST /admin/users/[id]/delete` (`/admin?success=deleted`, assertion `soft-delete: deleted`) and then proves the deleted owner's rows stay listed with the badge in both locales and on the detail.
- **V1 tables:** the new service reads only `orderV2`, `supportedExchangeCurrency`, and the `providerOrders`/`paymentLink` relations; the V1 ledger below is the pre-existing frozen `listForAdmin` composition.
- **Currency-pair UUIDs/verifiers/provider data:** never rendered (markup absence assertions) — the summary DTO's pair UUIDs ride server-side only for the money classification, exactly as the delivered owner directory.

## 4. Merge quality (develop merge, 3 additive conflicts)

- `scripts/run-admin-evidence.mjs`: the `--orders`, `--merchant-dashboard`, `--standalone-payment`, and new `--admin-orders` modes all coexist; the new env pair is additive.
- `package.json`: both the pre-existing pairs and the new `admin-orders:evidence`/`:verify` pair kept.
- `pop/specs/administrative-foundation.md`: the `Updated` line chains 10.2.1 → 10.1.1 → 10.3.1 with no rewording, and the Implemented slices keep the 10.1.1 and 10.3.1 entries alongside the new 10.2.1 entry. Nothing lost from either side.

## 5. Gates re-run by this critic (worktree, explicit exit codes)

| Gate | Result |
|------|--------|
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 (0 errors, 47 warnings — the pre-existing warning pattern; 3 fall in the new test file, see nits) |
| `pnpm test` | exit 0 — 171 files passed / 7 skipped, 1429 tests passed |
| `pnpm build` | exit 0 |
| `pnpm db:test` | exit 0 — 15/15 disposable-Postgres suites PASS |
| `pnpm admin-orders:evidence:verify` | exit 0 against run `20260727030000` (48 captures hash-bound, assertions + source inventory verified) |

## 6. ui-review lens

Six themes × two locales × 375/768/1440 grid plus 12 state captures; every capture carries severe-axe-clean, no-overflow, ≥44px targets, and focus-indicator assertions (enforced by `verify-admin-orders-evidence.mjs:63-66`). Spot-checked captures: the ready directory (pt-BR, deleted-owner badge rows), the read-only detail with owner attribution card, and the opaque invalid-query state. The deleted badge is a non-color outline badge; the owner cell link is a `data-ds-hit-target` ghost button (commit `dcbfc3f` fixed the 44px target); bilingual parity holds (8 new keys per locale); pagination distinctness proven through per-order detail links (assertion `pagination: firstPage 10, secondPage 7, distinct true`). The evidence honestly records that the `error` state has no runtime capture (unit/page tests only) — an accepted, documented limitation.

## Findings

No blocking findings. Nits (non-blocking):

1. **nit** — `src/orders/order-v2-admin-directory.test.ts:15,51-52`: unused `OrderV2Summary` import and unused `_input`/`_orderId` mock parameters (3 of the 47 lint warnings). Cosmetic; lint exits 0.
2. **nit** — `src/i18n/dictionaries/orders-directory/pt-BR.ts:67`: the badge "Excluída" reads feminine while the column header is "Proprietário" (masculine); defensible as agreeing with "conta", and it is the string the run-bound evidence contract pins. Cosmetic.
3. **nit (pre-existing, not this task)** — in the invalid-query state the toolbar omits `pageSize`/`pageSizes`, so the foundation defaults render a `25`/`[25,50,100]` select outside this directory's registered set (`data-directory.tsx:100-101`). The merchant 8.3.3 page has the identical pattern (`(merchant)/orders/page.tsx:79-93`); it is foundation-level cosmetic debt in a dead-end recovery state whose primary action is Reset, not a regression introduced here.

verificado por revisor independente (yolo) — 2026-07-27
