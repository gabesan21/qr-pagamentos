# Approval — [[5.3.2-public-endpoint-rate-limiting]]

- **Stage:** 003_human_approval · **Owner:** independent yolo critic

## Round 1 — 2026-07-21

### Brief for decision

- **Delivery:** bounded process-local token buckets on the final public checkout and storefront request inventory, returning empty `Cache-Control: no-store` `429` before work.
- **Trust boundary:** fixed surface plus hashed canonical proxy-overwritten client address; malformed or untrusted forwarding uses one anonymous key, with bounded capacity and no diagnostics.
- **Risk/gate:** pages must be able to emit a true 429 without forbidden middleware/proxy; otherwise the task blocks rather than weakening the contract.
- **Dependencies:** 5.2.1 must close before 004 fixes the storefront route inventory.
- **Verification:** source/static review only; runtime tests/build/database checks are skipped by explicit user command.

### Human response

_(write here: approved, or requested changes)_

- [ ] Done

### Yolo critic response

### Response from independent reviewer (yolo)

- [ ] Returned to 002 — the required page-level boundary is presently impossible under the stated constraints: both `/pay/[identifier]` and the pending `/store/[slug]` are App Router Server Component pages, which can render, redirect, or `notFound`, but do not own a `Response` capable of emitting an empty no-store `429` before cookies/projection.
- Recording a future preflight that will discover this conflict is not a deliverable plan; provide a viable approved architecture for exact page `429` without the forbidden middleware/proxy, or record the scope/spec decision that resolves the contradiction before resubmission.
- The API limiter design is otherwise bounded (hard entry cap plus deny-on-full), route-scoped, and conditionally safe only where deployment demonstrably overwrites `X-Forwarded-For`; request input alone cannot establish that trust boundary.

Returned by independent reviewer (yolo) — 2026-07-21

## Round 2 — 2026-07-21

### Brief for decision

- **Revision:** the deliverable is restricted to the three public payment-link API endpoints, the only current public surfaces able to return an opaque HTTP `429` before parsing/service work without forbidden middleware or proxy.
- **Explicit exclusion:** `/pay/[identifier]` and `/store/[slug]` are App Router document pages and are excluded; no fake 200/404 throttle state, middleware, or proxy is introduced.
- **Security contract:** the bounded token-bucket, trusted proxy-overwritten address hash/anonymous fallback and no-store empty 429 remain unchanged.

### Yolo critic response

### Resposta do revisor (yolo)

- [x] Feito — approved: the closed three-handler API inventory is the reachable public HTTP surface; excluding `/pay` and `/store` preserves their Server Component contract without weakening an implementable 429 boundary.
- [x] The plan retains the bounded single-instance token bucket, proxy-overwrite-only address trust, anonymous fail-closed fallback, and empty no-store 429 contract.

aprovado por revisor independente (yolo) — 2026-07-21
