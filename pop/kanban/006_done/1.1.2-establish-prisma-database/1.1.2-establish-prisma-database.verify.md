# Verification — [[1.1.2-establish-prisma-database]]

- **Stage:** 005_verifying · **Owner:** independent agent
- **Round:** 1 · **Date:** 2026-07-14
- **Verifier profile:** delegated Codex agent; requested `gpt-5.6-terra medium` was not selectable in this interface.
- **Verified commit:** `7c0601ef0ca8b8c52af783fa2efa1030f668c8da`
- **Platform:** Node 24.18.0, pnpm 11.13.0, Docker Engine 29.6.1 on linux/amd64, `postgres:18.4-bookworm` reporting PostgreSQL 18.4.

## Acceptance criteria

| # | Criterion | Run executed | Result | Evidence |
|---|-----------|--------------|--------|----------|
| 1 | Exact Prisma/pg dependencies, config, explicit generation, and lockfile work on the inherited platform. | `pnpm install --frozen-lockfile && pnpm db:generate && pnpm typecheck` | ✅ passed | Exit 0 under Node 24.18.0/pnpm 11.13.0 with both database URL variables unset; Prisma 7.8.0 generated to `./src/generated/prisma`; typecheck passed. An additional `pnpm install --frozen-lockfile --strict-peer-dependencies` also exited 0. |
| 2 | Migrator connectivity, fresh PostgreSQL 18.4 replay, and second no-op deploy. | `pnpm db:test` | ✅ passed | Two independent runs exited 0 and each printed `PASS migrator-connect`, `PASS migration-replay`, and `PASS migration-idempotence`. The harness supplies only `MIGRATION_DATABASE_URL` to both Prisma deploys and asserts one committed migration plus the recorded SHA-256 checksum. |
| 3 | The sole application model is the constrained foundation fixture. | `pnpm db:test` | ✅ passed | Both runs printed `PASS foundation-schema` and `PASS constraint-sqlstates`; the harness asserts SQLSTATE `23502`, named `23505`, and both named `23514` constraints. Static review found exactly one Prisma model and one versioned baseline migration. |
| 4 | Runtime Prisma CRUD and identity-sequence use work. | `pnpm db:test` | ✅ passed | Both runs printed `PASS runtime-crud`; the Prisma probe creates, reads, updates, and deletes through an environment containing only `DATABASE_URL`, and the created identifier is a bigint. |
| 5 | Migration/runtime identities are distinct and runtime has no ownership, DDL, temporary-object, metadata, role, membership, or excess ACL capability. | `pnpm db:test` | ✅ passed | Both runs printed `PASS role-separation` and `PASS runtime-denials`; catalog assertions and denied operations require SQLSTATE `42501`. Static review confirms admin bootstrap creates separate `NOINHERIT`, non-superuser, non-role-admin logins and migrator-scoped defaults. |
| 6 | No credential is committed and `.env.example` is exactly two non-usable placeholders. | `! git grep -nE '(postgres(ql)?://[^[:space:]]+:[^@[:space:]]+@|BEGIN (RSA|OPENSSH) PRIVATE KEY)' -- ':!pnpm-lock.yaml' && cmp -s .env.example <(printf '%s\n' 'MIGRATION_DATABASE_URL=<postgresql-migrator-url>' 'DATABASE_URL=<postgresql-runtime-url>') && git diff --check` | ✅ passed | Exit 0. Byte inspection confirms exactly two newline-terminated lines. A second scan of the full worktree and the complete task diff found no usable PostgreSQL credential or private-key marker; password/secret hits in the diff are prohibitions or checker assertions only. |
| 7 | Existing quality gates pass and liveness stays database-independent. | `pnpm check && git diff --exit-code origin/develop -- src/app/api/health/route.ts src/app/api/health/route.test.ts` | ✅ passed | Exit 0: typecheck and lint passed; Vitest reported 2 files passed/1 skipped and 4 tests passed/1 skipped; Next.js 16.2.10 production build passed. Health files have no diff from `origin/develop`; the route still returns only `{ "status": "ok" }`. |
| 8 | Database paths, commands, ownership, generated-code handling, and readiness deferral are documented in DOX/README without a spurious skill. | `pnpm db:contract-check && python3 scripts/pop_validate.py` | ✅ passed | Exit 0 with `PASS documentation-contract` and `Vault válido — nenhuma violação encontrada.` Root `AGENTS.md` routes Prisma edits to the 27-line `prisma/AGENTS.md`; every other changed path is governed by the root contract, and no project skill was added. |

## Repeated database run evidence

Both run 1 and run 2 printed the complete stable set:

```text
PASS migrator-connect
PASS migration-replay
PASS migration-idempotence
PASS foundation-schema
PASS runtime-crud
PASS constraint-sqlstates
PASS role-separation
PASS runtime-denials
```

Docker inspection before testing, between the two runs, and after the second run found no container or volume matching `qr-pagamentos-db-test-`. A separate image check reported `postgres (PostgreSQL) 18.4 (Debian 18.4-1.pgdg12+1)`.

## Independent repository audit

- `HEAD` exactly matches the expected commit and branch `task/1.1.2-establish-prisma-database`; `origin/develop` is `8cdc0678d18de133655447d405967f4c1e5fcec0` and is the direct parent/merge base.
- Migration history is introduced only by the task commit: one directory, `20260714000000_foundation_baseline`, plus `migration_lock.toml`. The migration SHA-256 is `60cc00c150a32f27997b9fe6ad00ce618609df267e30727949caa6c8e138ff81`; the lock-file SHA-256 is `99836963713b4f5b269ad49af0ed3d7b0b2e336115c2f92dc9ac683d139d0900`. No prior tracked migration was edited.
- Generated Prisma output is ignored by `.gitignore` and no path below `src/generated/prisma/` is tracked.
- Changed-path DOX review covered all 17 changed files: repository-root rules apply throughout, with the additional `prisma/AGENTS.md` contract applying to schema, bootstrap, and migration paths. Root and child indexes, commands, ownership boundaries, generated-code policy, immutable migrations, and readiness deferral are current.
- Final `git status --short` and `git diff --check` produced no output. Final Docker container and volume filters produced no output.

**Verdict:** approved → 006_done. All eight criteria passed at the expected commit; no failure or leftover resource was found, and return to `004_processing` is not required.
