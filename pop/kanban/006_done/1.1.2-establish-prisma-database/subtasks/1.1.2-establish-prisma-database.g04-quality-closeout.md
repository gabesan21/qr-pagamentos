# Group g04 — Quality and closeout — [[1.1.2-establish-prisma-database]]

- **Order:** 4.
- **Depends on:** g03.
- **Description:** Run every acceptance pair, preserve the health boundary, and close DOX/documentation drift.

## Movements

- [x] (agent) Add `db:contract-check` to statically assert exact package pins, two environment-variable consumers, exact two-line non-usable `.env.example`, explicit `CONNECT` grants for both roles, ignored generated output, immutable migration directory/name, fixture-only schema, README commands/boundaries, and root/child DOX routing.
  - Expected: it exits 0 with `PASS documentation-contract`; any extra example assignment/credential or mutation/removal of a required contract makes it fail nonzero.
  - If it fails: assertion is platform- or formatting-fragile → inspect semantic JSON/filesystem/AST-like structure rather than broad snapshot text; never weaken a substantive rule.
- [x] (agent) Run acceptance criteria 1–8 exactly as written in the plan, including `pnpm check`, `pnpm db:test`, secret scan, health diff, diff check, and `pop_validate`.
  - Expected: every command has the listed observable pass; the database harness leaves no container/volume and repeated execution is green.
  - If it fails: classify package/config, migration, privilege, constraint, quality, documentation, or harness cleanup; return to the owning movement and rerun the full table after repair.
- [x] (agent) Re-walk every changed path against root and `prisma/AGENTS.md`; refresh root/child indexes and README only where purpose, structure, inputs, outputs, ownership, commands, or quality rules changed.
  - Expected: no stale path/command, no generated files, no secret, no unrelated health/app behavior, and no unnecessary child contract/skill remains.
  - If it fails: missing local understanding → complete the nearest applicable contract within its line/reference caps before requesting verification.
- [x] (agent) Record execution notes and any approved fork/divergence in card/subtasks; if behavior differs materially from the approved plan/spec, stop and report return-to-002 rather than silently editing the spec.
  - Expected: implementation matches the approved plan; the administrative-foundation spec is unchanged in 004 and is queued for an implemented-slice update in 006.
  - If it fails: scope/spec divergence → do not mark subtasks complete; preserve evidence and ask the orchestrator to return the task to planning.

## Execution notes

- 2026-07-14 — Planning proposes no spec wording change; 006 must append the delivered slice and keep status `approved` until the remaining phase requirements land.
- 2026-07-14 — Acceptance criteria 1–8 passed under Node 24.18.0 and pnpm 11.13.0; `pnpm check`, the unchanged health diff, secret/example scan, diff check, documentation contract, and PoP validation were green.
- 2026-07-14 — No plan fork or material spec divergence occurred. The administrative-foundation spec remains unchanged in 004 and still requires its 006 implemented-slice update.
