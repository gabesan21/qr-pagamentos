# Group g02 — Roles and migration — [[1.1.2-establish-prisma-database]]

- **Order:** 2.
- **Depends on:** g01.
- **Description:** Define the least-privilege PostgreSQL bootstrap and one immutable foundation migration.

## Movements

- [x] (agent) Add an idempotent admin-only role/bootstrap SQL file for database `qr_pagamentos`: revoke `PUBLIC` database `CONNECT,TEMPORARY` and public-schema `CREATE`; create/alter `qr_migrator` and `qr_runtime` with `LOGIN NOINHERIT NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS`; explicitly grant database `CONNECT` to both roles; create schema `app` owned by migrator; grant runtime schema `USAGE`.
  - Expected: migrator can connect without owning the database; the file contains no password; runtime is neither owner nor member of migrator; rerunning bootstrap is safe; comments state that external operators assign login secrets.
  - If it fails: idempotent role alteration needs privileges unavailable to the admin bootstrap → keep it explicitly cluster-admin-only; any application/migration command needs role administration → abort as a boundary violation.
- [x] (agent) In bootstrap, set current-object and `ALTER DEFAULT PRIVILEGES FOR ROLE qr_migrator IN SCHEMA app` grants: application tables get only `SELECT,INSERT,UPDATE,DELETE`; sequences get only `USAGE`; the baseline migration revokes all runtime access to `_prisma_migrations`; runtime receives no `TRUNCATE,REFERENCES,TRIGGER,MAINTAIN,CREATE,TEMPORARY` or ownership.
  - Expected: grants cover the fixture table and its identity sequence, exclude migration metadata, and preserve DDL denial.
  - If it fails: an actual required Prisma object lacks access → use the plan's demonstrated-object fork and add its exact test; do not grant `ALL`.
- [x] (agent) Define only `DatabaseFoundationFixture` mapped to `_database_foundation_fixture` with `id BigInt` identity PK, `key varchar(64)` unique/not-null/nonblank, `quantity integer` not-null/nonnegative, and `created_at timestamptz(3)` defaulted by PostgreSQL.
  - Expected: no auth/product/order/payment/seed model or relation appears; names and native types match the plan exactly.
  - If it fails: Prisma cannot express a check → retain the field model and add the named check in reviewed custom SQL; do not replace database enforcement with application validation.
- [x] (agent) Generate `20260714000000_foundation_baseline` with `prisma migrate dev --create-only`, review/edit its SQL before application, and commit schema plus complete migration history; name unique and check constraints deterministically.
  - Expected: SQL creates only Prisma metadata plus the fixture/identity sequence and constraints; no migration has been applied before final review; no `db push` is used.
  - If it fails: migration was applied before review or history drifted → discard only the disposable local database and regenerate; never edit a migration once applied outside that disposable run.

## Execution notes

- 2026-07-14 — The fixture is infrastructure proof only and has no user-facing semantics.
- 2026-07-14 — Generated create-only SQL in a disposable PostgreSQL 18.4 database, renamed it to the approved timestamp, reviewed it before application, and removed the generation container.
- 2026-07-14 — Fresh replay established that Prisma 7 stores `_prisma_migrations` in schema `app`; the reviewed migration explicitly revokes runtime access there. Bootstrap, not the migrator, creates schema `app`, preserving the migrator's no-`CREATEDB` boundary.
