# Group g01 — Config and contracts — [[1.1.2-establish-prisma-database]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Establish exact dependency, connection, generation, documentation, and DOX contracts without contacting a database.

## Movements

- [x] (agent) In the task worktree, confirm dependency 1.1.1 and the root DOX path, then run `docker version && docker run --rm postgres:18.4-bookworm postgres --version`.
  - Expected: the worktree is based on `develop` containing commit `8cdc0678d18de133655447d405967f4c1e5fcec0`; Docker is reachable; PostgreSQL reports 18.4.
  - If it fails: wrong base → stop for orchestration repair; unsupported image variant → use the plan's 18.4 Debian variant fork; no Docker daemon → abort and mark blocked.
- [x] (agent) Add exact `prisma`, `@prisma/client`, `@prisma/adapter-pg` 7.8.0, `pg` 8.22.0, and `@types/pg` 8.20.0 packages; add `db:generate`, `db:test`, and `db:contract-check` scripts and refresh `pnpm-lock.yaml` with pnpm 11.13.0.
  - Expected: package and lock contain exact versions; existing Node/pnpm/Next/React/TypeScript pins and independent scripts remain unchanged.
  - If it fails: peer/engine conflict → capture the resolver output and abort to 002; never relax inherited pins or use ranges/dev releases.
- [x] (agent) Add `prisma.config.ts`, `prisma/schema.prisma`, ignored `src/generated/prisma/`, and lazy singleton `src/db/client.ts`: CLI reads only `MIGRATION_DATABASE_URL`, runtime reads only `DATABASE_URL`, schema is fixed to `app`, and generation requires no live database.
  - Expected: `pnpm db:generate` emits the v7 client at the declared path and `pnpm typecheck` succeeds without either URL set.
  - If it fails: config eagerly validates a URL during generation → use a syntactically valid unreachable non-secret sentinel only for generation, while migration/runtime entrypoints fail closed on missing real variables; generated import mismatch → correct output/import paths without committing generated code.
- [x] (agent) Add `.env.example` containing exactly `MIGRATION_DATABASE_URL=<postgresql-migrator-url>` and `DATABASE_URL=<postgresql-runtime-url>`, README database commands/boundaries, and a 20–30-line `prisma/AGENTS.md`; update root `AGENTS.md` with the child index, database commands, immutable-migration and generated-code routing.
  - Expected: the entire example file is only those two non-usable placeholders; root plus child contracts fully govern every planned edit; no project skill is invented and card Skills stays `none yet`.
  - If it fails: child contract exceeds ~60 lines or accumulates >7 references → keep broad rules at root and only migration-local decisions in the child.

## Execution notes

- 2026-07-14 — Planned from `origin/develop`; no implementation performed in 002.
- 2026-07-14 — Confirmed base `8cdc0678d18de133655447d405967f4c1e5fcec0`; Docker 29.6.1 ran `postgres:18.4-bookworm` as PostgreSQL 18.4 on linux/amd64, so no image fork was used.
- 2026-07-14 — Exact pins resolved with pnpm 11.13.0; generation and typecheck passed under Node 24.18.0 without a database URL.
