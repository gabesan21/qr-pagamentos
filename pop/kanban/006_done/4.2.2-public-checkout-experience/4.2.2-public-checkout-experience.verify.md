# Verification — [[4.2.2-public-checkout-experience]]

- **Reviewed commit:** `7576306` (`test: cover hidden polling timer cleanup`)
- **Reviewer:** independent yolo critic, fresh 005 round 4
- **Date:** 2026-07-21
- **Decision:** approved. This remains a critical task.

## Contract and source review

No blockers found.

- **B4 — deterministic pending-timer pause/resume proof:** [`src/app/pay/[identifier]/public-checkout-form.test.tsx:111`](../../../../../../src/app/pay/[identifier]/public-checkout-form.test.tsx:111) resolves the second non-terminal `PENDING` poll and proves one 5-second repeat timer is pending. It then hides the document, proves the timer count is zero, advances five seconds with the fetch count still at two, and restores visibility to produce exactly one third fetch. Removing `clearTimer()` would make this test fail.
- **B1/B2/B3 preserved:** `7576306` changes only the seven B4 assertions in the polling test relative to `fa1b4b5`; the persisted/default locale behavior and prior active-request abort, stale-response discard, duplicate-visible/no-overlap, resume, and unmount coverage are unchanged. The controller's generation, visibility, timer, abort, and single-flight guards remain consistent with AC3.
- The reviewed redaction/status boundary, policy-shaped form data, live-memory capability handling, dictionary-backed feedback, shared primitive use, and documented checkout/DOX contracts remain aligned with the linked specifications. No ownership drift was found.

## Verification evidence

| Check | Result |
| --- | --- |
| Source/spec/DOX/DESIGN/full-diff review | Completed against the card, plan, linked lifecycle/catalog specs, applicable contracts, and `64f9dc0..7576306`. |
| `node scripts/check-design-tokens.mjs` | Passed in a detached candidate worktree at `7576306`. |
| `git diff --check 64f9dc0..7576306`; `git show --check 7576306` | Passed. |
| Focused Vitest test, ESLint, TypeScript, aggregate check/build, browser/screenshot/a11y runtime, DB/container/install checks | Not run by explicit user constraint: static/source checks only, with no DB, installation, or broad tests. The candidate worktree has no installed dependencies. This is an evidence limit, not a pass. |

Verified by independent reviewer (yolo) — 2026-07-21.
