# Verification and critique — 3.2.1-product-schema-and-admin-crud

- **Stage:** 005_verifying
- **Reviewer:** independent reviewer (yolo)
- **Reviewed:** 2026-07-20

## Round 1 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Verification performed | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| AC1 | Product persistence and database contract | re-run, partial | `pnpm db:generate` (via `pnpm check`), `pnpm db:contract-check`, static review of model/migration | partial | Schema/migration define UUID, trimmed text, canonical 18/6 price, active/version and runtime DML. `pnpm db:test` was not run: the user authorized skipping Docker/database runtime tests. |
| AC2 | Active-admin product service, validation and CAS | re-run | `pnpm exec vitest run src/auth/product.test.ts` | passed | 7 focused tests passed; covers authorization, Unicode code-point validation at the service boundary, decimal grammar, and stale/unknown CAS outcomes. |
| AC3 | Protected product POST contract | re-run | `pnpm exec vitest run src/app/admin/products/route.test.ts src/app/admin/page.test.tsx` | passed | 9 focused tests passed; source review confirms cookie-principal reauthorization precedes parsing and routes map protected results to empty 401/403 and other failures to opaque relative redirects. |
| AC4a | Owned Textarea inventory | evidence, static review | source review; prior `artifacts/design-system/20260720235719/review.md` | partial | The official owned `Textarea`, specimen, semantic focus styling and documented state matrix are present. The prior run is useful historical evidence only; design-system runtime evidence was not rerun by user authorization. |
| AC4b | Accessible localized admin CRUD surface | re-run, source/semantic review | `pnpm admin:source-check`; focused product UI tests; source review | failed | Source gate passed with zero raw controls/adapters/styles/variants and the UI has labels, native POST forms, pending state, opaque recovery, line-break presentation, focus styling, and delete confirmation. Its UTF-16 `maxLength` attributes contradict the required Unicode-code-point bounds; see blocker B1. Playwright/admin evidence and screenshot/vision loop were explicitly skipped by user authorization. |
| AC4c | Disposable admin-evidence secret lifecycle | review | static review of `scripts/run-admin-evidence.mjs` | partial | The runner creates a 32-byte base64url key, writes it only to the 0700 temporary staging tree with 0400 permissions, redacts it on app-log failure, and removes the tree in `finally`. Compose/Playwright lifecycle was not rerun by user authorization. |
| AC5 | Parity, static checks, tests, build, scope | re-run | `pnpm check`; `git diff --check develop...HEAD`; source/diff review | passed, except B1 | Aggregate gate passed: typecheck, lint, 569 tests (4 skipped), and production build. Focused run added 28 passing tests. Diff check is clean; changed paths conform to declared fronts. Spec closeout remains for 006 after the implementation is corrected. |

### UI semantic review

Nielsen quick pass: visibility/feedback, user control (explicit delete disclosure), consistency, error recovery, recognition, and minimalist hierarchy pass by source inspection. The limitation is not cosmetic: the visible form prevents valid Unicode input.

WCAG semantic pass by source inspection: labels bind to fields, the live notice communicates success/error, native `details/summary` has a visible focus rule, and multiline fields use owned labelled controls. Automated runtime axe and the 320/768/1440 screenshot loop were intentionally not rerun; this report makes no visual-runtime claim.

### Implementation quality and findings

| Severity | Finding | Evidence | Required correction |
| --- | --- | --- |
| blocker | B1 — The browser controls enforce UTF-16 code-unit lengths, not the task's Unicode-code-point limits. A valid 128-code-point internal name of 128 emoji requires 256 UTF-16 units and cannot be entered; the same defect affects the two 160-code-point titles and two 2,000-code-point descriptions. The service and database correctly accept the values, so the admin CRUD UI is stricter than its contract. | `src/app/admin/product-management.tsx:56-60`; the service explicitly uses `[...trimmed].length` at `src/auth/product.ts:72`; HTML `maxlength` is measured in UTF-16 code units. | Remove these `maxLength` attributes and retain server validation, or replace them with client-side validation that counts code points. Add a UI-level regression test using supplementary-plane characters at each permitted bound. |

### Scope and integration

- [x] Changed paths respect the declared front ownership; no opportunistic dependency repair found.
- [x] No public product/checkout/payment-link behavior was introduced.
- [x] `pnpm check`, `pnpm db:contract-check`, `pnpm admin:source-check`, and diff check passed.
- [ ] Catalog-spec closeout is pending 006 and must follow the corrected implementation.

### Round 1 verdict

- **Decision:** rejected → 004_processing.
- **Blockers:** B1 — UI text limits use UTF-16 units rather than the required Unicode code points.
- **Suggestions/nits:** none.
- **Runtime limitation:** Per explicit user authorization, this review did not run Docker, `pnpm db:test`, Compose/admin Playwright evidence, design-system evidence, or the screenshot/vision loop. Prior design-system evidence was audited as historical evidence only.
- **Summary:** Persistence, authorization, opaque mutation behavior, exact-decimal handling, localized presentation, and static gates are sound. The admin UI cannot represent all values accepted by the specified code-point contract, so the CRUD objective is not yet fully met.

Verified by independent reviewer (yolo) — 2026-07-20

## Round 2 — 2026-07-20

### Objective and specification conformance

| ID | Criterion | Mode | Verification performed | Result | Evidence |
| --- | --- | --- | --- | --- | --- |
| AC1 | Product persistence and database contract | re-run, partial | Reviewed `schema.prisma` and `20260721010000_products/migration.sql`; reran `pnpm db:generate` through `pnpm check` and `pnpm db:contract-check`. | partial | The UUID, text/price, active/version, named constraint, and least-privilege DML contract remains present; the static contract check passed. `pnpm db:test` was deliberately not run under the user's runtime/Docker exclusion. |
| AC2 | Active-admin product service, validation and CAS | re-run | `pnpm exec vitest run src/auth/product.test.ts` (within the 30-test focused suite). | passed | Service tests cover active-admin authorization, trimmed Unicode-code-point boundaries, canonical 18/6 decimals without `Number`, malformed values, and stale/unknown CAS rejection. |
| AC3 | Protected product POST contract | re-run | `pnpm exec vitest run src/app/admin/products/route.test.ts src/app/admin/page.test.tsx` (within the focused suite). | passed | The route re-authorizes before parsing form input, returns only empty protected outcomes through the shared guard, and maps all other failures to opaque relative redirects. Page tests cover authorized product reads and localized opaque notices. |
| AC4a | Owned Textarea inventory | static review, partial | Reviewed the owned primitive, `DESIGN.md`, specimen/evidence tests, and source-inventory gate; reran `pnpm admin:source-check`. | partial | The Textarea remains owned, labelled, server-renderable, and documented with required states; source gate passed with zero raw controls/adapters/styles/variants. Design-system browser evidence was not rerun by authorization. |
| AC4b | Accessible localized admin CRUD surface | re-run, static/semantic review | Reran `pnpm exec vitest run src/app/admin/product-management.test.tsx` and `pnpm admin:source-check`; inspected rendered component source and the B1 correction. | passed, within stated runtime limitation | The correction removes every product-form `maxLength` attribute. The five parameterized UI tests render 128/160/2,000 supplementary-plane-character values, assert the code-point boundary, preserve the value in markup, and assert no `maxlength` attribute exists. A repository search of admin and owned UI sources finds `maxlength` only in that negative assertion, so HTML's UTF-16-unit `maxlength` algorithm cannot reject valid code-point-boundary product values. Labels, owned Textareas, native POST forms, pending feedback, opaque recovery, line-break rendering, and delete confirmation remain present. |
| AC4c | Disposable admin-evidence secret lifecycle | static review, partial | Reviewed `scripts/run-admin-evidence.mjs`. | partial | It generates a fresh 32-byte base64url value, writes it only below the temporary staging tree at `0400`, includes it in failure-log redaction, and removes the whole temporary tree in `finally`. Compose and Playwright lifecycle were not run by authorization. |
| AC5 | Parity, static checks, tests, build, and scope | re-run | Reran `pnpm check`, `pnpm db:contract-check`, `pnpm admin:source-check`, and `git diff --check develop`. | passed | `pnpm check` passed: Prisma generation, typecheck, lint, 578 passing tests (4 skipped), and production build. The static database contract and source gate passed; diff check is clean. Changed paths fit the approved persistence/server/UI/evidence fronts; catalog-spec closeout remains the 006 responsibility. |

### UI and implementation review

- Nielsen quick pass by source inspection: feedback/pending state, explicit destructive confirmation, localized recovery, consistent owned controls, and the empty/populated states are present. No severity 3–4 issue found.
- WCAG semantic pass by source inspection: visible labels bind to native fields, `aria-busy` is exposed during submit, descriptions use owned labelled `Textarea` controls, and the inherited focus/token contract remains in use. No automated axe or visual-runtime claim is made.
- **B1 resolved:** native `maxLength` was the sole UTF-16 code-unit constraint. Its removal is sufficient to prevent browser rejection at valid Unicode code-point boundaries, while the server continues to enforce the specified Unicode-code-point limits. The focused regression test protects both facts.
- No blocking clean-code, scope, authorization, decimal-exactness, DOX, or documentation inconsistency was found. No suggestions or nits.

## Final verdict

- **Decision:** approved.
- **Blockers:** none.
- **Runtime limitation:** Per explicit user authorization, this round did not run Docker, `pnpm db:test`, Compose/admin Playwright evidence, design-system evidence, or the 320/768/1440 screenshot/vision loop. AC1, AC4a, and AC4c therefore remain partial runtime evidence only; this approval is based on the authorized static/unit/aggregate gate scope.
- **Summary:** The admin UI now accepts values at all specified Unicode code-point boundaries without a conflicting HTML UTF-16-unit maximum, and its regression tests demonstrate the absence of the native constraint. The corrected delivery satisfies the approved product CRUD contract under the authorized verification scope.

Verified by independent reviewer (yolo) — 2026-07-20
