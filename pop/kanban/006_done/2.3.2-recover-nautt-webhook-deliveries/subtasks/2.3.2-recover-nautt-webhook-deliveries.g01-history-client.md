# Group g01 — Normalized history port — [[2.3.2-recover-nautt-webhook-deliveries]]

- **Order:** 1
- **Depends on:** none
- **Description:** Define the exact directly evidenced normalized history boundary without inventing a GET response parser.

## Movements

- [x] (agent) Walk root and Nautt DOX, then add `webhook-delivery-history-port.ts`: injected list/specific methods each accepting the caller's `AbortSignal`, with no internal retry and normalized records only.
  - Expected: the service boundary names documented endpoint roles but contains no fetch, HTTP status, envelope, cardinality wrapper, pagination, or nullability parser.
  - If it fails: implementation needs a concrete GET container → keep that decoder outside this task/port; do not borrow another endpoint's envelope.
- [x] (agent) Define/validate exactly the embedded record fields evidenced by `raw/webhook.md`: delivery/webhook/order UUIDs, closed event type, delivered/permanently-failed booleans, positive attempt number, and valid created-at. Exclude response status and delivered-at from normalized/persisted recovery evidence.
  - Expected: missing/type-invalid or contradictory requested identity fails redacted; no field outside the documented record becomes provider truth.
  - If it fails: permanent-failure handling requires an unevidenced field → stop and return to 002; do not guess its nullability.
- [x] (agent) Add focused normalized-port contract tests for list/specific fixtures, caller-supplied signal propagation, exact supported fields, validation/redaction, and source checks prohibiting GET wire assumptions.
  - Expected: `pnpm test -- src/integrations/nautt/webhook-delivery-history-port.test.ts` passes and grep assertions find no `data`/status/provider-cardinality/pagination decoder claim; the service, not this boundary, owns the fixed 10-second race.
  - If it fails: a test asserts a provider wrapper → remove the assertion and test only normalized records plus dependency call counts.

## Execution notes

- Evidence: the normalized record contains exactly delivery/webhook/order UUIDs, closed event type, delivered/permanently-failed flags, positive attempt number, and `Date` created-at. Extra `responseStatus`/`deliveredAt`, missing fields, invalid types, and unknown events fail closed. Focused port/recovery/reconciliation run: 24 tests passed.
