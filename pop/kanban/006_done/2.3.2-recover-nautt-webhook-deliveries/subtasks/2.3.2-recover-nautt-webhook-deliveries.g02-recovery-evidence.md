# Group g02 — Recovery evidence — [[2.3.2-recover-nautt-webhook-deliveries]]

- **Order:** 2
- **Depends on:** none
- **Description:** Add a fenced provider-order recovery lease and insert-only typed history evidence.

## Movements

- [x] (agent) Walk root and Prisma DOX, then add one forward migration/model for a composite owner/order-bound 30-second recovery lease; extend delivery and attempt rows with closed evidence source, recovery-only nullable digest, and only evidenced provider webhook/delivered/permanent-failure/attempt fields plus deterministic checks/indexes/ACL.
  - Expected: existing delivery/attempt rows backfill as `INTAKE`; only `RECOVERY` permits null digest and requires permanently-failed provider evidence; runtime receives only demonstrated DML/sequence use.
  - If it fails: Prisma cannot express a check/lease claim → enforce it in reviewed migration SQL plus a narrow typed store statement; never edit 2.3.1 migration or weaken FKs.
- [x] (agent) Extend the delivery store with a global known-ID classifier (`same-bound | conflicting-bound | absent`), atomic fresh/expired lease claim returning a random fence token, matching-token release/complete, and one transaction that inserts only still-unknown recovered deliveries+attempts while completing the lease.
  - Expected: known classification is callable before any lease; live concurrent claim loses before secrets/network; expired claim is reclaimable; stale token changes nothing; UUID collision preserves every prior field; batch failure rolls back all evidence.
  - If it fails: provider I/O would need an open transaction → persist lease first and close transaction; all GETs occur outside, then fenced final transaction.
- [x] (agent) Extend static/disposable/runtime DB probes for migration replay, checks, ACL, known binding classification, collision preservation, race winner, 25s-busy/30s-expiry boundaries, fencing, and atomic batch rollback.
  - Expected: `pnpm db:generate && pnpm db:contract-check && pnpm db:test` prints existing and recovery PASS markers; no transaction spans injected network holds.
  - If it fails: ACL/sequence or constraint probe fails → correct only new migration/harness before shipping; never grant DDL/ownership/migration access.

## Execution notes

- Evidence: migration `20260718050000_nautt_webhook_recovery` adds closed/consistent evidence checks and `webhook_recovery_lease`; disposable PostgreSQL prints `PASS migration-replay`, `PASS webhook-recovery-schema-lease`, `PASS role-separation`, and `PASS runtime-denials`. The runtime Prisma probe captures the complete pre-existing delivery row and ordered attempt rows as PostgreSQL JSON text, completes a recovery batch containing that collision plus a new UUID, asserts the known tuple bytes and attempt count/content are identical, and checks the new recovery delivery/attempt field matrix. It also covers 24.999s exclusion, 30s reclaim, stale-token failure, rollback, and typed evidence.
