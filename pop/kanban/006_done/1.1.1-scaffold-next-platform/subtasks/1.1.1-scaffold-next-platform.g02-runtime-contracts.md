# Group g02 — Runtime contracts — [[1.1.1-scaffold-next-platform]]

- **Order:** 2.
- **Depends on:** g01.
- **Description:** Add truthful liveness, explicit bilingual routes/dictionaries, and focused contract tests.

## Movements

- [x] (agent) Add the closed locale set `pt-BR`/`en`, server-only dictionaries with identical keys, explicit `[lang]` routes, and deterministic invalid-locale handling without automatic negotiation.
  - Expected: both locale roots render successfully, invalid locale values do not render a supported page, and dictionaries have exact key parity.
  - If it fails: generator route conflicts or async params differ → follow the stable Next.js 16 App Router API and keep locale behavior explicit; abort if a product decision is required.
- [x] (agent) Add unlocalized `GET /api/health` returning exact JSON `{"status":"ok"}`, HTTP 200, and `Cache-Control: no-store` without database/provider claims.
  - Expected: direct handler execution and the production server expose the same status, body, and caching contract.
  - If it fails: framework injects a broader cache header → set an explicit response header and assert the required `no-store` directive rather than an entire framework-dependent value.
- [x] (agent) Add Vitest tests for the health handler, exact supported locale set, dictionary parity, and deterministic locale validation; ensure `pnpm test` fails on zero tests.
  - Expected: at least one test file is collected and all contract assertions pass in Node environment.
  - If it fails: test imports require browser globals → extract pure locale/health contracts or use the plan's jsdom fork only when a rendered component test is essential.

## Execution notes

- 2026-07-13 — planned; no database/migration readiness or UI design decision is authorized in this group.
- 2026-07-13 — added statically generated `/pt-BR` and `/en` pages, deterministic `notFound()` handling for unsupported locale segments, a server-only dictionary loader, and exact dictionary-key parity.
- 2026-07-13 — added unlocalized `/api/health` liveness only; direct handler tests and the production probe confirm HTTP 200, exact JSON, and `no-store` without database, migration, or provider claims.
- 2026-07-13 — Vitest collected two files and passed four non-vacuous health/locale contract tests in the Node environment.
