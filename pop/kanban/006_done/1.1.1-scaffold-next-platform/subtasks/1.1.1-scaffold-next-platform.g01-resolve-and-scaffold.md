# Group g01 — Resolve and scaffold — [[1.1.1-scaffold-next-platform]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Resolve stable pins and curate the application skeleton without overwriting the PoP harness.

## Movements

- [x] (agent) In the task worktree, walk `AGENTS.md`, inventory all existing root paths, and record the clean pre-scaffold status in these notes.
  - Expected: the worktree is on `task/1.1.1-scaffold-next-platform`, harness paths and the `CLAUDE.md -> AGENTS.md` symlink are identified, and no application scaffold exists.
  - If it fails: wrong worktree or unexpected application files → stop before editing, reconcile the task branch/path with the orchestrator, and abort on unrelated overlap.
- [x] (agent) Resolve the exact current stable Node 24.x, pnpm, create-next-app/Next.js, React, TypeScript, ESLint, and Vitest versions; reject canary/prerelease tags and record them below.
  - Expected: every selected version is an exact stable release; Node is major 24 and Next.js is stable major 16.
  - If it fails: registry/network unavailable → retry through the approved network path; incompatible or prerelease-only resolution → trigger the matching plan abort.
- [x] (agent) Generate a strict TypeScript App Router scaffold noninteractively in a temporary directory, inspect its full file list/diff, then selectively apply application files to the worktree.
  - Expected: `package.json`, exact `packageManager`, `pnpm-lock.yaml`, Node pin, `src/app`, Next/TypeScript/ESLint config, and public assets exist while all harness files remain byte-preserved unless explicitly updated by g03.
  - If it fails: generator conflicts with root harness → discard the generated root metadata and curate only application paths per the plan fork; never overwrite `AGENTS.md` or `CLAUDE.md`.
- [x] (agent) Define separate `lint`, `typecheck`, `test`, `build`, `start`, and aggregate `check` scripts; configure pnpm's exact managed-version enforcement without Corepack; and complete a frozen install.
  - Expected: scripts invoke real tools, `pnpm --version` matches the exact `packageManager` value, the frozen install exits 0, and a second frozen install leaves pin/manifest/lock files unchanged.
  - If it fails: peer/version conflict → use the generator-compatible stable set and regenerate once; unresolved conflict → abort rather than weaken a gate.

## Execution notes

- 2026-07-13 — planned; exact resolved versions and scaffold inventory belong here during 004.
- 2026-07-13 — resumed on `task/1.1.1-scaffold-next-platform`; `CLAUDE.md -> AGENTS.md` and the root-only DOX tree were intact. The inherited worktree already contained the partial scaffold plus human-authored harness corrections; the card's explicit waiver made that complete dirty state the accepted baseline.
- 2026-07-13 — registry stable versions resolved: Node 24.18.0, pnpm 11.13.0, create-next-app/Next.js 16.2.10, React 19.2.7, TypeScript 7.0.2, ESLint 10.7.0, and Vitest 4.1.10. No concrete deployment-host constraint was found; host qualification remains deferred to 1.1.3.
- 2026-07-13 — the scaffold pins Node 24.18.0, pnpm 11.13.0, Next.js 16.2.10, React 19.2.7, Vitest 4.1.10, and exact generator-compatible TypeScript 5.9.3 / ESLint 9.39.5. Next's bundled lint parser crashes on TypeScript 7.0.2, while bundled React plugins crash and declare unmet peers on ESLint 10.7.0; both approved compatibility forks passed `pnpm peers check`.
- 2026-07-13 — pnpm managed-version enforcement reported 11.13.0 without Corepack; explicit build-script allowlisting for `sharp` and `unrs-resolver` completed two frozen installs without manifest or lockfile drift.
