# Verification — [[10.1.2-build-admin-dashboard-ui]]

- **Stage:** 005_verifying · **Gate:** yolo single quality gate (strong, independent) · **Mode:** `full`
- **Date:** 2026-07-27 · **Base:** 05dd378 · **Head:** 4f5e1f5 (3 commits: d6c6e1b, b649a2a, 4f5e1f5)
- **Mode justification:** full audit of the integrated diff plus re-run of every re-runnable gate, per the orchestrator's mandate; surface is a new administrator-facing composition over a frozen redacted projection.

## 1. Original request first (card "What / Why")

Card asks for a bilingual administrator dashboard landing (`/admin`) with KPI cards, period controls, tables, comparisons, and explicit loading/empty/error states, so administrators read platform usage and sales health at a glance.

**Delivered and verified:** `/admin` now composes, server-side and read-only, the delivered 10.1.1 global projection into ruled ledger cards (Users, Orders by source/state, split Sales, Funnel, Links & products, Top owners, Top products), a closed `today`/`7d`/`30d` plain-GET period switcher with pinned `7d` fallback, explicit empty state per section, the existing bilingual `loading.tsx` skeleton (byte-unchanged) as loading, and the existing admin error boundary for failures. The "Why" is met.

Two wording deviations from the card text, both contract-backed and non-blocking:

- **"charts"**: plan decision 1 bans charts per DESIGN.md (decorative charts prohibited); quantitative composition is ruled facts and captioned native tables, mirroring the 8.4.2 merchant precedent. Consistent with the durable design contract.
- **"recent activity"**: the admin DTO delivered by 10.1.1 carries no recent-activity member — the Administrator analytics definitions in `pop/specs/administrative-foundation.md` and the root `AGENTS.md` bullet pin the projection to counts, sales, funnel, and leaderboards, and this task's fence froze the projection byte-wise. Rendering a recent-activity feed would have required an out-of-scope projection change. Recorded as observation O1 below; the objective (usage and sales health at a glance) is met without it.

## 2. Gates re-run by this reviewer (explicit exit codes, worktree `pop/worktrees/10.1.2-build-admin-dashboard-ui`)

| Gate | Command | Exit |
|------|---------|------|
| Typecheck | `pnpm typecheck` | **0** |
| Unit/contract tests | `pnpm test` | **0** — 176 files passed / 8 skipped, 1513 tests passed / 26 skipped |
| Evidence verify | `pnpm admin-dashboard:evidence:verify` | **0** — `Verified admin-dashboard evidence 20260727044029` (45 captures hash-bound, axe/overflow/target/focus gates, review bound to manifest SHA-256) |
| Admin UI source check | `pnpm admin:source-check` | 1 — expected: failure list is the known 120-line baseline, proven byte-identical to base (see deviation D1) |
| Aggregate `pnpm check` / `db:test` / docker | not re-run | accepted on the 004 report (exit 0 each) plus the independent re-runs above and the artifact spot-check |

Visual spot-check (ui-review): `populated-pix-paper-pt-BR-1440.png` and `interaction-en-deleted-badge.png` inspected. Composition matches the 8.4.2 merchant precedent; the current period is a non-link entry distinguished by underline, strong type, and border (never color alone); sales render as two separate ruled groups (`62,65 BRL` + unlabeled pair under "Confirmadas", `10 BRL` under "Finalizadas localmente", never summed); the deleted owner keeps its leaderboard row with a localized outline `Badge` ("Excluído"/"Deleted"), aggregates unchanged; period controls carry `min-height: var(--control-min-height)` and the verify run asserts every target ≥44×44. At 375 px the leaderboard tables clip inside the owned shadcn `Table`'s `overflow-x-auto` container (`src/components/ui/table.tsx:11`) — the design system's owned narrow-width behavior, with the document-level overflow gate green on all 36 base captures. No slop, no severity ≥2 finding.

## 3. Fences

- **10.1.1 projection untouched:** `git diff --stat 05dd378..HEAD` contains no `src/orders/` file. Byte-frozen. ✓
- **No route / mutation / client data fetching:** no `route.ts`, no POST surface, no `"use client"` added under `src/app/admin/` by this task (the three pre-existing client files are untouched); the page is an async server component reading `searchParams` only. ✓
- **Shell files preserved:** `layout.tsx`, `guard.ts`, `shell-context.ts`, `error.tsx`, `loading.tsx` absent from the diff; the notice strip (`?success`/`?error`) is byte-preserved in `page.tsx`. ✓
- **Merchant modules never imported:** `src/app/admin/dashboard.tsx` imports only `next/link`, `@/components/ui/*`, `@/i18n/*`, `@/orders/*` (types). The rate and price shifters are local copies; both are **byte-identical in logic** to the merchant originals (`formatDashboardRate` at `src/app/(merchant)/dashboard.tsx:62-69`, `formatCatalogPrice` at `src/app/(merchant)/catalog/price-format.ts:5-12`) — semantics parity confirmed line by line, and `dashboard.test.tsx` pins the localized outputs. ✓
- **Rate limiter / server-request-log inventories:** untouched. ✓

## 4. Contract invariants

- `requireAdminShellContext()` (re-resolves the cookie principal, `ADMIN`-only via `requireAdminFromCookie`) runs **before** `getAdminAnalyticsService().getGlobal(principal, period)` (`src/app/admin/page.tsx:25-27`). ✓
- Period handling: unknown/hand-edited values hit the service's `invalid-period` kind and re-query the pinned `7d` without redirect; a rejected default throws to the existing error boundary (`page.tsx:13-20`). Tests assert both the pass-through and the two-step fallback. ✓
- Never-merged sales: `confirmedSales` and `locallyFinalizedSales` render as two separate ruled `<section>`s with per-pair exact-decimal lines; the page test asserts `34,90 BRL` and `10 BRL` present and the summed `44,90` absent. No `Number` conversion anywhere in the money/rate paths. ✓
- Exact-decimal rates with explicit n/a: textual four-digit shift, null → localized `adminDashboardRateUnavailable`. ✓
- Unlabeled-currency treatment: null-code pairs render `(moeda sem rótulo)` / `(unlabeled currency)` explicitly. ✓
- Redaction: rendered-HTML test asserts no UUID, email, verifier/capability/nonce/credential/provider strings; the composition renders only DTO members (owner identity exactly `{ username, deleted }`). ✓
- Bilingual parity: 56 keys × 2, key sets proven identical (`en` vs `pt-BR` slices); aggregators gained only additive import/spread lines. ✓ (Card text said 50 keys; actual is 56 — superset, parity intact.)
- Non-color markers: `aria-current="page"` on a non-link span with underline + strong + border; deleted-owner badge is text + outline, not color. ✓

## 5. Declared deviations

- **D1 — `scripts/check-admin-ui-inventory.mjs` allowedClasses whitelist:** the diff is four insert-only lines (13 class literals, including the combined `admin-dashboard__period admin-dashboard__period--current` literal exactly as used in `dashboard.tsx:44`). Failure list at HEAD (120 lines) recomputed and diffed against the failure list of the **base** script running over the **base** tree (extracted via `git archive 05dd378`): **byte-identical**. The whitelist masks nothing pre-existing and admits only the new owned classes. ✓
- **D2 — one-clause spec correction** (`/admin` no longer an honest scaffold, global payment-link area remains one): justified — it matches delivered reality and the DESIGN.md scaffold-exception amendment. ✓
- **D3 — spec append against stale 195-line base:** the append consists of the `Updated` header line, the D2 clause, and one implemented-slices bullet. Content verified correct, minimal, and worded exactly against delivered behavior (period fallback, never-merged sales, null-state label, captions, badge, no route/mutation/client boundary, 45-capture pair) — safe for the orchestrator to re-apply onto the 139-line compacted file at merge. Line count not flagged. ✓

## 6. Findings

- **O1 (sugestão)** — card wording vs delivered scope: the card's "What" lists "recent activity", which the frozen 10.1.1 admin projection deliberately does not carry (spec-pinned DTO). Delivery is consistent with the durable contract; if a future administrator recent-activity feed is wanted, it needs a projection amendment task, not a retrofit here. No action required for this task.
- **N1 (nit)** — `dashboard.tsx:99` and `:128`: React keys compose `code-label-amount`; two distinct currency pairs sharing null code, null label, and an identical amount would collide. Server-rendered static markup makes this cosmetic (no client reconciliation), and the projection groups per pair. Not blocking.
- **N2 (nit)** — `page.test.tsx:195` redaction regex includes the literal `provider order` while the dictionary legitimately contains "provedor"/"provider" in state labels ("Aguardando pagamento" etc.) — current assertions pass and cover the intent; tightening is optional.

No `bloqueante` findings.

## 7. Evidence and documentation sync

- Evidence harness: `tests/admin-dashboard.evidence.spec.ts` seeds the global fixture directly in the disposable database (SQL inserts only) — no app-code backdoor; account creation and the soft-delete go through the delivered administrator surfaces; `--admin-dashboard` mode in `scripts/run-admin-evidence.mjs` is additive (flag, spec selection, two env vars); `package.json` gains exactly the script pair; the verifier binds 45 captures by SHA-256 with a manifest-hash-bound review. ✓
- `DESIGN.md`: administrator dashboard composition paragraph, scaffold-exception amendment, and the 45-capture evidence binding — all matching delivered behavior. ✓
- Root `AGENTS.md`: the `src/app/admin/` bullet amendment matches the delivered composition and evidence scripts. ✓
- No other specs touched; no files outside the plan's affected areas. ✓

## Decision

**APPROVE** — the original request is met within the durable contracts, all re-runnable gates pass with the exit codes above, fences hold, and both declared deviations are verified as safe.

verificado por revisor independente (yolo) — 2026-07-27
