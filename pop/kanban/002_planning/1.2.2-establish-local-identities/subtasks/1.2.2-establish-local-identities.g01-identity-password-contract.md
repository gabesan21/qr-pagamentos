# Group g01 — Username/optional-email contract — [[1.2.2-establish-local-identities]]

- **Order:** 1
- **Depends on:** none
- **Description:** Replace the malformed email-first identity primitive with a username-first contract while preserving password security.

## Movements

- [ ] (agent) **g01.m1** Add/test exact `normalizeUsername`: NUL/CR/LF rejection, edge SP/HT trim, ASCII lowercase, 3–32 bytes, and `^[a-z0-9]+([._-][a-z0-9]+)*$`.
  - Expected: every exact valid/boundary/invalid vector in the plan passes with generic non-echoing failures.
  - If it fails: a requested username needs a broader alphabet/length → return to 002; never broaden implicitly or use locale-dependent folding.
- [ ] (agent) **g01.m2** Convert email to the exact optional contract and change safe DTOs to required `username` plus `email: string | null`; remove every identity/login-shaped email-only type.
  - Expected: absent/blank email maps to null, present email retains strict canonicalization, and DTOs omit hashes/internal values.
  - If it fails: optionality leaks as empty-string persistence or unsafe union handling → centralize normalization and require null at the persistence/DTO boundary.
- [ ] (agent) **g01.m3** Characterize and preserve the approved password/scrypt behavior; update only fixtures coupled to email-first users.
  - Expected: no scrypt grammar/resource/parser/vector change, and focused tests prove username/email changes do not alter password validation.
  - If it fails: password code needs behavioral change → return to 002 rather than bundling it with the identifier correction.

## Execution notes

- 2026-07-14 — Previous email-first g01 evidence is superseded by the human-corrected username-first contract; record failing-first and replacement observations here.
