# Group g04 — Verification, docs, and DOX closeout — [[1.2.2-establish-local-identities]]

- **Order:** 4.
- **Depends on:** g01, g02, g03.
- **Description:** Produce economical independent evidence and leave operator/spec/DOX contracts aligned with the delivered identity slice.

## Movements

- [x] (agent) **g04.m1** Review the diff for one intention per module, server-only secret handling, no public registration/reset/MFA/session scope, no unrelated refactor, and no generated/secret artifacts.
  - Expected: changed code maps directly to g01–g03; `git diff --check` is clean; ignored secret/generated files are not staged.
  - If it fails: local safe cleanup is needed → fix it and rerun focused tests; broad debt becomes a tracked follow-up, while scope/security drift returns to 002.
- [x] (agent) **g04.m2** First record exact `node --version`/`pnpm --version`, then run `pnpm db:generate`, `pnpm db:contract-check`, elevated `pnpm db:test`, `pnpm container:contract-check`, both named clean-clone scenarios, and `install/test.sh`; prepend every command with exact `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH"`.
  - Expected: every independent gate exits 0 and notes retain redacted positive/denial/order/idempotency/recovery evidence.
  - If it fails: classify product failure versus Docker isolation → repair product failures within the plan; request scoped elevation for isolation; persistent missing live evidence aborts.
- [x] (agent) **g04.m3** Run/record `PATH="$HOME/.local/share/pnpm/bin:$HOME/.local/share/pnpm:$PATH" pnpm check` once after specialized gates; do not run full `pnpm test` separately.
  - Expected: typecheck, lint, the sole full unit-suite execution (including exact scrypt/email/DTO vectors), and production build exit 0 on Node 24.18.0/pnpm 11.13.0.
  - If it fails: generated client or test/build ordering is stale → rerun generation and the failing constituent for diagnosis, repair only task-owned behavior, then rerun the aggregate gate.
- [x] (agent) **g04.m4** Update operator documentation and review/update root and Prisma `AGENTS.md` for changed structure, secret flow, migration/seed ownership, commands, and indexes; prepare the administrative-foundation implemented-slice update for 006.
  - Expected: docs explain initial seed and file-only recovery without secret examples; DOX accurately routes future edits; spec still defers email reset/MFA and remains approved until later slices.
  - If it fails: documentation requires an unsafe manual workaround or contradicts runtime → correct runtime/docs together within approved behavior; material contract divergence returns to 002 and is recorded under spec `Open`.

## Execution notes

- 2026-07-14 — Record exact commands, exit codes, concise redacted evidence, changed documentation/DOX paths, and any debt disposition here for 005 audit.
- 2026-07-14 — Pinned evidence: Node `v24.18.0`, pnpm `11.13.0`. `db:generate`, `db:contract-check`, disposable `db:test`, digest/static `container:contract-check`, clean-clone `identity-seed`, clean-clone `identity-recovery`, and `install/test.sh` all exited 0. Live evidence covered replay/denials, ordered seed, idempotence after email change, UUID-only recovery, and deleted-target no-create; outputs were redacted.
- 2026-07-14 — Final sole aggregate `pnpm check` exited 0: typecheck, lint, 4 passed/1 skipped test files (48 passed/1 skipped tests), and production build. Next emitted only the known multi-lockfile workspace-root warning; no product gate failed.
- 2026-07-14 — Reviewed `git diff --check`, tracked paths, and scope: no generated client, local secret, login/session/public registration/email reset/MFA/UI/final-active-admin behavior was staged. Updated `README.md`, root `AGENTS.md`, and `prisma/AGENTS.md`; the approved administrative-foundation spec already matches delivery and its implemented-slice entry remains intentionally reserved for 006.
- 2026-07-14 — Correction round affected gates all exited 0: `install/test.sh`, `pnpm container:contract-check`, clean-clone `identity-recovery` through the actual installer/helper, and the sole post-correction `pnpm check` (63 passed/1 skipped tests plus typecheck, lint, and build). Database files were untouched, so database gates were not repeated. Evidence now distinguishes unit installer input coverage from live helper/promotion coverage and no longer overstates the earlier direct-Compose run.
