# Group g02 — Corrected baseline migration and DB probes — [[1.2.2-establish-local-identities]]

- **Order:** 2
- **Depends on:** g01
- **Description:** Rewrite the explicitly unshipped local-identity baseline and prove username-first invariants in disposable PostgreSQL.

## Movements

- [ ] (agent) **g02.m1** Run the plan's exact `main` ancestry PASS command and non-mutating `qr-pagamentos_postgres-data` absence PASS command before editing/applying `20260714190000_local_identities` in place; then add required unique canonical username, nullable unique canonical email, and unchanged roles/status/credential/locator.
  - Expected: both commands print their exact PASS lines, one local-identity migration remains, no Phase 1.1/additive migration changes, and Prisma mirrors SQL.
  - If it fails: either BLOCKED line aborts without purge/apply; a retained database requires human reset or a new data-preserving plan, never a forced checksum.
- [ ] (agent) **g02.m2** Replace static/live identity probes for username vectors/collisions, multiple NULL emails, canonical non-NULL email/collisions, credentials, ordinary user lifecycle, immutable locator, and least-grant DML/denials.
  - Expected: disposable replay/idempotence and every positive/negative observation pass without broadening runtime privileges.
  - If it fails: Prisma cannot express a required check → keep representative schema and exact named SQL constraint in this rewritten migration; privilege failures receive only the demonstrated object grant.
- [ ] (agent) **g02.m3** Update Prisma DOX to retain default immutability and encode the narrow explicit unshipped+purged baseline exception.
  - Expected: future agents cannot infer general permission to rewrite migrations; the exception requires human confirmation and no retained data.
  - If it fails: wording could authorize ambiguous rewrites → keep the default prohibition and document this task-specific exception by migration/task id.

## Execution notes

- 2026-07-14 — Prior email-required migration evidence is invalidated. Record rewritten migration path/hash and disposable username/nullable-email/grant observations here.
