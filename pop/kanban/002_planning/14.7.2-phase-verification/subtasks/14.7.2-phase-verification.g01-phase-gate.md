# Frente F01 — Epoch 14 gate — [[14.7.2-phase-verification]]

- **Entrega:** the Epoch 14 verification pass — suite and evidence-spec realignment, the single-system regression, in-reach repairs, the two local evidence regenerations, the aggregated gate result, the consolidated human checklist file and the `develop` → `main` PR body draft.
- **Escopo:** verification and handover. Non-test source changes only where the suite proves a defect caused by 14.7.1's BEM retirement.
- **Responsável:** agent · **Papel:** `pop-phase-verifier`.
- **Paths de entrada:** o card, esta fatia, o [[14.7.2-phase-verification.plan|plano]] inteiro; o plano e as três fatias de 14.7.1 (`f01-workspace-consumers`, `f02-auth-consumers`, `f03-css-deletion-and-contracts`) com seus `Resultado` preenchidos, mais o ledger `pop/memory/**/14.7.1-retire-bem-and-refresh-contracts*.md` e o diff integrado; `pop/researches/template-fidelity-convergence/convergence-outcome.md` (o ledger de lacunas que F03 escreveu); para o piso de suíte, o `Resultado` de `pop/kanban/**/14.6.3-phase-verification/subtasks/*.g01-phase-gate.md` ou seu ledger, senão os de 14.5.4; para a checklist, as entradas de memória nomeadas em C09; `pop/scripts/pop_delivery.py` (linhas 57-80, para o corpo do PR).
- **Owns:** as suítes falsificadas em `src/app/**` e `scripts/*.test.ts`, as novas asserções de regressão do sistema único, `tests/login.evidence.spec.ts` e `tests/store-settings.evidence.spec.ts` (só realinhamento de seletor), os arquivos não-teste **listados como reparo**, `artifacts/design-system/**` e `artifacts/login/**` (escritos pelos scripts, exceto `artifacts/login/<runId>/review.md`, que você escreve), e `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md` (novo).
- **May read:** todo o diff integrado da epoch, `src/**`, `tests/**`, `scripts/**`, `docs/template/app/src/**`, `pop/specs/**`, `DESIGN.md`, `**/AGENTS.md`, `pop/memory/**`, `pop/researches/template-fidelity-convergence/**`, `package.json`, `playwright.config.ts`.
- **Must not edit:** `prisma/**`, qualquer `route.ts`, `src/components/ui/**` e `inventory.json`, `src/media/**`, `src/integrations/**`, `src/i18n/dictionaries/**`, `scripts/**` (exceto uma suíte `*.test.ts` que você realinhe), `src/app-shell/app-shell.css`, `pop/specs/**`, `DESIGN.md`, `**/AGENTS.md`, `docs/**`, `compose*.yaml`, `container/**`, `install/**`, cards e planos de outras tasks, `pop/roadmap/**`, `pop/memory/**`.
- **Depends on:** 14.7.1 inteira (F01, F02 **e** F03) integrada em `develop`. **Entrada esperada:** o diff integrado das três frentes; qualquer uma faltando ou incompatível → `BLOCKED`, nunca verificar meia phase.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao escrever teste e ao decidir reparo vs follow-up*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao realinhar asserção de composição*; [[.agents/skills/clean-code-review/SKILL|clean-code-review]] · [[.agents/skills/ui-review/SKILL|ui-review]] — *use ao classificar severidade e ao ler as capturas regeneradas*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** diff nos paths de `Owns` + relatório com o resultado agregado dos quatro gates em números exatos, a lista de reparos com `file:line`, as suítes realinhadas e novas, o estado das duas evidências locais, a lista de runners Docker desatualizados com seus pares, o path da checklist consolidada e o rascunho do corpo do PR no `Resultado`; status `concluída | BLOCKED`.
- **Critérios:** C01–C12 do [[14.7.2-phase-verification.plan|plano]] (C13/C14 são do agente principal e do humano).

## Contrato de execução

- **Comandos, sempre binários diretos a partir do worktree — nunca `pnpm <script>`, nunca container:** `./node_modules/.bin/tsc --noEmit`; `node scripts/generate-design-tokens.mjs --check && node scripts/generate-brand-assets.mjs --check && node scripts/check-font-provenance.mjs && node scripts/check-design-tokens.mjs && ./node_modules/.bin/eslint .`; `./node_modules/.bin/vitest run`; `./node_modules/.bin/next build`; mais `node scripts/check-admin-ui-inventory.mjs` (F03 podou a allowlist).
- **Ordem:** uma corrida completa de `vitest run` primeiro, para transformar a lista-piso em conjunto real de falhas; depois realinhar; depois a regressão; só então reparar o que continuar vermelho. Nunca use um reparo para salvar uma asserção obsoleta, nem uma asserção para esconder um defeito.
- **Piso das suítes falsificadas** (a corrida e o diff acrescentam, não subtraem), de 14.7.1 C10: `src/app/login/page.test.tsx`, `src/app/reset-password/page.test.tsx`, `src/app/(merchant)/settings/settings-surface.test.tsx`, `src/app/storefront-settings-management.test.tsx`, `src/app/design-system/specimen-mounted.test.tsx`, `routes.test.ts`, `scripts/check-admin-ui-inventory.test.ts`, e as suítes dos alvos apagados (`OrderListCard` em `order-views`, `src/app/admin/payment-link-management.tsx`). Some qualquer suíte que ainda asserte classe aposentada; prove por busca do nome da classe em `src/**` e `tests/**`.
- **Specs de evidência também são fonte:** `tests/login.evidence.spec.ts:174-175` seleciona `.auth-brand[data-brand-identity]` e `.auth-brand [data-brand-mark]`; `tests/store-settings.evidence.spec.ts:253` usa `.settings-surface__nav-link`. Realinhe ambos para `data-*`/role/`id` (nunca para uma string de utilitário), preservando exatamente o que provavam. Confirme que F02 manteve os `id`s ligados por evidência: `#login-form`, `#username`, `#password`, `form#reset-password-form`, `input#newPassword`, `input#confirmation` — um `id` perdido é reparo, não realinhamento. A ordem de foco de login (`["SELECT","username","password","BUTTON","button"]`) só muda se a estrutura mudou de verdade; explique.
- **Regressão devida, pequena e fixadora:** (a) nenhum nome de classe aposentado aparece em `src/**`; (b) os quatro seletores do bloco `(min-width: 900px)` (`.auth-card`, `__panel`, `__form`, `__language`) seguem nos elementos; (c) `src/app-shell/app-shell.css` é a única folha BEM restante fora das exceções documentadas.
- **Evidência local, depois do último commit que toca fonte, uma vez só:** `./node_modules/.bin/next build`; suba o servidor com `./node_modules/.bin/next start --hostname 127.0.0.1 --port 4319`; exporte `ADMIN_EVIDENCE_BASE_URL=http://127.0.0.1:4319` (é isso que desliga o `webServer` que chamaria `pnpm start`, `playwright.config.ts:20`); rode `./node_modules/.bin/playwright test design-system.evidence.spec.ts --project=chromium --workers=1` e `./node_modules/.bin/playwright test login.evidence.spec.ts --project=chromium --workers=1`; depois `node scripts/verify-design-system-evidence.mjs`, `node scripts/verify-login-evidence.mjs` e `node scripts/verify-brand-assets-evidence.mjs`. Derrube o servidor ao final. Porta ocupada → outra porta e a mesma URL exportada.
- **A review do login é sua e é argumentada.** O spec do login não escreve `review.md`; `scripts/verify-login-evidence.mjs:24-25` exige `artifacts/login/<runId>/review.md` contendo o `runId` e o SHA-256 do `manifest.json`, sem achado de severidade 2+. Espelhe o formato de `artifacts/login/20260908212004/review.md`, leia as oito capturas e **justifique cada diferença visível** contra a troca classe→utilitário que a causou; diferença que você não sabe explicar é defeito e vira reparo, nunca baseline nova. A review do design-system é escrita pelo próprio spec (`tests/design-system.evidence.spec.ts:497`) — o argumento vai no relatório.
- **Evidência com Docker é do usuário, sem exceção:** `reset-password` também sobe `docker compose` (`scripts/run-reset-password-evidence.mjs:60,97`), assim como tudo que passa por `run-admin-evidence.mjs` (`:75,89`). Declare cada área desatualizada com o par `pnpm <área>:evidence` / `pnpm <área>:evidence:verify` e **não execute nada** — nem os `verify-*` dessas áreas, que passariam sobre artefato velho e mentiriam.
- **Checklist consolidada** em `pop/notes/references/2026-09-08-epoch-14-human-verification-checklist.md`: frontmatter `author: agent` + `created: 2026-09-08`, ≤150 linhas, em inglês, agrupada por página/família (não por task), cada grupo com rota, locales `pt-BR`/`en`, os seis temas, 320 px e as larguras que a família exige, e uma tabela final com os pares de evidência Docker. Funda as entradas listadas em C09; item que você decidir não reproduzir sai com a razão escrita. Links com gatilho, sem duplicar contrato de spec.
- **Rascunho do corpo do PR** no `Resultado` desta fatia: uma linha por phase 14.1–14.7 dizendo o que entregou, seção "How to test" com os comandos locais e os pares Docker, link para a checklist, os números do gate e os follow-ups abertos. Não abra o PR: `pop_delivery.py scope-pr` fixa título e corpo (`pop/scripts/pop_delivery.py:75-76`), exige árvore limpa, `develop` como branch corrente e `gh` instalado; quem abre é o agente principal, e o merge é humano.
- Defeito real fora do alcance da phase é **follow-up no relatório**, nunca reparo e nunca critério novo.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga com o resto do gate.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** _(preencher na execução)_
- **Commit/artefato:** _(preencher)_
- **Arquivos alterados:** _(preencher, conferido contra `Owns`)_
- **Desvios:** _(preencher)_
- **Evidência:** _(quatro gates com números exatos, reparos com `file:line`, suítes realinhadas e novas, runs de evidência local com `runId`, runners Docker desatualizados, path da checklist)_
- **Rascunho do corpo do PR `develop` → `main`:** _(preencher — o agente principal usa este texto com `gh pr create --base main --head develop --body-file`)_
