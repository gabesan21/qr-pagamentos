# Frente F04 — Contracts and evidence — [[14.4.2-converge-admin-dashboard-and-directories]]

- **Entrega:** the durable record of what F01–F03 integrated: the extended administrator filter sets, the client-side period control, the retired dashboard BEM, the four recorded gaps, and the list of admin suites this diff falsified for `14.4.4-phase-verification`.
- **Escopo:** contract text only. No production code, no test, no kanban file, no memory — the closing act writes memory.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this slice, the plan's "Estratégia" and "Critérios de aceite", the integrated diff of F01–F03 and their Resultado sections.
- **Owns:** `pop/specs/administrative-foundation.md`, `DESIGN.md`, `src/data-directory/AGENTS.md` (only if the directory composition contract actually changed).
- **May read:** the whole repository, `pop/specs/**`, `src/**/AGENTS.md`, `pop/researches/template-fidelity-convergence/admin-area.md`.
- **Must not edit:** `src/**` production code and tests, `src/components/ui/AGENTS.md` (no primitive changed), other specs, `pop/roadmap/**`, `pop/kanban/**`, `pop/memory/**`.
- **Depends on:** F01, F02, F03. **Entrada esperada:** the three integrated diffs and their reports. Any front missing or `BLOCKED` → `BLOCKED`; never document intent as if it were delivered.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep the contract text minimal and true*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** the edited contract text plus a report listing each sentence changed and the phase checklist, status `concluída | BLOCKED`.
- **Critérios:** C12.

## Contrato de execução

- **Spec (`administrative-foundation.md`):** amend the 10.2.1 and 10.2.2 paragraphs so the registered filter sets match reality — orders `source`/`money`/`from`/`to`/`link` plus `state` (eight provider states and the explicit stateless value) and `merchant` (exact, case-insensitive owner username); links `state`/`type`/`kind`/`from`/`to` plus the same `merchant` and `money`. State that both remain read-only, `ADMIN_GLOBAL`, with unchanged rows, page sizes, keyset and `filter.<name>` grammar, and that the administrator link detail resolves a bounded lookup by identifier for the order-detail drill-down. Amend the 10.1.2 paragraph for the client-side period control (same closed set, same pinned default, `<noscript>` fallback) and the leaderboard drill-down into `filter.merchant`.
- **Recorded gaps and forks — one place, one line each:** no current-local-outcome filter (latest-append semantics are not expressible in the adapter without raw SQL or a denormalized column); no unified V1+V2 table (V1 is another table with no owner attribution behind a byte-frozen service, so one keyset page cannot span both — the ledger stays a restyled read-only section); no pair-label filter (registered filters are closed static enums, pair labels are registry rows); no dashboard sparkline (the projection carries no series). Keep each as a durable sentence, not a narrative of the task.
- **`DESIGN.md`:** record that the administrator dashboard left its BEM block for template utilities, that the period control is a route-local composition of the owned `Tabs` (no new inventory entry), and any state note the fronts introduced. Do not restate the plan.
- **DOX:** touch `src/data-directory/AGENTS.md` only if the directory composition contract itself changed; a caller registering more filters is not a change to it.
- **Phase checklist:** list in the report every admin suite this diff falsified — at least `src/app/admin/{page,dashboard,loading}.test.*`, `src/app/admin/error.test.ts`, `src/app/admin/orders/**/*.test.*`, `src/app/admin/payment-links/**/*.test.*`, both admin `directory-query.test.ts`, `src/orders/order-v2-admin-directory.test.ts` — as the input `14.4.4-phase-verification` inherits. Never edit or delete a test here.
- Contract text must be true against the integrated diff: a sentence the code does not honour is a defect, not documentation. When they disagree, report it instead of writing it.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <sentences changed, gaps recorded, phase checklist for 14.4.4>.
