# Frente F01 — Analytics projection and dashboard — [[14.5.1-converge-merchant-dashboard-and-orders]]

- **Entrega:** the additive owner analytics fields and `/` rebuilt to the template: client period control, by-state/by-source chart card, four inventory cards, clickable recent orders and leading products, storefront-disabled banner, first-run empty state, inline error+retry, and `.merchant-dashboard__*` deleted from `globals.css`.
- **Escopo:** the merchant dashboard route and the owner analytics projection it reads. The orders directory, the order details, the admin dashboard and every UI primitive are out.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** the card, this slice, the plan's "Estratégia" and "Critérios de aceite", `pop/researches/template-fidelity-convergence/merchant-commerce.md` (row `/` dashboard), `docs/template/app/src/pages/merchant/MerchantDashboard.tsx`, `docs/template/app/src/mock/types.ts`.
- **Owns:** `src/orders/merchant-analytics.ts`, `src/app/(merchant)/{page,dashboard,loading}.tsx`, new `src/app/(merchant)/{error,period-control}.tsx`, the `.merchant-dashboard*` rules in `src/app/globals.css`, `src/i18n/dictionaries/merchant-dashboard/{en,pt-BR}.ts`.
- **May read:** `src/app/admin/period-control.tsx` and `src/app/admin/dashboard.tsx` (pattern only), `src/components/ui/**`, `src/app/(merchant)/{catalog,settings}/**`, `src/orders/order-v2-view.ts`, `src/auth/storefront-settings.ts`, `pop/specs/{checkout-and-order-lifecycle,application-frontend-system}.md`, `DESIGN.md`.
- **Must not edit:** `src/app/admin/**`, `src/components/ui/**`, `src/app/(merchant)/orders/**`, `src/app/orders/**`, `src/orders/order-v2-{directory,view}.ts`, other dictionary domains, any rule in `globals.css` outside the merchant-dashboard block, `**/*.test.*`, `prisma/**`, `pop/specs/**`, `DESIGN.md`, any `AGENTS.md`.
- **Depends on:** nenhuma. **Entrada esperada:** nenhuma.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use before touching the projection*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the control, cards and states*.
- **Web:** deny · **Gate/delta:** nenhum (rodada 1).
- **Saída:** execution report naming the added projection fields, the dashboard rows, the states delivered and any gap recorded, status `concluída | BLOCKED`.
- **Critérios:** C01–C05, C11, C12.

## Contrato de execução

- **Projection:** add only `ordersInPeriod`, `byProviderState` (the eight provider states plus one stateless bucket for `AD_HOC`), `byOrigin` (`LINK`/`STANDALONE`/`AD_HOC`), the order `id` on recent activity, the product `id` on best sellers, the owner's total link count, active/archived product counts, and a derived `isFirstRun` (no link and no recent activity). Counts come from bounded owner-scoped reads over the same period bounds; amounts and rates keep the exact-decimal path and never touch `Number`; no existing field changes shape, no schema or migration is touched, and no provider or currency-pair UUID enters the DTO.
- **Period control:** a merchant-local client component composing the same owned `Tabs` pill composition the admin control uses, committing `period` with `router.replace` inside a transition and dropping nothing else from the URL; the closed `today`/`7d`/`30d` set and the `7d` default stay exactly as delivered. Do not move, import or edit the admin file, and do not add a variant to `src/components/ui/**`.
- **Composition:** stat 1 becomes orders in period with the source caption; the sales-breakdown card becomes the by-state/by-source chart (sales values stay on their stat cards); the inventory row becomes four cards (active links, total links, active products, archived products); recent rows show `#id · payer` with separate provider and outcome badges and link to `/orders/v2/<id>`; leading products link to `/catalog/products/<id>`. Progress bars keep the token-safe bucket approach.
- **States:** storefront-disabled banner with a CTA to `/settings#storefront-enabled`; first-run empty state replacing the populated rows with the create-link CTA; a new `error.tsx` rendering an inline strip whose retry calls `reset()`; `loading.tsx` geometry matching the delivered layout. A "stale" strip has no producer on a server-rendered page — record it as a gap in the report instead of faking one.
- Delete the `.merchant-dashboard*` block from `globals.css` and compose with tokens and the owned primitives only; no raw color, spacing, radius or shadow value.
- Every new or changed user-facing string lives in `merchant-dashboard/en.ts` and `merchant-dashboard/pt-BR.ts` under the same key.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar ou simular a dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <projection fields, `pnpm typecheck`/`pnpm lint` exit codes, gaps recorded>.
