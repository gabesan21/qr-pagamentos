# Group g02 — Orders and ownership — [[2.2.1-build-nautt-pricing-and-order-adapter]]

- **Order:** 2
- **Depends on:** g01
- **Description:** Add owner-bound one-shot onramp creation and opaque provider reads.

## Movements

- [ ] (agent) Define the injected `QuoteOwnershipStore` (`register` plus atomic `claimForCreation`) and an in-memory UUID-keyed implementation; wire quote issuance to register owner and authoritative expiry without overwriting a duplicate UUID.
  - Expected: a JSON-serialized/reconstructed quote reference resolves on the same registry, while a fresh registry treats it as unknown; no schema or provider fallback is introduced.
  - If it fails: correctness requires cross-process state → stop at the persistence abort for 2.2.2; do not hide global mutable state in the adapter.
- [ ] (agent) Implement `POST /orders/onramp` validation and strict response normalization, accepting a registered quote UUID reference plus only documented optional string fields.
  - Expected: exact body/call-count tests pass; `pix_qrcode ?? qrcode` and optional URL normalize without exposing raw payer/user blocks.
  - If it fails: a UUID pair requires unknown payer fields → leave the field caller-supplied and record RECON NEEDED; do not invent or persist it.
- [ ] (agent) (after previous) Make every failure after creation dispatch a redacted indeterminate result with exactly one fetch call and no internal retry.
  - Expected: HTTP, malformed 201, transport, timeout, and abort cases each assert one call and no key/body leakage.
  - If it fails: code cannot prove whether dispatch began → classify as indeterminate; only locally proven zero-call failures are definitive.
- [ ] (agent) Implement strict `GET /orders/{uuid}` with the same DTO and an indistinguishable 403/404 not-found/not-owned result.
  - Expected: exact owned URL/key tests pass; documented exact amounts and lowercase statuses survive unchanged.
  - If it fails: provider returns contradictory status casing/shape → reject strictly and record the fixture; do not add an invented status.
- [ ] (agent) (after all above) Add server-only owner orchestration that validates local input, atomically claims the UUID for the same owner/fresh expiry, then decrypts and dispatches; map unknown/cross-owner/expired/consumed to one opaque error and clear plaintext local references in `finally`.
  - Expected: serialized clone succeeds on the same registry; cross-owner does not consume A's quote; cross-owner, fresh-registry unknown, expiry, and replay each make zero decryption/provider calls; DTOs omit provider user/payer data.
  - If it fails: implementation requires a browser-supplied owner or a new client-facing API → abort as scope/authorization divergence and return to 002.
- [ ] (agent) Capture owner, creation, and read focused commands with exit status, relevant case/call-count/redaction assertions, and worktree path in these execution notes.
  - Expected: criteria 3–5 and 7 can be audited in 005 without duplicate focused execution.
  - If it fails: evidence is incomplete or ambiguous → re-run only the missing focused command in 004 and record its complete result.

## Execution notes

- 2026-07-17 — Pending execution; capture criteria 3–5 focused evidence, including serialized clone success plus cross-owner, fresh-registry unknown, expiry, and consumed replay zero-decrypt/zero-fetch.
