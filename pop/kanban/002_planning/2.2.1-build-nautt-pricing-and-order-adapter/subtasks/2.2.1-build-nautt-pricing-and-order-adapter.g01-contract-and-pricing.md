# Group g01 — Contract and pricing — [[2.2.1-build-nautt-pricing-and-order-adapter]]

- **Order:** 1
- **Depends on:** none
- **Description:** Establish exact-value/domain contracts and the strict quote adapter.

## Movements

- [ ] (agent) Walk root and `src/integrations/nautt/` DOX contracts; inspect the approved executor context and keep the fixed-origin webhook code behavior unchanged.
  - Expected: affected production paths are limited to `src/integrations/nautt/`; no schema/UI/env path is needed.
  - If it fails: a required boundary is missing → update the nearest applicable DOX contract first, or hit the scope abort and return to 002.
- [ ] (agent) Add exact positive-decimal and UUID validation plus discriminated quote input and minimal redacted quote/order types, with focused failing tests first.
  - Expected: strings remain byte-for-byte values; invalid runtime input is rejected before fetch/key decryption.
  - If it fails: a provider fixture needs an undocumented numeric form → reject the fixture and record the documentation gap; never convert through `Number`.
- [ ] (agent) (after previous) Implement strict pricing request/response parsing with fixed production origin, injected fetch/timeout/clock, one request, and `expiresAt = acceptedAt + 5 minutes`.
  - Expected: exact request assertion passes and returned DTO contains no raw envelope or owner/provider user data.
  - If it fails: documented success lacks the required tuple → use the strict-success fork and a redacted error; do not guess defaults.
- [ ] (agent) Capture the exact decimal and focused pricing commands, exit status, case/assertion summary, and worktree path in these execution notes.
  - Expected: criteria 1–2 can be audited in 005 without duplicate focused execution.
  - If it fails: evidence is incomplete or ambiguous → re-run the missing focused command in 004 and record its complete result.

## Execution notes

- 2026-07-17 — Pending execution; record focused-test evidence here for criteria 1–2.
