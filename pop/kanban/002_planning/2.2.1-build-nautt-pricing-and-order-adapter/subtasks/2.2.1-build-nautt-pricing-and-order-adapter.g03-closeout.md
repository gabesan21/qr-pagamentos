# Group g03 — Closeout — [[2.2.1-build-nautt-pricing-and-order-adapter]]

- **Order:** 3
- **Depends on:** g01, g02
- **Description:** Reconcile contracts/spec evidence and run all approved gates.

## Movements

- [ ] (agent) Review every changed path against root and nearest DOX contracts; update `src/integrations/nautt/AGENTS.md` only if ownership, protocol, structure, or quality rules changed.
  - Expected: the subtree remains locally understandable and no stale index/contract statement remains.
  - If it fails: closeout reveals a cross-subtree change → apply the scope abort and return to 002 rather than extending the diff.
- [ ] (agent) Confirm g01/g02 contain complete focused evidence, run the non-aggregate static source scan, then run `pnpm check`; capture both fresh outputs here.
  - Expected: criteria 1–8 have observable evidence, the static scan has no matches, and the aggregate gate is green.
  - If it fails: focused behavior failure → repair only the planned boundary and re-run that criterion; aggregate unrelated baseline failure → record exact evidence and stop for triage.
- [ ] (agent) Compare implementation with the approved integration spec; if reality diverged, add the exact item under its Open section and return to 002 for material changes.
  - Expected: code, plan, spec, and execution evidence describe the same single-attempt adapter boundary.
  - If it fails: silent spec rewrite would be required → do not proceed to 005; record divergence and re-plan.

## Execution notes

- 2026-07-17 — Pending execution.
