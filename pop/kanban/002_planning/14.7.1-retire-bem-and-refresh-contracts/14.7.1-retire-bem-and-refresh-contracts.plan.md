# Plan — [[14.7.1-retire-bem-and-refresh-contracts]]

- **Etapa:** 002_planning · **Papel:** `pop-planner` (separado da execução)

## Objetivo e resultado esperado

- **Objetivo:** end the parallel CSS system — every route-scoped BEM rule left in `src/app/globals.css` either loses its last consumer and is deleted, or is named as a sanctioned exception with the reason it cannot be a utility — and align `DESIGN.md`, [[pop/specs/application-frontend-system|the frontend spec]], the DOX tree and the audit's gap ledger with the converged single system.
- **Resultado observável:** `globals.css` below `/* generated-theme-tokens:end */` holds only tokens, `@theme inline`, base element rules and the documented exceptions; `src/app-shell/app-shell.css` is the one remaining route-neutral BEM file, declared as such in its own DOX; no contract still describes a class that no longer exists; the closed and still-open audit gaps exist as one readable list for 14.7.2's report.

## Estratégia

Retirement is by consumer, never by prefix: a rule leaves only after a grep in `src/**` (tests included) proves zero referrers, a rule whose only referrer is a dead export goes with that export, and a rule that survives does so for a written reason. Two reasons are accepted. First, `scripts/check-design-tokens.mjs` (inside `pnpm lint`) rejects any raw `px|rem|em|ch|#hex` in `src/app/**`, `src/app-shell/**`, `src/components/ui/**` and sanctions by name only two literals, both inside `globals.css`: `(min-width: 64rem)`, which is exactly Tailwind's `lg:` and therefore migrates freely, and `(min-width: 900px)`, which has no variant and cannot become `min-[900px]:` without violating the gate — so the auth split-card block stays CSS. Second, a migration needing a component API change (a descendant override reaching into `BrandIdentity`'s internals) is out of scope and becomes a recorded gap, never a forced refactor; neither front edits `check-design-tokens.mjs`, and a needed entry returns to the agente principal. 14.6.2 F02 runs in parallel and removes the public `storefront-*`/`receipt-rail*` blocks and the seven leftover `.checkout-*` rules, so this task does not plan them — but `/design-system` still consumes `receipt-rail{,__label,__facts}`, so **each front re-greps at run time**, treats the tree it finds as truth and reports any divergence from the inventory below instead of assuming it. Behaviour is frozen throughout: no route, dictionary key, form body, authorization, redaction or money path changes, rendered geometry preserved, replacements written with `var(--space-*)`/`var(--layout-max)` references and never a literal or an inline `style=`.

## Áreas afetadas e inventário de partida

- **Áreas:** `src/app/{auth-card,nautt-credential-surface,storefront-settings-management}.tsx`, `src/app/{login,reset-password,design-system}/**`, `src/app/(merchant)/settings/settings-surface.tsx`, `src/app/orders/order-views.tsx`, `src/app/admin/payment-link-management.tsx` (consumers); then `src/app/globals.css`, `DESIGN.md`, the frontend spec, four DOX files, the inventory allowlist and the new gap ledger.
- **(a) mortas — só apagar:** `.admin-shell`; `.admin-account{,-list,__facts,__actions}` (único consumidor é o export morto `OrderListCard`); `.admin-product{,-list,-description,__facts}` (único consumidor é `src/app/admin/payment-link-management.tsx`, sem importador); `.auth-card__form--tight` (confirmar).
- **(b) vivas com equivalente utilitário — migrar consumidor, depois apagar:** `.admin-shell__intro` + `.receipt-rail*` e `.ds-{ledger,section,section__heading,row,facts}` + `[data-ds-prose]` (`design-system/{page,interactive-specimens}.tsx`); `.nautt-facts` (`nautt-credential-surface.tsx`); `.settings-surface*` (`(merchant)/settings/settings-surface.tsx`, `storefront-settings-management.tsx`); folhas de auth `.auth-brand`, `.auth-card__{tagline,caption,strip,swatch,footer}`, `.auth-password-field*`, `.auth-forgot*`, `.auth-totp-actions`, `.auth-mode-toggle`, `.auth-page`, `.login-{page,form}`, `.reset-password-{page,form}`.
- **(c) exceções sancionadas — manter e documentar:** todo `src/app-shell/app-shell.css` (`.app-shell__*`, `.workspace-heading*`) como único CSS BEM de chrome; o bloco `(min-width: 900px)` com `.auth-card`, `.auth-card__panel`, `.auth-card__form`, `.auth-card__language`; `.brand-identity*` + `[data-brand-identity]` (DOX `src/brand/AGENTS.md`); `.sr-only`; a regra base `.font-mono, .font-money, code, kbd` e as demais regras de elemento; tokens, `@custom-variant`, `@theme inline`, `@layer base` e os `@media` de `prefers-color-scheme`/`prefers-reduced-motion`.

## Frentes de execução

### F01 — Workspace consumers off BEM

- **Entrega:** settings (merchant + storefront), o bloco Nautt, `/design-system` e os dois componentes mortos sem nenhum nome de classe BEM no markup, sobre utilitários projetados; o hub `src/app/admin/settings/**` (14.4.3) é a implementação de referência da mesma composição em duas colunas · **Contrato:** [[14.7.1-retire-bem-and-refresh-contracts.f01-workspace-consumers]] — *siga como fatia única desta frente* · **Papel:** `pop-executor`.
- **Owns:** `src/app/(merchant)/settings/settings-surface.tsx`, `src/app/storefront-settings-management.tsx`, `src/app/nautt-credential-surface.tsx`, `src/app/design-system/{page,interactive-specimens}.tsx`, `src/app/orders/order-views.tsx`, `src/app/admin/payment-link-management.tsx`, `src/app/(merchant)/links/legacy-links.tsx` (só o comentário que cita o arquivo apagado), e os testes colocados `(merchant)/settings/settings-surface.test.tsx` e `storefront-settings-management.test.tsx` (só realinhamento de asserção).
- **Must not edit:** `src/app/globals.css`, `src/app/auth-card.tsx`, `src/app/{login,reset-password}/**` (F02), `DESIGN.md`, `pop/specs/**`, `**/AGENTS.md`, `scripts/**`, `src/components/ui/**`, `src/app-shell/**`, `src/brand/**`, `src/app/{pay,store,api}/**`, `docs/**`, `artifacts/**`.
- **Depends on:** nenhuma (mesma onda que F02) · **Entrada esperada:** o hub admin de settings como referência; ausente ou divergente → migrar mesmo assim com utilitários simples e registrar o desvio, nunca `BLOCKED` por isso.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] · [[.agents/skills/ui-change/SKILL|ui-change]] · **Web:** deny · **Gate/delta:** nenhum · **Saída:** diff + report ≤15 linhas (classes soltas com prova de grep, exports/arquivos apagados com grep de importador, desvios e lacunas), evidência dos três comandos estáticos, status `concluída | BLOCKED` · **Critérios:** C1, C3, C4, C5, C6, C8.

### F02 — Authentication consumers off BEM

- **Entrega:** `/login`, `/reset-password` e o `AuthCard` sem as folhas de auth no markup, mantendo intacto o bloco `(min-width: 900px)` e reportando explicitamente os quatro seletores que sobrevivem por causa dele · **Contrato:** [[14.7.1-retire-bem-and-refresh-contracts.f02-auth-consumers]] — *siga como fatia única desta frente* · **Papel:** `pop-executor`.
- **Owns:** `src/app/auth-card.tsx`, `src/app/login/{page,login-form,totp-challenge-form}.tsx`, `src/app/reset-password/{page,reset-password-form}.tsx`, e os testes colocados `src/app/login/page.test.tsx` e `src/app/reset-password/page.test.tsx` (só realinhamento das asserções de classe).
- **Must not edit:** `src/app/globals.css`, todo o write set de F01, `src/brand/**`, `DESIGN.md`, `pop/specs/**`, `**/AGENTS.md`, `scripts/**`, `src/components/ui/**`, `src/app-shell/**`, `src/app/api/**`, ações e serviços de identidade, `docs/**`, `artifacts/**`.
- **Depends on:** nenhuma (write set disjunto de F01, mesma onda) · **Entrada esperada:** nenhuma externa.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] · [[.agents/skills/ui-change/SKILL|ui-change]] · **Web:** deny · **Gate/delta:** nenhum · **Saída:** diff + report ≤15 linhas (classes soltas, seletores que sobrevivem e por quê, asserções realinhadas, lacunas registradas), mesma evidência estática, status `concluída | BLOCKED` · **Critérios:** C2, C3, C5, C6, C8.

### F03 — CSS deletion, contracts, DOX and the gap ledger

- **Entrega:** as regras sem referrer apagadas de `globals.css` com prova de grep; `DESIGN.md`, o spec do frontend (compactado dentro de 150 linhas), os DOX tocados e a allowlist do inventário admin descrevendo o sistema único; o ledger consolidado de lacunas fechadas e abertas · **Contrato:** [[14.7.1-retire-bem-and-refresh-contracts.f03-css-deletion-and-contracts]] — *siga como fatia única desta frente* · **Papel:** `pop-executor`.
- **Owns:** `src/app/globals.css`, `DESIGN.md`, `pop/specs/application-frontend-system.md`, `src/app-shell/AGENTS.md`, `src/components/ui/AGENTS.md`, `src/data-directory/AGENTS.md`, `src/brand/AGENTS.md`, `scripts/check-admin-ui-inventory.mjs` (apenas o conjunto `allowedClasses`), `pop/researches/template-fidelity-convergence/convergence-outcome.md` (novo, ≤150 linhas).
- **Must not edit:** todo o write set de F01 e F02, qualquer outro `.tsx`, `src/app-shell/app-shell.css`, `scripts/check-design-tokens.mjs`, qualquer outro script, outras specs, `src/components/ui/inventory.json`, o `AGENTS.md` da raiz, `docs/**`, `artifacts/**`, `pop/memory/**`, `pop/roadmap/**`.
- **Depends on:** F01 e F02 integradas · **Entrada esperada:** os dois reports de classes soltas; ausentes → re-derivar por grep; regra ainda com referrer → não apagar e registrar o motivo.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] · [[.agents/skills/ui-change/SKILL|ui-change]] · **Web:** deny · **Gate/delta:** nenhum · **Saída:** diff + report ≤15 linhas (regras apagadas com grep, exceções documentadas, linhas do spec compactadas, allowlist podada, lacunas fechadas/abertas contadas), evidência estática + `node scripts/check-admin-ui-inventory.mjs`, status `concluída | BLOCKED` · **Critérios:** C3 a C9.

## Ordem e paralelismo

1. **Onda 1 (paralela):** F01 e F02 — write sets disjuntos, sem dependência lógica, ambos só soltam referrers sem tocar CSS. **Onda 2:** F03, depois das duas integradas. A cada onda o agente principal valida ownership com `pop_check_scope.py`, integra e confere os critérios `agent`.

## Riscos e condições de aborto

- **Risco:** 14.6.2 entregar `globals.css` diferente deste inventário (ex.: `.receipt-rail*` mantida por causa de `/design-system`) — mitigação: cada frente re-greppa antes de editar e reporta a divergência; F03 nunca apaga contra grep positivo.
- **Risco:** substituir classe por utilitário reintroduz literal e quebra `check-design-tokens` — mitigação: rodar o script em toda frente; `900px` fica em CSS, `64rem` vira `lg:`.
- **Risco:** apagar export/arquivo ainda importado por teste ou rota — mitigação: grep de importador em `src/**` e `tests/**` antes; positivo → manter e apenas migrar as classes.
- **Risco:** editar `/design-system` e as páginas de auth invalida evidência ligada por hash (`artifacts/design-system/current.json`) e as baselines de pixel de login/reset — mitigação: repassar a regeneração para 14.7.2 (C10/C11) como fato, não como surpresa.
- **Abortar se:** uma migração exigir mudar API de componente, chave de dicionário, rota, body ou regra de autorização/redação — sinalizar `blocked: true` com path e linha, e registrar como lacuna.

## Critérios de aceite

| # | Critério | Verifica | Verificação | Pass é | Modo 005 |
|---|----------|----------|-------------|--------|----------|
| C1 | Settings (merchant + storefront), Nautt e `/design-system` não carregam nenhum `settings-surface*`, `nautt-facts`, `ds-*`, `admin-shell*` ou `receipt-rail*` no markup; a composição em duas colunas usa `lg:` | agent | leitura do diff de F01 + grep dos nomes | grep 0 nos arquivos de F01, layout equivalente ao hub admin | evidência |
| C2 | `/login`, `/reset-password` e `AuthCard` não carregam folhas de auth; sobrevivem apenas `.auth-card`, `__panel`, `__form`, `__language` do bloco de 900px, nomeados no report | agent | leitura do diff de F02 + grep | nenhuma outra classe `auth-*`/`login-*`/`reset-*` no markup | evidência |
| C3 | Nenhuma mudança de comportamento: rota, chave de dicionário, body de form, autorização, redação, dinheiro e geometria renderizada preservados; migração que exigiria API nova virou lacuna | agent + phase | leitura dos três diffs contra as specs; `pnpm test` na 14.7.2 | só troca de apresentação; lacunas registradas com path | evidência + phase |
| C4 | Os exports/arquivos mortos (`OrderListCard`, `payment-link-management.tsx`) saem só com grep de importador zerado, ou ficam com desvio registrado | agent | leitura do diff + grep em `src/**` e `tests/**` | nenhuma remoção sem prova | evidência |
| C5 | Cada regra apagada de `globals.css` tem grep provando zero referrer; nenhuma regra ainda usada foi removida | agent | leitura do diff de F03 + busca dos nomes de classe | `globals.css` abaixo dos tokens só com base, `@theme` e exceções | evidência |
| C6 | Gates estáticos passam em cada frente: `./node_modules/.bin/tsc --noEmit`, `node scripts/check-design-tokens.mjs`, `./node_modules/.bin/eslint` nos arquivos mudados; nenhum `pnpm <script>` e nenhum container | agent | execução dos três comandos | exit 0 (erros pré-existentes fora do write set anotados) | evidência |
| C7 | `DESIGN.md`, o spec do frontend e os DOX declaram o sistema único: `app-shell.css` como único CSS BEM restante, o bloco de 900px com a razão do gate, `brand-identity*` e `.sr-only`; o spec continua ≤150 linhas | agent | leitura dos documentos + contagem de linhas | nenhum contrato citando classe inexistente; spec compactado, não truncado | evidência |
| C8 | O ledger `convergence-outcome.md` lista cada lacuna da auditoria como fechada (com o ledger de memória que a fecha) ou aberta (com a spec que a registra), sem descarte silencioso | agent | leitura do ledger contra os seis arquivos da auditoria e as entradas `*04-contracts*` de 14.4.2/14.4.3/14.5.1/14.5.2/14.5.3/14.6.1 | toda lacuna com destino nomeado | evidência |
| C9 | A allowlist de `check-admin-ui-inventory.mjs` fica só com classes que ainda existem (fecha o follow-up de 14.4.4) e o script segue passando | agent | leitura do diff + `node scripts/check-admin-ui-inventory.mjs` | nenhuma entrada morta; exit 0 | evidência |
| C10 | Suites e evidências afetadas repassadas à 14.7.2: `login/page.test.tsx`, `reset-password/page.test.tsx`, `(merchant)/settings/settings-surface.test.tsx`, `storefront-settings-management.test.tsx`, `design-system/specimen-mounted.test.tsx`, `routes.test.ts`, `scripts/check-admin-ui-inventory.test.ts`, os testes de orders/links dos componentes apagados, mais a regeneração da evidência do design-system e das baselines de login/reset | phase | `pnpm check` e a regeneração na 14.7.2 | suíte verde e hashes de fonte batendo | phase |
| C11 | Passada renderizada em `/login`, `/reset-password`, `/settings`, `/admin/settings`, `/design-system`, nos dois locales, seis temas e larguras 320/375/768/1440, mais os runners de evidência dependentes de Docker | user | verificação humana no navegador e nos runners | sem overflow, foco visível, nenhuma regressão visual contra o estado anterior | humano |

## Specs, contratos e topologia

- [[pop/specs/application-frontend-system|application-frontend-system]] — *siga para vocabulário projetado, estados e a11y; mudança esperada: o parágrafo do sistema único e das exceções, com compactação para caber em 150 linhas (F03)*. [[pop/specs/administrative-foundation|administrative-foundation]] e [[pop/specs/identity-security|identity-security]] — *siga para shell/diretórios e para login/MFA/recuperação; mudança esperada: nenhuma*.
- [`src/app-shell/AGENTS.md`](../../../../src/app-shell/AGENTS.md), [`src/components/ui/AGENTS.md`](../../../../src/components/ui/AGENTS.md), [`src/data-directory/AGENTS.md`](../../../../src/data-directory/AGENTS.md), [`src/brand/AGENTS.md`](../../../../src/brand/AGENTS.md) — *ande neles antes de editar cada subárvore; mudança esperada: uma linha de contrato em cada (F03)*.
- **Topologia:** `pop-execution-orchestrator` — três write sets disjuntos, uma onda paralela (F01 ∥ F02) seguida de F03, que só apaga CSS depois que as duas soltarem os referrers; separar consumidor de `globals.css` é o que evita write set sobreposto no mesmo arquivo. Perfis fixos por papel nativo; `size: M` altera só orçamento/profundidade.
