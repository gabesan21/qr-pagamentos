# Front F01 — Closed specimen composition — [[12.2.4-rebuild-design-system-specimen]]

- **Delivery:** inert `/design-system` composition and executable coverage map for every shared owner and applicable state in both locales.
- **Scope:** specimen/demo code only; consume the delivered token, asset, primitive, composition and directory contracts without changing them.
- **Owner:** agent.
- **Owns:** `src/app/design-system/**`, `src/i18n/dictionaries/design-system/**`, focused specimen unit/component tests under the route subtree.
- **May read:** active frontend spec, `DESIGN.md`, parity authority/template sources, 12.2.1–12.2.3 memory, `components.json`, every shared owner and dictionary type.
- **Must not edit:** `src/components/ui/**`, `src/data-directory/**`, `src/brand/**`, `src/design-system/**`, `src/app/globals.css`, other routes/dictionaries, parity/evidence artifacts, package/lock, specs/DOX.
- **Depends on:** task 12.2.3.
- **Expected input:** stable shared inventory with 20 reachable responsibilities, primitive foundation, N/A dispositions, canonical directory and asset family.
- **Skills:** `ui-change`, `clean-code-change`, `shadcn`, `vercel-react-best-practices`.
- **Criteria:** C01-C05.

## Execution contract

- Treat the coverage map as fail-closed input: one production source/public API/applicable state per deterministic fixture or real interaction probe; preserve explicit N/A.
- Use official owned variants/composition rules and local source APIs; add/update no registry component or dependency. Keep static fixture sections server-rendered and client boundaries interaction-specific.
- Fixture data is formatted, localized, redacted and inert. It may not call services, submit real mutations, authorize capability, import a product route, or simulate missing domain behavior.
- Locale uses the existing unprefixed preference contract; theme changes semantic tokens only. Keep `[lang]/design-system` as its existing 404 guard.
- Dependency absent/incompatible → `BLOCKED`; never implement it or write outside `Owns`.

## Result

- **Status:** pending.
- **Commit/artifact:** pending.
- **Files changed / deviations / evidence:** pending.
