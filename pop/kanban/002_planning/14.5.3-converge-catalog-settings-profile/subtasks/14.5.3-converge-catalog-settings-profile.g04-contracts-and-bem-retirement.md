# Frente F04 — Contracts, design memory and BEM retirement — [[14.5.3-converge-catalog-settings-profile]]

- **Entrega:** the four specs and `DESIGN.md` synced with the delivered catalog/settings/profile composition, the four recorded gaps and the single-save decision; the merchant BEM blocks this task replaced deleted from `globals.css` with no consumer left behind.
- **Escopo:** durable contracts and retired route CSS only — no application behaviour, markup or dictionary change.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.5.3-converge-catalog-settings-profile.md`; seções Estratégia e Critérios de [[14.5.3-converge-catalog-settings-profile.plan|plano]]; esta fatia; o diff integrado de F01, F02 e F03.
- **Owns:** `src/app/globals.css`, `pop/specs/{catalog-and-payment-links,storefront-and-customization,identity-security,nautt-finance-integration}.md`, `DESIGN.md`.
- **May read:** todo o diff integrado das frentes anteriores, `src/app/(merchant)/**`, `src/app/profile/*.tsx`, `src/app/{storefront-settings-management,storefront-preview,checkout-policy-management,nautt-credential-surface}.tsx`, `src/components/ui/AGENTS.md`, `src/components/ui/inventory.json`, `pop/specs/application-frontend-system.md`.
- **Must not edit:** qualquer arquivo de `src/` fora de `globals.css`, qualquer `*.test.ts(x)`, dicionários, `pop/specs/**` fora dos quatro nomeados, cards e planos.
- **Depends on:** F01, F02, F03.
- **Entrada esperada:** o diff integrado das três frentes, com os gaps observáveis no código; ausente ou incompatível → `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use ao alterar o CSS*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use ao registrar a memória de design*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff integrado nos paths de `Owns` + relato ≤10 linhas no bloco Resultado, evidência por leitura de arquivo, status `concluída | BLOCKED`.
- **Critérios:** C12, C13 do [[14.5.3-converge-catalog-settings-profile.plan|plano]].

## Contrato de execução

- Delete exactly these rules from `src/app/globals.css`: `.storefront-workspace`, `.storefront-workspace__fieldset`, `.storefront-workspace__actions`, `.storefront-logo-block`, `.storefront-logo-current`, `.storefront-logo-actions`, `.storefront-logo-staged`, `.storefront-preview*` and `.profile-workspace*`, `.profile-form__fieldset`. Confirm by search that no file references them before removing each one.
- Keep `.settings-surface*` (delivered by 14.1.2), `.storefront-logo` and every public storefront rule (`.storefront-shell`, `.storefront-rail`, `.storefront-products*`, `.storefront-cart*`, `.storefront-error*` and siblings): they serve `/store/**` and `/pay/**` and belong to 14.6/14.7. Do not add a rule; converged surfaces use tokens through utility classes.
- `catalog-and-payment-links`: record the flat form composition, the drag-and-drop uploader over the unchanged staging boundary, the currency preselect, the neutral archived badge, and the two gaps — no state control on create (route and service accept no state field) and no inline edit for an inactive category (store matches `active: true`; the spec keeps inactive targets opaque).
- `storefront-and-customization`: record the store controls, the logo staging read from the redirect target, the confirm-gated enabled switch with its client guard, the currency Clear, the five policy cards, and the decision that the workspace keeps one form and one Save with its reason.
- `identity-security`: record the enrollment order (QR and manual secret, then confirmation, then recovery codes shown once), the client-side confirmation bridge that leaves the route unchanged, the never-persisted codes, the download affordance and the regenerate confirmation; record the enrolled-since gap (`getStatus` projects only the state).
- `nautt-finance-integration`: record Validate as a status re-read with no new provider call, the reset confirmation restricted to the recoverable states, and the gap that replacement stays blocked outside `UNREGISTERED` — so no replace control is offered where the contract refuses it.
- `DESIGN.md`: record the merchant-local `SegmentedControl` in `merchant-controls.tsx` with its states and the one-line reason, the `ImageUploader` adoption on both merchant surfaces, the profile single-column stack and the retired BEM blocks. Do not restate token values already in the file.
- Write only what the delivered diff proves; a claim you cannot see in the code is a defect, not documentation.
- **Saber parar:** no máximo 2 tentativas quando a falha for de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <observação relevante>.
