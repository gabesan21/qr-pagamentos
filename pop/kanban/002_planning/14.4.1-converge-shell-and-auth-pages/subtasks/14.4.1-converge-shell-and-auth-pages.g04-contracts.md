# Frente F04 — Contracts and design memory — [[14.4.1-converge-shell-and-auth-pages]]

- **Entrega:** `DESIGN.md`, the frontend system spec and the shell DOX state the shell and authentication rules that shipped, including the recorded reset-state gap and the storefront link target.
- **Escopo:** durable documentation of the integrated diff. No code, no dictionary, no CSS, and no rule that the shipped diff does not implement.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `O quê/Por quê`; `.plan.md` sections Objetivo, Estratégia and Critérios; this slice; the integrated diffs and result notes of F01, F02 and F03; `pop/researches/template-fidelity-convergence/foundation-shell-auth.md`.
- **Owns:** `DESIGN.md`, `pop/specs/application-frontend-system.md`, `src/app-shell/AGENTS.md`.
- **May read:** the whole repository read-only, especially `src/app-shell/**`, `src/app/{login,reset-password,not-found.tsx}`, `src/app/auth-card.tsx`, `src/app/globals.css`, and the three subtask result notes.
- **Must not edit:** every source, dictionary, CSS and test path; the other specs; the card and the plan.
- **Depends on:** F01, F02, F03. **Entrada esperada:** the three fronts integrated with their result notes; ausente ou incompatível → `BLOCKED`.
- **Skills:** [[.agents/skills/ui-change/SKILL|ui-change]] — *use para manter o `DESIGN.md` como memória fiel ao código*. [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para registrar a dívida rastreável do estado de reset*.
- **Web:** deny.
- **Gate/delta:** nenhum (primeira rodada).
- **Saída:** documentation diff plus a short result note in this file, teto 10 linhas, evidência: seções alteradas e o par regra→arquivo que a sustenta; status `concluída | BLOCKED`.
- **Critérios:** C12 do [[14.4.1-converge-shell-and-auth-pages.plan|plano]].

## Decisões que restringem esta frente

- Record exactly six things, each traceable to a shipped file: the route-derived top-bar title and its fallback; the `lg` rail/drawer boundary and the single navigation copy; the rail footer composition with sign-out owned by the account menu; the merchant storefront link resolved from storefront settings and targeting `/store/<slug>`; the authentication composition with the leading brand panel, tagline, generated-asset strip and swatches, the forgot-password disclosure and the six-cell challenge; and the reset state model.
- The reset entry states the model **and** its limit: the page presents missing token and unusable link, the service cannot separate invalid, expired and used, and the split remains open work requiring a new service outcome. Write it as an open contract limit, never as an implemented behavior.
- `DESIGN.md` carries composition, geometry and state memory; the spec carries the durable application-wide contract; the DOX carries the shell subtree rules. Do not restate the same sentence in all three, and do not copy spec content into the plan or the card.
- Nothing here changes `identity-security` or `administrative-foundation`: routes, roles, capabilities and opaque outcomes are unchanged by this task.
- Any divergence between a front's result note and the actual diff is reported, not documented as if it had shipped.

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente (sandbox, permissão, flakiness); na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência.
- Não implementar, simular ou corrigir dependências por conta própria.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
