# Frente F01 — Storefront page and public BEM retirement — [[14.6.2-converge-storefront-and-standalone-pay]]

- **Entrega:** `/store/[slug]` (page, experience, loading, error) recomposed on the converged public checkout vocabulary delivered by [[14.6.1-converge-public-checkout]], and every **public** `storefront-*`/`receipt-rail*` block deleted from `globals.css` in favour of the projected utilities.
- **Escopo:** presentation of the public storefront route plus the storefront copy delta. Cart semantics (browser-local key/envelope, 1–9,999 quantities, BigInt per-currency totals, stale-item recovery notice), the `POST /api/store/[slug]/cart/checkout` body, the `201` clear-and-redirect to `/pay/[identifier]`, the `?amount=` prefill grammar and the `boxed|table` layouts are preserved byte-for-byte in behaviour.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.6.2-converge-storefront-and-standalone-pay.md`; this slice; sections "Objetivo", "Estratégia", "Riscos", "Critérios" of [[14.6.2-converge-storefront-and-standalone-pay.plan|the plan]]; [[researches/template-fidelity-convergence/public-checkout-and-inventory|the audit]] (Part A `/store/[slug]` row + the shared-vocabulary question); [[pop/specs/storefront-and-customization|storefront spec]] "Public redaction boundary" and "Public page and browser cart V1"; [[pop/specs/application-frontend-system|frontend spec]]; `DESIGN.md` "Public payment and storefront surfaces", "Shared component ownership", "State contract"; the 14.6.1 plan and its memory ledger (read before the first edit — it names the shell/rail/footer/outcome compositions to consume); `src/app/pay/[identifier]/**` as delivered; `src/components/ui/**` + [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md); `src/storefront/**`; `scripts/check-design-tokens.mjs` (read-only).
- **Owns:** `src/app/store/[slug]/page.tsx`, `src/app/store/[slug]/storefront-experience.tsx`, `src/app/store/[slug]/loading.tsx`, `src/app/store/[slug]/error.tsx`, `src/app/store/[slug]/page.test.tsx` and `src/app/store/[slug]/storefront-experience.test.tsx` (only to retire assertions on removed classes/props — never new coverage, never a run), `src/app/globals.css`, `src/i18n/dictionaries/storefront/en.ts`, `src/i18n/dictionaries/storefront/pt-BR.ts`.
- **May read:** every path listed under "Paths de entrada"; `src/app/(merchant)/settings/**`, `src/app/storefront-preview.tsx`, `src/app/storefront-settings-management.tsx` (only to confirm which `storefront-*` blocks stay).
- **Must not edit:** `src/app/store/[slug]/pay/**` (F02), `DESIGN.md` and `pop/specs/**` (F02), `src/components/ui/**`, `src/components/ui/inventory.json`, `src/checkout/**`, `src/storefront/**`, `src/app/pay/**`, every admin/merchant surface, `scripts/**`, `src/app/api/**`.
- **Depends on:** nenhuma (14.6.1 closed is the task-level dependency).
- **Entrada esperada:** 14.6.1's converged public checkout — merchant header/rail composition, `EmptyState` unavailable/error vocabulary, public footer with `LanguageSwitcher`, and the checkout dictionary keys for that shared furniture. Missing or unusable → `BLOCKED` with evidence; never fork a second rail/footer.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to keep one intention per diff and to name the retirement*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for token contract, anti-drift inventory and the six component states*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (compositions consumed from 14.6.1, CSS blocks removed, copy keys added per locale, deviations), evidence `pnpm typecheck` + `pnpm lint` output, status `concluída | BLOCKED`.
- **Critérios:** C1, C2, C5, C6, C7 do [[14.6.2-converge-storefront-and-standalone-pay.plan|plano]].

## Contrato de execução

- Consume, never re-create: the rail/identity (`Monogram` or the merchant logo + display name + trust line), the `EmptyState` vocabulary for unavailable/empty/error, and the public footer come from 14.6.1's owners.
- Retire from `globals.css` only the blocks the public routes stop using: `.receipt-rail*` and the public `.storefront-*` set (shell, rail, heading, introduction, products, card, price, group, product-*, stepper, custom-amount, cart, error, skeleton, logo, pay-footer, and the `[data-slot="empty"]` cart override). `.storefront-workspace*`, `.storefront-logo-block|current|actions|staged` and `.storefront-preview*` belong to the merchant settings surface — leave them untouched (14.5.3/14.7.1).
- Keep `<main data-theme-preview=… style={{ "--storefront-accent": storefront.accentColor } as CSSProperties}>` in `page.tsx` **verbatim**: it is the exact allowlisted expression in `scripts/check-design-tokens.mjs`, which this front may not edit.
- Replacement styling uses only the projected utilities (`bg-surface`, `text-text-2`, `rounded-card`, `shadow-card`, `max-w-app`, `font-display`, tone-soft pairs); no raw hex/px/rem/ch value and no new inline style.
- Both locales change together; storefront copy that already exists in the checkout dictionary is reused, not duplicated.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:**
- **Commit/artefato:**
- **Arquivos alterados:**
- **Desvios:**
- **Evidência:**
