# Frente F02 — Standalone pay reuse and contract lines — [[14.6.2-converge-storefront-and-standalone-pay]]

- **Entrega:** `/store/[slug]/pay` (page, experience, loading, error) rebuilt as a consumer of 14.6.1's payment-phase controller and outcome views under the same converged public shell as F01, plus the `DESIGN.md` and storefront-spec lines that record the converged public surface.
- **Escopo:** presentation and composition of the standalone payment route. The sessionless trust boundary is untouched: submit body stays exactly `{ idempotencyKey, amount, customer }`, the policy-exact required-field set stays derived from `checkoutDataPolicy`, capability polling stays on `POST /api/store/[slug]/checkout/status`, every unavailable cause stays the one opaque view, and the amount keeps the canonical grammar with exact money.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.6.2-converge-storefront-and-standalone-pay.md`; this slice; sections "Objetivo", "Estratégia", "Riscos", "Critérios" of [[14.6.2-converge-storefront-and-standalone-pay.plan|the plan]]; [[researches/template-fidelity-convergence/public-checkout-and-inventory|the audit]] (Part A `/store/[slug]/pay` row, "Key element gaps", tone drift lines); [[pop/specs/checkout-and-order-lifecycle|checkout spec]] "Standalone payment buyer experience"; [[pop/specs/storefront-and-customization|storefront spec]]; [[pop/specs/application-frontend-system|frontend spec]]; `DESIGN.md`; the 14.6.1 plan and memory ledger plus its delivered `src/app/pay/[identifier]/**` and any shared module it created (read before the first edit); F01's integrated diff; [`src/checkout/AGENTS.md`](../../../../../src/checkout/AGENTS.md); [`src/components/ui/AGENTS.md`](../../../../../src/components/ui/AGENTS.md); `src/components/ui/status-badge.tsx`.
- **Owns:** `src/app/store/[slug]/pay/page.tsx`, `src/app/store/[slug]/pay/standalone-payment-experience.tsx`, `src/app/store/[slug]/pay/loading.tsx`, `src/app/store/[slug]/pay/error.tsx`, `src/app/store/[slug]/pay/page.test.tsx` and `src/app/store/[slug]/pay/standalone-payment-experience.test.tsx` (only to retire assertions on removed classes/props — never new coverage, never a run), `DESIGN.md`, `pop/specs/storefront-and-customization.md`.
- **May read:** every path listed under "Paths de entrada"; `src/app/store/[slug]/*.tsx` and `src/app/globals.css` as F01 left them; `src/storefront/cart.ts`.
- **Must not edit:** `src/app/store/[slug]/*.tsx`, `src/app/globals.css`, `src/i18n/dictionaries/**` (F01 owns the storefront copy; a missing key goes back to the agente principal), `src/components/ui/**`, `src/components/ui/inventory.json`, `src/checkout/**`, `src/app/pay/**`, `src/app/api/**`, `scripts/**`, every other spec.
- **Depends on:** F01.
- **Entrada esperada:** F01's integrated storefront page, retired public BEM and storefront copy delta; 14.6.1's payment-phase controller (form yields to the payment phase, editing never discards an issued payment) and its eight outcome views. If those pieces exist but are welded to the V1/V2 presentation DTOs and cannot take standalone state, report `BLOCKED` with the exact evidence — duplicating the state machine or the outcome markup is out of contract.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use to delete the local fork instead of adapting it*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use for the state matrix, tone contract and anti-drift inventory*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff on the owned paths + report ≤15 lines (what was consumed from 14.6.1, what the local file stopped owning, spec/DESIGN lines written, deviations), evidence `pnpm typecheck` + `pnpm lint` output, status `concluída | BLOCKED`.
- **Critérios:** C1, C2, C3, C4, C6, C7, C8 do [[14.6.2-converge-storefront-and-standalone-pay.plan|plano]].

## Contrato de execução

- The local `paymentTone`/`paymentCopy` map goes away: provider states render through `ProviderStateBadge` with the owned tone map (`refunded` = neutral, `pending`/`created` = info), never a page-local tone.
- The unavailable view stops being `Card` + destructive `Alert` and adopts the same `EmptyState` vocabulary as the storefront and the checkout, keeping the return-to-store affordance and the single opaque cause.
- The page keeps `<main data-theme-preview=… style={{ "--storefront-accent": storefront.accentColor } as CSSProperties}>` **verbatim** (the allowlisted expression in `scripts/check-design-tokens.mjs`, which this front may not edit) and adopts the converged rail/footer composition inside it.
- Spec and `DESIGN.md` receive lines, not narration: one `Task 14.6.2` entry in the storefront spec's "Implemented slices" plus, when a requirement bullet stops describing the rendered surface, the minimal amendment; `DESIGN.md` updates "Public payment and storefront surfaces" to the converged shell and states.
- Creating a new shared component is out of scope: `src/components/ui/**` and `inventory.json` stay with 14.6.1/14.7 — a genuine need goes back to the agente principal.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:**
- **Commit/artefato:**
- **Arquivos alterados:**
- **Desvios:**
- **Evidência:**
