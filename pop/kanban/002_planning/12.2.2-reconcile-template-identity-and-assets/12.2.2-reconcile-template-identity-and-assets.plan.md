# Plan — [[12.2.2-reconcile-template-identity-and-assets]]

- **Stage:** 002_planning · **Owner:** agent planner

## Objective and expected result

- **Objective:** replace the superseded settlement-mark family with the approved template identity and install all supplied application-owned artwork through one deterministic, licensed, safe, hash-bound asset pipeline.
- **Observable result:** inline identity, generated static lockups, illustrations, fallbacks, six theme swatches, and 16/32/48 favicon frames reproduce their approved targets; drift, unsafe SVG, unknown bytes, incomplete provenance, or merchant-media crossover fails closed.

## Strategy

Keep `docs/template/**` immutable and non-runtime. Bind each of its 17 SVGs to its parity obligation, byte length, SHA-256, intrinsic geometry, role, source commit, and exact project-use authorization; do not claim authorship, ownership, or license terms beyond recorded evidence. Canonical production sources live under `src/brand/`; generated public derivatives and their manifest remain generator-owned.

Preserve the four public identity IDs to avoid consumer API churn, but replace their geometry/composition with the template QR glyph, Sora wordmark outlines supplied by 12.2.1, and the supplied merchant/store fallback. Once no source or manifest references IBM Plex Sans, remove that transient dependency from the manifest/lockfile. Emit other artwork under a distinct application-asset namespace; it is never merchant media. Use an element/attribute allowlist, local-only references, deterministic serialization, closed output inventory, and byte/hash/dimension checks.

Visual proof is asset-scoped: a fresh Chromium contact sheet compares source targets with production derivatives at intrinsic and representative UI sizes, both on-color modes and all six swatches. Route migrations and the final design-system specimen stay with later tasks.

## Affected areas

- `src/brand/**`, `scripts/{brand-contract,generate-brand-assets}*` — canonical identity, source register, generation, checks, and DOX.
- `public/brand/**`, `public/application-assets/**`, `src/app/favicon.ico` — generated outputs only; `package.json`/`pnpm-lock.yaml` are F01-only for obsolete IBM Plex Sans removal after reference elimination.
- `tests/**`, `scripts/*brand-assets-evidence*`, `artifacts/brand-assets/**` — fresh visual/runtime-independent evidence without package-script ownership.

## Gaps and preflight

- **Execution input:** 12.2.1 must provide pinned licensed Sora bytes/outlines and canonical six-theme token values; absence/incompatibility is `BLOCKED`, not permission to use live text, Google Fonts, IBM Plex substitution, or copied palette authority.
- **Observed baseline:** `node scripts/generate-brand-assets.mjs --check` verified 12 current assets; clean-worktree `pnpm` could not open its dependency-store SQLite and no local Vitest existed. Executor must install/link the pinned environment before tests.

## Execution fronts

### F01 — Canonical source and identity
- **Delivery:** exact source/provenance register, new QR geometry/Sora outline composition, closed inline identities, and fallback semantics.
- **Contract:** [[12.2.2-reconcile-template-identity-and-assets.g01-canonical-identity]] · **Owns:** named canonical/component files under `src/brand/`, plus package manifest/lock only for obsolete IBM dependency removal · **Depends on:** 12.2.1.
- **Criteria:** C01, C02, C06.

### F02 — Deterministic derivatives and contract
- **Delivery:** fail-closed generator/checker, complete manifest, application artwork, static variants, swatches, and favicon outputs.
- **Contract:** [[12.2.2-reconcile-template-identity-and-assets.g02-generated-assets]] · **Owns:** generator/checker, manifest, generated public outputs, favicon, brand DOX · **Depends on:** F01.
- **Criteria:** C01, C03, C04, C05, C06.

### F03 — Independent evidence
- **Delivery:** mutation tests plus fresh run-bound Chromium comparison/contact-sheet evidence without migrating route UI.
- **Contract:** [[12.2.2-reconcile-template-identity-and-assets.g03-asset-evidence]] · **Owns:** focused tests/evidence scripts, package scripts, brand-assets artifacts · **Depends on:** F02.
- **Criteria:** C02-C08.

## Order and topology

1. **Wave 1:** F01 after 12.2.1 is integrated.
2. **Wave 2:** F02 consumes F01's closed canonical inventory.
3. **Wave 3:** F03 independently exercises the integrated outputs.
4. **Integration:** sequential specialists; ownership is disjoint and each dependency is inspected before the next front.

## Risks and abort conditions

- Source approval is not an ownership claim; missing or contradictory usage authority blocks only the affected asset lane and must identify the exact record.
- Abort on unavailable licensed Sora derivation, mismatch with the 17 source hashes/parity IDs, unsafe/unparseable SVG, non-deterministic output, or need to edit `src/media/**`, DB/API/security, route consumers, tokens, or template bytes.

## Acceptance criteria

| # | Criterion | Verification | Pass is | 005 mode |
|---|---|---|---|---|
| C01 | Closed provenance/source inventory | `pnpm brand:check` + inspect manifest | all 17 unique source records bind full hash, bytes, SVG/viewBox, parity ID, role, source commit and evidence-backed project-use grant; mutations/unknowns fail | re-run |
| C02 | Faithful accessible identity | focused brand component/contract tests + Chromium evidence | QR glyph and Sora lockups match target; no live font in static SVG; decorative/standalone names and four identity IDs remain exact | re-run |
| C03 | Safe deterministic family | generate twice, `pnpm brand:check`, mutation tests | complete declared outputs are byte-stable; unsafe elements/attributes, external refs, text/fonts, duplicates, drift, or missing files fail | re-run |
| C04 | Coherent favicon | ICO/frame parser + raster evidence | exact 16/32/48 PNG frames derive from the new QR glyph, are legible, and bind dimensions/hashes | re-run |
| C05 | Complete artwork and swatches | manifest assertions + visual comparison | texture, illustrations, avatar/product/store fallbacks and six exact theme-ID swatches have one application owner and faithful outputs | evidence |
| C06 | Ownership/accessibility boundary | focused tests + `git diff --name-only` | app fallbacks never imply merchant ownership or enter media lifecycle; obsolete IBM package/lock entries disappear only after zero references; no `src/media`, route, DB/API, auth/security, template, token, or 12.2.1 font-source edits | re-run |
| C07 | Fresh visual evidence | direct run and verifier scripts | run-bound Chromium contact sheets cover 320/375/768/1440, positive/reversed identity, all assets/swatches, no external requests/console failures, and source-output raster tolerance | evidence |
| C08 | Aggregate regression gates | `pnpm frontend-parity:check && pnpm check && git diff --check` | parity inventory remains valid and project gate/diff pass at exact task HEAD | re-run |

## Specs and contracts

- [[specs/application-frontend-system|Application frontend system]] — implement its approved target; no new durable behavior or planned spec is required.
- [[specs/media-storage|Media storage]] — preserve merchant-owned JPEG/PNG/WebP lifecycle; no change expected.
- [`src/brand/AGENTS.md`](../../../../../../src/brand/AGENTS.md) — replace superseded geometry/output rules with the delivered single-owner contract.
- [[specs/administrative-design-system|Administrative design system]] — superseded history only; do not reactivate or edit.

## Execution topology
- **Shape:** sequential specialists (F01 → F02 → F03).
- **Rationale:** canonical geometry/provenance feeds generation, and only integrated bytes can be independently tested; separate write sets prevent generator/evidence drift.
