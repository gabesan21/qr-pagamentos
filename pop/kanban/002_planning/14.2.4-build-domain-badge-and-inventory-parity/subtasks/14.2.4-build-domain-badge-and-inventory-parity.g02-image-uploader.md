# Frente F02 — Owned `ImageUploader` — [[14.2.4-build-domain-badge-and-inventory-parity]]

- **Entrega:** `src/components/ui/image-uploader.tsx`, o primeiro owner de upload de imagem da aplicação: tile de drop, preview do arquivo staged, remoção, falha e retry.
- **Escopo:** só o componente compartilhado. **Nenhum caller é migrado** — `catalog-fields.tsx` e `storefront-settings-management.tsx` seguem intactos (adoção é 14.4–14.6).
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** card, seções "Objetivo", "Estratégia" e "Critérios de aceite" do [[14.2.4-build-domain-badge-and-inventory-parity.plan|plano]], esta fatia, `pop/specs/media-storage.md`, `pop/specs/application-frontend-system.md`, `src/components/ui/AGENTS.md`, `src/media/AGENTS.md`, `DESIGN.md`.
- **Owns:** `src/components/ui/image-uploader.tsx` (arquivo novo).
- **May read:** `docs/template/app/src/components/ui/ImageUploader.tsx`, `src/app/(merchant)/catalog/catalog-fields.tsx`, `src/app/storefront-settings-management.tsx`, `src/components/ui/{button,field,input,spinner,empty-state}.tsx`.
- **Must not edit:** tudo fora de `Owns` — `src/app/**`, `src/media/**`, `inventory.json`, qualquer `.test.tsx`, os owners de F01/F03. `inventory.json` e o teste de inventário são de F04.
- **Depends on:** nenhuma.
- **Entrada esperada:** as utilidades de token de 14.2.1 (`rounded-card`, `bg-surface-2`, `bg-accent-soft`, `text-text-3`, `shadow-card`); nome ausente → papel semântico equivalente, registrado na saída.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para declarar entrada/saída/erros antes de codar*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use para os seis estados, tokens e as leis numéricas de alvo*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** o arquivo novo + relato ≤15 linhas, evidência `pnpm typecheck` e `pnpm lint`, status `concluída | BLOCKED`.
- **Critérios:** C03, C10.

## Contrato de execução

- **A rota nunca entra no componente.** O caller injeta a função de staging (recebe o `File`, devolve o que a página precisa para exibir e referenciar o objeto staged). Nenhuma string `/products/images` ou `/storefront/logo`, nenhum `fetch` literal, nenhum identificador interno, revisão ou storage key — a fronteira de `pop/specs/media-storage.md` é do servidor.
- **Estados obrigatórios e distinguíveis sem cor:** idle (tile tracejado), drag-over, staging pendente, staged com preview e botão de remover, e falha com retry acionável; `disabled` distinto.
- **Acessibilidade:** o alvo de abertura é um controle nativo rotulado (não um `<button>` com `<input type="file">` aninhado, como no template, que é HTML inválido); operável por teclado; alvos ≥44×44px; foco visível; progresso e falha anunciados em live region (`status` polite / `alert` assertive).
- Aceite apenas os tipos que o servidor aceita (JPEG, PNG, WebP) na dica de `accept`; a validação real permanece do servidor e o componente nunca promete aprovação.
- Sem `dark:`, sem `style=`, sem hex/px/rem/em/ch; `"use client"` no topo, justificado pelo estado de arrasto e de staging.
- Entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
