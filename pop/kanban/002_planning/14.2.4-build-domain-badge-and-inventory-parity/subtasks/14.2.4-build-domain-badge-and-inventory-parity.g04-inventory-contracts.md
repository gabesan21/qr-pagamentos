# Frente F04 — Inventory, check script and contracts — [[14.2.4-build-domain-badge-and-inventory-parity]]

- **Entrega:** o inventário executável, seu validador, o DOX e a spec passam a descrever exatamente o que F01–F03 entregaram, com os dois source-checks verdes.
- **Escopo:** registro de contrato. Nenhum componente é implementado nem corrigido aqui.
- **Responsável:** agent · **Papel:** `pop-executor`.
- **Paths de entrada:** card, seções "Estratégia", "Critérios de aceite" e "Specs e contratos" do [[14.2.4-build-domain-badge-and-inventory-parity.plan|plano]], esta fatia, as três fatias de F01–F03 e seus diffs integrados, `pop/specs/application-frontend-system.md`.
- **Owns:** `src/components/ui/inventory.json`, `scripts/check-shared-ui-inventory.mjs`, `src/components/ui/primitive-inventory.test.tsx` (**somente** a asserção de conjunto de arquivos), `src/components/ui/AGENTS.md`, `DESIGN.md`, `pop/specs/application-frontend-system.md`.
- **May read:** os seis owners tocados por F01–F03, `scripts/check-design-system-coverage.mjs`, `src/app/design-system/coverage.ts`, `docs/frontend-template-parity/obligations.ndjson`.
- **Must not edit:** `docs/**` (autoridade de parity imutável), `package.json`, `src/app/**` — inclusive `coverage.ts`, cuja mudança exigiria regenerar a evidência do specimen —, os owners de F01–F03 e qualquer outro `.test.tsx`.
- **Depends on:** F01, F02, F03.
- **Entrada esperada:** os diffs integrados das três frentes, com a API pública e os estados finais. Faltando qualquer uma, responda `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para o contrato antes de alterar o validador*; [[.agents/skills/ui-change/SKILL|ui-change]] — *use para a seção 4, inventário anti-drift*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** diff nos arquivos owned + relato ≤20 linhas, evidência da saída literal de `node scripts/check-shared-ui-inventory.mjs` e `pnpm design-system:source-check`, status `concluída | BLOCKED`.
- **Critérios:** C06, C07, C08, C09, C10, C11.

## Contrato de execução

- **`owners` é intocável em cardinalidade.** As 20 entradas continuam mapeando 1:1 as obrigações de parity; só a `publicApi` e os `states` das entradas de F01/F03 crescem. Colocar o `ImageUploader` ali quebra três verificações e a exclusão de parity — é a condição de aborto do plano.
- **`localAdditions`** é a seção nova: um registro por componente local, com `owner`, `publicApi`, `states`, `notApplicable` e uma `insufficiency` de uma linha explicando por que o inventário existente não bastava. `check-shared-ui-inventory.mjs` passa a validar essa seção — arquivo existe, contrato completo, `insufficiency` substantiva — e a recusar owner que colida com `owners` ou com uma fonte excluída. Toda asserção atual permanece.
- **`primitive-inventory.test.tsx`:** apenas a asserção que compara o conjunto de `.tsx` do diretório com primitivos ∪ composições passa a considerar `localAdditions`; a contagem literal de composições acompanha. Nenhuma outra asserção, nenhum outro teste.
- **DOX e spec:** `src/components/ui/AGENTS.md` acrescenta as novas fronteiras de cliente (uploader e geração de QR) e a regra de adição local; `DESIGN.md` atualiza "Shared component ownership and anti-drift" e a matriz de estados com famílias de badge, uploader, sparkline, variante compacta e QR gerado; a spec do frontend substitui a frase "No upload component is admitted by this inventory" pela regra de adição local, **sem** tocar na exclusão `excluded-unreachable-generated-ui` nem nos números de 12.2.3.
- Registre no relato que os hashes de fonte de `design-system:evidence:verify` ficaram obsoletos e que a regeneração é o critério C11, da task de verificação da phase.
- Entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; nunca implementar, simular ou corrigir dependência.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao agente principal.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <gate ou observação relevante>.
