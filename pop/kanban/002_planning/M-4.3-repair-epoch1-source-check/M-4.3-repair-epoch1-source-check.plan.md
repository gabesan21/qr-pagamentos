# Plan — M-4.3-repair-epoch1-source-check

## Objective

`pnpm epoch1:source-check` fails on `develop` with ENOENT on `src/app/page.tsx`
(home moved to `src/app/(merchant)/page.tsx` in the Epoch 6 panel rebuild).
Repair the script's stale path/authorization expectations without weakening any
invariant that still matters.

## Recon findings

- Reproduced on `develop`: `pnpm epoch1:source-check` exits 1, ENOENT
  `src/app/page.tsx`. All other read targets exist:
  `src/app/login/{page.tsx,login-submit.tsx,submit/route.ts}`,
  `tests/login.evidence.spec.ts`, `src/app/layout.tsx`,
  `src/app/design-system/page.tsx`, `src/app/language-preference/route.ts`.
- The "home page rechecks the active principal" invariant still matters but
  moved and changed shape: `src/app/(merchant)/page.tsx` →
  `requireMerchantShellContext()` (`src/app/(merchant)/shell-context.ts`) →
  `requireOwnerFromCookie()` (`src/app/owner-guard.ts`) →
  `getAuthorizationService().requireUser(...)`, which wraps
  `resolve()` (status-aware; `src/auth/authorization.ts:33`). No
  `getSessionService().validate(` bypass anywhere in the chain.
- **Every other expectation is still correct** (eslint worktrees ignore,
  control-path visibility, login native POST form, submit listener
  add/remove + forbidden patterns, login evidence click/enter/pending,
  authorization recheck on login route / root layout / design-system page /
  language route — all still contain `getAuthorizationService().resolve(`).
- M-4.2 is **not yet integrated** on develop. Its branch
  (`task/M-4.2-exclude-worktrees-from-tooling`) adds a tsconfig worktrees
  exclusion `requireSource` to the same script and touches the same
  `Promise.all` destructuring line and final `console.log` — the executor must
  re-check develop at execution time and preserve M-4.2's additions if merged
  (keep the tsconfig read, its assertion, and `tsconfig_worktrees=excluded` in
  the OK line).
- No `scripts/check-epoch1-source.test.ts` exists. Do not create one; the
  mutation probe below is the regression evidence (keeps the change minimal).

## Front (single, cohesive)

Owns: `scripts/check-epoch1-source.mjs` only. No `src/` edits — the
investigation proved the code right and the script's path list outdated; no
assertion proved application code wrong.

1. Replace the `src/app/page.tsx` read with three reads:
   `src/app/(merchant)/page.tsx` (merchantHomePage),
   `src/app/(merchant)/shell-context.ts` (merchantShellContext),
   `src/app/owner-guard.ts` (ownerGuard).
2. Replace the "home page" entry in the authorization loop with a relocation
   chain of `requireSource` assertions:
   - merchant home page still delegates principal recheck:
     `requireMerchantShellContext\(` present;
   - shell context still routes through the owner guard:
     `requireOwnerFromCookie\(` present;
   - owner guard still performs status-aware authorization:
     `getAuthorizationService\(\)\.requireUser\(` present and
     `getSessionService\(\)\.validate\(` absent (reuse the existing
     forbidden-bypass message style).
   Keep the loop for login route / root layout / design-system page /
   language route unchanged.
3. Keep `homePage` variable name usage consistent (rename to something like
   `merchantHomePage`); keep all other assertions byte-identical.
4. If M-4.2 has integrated by execution time, apply this change on top of its
   tsconfig additions without dropping them.

## Acceptance

- `pnpm epoch1:source-check` exits 0 on develop (OK line printed).
- Mutation probe (revert after): temporarily remove the
  `requireMerchantShellContext(` call from `src/app/(merchant)/page.tsx` (or
  `requireUser(` from `owner-guard.ts`) → script must fail with the
  corresponding message; restore the file and re-verify exit 0.
- `CI=true pnpm check` green (lint + typecheck + test + build).
- `pop_validate` valid.
- No changes under `src/`.

## Risks

- Merge race with M-4.2's branch on the same script — mitigated by step 4.
- Over-tightening: assert only the delegation/call shapes proven above, not
  incidental file contents.
