# Frente F04 — Contracts and documentation — [[14.4.3-converge-admin-accounts-and-settings]]

- **Entrega:** `administrative-foundation` and `DESIGN.md` synced to the delivered composition, including the create-redirect decision, §4 operability and the three recorded gaps.
- **Escopo:** durable contracts and design memory only. No application code, no dictionary, no new spec.
- **Responsável:** agent.
- **Papel:** `pop-executor`.
- **Paths de entrada:** card `14.4.3-converge-admin-accounts-and-settings.md`; `…plan.md` (Estratégia, Critérios); this slice; the three `Resultado` sections of F01–F03; the integrated diff.
- **Owns:** `pop/specs/administrative-foundation.md`, `DESIGN.md`.
- **May read:** the whole integrated diff of this task, `pop/researches/template-fidelity-convergence/admin-area.md`, `pop/specs/{identity-security,storefront-and-customization,application-frontend-system}.md`, `src/components/ui/AGENTS.md`, `src/data-directory/AGENTS.md`.
- **Must not edit:** every `src/**` path, every other spec, every DOX `AGENTS.md`, `pop/memory/**`, `pop/roadmap/**`, the card and the plan.
- **Depends on:** F01, F02, F03.
- **Entrada esperada:** the three fronts integrated with their `Resultado` filled in; missing or contradictory → `BLOCKED`.
- **Skills:** [[.agents/skills/clean-code-change/SKILL|clean-code-change]] — *use para manter o contrato legível e mínimo*.
- **Web:** deny.
- **Gate/delta:** nenhum.
- **Saída:** spec and `DESIGN.md` diff + a `Resultado` section here, teto 12 linhas, evidência wikilinks das seções alteradas, status `concluída | BLOCKED`.
- **Critérios:** C11, C12, C13 do [[14.4.3-converge-admin-accounts-and-settings.plan|plano]].

## Contrato de execução

- Amend the existing administrator paragraphs in `administrative-foundation`; do not open a new spec and do not restate what the delivered text already says. The amendment names: the modal-driven create surface on `/admin/accounts`; the `POST /admin/users` redirect now landing on `/admin/accounts?success=created|error=create-failed` (the redirect map sentence and the delivered-destination rule); the `/admin/accounts/[id]` editor as five tabs over the same byte-frozen routes, whose outcomes still land on `/admin`; the deleted-target banner; and `/admin/settings` §4 as an operable administrator control over the existing singleton mutation.
- Record the three gaps as explicit open work, each with its cause in one clause: initial status (creation always writes `ACTIVE`), §1 UUID columns and inactive rows (`listMappings` is active-only and redacted to `{code,label}`, no activate intent), store-column slug (`AdminUserSummary` has no `storefrontSlug`). A gap written as a promise the code does not keep is a spec defect — write them as pending, not as delivered.
- Note the two feedback boundaries the fronts hit: the frozen role/status/password/delete outcomes toast on `/admin`, and the shared `catalog-*` notice cannot be attributed to §2 or §3 without a new vocabulary.
- `DESIGN.md` gains the admin-local `SegmentedControl` in the inventory with its six applicable states and the one-line reason (role boundary forbids importing the merchant control), plus the tabbed-editor and per-section-feedback composition rules. Do not restate token values already in the file.
- Keep both files inside their line rulers and every internal reference as a wikilink or repo-relative link.
- **Saber parar:** no máximo 2 tentativas quando a falha de um critério `agent` for de ambiente; na segunda registre `ambiente` e siga.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao agente principal com evidência; não implementar dependência nem alargar `Owns`.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do agente principal>.
- **Evidência:** <seções alteradas na spec e no `DESIGN.md`>.
