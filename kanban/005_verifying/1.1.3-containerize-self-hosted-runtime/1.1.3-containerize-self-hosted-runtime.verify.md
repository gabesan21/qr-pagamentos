# Verification — [[1.1.3-containerize-self-hosted-runtime]]

- **Stage:** 005_verifying · **Owner:** agent (+ user because `critical: true`)
- **Verifier profile:** requested `gpt-5.6-terra medium` was not selectable; verification ran in the available delegated Codex agent.
- **Committed HEAD:** `f2e96567d71c39d200036fe3f80bc6435e48ef5a`
- **Base:** `8ab3f72b5449620c6716578ca6aa955978898e88` (`origin/develop` merge-base)
- **Initial state:** correct task branch and clean worktree.

## Round 1 — 2026-07-14

| # | Criterion | Run executed | Result | Evidence |
|---|-----------|--------------|--------|----------|
| 1 | Exact standalone, non-root, secret-free images and live digest equality | `pnpm container:contract-check && pnpm container:test --clean-clone --scenario build` | ✅ passed | Live/committed indices matched: Node `sha256:cb4e8f7c443347358b7875e717c29e27bf9befc8f5a26cf18af3c3dec80e58c5`, amd64 child `sha256:0778d035a13f3f3833b7f2cb750e0df6cbce45583e84fd822f499f0c902a6c74`; PostgreSQL `sha256:b403bafdebf3f7027c15ceefbfc46899f575096549f549650b3fb2ab9a6a6930`, amd64 child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`. `PASS image-contract`; bootstrap/migrate/app were `1000:1000`, sized 295678987/295678985/250887489 bytes. |
| 2 | Compose secret identities, storage/network isolation, and four gates | `pnpm container:test --clean-clone --scenario config` | ✅ passed | `PASS source-secret-permissions`, `PASS staged-secret-permissions`, `PASS compose-config`; the executable model checks DB-only original admin grant and healthy/completed dependency conditions. |
| 3 | Clean build, startup, migration, runtime preflight, assets, and liveness | `pnpm container:test --clean-clone --scenario happy` (executed twice) | ✅ passed twice | Both fresh runs printed `PASS clean-build`, `db-secret-consumed-once`, `postgres-server-nonroot`, `bootstrap`, `migration`, `runtime-db-preflight`, `app-liveness`, and `static-assets`. |
| 4 | Inherited database contracts and Compose role separation | `pnpm db:test && pnpm container:test --clean-clone --scenario roles` | ✅ passed | DB: `PASS migrator-connect`, `migration-replay`, `migration-idempotence`, `foundation-schema`, `runtime-crud`, `constraint-sqlstates`, `role-separation`, `runtime-denials`. Compose: `PASS compose-role-separation`; runtime DDL/metadata/`SET ROLE` probes were denied and migrator was non-admin. |
| 5 | Reserved-character encoding and real fail-closed redacted authentication | `pnpm container:test --clean-clone --scenario failures` | ✅ passed | `PASS sql-escaping`, `PASS url-percent-encoding`, and `PASS auth-fail-closed` for admin, migrator, and runtime; harness asserted nonzero failures and absence of raw/encoded sentinels and database URLs in output/config/logs. |
| 6 | Deterministic rerun/restart, job invariance, PostgreSQL 18 persistence, cleanup | `pnpm container:test --clean-clone --scenario lifecycle` (executed twice) | ✅ passed twice | Both runs printed `PASS migration-noop`, `app-restart-runtime-preflight`, `app-restart-no-job-rerun`, `persistent-restart`, and `cleanup`; harness compares job IDs, exits, start/finish timestamps and repeats app preflight while retaining the named volume. |
| 7 | Initialization/runtime identities, secret/log isolation, and host exposure | `pnpm container:test --clean-clone --scenario isolation` | ✅ passed | `PASS db-initialization-identity`, `db-secret-consumed-once`, `postgres-server-nonroot`, `secret-isolation`, `no-secret-logs`, `port-isolation`; only app was loopback-published and DB had no host port. |
| 8 | Quality, committed-range documentation, DOX, specs, and whitespace | `BASE=$(git merge-base HEAD origin/develop) && pnpm check && pnpm db:contract-check && pnpm container:contract-check --docs "$BASE..HEAD" && python3 scripts/pop_validate.py && git diff --check "$BASE"..HEAD` | ❌ failed | `pnpm check` passed (4 tests passed, 1 skipped; build emitted `/pt-BR`, `/en`, `/api/health`), `db:contract-check` printed `PASS documentation-contract`, and live digest/image checks passed. The required run then exited 1: `Error: --docs requires a committed Git range`. It expands to `8ab3f72b5449620c6716578ca6aa955978898e88..HEAD`, while the checker regex accepts only hex SHA endpoints. Because of `&&`, PoP and whitespace checks were not reached by the exact run. |

### Criterion 8 diagnostic

No application change was made. Resolving both endpoints first and rerunning read-only:

```sh
BASE=$(git merge-base HEAD origin/develop) && TIP=$(git rev-parse HEAD) && pnpm container:contract-check --docs "$BASE..$TIP" && python3 scripts/pop_validate.py && git diff --check "$BASE".."$TIP"
```

exited 0 with `PASS container-documentation`, `PASS dox-contract`, `Vault válido — nenhuma violação encontrada.`, and a silent clean whitespace check. This isolates the failure to disagreement between the approved exact run (`..HEAD`) and the checker's SHA-only range validation; it does not make the exact acceptance run pass.

### Cleanup and integrity

- Every completed clean-clone scenario exited 0 through its `finally` cleanup: `down --volumes --remove-orphans --rmi local`, temporary source/staged secret removal, and clean-clone archive removal.
- Final Docker inventory filtered by the harness `qrct` prefix was empty for containers, networks, volumes, and images; `/tmp` had no `qr-container-clone-*` or `qrct*` remnants.
- Final application worktree remained clean at `f2e96567d71c39d200036fe3f80bc6435e48ef5a` before writing this task-side evidence file.
- Tracked secret-name scan found only `.env.compose.example`, `.env.example`, and the staging helper; no usable credential was found. Harness redaction scans passed.
- Host commands emitted a Node engine warning because the host is v26.4.0 while the repository requires 24.18.0; the exact digest-pinned Node 24.18.0 clean-clone images built and passed. This warning was not the criterion-8 failure.

**Automated verdict:** rejected → return to `004_processing`. Criterion 8's exact approved command must pass at a committed HEAD before a new independent 005 round. The task remains at the human gate and must not advance to 006.

## Human critical verification

Do not perform or check this gate until criterion 8 is repaired and a fresh automated 005 round passes. Then, from the task worktree, use only disposable files and resources:

```sh
export VERIFY_DIR="$(mktemp -d)"
export COMPOSE_PROJECT_NAME="qr-human-113-$(date +%s)"
export APP_PORT=33013
umask 077
openssl rand -base64 48 > "$VERIFY_DIR/postgres-admin-password"
openssl rand -base64 48 > "$VERIFY_DIR/qr-migrator-password"
openssl rand -base64 48 > "$VERIFY_DIR/qr-runtime-password"
chmod 0600 "$VERIFY_DIR"/*
printf 'APP_PORT=%s\nPOSTGRES_ADMIN_PASSWORD_FILE=%s\nMIGRATOR_PASSWORD_FILE=%s\nRUNTIME_PASSWORD_FILE=%s\nSTAGED_SECRETS_DIR=%s\n' \
  "$APP_PORT" \
  "$VERIFY_DIR/postgres-admin-password" \
  "$VERIFY_DIR/qr-migrator-password" \
  "$VERIFY_DIR/qr-runtime-password" \
  "$VERIFY_DIR/staged" > .env.compose
chmod 0600 .env.compose
pnpm container:prepare-secrets -- --env-file .env.compose
docker compose --env-file .env.compose build --pull
docker compose --env-file .env.compose up -d
docker compose --env-file .env.compose ps
docker compose --env-file .env.compose logs --no-color bootstrap migrate app
```

Wait for liveness, then verify both locales and the exact health response:

```sh
until curl -fsS "http://127.0.0.1:${APP_PORT}/api/health" >/dev/null; do sleep 1; done
curl -fsS "http://127.0.0.1:${APP_PORT}/pt-BR" >/dev/null
curl -fsS "http://127.0.0.1:${APP_PORT}/en" >/dev/null
test "$(curl -fsS "http://127.0.0.1:${APP_PORT}/api/health")" = '{"status":"ok"}'
```

Verify successful one-shot jobs, non-root users, and loopback-only publication:

```sh
BOOTSTRAP_ID=$(docker compose --env-file .env.compose ps -a -q bootstrap)
MIGRATE_ID=$(docker compose --env-file .env.compose ps -a -q migrate)
test "$(docker inspect --format '{{.State.ExitCode}}' "$BOOTSTRAP_ID")" = 0
test "$(docker inspect --format '{{.State.ExitCode}}' "$MIGRATE_ID")" = 0
test "$(docker compose --env-file .env.compose exec -T app id -u)" = 1000
docker compose --env-file .env.compose exec -T db sh -eu -c 'uid=$(awk "/^Uid:/{print \$2}" /proc/1/status); test "$uid" -gt 0'
test "$(docker compose --env-file .env.compose port app 3000)" = "127.0.0.1:${APP_PORT}"
test -z "$(docker compose --env-file .env.compose port db 5432 2>/dev/null || true)"
```

Prove an app-only restart repeats runtime preflight without changing either job:

```sh
JOBS_BEFORE=$(docker inspect --format '{{.Id}}|{{.State.ExitCode}}|{{.State.StartedAt}}|{{.State.FinishedAt}}' "$BOOTSTRAP_ID" "$MIGRATE_ID")
PREFLIGHT_BEFORE=$(docker compose --env-file .env.compose logs --no-color app | grep -c 'PASS runtime-db-preflight')
docker compose --env-file .env.compose restart app
until curl -fsS "http://127.0.0.1:${APP_PORT}/api/health" >/dev/null; do sleep 1; done
PREFLIGHT_AFTER=$(docker compose --env-file .env.compose logs --no-color app | grep -c 'PASS runtime-db-preflight')
JOBS_AFTER=$(docker inspect --format '{{.Id}}|{{.State.ExitCode}}|{{.State.StartedAt}}|{{.State.FinishedAt}}' "$BOOTSTRAP_ID" "$MIGRATE_ID")
test "$PREFLIGHT_AFTER" -gt "$PREFLIGHT_BEFORE"
test "$JOBS_AFTER" = "$JOBS_BEFORE"
```

Finally, destroy only this disposable project and prove no resources/files remain:

```sh
docker compose --env-file .env.compose down --volumes --remove-orphans --rmi local
pnpm container:prepare-secrets -- --env-file .env.compose --clean
rm -f .env.compose
rm -rf "$VERIFY_DIR"
test -z "$(docker ps -aq --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker network ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker volume ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test -z "$(docker image ls -q --filter "label=com.docker.compose.project=${COMPOSE_PROJECT_NAME}")"
test ! -e .env.compose
test ! -e "$VERIFY_DIR"
```

- [ ] Done

## Round 2 — post-range repair — 2026-07-14

- **Fresh verifier profile:** requested `gpt-5.6-terra medium` was not selectable; this round ran in a new available Codex agent distinct from planning, execution, and Round 1 verification.
- **Committed HEAD:** `796b541f7da0d5096bb47fa13616624951816183`
- **Base:** `8ab3f72b5449620c6716578ca6aa955978898e88` (`origin/develop` merge-base)
- **Initial state:** correct `task/1.1.3-containerize-self-hosted-runtime` branch, three commits ahead of `origin/develop`, with a clean worktree.

| # | Criterion | Exact run executed | Result | Evidence |
|---|-----------|--------------------|--------|----------|
| 1 | Exact standalone, non-root, secret-free images and live digest equality | `pnpm container:contract-check && pnpm container:test --clean-clone --scenario build` | ❌ failed | The first command failed closed before the build scenario: live Node index `sha256:39a4259b6f744868a8228742ad45aa3026f97302e5eec2fa4a38b30ca0a66e12` differs from committed `sha256:cb4e8f7c443347358b7875e717c29e27bf9befc8f5a26cf18af3c3dec80e58c5`. Independent live inspection found current linux/amd64 child `sha256:d45d78e7929b46875bbd4e29bea672d5bc48186c6c3588306521c815e78352d6`. PostgreSQL still matches committed/live index `sha256:b403bafdebf3f7027c15ceefbfc46899f575096549f549650b3fb2ab9a6a6930`, linux/amd64 child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`. Supplemental standalone build execution passed: bootstrap `1000:1000`/295678987 bytes, migrate `1000:1000`/295678985 bytes, app `1000:1000`/250887475 bytes. |
| 2 | Compose secret identities, storage/network isolation, and four gates | `pnpm container:test --clean-clone --scenario config` | ✅ passed | `PASS source-secret-permissions`, `PASS staged-secret-permissions`, `PASS compose-config`. |
| 3 | Clean build, startup, migration, runtime preflight, assets, and liveness | `pnpm container:test --clean-clone --scenario happy` (executed twice) | ✅ passed twice | Both fresh runs printed `PASS clean-build`, `db-secret-consumed-once`, `postgres-server-nonroot`, `bootstrap`, `migration`, `runtime-db-preflight`, `app-liveness`, and `static-assets`. |
| 4 | Inherited database contracts and Compose role separation | `pnpm db:test && pnpm container:test --clean-clone --scenario roles` | ✅ passed | DB printed all eight required lines: `migrator-connect`, `migration-replay`, `migration-idempotence`, `foundation-schema`, `runtime-crud`, `constraint-sqlstates`, `role-separation`, `runtime-denials`. The container run printed its happy prerequisites and `PASS compose-role-separation`. |
| 5 | Reserved-character encoding and real fail-closed redacted authentication | `pnpm container:test --clean-clone --scenario failures` | ✅ passed | `PASS sql-escaping`, `PASS url-percent-encoding`, and `PASS auth-fail-closed` for admin, migrator, and runtime, after the clean startup prerequisites. |
| 6 | Deterministic rerun/restart, job invariance, PostgreSQL 18 persistence, cleanup | `pnpm container:test --clean-clone --scenario lifecycle` (executed twice) | ✅ passed twice after one infrastructure retry | Both completed fresh runs printed `PASS migration-noop`, `app-restart-runtime-preflight`, `app-restart-no-job-rerun`, `persistent-restart`, and `cleanup`. An earlier pre-assertion attempt was interrupted by a Docker BuildKit RPC `connection reset by peer`; daemon 29.6.1 recovered, the interrupted project's inventory was empty, and two complete reruns then passed. |
| 7 | Initialization/runtime identities, secret/log isolation, and host exposure | `pnpm container:test --clean-clone --scenario isolation` | ✅ passed | `PASS db-initialization-identity`, `db-secret-consumed-once`, `postgres-server-nonroot`, `secret-isolation`, `no-secret-logs`, and `port-isolation`, after the clean startup prerequisites. |
| 8 | Quality, committed-range documentation, DOX, specs, and whitespace | `BASE=$(git merge-base HEAD origin/develop) && pnpm check && pnpm db:contract-check && pnpm container:contract-check --docs "$BASE..HEAD" && python3 scripts/pop_validate.py && git diff --check "$BASE"..HEAD` | ❌ failed | The unchanged exact command no longer reports `--docs requires a committed Git range`. `pnpm check` passed (4 tests passed, 1 skipped; production build emitted `/pt-BR`, `/en`, `/api/health`) and `db:contract-check` printed `PASS documentation-contract`; the container checker then failed at the same live Node index drift before reaching docs/DOX assertions, so `PASS container-documentation` and `PASS dox-contract` were not emitted and the `&&` tail was not reached. Run separately without changing the tree, `python3 scripts/pop_validate.py` printed `Vault válido — nenhuma violação encontrada.` and `git diff --check "$BASE"..HEAD` was silent/zero. |

### Round 2 cleanup and integrity

- Every completed clean-clone scenario removed its labeled containers, networks, volumes, local images, source secrets, staged secrets, and clean-clone directory. The interrupted lifecycle project also had empty container/network/volume/image inventory before retry.
- Final global `qrct` Docker inventory and `/tmp/qr-container-clone-*`/`qrct*` inventory were empty. `.env.compose` was absent.
- A tracked-file private-key/usable-URL/password-assignment filename scan returned no matches; the executable failure/isolation redaction scans passed.
- Final application worktree remained clean at `796b541f7da0d5096bb47fa13616624951816183`; base-range and worktree whitespace checks were clean.
- Host pnpm commands warned that host Node is 26.4.0 while the repository pin is 24.18.0; disposable images used the committed digest-pinned Node 24.18.0 base and their behavioral scenarios passed.

**Automated verdict:** rejected → return to `004_processing`. Criteria 1 and 8 require refreshing and independently verifying the exact Node tag's committed index/platform digest at a new committed HEAD. The post-range repair itself removed Round 1's symbolic-range rejection, but the unchanged exact criterion 8 cannot pass while live digest equality fails. Do not perform or check the preserved Human critical verification section above; keep `- [ ] Done` unchecked until a fresh automated 005 round passes all eight criteria.

## Round 3 — post-digest refresh — 2026-07-14

- **Fresh verifier profile:** requested `gpt-5.6-terra medium` was not selectable; this round ran in a third new available Codex agent, independent of planning, execution, and the first two verification rounds.
- **Committed HEAD:** `2152c5ce507f0462caf6149fb797ee7cddc1c5a4`
- **Base:** `8ab3f72b5449620c6716578ca6aa955978898e88` (`origin/develop` merge-base)
- **Initial state:** correct `task/1.1.3-containerize-self-hosted-runtime` branch and clean worktree.
- **Live equality at start and end:** Node index `sha256:39a4259b6f744868a8228742ad45aa3026f97302e5eec2fa4a38b30ca0a66e12`, linux/amd64 child `sha256:d45d78e7929b46875bbd4e29bea672d5bc48186c6c3588306521c815e78352d6`; PostgreSQL index `sha256:b403bafdebf3f7027c15ceefbfc46899f575096549f549650b3fb2ab9a6a6930`, linux/amd64 child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`. Both independent checks matched the committed pins.

| # | Exact run executed | Result | Evidence |
|---|--------------------|--------|----------|
| 1 | `pnpm container:contract-check && pnpm container:test --clean-clone --scenario build` | ✅ passed | Both `PASS tag-digest-equality` lines and `PASS image-contract`; bootstrap `1000:1000`/295676252 bytes, migrate `1000:1000`/295676250 bytes, app `1000:1000`/250882638 bytes. |
| 2 | `pnpm container:test --clean-clone --scenario config` | ✅ passed | `PASS source-secret-permissions`, `PASS staged-secret-permissions`, `PASS compose-config`. |
| 3 | `pnpm container:test --clean-clone --scenario happy` (executed twice) | ✅ passed twice | Both runs emitted `PASS clean-build`, `db-secret-consumed-once`, `postgres-server-nonroot`, `bootstrap`, `migration`, `runtime-db-preflight`, `app-liveness`, and `static-assets`. |
| 4 | `pnpm db:test && pnpm container:test --clean-clone --scenario roles` | ✅ passed | All eight database PASS lines plus `PASS compose-role-separation`. |
| 5 | `pnpm container:test --clean-clone --scenario failures` | ✅ passed | `PASS sql-escaping`, `PASS url-percent-encoding`, and real redacted `PASS auth-fail-closed` for admin, migrator, and runtime. |
| 6 | `pnpm container:test --clean-clone --scenario lifecycle` (executed twice) | ✅ passed twice | Both runs emitted `PASS migration-noop`, `app-restart-runtime-preflight`, `app-restart-no-job-rerun`, `persistent-restart`, and `cleanup`. |
| 7 | `pnpm container:test --clean-clone --scenario isolation` | ✅ passed | `PASS db-initialization-identity`, `db-secret-consumed-once`, `postgres-server-nonroot`, `secret-isolation`, `no-secret-logs`, and `port-isolation`. |
| 8 | `BASE=$(git merge-base HEAD origin/develop) && pnpm check && pnpm db:contract-check && pnpm container:contract-check --docs "$BASE..HEAD" && python3 scripts/pop_validate.py && git diff --check "$BASE"..HEAD` | ✅ passed | Four tests passed and one skipped; production routes `/pt-BR`, `/en`, and `/api/health` built; `PASS documentation-contract`, both digest lines, `PASS image-contract`, `PASS container-documentation`, `PASS dox-contract`, valid PoP vault, and silent whitespace check. |

### Round 3 cleanup and integrity

- Every clean-clone execution completed its `finally` cleanup. Final `qrct` container, network, volume, and image inventories were empty; `/tmp` had no `qr-container-clone-*` or `qrct*` remnant; `.env.compose` was absent.
- The final tracked-file scan found no private key, usable credential-bearing PostgreSQL URL, or literal password assignment. Failure/isolation redaction probes passed.
- The application worktree remained clean at `2152c5ce507f0462caf6149fb797ee7cddc1c5a4`; base-range and worktree whitespace checks were clean.
- Host commands warned that Node 26.4.0 differs from the repository pin; the digest-pinned Node 24.18.0 images were the clean-clone build/runtime under test and passed.

**Automated verdict:** all C1–C8 criteria passed. Because `critical: true`, remain in `005_verifying` and wait for the human gate; do not advance to 006. The human must execute every exact disposable command block in the preserved **Human critical verification** section above, in order, then mark its existing `- [ ] Done` checkbox. It remains intentionally unchecked.

## Round 4 — installer amendment — 2026-07-14

- **Verifier:** fresh Stage 005 verification distinct from the executor; a requested model profile was not selectable in this interface.
- **Committed HEAD:** `b4a21812b03c9edbbd3a0f8cca57c28276143c98`
- **Base:** `8ab3f72b5449620c6716578ca6aa955978898e88` (`origin/develop` merge-base)
- **Initial state:** correct task branch, five commits ahead of `origin/develop`, and clean worktree.
- **Reuse decision:** no Round 3 behavioral result was carried forward. The `2152c5c..HEAD` range changes both Docker/Compose base indices and the staging helper, so C1–C7 were rerun against the refreshed images; C8 and new C9 were also executed exactly.

| # | Exact run executed | Result | Evidence |
|---|--------------------|--------|----------|
| 1 | `pnpm container:contract-check && pnpm container:test --clean-clone --scenario build` | ✅ passed | Node live/committed index `sha256:6f7b03f7c2c8e2e784dcf9295400527b9b1270fd37b7e9a7285cf83b6951452d`, linux/amd64 child `sha256:d45d78e7929b46875bbd4e29bea672d5bc48186c6c3588306521c815e78352d6`; PostgreSQL index `sha256:7cfd7f5e307dbd8cf5d88761b22934c042a749362c0ab7efc41805572d58b3a3`, child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`; `PASS image-contract`. Images: bootstrap `1000:1000`/295676252 bytes, migrate `1000:1000`/295676250 bytes, app `1000:1000`/250882092 bytes. |
| 2 | `pnpm container:test --clean-clone --scenario config` | ✅ passed | `PASS source-secret-permissions`, `PASS staged-secret-permissions`, `PASS compose-config`. |
| 3 | `pnpm container:test --clean-clone --scenario happy` | ✅ passed | `PASS clean-build`, `db-secret-consumed-once`, `postgres-server-nonroot`, `bootstrap`, `migration`, `runtime-db-preflight`, `app-liveness`, and `static-assets`. |
| 4 | `pnpm db:test && pnpm container:test --clean-clone --scenario roles` | ✅ passed | All eight required database lines passed: `migrator-connect`, `migration-replay`, `migration-idempotence`, `foundation-schema`, `runtime-crud`, `constraint-sqlstates`, `role-separation`, `runtime-denials`; fresh container startup and `PASS compose-role-separation` also passed. |
| 5 | `pnpm container:test --clean-clone --scenario failures` | ✅ passed | `PASS sql-escaping`, `PASS url-percent-encoding`, and real redacted `PASS auth-fail-closed` for admin, migrator, and runtime, after all happy prerequisites. |
| 6 | `pnpm container:test --clean-clone --scenario lifecycle` | ✅ passed | `PASS migration-noop`, `app-restart-runtime-preflight`, `app-restart-no-job-rerun`, `persistent-restart`, and `cleanup`, after all happy prerequisites. |
| 7 | `pnpm container:test --clean-clone --scenario isolation` | ✅ passed | `PASS db-initialization-identity`, `db-secret-consumed-once`, `postgres-server-nonroot`, `secret-isolation`, `no-secret-logs`, and `port-isolation`, after all happy prerequisites. |
| 8 | `BASE=$(git merge-base HEAD origin/develop) && pnpm check && pnpm db:contract-check && pnpm container:contract-check --docs "$BASE..HEAD" && python3 scripts/pop_validate.py && git diff --check "$BASE"..HEAD` | ✅ passed | Typecheck, lint, 4 tests with 1 skipped, and production build passed with `/pt-BR`, `/en`, and `/api/health`; `PASS documentation-contract`, both live digest lines, `PASS image-contract`, `PASS container-documentation`, `PASS dox-contract`, valid PoP vault, and silent range whitespace check. |
| 9 | `bash -n install/*.sh && install/test.sh` | ✅ passed | Exact run printed `PASS install-contract`. Independent read confirmed an allowlisted `.env.install` parser accepts only the port and three secret-file paths; source files must be absolute, regular, non-symlink, invoking-user-owned, and mode 0600; staged copies become UID/GID 1000 and mode 0400. Debian and Ubuntu use Docker's official APT repository and the exact Engine/CLI/containerd/Buildx/Compose packages. Installation uses only Docker and standard host tools, never host Node/npm/pnpm. Default uninstall omits `--volumes` and package purge; `--purge-data` and `--remove-docker` are separate explicit flags. The contract test mutates only its `mktemp` directory and invokes installer/uninstaller exclusively with `--dry-run`, so it performs no package, repository, daemon, Compose, or host-filesystem mutation. |

### Round 4 cleanup and integrity

- Every fresh clean-clone run completed labeled cleanup. Final `qrct` container, network, volume, and image inventories were empty; `/tmp` contained no `qr-container-clone-*` or `qrct*` remnant.
- `.env.compose`, `.container-secrets`, and `install/.env.install` were absent. The tracked private-key/usable credential-bearing PostgreSQL URL scan returned no match; authentication/isolation redaction probes and the install plaintext-variable rejection passed.
- Final application worktree remained clean at `b4a21812b03c9edbbd3a0f8cca57c28276143c98`; worktree and base-range whitespace checks were silent/zero.
- Host pnpm commands warned that Node 26.4.0 differs from the repository pin. Clean-clone build/runtime used the exact digest-pinned Node 24.18.0 image and passed.

**Automated verdict:** all C1–C9 criteria passed. Because `critical: true`, remain in `005_verifying` at the human gate and do not advance to 006. The preserved **Human critical verification** command sequence above remains the required next action, and its sole `- [ ] Done` checkbox remains intentionally unchecked.
