# Group g01 — Images and contracts — [[1.1.3-containerize-self-hosted-runtime]]

- **Order:** 1.
- **Depends on:** none.
- **Description:** Pin bases and define narrow non-root standalone/app and database-operations images.

## Movements

- [x] (agent) Confirm the worktree base/dependencies and complete both RECON NEEDED checks; make the checker extract committed tag@index pairs, query each exact tag live, compare top-level index equality fail-closed, and separately record the matching platform manifest plus Engine/Compose/Buildx versions.
  - Expected: base includes `8ab3f72`; stable `PASS tag-digest-equality` lines name equal committed/live indices and distinct OS/architecture child digests for Node 24.18.0 and PostgreSQL 18.4.
  - If it fails: wrong base → stop for orchestration repair; missing tooling/tag/digest → abort; supported alternate architecture → use the plan fork and record it.
- [x] (agent) Set `output: "standalone"`; add strict `.dockerignore` and a digest-pinned multi-stage `Dockerfile` with frozen dependency/build stages, minimal `db-ops`, and UID/GID-1000 app target copying standalone, `public`, and `.next/static` with correct ownership.
  - Expected: no secret/build context noise enters; app command is direct `node server.js`; app and db-ops report nonzero UID; static files exist; exact pins remain unchanged.
  - If it fails: traced dependency missing → use only the authorized target-specific fork; arbitrary write/root need → abort.
- [x] (agent) Add non-shell bootstrap, migrate, runtime, and Node liveness wrappers. Bootstrap retains existing SQL plus tested escaping; role URLs use WHATWG setters; runtime opens runtime-role PostgreSQL, executes `SELECT 1`, closes, then starts server; all errors are redacted.
  - Expected: reserved-character SQL/URL probes pass; missing/empty/shared/bad secrets fail; bad runtime auth exits before bind; valid preflight prints only `PASS runtime-db-preflight`; subsequent localhost health never queries DB.
  - If it fails: safe escaping/redaction or role separation cannot be proven → abort rather than use shell interpolation or shared credentials.
- [x] (agent) Extend static contract checks and root/prisma DOX routing for Dockerfile, Compose, wrappers, immutable SQL ownership, and verification commands; create no child contract without a DOX trigger.
  - Expected: contract check detects floating images, root execution, secret-bearing build/env fields, missing assets, duplicated bootstrap SQL, wrong URLs, or stale routing.
  - If it fails: formatting-sensitive assertion → replace with semantic parsing; never weaken the substantive boundary.

## Execution notes

- 2026-07-14 — Planning used official current docs; no implementation occurred in 002.
- 2026-07-14 — RECON passed on base `8ab3f72`: Docker Engine 29.6.1, Compose 5.3.1, Buildx 0.35.0, BuildKit linux/amd64. Node index `sha256:cb4e8f7c443347358b7875e717c29e27bf9befc8f5a26cf18af3c3dec80e58c5` / amd64 child `sha256:0778d035a13f3f3833b7f2cb750e0df6cbce45583e84fd822f499f0c902a6c74`; PostgreSQL index `sha256:b403bafdebf3f7027c15ceefbfc46899f575096549f549650b3fb2ab9a6a6930` / amd64 child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`.
- 2026-07-14 — Final clean-clone image evidence: bootstrap 295678987 bytes, migrate 295678985 bytes, app 250887489 bytes; all report `1000:1000`.
- 2026-07-14 — Round-2 verification detected the plan-authorized live Node exact-tag drift. Commit `2152c5c` refreshed every committed `node:24.18.0-bookworm-slim` pin to index `sha256:39a4259b6f744868a8228742ad45aa3026f97302e5eec2fa4a38b30ca0a66e12`; linux/amd64 child `sha256:d45d78e7929b46875bbd4e29bea672d5bc48186c6c3588306521c815e78352d6`. PostgreSQL remained at index `sha256:b403bafdebf3f7027c15ceefbfc46899f575096549f549650b3fb2ab9a6a6930`; linux/amd64 child `sha256:16fa100a3a6e92c0556632870455e7f8c6f3df5cefddd67d6b95292732bd7ff0`. Exact C1 and a post-table live check matched without a second drift; refreshed images were bootstrap 295676252 bytes, migrate 295676250 bytes, app 250882638 bytes, all `1000:1000`.
