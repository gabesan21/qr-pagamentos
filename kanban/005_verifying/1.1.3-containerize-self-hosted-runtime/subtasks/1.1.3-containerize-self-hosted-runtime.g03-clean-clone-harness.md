# Group g03 — Clean-clone harness — [[1.1.3-containerize-self-hosted-runtime]]

- **Order:** 3.
- **Depends on:** g02.
- **Description:** Prove reproducible startup, role boundaries, failures, persistence, isolation, and cleanup without operator data.

## Movements

- [x] (agent) Add `container:test` that archives clean committed HEAD, creates invoking-user/0600 reserved-character sources, stages UID1000/0400 job/app copies, captures stdout/stderr, and registers unique-project plus staged-copy cleanup.
  - Expected: no untracked source; sources/staged files exercise identity and SQL/URL boundaries; parallel runs cannot collide; cleanup removes only its resources/files.
  - If it fails: dirty/uncommitted source → commit task changes first; cleanup can target non-test resources → abort and narrow project labels/names.
- [x] (agent) Implement `build`, `config`, `happy`, and `roles`: validate sources/staging/model, start chain, prove initial admin authentication and non-root server PIDs/no secret FDs, inspect jobs, runtime preflight before app bind, static/health, and 1.1.2 denials.
  - Expected: stable criteria 1–4 PASS lines; second migration no-op; app uses runtime only; health route source/response remains DB-independent.
  - If it fails: classify image, dependency, health, role, or asset fault; repair owning group then rerun from a fresh volume.
- [x] (agent) Implement isolated `failures` with valid reserved-character credentials, then wrong admin, migrator, and runtime credentials reaching real PostgreSQL authentication; runtime case invokes production preflight unchanged and never binds app.
  - Expected: each wrapper genuinely fails; runtime exit is nonzero with PostgreSQL auth evidence sanitized to stable code only; app container is absent/exited and raw/encoded values/full URLs are absent from stdout, stderr, logs, inspect.
  - If it fails: app starts or failure becomes ambiguous → abort; never corrupt migration history to manufacture the test.
- [x] (agent) Implement `lifecycle` and `isolation`: snapshot job IDs/exits/timestamps/event counts; restart/recreate app; require a new successful runtime `SELECT 1` before bind and unchanged job snapshots; inspect DB PIDs/FDs, history/config/outputs/logs/ports, then clean.
  - Expected: app returns healthy with new preflight evidence while both jobs remain byte-identical; DB server stays non-root and secret-independent; stable criteria 6–7 PASS lines; no value/listener/orphan remains.
  - If it fails: leakage/orphan/public listener → abort and preserve evidence; cleanup only the labeled test project.

## Execution notes

- 2026-07-14 — Human critical verification is deliberately deferred to 005; execution has no `(user)` item.
- 2026-07-14 — Final `f2e9656` clean-clone table passed build, config, happy twice, roles, failures, lifecycle twice, and isolation; every unique project removed its containers, networks, volumes, images, staged files, and sources.
