# Group g03 — Quality and DOX closeout — [[1.1.1-scaffold-next-platform]]

- **Order:** 3.
- **Depends on:** g01, g02.
- **Description:** Prove the clean baseline and make root documentation match the implemented structure and commands.

## Movements

- [x] (agent) Run frozen install, typecheck, lint, non-vacuous tests, build, production health/locale probes, and the aggregate check; fix only failures within the approved route.
  - Expected: every acceptance run exits 0 with its specified observable output and `.next/BUILD_ID` exists.
  - If it fails: known planned failure → apply its countermeasure and rerun the individual gate before aggregate check; unplanned or cross-scope failure → record and abort.
- [x] (agent) Update README with the real prerequisites, install/dev/quality/build/start commands, health contract, and explicit locale URLs; remove stale planning-only language.
  - Expected: a clean-clone contributor can run the baseline using only committed commands without inferring versions or paths.
  - If it fails: docs would require unimplemented database/Docker steps → keep those explicitly deferred to 1.1.2/1.1.3.
- [x] (agent) Review and update root `AGENTS.md` for the actual source layout, command contract, generated artifacts, DOX index, and changed-path ownership; add no child contract without an objective trigger.
  - Expected: every changed application path is understandable from the root contract plus any justified child; `CLAUDE.md` remains the root-contract symlink.
  - If it fails: root contract approaches its size/index limit or a subtree has multiple non-obvious rules → create one concise triggered child contract and link it with a one-line routing trigger.
- [x] (agent) Re-run all acceptance commands, `git diff --check`, inspect the complete task diff for secrets/unrelated harness changes, then mark all completed movements and record deviations.
  - Expected: all checks pass, no secret or unrelated path appears, and the card/subtask evidence exactly matches the worktree diff.
  - If it fails: unrelated or secret-bearing content appears → remove only task-created content, preserve user work, and abort if provenance is unclear.

## Execution notes

- 2026-07-13 — planned; final DOX review is mandatory even if no child contract is created.
- 2026-07-13 — frozen install, typecheck, ESLint, 4 Vitest assertions, production build, live health/locale probes, `pnpm check`, `git diff --check`, `.next/BUILD_ID`, symlink, and documentation acceptance all passed. A no-`.next` typecheck also passed.
- 2026-07-13 — README now documents exact prerequisites, every baseline command, explicit locale URLs, truthful liveness, and deferred database/migration readiness.
- 2026-07-13 — root `AGENTS.md` now owns `src/app`, `src/i18n`, command, pin, generated-path, and locale constraints. No child contract was created because no objective trigger exists yet.
- 2026-07-13 — full changed-path review found no credential-shaped content. Every inherited concurrent diff is intentionally retained under the human waiver; the only environment divergence is the executor host's Node 26.4.0 warning against the committed Node 24.18.0 pin.
