---
task: 12.4.1-remodel-administrator-dashboard
entry: 02-input-otp-cleanup
---

# Clear unhandled jsdom timer in input-otp test

Added `cleanup` to `src/components/ui/input-otp.test.tsx` to clear an unhandled jsdom timer that was failing the aggregate `pnpm check` gate. The change is test-local and does not alter application behavior.

## Evidência

- [[src/components/ui/input-otp.test.tsx|input-otp.test.tsx]] — unhandled timer cleaned up with `cleanup`.
