# Login page visual review

- Run: `20260909034243`
- Manifest SHA-256: `ce41cb4d252e2c7c57860a436523febea7f0e27ebfb2e6a73615387f0d2553f2`
- Grid: light/dark × 320/375/768/1440 on `/login`, pt-BR (persisted-locale default).
- Context: regenerated by `14.7.2` after `14.7.1` dropped the route-scoped
  auth BEM (`auth-page`, `auth-card__{tagline,caption,strip,swatch,
  form--tight,footer,panel-brand}`, `auth-password-field*`, `auth-forgot*`,
  `auth-totp-actions`, `auth-mode-toggle`, `login-page`, `login-form`) in
  favor of Tailwind utilities, and after `14.7.2` realigned
  `tests/login.evidence.spec.ts` to select the visible brand mark by
  `[data-slot="card-header"] [data-brand-identity]` instead of the
  `.auth-brand` class.
- Automated checks (focus order/visibility, contrast, axe serious/critical,
  overflow, brand mark size and contrast): none failing across all eight
  captures — `assertions.json` reports `brand.contrast` ≥ 4.5 and
  `markSize` ≥ 32 at every viewport, and `focusTraversal` reaches
  username/password/submit with a visible ring or ≥2px outline.
- Visual findings requiring correction: none. Compared against the prior
  baseline (`20260908212004`): the decorative leading panel (tagline,
  caption, six-theme swatch strip) and the visible form-column brand mark
  both render at parity — the panel/form/language split at the 900px
  breakpoint, the stacked `CardFooter` (submit button over the forgot-
  password note, fixed by the prior run), and the six-swatch strip are all
  unchanged in geometry, only the underlying CSS moved from BEM rules to
  projected utilities plus the four sanctioned `.auth-card*` exceptions.
  Spot-checked 320 (dark) and 1440 (light): no overlap, clipping, or
  hierarchy drift.
- Remaining findings: none (severity <2).
