# Brand asset contract

- `geometry.ts` is the only canonical mark geometry; never duplicate or redraw
  it in a page, component, generated file, or favicon routine.
- `brand-identity.tsx` owns the four closed inline identities: mark-only,
  product lockup, compact role-shell lockup, and merchant fallback.
- Inline identities inherit one semantic foreground; never branch geometry or
  component classes by theme.
- A mark beside visible identity text is decorative. A standalone meaningful
  mark receives one caller-supplied localized accessible name; never add an SVG
  `title` that creates a duplicate announcement.
- `assets.manifest.json`, `public/brand/**`, and `src/app/favicon.ico` are
  generated by `pnpm brand:generate`; never edit their bytes directly.
- `pnpm brand:check` must fail on byte, hash, dimension, inventory, SVG-safety,
  provenance, or favicon-frame drift.
- Static positive/reversed exports are for contexts that cannot inherit
  semantic UI color. Never use them to create per-theme UI forks.
- Merchant fallback identifies QR Pagamentos only when no merchant image
  capability exists; never imply merchant ownership or add upload/media work
  in this subtree.

## Related contracts

- [`../../DESIGN.md`](../../DESIGN.md) — follow before changing identity
  composition, size, clear space, color, or accessibility usage.
- [`../../pop/specs/administrative-design-system.md`](../../pop/specs/administrative-design-system.md)
  — follow when changing the durable family or consumer boundary.
